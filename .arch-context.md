# Phase 81 — Architecture Context Inventory

**Phase:** Inbound Correspondence & the First Gated MCP Write-Back (Email → Kazi)
**Generated:** 2026-06-21
**Net-new:** `Correspondence` entity + MCP write tools (`file_correspondence`, `attach_document`, `propose_task`, `resolve_matter_by_email`) + exposing `AiExecutionGate` creation over MCP.
**Reuses:** Phase 78 MCP pipeline, `AiExecutionGate`, `DocumentService`, `CustomerRepository.findByEmail`, `TaskService.createTask`.
**Repo root:** `/Users/rakheendama/Projects/2026/b2b-strawman`

---

## 1. ARCHITECTURE.md Section Numbering

`architecture/ARCHITECTURE.md` top-level (`^## `) headers:

| # | Header |
|---|--------|
| 1 | Overview |
| 2 | Architecture Decision Records |
| 3 | Component Architecture |
| 4 | Sequence Diagrams |
| 5 | Data Flow Diagrams |
| 6 | Security Architecture |
| 7 | Infrastructure Architecture |
| 8 | Database Architecture |
| 9 | Phase 2 — Billing & Tiered Tenancy |
| 10 | Phase 4 — Customers, Document Scopes & Tasks |
| (unnumbered) | Appendix A: Clerk JWT Claims |
| (unnumbered) | Appendix B: Environment Variables Reference |

> NOTE: Numbered top-level sections stop at **10**. Phases 5–80 are NOT folded into ARCHITECTURE.md as new top-level numbered sections — they live as standalone `architecture/phase*-*.md` docs. **Highest numbered section = 10. Next available numbered section = `## 11`** (would land before the appendices). Follow the established pattern: Phase 81's architecture section is normally a standalone phase doc, but if appending here, use `## 11. Phase 81 — Inbound Correspondence & Gated MCP Write-Back`.

---

## 2. ADR Numbering

`adr/` highest numeric ADR = **ADR-318** (`ADR-318-pipeline-metrics.md`). (Plus a separate `ADR-T001..T008` template-track series.)

Recent run: ADR-300..306 (MCP), 307..312 (sessions/branding), 313..318 (CRM/pipeline).

**ADR-319, 320, 321, 322, 323 are FREE** — confirmed. Use these for Phase 81.

---

## 3. Migration Numbering

- `db/migration/global/` highest = **V25** (`V25__create_shard_config.sql`). Next free = **V26**.
- `db/migration/tenant/` highest = **V129** (`V129__create_mcp_egress_consents.sql`). Next free = **V130**.

Confirmed: expected tenant V129 matches; **next tenant migration = V130**.
Phase 81 will need a tenant migration for the `correspondence` table (+ any `documents.correspondence_id` column + a new-capability seed) starting at V130.

Relevant recent tenant migrations: V122 `ai_foundation`, V123 `ai_execution_gate_version`, V127/V128 `compliance_audit_*`, V129 `mcp_egress_consents`.

---

## 4. MCP Server Module (CRUX)

Location: `backend/src/main/java/io/b2mash/b2b/b2bstrawman/mcp/`

### 4.1 Module map
```
mcp/
  McpServerConfig.java          # @Configuration package anchor; server built by Spring AI autoconfig from spring.ai.mcp.server.* props
  McpEnablementService.java     # effectiveState() = integration enabled AND POPIA consent granted
  McpEnablementController.java
  McpCapabilityGuard.java       # gatedTool()/gatedResource() — inline capability gate (NOT @RequiresCapability)
  McpToolAudit.java             # emitInvoked()/emitDenied() — THE common audit+metrics path (no central dispatch exists)
  McpToolErrors.java            # asResult(McpError) -> CallToolResult
  McpAuditMetadata.java         # sanitised details builder (rowCount, entityRefs, params)
  McpMetrics.java               # recordOk()/recordDenied() per-tenant metrics
  McpPagination.java            # paginate(), DEFAULT_MAX_SIZE=50, responseTooLarge ceiling
  McpSessionAuditListener.java  # emits mcp.session.opened
  consent/  McpConsentService, McpEgressConsent, McpEgressConsentRepository
  dto/      McpError, McpPage, McpClientDto, McpMatterDto, McpInvoiceDto, McpTrustBalanceDto, ... (all read DTOs)
  resource/ ClientResource, FirmProfileResource, MatterResource (@McpResource)
  tool/     ActivityTools, AuditTools, BillingTools, ClientTools, ComplianceTools, DocumentTools,
            MatterTools, TrustTools, McpPingTool, McpToolRegistry
```

### 4.2 Tool registration / discovery pattern
- Tools are **method-level `@McpTool`** (param `name`, `description`) on `@Component` beans, with `@McpToolParam` on each argument. Resources use `@McpResource`.
- **No manual registration.** Spring AI 2.0.0-M6 autoconfiguration scans `@McpTool`/`@McpResource` methods. `McpServerConfig` declares NO beans — it intentionally does not fight autoconfig.
- `McpToolRegistry` is a **read-only observational seam only** (wraps the SDK's `ServerMcpAnnotatedBeans` discovery bean: `getBeansByAnnotation(McpTool.class)`). It does NOT register or gate; used by read-only-assertion tests.
- **CRITICAL CONSTRAINT for write tools:** Spring AI 2.0.0-M6 exposes **no `tools/call` interceptor and no central dispatch**. Therefore every cross-cutting concern (enablement gate, capability gate, audit) is an **inline guard at the top of each tool method**. A new write tool MUST replicate this inline pattern; there is no central place to add it.

To add a write tool: create a new `@Component` (e.g. `CorrespondenceWriteTools`) with `@McpTool`-annotated methods, each beginning with the enablement check, then `McpCapabilityGuard.gatedTool("MCP_WRITE", ...)`, then audit via `McpToolAudit`.

### 4.3 Tenant / member / capability resolution per call
MCP requests ride the **standard authenticated Spring Security filter chain** (servlet, `type=SYNC`, NOT WebFlux), so by the time a tool runs, `RequestScopes` ScopedValues are bound:
`TENANT_ID` → `ORG_ID` → `MEMBER_ID` → `ORG_ROLE` → `CAPABILITIES`.
Inside tools: `ActorContext.fromRequestScopes()`, `RequestScopes.requireMemberId()`, `RequestScopes.hasCapability(cap)`.

### 4.4 Enablement + POPIA egress consent gate
`McpEnablementService.effectiveState()` (re-evaluated every call, no caching):
```java
return integrationEnabled && consentService.isCurrentlyGranted();
// integrationEnabled = OrgIntegration(IntegrationDomain.MCP).isEnabled()
// consent            = latest McpEgressConsent row action == GRANTED
```
Every `@McpTool`/`@McpResource` calls `effectiveState()` as its FIRST statement and returns `McpError.notEnabled()` when false. Consent is append-only (`grant`/`revoke` insert rows); `McpConsentService` reads `findTopByOrderByConsentedAtDescCreatedAtDesc()`. Consent version default = `popia-egress-v1`.

### 4.5 Auth (ADR-303) → tenant/member resolution
MCP server = OAuth 2.1 protected resource reusing the existing Keycloak JWT pipeline (mandate: "reuse auth, do not fork it"). `/.well-known/oauth-protected-resource` advertises Keycloak as the auth server. `/mcp` is on the authenticated path (NOT excluded by `MemberFilter.shouldNotFilter`, unlike `/internal/*`). Chain:
1. `BearerTokenAuthenticationFilter` + `ClerkJwtAuthenticationConverter` — validate token, extract identity.
2. `TenantFilter` — `o.id` claim → `org_schema_mapping` → binds `TENANT_ID`/`ORG_ID` (sets Hibernate search_path).
3. `MemberFilter` — JWT subject → `Member` → `OrgRole` → binds `MEMBER_ID`/`ORG_ROLE` + `OrgRoleService.resolveCapabilities()` → `CAPABILITIES`.
So an MCP caller's scope == their web scope automatically; audit attributes to the real member.

### 4.6 Read audit family
Reads emit via `McpToolAudit`: event type **`mcp.tool.invoked`** (success) / **`mcp.access.denied`** (refusal), `entityType="mcp_tool"`, `entityId = current member id`, details from `McpAuditMetadata` (rowCount, entityRefs, params, deniedGate). Session: `mcp.session.opened`. Audit/metrics emission is wrapped so it can never break a tool call.

### 4.7 Representative read tool (FULL SOURCE — pattern template)
`mcp/tool/ClientTools.java`:
```java
package io.b2mash.b2b.b2bstrawman.mcp.tool;

import io.b2mash.b2b.b2bstrawman.audit.AuditService;
import io.b2mash.b2b.b2bstrawman.customer.CustomerProjectService;
import io.b2mash.b2b.b2bstrawman.customer.CustomerService;
import io.b2mash.b2b.b2bstrawman.customer.LifecycleStatus;
import io.b2mash.b2b.b2bstrawman.exception.InvalidStateException;
import io.b2mash.b2b.b2bstrawman.exception.ResourceNotFoundException;
import io.b2mash.b2b.b2bstrawman.mcp.McpAuditMetadata;
import io.b2mash.b2b.b2bstrawman.mcp.McpEnablementService;
import io.b2mash.b2b.b2bstrawman.mcp.McpMetrics;
import io.b2mash.b2b.b2bstrawman.mcp.McpPagination;
import io.b2mash.b2b.b2bstrawman.mcp.McpToolAudit;
import io.b2mash.b2b.b2bstrawman.mcp.McpToolErrors;
import io.b2mash.b2b.b2bstrawman.mcp.dto.McpClientDto;
import io.b2mash.b2b.b2bstrawman.mcp.dto.McpClientListItem;
import io.b2mash.b2b.b2bstrawman.mcp.dto.McpError;
import io.b2mash.b2b.b2bstrawman.mcp.dto.McpPage;
import io.b2mash.b2b.b2bstrawman.multitenancy.ActorContext;
import java.util.Locale;
import java.util.UUID;
import org.springframework.ai.mcp.annotation.McpTool;
import org.springframework.ai.mcp.annotation.McpToolParam;
import org.springframework.stereotype.Component;
import tools.jackson.databind.ObjectMapper;

@Component
public class ClientTools {

  private final CustomerService customerService;
  private final CustomerProjectService customerProjectService;
  private final AuditService auditService;
  private final ObjectMapper objectMapper;
  private final McpEnablementService enablement;
  private final McpMetrics metrics;

  public ClientTools(
      CustomerService customerService,
      CustomerProjectService customerProjectService,
      AuditService auditService,
      ObjectMapper objectMapper,
      McpEnablementService enablement,
      McpMetrics metrics) {
    this.customerService = customerService;
    this.customerProjectService = customerProjectService;
    this.auditService = auditService;
    this.objectMapper = objectMapper;
    this.enablement = enablement;
    this.metrics = metrics;
  }

  @McpTool(
      name = "list_clients",
      description =
          "List the firm's clients (customers), org-wide. Optionally filter by lifecycleStatus"
              + " (PROSPECT, ONBOARDING, ACTIVE, DORMANT, OFFBOARDING, OFFBOARDED). Paginated —"
              + " page size is capped at 50.")
  public Object listClients(
      @McpToolParam(required = false, description = "Zero-based page index (default 0).")
          Integer page,
      @McpToolParam(required = false, description = "Page size, capped at 50 (default 50).")
          Integer size,
      @McpToolParam(
              required = false,
              description = "Filter by lifecycle status short enum name, e.g. ACTIVE.")
          String lifecycleStatus) {
    if (!enablement.effectiveState()) {
      return McpToolErrors.asResult(McpError.notEnabled(), objectMapper);
    }
    long startNanos = System.nanoTime();
    LifecycleStatus parsed;
    try {
      parsed =
          lifecycleStatus == null
              ? null
              : LifecycleStatus.valueOf(lifecycleStatus.trim().toUpperCase(Locale.ROOT));
    } catch (IllegalArgumentException e) {
      return McpToolErrors.asResult(
          McpError.invalidRequest("Unknown lifecycleStatus. See the tool description for values."),
          objectMapper);
    }

    var customers =
        (parsed == null
                ? customerService.listCustomers()
                : customerService.listCustomersByLifecycleStatus(parsed))
            .stream().map(McpClientListItem::from).toList();
    if (McpPagination.exceedsResponseCeiling(customers.size())) {
      return McpToolErrors.asResult(McpError.responseTooLarge(), objectMapper);
    }
    McpPage<McpClientListItem> pageResult =
        McpPagination.paginate(customers, page, size, McpPagination.DEFAULT_MAX_SIZE);
    var meta =
        McpAuditMetadata.builder()
            .rowCount(pageResult.items().size())
            .entityRefs(pageResult.items().stream().map(McpClientListItem::id).toList())
            .param("lifecycleStatus", parsed == null ? null : parsed.name())
            .build();
    emitInvoked("list_clients", meta, startNanos);
    return pageResult;
  }

  @McpTool(
      name = "get_client",
      description =
          "Fetch one client (customer) by id, including its contact details and the matters linked"
              + " to it. Returns a non-leaking not-found error if the client does not exist.")
  public Object getClient(@McpToolParam(description = "Client (customer) id.") UUID id) {
    if (!enablement.effectiveState()) {
      return McpToolErrors.asResult(McpError.notEnabled(), objectMapper);
    }
    long startNanos = System.nanoTime();
    var actor = ActorContext.fromRequestScopes();
    try {
      var customer = customerService.getCustomer(id);
      var linkedProjects = customerProjectService.listProjectsForCustomer(id, actor);
      var dto = McpClientDto.from(customer, linkedProjects);
      var meta = McpAuditMetadata.builder().rowCount(1).entityRef(id).build();
      emitInvoked("get_client", meta, startNanos);
      return dto;
    } catch (ResourceNotFoundException | InvalidStateException e) {
      return McpToolErrors.asResult(McpError.notFound("client"), objectMapper);
    }
  }

  private void emitInvoked(String tool, McpAuditMetadata meta, long startNanos) {
    McpToolAudit.emitInvoked(tool, meta, auditService, metrics, McpToolAudit.elapsed(startNanos));
  }
}
```

> Note: `ClientTools` is org-wide and (pre-565B) does NOT call `McpCapabilityGuard`. `TrustTools` DOES — it wraps the body in `McpCapabilityGuard.gatedTool("VIEW_TRUST", ...)`. Write tools should follow the `TrustTools` capability-gated shape with a new `MCP_WRITE` capability (see §9).

### 4.8 Registry / discovery seam (FULL SOURCE)
`mcp/tool/McpToolRegistry.java`:
```java
@Component
public class McpToolRegistry {
  private final ServerMcpAnnotatedBeans annotatedBeans;
  public McpToolRegistry(ServerMcpAnnotatedBeans annotatedBeans) { this.annotatedBeans = annotatedBeans; }
  public List<Object> registeredToolBeans() { return annotatedBeans.getBeansByAnnotation(McpTool.class); }
  public int toolBeanCount() { return registeredToolBeans().size(); }
  public List<Object> registeredResourceBeans() { return annotatedBeans.getBeansByAnnotation(McpResource.class); }
}
```
(Imports: `org.springframework.ai.mcp.annotation.McpTool`/`McpResource`, `...autoconfigure.annotations.McpServerAnnotationScannerAutoConfiguration.ServerMcpAnnotatedBeans`.)

---

## 5. AiExecutionGate (CRUX)

Location: `backend/src/main/java/io/b2mash/b2b/b2bstrawman/integration/ai/gate/`
Files: `AiExecutionGate`, `AiExecutionGateRepository`, `AiExecutionGateService`, `AiExecutionGateController`, `GateAction` (sealed), `GateActionExecutor`, `AiGateApprovedEvent`/`RejectedEvent`/`ExpiredEvent`, `AiGateExpiryHandler`.

### 5.1 Entity (`ai_execution_gates`)
Fields: `id` (UUID), `execution` (`@ManyToOne` LAZY → `AiExecution`, `execution_id` NOT NULL), `gateType` (String, len 40 — the **discriminator**), `status` (String, len 20: `PENDING`→`APPROVED`/`REJECTED`/`EXPIRED`), `proposedAction` (`@JdbcTypeCode(JSON)` `Map<String,Object>` jsonb NOT NULL), `aiReasoning` (TEXT NOT NULL), `reviewedBy` (UUID), `reviewedAt` (Instant), `reviewNotes` (TEXT), `expiresAt` (Instant NOT NULL), `version` (`@Version` int), `createdAt`.
State methods: `requirePendingStatus()` (checks PENDING + not expired), `approve(reviewerId, notes)`, `reject(reviewerId, notes)`, `expire()`.

### 5.2 AiExecutionGateService
- `approve(gateId, reviewerId, notes)` — load → `gate.approve()` → save → **`gateActionExecutor.execute(gate)`** → publish `AiGateApprovedEvent` → audit `ai.gate.approved` (entityType `ai_execution_gate`). `@Transactional`.
- `reject(...)` — `gate.reject()` → save → publish `AiGateRejectedEvent` → audit `ai.gate.rejected`. (No executor call.)
- `expireStaleGates()` — `@Scheduled(fixedRate=3600000)` + `@SchedulerLock`; per-tenant via `TenantScopedRunner` / `JobEnqueuer` fan-out; `expireStaleGatesForTenant()` → `findPendingExpiredBefore(now)` → `gate.expire()` → audit `ai.gate.expired` (actorType `SYSTEM`, source `SCHEDULER`) → publish `AiGateExpiredEvent`.
- `listGates(status, gateType, pageable)`, `getGate(gateId)`.
- **NO public gate-creation method exists** — only approve/reject/expire/list/get. Gate creation today happens inside AI skill execution. Phase 81 must add a creation method (see §5.7).

### 5.3 AiExecutionGateController (`/api/ai/gates`)
All endpoints `@PreAuthorize("isAuthenticated()")` + **`@RequiresCapability("AI_REVIEW")`**:
- `GET /` listGates (filters status, gateType) → `Page<GateListResponse>`
- `GET /{id}` getGate → `GateDetailResponse`
- `POST /{id}/approve` (body `GateReviewRequest{notes}`, reviewer = `RequestScopes.requireMemberId()`)
- `POST /{id}/reject`

### 5.4 GateActionExecutor dispatch pattern (how an approved gate runs its action)
`GateAction` is a **sealed interface**; the `gate_type` String column discriminates. `parseAction(gateType, proposedAction Map)` does a `switch (gateType)` over string constants, deserialising the JSONB map into a typed record. `execute(gate)` then does a `switch` over the sealed `GateAction` subtypes (exhaustive pattern match) and dispatches to a private `executeXxx(...)` helper that calls the relevant domain service with `gate.getReviewedBy()` and `gate.getExecution().getId()`.

**Existing action types** (`gate_type` string → `GateAction` record → service called):
| gate_type | record | dispatch target |
|-----------|--------|-----------------|
| `MARK_KYC_COMPLETE` | `MarkKycCompleteAction(checklistItemIds, completionNotes)` | `ChecklistInstanceService.completeItem` |
| `SELECT_MATTER_TEMPLATE` | `SelectMatterTemplateAction(templateId, customisationNotes)` | no-op (frontend pre-fill) |
| `CONFIRM_CONFLICT_SCREEN` | `ClearConflictAction(conflictCheckId, clearanceNotes)` | `ConflictCheckService.resolve` |
| `CREATE_REVIEW_REPORT` | `CreateReviewReportAction(projectId, documentId, reviewOutput)` | `AiReviewReportGenerator.generateReviewReport` |
| `CREATE_DRAFT_DOCUMENT` | `CreateDraftDocumentAction(templateId, projectId, draftOutput)` | `AiDraftDocumentGenerator.generateDraft` |
| `PUBLISH_COMPLIANCE_REPORT` | `PublishComplianceReportAction(auditOutput)` | `ComplianceAuditReportService.publishReport` |

**To add "create task from correspondence":** (1) add a new permitted record to `GateAction` (e.g. `CreateTaskFromCorrespondenceAction(correspondenceId, projectId, title, dueDate, ...)`); (2) add a `case "CREATE_TASK_FROM_CORRESPONDENCE"` arm to `parseAction`; (3) add a `case GateAction.CreateTaskFromCorrespondenceAction a -> executeCreateTask(a, reviewerId)` arm to `execute`; (4) the helper calls `TaskService.createTask(...)`. The `switch` over the sealed interface is **exhaustive**, so the compiler enforces adding the new arm.

### 5.5 GateActionExecutor (FULL SOURCE)
```java
package io.b2mash.b2b.b2bstrawman.integration.ai.gate;

import io.b2mash.b2b.b2bstrawman.checklist.ChecklistInstanceService;
import io.b2mash.b2b.b2bstrawman.compliance.ComplianceAuditReportService;
import io.b2mash.b2b.b2bstrawman.integration.ai.skill.complianceaudit.ComplianceAuditOutput;
import io.b2mash.b2b.b2bstrawman.integration.ai.skill.contractreview.AiReviewReportGenerator;
import io.b2mash.b2b.b2bstrawman.integration.ai.skill.contractreview.ContractReviewOutput;
import io.b2mash.b2b.b2bstrawman.integration.ai.skill.drafting.AiDraftDocumentGenerator;
import io.b2mash.b2b.b2bstrawman.integration.ai.skill.drafting.DraftingOutput;
import io.b2mash.b2b.b2bstrawman.verticals.legal.conflictcheck.ConflictCheckService;
import io.b2mash.b2b.b2bstrawman.verticals.legal.conflictcheck.ConflictCheckService.ResolveRequest;
import java.util.List;
import java.util.Map;
import java.util.UUID;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;
import tools.jackson.databind.ObjectMapper;

@Service
public class GateActionExecutor {

  private static final Logger log = LoggerFactory.getLogger(GateActionExecutor.class);

  private final ChecklistInstanceService checklistInstanceService;
  private final ConflictCheckService conflictCheckService;
  private final AiReviewReportGenerator aiReviewReportGenerator;
  private final AiDraftDocumentGenerator aiDraftDocumentGenerator;
  private final ComplianceAuditReportService complianceAuditReportService;
  private final ObjectMapper objectMapper;

  public GateActionExecutor(
      ChecklistInstanceService checklistInstanceService,
      ConflictCheckService conflictCheckService,
      AiReviewReportGenerator aiReviewReportGenerator,
      AiDraftDocumentGenerator aiDraftDocumentGenerator,
      ComplianceAuditReportService complianceAuditReportService,
      ObjectMapper objectMapper) {
    this.checklistInstanceService = checklistInstanceService;
    this.conflictCheckService = conflictCheckService;
    this.aiReviewReportGenerator = aiReviewReportGenerator;
    this.aiDraftDocumentGenerator = aiDraftDocumentGenerator;
    this.complianceAuditReportService = complianceAuditReportService;
    this.objectMapper = objectMapper;
  }

  @Transactional
  public void execute(AiExecutionGate gate) {
    GateAction action = parseAction(gate.getGateType(), gate.getProposedAction());
    UUID reviewerId = gate.getReviewedBy();

    switch (action) {
      case GateAction.MarkKycCompleteAction a -> executeMarkKycComplete(a, reviewerId);
      case GateAction.SelectMatterTemplateAction a -> {
        log.info("Gate {} approved: template selection (frontend pre-fill)", gate.getId());
      }
      case GateAction.ClearConflictAction a -> executeClearConflict(a, reviewerId);
      case GateAction.CreateReviewReportAction a ->
          executeCreateReviewReport(a, gate.getExecution().getId(), reviewerId);
      case GateAction.CreateDraftDocumentAction a ->
          executeCreateDraftDocument(a, gate.getExecution().getId(), reviewerId);
      case GateAction.PublishComplianceReportAction a ->
          executePublishComplianceReport(a, gate.getExecution().getId(), reviewerId);
    }
  }

  private void executeMarkKycComplete(GateAction.MarkKycCompleteAction action, UUID reviewerId) {
    for (UUID itemId : action.checklistItemIds()) {
      checklistInstanceService.completeItem(itemId, action.completionNotes(), null, reviewerId);
    }
  }

  private void executeClearConflict(GateAction.ClearConflictAction action, UUID reviewerId) {
    var request = new ResolveRequest("CLEARED", action.clearanceNotes(), null);
    conflictCheckService.resolve(action.conflictCheckId(), request, reviewerId);
  }

  private void executeCreateReviewReport(
      GateAction.CreateReviewReportAction action, UUID executionId, UUID reviewerId) {
    ContractReviewOutput output =
        objectMapper.convertValue(action.reviewOutput(), ContractReviewOutput.class);
    aiReviewReportGenerator.generateReviewReport(output, action.projectId(), executionId, reviewerId);
  }

  private void executeCreateDraftDocument(
      GateAction.CreateDraftDocumentAction action, UUID executionId, UUID reviewerId) {
    DraftingOutput output = objectMapper.convertValue(action.draftOutput(), DraftingOutput.class);
    aiDraftDocumentGenerator.generateDraft(
        output, action.templateId(), action.projectId(), executionId, reviewerId);
  }

  private void executePublishComplianceReport(
      GateAction.PublishComplianceReportAction action, UUID executionId, UUID reviewerId) {
    ComplianceAuditOutput output =
        objectMapper.convertValue(action.auditOutput(), ComplianceAuditOutput.class);
    complianceAuditReportService.publishReport(output, executionId, reviewerId);
  }

  @SuppressWarnings("unchecked")
  private GateAction parseAction(String gateType, Map<String, Object> proposedAction) {
    try {
      return switch (gateType) {
        case "MARK_KYC_COMPLETE" -> {
          List<String> itemIdStrings = (List<String>) proposedAction.get("checklist_item_ids");
          List<UUID> itemIds = itemIdStrings.stream().map(UUID::fromString).toList();
          String notes = (String) proposedAction.get("completion_notes");
          yield new GateAction.MarkKycCompleteAction(itemIds, notes);
        }
        case "SELECT_MATTER_TEMPLATE" -> {
          UUID templateId = UUID.fromString((String) proposedAction.get("template_id"));
          String notes = (String) proposedAction.get("customisation_notes");
          yield new GateAction.SelectMatterTemplateAction(templateId, notes);
        }
        case "CONFIRM_CONFLICT_SCREEN" -> {
          UUID conflictCheckId = UUID.fromString((String) proposedAction.get("conflict_check_id"));
          String notes = (String) proposedAction.get("clearance_notes");
          yield new GateAction.ClearConflictAction(conflictCheckId, notes);
        }
        case "CREATE_REVIEW_REPORT" -> {
          UUID projectId = UUID.fromString((String) proposedAction.get("project_id"));
          UUID documentId = UUID.fromString((String) proposedAction.get("document_id"));
          Map<String, Object> reviewOutput = (Map<String, Object>) proposedAction.get("review_output");
          yield new GateAction.CreateReviewReportAction(projectId, documentId, reviewOutput);
        }
        case "CREATE_DRAFT_DOCUMENT" -> {
          UUID draftTemplateId = UUID.fromString((String) proposedAction.get("template_id"));
          UUID projectId = UUID.fromString((String) proposedAction.get("project_id"));
          Map<String, Object> draftOutput = (Map<String, Object>) proposedAction.get("draft_output");
          yield new GateAction.CreateDraftDocumentAction(draftTemplateId, projectId, draftOutput);
        }
        case "PUBLISH_COMPLIANCE_REPORT" -> {
          Map<String, Object> auditOutput = (Map<String, Object>) proposedAction.get("audit_output");
          yield new GateAction.PublishComplianceReportAction(auditOutput);
        }
        default -> throw new IllegalArgumentException("Unknown gate type: " + gateType);
      };
    } catch (NullPointerException | ClassCastException | IllegalArgumentException e) {
      if (e instanceof IllegalArgumentException && e.getMessage().startsWith("Unknown gate type")) {
        throw e;
      }
      throw new IllegalStateException(
          "Invalid gate action data for type " + gateType + ": " + e.getMessage(), e);
    }
  }
}
```

### 5.6 GateAction sealed interface (FULL SOURCE)
```java
public sealed interface GateAction
    permits GateAction.MarkKycCompleteAction,
        GateAction.SelectMatterTemplateAction,
        GateAction.ClearConflictAction,
        GateAction.CreateReviewReportAction,
        GateAction.CreateDraftDocumentAction,
        GateAction.PublishComplianceReportAction {

  record MarkKycCompleteAction(List<UUID> checklistItemIds, String completionNotes) implements GateAction {}
  record SelectMatterTemplateAction(UUID templateId, String customisationNotes) implements GateAction {}
  record ClearConflictAction(UUID conflictCheckId, String clearanceNotes) implements GateAction {}
  record CreateReviewReportAction(UUID projectId, UUID documentId, Map<String, Object> reviewOutput) implements GateAction {}
  record CreateDraftDocumentAction(UUID templateId, UUID projectId, Map<String, Object> draftOutput) implements GateAction {}
  record PublishComplianceReportAction(Map<String, Object> auditOutput) implements GateAction {}
}
```

### 5.7 Phase-81 implication for MCP-created gates
Gate creation today is internal to AI skill execution (each gate ties to an `AiExecution` via `execution_id NOT NULL`). Exposing gate creation over MCP (`propose_task`) requires:
- A new **public creation method** on `AiExecutionGateService` (none exists today) that builds an `AiExecutionGate(execution, gateType, proposedAction, aiReasoning, expiresAt)`.
- The `execution_id NOT NULL` FK is a design constraint: either create a lightweight `AiExecution` representing the MCP write, OR relax/extend the FK (architectural decision — flag in an ADR). The richer reuse is to model the MCP correspondence write as an `AiExecution`.
- A new `gate_type` + `GateAction` arm + executor dispatch (§5.4).
- The approval surface already exists (`/api/ai/gates` + `AI_REVIEW` capability), so the human-review/approve loop is reusable as-is.

---

## 6. DocumentService

Location: `backend/src/main/java/io/b2mash/b2b/b2bstrawman/document/`

### 6.1 `Document` entity (`documents` table)
Fields: `id` (UUID), `projectId` (`project_id`, nullable FK), `fileName` (NOT NULL len 500), `contentType` (len 100), `size` (long), `s3Key` (NOT NULL len 1000), `status` (`Status`: `PENDING`/`UPLOADED`/`FAILED`), `uploadedBy` (UUID NOT NULL), `uploadedAt` (Instant), `scope` (String len 20: **`PROJECT`/`ORG`/`CUSTOMER`**), `customerId` (`customer_id`, nullable FK), `visibility` (String len 20: **`INTERNAL`/`SHARED`/`PORTAL`**), `source` (String len 30 default **`MANUAL`**; values `MANUAL`/`AI_GENERATED`/`TEMPLATE_GENERATED`), `aiExecutionId` (`ai_execution_id` nullable — link used only when source=AI_GENERATED), `createdAt` (immutable).
Scope/Source/Visibility are nested constant classes (stored as String, not JPA enums). **No multitenancy annotations** (schema-per-tenant via search_path).

### 6.2 Upload methods (signatures)
Thin entry points (resolve org/member from RequestScopes):
```java
UploadInitResult initiateUpload(UUID projectId, String fileName, String contentType, long size, ActorContext actor);
UploadInitResult initiateOrgUpload(String fileName, String contentType, long size);
UploadInitResult initiateCustomerUpload(UUID customerId, String fileName, String contentType, long size);
```
Full overloads (explicit orgId/memberId):
```java
UploadInitResult initiateUpload(UUID projectId, String fileName, String contentType, long size, String orgId, ActorContext actor);
UploadInitResult initiateOrgUpload(String fileName, String contentType, long size, String orgId, UUID memberId);
UploadInitResult initiateCustomerUpload(UUID customerId, String fileName, String contentType, long size, String orgId, UUID memberId);
```
Confirm:
```java
Document confirmUpload(UUID documentId, ActorContext actor);
DocumentResponse confirmUploadResponse(UUID documentId, ActorContext actor);
```
Return: `record UploadInitResult(UUID documentId, String presignedUrl, long expiresInSeconds)`.

> NOTE: there is no method literally named `initiateProjectUpload` — the project-scope method is `initiateUpload(UUID projectId, ...)`.

### 6.3 S3 key structure (`S3PresignedUrlService`)
- PROJECT: `S3PresignedUrlService.buildKey(orgId, projectId, documentId)` → `org/{orgId}/project/{projectId}/{documentId}`
- ORG: `buildOrgKey(orgId, documentId)` → `org/{orgId}/org-docs/{documentId}`
- CUSTOMER: `buildCustomerKey(orgId, customerId, documentId)` → `org/{orgId}/customer/{customerId}/{documentId}`

### 6.4 Attaching a `correspondenceId`
There is **no generic source-reference field** today (only `source` enum + `aiExecutionId`). Two options for `attach_document` linking a Document to a Correspondence:
1. Add nullable `correspondence_id` UUID column to `documents` (mirrors `ai_execution_id`) + setter; optionally extend `source` with `EMAIL_INGEST` — **simplest, recommended**.
2. Generalise to `source_reference_id` + `source_reference_type` — more flexible, more churn. (YAGNI: prefer option 1.)
Either way Phase 81 likely reuses CUSTOMER-scope (or PROJECT-scope) upload; correspondence attachments route through `initiateCustomerUpload`/`initiateUpload` + a `correspondenceId` set on confirm.

---

## 7. Customer + Task

### 7.1 CustomerRepository (`customer/CustomerRepository.java`)
```java
Optional<Customer> findByEmail(String email);          // <-- reuse for resolve_matter_by_email
boolean existsByEmail(String email);
Optional<Customer> findByIdNumberExact(@Param("idNumber") String idNumber);
List<Customer> findByRegistrationNumber(@Param("registrationNumber") String registrationNumber);
List<Customer> findBySimilarName(@Param("name") String name, @Param("threshold") double threshold, @Param("maxResults") int maxResults);
```

### 7.2 TaskService.createTask (full signature) — `task/TaskService.java`
```java
public Task createTask(
    UUID projectId,
    String title,
    String description,
    String priority,           // String, mapped to TaskPriority
    String type,
    LocalDate dueDate,
    ActorContext actor,
    Map<String, Object> customFields,
    List<UUID> appliedFieldGroups,
    UUID assigneeId,
    String recurrenceRule,
    LocalDate recurrenceEndDate,
    BigDecimal estimatedHours)
```
No request record — params passed individually. (There are simpler overloads too; this is the full one.)

### 7.3 Task entity key fields (`task/Task.java`, table `tasks`)
`id` (UUID), **`projectId` (`project_id` UUID NOT NULL — stored as raw UUID, no FK object)**, `title` (len 500), `description` (TEXT), `status` (enum `TaskStatus`: OPEN/IN_PROGRESS/DONE/CANCELLED), `priority` (enum `TaskPriority`: LOW/MEDIUM/HIGH/URGENT), `dueDate` (LocalDate), `assigneeId` (UUID), `createdBy`, `createdAt`/`updatedAt`/`completedAt`/`cancelledAt`, `type` (len 100), `customFields` (jsonb), `appliedFieldGroups` (jsonb List<UUID>), `recurrenceRule`/`recurrenceEndDate`, `parentTaskId`, `estimatedHours` (BigDecimal 8,2), `version` (`@Version`).
**Task is PROJECT-only — NO customerId/customer link.** Customer↔Task association is mediated indirectly via `CustomerProject` (a customer→project join). So `propose_task` from a correspondence must resolve to a `projectId` (matter). A correspondence tied only to a customer with no matter has no direct task target.

---

## 8. Audit + Activity Registries

### 8.1 Audit
- Registry: `audit/AuditEventTypeRegistry.java` — a `List.of(AuditEventTypeMetadata(...))` in the constructor (ADR-261). Resolution = **longest-prefix-wins** (exact → `prefix.*` glob → INFO/STANDARD default). Unregistered event types still work (auto-synthesised), but register a family for proper severity/label.
- To register the new family, add: `new AuditEventTypeMetadata("mcp.write.*", "MCP Write", AuditSeverity.NOTICE, AuditEventGroup.STANDARD)`.
- `AuditEventBuilder` (in `audit/`): `.eventType(String)` `.entityType(String)` `.entityId(UUID)` `.actorId(UUID)` (auto from `RequestScopes.MEMBER_ID`) `.actorType(String)` (USER/SYSTEM auto) `.source(String)` (API/INTERNAL/SCHEDULER) `.details(Map<String,Object>)` `.build()`. Logged via `AuditService.log(...)`.
- **Naming convention:** dot-namespaced `{domain}.{entity}.{action}`. Existing: `ai.gate.approved`, `ai.specialist.invoked`, `mcp.access.denied`, `mcp.session.opened`, `mcp.tool.invoked`, `document.uploaded`, `document.generated`, `comment.created`, `proposal.sent`, `trust.transaction.approved`, `billing_run.created`, `security.login.failure`, `dataprotection.dsar.*`.
- For Phase 81 define **`mcp.write.*`** family, e.g. `mcp.write.correspondence_filed`, `mcp.write.document_attached`, `mcp.write.task_proposed`, `mcp.write.matter_resolved`.

### 8.2 Activity feed
- **No separate registry/enum.** The activity feed is **driven entirely by audit events**: `activity/ActivityService` queries `AuditEventRepository`, resolves actor names, and `activity/ActivityMessageFormatter.format()` turns each event into a human string.
- `ActivityMessageFormatter.formatMessage(eventType, entityType, actorName, details)` is a `switch (eventType)` (default: `"%s performed %s on %s"`). `resolveEntityName(entityType, details)` extracts a display name.
- To surface Correspondence activity: (1) emit the `mcp.write.*` audit events from the service; (2) add `case "mcp.write.correspondence_filed" -> ...` arms to `formatMessage`; (3) add a `case "correspondence" -> getSubject(details)` arm to `resolveEntityName`; (4) add `getSubject(...)`-style detail extractors. No enum/registry file to touch.

---

## 9. Capability Model

Location: `orgrole/`

- **Annotation** `orgrole/RequiresCapability.java`: `@Target(METHOD) @Retention(RUNTIME)`, single `String value()`. Enforced by `orgrole/CapabilityAuthorizationManager.java` (Spring Security `AuthorizationManager<MethodInvocation>`; reads the annotation, returns `new AuthorizationDecision(RequestScopes.hasCapability(value))`). Registered as a method interceptor in `SecurityConfig`.
- **Enum** `orgrole/Capability.java` — canonical list. Existing relevant: `MCP_ACCESS` (front-door read gate, ADR-304), `AI_ASSISTANT_USE`, `AI_MANAGE`, `AI_EXECUTE`, `AI_REVIEW` (gate review). `OWNER_ONLY = {MANAGE_COMPLIANCE_DESTRUCTIVE, OVERRIDE_MATTER_CLOSURE, FINANCIAL_RECONCILE}`. `ALL_NAMES` auto-derived; `fromString()` validates. **Add new value to introduce write capability:**
  ```java
  /** Write/file data into Kazi via the MCP connector (gated write-back). */
  MCP_WRITE;
  ```
- **Role mapping**: `orgrole/OrgRoleService.resolveCapabilities(memberId)` — owner system role gets `Capability.ALL_NAMES`; admin gets ALL minus `OWNER_ONLY`; member gets explicit `org_role_capabilities` rows + per-member `+CAP`/`-CAP` overrides. Custom roles store rows in `org_role_capabilities` (seeded by tenant migration `V67__create_org_role_tables.sql`; later caps added by e.g. `V77__add_manage_compliance_capability.sql` with idempotent `INSERT ... WHERE NOT EXISTS`). New caps are auto-granted to owner/admin by code; to grant to other roles add a V130+ migration following the V77 pattern.
- **MCP tool capability check**: read tools wrap the body in `McpCapabilityGuard.gatedTool(capability, toolName, auditService, metrics, objectMapper, startNanos -> {...})`. Example: `TrustTools` uses `"VIEW_TRUST"`; `ComplianceTools`/`AuditTools`/`BillingTools` use their respective caps. **NOT `@RequiresCapability`** — the annotation throws `AccessDeniedException` which (a) leaks the reason via Spring AI's error converter, (b) isn't reliably intercepted (no central tools/call dispatch), (c) bypasses the bespoke `mcp.access.denied` audit. Write tools MUST use `McpCapabilityGuard.gatedTool("MCP_WRITE", ...)`.

---

## 10. Entity Pattern Reference (FULL SOURCE)

Entity used: **`timeentry/TimeEntry.java`** (representative recent entity).

> **Multitenancy note:** TimeEntry (like Task, Document, AiExecutionGate) uses **NO** `@FilterDef`/`@Filter`/`TenantAware`/`TenantAwareEntityListener`. Isolation is **pure schema-per-tenant** (search_path set on connection checkout). Standard `JpaRepository.findById()` is tenant-safe. There is no codebase-wide `findOneById` JPQL convention; repositories use plain `findById` (returns `Optional`) plus custom `@Query` JPQL/native methods. The CLAUDE.md "findOneById JPQL" expectation does NOT match this repo — new entities follow the plain-`findById` + custom-`@Query` pattern shown below.

```java
package io.b2mash.b2b.b2bstrawman.timeentry;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.Table;
import java.math.BigDecimal;
import java.math.RoundingMode;
import java.time.Instant;
import java.time.LocalDate;
import java.util.UUID;

@Entity
@Table(name = "time_entries")
public class TimeEntry {

  @Id
  @GeneratedValue(strategy = GenerationType.UUID)
  private UUID id;

  @Column(name = "task_id", nullable = false)
  private UUID taskId;

  @Column(name = "member_id", nullable = false)
  private UUID memberId;

  @Column(name = "date", nullable = false)
  private LocalDate date;

  @Column(name = "duration_minutes", nullable = false)
  private int durationMinutes;

  @Column(name = "billable", nullable = false)
  private boolean billable;

  @Column(name = "billing_rate_snapshot", precision = 12, scale = 2)
  private BigDecimal billingRateSnapshot;

  @Column(name = "billing_rate_currency", length = 3)
  private String billingRateCurrency;

  @Column(name = "invoice_id")
  private UUID invoiceId;

  @Column(name = "description", columnDefinition = "TEXT")
  private String description;

  @Column(name = "created_at", nullable = false, updatable = false)
  private Instant createdAt;

  @Column(name = "updated_at", nullable = false)
  private Instant updatedAt;

  protected TimeEntry() {}

  public TimeEntry(UUID taskId, UUID memberId, LocalDate date, int durationMinutes,
      boolean billable, Integer rateCents, String description) {
    this.taskId = taskId;
    this.memberId = memberId;
    this.date = date;
    this.durationMinutes = durationMinutes;
    this.billable = billable;
    this.description = description;
    this.createdAt = Instant.now();
    this.updatedAt = Instant.now();
  }

  // getters/setters + computed getBillableValue()/getCostValue() (minutes/60 * rate, HALF_UP) ...
}
```
(Real file also carries deprecated `rateCents` + cost-rate snapshot fields; trimmed above for the pattern.)

Repository pattern (`TimeEntryRepository` — JPQL + native, plain `findById` inherited):
```java
public interface TimeEntryRepository extends JpaRepository<TimeEntry, UUID> {

  @Query("SELECT te.id FROM TimeEntry te WHERE te.date < :cutoffDate")
  List<UUID> findIdsByDateBefore(@Param("cutoffDate") LocalDate cutoffDate);

  @Modifying(clearAutomatically = true)
  @Query("UPDATE TimeEntry t SET t.invoiceId = NULL WHERE t.invoiceId = :invoiceId")
  void unbillByInvoiceId(@Param("invoiceId") UUID invoiceId);

  @Query("SELECT te FROM TimeEntry te WHERE te.taskId = :taskId ORDER BY te.date DESC, te.createdAt DESC")
  List<TimeEntry> findByTaskId(@Param("taskId") UUID taskId);

  @Query("SELECT COUNT(te) FROM TimeEntry te WHERE te.taskId = :taskId")
  long countByTaskId(@Param("taskId") UUID taskId);

  // ... many native-SQL aggregation projections (search_path provides tenant isolation) ...
}
```
For a richer entity (JSONB + `@Version` + `@ManyToOne(LAZY)`) compare `AiExecutionGate` (§5.1) — that is the better template for `Correspondence` if it carries JSONB metadata + optimistic locking.

---

## 11. Recent ADR Format Template (FULL SOURCE skeleton — ADR-318)

`adr/ADR-318-pipeline-metrics.md` (highest-numbered). Section structure to mirror for ADR-319..323:
**Title line** `# ADR-NNN: <Title>` → **Status** (Accepted) → **Context** → **Options Considered** (numbered, each with Pros/Cons, CHOSEN marked) → **Decision** (one paragraph) → **Rationale** (numbered) → **Consequences** (Positive/Negative/Neutral/Related bullets, with `[ADR-NNN](file.md)` cross-links).

```markdown
# ADR-318: Pipeline Metrics — Weighted Value, Win Rate & Currency Assumption

**Status**: Accepted

**Context**:

Phase 80's reporting surface must give a firm an honest, at-a-glance read on its pipeline ...
[constraints, existing entities, founder decision deferring ReportDefinition] ...

**Options Considered**:

1. **Purpose-built summary endpoint: weighted value + windowed win rate (CHOSEN)** — ...
   - Pros: ...
   - Cons: ...
2. **Full `ReportDefinition`-based pipeline reports** — ...
   - Pros: ...
   - Cons: ...
3. **Raw counts only (no weighting)** — ...
   - Pros: ...
   - Cons: ...

**Decision**: Option 1 — a purpose-built pipeline-summary endpoint computing weighted value ...

**Rationale**:

1. **Weighted value over OPEN deals is the honest forecast.** ...
2. **A defined, shared window keeps win rate trustworthy.** ...
3. **`ReportDefinition` is the wrong tool for v1.** ...
4. **Single currency is a deliberate, documented v1 assumption.** ...

**Consequences**:
- Positive: ...
- Negative: ...
- Related: [ADR-314](ADR-314-pipeline-stage-model.md) (...), [ADR-313](ADR-313-crm-lead-model.md) (...).
```

ADR-303 (MCP authentication) uses the same format and is the key cross-reference for any new MCP-write auth ADR: MCP = OAuth 2.1 protected resource reusing the Keycloak JWT pipeline; `/mcp` on the authenticated chain binds TENANT/MEMBER/CAPABILITIES via `TenantFilter`+`MemberFilter`; no API keys, no impersonation. ADR-304 (MCP_ACCESS front-door capability gate) and ADR-305 (POPIA consent + audit) are the cross-references for layering `MCP_WRITE` and write-audit on top.

---

## 12. Backend Packages & Frontend Routes

### Backend packages (`src/main/java/io/b2mash/b2b/b2bstrawman/`)
acceptance, accessrequest, activity, assistant, audit, automation, billing, billingrate, billingrun, budget, calendar, capacity, checklist, clause, comment, compliance, config, costrate, customer, customerbackend, dashboard, datarequest, deadline, demo, dev, document, event, exception, expense, fielddefinition, informationrequest, infrastructure, **integration** (→ ai/gate, ai/execution, ai/skill, email, accounting, ...), invitation, invoice, **mcp** (→ tool, resource, dto, consent), member, multitenancy, mywork, notification, onboarding, orgrole, packs, portal, prerequisite, project, projecttemplate, proposal, provisioning, report, reporting, retainer, retention, s3, schedule, security, seeder, settings, setupstatus, tag, task, tax, template, timeentry, verticals, view.

> No existing `correspondence` package — Phase 81 net-new (likely `correspondence/` for the entity + a new `mcp/tool/` write-tools class). Note `integration/email/` already exists (email integration — check before adding inbound-email plumbing).

### Frontend routes (`frontend/app/(app)/org/[slug]/`)
ai, calendar, compliance, conflict-check, court-calendar, customers, dashboard, deadlines, documents, information-requests, invoices, layout.tsx, legal, my-work, notifications, profitability, projects, proposals, reports, resources, retainers, schedules, settings, team, trust-accounting.

> No `correspondence` route yet. An inbound-correspondence inbox/review UI is candidate net-new; the gate-approval surface likely lives under `ai/` (gate queue uses `AI_REVIEW`).

---

## Numbering Summary (quick reference)
| Item | Value |
|------|-------|
| Next ARCHITECTURE.md section | `## 11` (highest numbered = 10; or standalone phase doc per convention) |
| Next ADR | **ADR-319** (319–323 free, confirmed) |
| Next global migration | **V26** (highest = V25) |
| Next tenant migration | **V130** (highest = **V129**, confirmed) |
| New capability | `MCP_WRITE` in `orgrole/Capability.java` |
| New backend package | `correspondence/` + `mcp/tool/CorrespondenceWriteTools.java` |
| New audit family | `mcp.write.*` |
| Key reuse | `CustomerRepository.findByEmail`, `TaskService.createTask`, `DocumentService.initiate*Upload`, `AiExecutionGate`+`GateActionExecutor` |

### Two architecture decisions that need an ADR call
1. **MCP write enforcement** — no central tools/call dispatch in Spring AI 2.0.0-M6, so write gating is inline per tool (`effectiveState()` + `McpCapabilityGuard.gatedTool("MCP_WRITE", ...)`). This needs to be a deliberate ADR (mirrors the McpCapabilityGuard rationale).
2. **Gate creation over MCP & the `execution_id NOT NULL` FK** — `AiExecutionGate` requires an `AiExecution`. Either model the MCP correspondence write as an `AiExecution`, or relax the FK. This is a structural decision (ADR-worthy), plus a new `AiExecutionGateService` creation method + new `GateAction` arm.
