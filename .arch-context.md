# Architecture Context Inventory for Phase 53 (Dashboard Polish, Navigation Cleanup, Document Preview)

Generated: 2026-03-21

**Phase type**: PURE FRONTEND -- no new backend endpoints, entities, or migrations.

---

## 1. ARCHITECTURE.md Section Headers

File: `architecture/ARCHITECTURE.md`

| Line | Header |
|------|--------|
| 3 | `## 1. Overview` |
| 50 | `## 2. Architecture Decision Records` |
| 409 | `## 3. Component Architecture` |
| 704 | `## 4. Sequence Diagrams` |
| 874 | `## 5. Data Flow Diagrams` |
| 980 | `## 6. Security Architecture` |
| 1083 | `## 7. Infrastructure Architecture` |
| 1236 | `## 8. Database Architecture` |
| 1344 | `## 9. Phase 2 -- Billing & Tiered Tenancy` |
| 1874 | `## 10. Phase 4 -- Customers, Document Scopes & Tasks` |
| 2446 | `## Appendix A: Clerk JWT Claims` |
| 2460 | `## Appendix B: Environment Variables Reference` |

- **Last numbered section**: 10 (Phase 4)
- **Next available section number**: 11
- Note: Phases 5+ have standalone architecture docs in `architecture/phase*-*.md`, not inline in ARCHITECTURE.md

---

## 2. ADR Numbering

Total ADR files in `adr/`: 204

**Highest ADR number**: ADR-204 (`ADR-204-virtual-thread-scoped-value-rebinding.md`)
**Next available ADR number**: 205

The requirements doc specifies **ADR-205: Chart theming strategy** as the ADR topic for this phase.

Recent ADRs (last 10):
- ADR-195: dsar-deadline-calculation
- ADR-196: pre-anonymization-export-storage
- ADR-197: calculated-vs-stored-deadlines
- ADR-198: post-create-action-execution
- ADR-199: filing-status-lazy-creation
- ADR-200: llm-chat-provider-interface
- ADR-201: secret-store-reuse-for-ai-keys
- ADR-202: consumer-callback-streaming
- ADR-203: completable-future-confirmation
- ADR-204: virtual-thread-scoped-value-rebinding

---

## 3. Frontend Dependencies (from package.json)

| Package | Version | Role in Phase 53 |
|---------|---------|------------------|
| `recharts` | `^3.7.0` | Chart library -- enhanced, NOT replaced |
| `motion` | `^12.34.0` | Framer Motion -- animation for new widgets |
| `lucide-react` | `^0.563.0` | Icon library |
| `next` | `16.1.6` | Framework |
| `react` | `19.2.3` | UI library |
| `tailwindcss` | `^4` | CSS-first config (no tailwind.config.ts) |
| `swr` | `^2.4.1` | Client-side data fetching |
| `radix-ui` | `^1.4.3` | Shadcn primitives |
| `sonner` | `^2.0.7` | Toast notifications |
| `clsx` + `tailwind-merge` | latest | `cn()` class merging |

No new dependencies needed for this phase.

---

## 4. Design System Reference (Signal Deck)

### 4.1 Fonts
| Role | Font | CSS Variable | Usage |
|------|------|-------------|-------|
| Display | **Sora** | `--font-display` | h1, hero text, headings. Use `font-display` class |
| Body / UI | **IBM Plex Sans** | `--font-sans` | Paragraphs, buttons, labels, table text |
| Code / Stats | **JetBrains Mono** | `--font-mono` | KPI numbers, inline stats. Use `font-mono tabular-nums` for data |

### 4.2 Color System (from globals.css)

**Slate OKLCH Scale (hue ~260)**:
```
slate-50:  oklch(98.2% 0.003 260)
slate-100: oklch(95.8% 0.005 260)
slate-200: oklch(91% 0.008 260)
slate-300: oklch(85.5% 0.012 260)
slate-400: oklch(70% 0.018 260)
slate-500: oklch(55.5% 0.025 260)
slate-600: oklch(44.5% 0.020 260)
slate-700: oklch(37% 0.018 260)
slate-800: oklch(26% 0.014 260)
slate-900: oklch(20.5% 0.011 260)
slate-950: oklch(13.5% 0.006 258)
```

**Teal Accent**:
```
teal-500: oklch(72% 0.15 185)
teal-600: oklch(62% 0.16 183)
```

**Semantic Tokens (light mode)**:
- `--background`: `oklch(94% 0.008 260)` (concrete gray)
- `--card`: `oklch(99.5% 0.002 260)` (near-white, lifted)
- `--primary`: slate-950
- `--accent`: teal-600
- `--sidebar`: slate-950 (dark sidebar in both modes)

**Chart Colors (light mode)**:
```
--chart-1: oklch(0.646 0.222 41.116)   -- warm orange
--chart-2: oklch(0.6 0.118 184.704)    -- teal
--chart-3: oklch(0.398 0.07 227.392)   -- dark blue
--chart-4: oklch(0.828 0.189 84.429)   -- yellow
--chart-5: oklch(0.769 0.188 70.08)    -- amber
```

**Chart Colors (dark mode)**:
```
--chart-1: oklch(0.488 0.243 264.376)  -- blue
--chart-2: oklch(0.696 0.17 162.48)    -- teal
--chart-3: oklch(0.769 0.188 70.08)    -- amber
--chart-4: oklch(0.627 0.265 303.9)    -- purple
--chart-5: oklch(0.645 0.246 16.439)   -- red
```

### 4.3 Anti-Patterns
- Never use olive/neutral/zinc/gray -- use **slate**
- Never use indigo for accents -- use **teal**
- Never import `motion` in server components
- Never pass functions/component refs from Server to Client Components

---

## 5. globals.css (FULL SOURCE)

File: `frontend/app/globals.css`

```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  /* Semantic color mappings */
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);

  /* Chart colors */
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);

  /* Sidebar semantic colors */
  --color-sidebar: var(--sidebar);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);

  /* Slate color scale (OKLCH, hue ~260) */
  --color-slate-50: oklch(98.2% 0.003 260);
  --color-slate-100: oklch(95.8% 0.005 260);
  --color-slate-200: oklch(91% 0.008 260);
  --color-slate-300: oklch(85.5% 0.012 260);
  --color-slate-400: oklch(70% 0.018 260);
  --color-slate-500: oklch(55.5% 0.025 260);
  --color-slate-600: oklch(44.5% 0.020 260);
  --color-slate-700: oklch(37% 0.018 260);
  --color-slate-800: oklch(26% 0.014 260);
  --color-slate-900: oklch(20.5% 0.011 260);
  --color-slate-950: oklch(13.5% 0.006 258);

  /* Teal accent */
  --color-teal-500: oklch(72% 0.15 185);
  --color-teal-600: oklch(62% 0.16 183);

  /* Fonts */
  --font-sans: var(--font-plex);
  --font-mono: var(--font-jetbrains);
  --font-display: var(--font-sora);

  /* Radius */
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
  --radius-2xl: calc(var(--radius) + 8px);
  --radius-3xl: calc(var(--radius) + 12px);
  --radius-4xl: calc(var(--radius) + 16px);
}

:root {
  --radius: 0.5rem;
  /* ... slate raw scale, teal, semantic tokens for light mode ... */
  /* Chart colors */
  --chart-1: oklch(0.646 0.222 41.116);
  --chart-2: oklch(0.6 0.118 184.704);
  --chart-3: oklch(0.398 0.07 227.392);
  --chart-4: oklch(0.828 0.189 84.429);
  --chart-5: oklch(0.769 0.188 70.08);
  /* Sidebar dark in both modes */
}

.dark {
  /* ... dark mode semantic tokens ... */
  --chart-1: oklch(0.488 0.243 264.376);
  --chart-2: oklch(0.696 0.17 162.48);
  --chart-3: oklch(0.769 0.188 70.08);
  --chart-4: oklch(0.627 0.265 303.9);
  --chart-5: oklch(0.645 0.246 16.439);
}

@layer base {
  * { @apply border-border outline-ring/50; }
  html { scroll-behavior: smooth; }
  body { @apply bg-background text-foreground; }
}

@utility tabular { font-variant-numeric: tabular-nums; }
@utility scroll-shadow { /* gradient scroll shadows */ }
```

---

## 6. Sidebar Navigation (FULL SOURCE)

File: `frontend/lib/nav-items.ts`

```typescript
import {
  LayoutDashboard,
  ClipboardList,
  CalendarDays,
  FolderOpen,
  FileText,
  Users,
  UserRound,
  Bell,
  TrendingUp,
  BarChart3,
  Receipt,
  CalendarClock,
  ShieldCheck,
  Settings,
  Scale,
  Gavel,
  type LucideIcon,
} from "lucide-react";
import type { CAPABILITIES } from "@/lib/capabilities";

/** Union of all known capability strings */
type CapabilityName = (typeof CAPABILITIES)[keyof typeof CAPABILITIES];

export interface NavItem {
  label: string;
  href: (slug: string) => string;
  icon: LucideIcon;
  /** Use exact match instead of startsWith for active detection */
  exact?: boolean;
  /** If set, sidebar only shows this item when the user has this capability */
  requiredCapability?: CapabilityName;
  /** If set, sidebar only shows this item when the org has this module enabled */
  requiredModule?: string;
  /** Optional keywords for future command palette fuzzy search */
  keywords?: string[];
}

export interface NavGroup {
  id: string;
  label: string;
  items: NavItem[];
  defaultExpanded: boolean;
}

export interface SettingsItem {
  title: string;
  description: string;
  href: (slug: string) => string;
  adminOnly?: boolean;
  comingSoon?: boolean;
}

/**
 * Grouped navigation items organized into 5 logical zones.
 * This is the source of truth for sidebar navigation structure.
 */
export const NAV_GROUPS: NavGroup[] = [
  {
    id: "work",
    label: "Work",
    defaultExpanded: true,
    items: [
      {
        label: "Dashboard",
        href: (slug) => `/org/${slug}/dashboard`,
        icon: LayoutDashboard,
        exact: true,
      },
      {
        label: "My Work",
        href: (slug) => `/org/${slug}/my-work`,
        icon: ClipboardList,
        exact: true,
      },
      {
        label: "Calendar",
        href: (slug) => `/org/${slug}/calendar`,
        icon: CalendarDays,
        exact: true,
      },
      {
        label: "Court Calendar",
        href: (slug) => `/org/${slug}/court-calendar`,
        icon: Gavel,
        exact: true,
        requiredCapability: "PROJECT_MANAGEMENT",
        requiredModule: "court_calendar",
      },
    ],
  },
  {
    id: "delivery",          // RENAME to "projects"
    label: "Delivery",       // RENAME to "Projects"
    defaultExpanded: true,
    items: [
      {
        label: "Projects",
        href: (slug) => `/org/${slug}/projects`,
        icon: FolderOpen,
      },
      {
        label: "Documents",  // REMOVE this item
        href: (slug) => `/org/${slug}/documents`,
        icon: FileText,
        exact: true,
      },
      {
        label: "Recurring Schedules",
        href: (slug) => `/org/${slug}/schedules`,
        icon: CalendarClock,
        requiredCapability: "PROJECT_MANAGEMENT",
      },
    ],
  },
  {
    id: "clients",
    label: "Clients",
    defaultExpanded: false,
    items: [
      {
        label: "Customers",
        href: (slug) => `/org/${slug}/customers`,
        icon: UserRound,
        requiredCapability: "CUSTOMER_MANAGEMENT",
      },
      // ADD "Proposals" HERE (moved from Finance)
      {
        label: "Retainers",
        href: (slug) => `/org/${slug}/retainers`,
        icon: FileText,
        requiredCapability: "INVOICING",
      },
      {
        label: "Compliance",
        href: (slug) => `/org/${slug}/compliance`,
        icon: ShieldCheck,
        requiredCapability: "CUSTOMER_MANAGEMENT",
      },
      {
        label: "Deadlines",
        href: (slug) => `/org/${slug}/deadlines`,
        icon: CalendarClock,
        exact: true,
        requiredCapability: "CUSTOMER_MANAGEMENT",
        requiredModule: "regulatory_deadlines",
      },
    ],
  },
  {
    id: "finance",
    label: "Finance",
    defaultExpanded: false,
    items: [
      {
        label: "Invoices",
        href: (slug) => `/org/${slug}/invoices`,
        icon: Receipt,
        requiredCapability: "INVOICING",
      },
      {
        label: "Proposals",   // MOVE to Clients zone
        href: (slug) => `/org/${slug}/proposals`,
        icon: FileText,
        requiredCapability: "INVOICING",
      },
      {
        label: "Profitability",
        href: (slug) => `/org/${slug}/profitability`,
        icon: TrendingUp,
        exact: true,
        requiredCapability: "FINANCIAL_VISIBILITY",
      },
      {
        label: "Reports",
        href: (slug) => `/org/${slug}/reports`,
        icon: BarChart3,
        exact: true,
        requiredCapability: "FINANCIAL_VISIBILITY",
      },
      {
        label: "Trust Accounting",
        href: (slug) => `/org/${slug}/trust-accounting`,
        icon: Scale,
        exact: true,
        requiredCapability: "FINANCIAL_VISIBILITY",
        requiredModule: "trust_accounting",
      },
    ],
  },
  {
    id: "team",
    label: "Team & Resources",  // RENAME to "Team"
    defaultExpanded: true,
    items: [
      {
        label: "Team",
        href: (slug) => `/org/${slug}/team`,
        icon: Users,
        exact: true,
      },
      {
        label: "Resources",
        href: (slug) => `/org/${slug}/resources`,
        icon: Users,
        exact: true,
        requiredCapability: "RESOURCE_PLANNING",
      },
    ],
  },
];

/**
 * Utility items pinned to the sidebar footer.
 */
export const UTILITY_ITEMS: NavItem[] = [
  {
    label: "Notifications",
    href: (slug) => `/org/${slug}/notifications`,
    icon: Bell,
    exact: true,
  },
  {
    label: "Settings",
    href: (slug) => `/org/${slug}/settings`,
    icon: Settings,
  },
];

/**
 * Settings page items for command palette indexing.
 */
export const SETTINGS_ITEMS: SettingsItem[] = [
  { title: "Billing", description: "Manage your subscription and view usage", href: (slug) => `/org/${slug}/settings/billing` },
  { title: "Notifications", description: "Configure notification preferences", href: (slug) => `/org/${slug}/settings/notifications` },
  { title: "Rates & Currency", description: "Manage billing rates, cost rates, and default currency", href: (slug) => `/org/${slug}/settings/rates` },
  { title: "Tax", description: "Configure tax registration, labels, and inclusive pricing", href: (slug) => `/org/${slug}/settings/tax` },
  { title: "Time Tracking", description: "Configure time reminders and default expense markup", href: (slug) => `/org/${slug}/settings/time-tracking` },
  { title: "Custom Fields", description: "Define custom fields and groups for projects, tasks, and customers", href: (slug) => `/org/${slug}/settings/custom-fields` },
  { title: "Tags", description: "Manage org-wide tags for projects, tasks, and customers", href: (slug) => `/org/${slug}/settings/tags` },
  { title: "Templates", description: "Manage document templates and branding", href: (slug) => `/org/${slug}/settings/templates` },
  { title: "Clauses", description: "Manage reusable clause library for document generation", href: (slug) => `/org/${slug}/settings/clauses` },
  { title: "Checklists", description: "Manage checklist templates for customer onboarding", href: (slug) => `/org/${slug}/settings/checklists` },
  { title: "Document Acceptance", description: "Configure default expiry period for document acceptance requests", href: (slug) => `/org/${slug}/settings/acceptance` },
  { title: "Compliance", description: "Configure retention policies, dormancy thresholds, and data requests", href: (slug) => `/org/${slug}/settings/compliance` },
  { title: "Project Templates", description: "Create and manage reusable project blueprints", href: (slug) => `/org/${slug}/settings/project-templates` },
  { title: "Project Naming", description: "Configure auto-naming patterns for new projects", href: (slug) => `/org/${slug}/settings/project-naming` },
  { title: "Request Templates", description: "Create and manage reusable information request templates", href: (slug) => `/org/${slug}/settings/request-templates` },
  { title: "Request Settings", description: "Configure default reminder interval for information requests", href: (slug) => `/org/${slug}/settings/request-settings` },
  { title: "Batch Billing", description: "Configure async thresholds, email rate limits, and default billing currency", href: (slug) => `/org/${slug}/settings/batch-billing`, adminOnly: true },
  { title: "Capacity", description: "Set default weekly capacity hours for team members", href: (slug) => `/org/${slug}/settings/capacity` },
  { title: "Email", description: "View email delivery logs, stats, and rate limits", href: (slug) => `/org/${slug}/settings/email`, adminOnly: true },
  { title: "Automations", description: "Create rules to automate tasks, notifications, and workflows", href: (slug) => `/org/${slug}/settings/automations`, adminOnly: true },
  { title: "Roles & Permissions", description: "Define custom roles and manage team permissions.", href: (slug) => `/org/${slug}/settings/roles`, adminOnly: true },
  { title: "Organization", description: "Update org name, logo, and details", href: () => "#", comingSoon: true },
  { title: "Security", description: "Configure authentication and access policies", href: () => "#", comingSoon: true },
  { title: "Integrations", description: "Connect third-party tools and services", href: (slug) => `/org/${slug}/settings/integrations` },
];

export const NAV_ITEMS: NavItem[] = [
  ...NAV_GROUPS.flatMap((g) => g.items),
  ...UTILITY_ITEMS,
];
```

### Required Sidebar Changes (Phase 53)

| Change | Current | Target |
|--------|---------|--------|
| Delivery group ID | `id: "delivery"` | `id: "projects"` |
| Delivery group label | `"Delivery"` | `"Projects"` |
| Documents item | Present in Delivery group | **REMOVE entirely** |
| Proposals item | In Finance group | **MOVE to Clients group** (after Customers, before Retainers) |
| Team group label | `"Team & Resources"` | `"Team"` |
| Settings Organization | `comingSoon: true` | Functional (org docs section added) |

---

## 7. Desktop Sidebar Component (FULL SOURCE)

File: `frontend/components/desktop-sidebar.tsx`

```tsx
"use client";

import { Fragment } from "react";
import { usePathname } from "next/navigation";
import Link from "next/link";
import { Shield, Search } from "lucide-react";
import { motion } from "motion/react";
import { cn } from "@/lib/utils";
import { NAV_GROUPS, UTILITY_ITEMS } from "@/lib/nav-items";
import { NavZone } from "@/components/nav-zone";
import { SidebarUserFooter } from "@/components/sidebar-user-footer";
import { useCommandPalette } from "@/components/command-palette-provider";

interface DesktopSidebarProps {
  slug: string;
  groups?: string[];
}

export function DesktopSidebar({ slug, groups = [] }: DesktopSidebarProps) {
  const pathname = usePathname();
  const { setOpen } = useCommandPalette();

  return (
    <aside className="hidden w-60 flex-col bg-slate-950 md:flex">
      {/* Header */}
      <div className="flex h-14 items-center px-4">
        <span className="text-base font-bold tracking-tight text-white">DocTeams</span>
      </div>
      <div className="mx-4 border-t border-white/10" />
      <div className="flex items-center gap-2 px-4 py-3">
        <span className="truncate text-teal-500/80 font-mono text-xs tracking-wider uppercase">{slug}</span>
      </div>
      <div className="mx-4 border-t border-white/10" />

      {/* Search pill */}
      <button
        type="button"
        aria-label="Search, Command K"
        onClick={() => setOpen(true)}
        className="mx-4 mb-2 flex w-[calc(100%-2rem)] items-center gap-2 rounded-md border border-white/10 bg-white/5 px-3 py-1.5 text-xs text-white/40 hover:bg-white/10 hover:text-white/60 transition-colors"
      >
        <Search className="h-3.5 w-3.5" />
        Search...
        <kbd className="ml-auto rounded bg-white/10 px-1 py-0.5 text-[10px] font-mono">Cmd+K</kbd>
      </button>

      {/* Nav body -- zone-based */}
      <nav aria-label="Main navigation" className="flex flex-1 flex-col gap-0 p-2">
        {NAV_GROUPS.map((group, index) => (
          <Fragment key={group.id}>
            {index > 0 && (
              <div className="my-1 mx-2 border-t border-white/5" />
            )}
            <NavZone zone={group} slug={slug} />
          </Fragment>
        ))}
      </nav>

      {/* Platform Admin */}
      {groups.includes("platform-admins") && (
        <>
          <div className="mx-4 border-t border-white/10" />
          <div className="p-2">
            <Link href="/platform-admin/access-requests" className={cn(/* ... */)} >
              <Shield className="h-4 w-4" />
              Platform Admin
            </Link>
          </div>
        </>
      )}

      {/* Utility footer */}
      <div className="mx-4 border-t border-white/10" />
      <div className="p-2">
        {UTILITY_ITEMS.map((item) => {
          const href = item.href(slug);
          const isActive = item.exact ? pathname === href : pathname.startsWith(href);
          return (
            <Link key={item.label} href={href} className={cn(/* active/inactive styles */)}>
              {isActive && (
                <motion.div
                  layoutId="sidebar-indicator"
                  aria-hidden="true"
                  className="absolute left-0 top-1 bottom-1 w-0.5 rounded-full bg-teal-500"
                  transition={{ type: "spring", stiffness: 350, damping: 30 }}
                />
              )}
              <item.icon className="h-4 w-4" />
              {item.label}
            </Link>
          );
        })}
      </div>
      <SidebarUserFooter />
    </aside>
  );
}
```

---

## 8. NavZone Component (FULL SOURCE)

File: `frontend/components/nav-zone.tsx`

```tsx
"use client";

import { useState } from "react";
import { usePathname } from "next/navigation";
import Link from "next/link";
import { ChevronRight } from "lucide-react";
import { AnimatePresence, motion } from "motion/react";
import { cn } from "@/lib/utils";
import { useCapabilities } from "@/lib/capabilities";
import { useTerminology } from "@/lib/terminology";
import { useOrgProfile } from "@/lib/org-profile";
import type { NavGroup } from "@/lib/nav-items";

export interface NavZoneProps {
  zone: NavGroup;
  slug: string;
  onNavItemClick?: () => void;
}

export function NavZone({ zone, slug, onNavItemClick }: NavZoneProps) {
  const [expanded, setExpanded] = useState(zone.defaultExpanded ?? true);
  const pathname = usePathname();
  const { hasCapability } = useCapabilities();
  const { t } = useTerminology();
  const { isModuleEnabled } = useOrgProfile();

  const visibleItems = zone.items.filter(
    (item) =>
      (!item.requiredCapability || hasCapability(item.requiredCapability)) &&
      (!item.requiredModule || isModuleEnabled(item.requiredModule)),
  );

  if (visibleItems.length === 0) return null;

  return (
    <div>
      <button
        type="button"
        aria-expanded={expanded}
        onClick={() => setExpanded((prev) => !prev)}
        className="flex w-full items-center justify-between px-3 py-1.5 ..."
      >
        <span className="text-[11px] font-medium tracking-widest text-white/40 uppercase">
          {zone.label}
        </span>
        <ChevronRight className={cn("h-3 w-3 text-white/40 transition-transform duration-150", expanded && "rotate-90")} />
      </button>
      <AnimatePresence initial={false}>
        {expanded && (
          <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: "auto", opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.15, ease: "easeOut" }} className="overflow-hidden">
            {visibleItems.map((item) => {
              const href = item.href(slug);
              const isActive = item.exact ? pathname === href : pathname.startsWith(href);
              return (
                <Link key={item.href(slug)} href={href} onClick={onNavItemClick} className={cn(/* active/inactive */)}>
                  {isActive && (
                    <motion.div layoutId={`sidebar-indicator-${zone.id}`} aria-hidden="true" className="absolute left-0 top-1 bottom-1 w-0.5 rounded-full bg-teal-500" transition={{ type: "spring", stiffness: 350, damping: 30 }} />
                  )}
                  <item.icon className="h-4 w-4" />
                  {t(item.label)}
                </Link>
              );
            })}
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
}
```

---

## 9. Org Dashboard Page (FULL SOURCE)

File: `frontend/app/(app)/org/[slug]/dashboard/page.tsx`

```tsx
import { getCurrentUserEmail } from "@/lib/auth";
import { fetchMyCapabilities } from "@/lib/api/capabilities";
import { api, ApiError, handleApiError } from "@/lib/api";
import { Loader2 } from "lucide-react";
import { ProvisioningPendingRefresh } from "./provisioning-pending-refresh";
import { DashboardHeader } from "./dashboard-header";
import { KpiCardRow } from "@/components/dashboard/kpi-card-row";
import { ProjectHealthWidget } from "@/components/dashboard/project-health-widget";
import { TeamWorkloadWidget } from "@/components/dashboard/team-workload-widget";
import { RecentActivityWidget } from "@/components/dashboard/recent-activity-widget";
import { IncompleteProfilesWidget } from "@/components/dashboard/incomplete-profiles-widget";
import { InformationRequestsWidget } from "@/components/dashboard/information-requests-widget";
import { AutomationsWidget } from "@/components/automations/automations-widget";
import { TeamCapacityWidget } from "@/components/dashboard/team-capacity-widget";
import { MyScheduleWidget } from "@/components/dashboard/my-schedule-widget";
import { DeadlineWidget } from "@/components/dashboard/deadline-widget";
import { ModuleGate } from "@/components/module-gate";
import {
  fetchDashboardKpis, fetchProjectHealth, fetchTeamWorkload,
  fetchDashboardActivity, fetchAggregatedCompleteness,
  fetchInformationRequestSummary, fetchAutomationSummary,
} from "@/lib/actions/dashboard";
import {
  getTeamCapacityGrid, listAllocations, listLeaveForMember,
  type TeamCapacityGrid, type AllocationResponse, type LeaveBlockResponse,
} from "@/lib/api/capacity";
import { resolveDateRange, getCurrentMonday, formatDate as formatDateUtil, addWeeks } from "@/lib/date-utils";
import type { OrgMember } from "@/lib/types";
import { GettingStartedCard } from "@/components/dashboard/getting-started-card";

export default async function OrgDashboardPage({
  params, searchParams,
}: {
  params: Promise<{ slug: string }>;
  searchParams: Promise<{ from?: string; to?: string }>;
}) {
  const { slug } = await params;
  const resolvedSearchParams = await searchParams;
  const caps = await fetchMyCapabilities();
  const isAdmin = caps.isAdmin || caps.isOwner;
  const { from, to } = resolveDateRange(resolvedSearchParams);

  // Parallel data fetch: kpis, projectHealth, teamWorkload, activity, aggregatedCompleteness, requestSummary, automationSummary
  // ... (try/catch with provisioning state handling)

  // Capacity data: capacityGrid, myAllocations, myLeave
  // ... (parallel fetch with email + members resolution)

  return (
    <div className="space-y-8">
      <DashboardHeader from={from} to={to} />
      <GettingStartedCard />
      <KpiCardRow kpis={kpis ?? null} isAdmin={isAdmin} orgSlug={slug} />
      <div className="grid grid-cols-1 gap-6 lg:grid-cols-5">
        <div className="lg:col-span-3">
          <ProjectHealthWidget projects={projectHealth ?? null} orgSlug={slug} />
        </div>
        <div className="space-y-6 lg:col-span-2">
          <TeamWorkloadWidget data={teamWorkload ?? null} isAdmin={isAdmin} />
          <TeamCapacityWidget data={capacityGrid} orgSlug={slug} />
          <MyScheduleWidget allocations={myAllocations} leaveBlocks={myLeave} weeklyCapacity={myWeeklyCapacity} projectNames={...} />
          <ModuleGate module="regulatory_deadlines">
            <DeadlineWidget orgSlug={slug} />
          </ModuleGate>
          {isAdmin && <IncompleteProfilesWidget data={aggregatedCompleteness ?? null} orgSlug={slug} />}
          {isAdmin && <InformationRequestsWidget data={requestSummary ?? null} orgSlug={slug} />}
          {isAdmin && <AutomationsWidget data={automationSummary ?? null} orgSlug={slug} />}
          <RecentActivityWidget items={activity ?? null} orgSlug={slug} />
        </div>
      </div>
    </div>
  );
}
```

### Current Dashboard Layout
- **Row 1**: DashboardHeader (title + date range selector)
- **Row 2**: GettingStartedCard (dismissible onboarding)
- **Row 3**: KpiCardRow (3-5 KPI cards: Active Projects, Hours Logged, Billable %, Overdue Tasks, Avg Margin)
- **Row 4**: Two-column grid (3:2 ratio)
  - Left (3/5): ProjectHealthWidget
  - Right (2/5): Stack of TeamWorkload, TeamCapacity, MySchedule, Deadlines, IncompleteProfiles, InfoRequests, Automations, RecentActivity

---

## 10. Dashboard Components (FULL SOURCE)

File: `frontend/components/dashboard/` -- 17 files:

### 10.1 KpiCard

```tsx
// components/dashboard/kpi-card.tsx
// Renders a single metric card with:
// - Label (uppercase tracking-wider)
// - Value (text-2xl font-bold font-mono tabular-nums)
// - Change indicator (arrow + percentage, green/red)
// - SparklineChart (optional, 80x24px)
// - Optional href (makes card clickable)
// - Empty state (italic muted text)
```

### 10.2 KpiCardRow

```tsx
// components/dashboard/kpi-card-row.tsx
// Renders 3-5 KPI cards in a responsive grid
// Cards: Active Projects, Hours Logged, Billable % (admin), Overdue Tasks, Avg. Margin (admin)
// Grid: 2 cols → 3 cols (member) or 5 cols (admin)
// Computes changePercent, changeDirection, formatHours
```

### 10.3 SparklineChart

```tsx
// components/dashboard/sparkline-chart.tsx
// Custom SVG sparkline (no Recharts)
// Default 80x24px, gradient fill, polyline stroke
// Props: data: number[], width, height, color
```

### 10.4 HorizontalBarChart

```tsx
// components/dashboard/horizontal-bar-chart.tsx
// Recharts BarChart (layout="vertical") with stacked segments
// Used by TeamWorkloadWidget
// DEFAULT_COLORS: ["#2563eb", "#e11d48", "#e77e23", "#8b5cf6", "#06b6d4"]
```

### 10.5 HealthBadge

```tsx
// components/dashboard/health-badge.tsx
// Colored dot + text for HEALTHY/AT_RISK/CRITICAL/UNKNOWN
// Sizes: sm (dot only), md (dot + text), lg (dot + text + reasons list)
// Colors: green-500, amber-500, red-500, gray-400
```

### 10.6 CompletionProgressBar

```tsx
// components/dashboard/completion-progress-bar.tsx
// Thin progress bar with percentage text
// Color: green >66%, amber >33%, red <=33%
```

### 10.7 MiniProgressRing

```tsx
// components/dashboard/mini-progress-ring.tsx
// SVG circular progress ring
// Auto-color: green >66%, amber >33%, red <=33%
// Props: value (0-100), size (default 32), color
```

### 10.8 DateRangeSelector

```tsx
// components/dashboard/date-range-selector.tsx
// Preset buttons: This Week, This Month, Last 30 Days, This Quarter, Custom (disabled)
// Refresh button
// Updates URL search params on selection
```

### 10.9 ProjectHealthWidget

```tsx
// components/dashboard/project-health-widget.tsx
// Card with filter tabs (All / At Risk / Critical)
// Clickable project rows: HealthBadge + name + customer + CompletionProgressBar + reasons
// Max 10 visible rows
// "View all projects" footer link
```

### 10.10 TeamWorkloadWidget

```tsx
// components/dashboard/team-workload-widget.tsx
// Card with dynamically imported HorizontalBarChart
// Transforms TeamWorkloadEntry[] into stacked bar data
// Shows legend
```

### 10.11 TeamCapacityWidget

```tsx
// components/dashboard/team-capacity-widget.tsx
// Card with MiniProgressRing (64px) showing team utilization %
// Stats: allocated/capacity hours, over-allocated count badge, under-utilized count
// "View resources" footer link
```

### 10.12 RecentActivityWidget

```tsx
// components/dashboard/recent-activity-widget.tsx
// Card with activity feed (avatar initials, icon, description, project badge, relative time)
// Entity icon map: task=CheckSquare, document=FileText, comment=MessageSquare, time_entry=Clock, project_member=Users
```

### 10.13 MyScheduleWidget

```tsx
// components/dashboard/my-schedule-widget.tsx
// Card showing this week's project allocations with hours
// Capacity remaining bar (green) or over-allocation warning (red)
// Upcoming leave blocks
```

### 10.14 IncompleteProfilesWidget

```tsx
// components/dashboard/incomplete-profiles-widget.tsx
// Admin-only card showing incomplete customer profile count + top missing fields
```

### 10.15 InformationRequestsWidget

```tsx
// components/dashboard/information-requests-widget.tsx
// Admin-only card: 3-stat grid (Pending Review, Overdue, Completed %)
```

### 10.16 GettingStartedCard

```tsx
// components/dashboard/getting-started-card.tsx
// Dismissible onboarding checklist with progress bar
// Auto-hides when dismissed or all complete
// Uses useOnboardingProgress() hook
```

### 10.17 DeadlineWidget

```tsx
// components/dashboard/deadline-widget.tsx
// SWR-powered card: Total/Filed/Pending/Overdue deadline counts for current month
// Module-gated: regulatory_deadlines
// 5-minute refresh interval
```

### 10.18 AutomationsWidget (in components/automations/)

```tsx
// components/automations/automations-widget.tsx
// Admin-only card: Active Rules count, Executions Today, success/fail breakdown
// "Manage Automations" link
```

---

## 11. Org Layout (App Shell)

File: `frontend/app/(app)/org/[slug]/layout.tsx`

```tsx
export default async function OrgLayout({ children, params }) {
  // Auth check, capability fetch, settings fetch
  return (
    <CapabilityProvider>
      <OrgProfileProvider>
      <TerminologyProvider>
      <RecentItemsProvider>
        <CommandPaletteProvider>
        <AssistantProvider>
        <div className="flex min-h-screen">
          <DesktopSidebar slug={slug} groups={groups} />
          <div className="flex flex-1 flex-col">
            <header className="sticky top-0 z-30 flex h-14 items-center gap-4 border-b border-slate-200/60 bg-slate-100/80 px-4 backdrop-blur-md md:px-6 dark:border-slate-800/60 dark:bg-slate-950/90">
              <MobileSidebar slug={slug} groups={groups} />
              <Breadcrumbs slug={slug} />
              <div className="ml-auto flex items-center gap-3">
                <AuthHeaderControls />
                <PlanBadge />
                <NotificationBell orgSlug={slug} />
              </div>
            </header>
            <main id="main-content" className="flex-1 bg-background dark:bg-slate-950">
              <div className="mx-auto max-w-7xl px-6 py-6 lg:px-10">
                <ErrorBoundary>
                  <PageTransition>{children}</PageTransition>
                </ErrorBoundary>
              </div>
            </main>
          </div>
        </div>
        <AssistantPanel slug={slug} orgRole={capData.role} />
        <AssistantTrigger />
        </AssistantProvider>
        </CommandPaletteProvider>
      </RecentItemsProvider>
      </TerminologyProvider>
      </OrgProfileProvider>
    </CapabilityProvider>
  );
}
```

Key layout dimensions:
- Sidebar: `w-60` (240px), `bg-slate-950`
- Header: `h-14`, sticky, `bg-slate-100/80 backdrop-blur-md`
- Content: `max-w-7xl px-6 py-6 lg:px-10`

---

## 12. Org Documents Page (TO BE REMOVED)

File: `frontend/app/(app)/org/[slug]/documents/page.tsx`

```tsx
// Server component that fetches api.get<Document[]>("/api/documents?scope=ORG")
// Renders a table with: File (icon + name), Size, Scope badge, Status badge, Upload date
// Empty state with upload CTA for admins
// Uses OrgDocumentUpload component for file upload
// This page will be REMOVED and the functionality moved to Settings > Organization
```

---

## 13. Settings Pages

### 13.1 Settings Hub (redirects to general)

File: `frontend/app/(app)/org/[slug]/settings/page.tsx`

```tsx
import { redirect } from "next/navigation";

export default async function SettingsPage({ params }) {
  const { slug } = await params;
  redirect(`/org/${slug}/settings/general`);
}
```

### 13.2 Settings > General Page

File: `frontend/app/(app)/org/[slug]/settings/general/page.tsx`

```tsx
// Server component: fetches capabilities + org settings
// Admin/owner only (shows permission denied for others)
// Renders:
//   - VerticalProfileSection (industry vertical selector)
//   - GeneralSettingsForm (currency, logo, brand color, document footer, tax config)
// This is where the Org Documents section will be ADDED
```

### 13.3 Settings ITEMS entry for Organization

Currently in nav-items.ts:
```typescript
{ title: "Organization", description: "Update org name, logo, and details", href: () => "#", comingSoon: true }
```
This needs to be updated to point to a functional page (or the general settings page) and remove `comingSoon: true`.

---

## 14. Document Preview Components

### 14.1 GenerateDocumentDialog (FULL SOURCE)

File: `frontend/components/templates/GenerateDocumentDialog.tsx`

```tsx
"use client";
// Multi-step dialog: clauses → preview
// Preview renders HTML in an iframe:
//   <iframe sandbox="" srcDoc={html} className="h-[500px] w-full bg-white" title="Document Preview" />
// Container: rounded-lg border border-slate-200 (no A4 constraint)
// Actions: Download PDF, Save to Documents, Send for Acceptance
// The iframe is UNCONSTRAINED -- needs A4 proportioning per Phase 53 requirements
```

Key preview rendering code (current):
```tsx
{html && (
  <div className="overflow-hidden rounded-lg border border-slate-200 dark:border-slate-800">
    <iframe
      sandbox=""
      srcDoc={html}
      className="h-[500px] w-full bg-white"
      title="Document Preview"
    />
  </div>
)}
```

**Required change**: Wrap in A4-proportioned container (210:297 ratio), apply CSS `transform: scale()` to fit, add paper shadow effect on dark background.

### 14.2 GenerateDocumentDropdown

File: `frontend/components/templates/GenerateDocumentDropdown.tsx`

```tsx
"use client";
// Dropdown menu listing available templates
// Prerequisite check before opening dialog
// Auto-open via ?generateTemplate=<id> URL param
// Delegates to GenerateDocumentDialog or GenerateDocxDialog
```

### 14.3 GeneratedDocumentsList

File: `frontend/components/templates/GeneratedDocumentsList.tsx`

```tsx
"use client";
// Table listing generated documents: Template Name, Generated By, Date, File Size, Format, Acceptance, Actions
// Actions: Download PDF, Download DOCX, Send for Acceptance, Delete
// Expandable acceptance detail panel
// Uses SWR-like pattern with manual fetch + refresh events
```

---

## 15. My Work Page (FULL SOURCE SUMMARY)

File: `frontend/app/(app)/org/[slug]/my-work/page.tsx`

Current layout:
- **Row 1**: MyWorkHeader (title) + Timesheet link button
- **Row 2**: PersonalKpis (KPI cards for personal metrics)
- **Row 3**: Two-column grid -- TimeBreakdown (donut) + UpcomingDeadlines
- **Row 4**: Three-column grid (2:1 ratio)
  - Left (2/3): MyWorkTasksClient (assigned/unassigned tabs with saved views)
  - Right (1/3): WeeklyTimeSummary, TodayTimeEntries, MyExpenses

Data fetching: all server-side with try/catch non-fatal patterns. Date range defaults to current week (Mon-Sun).

---

## 16. Project Detail Overview Tab (FULL SOURCE SUMMARY)

File: `frontend/components/projects/overview-tab.tsx`

Current layout:
- **Setup cards**: SetupProgressCard, ActionCard (unbilled time), TemplateReadinessCard
- **Health header**: OverviewHealthHeader (project health with reasons)
- **Metrics strip**: OverviewMetricsStrip (budget, hours, members, margin)
- **Two-column body** (lg:grid-cols-2):
  - Left: Tasks card (overdue + upcoming lists)
  - Right: Team Hours card (member bars) + Recent Activity card

Data: all fetched via Promise.allSettled in the async server component.

---

## 17. Frontend Route Structure (Complete)

### App routes (`frontend/app/(app)/org/[slug]/`)
```
dashboard/page.tsx
customers/page.tsx
customers/[id]/page.tsx
projects/page.tsx
projects/[id]/page.tsx
projects/[id]/expenses/page.tsx
documents/page.tsx              <-- TO BE REMOVED
invoices/page.tsx
invoices/[id]/page.tsx
invoices/billing-runs/page.tsx
invoices/billing-runs/[id]/page.tsx
invoices/billing-runs/new/page.tsx
proposals/page.tsx
proposals/[id]/page.tsx
my-work/page.tsx
my-work/timesheet/page.tsx
notifications/page.tsx
profitability/page.tsx
reports/page.tsx
reports/[reportSlug]/page.tsx
resources/page.tsx
resources/utilization/page.tsx
retainers/page.tsx
retainers/[id]/page.tsx
schedules/page.tsx
schedules/[id]/page.tsx
calendar/page.tsx
deadlines/page.tsx
compliance/page.tsx
compliance/requests/page.tsx
compliance/requests/[id]/page.tsx
conflict-check/page.tsx
court-calendar/page.tsx
trust-accounting/page.tsx
information-requests/[id]/page.tsx
team/page.tsx
settings/page.tsx  (redirects to general)
settings/general/page.tsx
settings/billing/page.tsx
settings/rates/page.tsx
settings/notifications/page.tsx
settings/custom-fields/page.tsx
settings/tags/page.tsx
settings/templates/page.tsx
settings/templates/[id]/edit/page.tsx
settings/templates/new/page.tsx
settings/project-templates/page.tsx
settings/project-templates/[id]/page.tsx
settings/checklists/page.tsx
settings/checklists/[id]/page.tsx
settings/checklists/[id]/edit/page.tsx
settings/checklists/new/page.tsx
settings/clauses/page.tsx
settings/acceptance/page.tsx
settings/compliance/page.tsx
settings/data-protection/page.tsx
settings/data-protection/requests/page.tsx
settings/integrations/page.tsx
settings/email/page.tsx
settings/tax/page.tsx
settings/automations/page.tsx
settings/automations/[id]/page.tsx
settings/automations/new/page.tsx
settings/automations/executions/page.tsx
settings/request-settings/page.tsx
settings/request-templates/page.tsx
settings/request-templates/[id]/page.tsx
settings/request-templates/new/page.tsx
settings/capacity/page.tsx
settings/batch-billing/page.tsx
settings/project-naming/page.tsx
settings/time-tracking/page.tsx
settings/roles/page.tsx
```

### Other app routes
```
(app)/dashboard/page.tsx            # Pre-org dashboard (no org selected)
(app)/create-org/page.tsx
(app)/platform-admin/access-requests/page.tsx
(mock-auth)/mock-login/page.tsx
accept/[token]/page.tsx
request-access/page.tsx
portal/(authenticated)/documents/page.tsx
portal/(authenticated)/projects/page.tsx
portal/(authenticated)/projects/[id]/page.tsx
portal/(authenticated)/proposals/page.tsx
portal/(authenticated)/proposals/[id]/page.tsx
portal/(authenticated)/requests/page.tsx
portal/(authenticated)/requests/[id]/page.tsx
portal/(authenticated)/acceptances/page.tsx
portal/(authenticated)/profile/page.tsx
portal/page.tsx
```

---

## 18. Quick Reference Summary

| Resource | Current Max | Next Available |
|----------|-------------|----------------|
| ARCHITECTURE.md section | 10 | 11 |
| ADR number | 204 | 205 |
| Dashboard components | 17 files | -- |
| Frontend routes (org-scoped) | ~70 pages | -- |
| Recharts components | HorizontalBarChart (only one using Recharts directly) | -- |
| Custom SVG charts | SparklineChart, MiniProgressRing, CompletionProgressBar | -- |

### Key Files to Modify (Phase 53)

| File | Change |
|------|--------|
| `frontend/lib/nav-items.ts` | Rename groups, remove Documents, move Proposals |
| `frontend/app/(app)/org/[slug]/dashboard/page.tsx` | Layout restructure |
| `frontend/components/dashboard/*.tsx` | Visual refresh, new components |
| `frontend/app/(app)/org/[slug]/my-work/page.tsx` | Layout restructure |
| `frontend/components/projects/overview-tab.tsx` | Visual refresh |
| `frontend/components/templates/GenerateDocumentDialog.tsx` | A4 preview container |
| `frontend/app/(app)/org/[slug]/documents/page.tsx` | DELETE |
| `frontend/app/(app)/org/[slug]/settings/general/page.tsx` | Add org documents section |
| `frontend/app/globals.css` | Potentially add chart theme variables |

### Files NOT Modified (read-only context)
| File | Why included |
|------|-------------|
| `frontend/components/desktop-sidebar.tsx` | Consumes nav-items.ts (auto-updates) |
| `frontend/components/nav-zone.tsx` | Consumes nav-items.ts (auto-updates) |
| `frontend/app/(app)/org/[slug]/layout.tsx` | App shell context (no changes) |
| `frontend/app/globals.css` | Design token reference |
