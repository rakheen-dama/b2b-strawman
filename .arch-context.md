# Architecture Context Inventory

Snapshot for a downstream architecture-writing agent. Built 2026-04-17 from
`bugfix_cycle_consulting_2026-04-17` branch.

---

## 1. ARCHITECTURE.md Section Numbering

`architecture/ARCHITECTURE.md` H2 headers:

| Line | Header |
|------|--------|
| 3    | `## 1. Overview` |
| 50   | `## 2. Architecture Decision Records` |
| 409  | `## 3. Component Architecture` |
| 704  | `## 4. Sequence Diagrams` |
| 874  | `## 5. Data Flow Diagrams` |
| 980  | `## 6. Security Architecture` |
| 1083 | `## 7. Infrastructure Architecture` |
| 1236 | `## 8. Database Architecture` |
| 1344 | `## 9. Phase 2 — Billing & Tiered Tenancy` |
| 1874 | `## 10. Phase 4 — Customers, Document Scopes & Tasks` |
| 2446 | `## Appendix A: Clerk JWT Claims` |
| 2460 | `## Appendix B: Environment Variables Reference` |

- **Last numbered section in main ARCHITECTURE.md:** `10. Phase 4 — Customers, Document Scopes & Tasks`
- **Next available section number in ARCHITECTURE.md:** `11`
- **Convention (actual):** Since Phase 5 onward, each phase ships as a standalone file `architecture/phase{N}-*.md`; the main ARCHITECTURE.md has not been extended since Phase 4. New phase architecture should be a standalone file, not a new H2 in ARCHITECTURE.md.

### Standalone architecture files in `architecture/`

```
ARCHITECTURE.md
findings-fields-clauses-templates-gaps.md
phase5-task-time-lifecycle.md
phase6-audit-compliance-foundations.md
phase6.5-notifications-comments-activity.md
phase7-customer-portal-backend.md
phase8-rate-cards-budgets-profitability.md
phase9-operational-dashboards.md
phase10-invoicing-billing.md
phase11-tags-custom-fields-views.md
phase12-document-templates.md
phase13-dedicated-schema-only.md
phase14-customer-compliance-lifecycle.md
phase15-contextual-actions-setup-guidance.md
phase16-project-templates-recurring-schedules.md
phase17-retainer-agreements-billing.md
phase18-task-detail-experience-missing-functionality.md
phase19-reporting-data-export.md
phase20-e2e-auth-abstraction.md
phase21-integration-ports-byoak.md
phase22-customer-portal-frontend.md
phase23-custom-field-maturity.md
phase24-outbound-email-delivery.md
phase25-online-payment-collection.md
phase26-invoice-tax-handling.md
phase27-document-clauses.md
phase28-document-acceptance.md
phase29-entity-lifecycle-integrity.md
phase30-expenses-recurring-calendar.md
phase31-document-system-redesign.md
phase32-proposal-engagement-pipeline.md
phase33-data-completeness-prerequisites.md
phase34-client-information-requests.md
phase36-keycloak-gateway-bff.md
phase37-workflow-automations.md
phase38-resource-planning-capacity.md
phase39-admin-approved-provisioning.md
phase40-bulk-billing-batch-operations.md
phase41-org-roles-capabilities.md
phase42-word-template-pipeline.md
phase43-ux-quality-pass.md
phase44-navigation-zones-command-palette-settings.md
phase45-in-app-ai-assistant.md
phase46-rbac-decoupling.md
phase47-vertical-qa-accounting.md
phase48-qa-gap-closure.md
phase49-vertical-architecture.md
phase50-data-protection-compliance.md
phase51-accounting-practice-essentials.md
phase52-in-app-ai-assistant.md
phase53-dashboard-polish-nav-cleanup.md
phase54-keycloak-e2e-test-suite.md
phase55-legal-foundations.md
phase56-production-infrastructure.md
phase57-tenant-subscription-payments.md
phase58-demo-readiness-admin-billing.md
phase59-user-help-documentation.md
phase60-trust-accounting.md
phase61-legal-compliance-refinements.md
phase62-feature-module-gating.md
phase63-custom-field-graduation.md
phase64-legal-vertical-qa.md
phase65-packs-catalog-install-pipeline.md
phase66-consulting-vertical-profile.md
template-multitenant-saas-starter.md
```

- **Highest phase file:** `phase66-consulting-vertical-profile.md`
- **Next phase file for Phase 67:** `architecture/phase67-*.md`
- **Note:** Phase 35 file is missing (rolled back per MEMORY.md). Phase numbering jumps 34 → 36.

---

## 2. ADR Numbering

- **Highest numeric ADR:** `ADR-246-profile-gated-dashboard-widgets.md`
- **Next available numeric ADR:** `ADR-247`
- **Template ADRs:** `ADR-T001`..`ADR-T007` (template-extraction project; separate series, not in sequence with numeric ADRs)

Recent numeric ADRs (last 10):

```
ADR-237-structural-vs-custom-field-boundary.md
ADR-238-entity-type-varchar-vs-enum.md
ADR-239-horizontal-vs-vertical-module-gating.md
ADR-240-unified-pack-catalog-install-pipeline.md
ADR-241-add-only-pack-semantics.md
ADR-242-never-used-uninstall-rule.md
ADR-243-scope-two-pack-types-for-v1.md
ADR-244-pack-only-vertical-profiles.md
ADR-245-localized-profile-derivatives.md
ADR-246-profile-gated-dashboard-widgets.md
```

ADR count: ~246 numeric ADRs + 7 template ADRs.

---

## 3. Migration Numbering

### Tenant migrations (`backend/src/main/resources/db/migration/tenant/`)

- **Highest V number:** `V95__backfill_pack_installs.sql`
- **Next available V:** `V96`

Last 5 tenant migrations:

```
V91__contingency_percent_check_constraints.sql
V92__deactivate_legacy_legal_client_onboarding.sql
V93__deactivate_legacy_legal_client_onboarding_all_variants.sql
V94__create_pack_install.sql
V95__backfill_pack_installs.sql
```

### Global migrations (`backend/src/main/resources/db/migration/global/`)

- **Highest V number:** `V18__subscription_billing_method.sql`
- **Next available V:** `V19`
- Tenant and global migrations have independent V-number sequences (both start at V1).

Last 5 global migrations:

```
V14__portal_requests.sql
V15__create_access_requests.sql
V16__enable_pg_trgm.sql
V17__subscription_payments.sql
V18__subscription_billing_method.sql
```

---

## 4. Entity Inventory — Existing + Relevant to Phase 67

Phase 67 = disbursements, matter closure, statement of account, conveyancing.

### Target existing entities (file paths)

| Entity / file |
|---------------|
| `Expense` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/expense/Expense.java` |
| `ExpenseBillingStatus` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/expense/ExpenseBillingStatus.java` |
| `ExpenseCategory` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/expense/ExpenseCategory.java` |
| `Invoice` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/Invoice.java` |
| `InvoiceLine` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceLine.java` |
| `InvoiceLineType` (enum, discriminator — MANUAL, TIME, EXPENSE, TARIFF, FIXED_FEE, etc.) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceLineType.java` |
| `InvoiceCounter` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceCounter.java` |
| `InvoiceStatus` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceStatus.java` |
| (No distinct `InvoiceLineSource` class — `lineSource` appears to be a string column set on `InvoiceLine`, e.g., `line.setLineSource("TARIFF")`.) |
| `Project` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/project/Project.java` |
| `ProjectStatus` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/project/ProjectStatus.java` |
| `ProjectLifecycleGuard` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/project/ProjectLifecycleGuard.java` |
| `ProjectDeletionGuard` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/project/ProjectDeletionGuard.java` |
| `TrustAccount` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/TrustAccount.java` |
| `TrustAccountStatus` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/TrustAccountStatus.java` |
| `TrustAccountType` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/TrustAccountType.java` |
| `TrustTransaction` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/transaction/TrustTransaction.java` |
| `ClientLedgerCard` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/ledger/ClientLedgerCard.java` |
| `ClientLedgerService` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/ledger/ClientLedgerService.java` |
| `ClientLedgerController` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/ledger/ClientLedgerController.java` |
| `TaxRate` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/tax/TaxRate.java` |
| `TaxType` (enum in invoice/) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/TaxType.java` |
| `TaxCalculationService` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/tax/TaxCalculationService.java` |
| (No `TaxApplication` entity found — tax is applied to `InvoiceLine` via `taxRateId` FK + `InvoiceTaxService`.) |
| `DocumentTemplate` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/template/DocumentTemplate.java` |
| `GeneratedDocument` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/template/GeneratedDocument.java` |
| `Document` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/document/Document.java` |
| `AcceptanceRequest` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/acceptance/AcceptanceRequest.java` |
| `AcceptanceStatus` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/acceptance/AcceptanceStatus.java` |
| `AcceptanceSubmission` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/acceptance/AcceptanceSubmission.java` |
| `RetentionPolicy` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/retention/RetentionPolicy.java` |
| `CourtDate` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/courtcalendar/CourtDate.java` |
| `PrescriptionTracker` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/courtcalendar/PrescriptionTracker.java` |
| `AdverseParty` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/conflictcheck/AdverseParty.java` |
| `AdversePartyLink` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/conflictcheck/AdversePartyLink.java` |
| `ConflictCheck` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/conflictcheck/ConflictCheck.java` |
| `VerticalModuleRegistry` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/VerticalModuleRegistry.java` |
| `VerticalModuleGuard` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/VerticalModuleGuard.java` |
| `VerticalProfileRegistry` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/VerticalProfileRegistry.java` |
| `VerticalProfileService` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/VerticalProfileService.java` |
| `VerticalProfileController` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/VerticalProfileController.java` |
| `ModuleCategory` (enum) — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/ModuleCategory.java` |
| `ModuleStatusResponse` — `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/ModuleStatusResponse.java` |

### Complete @Entity list (101 entities)

Paths relative to `backend/src/main/java/io/b2mash/b2b/b2bstrawman/`:

```
settings/OrgSettings.java
template/DocumentTemplate.java
template/GeneratedDocument.java
packs/PackInstall.java
automation/AutomationRule.java
automation/AutomationAction.java
automation/ActionExecution.java
automation/AutomationExecution.java
automation/FieldDateNotificationLog.java
proposal/Proposal.java
proposal/ProposalTeamMember.java
proposal/ProposalMilestone.java
proposal/ProposalCounter.java
task/Task.java
task/TaskItem.java
project/Project.java
invoice/Invoice.java
invoice/InvoiceLine.java
invoice/InvoiceCounter.java
invoice/PaymentEvent.java
customer/Customer.java
customer/CustomerProject.java
checklist/ChecklistInstanceItem.java
checklist/ChecklistTemplateItem.java
checklist/ChecklistTemplate.java
checklist/ChecklistInstance.java
reporting/ReportDefinition.java
verticals/legal/trustaccounting/investment/TrustInvestment.java
verticals/legal/trustaccounting/interest/InterestAllocation.java
verticals/legal/trustaccounting/interest/InterestRun.java
verticals/legal/trustaccounting/ledger/ClientLedgerCard.java
verticals/legal/trustaccounting/reconciliation/TrustReconciliation.java
verticals/legal/trustaccounting/reconciliation/BankStatement.java
verticals/legal/trustaccounting/reconciliation/BankStatementLine.java
verticals/legal/trustaccounting/transaction/TrustTransaction.java
verticals/legal/trustaccounting/lpff/LpffRate.java
verticals/legal/trustaccounting/TrustAccount.java
verticals/legal/tariff/TariffSchedule.java
verticals/legal/tariff/TariffItem.java
verticals/legal/conflictcheck/ConflictCheck.java
verticals/legal/conflictcheck/AdversePartyLink.java
verticals/legal/conflictcheck/AdverseParty.java
verticals/legal/courtcalendar/PrescriptionTracker.java
verticals/legal/courtcalendar/CourtDate.java
billing/Subscription.java
billing/SubscriptionPayment.java
provisioning/Organization.java
schedule/RecurringSchedule.java
schedule/ScheduleExecution.java
billingrate/BillingRate.java
billingrun/BillingRun.java
billingrun/BillingRunItem.java
billingrun/BillingRunEntrySelection.java
deadline/FilingStatus.java
datarequest/ProcessingActivity.java
datarequest/DataSubjectRequest.java
retention/RetentionPolicy.java
member/Member.java
member/ProjectMember.java
invitation/PendingInvitation.java
orgrole/OrgRole.java
accessrequest/AccessRequest.java
capacity/ResourceAllocation.java
capacity/MemberCapacity.java
capacity/LeaveBlock.java
projecttemplate/ProjectTemplate.java
projecttemplate/TemplateTaskItem.java
projecttemplate/TemplateTask.java
informationrequest/RequestItem.java
informationrequest/RequestCounter.java
informationrequest/InformationRequest.java
informationrequest/RequestTemplateItem.java
informationrequest/RequestTemplate.java
multitenancy/OrgSchemaMapping.java
fielddefinition/FieldDefinition.java
fielddefinition/FieldGroup.java
fielddefinition/FieldGroupMember.java
expense/Expense.java
clause/TemplateClause.java
clause/Clause.java
acceptance/AcceptanceRequest.java
tax/TaxRate.java
integration/email/EmailDeliveryLog.java
integration/OrgIntegration.java
integration/secret/OrgSecret.java
comment/Comment.java
audit/AuditEvent.java
retainer/RetainerPeriod.java
retainer/RetainerAgreement.java
portal/PortalContact.java
portal/MagicLinkToken.java
view/SavedView.java
timeentry/TimeEntry.java
tag/Tag.java
tag/EntityTag.java
notification/NotificationPreference.java
notification/Notification.java
document/Document.java
costrate/CostRate.java
budget/ProjectBudget.java
```

---

## 5. Backend Package Structure

Top-level packages under `backend/src/main/java/io/b2mash/b2b/b2bstrawman/`:

```
acceptance
accessrequest
activity
assistant
audit
automation
billing
billingrate
billingrun
budget
calendar
capacity
checklist
clause
comment
compliance
config
costrate
customer
customerbackend
dashboard
datarequest
deadline
demo
dev
document
event
exception
expense
fielddefinition
informationrequest
integration
invitation
invoice
member
multitenancy
mywork
notification
onboarding
orgrole
packs
portal
prerequisite
project
projecttemplate
proposal
provisioning
report
reporting
retainer
retention
s3
schedule
security
seeder
settings
setupstatus
tag
task
tax
template
timeentry
verticals
view
```
Plus `BackendApplication.java` at root.

### Subpackages under `verticals/`

```
verticals/
├── legal/
├── ModuleCategory.java
├── ModuleStatusResponse.java
├── VerticalModuleGuard.java
├── VerticalModuleRegistry.java
├── VerticalProfileController.java
├── VerticalProfileRegistry.java
└── VerticalProfileService.java
```

### Subpackages under `verticals/legal/`

```
verticals/legal/
├── conflictcheck/
│   ├── AdverseParty.java / AdversePartyLink.java + repos/controllers
│   ├── ConflictCheck.java + repo/controller/service
├── courtcalendar/
│   ├── CourtDate.java / PrescriptionTracker.java + repos/controllers/service
│   ├── CourtDateReminderJob.java
│   └── PrescriptionRuleRegistry.java
├── tariff/
│   ├── TariffSchedule.java / TariffItem.java + repos
│   ├── TariffController.java / TariffService.java
│   └── LegalTariffSeeder.java
└── trustaccounting/
    ├── TrustAccount.java / TrustAccountService / Controller / Repository
    ├── TrustAccountingConstants.java, TrustAccountStatus, TrustAccountType, InvestmentBasis
    ├── event/          # TrustDomainEvent, TrustNotificationHandler, TrustTransactionApprovalEvent
    ├── interest/       # InterestAllocation, InterestRun + repos / service / controller
    ├── investment/     # TrustInvestment + repo / service / controller
    ├── ledger/         # ClientLedgerCard + repo / service / controller
    ├── lpff/           # LpffRate + repo
    ├── reconciliation/ # TrustReconciliation, BankStatement, BankStatementLine + parsers (Absa, Fnb, Nedbank, StandardBank, Generic)
    ├── report/         # Section35, TrustReceiptsPayments, ClientTrustBalances, InvestmentRegister, etc.
    └── transaction/    # TrustTransaction + repo / service / controller
```

---

## 6. Frontend Route Structure

Top-level directories under `frontend/app/(app)/org/[slug]/`:

```
calendar
compliance
conflict-check
court-calendar
customers
dashboard
deadlines
documents
information-requests
invoices
legal
my-work
notifications
profitability
projects
proposals
reports
resources
retainers
schedules
settings
team
trust-accounting
```
Plus `layout.tsx` at root.

### Subdirs under `legal/`

```
legal/
├── adverse-parties
└── tariffs
```

(Conflict-check and court-calendar are top-level routes, not under `legal/`.)

---

## 7. Existing Entity Pattern Reference — `TrustTransaction.java`

Full source of `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/transaction/TrustTransaction.java`:

```java
package io.b2mash.b2b.b2bstrawman.verticals.legal.trustaccounting.transaction;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.GeneratedValue;
import jakarta.persistence.GenerationType;
import jakarta.persistence.Id;
import jakarta.persistence.PrePersist;
import jakarta.persistence.Table;
import java.math.BigDecimal;
import java.time.Instant;
import java.time.LocalDate;
import java.util.UUID;

@Entity
@Table(name = "trust_transactions")
public class TrustTransaction {

  @Id
  @GeneratedValue(strategy = GenerationType.UUID)
  private UUID id;

  @Column(name = "trust_account_id", nullable = false)
  private UUID trustAccountId;

  @Column(name = "transaction_type", nullable = false, length = 20)
  private String transactionType;

  @Column(name = "amount", nullable = false, precision = 15, scale = 2)
  private BigDecimal amount;

  @Column(name = "customer_id")
  private UUID customerId;

  @Column(name = "project_id")
  private UUID projectId;

  @Column(name = "counterparty_customer_id")
  private UUID counterpartyCustomerId;

  @Column(name = "invoice_id")
  private UUID invoiceId;

  @Column(name = "reference", nullable = false, length = 200)
  private String reference;

  @Column(name = "description", columnDefinition = "TEXT")
  private String description;

  @Column(name = "transaction_date", nullable = false)
  private LocalDate transactionDate;

  @Column(name = "status", nullable = false, length = 20)
  private String status;

  @Column(name = "approved_by")
  private UUID approvedBy;

  @Column(name = "approved_at")
  private Instant approvedAt;

  @Column(name = "second_approved_by")
  private UUID secondApprovedBy;

  @Column(name = "second_approved_at")
  private Instant secondApprovedAt;

  @Column(name = "rejected_by")
  private UUID rejectedBy;

  @Column(name = "rejected_at")
  private Instant rejectedAt;

  @Column(name = "rejection_reason", length = 500)
  private String rejectionReason;

  @Column(name = "reversal_of")
  private UUID reversalOf;

  @Column(name = "reversed_by_id")
  private UUID reversedById;

  @Column(name = "bank_statement_line_id")
  private UUID bankStatementLineId;

  @Column(name = "recorded_by", nullable = false)
  private UUID recordedBy;

  @Column(name = "created_at", nullable = false, updatable = false)
  private Instant createdAt;

  protected TrustTransaction() {}

  public TrustTransaction(
      UUID trustAccountId,
      String transactionType,
      BigDecimal amount,
      UUID customerId,
      UUID projectId,
      UUID counterpartyCustomerId,
      String reference,
      String description,
      LocalDate transactionDate,
      String status,
      UUID recordedBy) {
    this.trustAccountId = trustAccountId;
    this.transactionType = transactionType;
    this.amount = amount;
    this.customerId = customerId;
    this.projectId = projectId;
    this.counterpartyCustomerId = counterpartyCustomerId;
    this.reference = reference;
    this.description = description;
    this.transactionDate = transactionDate;
    this.status = status;
    this.recordedBy = recordedBy;
  }

  @PrePersist
  protected void onCreate() {
    this.createdAt = Instant.now();
  }

  public UUID getId() { return id; }
  public UUID getTrustAccountId() { return trustAccountId; }
  public String getTransactionType() { return transactionType; }
  public BigDecimal getAmount() { return amount; }
  public UUID getCustomerId() { return customerId; }
  public UUID getProjectId() { return projectId; }
  public UUID getCounterpartyCustomerId() { return counterpartyCustomerId; }
  public UUID getInvoiceId() { return invoiceId; }
  public String getReference() { return reference; }
  public String getDescription() { return description; }
  public LocalDate getTransactionDate() { return transactionDate; }
  public String getStatus() { return status; }
  public UUID getApprovedBy() { return approvedBy; }
  public Instant getApprovedAt() { return approvedAt; }
  public UUID getSecondApprovedBy() { return secondApprovedBy; }
  public Instant getSecondApprovedAt() { return secondApprovedAt; }
  public UUID getRejectedBy() { return rejectedBy; }
  public Instant getRejectedAt() { return rejectedAt; }
  public String getRejectionReason() { return rejectionReason; }
  public UUID getReversalOf() { return reversalOf; }
  public UUID getReversedById() { return reversedById; }
  public UUID getBankStatementLineId() { return bankStatementLineId; }
  public UUID getRecordedBy() { return recordedBy; }
  public Instant getCreatedAt() { return createdAt; }

  // --- Mutable setters (status lifecycle + reversal linking) ---
  public void setStatus(String status) { this.status = status; }
  public void setReversedById(UUID reversedById) { this.reversedById = reversedById; }
  public void setReversalOf(UUID reversalOf) { this.reversalOf = reversalOf; }
  public void setApprovedBy(UUID approvedBy) { this.approvedBy = approvedBy; }
  public void setApprovedAt(Instant approvedAt) { this.approvedAt = approvedAt; }
  public void setRejectedBy(UUID rejectedBy) { this.rejectedBy = rejectedBy; }
  public void setRejectedAt(Instant rejectedAt) { this.rejectedAt = rejectedAt; }
  public void setRejectionReason(String rejectionReason) { this.rejectionReason = rejectionReason; }
  public void setInvoiceId(UUID invoiceId) { this.invoiceId = invoiceId; }
  public void setSecondApprovedBy(UUID secondApprovedBy) { this.secondApprovedBy = secondApprovedBy; }
  public void setSecondApprovedAt(Instant secondApprovedAt) { this.secondApprovedAt = secondApprovedAt; }
  public void setBankStatementLineId(UUID bankStatementLineId) { this.bankStatementLineId = bankStatementLineId; }
}
```

**Pattern notes for Phase 67 entity authors:**

- No multitenancy boilerplate (`@Filter`, `tenant_id` columns) — schema-per-tenant handles isolation (ADR-064).
- `@Id` + `@GeneratedValue(GenerationType.UUID)` — server-side UUID generation.
- Protected no-arg constructor for Hibernate; public constructor takes required fields only.
- `@PrePersist` sets `createdAt` — no `@EntityListeners`.
- Status stored as `String` (varchar), not `@Enumerated` — ADR-238 explains the choice.
- Currency precision/scale: legal/financial uses `precision = 15, scale = 2`. `Expense` uses `precision = 12, scale = 2` for lighter domains.
- All relationship fields stored as bare `UUID` FKs, no `@ManyToOne` — joins happen at service layer.

Alternative pattern reference: `backend/src/main/java/io/b2mash/b2b/b2bstrawman/expense/Expense.java` — richer pattern with mutation methods (`update`, `writeOff`, `restore`, `markBilled`, `unbill`) that encapsulate state transitions on the entity itself, plus derived status getter (`getBillingStatus()`). Relevant because Phase 67 disbursements = legal-vertical expenses with trust-account linkage.

---

## 8. Recent ADR Format — Full text of ADR-245 and ADR-246

### ADR-245: Localized Profile as Derivative of Generic Profile

```
# ADR-245: Localized Profile as Derivative of Generic Profile

**Status**: Accepted

**Context**:

The vertical profile registry (ADR-181, ADR-189) currently holds three profiles: `accounting-za`, `legal-za`, and `consulting-generic`. Phase 66 adds a fourth, `consulting-za`, creating a situation where two profiles serve the same vertical at different localisation levels — a country-agnostic generic baseline (`consulting-generic`) and a South-African variant (`consulting-za`) that carries ZAR rates, VAT 15%, en-ZA terminology, and SA-specific cultural defaults.

The design question is how these profiles should relate to each other. The platform expects more country variants as the product expands: `consulting-uk`, `consulting-us`, `consulting-au`, and parallel forks for accounting and legal once the product leaves South Africa. Without a clear coexistence model, the registry will either (a) fill up with near-duplicate profiles that copy-paste 80% of the generic pack content and accrue drift, or (b) collapse into a single profile with tenant-level overrides that obscure which content is universal and which is country-specific.

A second concern is discoverability. Platform admins provisioning a new tenant pick a profile from a list. If the list shows five consulting profiles (`consulting-generic`, `consulting-za`, `consulting-uk`, etc.), the naming convention needs to make it obvious which variant a given tenant should pick, and what the generic baseline means if it's still offered.

A third concern is long-term divergence. South African VAT is 15%; UK VAT is 20%; US has no VAT. If every country variant is a copy of the generic, maintaining the generic becomes a chore — every pack-content update has to be manually mirrored across country variants. Conversely, if country variants layer on top of a shared generic baseline, the layering needs a well-understood semantic.

**Options Considered**:

1. **Single generic profile with per-tenant locale / currency overrides** — no country variants; every tenant picks `consulting-generic` and sets locale/currency/tax settings on the tenant record.
   - Pros:
     - One profile to maintain; zero drift
     - Simplest registry
     - Per-tenant flexibility for edge cases (a multinational agency with offices in SA + UK)
   - Cons:
     - Pack content must be locale-neutral — no ZAR rates, no VAT 15% default, no en-ZA terminology, no SA-specific compliance references
     - Tenants must manually configure dozens of settings on day one; defeats the "demo-ready in one click" promise
     - Cannot express country-specific compliance packs (FICA, POPIA) because the profile has no country affinity
     - Forces every country's cultural assumptions into tenant-level config rather than shared defaults

2. **Country variants coexist with a generic baseline, explicit layering via pack references** — `{vertical}-generic` is the universal baseline; `{vertical}-{country}` profiles are derivatives that reference the generic's pack content where it's universal and override with country-specific packs where it isn't.
   - Pros:
     - Universal pack content (e.g., common project templates, standard clauses) lives in generic and is maintained once
     - Country variants are small — they override only what's locale-specific (rates, VAT, compliance, terminology)
     - Generic profile remains usable as a fallback for international tenants or early-access pilots
     - Naming pattern `{vertical}-{country-code}` is self-documenting
     - Future verticals that start generic can graduate to country variants without breaking existing tenants
   - Cons:
     - Two profiles to document per vertical once country variants exist
     - Platform admins must understand the difference at provisioning time
     - Pack-layering semantics need to be documented — which packs are additive, which are replacements

3. **Replace generic with country-specific whenever a country variant exists** — `consulting-generic` is retired the moment `consulting-za` ships; future country variants stand alone with no shared baseline.
   - Pros:
     - No layering model to maintain
     - Each country profile is self-contained and easy to reason about
   - Cons:
     - Content duplication: every country profile copies the generic pack content and then overrides locale-specific bits
     - Drift: an update to one country's project template pack doesn't propagate to the others
     - No fallback profile for international or single-tenant pilots
     - Retiring `consulting-generic` invalidates any test tenants or demos built against it

**Decision**: Option 2 — country variants coexist with a generic baseline, with explicit layering via pack references.

**Rationale**:

**Preserves the generic baseline for international expansion.** When Kazi adds its first UK consulting tenant, `consulting-generic` remains a valid starting point. The UK variant can be added later without disturbing the SA variant. Keeping the generic alive also keeps a country-neutral reference profile useful for testing pack loaders, running demos for prospects who haven't disclosed their country, and bootstrapping future forks.

**Explicit locale layering.** Country variants override exactly the locale-sensitive pack types — rate pack (currency), document templates (VAT number, local terminology), compliance pack (FICA / SARS / CIPC for SA, HMRC for UK, IRS for US), terminology overrides (en-ZA vs. en-GB spellings), and tax defaults. Pack types that are locale-neutral (field definitions for `campaign_type`, `deliverable_type`, the creative brief questionnaire, the 8 standard agency clauses) can either be duplicated (cheap, frozen) or referenced from the generic (harder to evolve but zero-drift).

**Naming pattern.** `{vertical}-{country-code}` layers over `{vertical}-generic`. Country codes use ISO-3166-1 alpha-2 (`za`, `uk`, `us`, `au`, `nz`, `ke`, `ng`). Generic is always the suffix `-generic`. This gives a predictable provisioning UX: SA agency picks `consulting-za`; an international or untyped tenant picks `consulting-generic`; UK will pick `consulting-uk` when it exists.

**Phase 66 specifics.** `consulting-za` duplicates rather than references generic pack content for the slices it overrides — this is the pragmatic v1 choice because the pack loaders do not yet support pack-reference chains. Future platform work can introduce a shared-pack mechanism (an explicit `inherits: "consulting-generic"` field in the manifest) once a second country variant exists and the drift cost is real. Until then, each country variant is self-contained but explicitly names the generic as its ancestor in documentation and status files.

**Multi-tenant cost.** Because pack content is classpath-resident and organisation-agnostic, having `consulting-generic` and `consulting-za` coexist costs only disk space in the JAR. No per-tenant duplication.

**Consequences**:

- `consulting-generic` is preserved, unchanged; `consulting-za` ships alongside it and is the preferred profile for SA agencies.
- Country variants follow the naming pattern `{vertical}-{country-code}`.
- The profile-picker UI (platform-admin provisioning) shows both profiles when they exist. Picking guidance: SA tenant → `consulting-za`; international or unknown → `consulting-generic`.
- Layering is currently implicit — country variants duplicate rather than reference the generic's pack content — but the naming convention leaves room for explicit layering later without breaking existing profiles.
- When the second country variant (`consulting-uk` etc.) lands, a pack-reference mechanism should be considered in its own ADR; until then, duplication is the lower-risk choice.
- Country-specific compliance packs (FICA for SA, equivalents elsewhere) live on the country variant, never on the generic.
- Tax defaults, locale, currency, and terminology key always live on the country variant — the generic is locale-neutral.
- Related: [ADR-181](ADR-181-vertical-profile-structure.md), [ADR-189](ADR-189-vertical-profile-storage.md), [ADR-184](ADR-184-vertical-scoped-pack-filtering.md), [ADR-185](ADR-185-terminology-switching-approach.md), [ADR-244](ADR-244-pack-only-vertical-profiles.md).
```

### ADR-246: Profile-Gated Dashboard Widgets

```
# ADR-246: Profile-Gated Dashboard Widgets

**Status**: Accepted

**Context**:

Phase 66 adds a single piece of UI: a team-utilization widget on the company dashboard. It reads existing Phase 38 `UtilizationService` data — a horizontal feature available to every tenant — and surfaces `teamAverages.avgBillableUtilizationPct` as a KPI card with a 4-week sparkline trend. The widget is intended to be visible only when the active vertical profile is `consulting-za`, because agencies value team billable utilization as a primary operating metric while legal and accounting firms tend to use matter- or deadline-centric KPIs instead.

The mechanical question is how to gate the widget. Kazi has two existing gating primitives:

- **`<ModuleGate>`** — checks `VerticalModuleRegistry` for an enabled module. Used throughout the frontend to hide backend-module-dependent surfaces (trust accounting screens for non-legal tenants, SARS calendar for non-accounting tenants).
- **Profile check (`OrgProfileProvider` + ad-hoc profile ID comparisons)** — reads the active profile ID from context. No first-class hook today; each call site duplicates the boilerplate.

Using `<ModuleGate>` for the utilization widget would require creating a `consulting` or `agency-utilization` module purely to drive the gate — an empty module whose only purpose is to return `true` under `consulting-za`. This is exactly the module sprawl ADR-244 decided against. On the other hand, leaving the widget completely ungated means every profile sees a KPI that doesn't match how non-consulting verticals think about their work, adding visual clutter and implying a priority the tenant doesn't share.

A third option is to ship the widget unconditionally and rely on the dashboard's per-tenant customization to hide it — but customization is a user-action, not a defaults story, and founders of legal/accounting tenants would see a consulting-shaped dashboard on first login. The goal is sensible defaults keyed to the profile.

**Options Considered**:

1. **Create a `consulting` module to gate the widget, same pattern as legal/accounting modules** — treat profile-specific UI surfacings as module territory.
   - Pros:
     - Reuses the existing `<ModuleGate>` primitive; one gating story across the app
     - Module registry is the single source of truth for "is this feature active"
     - Consistent with how legal/accounting modules gate their own dashboard widgets
   - Cons:
     - Requires an empty backend module — no entities, no services, no controllers, just a registration
     - Module sprawl: every piece of profile-specific UI becomes a module, even when the UI is re-framing existing horizontal data
     - Contradicts ADR-244 (`consulting-za` has `enabledModules: []` by design)
     - Raises confusion: is `consulting` a module? It has no backend. Documentation has to explain why

2. **Profile-gate the widget via a small `useProfile()` hook — no new module** — introduce a first-class `useProfile()` helper that reads the active profile ID from `OrgProfileProvider` and lets the widget render conditionally.
   - Pros:
     - Aligns with ADR-244: modules are for backend capabilities, profiles gate UI framing
     - One tiny hook; no backend changes
     - Makes profile-specific UI cheap for future phases — every vertical gets to add re-framed horizontal widgets without needing a backend module
     - Keeps `<ModuleGate>` semantically clean (it's about backend capabilities being on/off)
     - Distinguishes "the data exists and everyone has it" (horizontal feature) from "the data comes from a module that's enabled per tenant" (vertical capability)
   - Cons:
     - Two gating primitives to understand (`<ModuleGate>` for modules, `useProfile()` for profile-specific framing)
     - Engineers must pick the correct gate for the correct situation; wrong choice will work but will drift the architecture
     - Requires a small conceptual distinction to be documented

3. **Ship the widget unconditionally; users hide via dashboard settings** — render for all profiles and rely on user action to suppress.
   - Pros:
     - No gating logic at all
     - Users who value utilization see it regardless of profile
     - Simplest implementation
   - Cons:
     - Wrong defaults for legal and accounting tenants; they see an agency KPI on first login
     - Assumes tenants customise their dashboards, which many never do
     - Mixes vertical semantics on one dashboard; the "company dashboard" stops being vertical-shaped
     - Counter to the Phase 64 lesson that sensible defaults matter more than exposed toggles

**Decision**: Option 2 — profile-gate the widget via a small `useProfile()` hook; no new module.

**Rationale**:

**Modules are for backend capabilities; profiles are for UI framing.** ADR-244 nailed the module semantic down: a module exists when a vertical introduces a backend primitive the horizontal stack can't express. The utilization widget is the opposite — it surfaces existing horizontal data with vertical-specific framing. Gating it via a module would force us to create a backend-less module solely to drive a frontend gate, undoing ADR-244 the same day it ships.

**Small, reusable pattern.** A `useProfile()` hook is cheap. It reads `OrgProfileProvider` context and returns the active profile ID. Any future widget that wants to surface horizontal data with vertical-specific framing can use the same hook. This is a pattern we expect to reuse as more verticals ship:

- A matter-load widget for `legal-za` (re-framing existing project-count data)
- A filing-pipeline widget for `accounting-za` (re-framing existing deadline data)
- A headcount-velocity widget for a future recruitment vertical (re-framing existing member-activity data)

Each of these is a re-skinning of horizontal data, not a new capability. Profile-gating is the right seam.

**Clear semantic separation.** With Option 2, the rule is:

- *Backend capability visible / hidden per tenant?* → use `<ModuleGate>`
- *Horizontal feature re-framed for a specific profile's worldview?* → use `useProfile()`

This keeps `<ModuleGate>` honest. A `<ModuleGate>` check that returns `true` for `consulting-za` despite no actual module existing would be a lie in the type system.

**Discoverability for other engineers.** Widgets adopting profile-gating are visually distinct in the code: a leading `if (profile !== "consulting-za") return null;` (or equivalent `<ProfileGate profile="...">`) reads as a UI-level decision, not a capability decision.

**Multi-tenant cost.** Zero. The hook reads existing context; there's no backend change, no registry change, no per-tenant cost.

**Consequences**:

- A new frontend hook `useProfile()` lives at `frontend/lib/hooks/useProfile.ts` (if not already present); it reads from `OrgProfileProvider` and returns the profile ID.
- The team-utilization widget renders conditionally on `profile === "consulting-za"`; other profiles see nothing.
- Future profile-specific dashboard widgets follow the same pattern. A small `<ProfileGate>` wrapper component may emerge once 2–3 widgets adopt the hook; optional and out of scope for Phase 66.
- Documentation (frontend CLAUDE.md or a component README) spells out the rule: `<ModuleGate>` for modules, `useProfile()` for profile-specific framing.
- `<ModuleGate>` remains unchanged; this ADR does not modify its behaviour. It continues to gate backend-module-dependent surfaces unchanged.
- The `consulting-za` profile keeps `enabledModules: []` — no module is created for the widget.
- Related: [ADR-181](ADR-181-vertical-profile-structure.md), [ADR-192](ADR-192-enabled-modules-authority.md), [ADR-239](ADR-239-horizontal-vs-vertical-module-gating.md), [ADR-244](ADR-244-pack-only-vertical-profiles.md).
```

**Format takeaways for the architect:**
- H1 = `# ADR-{N}: {Title in Title Case}`
- Required H2 sections in order: `**Status**`, `**Context**`, `**Options Considered**`, `**Decision**`, `**Rationale**`, `**Consequences**`
- Status values observed: `Accepted`
- Options Considered is a numbered list; each option has bold title + em-dash tagline + Pros / Cons bullets
- Decision line begins with "Option N — <restated decision>"
- Rationale uses `**Bold lead-in phrase.**` sub-paragraphs
- Consequences is a bullet list; last bullet always `Related: [ADR-NNN](...)` cross-links.

---

## 9. Pack Loader Patterns

### Pack directories under `backend/src/main/resources/`

**`field-packs/*.json`** — 9 files:
```
accounting-za-customer-trust.json
accounting-za-customer.json
accounting-za-project.json
common-project.json
common-task.json
consulting-za-customer.json
consulting-za-project.json
legal-za-customer.json
legal-za-project.json
```

**`clause-packs/`** — 4 directories:
```
accounting-za-clauses
consulting-za-clauses
legal-za-clauses
standard-clauses
```

**`template-packs/`** — 5 directories:
```
accounting-za
common
compliance-za
consulting-za
legal-za
```

**`project-template-packs/*.json`** — 3 files:
```
accounting-za.json
consulting-za.json
legal-za.json
```

**`request-packs/*.json`** — 6 files:
```
annual-audit.json
company-registration.json
consulting-za-creative-brief.json
monthly-bookkeeping.json
tax-return.json
year-end-info-request-za.json
```

**`vertical-profiles/*.json`** — 4 files:
```
accounting-za.json
consulting-generic.json
consulting-za.json
legal-za.json
```

### Other pack-adjacent directories (not asked but relevant)

- `compliance-packs/` — 6 subdirs: `fica-kyc-za`, `generic-onboarding`, `legal-za-individual-onboarding`, `legal-za-trust-onboarding`, `sa-fica-company`, `sa-fica-individual`
- `rate-packs/*.json` — 3: `accounting-za.json`, `consulting-generic.json`, `consulting-za.json`
- `schedule-packs/*.json` — 1: `accounting-za.json`
- `automation-templates/*.json` — 4: `accounting-za.json`, `common.json`, `consulting-za.json`, `legal-za.json`
- `tariff-seed/` — exists, legal-za tariff schedules seeded via `LegalTariffSeeder`
- `assistant/`, `templates/` — also exist

### Pack installer / seeder classes

Under `backend/src/main/java/io/b2mash/b2b/b2bstrawman/packs/`:

```
AutomationPackInstaller.java
ContentHashUtil.java
PackCatalogController.java
PackCatalogEntry.java
PackCatalogService.java
PackInstall.java                 (@Entity)
PackInstaller.java               (interface)
PackInstallRepository.java
PackInstallResponse.java
PackInstallService.java
PackType.java
TemplatePackInstaller.java
UninstallCheck.java
```

Under `backend/src/main/java/io/b2mash/b2b/b2bstrawman/seeder/`:

```
AbstractPackSeeder.java
ProjectTemplatePackDefinition.java
ProjectTemplatePackSeeder.java
RatePackDefinition.java
RatePackSeeder.java
SchedulePackDefinition.java
SchedulePackSeeder.java
```

Additional seeders elsewhere:
- `backend/src/main/java/io/b2mash/b2b/b2bstrawman/template/TemplatePackSeeder.java`
- `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/tariff/LegalTariffSeeder.java`
- `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/report/TrustReportPackSeeder.java`

---

## 10. Capability Pattern

**Key finding:** The codebase uses `@RequiresCapability("NAME")` (a custom annotation in `orgrole/`) — NOT `@PreAuthorize("@capabilities.has(...)")`. That Spring SpEL form yields zero matches anywhere.

Enforcement flow:
- `orgrole/RequiresCapability.java` — annotation
- `orgrole/CapabilityAuthorizationManager.java` — reflection-based interceptor
- `orgrole/CapabilityAuthorizationService.java` — resolves member → role → capability set
- `SecurityConfig.java` comment: "Runs after `@PreAuthorize` so both can coexist"

`@PreAuthorize` is reserved for platform-admin checks:
```
@PreAuthorize("@platformSecurityService.isPlatformAdmin()")
```
on `PlatformBillingController`, `PlatformAdminController`, `OrgController`, `DemoAdminController`, and `AssistantController` (which uses `@PreAuthorize("isAuthenticated()")`).

Example `@RequiresCapability` usages:

```
verticals/VerticalProfileController.java:25          @RequiresCapability("TEAM_OVERSIGHT")
verticals/VerticalProfileController.java:32          @RequiresCapability("TEAM_OVERSIGHT")
fielddefinition/FieldDefinitionController.java:47    @RequiresCapability("TEAM_OVERSIGHT")
fielddefinition/FieldDefinitionController.java:56    @RequiresCapability("TEAM_OVERSIGHT")
fielddefinition/FieldDefinitionController.java:63    @RequiresCapability("TEAM_OVERSIGHT")
fielddefinition/FieldDefinitionController.java:70    @RequiresCapability("TEAM_OVERSIGHT")
fielddefinition/FieldDefinitionController.java:82    @RequiresCapability("TEAM_OVERSIGHT")
compliance/CompliancePackController.java:21          @RequiresCapability("CUSTOMER_MANAGEMENT")
verticals/legal/conflictcheck/ConflictCheckController.java:34  @RequiresCapability("MANAGE_LEGAL")
verticals/legal/conflictcheck/ConflictCheckController.java:42  @RequiresCapability("VIEW_LEGAL")
verticals/legal/conflictcheck/ConflictCheckController.java:55  @RequiresCapability("VIEW_LEGAL")
verticals/legal/conflictcheck/ConflictCheckController.java:61  @RequiresCapability("MANAGE_LEGAL")
verticals/legal/conflictcheck/AdversePartyController.java:37   @RequiresCapability("VIEW_LEGAL")
verticals/legal/conflictcheck/AdversePartyController.java:46   @RequiresCapability("VIEW_LEGAL")
verticals/legal/conflictcheck/AdversePartyController.java:52   @RequiresCapability("MANAGE_LEGAL")
```

**Capability string vocabulary observed** (partial): `TEAM_OVERSIGHT`, `CUSTOMER_MANAGEMENT`, `MANAGE_LEGAL`, `VIEW_LEGAL`. A complete enumeration lives in `orgrole/Capability.java` (fixed set per ADR-162).

For Phase 67 (legal): expect to use `VIEW_LEGAL` for read endpoints, `MANAGE_LEGAL` for writes. Disbursement/conveyancing may warrant new capabilities — check `Capability.java` before declaring.

---

## 11. Domain Event Pattern

Events live under `event/` package as plain Java classes (likely records, though not verified with file contents).

### Full event inventory — `backend/.../event/` (40 events)

```
AcceptanceRequestAcceptedEvent
AcceptanceRequestExpiredEvent
AcceptanceRequestRevokedEvent
AcceptanceRequestSentEvent
AcceptanceRequestViewedEvent
BudgetThresholdEvent
CommentCreatedEvent
CommentDeletedEvent
CommentUpdatedEvent
CommentVisibilityChangedEvent
CustomerStatusChangedEvent
DocumentGeneratedEvent
DocumentUploadedEvent
DomainEvent                       (base interface/class)
ExpenseCreatedEvent
ExpenseDeletedEvent
FieldDateApproachingEvent
InformationRequestCancelledEvent
InformationRequestCompletedEvent
InformationRequestDraftCreatedEvent
InformationRequestSentEvent
InvoiceApprovedEvent
InvoicePaidEvent
InvoiceSentEvent
InvoiceVoidedEvent
MemberAddedToProjectEvent
ProjectArchivedEvent
ProjectCompletedEvent
ProjectReopenedEvent
ProposalSentEvent
RequestItemAcceptedEvent
RequestItemRejectedEvent
RequestItemSubmittedEvent
TaskAssignedEvent
TaskCancelledEvent
TaskClaimedEvent
TaskCompletedEvent
TaskRecurrenceCreatedEvent
TaskReopenedEvent
TaskStatusChangedEvent
TimeEntryChangedEvent
```

Additional vertical-scoped event subtree: `verticals/legal/trustaccounting/event/`
- `TrustDomainEvent`
- `TrustTransactionApprovalEvent`
- `TrustNotificationHandler` (listener)

### Publishing pattern

Publisher field name varies by class: `eventPublisher` or `applicationEventPublisher` — both are `ApplicationEventPublisher` injections. Examples:

```
compliance/CustomerLifecycleService.java:189       eventPublisher.publishEvent(
compliance/CustomerLifecycleService.java:299       eventPublisher.publishEvent(
member/ProjectMemberService.java:87                eventPublisher.publishEvent(
timeentry/TimeEntryBatchService.java:190           applicationEventPublisher.publishEvent(
timeentry/TimeEntryService.java:525                applicationEventPublisher.publishEvent(
expense/ExpenseService.java:494                    applicationEventPublisher.publishEvent(
expense/ExpenseService.java:514                    applicationEventPublisher.publishEvent(
document/DocumentService.java:244                  eventPublisher.publishEvent(
document/DocumentService.java:285                  eventPublisher.publishEvent(
document/DocumentService.java:301                  eventPublisher.publishEvent(
```

### Phase 67 candidate events (not yet present — architect can introduce)

- `DisbursementRecordedEvent`, `DisbursementApprovedEvent`, `DisbursementBilledEvent`
- `MatterClosedEvent` (likely distinct from `ProjectCompletedEvent` for legal semantics)
- `StatementOfAccountGeneratedEvent`
- `ConveyancingMilestoneReachedEvent`

Follow existing synchronous-fanout pattern (ADR-036 / ADR-037).

---

## 12. Unbilled-Items / Invoice Generation

### Orchestrator classes

| Role | Class | File |
|------|-------|------|
| HTTP entrypoint | `InvoiceController` | `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceController.java` |
| Facade | `InvoiceService` | `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceService.java` |
| **Creation/draft orchestrator** | `InvoiceCreationService` | `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceCreationService.java` |
| Lifecycle transitions | `InvoiceTransitionService` | `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/InvoiceTransitionService.java` |
| Unbilled queries | `UnbilledTimeService` | `backend/src/main/java/io/b2mash/b2b/b2bstrawman/invoice/UnbilledTimeService.java` |
| Bulk batch generation | `BillingRunService`, `BillingRunSelectionService` | `backend/src/main/java/io/b2mash/b2b/b2bstrawman/billingrun/` |

### Primary generation entry-point

```java
@Transactional
public InvoiceResponse InvoiceCreationService.createDraft(CreateInvoiceRequest request, UUID createdBy)
```

Driven by `InvoiceController.createDraft(CreateInvoiceRequest, ...)`. Within `createDraft`:

1. Validates via private `validateInvoicePrerequisites(UUID customerId)` — ACTIVE status, `CustomerLifecycleGuard.requireActionPermitted(customer, LifecycleAction.CREATE_INVOICE)`, `PrerequisiteService.checkForContext(PrerequisiteContext.INVOICE_GENERATION, ...)`.
2. Constructs `Invoice` (DRAFT status, currency, customer snapshot, org snapshot).
3. Saves invoice.
4. `createTimeEntryLines(invoice, request.timeEntryIds(), customerId, currency)` — loops `timeEntryRepository.findById(id)` for each explicit ID.
5. `createExpenseLines(invoice, request.expenseIds(), customerId, sortOffset)` — loops `expenseRepository.findById(id)` for each explicit ID.
6. `invoiceTaxService.applyDefaultTaxToLines(invoice.getId())`.
7. `invoiceTaxService.recalculateInvoiceTotals(invoice)`.
8. Field groups + audit event.

### How expenses are sourced

`InvoiceCreationService` accepts a list of explicit `expenseIds` from the request — it does **not** call `ExpenseService.listUnbilled`. It calls `expenseRepository.findById(id)` per ID. The caller (UI, API client, or `BillingRunService`) decides which unbilled expenses to include.

Unbilled discovery is separate:

- `UnbilledTimeService.getUnbilledTime(UUID customerId, LocalDate from, LocalDate to)` — returns `UnbilledTimeResponse` grouping both time entries and expenses for a customer (line 313 calls `expenseRepository.findUnbilledBillableByCustomerId(customerId)`).
- `UnbilledTimeService.getUnbilledSummary(...)` — returns customer-level summaries for billing runs.
- `InvoiceService.getUnbilledTime(...)` / `getUnbilledSummary(...)` — thin façade methods delegating to `UnbilledTimeService`.
- `ExpenseRepository.findUnbilledBillableByCustomerId(UUID customerId)` — query method, expense-side counterpart.
- `ProjectController.getUnbilledSummary(@PathVariable UUID id)` returns `UnbilledTimeSummary` per-project.
- `CustomerController.getUnbilledTime(...)` exposes the same façade method on the customer route.

### Time-entry + expense billing contract

There is **no shared interface** between time entries and expenses in the billing flow. They share a domain convention but not a polymorphic contract:

- Both have `UUID invoiceId` FK column (nullable).
- Both have `billable: boolean` field.
- `Expense` encapsulates its billing status as a derived enum `ExpenseBillingStatus {BILLED, NON_BILLABLE, UNBILLED}` via `getBillingStatus()`; `TimeEntry` uses a separate enum `timeentry/BillingStatus.java`.
- `Expense` has mutation methods: `markBilled(UUID invoiceId)`, `unbill()`, `writeOff()`, `restore()`.
- `TimeEntry` uses a direct setter: `timeEntry.setInvoiceId(invoice.getId())` (seen in `InvoiceCreationService.createTimeEntryLines` line 723).
- `InvoiceLineType` acts as the discriminator on generated lines: `TIME`, `EXPENSE`, `MANUAL`, `TARIFF`, `FIXED_FEE` (see ADR-118).

For Phase 67 disbursements: the natural seam is to either (a) extend `Expense` with a `disbursement`/`soft-cost` category + trust-account linkage, or (b) introduce `LegalDisbursement` as a sibling entity with an identical `markBilled / unbill` contract. No shared `Billable` interface exists today — establishing one now may be a good architectural move, but is not the current convention. ADR-114 (expense billing status derivation) and ADR-115 (expense markup model) are the governing prior art.

### Batch / bulk flow (parallel to single-invoice `createDraft`)

`billingrun/BillingRunService` + `BillingRunSelectionService` orchestrate multi-customer batch invoice generation. `BillingRunItem` carries unbilled aggregates per customer. This is the architecturally cleaner place for any "auto-generate invoices from unbilled disbursements + time" workflow in Phase 67.

### Tax application hook

`InvoiceTaxService` is called twice in `createDraft`:
- `applyDefaultTaxToLines(invoiceId)` — applies default tax rate to newly-created lines
- `recalculateInvoiceTotals(invoice)` — computes subtotal + tax + total

`TaxRate` entity (`tax/TaxRate.java`) is tenant-scoped, immutable per ADR-103. `InvoiceLine.taxRateId` is the FK; there is no separate `TaxApplication` entity.
