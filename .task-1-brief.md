# Task 1 Brief: Extract MemberCacheService from MemberFilter

## Files to Change

### New: `backend/src/main/java/io/b2mash/b2b/b2bstrawman/member/MemberCacheService.java`

### Modify: `MemberFilter.java`
- **Lines 36-37**: Remove `Cache<String, MemberInfo> memberCache` field + Caffeine builder
- **Line 90**: Remove `private record MemberInfo(UUID memberId, String orgRole) {}`
- **Lines 86-88**: Remove `evictFromCache()` method
- **Lines 106-109**: Replace `memberCache.get(cacheKey, k -> ...)` with `memberCacheService.get(tenantId, clerkUserId)` / `memberCacheService.put()`
- Add `MemberCacheService` constructor injection

### Modify: `MemberSyncService.java`
- **Line 136**: `memberFilter.evictFromCache(schemaName, clerkUserId)` → `memberCacheService.evict(schemaName, clerkUserId)`
- **Line 170**: Same pattern for `deleteMember()` path
- Add `MemberCacheService` constructor injection

## MemberCacheService Design

```java
@Service
public class MemberCacheService {
    public record MemberInfo(UUID memberId, String orgRole) {}

    private final Cache<String, MemberInfo> cache = Caffeine.newBuilder()
            .maximumSize(50_000)
            .expireAfterWrite(Duration.ofHours(1))
            .build();

    private static String key(String tenantId, String userId) {
        return tenantId + ":" + userId;
    }

    public MemberInfo get(String tenantId, String userId) {
        return cache.getIfPresent(key(tenantId, userId));
    }

    public void put(String tenantId, String userId, MemberInfo info) {
        cache.put(key(tenantId, userId), info);
    }

    public void evict(String tenantId, String userId) {
        cache.invalidate(key(tenantId, userId));
    }

    public void evictAll() {
        cache.invalidateAll();
    }
}
```

## Test files (no changes needed — integration tests validate behavior indirectly)
- `MemberFilterIntegrationTest.java` — validates cache through side effects
- `MemberSyncIntegrationTest.java` — validates eviction through endpoint responses
- `MemberFilterJitSyncTest.java`

## Risks
- MemberInfo record must become public (was private in MemberFilter)
- MemberFilter uses `memberCache.get(cacheKey, loader)` (atomic load) — switch to get/put pattern since resolver logic stays in MemberFilter
- Keep MemberFilter's `resolveOrCreateMember()` method intact — just delegate cache storage

## Verification
```bash
cd backend && ./mvnw clean verify -q 2>&1 | tail -20
```
