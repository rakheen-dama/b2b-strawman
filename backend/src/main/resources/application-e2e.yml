spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://mock-idp:8090/.well-known/jwks.json

internal:
  api:
    key: e2e-test-api-key

integration:
  encryption-key: "+kW8MHOTHhm4Cq+FCJG3Gk1rsJ6s87v0gdC7oZYYo4o="

cors:
  allowed-origins:
    - http://localhost:3000
    - http://localhost:3001
    - http://localhost:3002

portal:
  jwt:
    secret: e2e-test-portal-secret-must-be-at-least-32-bytes-long
  magic-link:
    secret: e2e-test-magic-link-secret-must-be-at-least-32-bytes
