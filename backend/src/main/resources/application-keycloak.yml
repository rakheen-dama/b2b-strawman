# Keycloak profile configuration
# Activated via: SPRING_PROFILES_ACTIVE=keycloak
# TODO: Add Keycloak-specific properties (issuer-uri, JWKS endpoint) in a subsequent epic

app:
  jit-provisioning:
    enabled: true
