parent=base
import=common/keycloak

# Frontend bounce base for the org-invite link (org-invite.ftl). Env-driven so
# local dev defaults to localhost:3000 and the VPS/prod deploy overrides via
# KC_INVITE_BOUNCE_BASE_URL. Resolves GAP-L-01's hard-coded-host TODO.
bounceBase=${env.KC_INVITE_BOUNCE_BASE_URL:http://localhost:3000/accept-invite}
