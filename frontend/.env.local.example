
# Auth mode: "keycloak" (production/local dev) or "mock" (E2E only)
NEXT_PUBLIC_AUTH_MODE=keycloak

# API Gateway URL (Keycloak BFF)
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8443
GATEWAY_URL=http://localhost:8443

# Keycloak origin + realm + client (used by the /accept-invite bounce page)
NEXT_PUBLIC_KEYCLOAK_URL=http://localhost:8180
NEXT_PUBLIC_KEYCLOAK_REALM=docteams
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=gateway-bff

# Public app URL — used as post_logout_redirect_uri by the invite bounce page
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend API URL (server-side, for internal API calls)
BACKEND_URL=http://localhost:8080

# Internal API Key (must match backend's internal.api.key)
INTERNAL_API_KEY=local-dev-api-key-change-in-production

# Documentation site URL (for "Read the guide" / "Learn more" links)
NEXT_PUBLIC_DOCS_URL=http://localhost:3003
