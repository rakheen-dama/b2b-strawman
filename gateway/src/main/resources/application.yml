server:
  port: 8443
  servlet:
    session:
      cookie:
        http-only: true
        secure: false
        same-site: lax
        name: SESSION

spring:
  application:
    name: gateway

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:app}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:changeme}

  session:
    store-type: jdbc
    jdbc:
      # TODO: Use Flyway migration for session schema in production
      initialize-schema: always
      table-name: SPRING_SESSION
    timeout: 8h

  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: ${KEYCLOAK_CLIENT_ID:gateway-bff}
            client-secret: ${KEYCLOAK_CLIENT_SECRET:docteams-web-secret}
            scope: openid,profile,email,organization
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          keycloak:
            issuer-uri: ${KEYCLOAK_ISSUER:http://localhost:8180/realms/docteams}

  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: backend-api
              uri: ${BACKEND_URL:http://localhost:8080}
              predicates:
                - Path=/api/**
              filters:
                - TokenRelay=

  jmx:
    enabled: false

  threads:
    virtual:
      enabled: true

gateway:
  frontend-url: ${FRONTEND_URL:http://localhost:3000}

keycloak:
  admin:
    url: ${KEYCLOAK_ADMIN_URL:http://localhost:8180}
    realm: ${KEYCLOAK_ADMIN_REALM:docteams}
    # Master realm admin credentials for Admin REST API access (password grant)
    auth-server-url: ${KEYCLOAK_ADMIN_URL:http://localhost:8180}
    username: ${KEYCLOAK_ADMIN_USERNAME:admin}
    password: ${KEYCLOAK_ADMIN_PASSWORD:admin}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: when-authorized
