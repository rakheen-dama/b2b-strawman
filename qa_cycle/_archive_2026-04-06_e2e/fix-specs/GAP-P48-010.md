# GAP-P48-010: Settings Rates Page Returns 404 Instead of Permission Denied

## Decision: SPEC_READY

## Effort: S (15 minutes)

## Problem

When Carol (Member role, no FINANCIAL_VISIBILITY capability) navigates to `/settings/rates`, she gets a generic Next.js 404 page. The page uses `notFound()` for unauthorized users. The `/settings/general` page shows a proper permission message instead. The QA script at step 90.16 expects a permission error.

## Fix

In `frontend/app/(app)/org/[slug]/settings/rates/page.tsx`, replace the `notFound()` call with an inline permission-denied message, matching the pattern from `/settings/general/page.tsx`.

Replace this block (lines 18-20):
```tsx
if (!capData.isAdmin && !capData.isOwner && !capData.capabilities.includes("FINANCIAL_VISIBILITY")) {
    notFound();
}
```

With this pattern (matching general settings):
```tsx
if (!capData.isAdmin && !capData.isOwner && !capData.capabilities.includes("FINANCIAL_VISIBILITY")) {
    return (
      <div className="space-y-8">
        <Link
          href={`/org/${slug}/settings`}
          className="inline-flex items-center gap-1 text-sm text-slate-600 hover:text-slate-900 dark:text-slate-400 dark:hover:text-slate-100"
        >
          <ChevronLeft className="size-4" />
          Settings
        </Link>
        <h1 className="font-display text-3xl text-slate-950 dark:text-slate-50">
          Rates & Currency
        </h1>
        <p className="text-slate-600 dark:text-slate-400">
          You do not have permission to manage rates and currency settings.
          Only admins and owners can access this page.
        </p>
      </div>
    );
}
```

Note: `Link` and `ChevronLeft` are already imported in the file.

## Files to Modify

| File | Action |
|------|--------|
| `frontend/app/(app)/org/[slug]/settings/rates/page.tsx` | Replace `notFound()` with permission-denied message |

## Verification

1. Login as Carol (member)
2. Navigate to `/settings/rates`
3. Verify the page shows "You do not have permission to manage rates and currency settings" instead of a 404 page
4. Verify the "Settings" back link works
