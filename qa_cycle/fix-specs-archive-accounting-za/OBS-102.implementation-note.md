# OBS-102 — Implementation Note

## PR
- **Number**: #1225
- **Title**: `fix(OBS-102): add Trust Accounting to Settings sidebar`
- **Base**: `main`
- **Head**: `fix/OBS-102` (deleted post-merge)
- **Strategy**: squash-merge
- **Merge commit on main**: `53339d959e21a0a3bb79a9fdcc983810d8c0a1de`
- **Merged**: 2026-04-30 (UTC 23:25)
- **URL**: https://github.com/rakheen-dama/b2b-strawman/pull/1225

## Files Changed
- `frontend/components/settings/settings-nav-groups.ts` — +6 / -0

Diff:
```diff
@@ -63,6 +63,12 @@ export const SETTINGS_NAV_GROUPS: SettingsNavGroup[] = [
         requiredModule: "bulk_billing",
       },
       { label: "Capacity", href: "capacity" },
+      {
+        label: "Trust Accounting",
+        href: "trust-accounting",
+        adminOnly: true,
+        requiredModule: "trust_accounting",
+      },
     ],
   },
```

## Verification (frontend, pre-merge)
| Check | Result | Evidence |
|-------|--------|----------|
| `pnpm install` | OK | `/tmp/install-obs102.log` (5.8s) |
| `pnpm run lint` | OK (0 errors) | `/tmp/lint-obs102.log` — 96 pre-existing unused-var warnings only; no errors introduced |
| `pnpm run build` | OK | `/tmp/build-obs102.log` — Next.js 16 production build successful, route map still includes `/org/[slug]/settings/trust-accounting` |
| `pnpm test` (Vitest) | OK | `/tmp/test-obs102.log` — 339 files / 2119 passed / 2 skipped |

All on a single attempt — no retries needed.

## Behaviour
- Owner / Admin in a tenant where `enabledModules` includes `trust_accounting` (i.e. `legal-za` vertical) will now see **Trust Accounting** as the last entry in the **Finance** group of the Settings left rail.
- Member-role users will not see the link (matches page-level `caps.isAdmin || caps.isOwner` guard at `app/(app)/org/[slug]/settings/trust-accounting/page.tsx:36`).
- Tenants without the `trust_accounting` module (accounting, consulting, etc.) will not see the link (matches page-level module guard at line 30).
- The sidebar component (`frontend/components/settings/settings-sidebar.tsx`) already filters on both flags — no other code changes were required.

## Next Steps (orchestrator)
1. Pull main into `bugfix_cycle_2026-04-30` so the running Keycloak-mode frontend HMR picks up the change.
2. Dispatch QA agent to re-run Day 1 checkpoint 1.5: log in as Thandi (Owner) at `mathebula-partners`, open Settings, confirm `Trust Accounting` is visible under Finance and links to `/org/mathebula-partners/settings/trust-accounting`.
3. (Optional) verify the link is hidden for Carol (Member) and for a non-legal tenant.
4. Flip OBS-102 from FIXED → VERIFIED in `qa_cycle/status.md` and proceed to Day 2.

## Notes
- Frontend HMR means **no service restart required** after the orchestrator merges main back into the bugfix branch.
- No backend or migration changes — Keycloak/Postgres state is unaffected.
- Pre-existing test stderr noise (`blob:http://localhost/...` from happy-dom) is unrelated to this fix and present on main.
