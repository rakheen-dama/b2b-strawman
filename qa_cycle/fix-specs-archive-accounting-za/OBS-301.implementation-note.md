# OBS-301 Implementation Note

**PR**: https://github.com/rakheen-dama/b2b-strawman/pull/1227
**Merge SHA**: `48656aa7ba4aeb65580536936fab1d0b0f95c509`
**Merged**: 2026-04-30
**Branch**: `fix/OBS-301` → `main` (squash, branch deleted)

## Files changed (4)

1. `backend/src/main/java/io/b2mash/b2b/b2bstrawman/projecttemplate/dto/InstantiateTemplateRequest.java`
   — `@Size(max = 255) String description` → `@Size(max = 2000) String description`. Single-character constant change.
2. `backend/src/test/java/io/b2mash/b2b/b2bstrawman/projecttemplate/ProjectTemplateControllerTest.java`
   — added `shouldInstantiateTemplateWithLongDescription` (`@Order(215)`): POSTs `/api/project-templates/{id}/instantiate` with a 1500-char description and asserts `201 Created` + `$.description == longDescription`. Order `215` to slot the new test inside the existing instantiate-tests cluster (`@Order(20-23)`) without disturbing relative order of pre-existing tests.
3. `frontend/app/(app)/org/[slug]/settings/project-templates/actions.ts`
   — extended `instantiateTemplateAction` return type with `fieldErrors?: Array<{ field: string; message: string }>`. In the `error.status === 400` branch, pulls `error.detail?.fieldErrors` (cast to the expected shape) and threads it through alongside the existing `error` string. No callers other than `NewFromTemplateDialog` consume this action; safe additive change.
4. `frontend/components/templates/NewFromTemplateDialog.tsx`
   — added `fieldErrors` state (`Record<string, string>`); reset on dialog open and on entry to `doCreateProject`; on a non-success result with `fieldErrors`, collapse the `[{field, message}]` array into the record. Renders an inline `<p className="text-destructive text-xs mt-1">` under name / description / referenceNumber / workType inputs when the corresponding key exists in the map. The existing top-level `{error && <p>...</p>}` banner is preserved as a fallback for non-validation errors.

## Verification (all green pre-PR)

| Check | Command | Result |
|-------|---------|--------|
| Backend spotless | `./mvnw spotless:apply -q` | exit 0 |
| Backend compile | `./mvnw compile test-compile -q` | exit 0 |
| `ProjectTemplateControllerTest` | `./mvnw test -Dtest=ProjectTemplateControllerTest` | 31/31 pass (was 30/30 pre-fix; +1 new test) |
| `InstantiateTemplateIntegrationTest` | `./mvnw test -Dtest=InstantiateTemplateIntegrationTest` | 13/13 pass |
| Frontend install | `pnpm install` | exit 0 |
| Frontend lint | `pnpm run lint` | 0 errors / 96 pre-existing warnings |
| Frontend build | `pnpm run build` | exit 0; route map intact |
| Frontend tests | `pnpm test` | 339 files / 2119 passed / 2 skipped |

## Post-merge regression (`scripts/run-regression-test.sh`)

API: 6 customer-lifecycle failures (`Create customer` 403, then chain). UI: 4 fails (`PORTAL-03 Portal requests page`, `PORTAL-03 No firm-side leakage timeout`, `PROP-03 No unresolved variables in portal proposal view`, `PACK-01 Install pack`).

**All unrelated to OBS-301**: none touch the `InstantiateTemplateRequest` validation path or the `NewFromTemplateDialog` component. The PORTAL-03 portal-requests failure was already a pre-existing untracked test-result artefact in the main repo at session start (`frontend/test-results/portal-portal-navigation-P-4c0ee-gation-Portal-requests-page-chromium/`). Recommend orchestrator: file separate observations if not already tracked.

## Backend restart required

The `@Size(max=2000)` constraint binds at JVM class-load. The running Keycloak-dev backend at `:8080` still has the old `max=255` cached until restarted. Orchestrator must run `bash compose/scripts/svc.sh restart backend` (and bring main into the bugfix branch) before QA re-runs Day 3 ck 3.4. Frontend HMR will pick up `NewFromTemplateDialog.tsx` and `actions.ts` automatically.

## Re-run criteria for QA

- **Positive**: As Bob, open New Matter from Template → pick Litigation-RAF → Next → submit **without trimming** the prefilled description (~273 chars). Expect 201 + redirect to matter detail page.
- **Negative**: Set description to 2100 chars → submit. Expect inline `<p className="text-destructive text-xs mt-1">` under the Description Textarea reading "must be at most 2000 characters" (or whatever Hibernate validator produces) — NOT just the generic "1 field(s) have validation errors" banner.

## Scope discipline

No drift. Only the 4 files listed above were modified. Other DTOs with similar latent caps (none identified that would block the legal-za path) deliberately left for a future cleanup epic to keep this PR atomic to OBS-301.
