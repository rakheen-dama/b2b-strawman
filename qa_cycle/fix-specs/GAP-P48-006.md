# GAP-P48-006: Invoice Button Label "Mark as Sent" -> "Send Invoice"

## Decision: SPEC_READY

## Effort: S (5 minutes)

## Problem

The lifecycle script (step 30.14) says "Click Send" but the UI button says "Mark as Sent". The action does trigger the invoice email (via `InvoiceEmailEventListener`), so the label is misleading — it implies a manual status toggle rather than an actual send action.

## Fix

In `frontend/components/invoices/invoice-header-actions.tsx`, change the button label from `Mark as Sent` to `Send Invoice`.

Current (line ~100):
```tsx
Mark as Sent
```

Change to:
```tsx
Send Invoice
```

## Files to Modify

| File | Action |
|------|--------|
| `frontend/components/invoices/invoice-header-actions.tsx` | Change button text from "Mark as Sent" to "Send Invoice" |

## Verification

Navigate to an APPROVED invoice detail page and verify the button reads "Send Invoice" instead of "Mark as Sent". Clicking it should still trigger the email send flow.
