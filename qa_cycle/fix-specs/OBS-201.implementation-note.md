# OBS-201 Implementation Note

## PR
- Number: **#1226** — `fix(OBS-201): client wizard ID Number dedupe + intake accordion UX`
- Squash-merged to `main`: **cad32d07**
- Date: 2026-04-30

## Files Changed
- `frontend/components/customers/create-customer-dialog.tsx` — dropped Step-1 generic `idNumber` input; mirrored SA Legal `id_passport_number` custom field into entity column at submit.
- `frontend/components/customers/intake-fields-section.tsx` — groups with any visible fields auto-open; flatten the inner "Additional Information" toggle when group has zero required fields.

## Verification (in worktree, frontend dir)
- `pnpm lint` → 0 errors, 96 pre-existing warnings (all unrelated, no new noise).
- `pnpm build` → success, route map confirms client routes still ship.
- `pnpm test` → 339 test files, 2119 passed, 2 skipped (matches main baseline).

## Mandate Compliance
- Frontend-only. No backend changes, no field-pack changes, no migration.
- Does not affect downstream consumers — Conflict Check pre-fill and customer detail header still read from `customer.idNumber`, which is now populated from the SA Legal `id_passport_number` mirror at submit time.
- Doc templates that bind `customer.customFields.id_passport_number` continue to work (the SA Legal field is still the user-facing input).

## Next Step
QA cycle to re-run Day 2 ck 2.2 (create-client wizard UX) before proceeding to Day 3. Expect:
- Step 1: only one ID-related field (none — moved entirely to Step 2 SA Legal group).
- Step 2: SA Legal group is open by default; no nested "Additional Information" toggle.
- Conflict Check still pre-fills with the entered ID number.

## Note on Worktree Continuation
The Dev agent (`agent-a01354280e3209791`) ran out of context after applying the 2 file edits. Orchestrator picked up: ran lint/build/test from the worktree, committed, pushed, opened PR, self-reviewed, squash-merged.
