# OBS-404 Implementation Note

## PR
- Number: **#1228** — `fix(OBS-404): rebrand portal DocTeams → Kazi + guardrail`
- Squash-merged to `main`: **defd26c7**
- Date: 2026-04-30

## Files Changed (5)
- `portal/lib/brand.ts` (new) — `export const BRAND_NAME = "Kazi"` constant.
- `portal/lib/__tests__/brand.test.ts` (new) — Vitest guardrail walking `portal/{app,components,lib}` for any `DocTeams` literal.
- `portal/components/portal-footer.tsx` — `"Powered by DocTeams"` → `"Powered by {BRAND_NAME}"`.
- `portal/app/login/page.tsx` — `orgName` fallback `"DocTeams"` → `BRAND_NAME`.
- `portal/app/accept/[token]/acceptance-page.tsx` — `orgName` fallback `"DocTeams"` → `BRAND_NAME`.

## Verification (portal pnpm workspace)
- `pnpm lint` → 0 errors, 6 pre-existing img-tag warnings.
- `pnpm build` → green.
- `pnpm test` → 42 files / 187 tests / all passed (includes the new brand-guardrail).

## Mandate Compliance
- Portal-only. No backend, no frontend, no DB changes.
- Zero scope creep — guardrail prevents future leaks.
- Out-of-scope leaks (frontend marketing testimonials, mock-auth layout, backend assistant prompts) are documented in the spec for a future cycle, but excluded from this PR.

## Note on Anthropic-API Mid-Task Failure
The Dev subagent (`agent-a48a65a7166d10ab2`) hit a 403 from the Anthropic API (`Account is no longer a member of the organization associated with this token`) after 31 tool uses. The agent's worktree already contained all 5 expected file edits at the moment of failure. The orchestrator picked up: ran lint/build/test from the worktree, committed, opened the PR, self-reviewed (only the expected 5 files changed), squash-merged, and pruned the stale worktree refs.
