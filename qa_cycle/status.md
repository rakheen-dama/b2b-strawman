# QA Cycle Status — Legal ZA Full Lifecycle (Keycloak) — 2026-04-26

## Current State

**Purpose**: Run the legal ZA full-lifecycle scenario (`qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`) end-to-end on the Keycloak dev stack. Fix every gap encountered at the root cause — no workarounds, no SQL shortcuts. Prior verify-cycle status archived to `qa_cycle/_archive_2026-04-26_post-verify/status.md`.

- **Branch**: `bugfix_cycle_2026-04-26-day10` (cut from `main` `c194faca` for Day 10 walk; Day 8 walk PR #1176 squash-merged)
- **Scenario**: `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`
- **ALL_DAYS_COMPLETE**: false
- **QA Position**: Day 11, Checkpoint 11.1 (Day 10 CLOSED on `bugfix_cycle_2026-04-26-day10` cycle-21 walk against main `c194faca` — backend PID 58335. **8 PASS / 0 FAIL / 0 PARTIAL / 0 BLOCKED / 1 SKIPPED-not-applicable** for Day 10's 8 in-scope checkpoints + 3 wrap-up rollups (all PASS). Phase A: PROP-0002 acceptance flowed from portal to firm read-model — global `/proposals` page shows PROP-0002 "Cycle19 Verify" with **Accepted** badge + Apr 27 2026 timestamp matching Sipho's portal accept; per-proposal detail confirms all three timestamps (Created/Sent/Accepted). RAF matter `cc390c4f-…` status ACTIVE (auto-active since creation Day 3; no manual transition needed). Phase B: Manual deposit recorded for **R 50 000,00** against Sipho/RAF-2026-001 via `/trust-accounting/transactions` Record Deposit dialog (UUID inputs); status RECORDED (no approval queue since `require_dual_approval=false` on Mathebula Trust — Main); reconciled across all three views: trust account cashbook + Sipho's client ledger card (auto-created `36765353-…`) + matter Trust tab = R 50 000,00. DB: `trust_transactions.id=13ca4d28-…`, `client_ledger_cards.balance=50000.00`, `total_deposits=50000.00`, `last_transaction_date=2026-04-27`. Side-effect: trust-deposit nudge email auto-fired to Sipho at 2026-04-27T04:01:46Z (Mailpit `m7qSdt8XULPXMs6mX6Vbcn`, subject "Mathebula & Partners: Trust account activity") with View trust ledger CTA → `:3002/trust/46d1177a-…`. Day 11 §11.1 prerequisite met. 10.6 SKIPPED-not-applicable (dual-approval is OFF). **Zero new bugs logged.** Two informational follow-ups noted (NOT logged as gaps): (a) Record Deposit dialog uses raw UUIDs for Client/Matter — UX papercut for non-technical users, pre-existing; (b) trust-deposit nudge email shows raw ISO timestamp + unformatted amount `50000` + uppercase `DEPOSIT` — template polish; will affect Day 11 §11.5 sanitisation check but not block Day 11 portal trust ledger view. Console: only 1 pre-existing hydration warning; 0 errors during deposit flow.)
- **Dev Stack**: Running — backend:8080 (PID 58335, fresh JVM post-PR #1175), gateway:8443, frontend:3000, portal:3002 all healthy as of 2026-04-27 SAST
- **NEEDS_REBUILD**: false
- **Cycle Count**: 21

## Tracker

| GAP_ID | Severity | Status | Owner | Summary | Evidence |
|--------|----------|--------|-------|---------|----------|
| BUG-CYCLE26-01 | LOW | VERIFIED | qa | Team-invite form: Radix Select role choice ("Admin") does not propagate to backend POST under Playwright MCP; backend records role=member regardless. Root cause shared with -02 — fragile RHF binding pattern (custom onChange shadows register, role state held in useState outside RHF). Spec: qa_cycle/fix-specs/BUG-CYCLE26-01-02.md. Fixed in PR #1162 (a6b7cbab) and re-merged in PR #1164 (squash merge `68c71cb8`). **VERIFIED 2026-04-26 cycle-5** — fresh invite to `verify-admin-1@mathebula-test.local` with Admin role → backend log `Created invitation … with role=admin`. | qa_cycle/checkpoint-results/day-00.md §Cycle 5 Retest; `retest-2026-04-26-team-invite-admin.png` |
| BUG-CYCLE26-02 | LOW | VERIFIED | qa | Team-invite form: MCP-driven `fill()` on email input does not consistently flow through react-hook-form `register()`; Server Action POSTs return 200 but never reach backend. Same root cause as -01 — combined fix. Spec: qa_cycle/fix-specs/BUG-CYCLE26-01-02.md. Fixed in PR #1162 (a6b7cbab) and re-merged in PR #1164 (squash merge `68c71cb8`). **VERIFIED 2026-04-26 cycle-5** — MCP `fill()` reaches backend without native-setter workaround for both admin + member invites; success copy survives form.reset. | qa_cycle/checkpoint-results/day-00.md §Cycle 5 Retest |
| GAP-L-90 | BUG | VERIFIED | qa | Branding: brand-colour Save Settings persists `brand_color` to org_settings and injects `--brand-color: <hex>` on `<html>`, but no CSS rule consumes `var(--brand-color)`; setting brand colour has zero visible effect on UI. Logo persistence works fine. Scoped fix: sidebar active indicator + sidebar org-name label (high-signal, minimal impact). Spec: qa_cycle/fix-specs/GAP-L-90.md. Fixed in PR #1163 (b3b5a8d4) and re-merged in PR #1164 (squash merge `68c71cb8`). **VERIFIED 2026-04-26 cycle-5** — desktop sidebar active indicator + org-name label both render `rgb(27, 51, 88)` (navy `#1B3358`); mobile sidebar utility-footer active indicator also navy. `:root --brand-color` computed = `#1B3358`. | qa_cycle/checkpoint-results/day-01.md §Cycle 5 Retest; `retest-2026-04-26-sidebar-navy.png`, `retest-2026-04-26-mobile-sidebar-navy.png` |
| GAP-L-91 | LOW | RESOLVED | product | Legal-za rate pack ships LSSA 2024/2025 schedule (the latest real-world LSSA schedule — tariffs are revised every 2–3 years). Scenario expectation of "2026 schedule" was aspirational. Resolved by editing scenario 1.4 to reference the latest published LSSA schedule. No code change. | qa/testplan/demos/legal-za-full-lifecycle-keycloak.md §1.4 (edited 2026-04-26); evidence `day-01-1.3-1.4-tariff-schedule.png` |
| BUG-CYCLE26-03 | BUG | VERIFIED | qa | Client detail page (`/customers/[id]`) has no in-context **Run Conflict Check** button. Conflict Check is only reachable via global `/conflict-check` workspace (Clients sidebar group). Spec: qa_cycle/fix-specs/BUG-CYCLE26-03.md. Fixed in PR #1167 (`d138b48f`) and re-merged in PR #1168 (squash merge `1c0fde1b`). **VERIFIED 2026-04-26 cycle-10** — Sipho's customer-detail action bar renders `Run Conflict Check` link with deep-link `/conflict-check?customerId=c4f70d86-…&checkedName=Sipho+Dlamini&checkedIdNumber=8501015800088`; click navigates with form prefilled (Name, ID, customer-link Sipho selected); submit succeeds, "No Conflict" result, `conflict_checks` row `dee0a260-…` written with FK to Sipho. Edge case: invalid `?customerId=not-a-uuid` does not crash, dropdown falls back to "-- None --". | qa_cycle/checkpoint-results/day-02.md §Cycle 10 Retest §Fix 3; `retest-pr1168-customer-detail.yml`, `retest-pr1168-conflict-check-prefilled.yml` |
| BUG-CYCLE26-04A | BUG | VERIFIED | qa | KYC integration cannot be enabled in UI — `KycIntegrationCard.tsx` lacks the Enable toggle that `PaymentIntegrationCard.tsx` already implements. Spec: qa_cycle/fix-specs/BUG-CYCLE26-04A.md. Fixed in PR #1165 (`221d4069`) and re-merged in PR #1168 (squash merge `1c0fde1b`). **VERIFIED 2026-04-26 cycle-10** — Switch (`#enabled-KYC_VERIFICATION`) renders in card body when provider configured; toggle ON → DB `enabled=t` and card status flips Disabled→Configured; toggle OFF → DB `enabled=f` and card flips Configured→Disabled; toggle ON again → DB `enabled=t` and card returns to Configured. End-to-end UI→server-action→backend→DB→revalidate path works both directions. | qa_cycle/checkpoint-results/day-02.md §Cycle 10 Retest §Fix 1; `retest-pr1168-kyc-toggle-before.yml`, `retest-pr1168-kyc-toggle-after-on.yml`, `retest-pr1168-kyc-snapshot-3.yml`, `retest-pr1168-kyc-snapshot-4.yml` |
| BUG-CYCLE26-04B | BUG | VERIFIED | qa | KYC discoverability on client detail: no status badge in header, no Verify-KYC action shortcut. Spec: qa_cycle/fix-specs/BUG-CYCLE26-04B.md. Fixed in PR #1166 (`1d9a3642`) and re-merged in PR #1168 (squash merge `1c0fde1b`). **VERIFIED 2026-04-26 cycle-10** — `KycStatusBadge` component renders correctly per state (verified/pending/null); for Sipho (no checklist instances yet) state=unverified → badge correctly returns null per spec §2 ("don't clutter the header for unverified state"); Verify-KYC outline button visible in admin action bar with deep-link to `?tab=onboarding`; click navigates to correct URL. Edge case PASS: button still renders alongside Run Conflict Check (reviewer-fix verification — `enabledModules` outside admin try-catch). Verified-state transition path skipped per spec §5 (optional). | qa_cycle/checkpoint-results/day-02.md §Cycle 10 Retest §Fix 2; `retest-pr1168-customer-detail.yml`, `retest-pr1168-after-verify-kyc-click.yml`, `retest-pr1168-sipho-prospect.yml` |
| BUG-CYCLE26-05 | LOW | WONT_FIX | qa | Tooling-only regression: Playwright MCP `browser_take_screenshot` repeatedly times out (`Timeout 5000ms exceeded after fonts loaded`) on Next.js dev frontend port 3000 mid-session. Earlier in same session PNGs captured fine; failure flips on after several hours of session uptime — consistent with long-lived headless Chromium GPU/CPU pressure or Playwright MCP server-side connection pool, not our font config. No real user is on Playwright MCP; Web Vitals not regressed; `next/font/google` config unchanged and verified Day 1. Workaround: YAML DOM snapshots via `browser_snapshot --filename` (used in cycle-6 verification log without blocking the scenario). Closure rationale: qa_cycle/fix-specs/BUG-CYCLE26-05.md. Re-open as tooling/infra issue if it blocks future cycles. | qa_cycle/checkpoint-results/day-02.md §2.7 + Cycle-6 verification |
| BUG-CYCLE26-06 | **HIGH** | VERIFIED | qa | Sending a proposal to an INDIVIDUAL customer fails with "2 required field(s) missing for Proposal Sending" because `StructuralPrerequisiteCheck.PROPOSAL_SEND_FIELDS` hard-requires `Customer.contact_name` + `Customer.contact_email` — but those columns are NULL by design for INDIVIDUAL customers (recipient identity is held in portal_contact). Spec: qa_cycle/fix-specs/BUG-CYCLE26-06.md. Fixed in PR #1172 and re-merged on main as PR #1173 (squash `5365e48a`). **VERIFIED 2026-04-27 cycle-17** — Send Proposal on `0781c5ad-…` for INDIVIDUAL customer Sipho (NULL contact_name/contact_email, ACTIVE portal_contact with email, populated address_line1) no longer surfaces the "N required field(s) missing for Proposal Sending" error. The Send dialog now reaches the next downstream gate (`Proposal content must not be empty`, `ProposalService.java:618-621`) — this is a different `InvalidStateException` after `prerequisiteCheck.passed()` returns true; conclusive evidence the prereq fix landed. ACTIVE-tightening also observable: Recipient combobox lists only the single ACTIVE portal_contact. | qa_cycle/checkpoint-results/day-07.md §Cycle 17 Retest; `cycle17-retest-7.8-send-dialog-recipient-selected.yml`, `cycle17-retest-7.8-after-send.yml` |
| BUG-CYCLE26-07 | **HIGH** | VERIFIED | qa | Side-effect surfaced after BUG-CYCLE26-06 fix landed: `ProposalService.sendProposal()` gate-2 (`InvalidStateException("Proposal content must not be empty")`, line 618-621) blocked every proposal created via the matter-level `New Engagement Letter` dialog because that dialog never seeds `content_json`. Fix: new `ProposalContentSeeder` @Service emits a minimal Tiptap doc (heading + greeting with `client_name` variable + fee summary + optional rate/hours/description/expiry paragraphs + standard terms) from the create-time form fields; `ProposalService.createProposal` invokes the seeder when caller omits or empties `contentJson`. Gate-2 stays as a defensive guard. Frontend dialog unchanged — full template/clause authoring deferred to L-49 Sprint 3. Spec: qa_cycle/fix-specs/BUG-CYCLE26-07.md. Fixed in PR #1174 (squash `7d602dee`) and re-merged on main as PR #1175 (squash `0a58a242`). **VERIFIED 2026-04-27 cycle-19** — fresh proposal `69e3d65f-…` (PROP-0002) created via matter-level dialog with Title=`Cycle19 Verify`, Fee Model=HOURLY, Hourly Rate Note=`R850/hr per LSSA 2024/2025 schedule`. DB shows `content_json` length 774 (vs old `0781c5ad-…` row's `'{}'` length 2), root `type=doc`, 6 content children including `variable{key:client_name}` node — exact seeder spec. Send Proposal flow with Sipho's ACTIVE portal_contact succeeds: status DRAFT→SENT, `sent_at=2026-04-27 01:54:31.181629+00`, `portal_contact_id=f3f74a9d-…`. `portal.portal_proposals` row written with `content_html` length 1223; rendered HTML body contains `<p>Dear Sipho Dlamini,</p>` confirming `TiptapRenderer` + `ProposalVariableResolver` substituted `{{client_name}}` correctly. Portal-side: Sipho's `:3002/proposals/69e3d65f-…` renders heading "Cycle19 Verify", status badge SENT, body text, Accept Proposal + Decline buttons. | qa_cycle/checkpoint-results/day-07.md §Cycle 19 Retest; `cycle19-7.1-engagement-dialog.yml`, `cycle19-7.9-after-send.yml`, `cycle19-7.11-portal-proposal-detail.yml` |
| BUG-CYCLE26-08 | LOW | OPEN | product | Trust transactions filter list shows `"Interest Lpff"` instead of `"Interest LPFF"` for the `INTEREST_LPFF` enum. Other enum types render correctly (Refund, Reversal, Interest Credit, Fee Transfer). Likely cause: a generic title-case helper in the type-label conversion path that lowercases everything except first letter; needs an acronym whitelist or the enum-to-label map needs a manual entry for `INTEREST_LPFF`. CR finding on cycle-21 PR #1177. | `qa_cycle/checkpoint-results/cycle21-day10-10.4-transactions-page.yml:140-141`; CR review on PR #1177 |
| BUG-CYCLE26-09 | LOW | OPEN | product | Breadcrumb on `/trust-accounting/client-ledgers/{id}` shows the raw URL slug `client-ledgers` and the raw customer UUID (`c4f70d86-…`) instead of human-readable labels (`Client Ledgers` and the customer's display name). Affects every per-customer ledger detail page. CR finding on cycle-21 PR #1177. | `qa_cycle/checkpoint-results/cycle21-day10-10.7-sipho-ledger-detail.yml:88-100`; CR review on PR #1177 |
| BUG-CYCLE26-10 | LOW | OPEN | product | Activity feed message templates emit literal `"for unknown"` when the recipient/target name is null (e.g., `Bob Ndlovu accepted "Bank statement (≤ 3 months)" for unknown`). Should either resolve the missing name from the underlying entity or omit the trailing `for {name}` clause when name is unavailable. Affects every accept/review activity message on matter detail pages. CR finding on cycle-21 PR #1177. | `qa_cycle/checkpoint-results/cycle21-day10-10.1-raf-matter-detail.yml:276-290`; CR review on PR #1177 |

## Standing Rules (apply to every agent)

- **Root-cause every bug**. No workarounds. No SQL INSERT/UPDATE/DELETE to skip steps. All operations through REST API or browser UI.
- **No mocks for QA**. Drive the browser via Playwright MCP. Mailpit API is the only legitimate REST surface for QA.
- **One fix per PR**. Dev agents commit to `fix/{GAP_ID}` branches off this cycle branch, PR-and-merge with squash.
- **Restart after Java changes**. Backend/gateway: `bash compose/scripts/svc.sh restart {service}`. Frontend/portal: HMR auto-reloads.
- **Read CLAUDE.md** in the relevant subdirectory before changing service code.
- **Commit between turns**. Each agent pushes its state changes (status.md, fix-spec, checkpoint result) before returning.

## Per-Day Workflow (orchestrator behavior — overrides default skill flow)

The skill's default Step 1 flow says "ELSE → QA walks next checkpoint". This cycle uses a stricter per-day loop instead:

1. **Walk one day** on a fresh `bugfix_cycle_2026-04-26-dayN` branch (cut from `main`). Stop at end-of-day or first blocker.
2. **Triage every gap** found that day via Product Agent. WONT_FIX is allowed only for tooling/out-of-scope; real product bugs (even non-blocking UX issues) become SPEC_READY.
3. **Fix every spec** via Dev Agent (worktree-isolated). Each fix is a PR into `bugfix_cycle_*-dayN` and squash-merges there.
4. **When the day's tracker has 0 OPEN/SPEC_READY items**, PR `bugfix_cycle_*-dayN` → `main`. Run review cycle (CodeRabbit + independent reviewer). Address all actionable findings before merging.
5. **After merge to main**, dispatch a QA retest agent that verifies each fix on `main` (no SQL shortcuts, real browser flow). Mark gaps `FIXED → VERIFIED` only after the retest passes on `main`.
6. **Then** advance QA Position to next day. Cut a fresh `bugfix_cycle_*-dayN+1` branch from `main` and repeat.

The orchestrator's job between agent turns is to read this status file, decide which step in the loop above is next, and dispatch the matching agent. Do NOT skip the PR-to-main-and-retest step at the end of each day.

## Known Out-of-Scope Constraints (do NOT open gaps for these)

- **Payment integration is a stub**. There is no real PSP integration (Stripe, Yoco, Peach, etc.). Invoice "Mark Paid" / portal payment flows are stub endpoints that just flip status. Do NOT open gaps for missing payment-gateway redirects, webhook signing, real card processing, or PSP-side reconciliation. If the scenario asks the user to "pay an invoice", treat the stub Mark-Paid action as the equivalent and continue.
- **KYC provider integration is a stub**. There is no real KYC vendor integration (Onfido, Veriff, Smile ID, etc.). The KycVerificationDialog and `Run KYC` flow call a stub backend that just returns a status. The UI surfaces (toggle, badge, verify button, status display) are real and must work; the underlying provider call is mocked. Do NOT open gaps for missing real-provider OAuth, webhook signing, real document analysis, or provider-side reconciliation.

## Log

- 2026-04-26 SAST — Orchestrator: cycle initialized on branch `bugfix_cycle_2026-04-26`. Prior verify-cycle status (ALL_DAYS_COMPLETE 2026-04-25) archived. Dev stack confirmed healthy. About to dispatch QA agent for Day 0.
- 2026-04-26 SAST — QA: Day 0 cycle-1 — 30 PASS / 2 PARTIAL (tooling) / 0 FAIL / 0 BLOCKER. All Day-0 wrap-up checks satisfied: 3 KC users registered, tenant_5039f2d497cf provisioned with vertical=legal-za, full legal sidebar nav present, zero tier/upgrade UI. Two LOW-severity tooling-only bugs logged (BUG-CYCLE26-01, BUG-CYCLE26-02) — both Playwright-MCP-Radix interaction quirks, not real-user product defects. L-22 (KC owner registration → /org/{slug}/dashboard) reconfirmed working. Bumping QA Position to Day 1, Checkpoint 1.1.
- 2026-04-26 SAST — QA: Day 1 cycle-2 — 5 PASS / 2 PARTIAL / 0 FAIL / 0 BLOCKER. Logo upload + persisted to S3, brand_color persisted to DB, LSSA tariff schedule confirmed pre-seeded (19 items in ZAR, schedule year 2024/2025), Section 86 trust account "Mathebula Trust — Main" created with R 0,00 balance. GAP-L-90 (BUG): brand-colour CSS variable injected but no stylesheet consumes it — visual no-op. GAP-L-91 (LOW): tariff schedule is 2024/2025 not 2026 — scenario or seed pack mismatch. Both non-cascading; advancing to Day 2.
- 2026-04-26 SAST — Product: triaged 4 OPEN items → 3 SPEC_READY + 1 RESOLVED. Combined BUG-CYCLE26-01 + -02 into single spec `BUG-CYCLE26-01-02.md` (shared root cause: RHF binding pattern in invite-member-form mixes register-shorthand with override-onChange and useState-outside-form). GAP-L-90 specced as scoped sidebar fix (active indicator + org-name label only — not a full theme overhaul). GAP-L-91 resolved-via-scenario-edit: scenario §1.4 now references "latest published LSSA schedule (2024/2025)" since LSSA tariffs are revised every 2–3 years. Cycle Count → 4.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-01-02 merged (PR #1162). Refactored invite form to FormField+Controller pattern. Frontend HMR picks up changes — no service restart needed.
- 2026-04-26 SAST — Dev: GAP-L-90 merged (PR #1163). Sidebar consumers now read --brand-color; fallback teal in :root. HMR — no restart.
- 2026-04-26 SAST — QA: Cycle 5 retest of PR #1164 (commit 68c71cb8). 3/3 fixes VERIFIED, 0 REOPENED. BUG-CYCLE26-01 confirmed via fresh `verify-admin-1@…` invite → backend log `role=admin`. BUG-CYCLE26-02 confirmed — MCP fill() reaches backend without native-setter workaround for both admin + member invites; success copy survives form.reset. GAP-L-90 confirmed — desktop sidebar active indicator (`.absolute.w-0.5`) + org-name label both `rgb(27,51,88)`, mobile sidebar utility-footer active indicator also navy, `:root --brand-color` computed = `#1B3358`. Screenshots saved. QA Position **HELD at Day 2 / 2.1** per user directive — did NOT walk forward into Day 2.
- 2026-04-26 SAST — QA: Day 2 cycle-6 — 5 PASS / 2 PARTIAL / 0 FAIL / 0 BLOCKER / 2 SKIPPED. Bob (admin) created Sipho Dlamini (INDIVIDUAL, sipho.portal@example.com, ID 8501015800088, ZA) with SA Legal — Client Details field group populated; conflict check returned NO_CONFLICT (2 history rows confirm no false positives). KYC verification gapped: BUG-CYCLE26-04 (no UI Enable toggle, no client-detail Run-KYC button — adapter unreachable from firm UI). Discoverability gap BUG-CYCLE26-03 (Run Conflict Check is only in global workspace, not on client detail). Tooling regression BUG-CYCLE26-05 (PNG screenshots time out mid-session — DOM YAML used as substitute). All 3 non-cascading; advancing to Day 3.
- 2026-04-26 SAST — Product: cycle-7 triage — 3 OPEN → 2 SPEC_READY (BUG-CYCLE26-03, BUG-CYCLE26-04A, BUG-CYCLE26-04B counted as one bucket: KYC blocker), 1 WONT_FIX (BUG-CYCLE26-05 tooling-only). Split BUG-CYCLE26-04 into 04A (Enable toggle on `KycIntegrationCard.tsx`, mirrors `PaymentIntegrationCard.tsx:476–492` pattern — demo blocker, ship first) and 04B (KYC status badge + Verify-KYC action shortcut on customer detail header — depends on 04A landing). Backend `PATCH /api/integrations/{domain}/toggle` already exists, frontend just wasn't calling it. KYC verify dialog itself is already wired (`KycVerificationDialog` per checklist row); the gap is purely the integration-enable toggle and client-header discoverability. BUG-CYCLE26-03 specced as customer-detail action button + searchParams-driven prefill on conflict-check route (no backend changes). Cycle Count → 7.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-04A merged (PR #1165). KYC card now has Enable Switch — pattern-matched from PaymentIntegrationCard. HMR — no restart.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-04B merged (PR #1166). Customer detail now surfaces KYC status badge + verify shortcut. Stub provider; UI surface only. HMR — no restart.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-03 merged (PR #1167). Run Conflict Check shortcut now on client detail. HMR — no restart.
- 2026-04-27 SAST — QA: Day 3 cycle-11 — 14 PASS / 0 FAIL / 0 BLOCKER. Fresh walk on main `34ec2f77` (branch `bugfix_cycle_2026-04-26-day3`) — Bob creates RAF matter for Sipho (`Dlamini v Road Accident Fund` ref `RAF-2026-001`, ACTIVE, `applied_field_groups=[common-project, legal-za-project]` only — Conveyancing pack correctly excluded). Save Custom Fields on PROSPECT matter persists `case_number=RAF-2026-001-CASE` + `opposing_party=Road Accident Fund` + `court_name=Gauteng Division, Pretoria` (L-35 re-verified browser-driven). Create Information Request dialog: FICA Onboarding Pack (3 items) selectable (L-33), Sipho portal_contact auto-populated (L-34), Due Date (optional) field present (L-41 — column + picker functional, REQ-0001 due_date NULL because prior turn didn't fill the optional field; not a regression). REQ-0001 SENT (3 items: ID copy / Proof of residence / Bank statement, all PENDING), magic-link email in Mailpit with portal :3002 URL + token + orgId (L-42). Day 4 = Sipho first portal login.
- 2026-04-26 SAST — QA: Cycle 10 retest of PR #1168 (commit `1c0fde1b`) on `main`. 3/3 fixes VERIFIED, 0 REOPENED. **BUG-CYCLE26-04A**: KYC Switch round-trips ON/OFF/ON; DB `enabled` toggles `f`→`t`→`f`→`t`; card status follows Disabled↔Configured. **BUG-CYCLE26-04B**: header badge correctly returns null for unverified state per spec; Verify-KYC button visible with `?tab=onboarding` deep-link; navigates correctly (Onboarding tab gracefully falls back to Matters since Sipho is PROSPECT with 0 checklist instances — pre-existing upstream behavior, not a regression). **BUG-CYCLE26-03**: Run Conflict Check button on Sipho detail with full deep-link; form prefills correctly; submit creates `conflict_checks` row `dee0a260-…` with FK; invalid-UUID edge case doesn't crash. Stack auth: Bob's session expired mid-test, re-auth as Thandi (Owner) — no impact on verification claim since admin gate covers both. Screenshot tooling still degraded (BUG-CYCLE26-05 WONT_FIX); YAML evidence used. QA Position advanced to Day 3 / 3.1 — orchestrator decides when to walk.
- 2026-04-27 SAST — QA: Day 4 cycle-12 — 14 PASS / 0 FAIL / 0 BLOCKER. Fresh walk on main `038d7a7a` (branch `bugfix_cycle_2026-04-26-day4`) — Day-3 magic-link token expired (~40min before cycle started), so Bob drove a fresh REQ-0002 send firm-side (`d8a58ade-…`, Mailpit `5J7vGK7B9w5LFAGvzFc9zf`, fresh token + orgId). Sipho exchanged token → portal `/home` "Pending info requests / 2" → `/requests/d8a58ade-…` detail rendered 3 FICA items → uploaded all 3 PDFs (test-fica-id/address/funds) → all 3 items SUBMITTED with document_ids, parent SENT→IN_PROGRESS. Portal read-model live-synced (3/3 submitted). L-42/L-43/L-44/L-47/P-01/P-02 all re-verified browser-driven. Zero new gaps. 4.11 SKIPPED-by-design (P-05 polish deferral, no per-item note textbox); 4.13 PASS-with-nuance (home count drops only after firm marks COMPLETED on Day 5 — implementation is `status !== "COMPLETED"`, consistent with Cycle 1 verify). Day 4 CLOSED → advance to Day 5 / 5.1.
- 2026-04-27 SAST — QA: Day 5 cycle-13 — 6 PASS / 0 FAIL / 0 BLOCKER. Fresh walk on main `597b4b60` (branch `bugfix_cycle_2026-04-26-day5`) — Bob reviewed REQ-0002 `d8a58ade-…` firm-side. Initial 5.3 walk found Download buttons missing; investigation showed running backend JVM (started 01:31) predated PR #1145 (`d2d0c26b` `documentFileName` mapper landed later) — `git merge-base --is-ancestor d2d0c26b main` confirms fix is on main. After `bash svc.sh restart backend` (33s), Download buttons render with filenames per L-45 fix; clicked Accept x3 → progress 1/3 → 2/3 → 3/3, parent COMPLETED at 2026-04-26 23:06:08, all items ACCEPTED with reviewed_at + reviewed_by populated. Portal read-model live-synced via L-43 listener (status=COMPLETED, accepted=3). Matter Overview FICA tile = `Done / Verified Apr 27, 2026 / View request` (L-46 re-verified). Mailpit got 4 outbound emails (3 per-item + 1 closure). Zero new gaps; zero regressions. Day 5 CLOSED → advance to Day 7 / 7.1 (scenario skips Day 6).
- 2026-04-27 SAST — QA: Day 7 cycle-14 — 2 PASS / 1 PARTIAL / 5 SKIPPED-BY-DESIGN / 1 FAIL / 2 BLOCKED (cascade). Fresh walk on main `3e018078` (branch `bugfix_cycle_2026-04-26-day7`). Context-swapped Bob → Thandi via gateway POST /logout + KC password login. **L-48 VERIFIED on UI** for the first time — matter-level `+ New Engagement Letter` button on RAF detail opens dialog with Customer combobox correctly locked to Sipho (no `/api/customers` 404). Created proposal `0781c5ad-… / PROP-0001 / DRAFT / HOURLY / Hourly Rate Note=R850/hr per LSSA 2024/2025 schedule / expires 2026-05-04`. 7.7 PASS (status=DRAFT). 7.8 FAIL → **BUG-CYCLE26-06 (HIGH, NEW BLOCKER)**: Send Proposal blew up with "2 required field(s) missing for Proposal Sending". Backend `PROPOSAL_SEND` prereq requires `Customer.contact_name` + `Customer.contact_email` — both NULL on Sipho (INDIVIDUAL customer, addressed via portal_contact). 7.9–7.11 BLOCKED. 7.2–7.5 SKIPPED-BY-DESIGN (L-49 Sprint 3). Mid-walk JWT expiry recovered via `/oauth2/authorization/keycloak` re-auth.
- 2026-04-27 SAST — Product: cycle-15 triage — BUG-CYCLE26-06 → SPEC_READY (option 1: backend fallback). Verified root cause at `backend/src/main/java/io/b2mash/b2b/b2bstrawman/prerequisite/StructuralPrerequisiteCheck.java:50-54` (`PROPOSAL_SEND_FIELDS` hard-requires `contact_name`/`contact_email`) called from `PrerequisiteService.java:198-220`. Fix is contained: add a 3-arg `check(customer, context, portalContactIdentitySatisfied)` overload to `StructuralPrerequisiteCheck`; reorder `PrerequisiteService.PROPOSAL_SEND` branch to look up portal_contacts FIRST, thread the `hasActiveContactWithEmail` boolean into the structural check, suppress contact_name/contact_email violations when satisfied, keep `address_line1` + portal-contact-existence checks intact. Confirmed downstream: `ProposalVariableResolver:43` and `AcceptanceService:228` already use portal_contact identity, never `Customer.contact_*` — those columns are dead weight at send time. No schema change. Spec: qa_cycle/fix-specs/BUG-CYCLE26-06.md. Cycle Count → 15.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-06 merged (PR #1172, squash `aa8e016c`). `StructuralPrerequisiteCheck` got 3-arg `check(customer, context, portalContactIdentitySatisfied)` overload that suppresses contact_name/contact_email checks for PROPOSAL_SEND when an ACTIVE portal_contact with email exists. `PrerequisiteService` reordered: portal_contact lookup first, ACTIVE-tightened existence check, then thread the boolean into structural check. 33/33 targeted tests pass. Backend restarted (PID 16195+ → fresh JVM). Cycle Count → 16.
- 2026-04-27 SAST — QA: Cycle 17 retest of PR #1173 (commit `5365e48a`) on `main`. **BUG-CYCLE26-06 VERIFIED** end-to-end: Send Proposal on `0781c5ad-…` for INDIVIDUAL customer Sipho (NULL contact_name/contact_email, ACTIVE portal_contact + email, populated address_line1) no longer triggers `"N required field(s) missing for Proposal Sending"`. Send dialog now reaches the next downstream `InvalidStateException("Proposal content must not be empty")` gate at `ProposalService.java:618-621` — the message-shape change is conclusive evidence the prereq fix landed. Recipient combobox shows only the single ACTIVE portal_contact (ACTIVE-tightening confirmed). Proposal stays DRAFT (correct, blocked at gate-2 before status mutation). Mailpit empty (correct). New finding: **BUG-CYCLE26-07 (HIGH, OPEN)** — gate-2 (`content_json` empty) blocks every proposal created via the matter-level New Engagement Letter dialog; pre-existing since Mar 1, 2026 commit `4023f683` but masked by gate-1. Different root cause from BUG-CYCLE26-06; Day 7 7.8 onwards remains blocked but on a separate issue. QA Position **HELD at Day 7 / 7.8** — orchestrator triages BUG-CYCLE26-07 before Day 8 walk. Cycle Count → 17.
- 2026-04-27 SAST — Product: cycle-18 triage — BUG-CYCLE26-07 → SPEC_READY (option 1B: seed minimal Tiptap doc at `createProposal()` time, NOT at Send time). Verified root cause at `backend/src/main/java/io/b2mash/b2b/b2bstrawman/proposal/ProposalService.java:617-621` (gate-2) and `:156` (where `contentJson` is conditionally set — the source of the empty-map persistence). Confirmed downstream consumer `ProposalPortalSyncEventHandler.java:97-98` calls `tiptapRenderer.render(proposal.getContentJson(), …)` — a non-empty Tiptap `doc` tree is the legitimate contract, so gate-2 must stay honest. Rejected option 2 (frontend seed) — adds two seed sites and skips backend test surface; rejected option 3 (Generate engagement letter step) — that's GAP-L-49 Sprint 3 deferred (L effort, multi-day). Fix is contained: new `ProposalContentSeeder` service builds doc from `(title, feeModel, hourlyRateNote, amounts, expiresAt)` + `{{client_name}}` variable; `ProposalService.createProposal` falls back to seeder when caller omits / sends empty contentJson; gate-2 untouched. Spec: qa_cycle/fix-specs/BUG-CYCLE26-07.md. Branch updated to `bugfix_cycle_2026-04-26-day7-v2`. Cycle Count → 18.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-07 merged (PR #1174, squash `7d602dee`). New `ProposalContentSeeder` @Service emits minimal Tiptap doc (heading + greeting with `client_name` variable + fee summary + optional rate/hours/description/expiry paragraphs + standard terms) at `createProposal()` time when contentJson is null/empty. Existing `sendProposal_emptyContent_returns400` updated to forcibly clear `content_json` via direct SQL so gate-2 keeps regression coverage. Targeted tests: 11/11 `ProposalContentSeederTest` + 15/15 `ProposalSendTest` + 101/101 across Proposal* package green. Backend restarted (fresh JVM on :8080). Cycle Count → 19.
- 2026-04-27 SAST — QA: Cycle 19 retest of PR #1175 (squash `0a58a242`) on `main` + Day 7 §7.8-§7.11 walk. **BUG-CYCLE26-07 VERIFIED end-to-end.** Created fresh proposal `69e3d65f-…` (PROP-0002, Cycle19 Verify, HOURLY, R850/hr) via matter-level New Engagement Letter dialog. DB confirms `content_json` populated with 6-child Tiptap doc per seeder spec (vs old proposal's `'{}'`). Send Proposal succeeds — gate-2 cleared; status DRAFT→SENT with `sent_at` populated and `portal_contact_id` set; `portal.portal_proposals` row written with rendered HTML containing `<p>Dear Sipho Dlamini,</p>` (client_name variable interpolation). Sipho's portal session at `:3002/proposals/69e3d65f-…` renders heading + body + Accept/Decline. **§7.10 Mailpit NOT-APPLICABLE — informational** scope gap: matter-level Send Proposal codepath publishes `ProposalSentEvent` whose listeners only do in-app notifications + portal sync; no email-channel listener exists. Orphaned `portal-new-proposal.html` template suggests this was planned but never wired. Not a BUG-CYCLE26-08 (no regression from PR #1175; portal sync still surfaces the proposal to Sipho). Suggested deferred follow-up: wire `PortalEmailService.sendNewProposalEmail()` listener for `ProposalSentEvent`. **Day 7 CLOSED** → advance to Day 8 / 8.1. QA Position: `Day 8 / 8.1`. Branch field updated to `main`. Cycle Count → 19.
- 2026-04-27 SAST — QA: Day 8 cycle-20 — 6 PASS / 0 FAIL / 2 PARTIAL / 0 BLOCKED / 2 SKIPPED-BY-DESIGN (10 checkpoints) + 4 wrap-up checks (3 PASS, 1 PARTIAL — magic-link re-auth path works after backend curl fallback for missing-orgId on portal `/login` form). Fresh walk on `bugfix_cycle_2026-04-26-day8` (cut from main `e79f0b33`) as Sipho via fresh magic-link. PROP-0002 reviewed end-to-end: portal `/proposals` list → detail page (rendered Tiptap body with greeting "Dear Sipho Dlamini," + Fee Arrangement + Rate "R850/hr per LSSA 2024/2025 schedule" + standard terms + Accept/Decline buttons) → click Accept → status SENT→ACCEPTED instantly, showing success card "Thank you for accepting this proposal. Your project has been set up." → DB confirms `proposals.status=ACCEPTED`, `accepted_at=2026-04-27 03:18:57.981719+00`, `portal_proposals.status=ACCEPTED` (read-model live-synced), firm-side `PROPOSAL_ACCEPTED` notification fired with project auto-created. Page reload shows "This proposal has been accepted." with NO Accept button — double-accept prevented at UI level. `/proposals` list post-accept: "Awaiting Your Response" section header gone, PROP-0002 shows ACCEPTED badge. Terminology consistent ("proposal" throughout sidebar/heading/route/button/copy). 8.1 PARTIAL (no proposal email in Mailpit — same as §7.10 NOT-APPLICABLE; portal-list path used). 8.2 PARTIAL (no fee estimate breakdown / VAT / effective date per L-49 Sprint-3 deferral). 8.3 SKIPPED-BY-DESIGN (L-49 cascade). 8.6 N/A (this tenant uses /proposals/[id] not /accept/[token]). 8.9 SKIPPED-BY-DESIGN (no Pending-proposals home tile to drop from). **Zero new bugs logged.** Three informational follow-ups noted but NOT logged as BUG-CYCLE26-XX gaps (all pre-existing, all non-blocking): (a) §7.10 carry-forward — wire portal-new-proposal email listener; (b) cosmetic — add Pending-proposals tile to portal home dashboard; (c) frontend portal `/login` request-link form missing orgId field (backend curl with explicit orgId works). **Day 8 CLOSED** → advance to Day 10 / 10.1 (scenario skips Day 9). Tooling note: at dispatch start the Playwright MCP browser was held by stale Chrome PID 14210 from a sibling idle claude session; cleared the singleton lock and killed the stale Chrome cleanly. Cycle Count → 20.
- 2026-04-27 SAST — QA: Day 10 cycle-21 — 8 PASS / 0 FAIL / 0 PARTIAL / 0 BLOCKED / 1 SKIPPED-not-applicable (8 in-scope checkpoints + 3 wrap-up rollups all PASS). Fresh walk on `bugfix_cycle_2026-04-26-day10` (cut from main `c194faca`) as Thandi (Owner) via Keycloak. Phase A: PROP-0002 ACCEPTED state flows portal→firm cleanly — global `/proposals` shows Accepted badge + Apr 27 2026 timestamp matching Sipho's Day 8 portal accept; per-proposal detail confirms all three timestamps (Created/Sent/Accepted) at Apr 27 2026; RAF matter `cc390c4f-…` already ACTIVE (auto-active since Day 3 creation; no PROSPECT state in this build's project lifecycle enum). Phase B: Manual deposit R 50 000,00 against Sipho/RAF-2026-001 via Record Deposit dialog (UUID inputs — UX papercut, pre-existing); status RECORDED (no approval queue, dual-approval off); three reconcile points all PASS — trust account cashbook + Sipho's auto-created client ledger card (`36765353-…`, `balance=50000.00`, `total_deposits=50000.00`) + matter Trust tab "Funds Held" = R 50 000,00. Side-effect: trust-activity nudge email auto-fired to Sipho (Mailpit `m7qSdt8XULPXMs6mX6Vbcn`, "Mathebula & Partners: Trust account activity"), Day 11 §11.1 prereq satisfied. Email body has cosmetic-only polish gaps (raw ISO timestamp, unformatted `50000`, uppercase `DEPOSIT`) — will affect Day 11 §11.5 sanitisation polish but does not block the trust-ledger view. 10.6 SKIPPED-not-applicable (dual-approval off so no approval queue path to exercise). **Zero new bugs logged.** Two informational follow-ups noted but NOT logged as gaps (raw-UUID Record Deposit dialog inputs + nudge email body polish — both pre-existing scope quirks, neither blocks Day 11). **Day 10 CLOSED** → advance to Day 11 / 11.1. Cycle Count → 21.
- 2026-04-27 SAST — Orchestrator: cycle-21 CodeRabbit review (PR #1177) flagged three real product polish bugs visible in Day 10 evidence (not blockers, not from scenario walk): added BUG-CYCLE26-08 (`INTEREST_LPFF` enum-prettify renders "Interest Lpff" instead of "Interest LPFF" — `cycle21-day10-10.4-transactions-page.yml:140`), BUG-CYCLE26-09 (breadcrumb shows raw `client-ledgers` slug + raw customer UUID instead of friendly labels — `cycle21-day10-10.7-sipho-ledger-detail.yml:88-100`), BUG-CYCLE26-10 (activity-feed message templates emit literal `"for unknown"` when target/recipient name is null — `cycle21-day10-10.1-raf-matter-detail.yml:276-290`). All three logged as OPEN, severity LOW (cosmetic), to be specced/fixed after Day 11 walk closes. CR's QA-evidence-modification asks (UUID redaction, dev-overlay filtering, personal-data placeholders, snapshot-serializer changes) declined — QA evidence is point-in-time DOM capture; redacting it defeats the audit-trail purpose.
