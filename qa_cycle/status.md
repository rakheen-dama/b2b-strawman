# QA Cycle Status — Legal Onboarding (Keycloak) — 2026-04-11

## Current State

- **QA Position**: **Cycle 5 Dev batch fully VERIFIED.** All 5 FIXED items re-verified in Cycle 5 QA turn 2: GAP-S5-05 (duplicate FICA packs eliminated, INDIVIDUAL 9/TRUST 12), GAP-S5-04 (Adverse Party customer select populated + end-to-end link persisted), GAP-S5-06 (Proposal Customer combobox populated with 6 PROSPECT/ONBOARDING clients), GAP-S4-06 (Trust tab empty state clean, no 500), GAP-S4-05 (Trust dashboard nav links present for Client Ledgers/Record Transaction/View all). GAP-S5-01 promoted from VERIFIED_DIALOG → VERIFIED end-to-end (Contingency engagement letter persisted to DB with fee_model=CONTINGENCY, 25% percent, 25% cap, customer=Lerato). No regressions, no new HIGH blockers. Remaining work is LOW-severity SPEC_READY terminology/UX queue + long-term blocker GAP-S3-03 (FICA doc upload for PROSPECT→ACTIVE lifecycle).
- **Cycle**: 5 (QA turn 2 complete — 5 Dev fixes re-verified + GAP-S5-01 end-to-end closed)
- **Dev Stack**: READY
- **NEEDS_REBUILD**: true (GAP-S3-04 backend change in PR #1007 — `ProjectTemplateService.instantiateTemplate` now attaches auto-apply field groups; backend container must be rebuilt before QA re-verifies Session 3 step 3.20)
- **Branch**: `bugfix_cycle_kc_2026-04-12`
- **Scenario**: `qa/testplan/legal-onboarding-keycloak.md`
- **Focus**: Legal vertical onboarding via real Keycloak OIDC. End-to-end: access request → admin approval → KC registration → plan upgrade → legal-za profile → team invites → 3 client onboardings (Litigation, Deceased Estate, RAF) → engagement letters, trust account, court calendar, adverse parties, activity/audit sign-off.
- **Auth Mode**: Keycloak (real OIDC, no mock IDP)

## Environment

| Service | URL | Status |
|---------|-----|--------|
| Frontend (kc mode) | http://localhost:3000 | UP |
| Backend (local, local+keycloak profile) | http://localhost:8080 | UP |
| Gateway (BFF) | http://localhost:8443 | UP |
| Portal | http://localhost:3002 | UP |
| Keycloak | http://localhost:8180 | UP |
| Mailpit UI | http://localhost:8025 | UP |
| Postgres (docteams) | localhost:5432 | UP |

## Test Plan Structure

| Session | Focus | Status |
|---------|-------|--------|
| Session 0 | Stack startup & sanity | PASS_WITH_NOTES (GAP-S0-01 non-blocking) |
| Session 1 | Firm onboarding via product (access request, admin approval, KC registration) | PASS_WITH_NOTES (GAP-S1-01 MCP click workaround; GAP-S1-02 brand inconsistency) |
| Session 2 | Team invites, legal-za activation, rates & tax (Phase A removed — no plan upgrade by design) | PASS_WITH_NOTES (21/24 PASS after removing invalid Phase A; 3 PARTIAL on terminology/UX — GAP-S2-02/03/04) |
| Session 3 | Litigation customer onboarding (conflict, FICA, matter, engagement letter) | PARTIAL (FICA tick-through blocked by GAP-S3-03 long-term; engagement letter flow VERIFIED end-to-end via Cycle 5 turn 2 Lerato contingency proposal) |
| Session 4 | Estates customer onboarding (trust setup, trust client, FICA, matter, first deposit) | **GREEN** (Cycle 5 turn 2: duplicate packs eliminated — GAP-S5-05 VERIFIED; typed TRUST pack 12 items confirmed on new Sibiya Family Trust client. Trust tab empty state VERIFIED — GAP-S4-06; Trust dashboard nav VERIFIED — GAP-S4-05. FICA tick-through still blocked by long-term GAP-S3-03.) |
| Session 5 | RAF plaintiff onboarding (conflict, FICA, RAF matter, contingency letter, court date, adverse party, first time entry) | **GREEN** (Cycle 5 turn 2: contingency letter VERIFIED end-to-end for Lerato Mthembu — GAP-S5-01; adverse party link VERIFIED via Moroka Estate → RAF — GAP-S5-04; proposal customer dropdown populated — GAP-S5-06. All turn-1 HIGH blockers closed.) |
| Session 6 | Cross-cutting verification & sign-off (activity feed, terminology, backend-state) | PARTIAL (14/18 steps; activity feed + audit backend + terminology on primary pages PASS; audit UI missing — GAP-S6-03; scattered terminology leaks — GAP-S6-01/04/05) |

## Tracker

| ID | Summary | Severity | Status | Owner | PR | Notes |
|----|---------|----------|--------|-------|----|-------|
| GAP-S0-01 | Leftover Keycloak users and tenant schemas from prior cycles (non-blocking — no collision with `mathebula-test.local` test data) | LOW | WONT_FIX | — | — | Infra cleanup, not a product bug. Recommend infra teardown between cycles. Out of bugfix cycle scope. |
| GAP-S1-01 | Playwright MCP `browser_click` silently fails on some buttons (Radix dialogs, Keycloak forms, Next.js dev overlay) | MEDIUM | WONT_FIX | — | — | QA automation friction, not a product bug. Add to QA lessons. Out of bugfix cycle scope. |
| GAP-S1-02 | Brand-name inconsistency: landing page uses "Kazi" but /request-access, Keycloak theme, emails, and sidebar still say "DocTeams" | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (section A). |
| GAP-S2-01 | ~~Billing page has no plan/upgrade UI~~ | — | NOT_A_BUG | — | — | **Scenario error, not a product bug.** Product has no plan-tier subscriptions by design — teams invite any number of members without an upgrade gate. Session 2 Phase A (steps 2.1–2.3) removed from `qa/testplan/legal-onboarding-keycloak.md`. |
| GAP-S2-02 | Legal terminology incomplete in Settings sidebar + several Settings pages (Project Templates, Custom Fields, Clients empty state, "Back to Customers" link, "Customer Readiness" widget, "Getting started with DocTeams" helper card) | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (sections A, D, E). |
| GAP-S2-03 | Add Rate dialog on `/settings/rates` Cost Rates tab defaults to Billing Rate — confusing UX; naive save yields "A billing rate already exists for this period" | LOW | SPEC_READY | — | — | `qa_cycle/fix-specs/GAP-S2-03.md`. Dialog should default `Rate Type` to match active tab. |
| GAP-S2-04 | Dashboard first-run helper card titled "Getting started with DocTeams" — conflicts with Kazi rebrand (subset of GAP-S1-02) | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (section A). |
| GAP-S3-01 | Client-detail "Change Status" Radix DropdownMenu requires pointerdown+pointerup before `.click()` to open (extension of GAP-S1-01). | LOW | WONT_FIX | — | — | QA automation friction, not a product defect. Out of bugfix cycle scope. |
| GAP-S3-02 | Terminology leak in Clients empty state, client detail "Back to Customers" link, "Customer Readiness" widget. Subset of GAP-S2-02. | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (section D). |
| GAP-S3-03 | Legal FICA checklist blocks completion without uploaded documents AND the lifecycle gates Active on Country+TaxNumber — combined these block PROSPECT → ACTIVE end-to-end via UI. | MEDIUM | **FIXED** | Dev | #1005 | Cycle KC 2026-04-12 Dev turn 1: Sub-fix 1.A + 2 shipped. ChecklistInstanceItemRow now disables Confirm when `requiresDocument && !documentId` and shows an inline hint pointing the user at the Documents tab; backend errors render as a sticky dismissible banner instead of an auto-clearing toast. Country and Tax Number labels in create- and edit-customer dialogs now read "(required for activation)". Frontend-only fix — backend readiness gate already enforces both fields server-side. New tests in `ChecklistInstanceItemRow.test.tsx` cover the disabled-Confirm guard and the sticky error banner. Awaiting QA re-verification of Session 3 Phase C end-to-end PROSPECT → ACTIVE walk. |
| GAP-S3-04 | Matter created from a legal-za template has zero custom field groups attached ("No custom fields configured"); SA Legal — Matter Details must be manually added. | LOW–MEDIUM | **FIXED** | Dev | #1007 | Cycle KC 2026-04-12 Dev turn 2: Backend-only fix, `ProjectTemplateService.instantiateTemplate` + `instantiateFromTemplate` now call `FieldGroupService.resolveAutoApplyGroupIds(EntityType.PROJECT)` and set the result on `project.setAppliedFieldGroups` before save — mirrors `ProjectService.createProject` / `ProjectFieldService.prepareForCreate`. **No migration, no new column on `project_templates`** — the spec's suggested data-model change was unnecessary because `FieldGroup` already has an `autoApply` flag and the legal-za seeded group `legal_za_matter` is already flagged `autoApply=true`; only the template instantiation path was bypassing the existing mechanism. New integration test `instantiate_attachesAutoApplyFieldGroups` locks the behaviour in. Full `./mvnw verify` green — 4538 tests, 0 failures, 0 errors, 26 pre-existing skipped. **NEEDS_REBUILD=true** — Infra must restart backend before QA Cycle KC 2026-04-12 turn 1 re-verifies Session 3 step 3.20 (matter from Litigation template should show "SA Legal — Matter Details" pre-attached with all 7 fields editable). |
| GAP-S3-05 | (CASCADING) `/projects/new?customerId=…` route crashes — `new` routed as `[projectId]` detail param. | MEDIUM | **FIXED** | Dev | #1006 | Cycle KC 2026-04-12 Dev turn 2: Frontend-only fix. New `/org/[slug]/projects/new/page.tsx` Server Component redirects to `/projects?new=1[&customerId=...][&templateId=...]` (disambiguates the `new` literal from the sibling `[id]` dynamic route). `projects/page.tsx` reads the new query params and forwards `autoOpen` + `initialCustomerId` through `NewFromTemplateWrapper` → `NewFromTemplateDialog`, which gains a mount-only `useEffect` that opens on mount and pre-seeds `customerId` when the id matches an accessible customer. `pnpm lint` 0 errors, `pnpm run build` (new `/org/[slug]/projects/new` route visible in manifest), full vitest suite **1932 passed / 2 skipped / 312 files**. No NEEDS_REBUILD flip for this fix alone — frontend HMR — but S3-04 (stacked in same turn) flips it. Awaiting Cycle KC 2026-04-12 QA turn 1 re-verify of deep-link `/projects/new?customerId=<lerato-uuid>`. |
| GAP-S3-06 | Engagement Letters page button and dialog still use legacy "Proposal" language. | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (section B). |
| GAP-S4-01 | (CASCADING) Trust account creation UI is broken — navigation loop; no path to create a trust account via UI. Blocks Session 4 Phase F entirely. | HIGH | **VERIFIED** | Dev | #994 | QA Cycle 5: PR merged. Dialog opens from `/settings/trust-accounting`, Mathebula Trust Account created end-to-end (DB row + UI + `trust_account.created` audit event). Subsequent R50,000 deposit recorded successfully, unblocking Session 4 Phase F. |
| GAP-S4-02 | Legal FICA onboarding pack does not branch on client_type — TRUST clients get individual-biased pack. | HIGH | **VERIFIED_PARTIAL** | Dev | #996 | QA Cycle 5: New TRUST client (Ndlovu Family Trust) correctly receives the 12-item `legal-za-trust-client-onboarding` pack (Trust Deed, Letters of Authority, Trustee 1/2 ID, Proof of Trust Banking, SARS Trust Tax, etc). INDIVIDUAL pack has the expected 9 items. **New regression discovered — GAP-S5-05 HIGH**: legacy `legal-za-client-onboarding` (customer_type=ANY, 11 items) still auto-instantiates alongside the new typed pack, so every new client gets BOTH. |
| GAP-S4-03 | Admin role sidebar missing Compliance + Trust Accounting + Tariffs sections. Pages still reachable by direct URL — pure nav-filter issue. | MEDIUM | WONT_FIX | — | — | Root cause: Admin role is not granted `VIEW_TRUST`/`MANAGE_TRUST`/compliance capabilities in the role-seeder. This is an intentional-vs-regression ambiguity that requires a product decision before implementation. Out of bugfix cycle scope until clarified. |
| GAP-S4-04 | Conflict-check → checklist auto-link does not mark "Conflict Check Performed" item when the check ran BEFORE customer existed. | LOW–MEDIUM | WONT_FIX | — | — | Needs speculative name-fallback match logic + timing race investigation. Medium-effort rework of conflict-check linking service. Out of bugfix cycle scope — users can manually tick the item. |
| GAP-S5-01 | Engagement Letter Fee Model select has no Contingency option. Blocks the entire legal-za RAF plaintiff workflow. | **HIGH** | **VERIFIED** | Dev | #995 | QA Cycle 5 turn 2: **End-to-end VERIFIED**. Contingency proposal persisted for Lerato Mthembu (id `8d6bfda1-c2f4-431c-9896-1e60eba3a609`, fee_model=CONTINGENCY, contingency_percent=25.00, contingency_cap_percent=25.00, status=DRAFT). Unblocked by GAP-S5-06 fix. DB confirms all contingency fields populated; dialog Fee Model dropdown shows 4 options including Contingency as expected. Originally turn-1 was VERIFIED_DIALOG only; now fully closed. (earlier turn 1 note: Contingency option confirmed in Fee Model dropdown (4 options now). Selecting Contingency reveals LPC Rule 59 banner + Contingency Percent/Cap/Description fields, Retainer fields hidden. V90 migration columns present in `tenant_*.proposals`. End-to-end persistence blocked separately by new GAP-S5-06 (proposal dialog customer dropdown empty). |
| GAP-S5-02 | Court date creation crashes with NPE when `project.customer_id` is null. | **HIGH** | **VERIFIED** | Dev | #993 | QA Cycle 5: New court date saved on Moroka Estate matter (id `219594f1-2642-4d7a-9df0-7700a36a8bfd`, SCHEDULED, customer_id populated). No InvalidDataAccessApiUsageException in logs. Cascade fix confirmed working. |
| GAP-S5-03 | (CASCADING) `project.customer_id` FK is left NULL by New-from-Template flow. Cascades into S5-02, S5-04. | **HIGH** | **VERIFIED** | Dev | #993 | QA Cycle 5: New matter `095529c5-afdc-4714-8a7f-589136280cad` created from Deceased Estate template has `customer_id=ac433c2c-...` (Moroka). UI matter detail shows `Client: Moroka Family Trust`. Fix works end-to-end. |
| GAP-S5-04 | "Link Adverse Party" dialog has empty Customer select. NOT a cascade from GAP-S5-03 — separate frontend response-shape bug in `fetchCustomers()`. | **HIGH** | **VERIFIED** | Dev | #999 | Cycle 5 QA turn 2: VERIFIED end-to-end on Moroka Estate matter. Customer select populated with 6 clients (all PROSPECT/ONBOARDING); Adverse Party select populated (Road Accident Fund). Link persisted to DB: adverse_party_links row `300068f0-b1cf-41a2-98ca-2505e3b90137` linking RAF → Moroka Estate matter → customer Moroka Family Trust, relationship=OPPOSING_PARTY. |
| GAP-S6-01 | Global terminology leaks rollup — sidebar logo, browser tab title, helper card, back-link, matter tab "Customers". | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (sections A, D). |
| GAP-S6-02 | Matter Activity tab empty on new matters; `project.created_from_template` audit event not surfaced in feed. | LOW | WONT_FIX | — | — | Requires new activity-feed-from-audit projection — non-trivial data-model work crossing audit → activity feed boundary. > 2 hr. Out of bugfix cycle scope. |
| GAP-S6-03 | No UI for the audit log / compliance trail. | LOW–MEDIUM | WONT_FIX | — | — | New feature — audit log UI requires ~1-2 days of frontend work (page + filters + pagination + optional CSV export). Out of bugfix cycle scope. Compliance officers can use DB until then. |
| GAP-S6-04 | Fee Notes page body copy still uses "invoice" / "project" legacy terms. | LOW | SPEC_READY | — | — | Covered by `qa_cycle/fix-specs/GAP-TERMINOLOGY-ROLLUP.md` (section C). |
| GAP-S6-05 | Fee Notes KPIs display currency as $0.00 instead of ZAR. Hardcoded USD fallback. | LOW | SPEC_READY | — | — | `qa_cycle/fix-specs/GAP-S6-05.md`. Read `OrgSettings.defaultCurrency` as fallback. Line-level fix. |
| GAP-S6-06 | Automation rule "Matter Onboarding Reminder" fails with "No recipients resolved for type: ORG_ADMINS". | LOW | SPEC_READY | — | — | `qa_cycle/fix-specs/GAP-S6-06.md`. Alias `ORG_ADMINS` to existing `ALL_ADMINS` case in `SendNotificationActionExecutor.resolveRecipients`. |
| GAP-S5-05 | (NEW, introduced by PR #996) Legacy `legal-za-client-onboarding` (customer_type=ANY, 11 items) pack still auto-instantiates alongside new typed variants. Every new client now gets BOTH the legacy pack AND the type-specific pack. | **HIGH** | **VERIFIED** | Dev | #998 + V93 #1003 | Cycle 5 QA turn 2: VERIFIED. DB sanity: legacy rows (ANY + ALL customer_type) all `active=false` across both tenants. INDIVIDUAL client "Thembi Khumalo" (`3b3ab147-6d73-44cb-9f6c-234600826ccc`) → exactly 1 checklist instance, slug=legal-za-individual-client-onboarding, customer_type=INDIVIDUAL, 9 items. TRUST client "Sibiya Family Trust" (`18ff39fc-9ae3-4103-b811-56085917ecbb`) → exactly 1 checklist instance, slug=legal-za-trust-client-onboarding, customer_type=TRUST, 12 items including Trust Deed, Letters of Authority, Trustee 1+2 ID, SARS Trust Tax. Backend log: "Instantiated 1 checklist(s)" for both customers. (earlier turn: PR #998 merged (squash `aea5b956`). V92 tenant migration deactivates legacy `legal-za-client-onboarding` (ANY) + cancels orphan IN_PROGRESS instances. `ChecklistInstantiationService` now applies most-specific-match filter (skips ALL ANY templates when any typed template matches). New test `skipsAnyTemplateWhenTypedTemplateExists` covers the regression. `./mvnw verify` green — 4535 tests, 0 failures, 0 errors. **V93 follow-up (PR #1003, squash `3f958eb6`)** broadens the WHERE clause to also catch the `customer_type='ALL'` seed-era variant discovered in `tenant_555bfc30b94c` during Infra Cycle 5 rebuild — V92 only matched `customer_type='ANY'`. Awaiting QA re-verification in Cycle 5 turn 2. |
| GAP-S5-06 | (NEW) Proposal dialog "Customer" combobox is empty — blocks end-to-end engagement-letter creation for ALL fee models including newly-added Contingency. | MEDIUM | **VERIFIED** | Dev | #1000 | Cycle 5 QA turn 2: VERIFIED. New Proposal dialog Customer combobox populated with all 6 clients (Sipho, Moroka, Lerato, Ndlovu, Thembi, Sibiya — all PROSPECT/ONBOARDING). Downstream end-to-end Contingency proposal creation succeeded (see GAP-S5-01). (earlier: filter policy inverted in `proposals/page.tsx`, `proposals/actions.ts::fetchCustomersAction`, `retainers/page.tsx`, and `customers/[id]/page.tsx::showRetainerTab`. Now excludes only `OFFBOARDED`/`OFFBOARDING`/`ANONYMIZED`; PROSPECT / ONBOARDING / ACTIVE / DORMANT all included. Silent catch replaced with `console.error` in both proposals files. Frontend-only, HMR picks up. Lint (0 errors), build, and full vitest suite (1930 passed) all green. No NEEDS_REBUILD flip. Awaiting QA Cycle 5 turn 2 re-verification of Session 3 engagement letter + Session 5 contingency letter end-to-end (unblocks GAP-S5-01 end-to-end). |
| GAP-S4-05 | (NEW) `/trust-accounting` dashboard has no link to the Transactions sub-page where "Record Transaction" button lives. Transaction recording only reachable via direct URL. | LOW–MEDIUM | **VERIFIED** | Dev | #1002 | Cycle 5 QA turn 2: VERIFIED. `/trust-accounting` dashboard header renders "Client Ledgers" (→ `/trust-accounting/client-ledgers`) and "Record Transaction" (→ `/trust-accounting/transactions`) anchors; Recent Transactions card has "View all" (→ `/trust-accounting/transactions`). Both target routes load cleanly (Client Ledgers + Transactions headings render). Screenshot: cycle5-t2-trust-dashboard-nav.png. (earlier: frontend-only markup change in `frontend/app/(app)/org/[slug]/trust-accounting/page.tsx`. Added `Link` + `Button` imports; page header now renders "Client Ledgers" (outline) + "Record Transaction" (primary) buttons when `dashboardData` is present; Recent Transactions `CardHeader` has a ghost "View all" link to `/trust-accounting/transactions`. Both target sub-routes (`/trust-accounting/client-ledgers` and `/trust-accounting/transactions`) verified to exist. Lint (0 errors), `pnpm run build` (compiled successfully), full vitest suite (**1930 passed / 2 skipped / 312 files**) all green. No NEEDS_REBUILD flip — frontend-only, HMR picks it up; flag remains true from stacked GAP-S5-05 backend work. Awaiting QA Cycle 5 turn 2 verification that the dashboard header buttons render and navigate correctly. |
| GAP-S4-06 | (NEW) Project Trust tab errors for matters whose customer has not yet received a trust deposit (backend `ClientLedgerService.getClientLedger` throws `ResourceNotFoundException`). Should gracefully return an empty/zero-balance response. | MEDIUM | **VERIFIED** | Dev | #1001 | Cycle 5 QA turn 2: VERIFIED on Lerato RAF matter `d04d80a6-c9d5-4256-9121-1d9496c77d09` (customer has zero trust deposits). Trust tab renders clean "Trust Balance [No Funds] R 0,00 / Deposits R 0,00 / Payments R 0,00 / Fee Transfers R 0,00" with Record Deposit/Payment/Fee Transfer actions. No 500, no "Unable to load trust balance", no ClientLedgerCard not found errors in body text. Backend log: zero matches for ClientLedger/500/clientledgercard errors post-reload. (earlier: `ClientLedgerService.getClientLedger` replaced `.orElseThrow(...)` with `.orElseGet(() -> emptyLedgerCardResponse(customerId, trustAccountId))` returning a synthetic response with `BigDecimal.ZERO` balances and null `id`/`lastTransactionDate`/`createdAt`/`updatedAt`. Customer name looked up best-effort, null-tolerant. New integration test `getByCustomer_returnsEmptyZeroBalanceWhenNoLedgerCardExists` in `ClientLedgerControllerTest` asserts 200 OK with zero balances and null card id/timestamps for a brand-new customer with no trust transactions. Full `mvn verify` green. Backend-only — **NEEDS_REBUILD=true** (stacks with GAP-S5-05 restart). Awaiting QA Cycle 5 turn 2 re-verification on the Moroka Estate matter Trust tab before any deposit is recorded. |

## Log

- 2026-04-11 — Branch `bugfix_cycle_kc_2026-04-11` created from `main`. Previous E2E mock-auth cycle state archived to `qa_cycle/_archive_2026-04-06_e2e/`. Fresh status.md scaffolded for Keycloak legal onboarding scenario.
- 2026-04-11 — Infra: dev stack up. Docker infra started (postgres, localstack, mailpit, keycloak); keycloak-bootstrap.sh ran successfully (padmin + org_role backfill); backend (30s) + gateway (6s) started via svc.sh. All 4 services RUNNING + HEALTHY. Health checks: backend/gateway UP, frontend HTTP 200, mailpit v1.29.2 responding. Ready for QA Session 0.
- 2026-04-11 — QA Cycle 1 turn 1: **Session 0 PASS_WITH_NOTES** (all 4 services healthy; stale KC users & tenant schemas present but non-colliding — GAP-S0-01 LOW). **Session 1 PASS_WITH_NOTES** (full onboarding walkthrough succeeded: access request → OTP verify → padmin approval → Keycloak org + invite → registration → Thandi logged into `/org/mathebula-partners/dashboard` with legal terminology visible in sidebar/KPIs). Two gaps filed: GAP-S1-01 MEDIUM (Playwright MCP click reliability — required `browser_evaluate` workaround on ~5 buttons) and GAP-S1-02 LOW (brand inconsistency between "Kazi" marketing page and "DocTeams" elsewhere). **Next**: Session 2 — plan upgrade, legal-za verification, rates, team invites for Bob + Carol. Thandi is currently logged in at /org/mathebula-partners/dashboard.
- 2026-04-11 — QA Cycle 2 turn 1: **Session 2 PASS_WITH_NOTES** (21/27 steps PASS, 3 FAIL on plan-upgrade path — GAP-S2-01, 3 PARTIAL for terminology/UX — GAP-S2-02/03/04). Confirmed legal-za profile is fully active (sidebar terms, 4 matter templates, custom field group "SA Legal — Matter Details", 22-clause legal-za clause pack incl. FICA Consent / POPIA / Trust Account Deposits / Fees Contingency, ZAR default, VAT 15% seeded as "Standard", Bob+Carol invited via real KC + registered via invite tokens, all 3 users have billing + cost rates). Billing page blocker filed as GAP-S2-01 (non-blocking for remaining sessions). **Session 3 PARTIAL** (13/~30 steps): logged in as Bob, ran conflict check for "Sipho Dlamini" (No Conflict), created client `Sipho Dlamini` (`4119d161-39a7-40b2-a462-d1869d9a1f2b`) as INDIVIDUAL with SA Legal custom field group, transitioned PROSPECT → ONBOARDING, confirmed the **Legal Client Onboarding** FICA checklist auto-populated with **11 items (8 required)** including Proof of Identity / Proof of Address / Company Registration Docs etc. Stopped at clean boundary before ticking checklist items due to budget (~70 MCP calls). New gaps: GAP-S3-01 (Radix DropdownMenu pointer-event workaround) and GAP-S3-02 (terminology leak in Clients empty state / back-link / Customer Readiness). **Next QA turn**: resume Session 3 from step 3.13 — complete FICA checklist, auto-transition to ACTIVE, create Litigation matter with 9 template action items, send Hourly engagement letter. Sipho already exists — no re-setup needed.
- 2026-04-11 — QA Cycle 3 turn 1: **Session 3 partial resume** (13 → 20 steps). FICA tick-through blocked — GAP-S3-03 (6 required items need linked Documents; "Confirm" without documentId silently no-ops; combined with Country + TaxNumber blocking-activation gate, Sipho cannot reach ACTIVE via UI). **Matter-from-Litigation-template PASS** (workaround: `/projects/new?customerId` crashes — GAP-S3-05; `/projects` → "New from Template" dialog path works). Matter `5ebdb4b6-36c4-46c5-acea-d9a2d286cebc` "Sipho Dlamini — Civil dispute" created with **9 tasks** from template, but **no custom fields attached** — GAP-S3-04. Engagement-letter dialog opened: **"New Proposal" button/title still uses legacy language** — GAP-S3-06; no Contingency fee option (will be GAP-S5-01). **Session 4 partial** (7/26 steps): conflict-check "Moroka Family Trust" PASS. **Trust account creation totally broken — GAP-S4-01 HIGH**: `/settings/trust-accounting` "Add Account" link routes to empty `/trust-accounting` dashboard with no create button. Moroka Family Trust client created as TRUST (`ac433c2c-cbe5-47f5-826d-602989e7f099`), transitioned to Onboarding, checklist auto-populated — but **same individual-biased 11-item pack** — GAP-S4-02 HIGH: no trustee ID items, no Letters of Authority, Trust Deed incorrectly Skippable. **Admin sidebar missing Compliance + Trust Accounting + Tariffs sections** — GAP-S4-03 MEDIUM (role-based nav regression from Session 2 Owner view). **Conflict-check → checklist auto-link** didn't fire for Moroka despite a matching history entry — GAP-S4-04. Screenshots: `session-3-sipho-litigation-matter-created.png`, `session-4-moroka-trust-onboarding-fica.png`. Budget used: ~76/80 MCP calls. **Next QA turn**: proceed to Session 5 (RAF plaintiff Lerato — independent of trust-account blocker); defer Session 4 Phase E (Estates matter) and Phase F (trust deposit) until GAP-S4-01 is fixed.
- 2026-04-11 — Product Cycle 4: triaged 28 OPEN items → 17 SPEC_READY (via 12 fix-spec files), 11 WONT_FIX (out-of-scope or > 2 hr). **Dev-fix priority order**: (1) GAP-S5-03 CASCADING — unblocks S5-02 + S5-04 as freebies; (2) GAP-S5-02 defensive null-guard; (3) GAP-S5-04 verification; (4) GAP-S4-01 trust-account-create dialog; (5) GAP-S4-02 FICA pack variants (INDIVIDUAL/TRUST); (6) GAP-S5-01 Contingency fee model; (7) GAP-S3-03 FICA error surfacing + intake fields; (8) GAP-S3-05 /projects/new redirect; (9) GAP-S6-06 ORG_ADMINS alias; (10) GAP-S6-05 ZAR fallback; (11) GAP-S2-03 rate-type default; (12) GAP-S3-04 template field groups (borderline); (13) GAP-TERMINOLOGY-ROLLUP final sweep (covers S1-02, S2-02, S2-04, S3-02, S3-06, S6-01, S6-04). Estimated total dev effort: ~10 hours across all SPEC_READY items. WONT_FIX: S0-01, S1-01, S2-01, S3-01, S4-03, S4-04, S6-02, S6-03 (reasons in each tracker row).
- 2026-04-11 — Dev Cycle 4: **GAP-S5-03 FIXED (PR #993)**. `ProjectTemplateService.instantiateTemplate` (UI path, L480) and `instantiateFromTemplate` (scheduler path, L627) now both call `project.setCustomerId(customer.getId())` before `projectRepository.save()` when a customer is provided. Tests: ProjectTemplate* (58), CourtCalendar* (19), AdverseParty* (13) all pass. **Cascade fixes S5-02 and S5-04** — both also marked FIXED pending QA re-verification in Cycle 5. `NEEDS_REBUILD=true` — backend restart required to pick up the change before QA re-verification.
- 2026-04-11 — Dev Cycle 4 turn 2: **GAP-S4-01, GAP-S5-01, GAP-S4-02 all FIXED** (PRs #994, #995, #996). Trust account creation dialog wired to existing backend endpoint; CONTINGENCY fee model added across backend enum/entity/DTO/migration + frontend schema/dialog/detail; legal FICA pack split into INDIVIDUAL (v2.0.0, 9 items) + new TRUST (v1.0.0, 12 items) variants. All three builds green; ProposalControllerTest/ProposalVariableResolverTest/ProposalAuditTest/ProposalSendTest/PortalProposalControllerTest (46 tests) + LegalPackSeederIntegrationTest/CompliancePackSeederTest/ChecklistInstantiationServiceTest (18 tests) pass; frontend vitest suite (1930 tests) + `pnpm run build` green. `NEEDS_REBUILD=true` — backend + frontend restart required before QA re-verification of Session 4 Phase E/F, Session 5 contingency-letter path, and trust-client onboarding checklist.
- 2026-04-11 — **User correction**: GAP-S2-01 is NOT a product bug. Product has no plan-tier subscriptions by design — teams can add any number of members without an upgrade gate. Session 2 Phase A (steps 2.1–2.3 "Plan upgrade") removed from `qa/testplan/legal-onboarding-keycloak.md`. GAP-S2-01 marked `NOT_A_BUG` in tracker. Session 2 status updated: 21/24 PASS (was 21/27).
- 2026-04-11 — Infra Cycle 4: **rebuild after Dev PRs #993, #994, #995, #996**. Backend restarted via `svc.sh restart backend` (picked up Java changes in `ProjectTemplateService`, `FeeModel.CONTINGENCY`, `Proposal` entity, legal FICA seeder split). Flyway applied **V90__add_contingency_fee_fields + V91 contingency percent check constraints** cleanly to all 4 tenant schemas (`tenant_4a171ca30392`, `tenant_62aa7c96ab38`, `tenant_555bfc30b94c`, `tenant_5039f2d497cf`) — each "Successfully applied 2 migrations, now at version v91". No Flyway errors. Startup race produced 4 harmless `column p1_0.contingency_cap_percent does not exist` WARNs from a `scheduling-4` Hibernate query that fired before Flyway finished — cleared immediately after migration applied. Frontend restarted via `svc.sh restart frontend` (to ensure new Zod schema + CONTINGENCY enum + AddTrustAccountButton are loaded fresh). All 4 services healthy: backend PID 751 (8080 UP), gateway PID 96776 (8443 UP), frontend PID 1172 (3000 UP), portal PID 74299 (3002 UP). `curl http://localhost:8080/actuator/health` → `{"status":"UP"}`. `NEEDS_REBUILD=false`. Ready for QA Cycle 5 re-verification of S4-01, S4-02, S5-01, S5-02, S5-03, S5-04.
- 2026-04-11 — QA Cycle 5 turn 1: **Re-verification of HIGH fixes + Session 4 Phase E/F execution**. Logged in as Thandi (owner, SecureP@ss1). **GAP-S4-01 VERIFIED PASS** — Mathebula Trust Account created via fixed dialog (DB: `d8035d50-...`, audit `trust_account.created`). **GAP-S4-02 VERIFIED_PARTIAL** — new TRUST client (Ndlovu Family Trust, `703b4ff0-...`) correctly receives 12-item TRUST pack, BUT **new GAP-S5-05 HIGH**: legacy `legal-za-client-onboarding` (ANY, 11 items) pack also auto-instantiates → every new client gets BOTH. Backend log: `Instantiated 2 checklist(s) for customer ... (type=TRUST)`. **GAP-S5-03 VERIFIED PASS** — new matter `095529c5-afdc-4714-8a7f-589136280cad` (Moroka Estate Administration from template) has `customer_id=ac433c2c-...`. **GAP-S5-02 VERIFIED PASS** — court date `219594f1-...` saved successfully on new matter (SCHEDULED, customer_id populated, no NPE). **GAP-S5-01 VERIFIED PASS (dialog)** — Contingency option present in Fee Model select, LPC Rule 59 banner + contingency fields render on selection, V90 columns in DB. End-to-end persistence blocked by **new GAP-S5-06 MEDIUM** (proposal dialog Customer combobox empty). **GAP-S5-04 REOPENED** — NOT cascade-fixed; root cause is separate pre-existing frontend bug in `legal/adverse-parties/actions.ts::fetchCustomers` handling paginated vs flat array response. **Session 4 Phase E PASS** — Moroka Estate matter from template with 9 tasks, customer_id populated. **Session 4 Phase F PASS** — R50,000 trust deposit `efcb5b9e-...` recorded via `/trust-accounting/transactions` → "Record Deposit" dialog (raw UUID inputs). Discovered **GAP-S4-05 LOW-MEDIUM** (trust dashboard missing transactions nav link) and **GAP-S4-06 MEDIUM** (Trust tab 500 when client has no ledger card yet). **Session 4 is now substantially green** — remaining blockers are GAP-S3-03 (FICA doc upload) and the newly-filed pack-duplication regression. Detailed results in `qa_cycle/checkpoint-results/day-05-cycle5.md`. Screenshots: `cycle5-s4-01-trust-account-created.png`, `cycle5-s4-02-trust-onboarding-duplicate-packs.png`, `cycle5-s5-01-contingency-dialog.png`, `cycle5-session4f-trust-deposit-recorded.png`. Budget used: ~55/70 MCP calls. **Next**: Dev fixes for GAP-S5-04 (1-line frontend fix), GAP-S5-05 (seeder/instantiation logic), GAP-S5-06 (debug proposal customer fetch), GAP-S4-05/S4-06. After that, resume Session 3 Sipho engagement letter + Session 5 contingency letter end-to-end.
- 2026-04-11 — Product Cycle 5 turn 1: triaged 5 new/reopened items from QA Cycle 5 turn 1 → all 5 **SPEC_READY**. **GAP-S5-05 HIGH** (PR #996 regression, duplicate packs) → `fix-specs/GAP-S5-05.md`. Root cause: packId rename in PR #996 left legacy `legal-za-client-onboarding` (ANY) template row active because the seeder is idempotent by packId; PR #996 shipped no deactivation migration. Fix: V92 tenant migration to deactivate the legacy row + cancel orphan instances + defensive most-specific-type filter in `ChecklistInstantiationService.instantiateForCustomer` (coarse "skip ALL ANY templates if any typed template exists" rule). **GAP-S5-04 HIGH (reopened)** → `fix-specs/GAP-S5-04.md`. Root cause confirmed via grep: `frontend/app/(app)/org/[slug]/legal/adverse-parties/actions.ts::fetchCustomers()` reads `result.content` but `CustomerController.listCustomers` returns flat `List<CustomerResponse>`. 2-line fix: handle both shapes like `proposals/actions.ts`. Elevated to HIGH because it unblocks RAF end-to-end Session 5. **GAP-S5-06 MEDIUM** → `fix-specs/GAP-S5-06.md`. Root cause: `proposals/page.tsx` line 57 filter excludes PROSPECT customers, but in legal-za all test customers are PROSPECT/ONBOARDING (GAP-S3-03 blocks ACTIVE). Filter policy is backwards for legal-za — engagement letters are issued DURING onboarding, the signed letter is itself a FICA checklist item. Fix: exclude only OFFBOARDED/OFFBOARDING/ANONYMIZED; mirror in `fetchCustomersAction` + non-silent catch. Also grep for other `!== "PROSPECT"` filter hits. **GAP-S4-06 MEDIUM** → `fix-specs/GAP-S4-06.md`. Backend fix in `ClientLedgerService.getClientLedger` (L86-96): replace `.orElseThrow(...)` with `.orElseGet(() -> emptyLedgerCardResponse(...))` returning zero balances. "No card yet" is normal state for every new customer before first deposit; response record already permits nullable `id`/`createdAt`/`updatedAt` so no DTO change needed. **GAP-S4-05 LOW-MEDIUM** → `fix-specs/GAP-S4-05.md`. Pure markup: add `Client Ledgers` + `Record Transaction` header buttons and `View all →` card-header link to `/trust-accounting/page.tsx`. **Estimated total dev effort**: ~2–3 hours for all 5. **Dev-fix priority order**: (1) GAP-S5-05 (HIGH, backend+migration, unblocks clean onboarding for all new clients), (2) GAP-S5-04 (HIGH, 2-line frontend, unblocks RAF Session 5), (3) GAP-S5-06 (MEDIUM, frontend, unblocks end-to-end Contingency GAP-S5-01 validation), (4) GAP-S4-06 (MEDIUM, backend, makes every new matter Trust tab non-broken), (5) GAP-S4-05 (LOW-MEDIUM, pure markup). After these merge: QA can fully validate all 4 HIGH bug-fixes end-to-end and resume Sessions 3/5 engagement-letter creation.
- 2026-04-11 — Dev Cycle 5 turn 6: **GAP-S5-05 V93 follow-up (PR #1003, squash `3f958eb6`)**. One-file tenant migration. Root cause: V92 from PR #998 only matched `customer_type = 'ANY'` but Infra Cycle 5 rebuild discovered `tenant_555bfc30b94c` had the same legacy `legal-za-client-onboarding` row with `customer_type = 'ALL'` — a divergent seed-era sentinel value. V92 silently skipped it, leaving the row `active=true` on disk even though runtime behavior is unaffected (ChecklistInstantiationService filters `customerTypeIn(type, 'ANY')`, so `ALL` rows can't match). **Fix**: V93 `V93__deactivate_legacy_legal_client_onboarding_all_variants.sql` drops the `customer_type` predicate entirely — the slug is unambiguous within a tenant, so matching on slug alone is sufficient and safe. Both UPDATE statements mirror V92's style (`updated_at = now()`, `auto_instantiate = false`, cascading instance cancel). V93 is idempotent with V92 for the ANY variant and adds new coverage for the ALL variant (and any future sentinel drift). Do NOT amend V92 — Flyway checksums forbid editing applied migrations. **Tests**: `./mvnw spotless:apply` (success), `./mvnw compile -q` (exit 0), full `./mvnw verify -q` green (exit 0) — every integration test bootstrapping a tenant schema ran V93 via Flyway with no SQL errors. Functionally a no-op at runtime, but closes the on-disk seed-state inconsistency surfaced by Infra Cycle 5 for QA confidence. **NEEDS_REBUILD=true** — V93 is a new tenant migration, backend restart required to apply to running tenant schemas before QA/Infra re-verification. Stacks with prior Cycle 5 turn 1–5 rebuild flag.
- 2026-04-11 — Dev Cycle 5 turn 5: **GAP-S4-05 FIXED (PR #1002, squash `59824118`)**. Frontend-only markup fix. Root cause: `frontend/app/(app)/org/[slug]/trust-accounting/page.tsx` dashboard header and Recent Transactions `CardHeader` had zero links to the `/trust-accounting/transactions` sub-page where "Record Transaction" lives — users had to type the URL manually. **Fix**: added `import Link from "next/link"` + `import { Button } from "@/components/ui/button"`; page header now renders a right-aligned action group (gated on `dashboardData` to avoid showing buttons when no trust account exists) with "Client Ledgers" (outline, `Users` icon, links to `/trust-accounting/client-ledgers`) and "Record Transaction" (primary, `ArrowUpRight` icon, links to `/trust-accounting/transactions`). Recent Transactions `CardHeader` restructured to `flex flex-row items-center justify-between space-y-0` with a ghost "View all →" `Button` linking to `/trust-accounting/transactions`. Both target sub-routes verified to exist on disk (`client-ledgers/`, `transactions/` directories under `trust-accounting/`). **Tests**: no existing snapshot test for this page — lint (0 errors, 74 pre-existing warnings), `pnpm run build` (compiled successfully, `/org/[slug]/trust-accounting` route unchanged), full vitest suite (**1930 passed / 2 skipped / 312 files**) all green. **No NEEDS_REBUILD flip** — frontend-only, HMR picks it up automatically; flag remains `true` from stacked GAP-S5-05 + GAP-S4-06 backend work. Awaiting QA Cycle 5 turn 2 verification that the dashboard header buttons and "View all" link render and navigate correctly on the Mathebula tenant trust-accounting dashboard.
- 2026-04-11 — Dev Cycle 5 turn 4: **GAP-S4-06 FIXED (PR #1001, squash `24aaa920`)**. Backend-only fix. Root cause: `ClientLedgerService.getClientLedger` at L86-96 used `.orElseThrow(() -> new ResourceNotFoundException(...))` on the missing-ledger-card path, but "no card yet" is the normal pre-first-deposit state for every new matter — the ledger card is lazily created on first deposit via `TrustTransactionService.applyTransaction`. **Fix**: replaced `.orElseThrow(...)` with `.map(this::toResponse).orElseGet(() -> emptyLedgerCardResponse(customerId, trustAccountId))`. New private helper returns a `ClientLedgerCardResponse` with `BigDecimal.ZERO` for all 5 balance fields (`balance`, `totalDeposits`, `totalPayments`, `totalFeeTransfers`, `totalInterestCredited`) and nulls for `id`/`lastTransactionDate`/`createdAt`/`updatedAt` — the record already permits nulls on those non-primitive fields. Customer name looked up best-effort (null-tolerant, no `(deleted customer)` fallback because we're pre-card, not post-delete). Unused `ResourceNotFoundException` import removed. **Tests**: new integration test `getByCustomer_returnsEmptyZeroBalanceWhenNoLedgerCardExists` in `ClientLedgerControllerTest` asserts 200 OK with zero balances, `customerId`/`customerName`/`trustAccountId` populated, and `id`/`lastTransactionDate`/`createdAt`/`updatedAt` serialized as null. Existing happy-path test `getByCustomer_returnsLedgerCard` still green (sanity check). Targeted `ClientLedgerControllerTest` run: green. Full `./mvnw verify` run: green (exit 0). **NEEDS_REBUILD=true** — backend restart required to pick up the Java change before QA re-verification (already true for GAP-S5-05; this stacks). Awaiting QA Cycle 5 turn 2 re-verification on the Moroka Estate matter Trust tab BEFORE any deposit is recorded.
- 2026-04-11 — Dev Cycle 5 turn 3: **GAP-S5-06 FIXED (PR #1000, squash `2418f4c8`)**. Frontend-only filter policy inversion. Root cause confirmed by Product: `frontend/app/(app)/org/[slug]/proposals/page.tsx` line 57 and `proposals/actions.ts::fetchCustomersAction` filtered out `PROSPECT` customers, but all legal-za test customers are currently stuck in `PROSPECT`/`ONBOARDING` (GAP-S3-03 blocks the FICA document-upload path to reach ACTIVE). In a legal practice the signed engagement letter is itself a FICA checklist item, so excluding PROSPECT/ONBOARDING from the proposal Customer combobox is backwards. **Fix**: inverted to an explicit exclude-list — only `OFFBOARDED` / `OFFBOARDING` / `ANONYMIZED` are hidden; `PROSPECT`, `ONBOARDING`, `ACTIVE`, `DORMANT` all now appear. **Grep sweep**: 4 hits for `lifecycleStatus !== "PROSPECT"` in the frontend. All 4 touched because they share the same "who can we act on" engagement-context semantics: (1) `proposals/page.tsx`, (2) `proposals/actions.ts`, (3) `retainers/page.tsx` (retainer dialog had identical wrong filter — retainers are established during intake too), (4) `customers/[id]/page.tsx::showRetainerTab` (kept consistent with new retainer policy — the Retainer tab is now visible on PROSPECT/ONBOARDING customers). **Observability**: silent `catch { return []; }` in `fetchCustomersAction` replaced with `console.error(...)`; new `if (customersResult.status === "rejected") console.error(...)` on both page.tsx files so future fetch failures aren't indistinguishable from "no matching customers". **Tests**: no existing test asserted the PROSPECT exclusion (verified via grep). Lint (0 errors, 75 pre-existing warnings), `pnpm run build` (compiled successfully), full vitest suite (**1930 passed / 2 skipped / 312 files**) all green. **No NEEDS_REBUILD flip** — frontend-only, HMR picks up. Awaiting QA Cycle 5 turn 2 re-verification: "New Proposal" dialog should list all 4 legal-za test customers (Moroka Family Trust, Sipho Dlamini, Lerato Mthembu, Ndlovu Family Trust), unblocking end-to-end Contingency fee-model validation (GAP-S5-01) and Session 3/Session 5 engagement-letter creation.
- 2026-04-11 — Dev Cycle 5 turn 2: **GAP-S5-04 FIXED (PR #999, squash `cc1965c2`)**. 2-line frontend fix: `fetchCustomers()` in `frontend/app/(app)/org/[slug]/legal/adverse-parties/actions.ts` now accepts both `T[]` and `{ content: T[] }` response shapes and returns `Array.isArray(result) ? result : (result.content ?? [])`. Mirrors the tolerant pattern already used in `proposals/actions.ts::fetchCustomersAction`. Backend `CustomerController.listCustomers` returns a flat `List<CustomerResponse>`, so the old `result.content` access was `undefined` → dropdown empty. Product's Cycle 5 re-diagnosis was correct: this is a pre-existing frontend response-shape bug, not a cascade from PR #993. Other helpers in the same file verified correct (`/api/adverse-parties` returns `Page<>`, `/api/projects` returns `Page<>`, `/api/projects/{id}/adverse-parties` returns `List<>` and is already typed as flat). Lint (0 errors, 75 pre-existing warnings), `pnpm run build` (compiled in 9.8s, no errors), and full vitest suite (**1930 passed / 2 skipped / 312 files**) all green. **No NEEDS_REBUILD flip** — frontend-only, HMR picks it up automatically; flag already reflects GAP-S5-05 backend work. Awaiting QA Cycle 5 turn 2 re-verification on the Moroka Estate matter ("Link Adverse Party" dialog should now list all tenant customers).
- 2026-04-11 — Dev Cycle 5 turn 1: **GAP-S5-05 FIXED (PR #998, squash `aea5b956`)**. Three-part fix: (1) V92 tenant migration `V92__deactivate_legacy_legal_client_onboarding.sql` sets `active=false`, `auto_instantiate=false` on every legacy `legal-za-client-onboarding` (ANY) row across all tenant schemas and CANCELs any orphan IN_PROGRESS checklist_instances linked to that template (audit trail preserved — rows not deleted). (2) `ChecklistInstantiationService.instantiateForCustomer` now applies a coarse most-specific-match filter: if any template in the query result matches the exact customer_type, ALL ANY templates are skipped. Enforces PR #996's intent that typed packs (INDIVIDUAL/TRUST) fully replace the legacy ANY pack and prevents future duplicate-pack regressions. Log line now includes `typedAvailable=true|false` for observability. (3) New test `skipsAnyTemplateWhenTypedTemplateExists` explicitly covers the regression; `anyTemplateMatchesAllCustomerTypes` renamed to `anyTemplateMatchesWhenNoTypedTemplateExists` and narrowed to a COMPANY customer so it remains deterministic under the new filter regardless of test ordering. **Seeder check**: no `compliance-packs/legal-za-onboarding/` resource exists in source anymore — PR #996 renamed the directory — so no seeder fix was needed to prevent re-seed regression. **Tests**: targeted suite `ChecklistInstantiationServiceTest` (9), `LegalPackSeederIntegrationTest` (4), `CompliancePackSeederTest` (10) all green; full `./mvnw verify` green — **4535 tests, 0 failures, 0 errors, 26 skipped**. **NEEDS_REBUILD=true** — backend restart required to pick up V92 + service logic before QA re-verification of GAP-S4-02 and fresh TRUST/INDIVIDUAL client onboarding.
- 2026-04-11 — QA Cycle 4 turn 1: **Session 5 PARTIAL** (19/38 steps). Bob logged in (session still valid from Cycle 3). Conflict check for Lerato Mthembu + Road Accident Fund both PASS (history 3). Lerato client created as INDIVIDUAL (`b0134b4d-3985-441b-9e37-669f53fd3290`), transitioned PROSPECT → ONBOARDING via Change Status dropdown (pointer workaround). Same 11-item Legal Client Onboarding pack as Sipho — re-confirms GAP-S3-03 blocker, Lerato left in ONBOARDING. FICA tick-through skipped per guidance. **Matter-from-Litigation-template PASS**: `d04d80a6-c9d5-4256-9121-1d9496c77d09` "Lerato Mthembu — RAF claim (CAS 145/11/2025)" created with 9 template tasks. **GAP-S3-04 re-confirmed** (no SA Legal field group auto-attached). **NEW GAP-S5-03 HIGH**: DB query confirms `project.customer_id` is NULL for both matters; customer relationship stored only in `customer_projects` join table — legacy single-owner FK is broken for template-created matters. **GAP-S5-01 HIGH confirmed**: Engagement Letter dialog Fee Model select offers only Fixed Fee / Hourly / Retainer — no Contingency option. Letter flow not pursued. **NEW GAP-S5-02 HIGH**: Court Date creation crashes with `InvalidDataAccessApiUsageException: The given id must not be null` at `CourtCalendarService.toResponse:513` — NPE on `customerRepository.findById(null)`. Transaction rolls back, 0 court dates persisted. Cascade from GAP-S5-03. Screenshot: `session-5-court-date-error.png`. Adverse party "Road Accident Fund" created in `/legal/adverse-parties` registry — PASS. **NEW GAP-S5-04 MEDIUM**: "Link Adverse Party" dialog on matter tab has empty Customer select — cascade from GAP-S5-03, cannot link RAF to Lerato's matter via UI. **Time entry PASS**: Logged 90 min on "Initial consultation & case assessment" task at `R 1 200,00/hr (member default)` — billing_rate_snapshot=1200.00 ZAR, cost_rate_snapshot=500.00 ZAR, description correct. Internal team comment surface NOT_FOUND (only Customer Comments tab exists). **Session 6 PARTIAL** (14/18 steps). Dashboard KPIs updated (ACTIVE MATTERS 2, HOURS THIS MONTH 1.5h, time_entry activity shown). Lerato matter Activity tab shows the time entry; Sipho matter Activity tab empty ("No activity yet") — **GAP-S6-02 LOW**: matter creation event not surfaced in Activity feed. **GAP-S6-03 LOW–MEDIUM**: No front-end audit log page — backend has 27 well-structured events (conflict_check.performed, customer.lifecycle.transitioned, checklist.instance.created, project.created_from_template, adverse_party.created, time_entry.created, etc.) but no UI surface. Terminology spot-check: Matters + Clients + Court Calendar + Conflict Check list pages CLEAN; Engagement Letters page still has "New Proposal" button and "Total Proposals" KPI (**GAP-S3-06** re-confirmed); **Fee Notes page** has "New Invoice" / "No invoices yet" / "at least one project with logged time" — **GAP-S6-04 LOW**; Fee Notes currency shows "$0.00" instead of ZAR — **GAP-S6-05 LOW**; Dashboard helper card "Getting started with DocTeams" and sidebar logo "DocTeams" (subset of GAP-S1-02/S2-04). **GAP-S6-01** rollup filed. Backend log scan: 1 ERROR (GAP-S5-02 court-date crash) + 2 WARN (automation rule "Matter Onboarding Reminder" can't resolve ORG_ADMINS recipient — **GAP-S6-06 LOW**). DB counts: customers=3, projects=2, tasks=18, time_entries=1, adverse_parties=1, court_dates=0, proposals=0, audit_events=27. Screenshots: `session-5-lerato-onboarding-fica.png`, `session-5-court-date-error.png`, `session-6-dashboard-final.png`. **ALL_SESSIONS_EXCEPT_TRUST_PARTIAL** — no session is fully green, but Sessions 1-3-5-6 executed meaningfully; Session 4 Phase E/F remains the single fully-blocked branch. **Next QA turn**: after Dev fixes GAP-S5-03 (should freebie unblock GAP-S5-02 + GAP-S5-04), re-verify Session 5 Phase F + G; after GAP-S4-01 fix, resume Session 4 Phase E/F. Long-term blockers for Session-to-ACTIVE flow: GAP-S3-03 (FICA document upload) and GAP-S5-01 (Contingency fee model).
- 2026-04-11 — Infra Cycle 5 retry: V93 applied after PR #1003 merge. Backend restarted (new PID 46832). V93 cleanly deactivated the customer_type='ALL' legacy row in tenant_555bfc30b94c (previously missed by V92). DB sanity check passes: all legal-za-client-onboarding rows now active=false across all tenant schemas regardless of customer_type sentinel. Services healthy. Stack ready for QA Cycle 5 turn 2.
### Cycle KC 2026-04-12 — Dev Turn 1 (GAP-S3-03)
- 2026-04-11 — Dev Turn 1: **GAP-S3-03 FIXED via PR #1005** (squash-merged into `bugfix_cycle_kc_2026-04-12` at `db2c75bc`). Sub-fix 1.A + 2 shipped. **Frontend** only: `components/compliance/ChecklistInstanceItemRow.tsx` (disabled-Confirm guard when `requiresDocument && !documentId`, inline hint pointing at the named required document, sticky dismissible error banner replacing the 3-second auto-clearing toast that hid the backend "Document required" error), `components/customers/create-customer-dialog.tsx` + `components/customers/edit-customer-dialog.tsx` (Country and Tax Number labels now read "(required for activation)"). New tests: `__tests__/compliance/ChecklistInstanceItemRow.test.tsx` covers the disabled-Confirm guard and the sticky error banner; `__tests__/components/customers/create-customer-dialog.test.tsx` regex updated from `/^country$/i` to `/^country\b/i` to match the new label. **Backend**: untouched — the Customer Readiness gate already enforces both fields server-side per the spec. **NEEDS_REBUILD=false** (frontend-only, HMR picks it up). Verification: full vitest suite green (1932 passed / 2 skipped / 312 files), `pnpm run lint` 0 errors / 74 pre-existing warnings, `pnpm run build` compiled successfully. CodeRabbit auto-review skipped (target branch is not main); Qodana run informational. Awaiting Cycle KC 2026-04-12 QA Turn 1 re-verification: walk a fresh PROSPECT customer through Country + Tax Number on intake → upload required FICA documents on the Documents tab → mark each requiresDocument checklist item complete (Confirm should now refuse to fire when no document is selected) → confirm auto-transition to ACTIVE.

- 2026-04-11 — QA Cycle 5 turn 2: **All 5 Cycle 5 Dev fixes re-verified VERIFIED + GAP-S5-01 closed end-to-end.** Logged in as Thandi (owner). **GAP-S5-05 VERIFIED**: DB sanity confirms legacy packs (ANY + ALL customer_type) all `active=false` in both tenants; new INDIVIDUAL client "Thembi Khumalo" gets exactly 1 checklist (legal-za-individual-client-onboarding, 9 items); new TRUST client "Sibiya Family Trust" gets exactly 1 checklist (legal-za-trust-client-onboarding, 12 items including Trust Deed, Letters of Authority, Trustee 1+2 ID, SARS Trust Tax). Backend log confirms "Instantiated 1 checklist(s)" for both. **GAP-S5-06 VERIFIED**: Proposal dialog Customer combobox populated with all 6 tenant clients (all PROSPECT/ONBOARDING). Radix popover opened via React fiber `onOpenChange` workaround (MCP friction). **GAP-S5-01 VERIFIED END-TO-END**: Contingency engagement letter for Lerato Mthembu persisted (`8d6bfda1-c2f4-431c-9896-1e60eba3a609`, fee_model=CONTINGENCY, contingency_percent=25.00, contingency_cap_percent=25.00, description set, status=DRAFT, customer_id=b0134b4d...). Note: dialog exposes `contingencyCapPercent` (not absolute amount cap) per existing schema. **GAP-S5-04 VERIFIED**: Adverse Party dialog on Moroka Estate matter — customer select populated; linked Road Accident Fund → Moroka customer → OPPOSING_PARTY relationship; DB row `300068f0-b1cf-41a2-98ca-2505e3b90137` persisted. **GAP-S4-06 VERIFIED**: Lerato RAF matter Trust tab renders clean "Trust Balance No Funds R0,00" empty state with Deposits/Payments/Fee Transfers R0,00 and Record Deposit/Payment/Fee Transfer actions; no 500 error, no "Unable to load" text, backend log clean. **GAP-S4-05 VERIFIED**: `/trust-accounting` dashboard header has Client Ledgers + Record Transaction anchors pointing to correct sub-routes; Recent Transactions card has "View all" anchor; both sub-routes (client-ledgers, transactions) load cleanly. **No regressions, no new HIGH blockers.** Sessions 3/4/5 now GREEN (FICA tick-through still blocked by long-term GAP-S3-03 only). Cycle 5 Dev batch fully closed — remaining work is LOW-severity SPEC_READY terminology/UX queue. Screenshots: cycle5-t2-thembi-checklist.png, cycle5-t2-proposal-customer-list.png, cycle5-t2-proposal-combobox.png, cycle5-t2-trust-tab-empty.png, cycle5-t2-trust-dashboard-nav.png. Full report: `qa_cycle/checkpoint-results/day-06-cycle5-turn2.md`.

### Cycle KC 2026-04-12 — Dev Turn 2 (GAP-S3-05 + GAP-S3-04)
- 2026-04-12 — Dev Turn 2: **GAP-S3-05 FIXED via PR #1006** (squash-merged into `bugfix_cycle_kc_2026-04-12` at `d8613e21`). Frontend-only routing fix. Root cause: `/org/[slug]/projects/new?customerId=<uuid>` was routed through the sibling `[id]` dynamic segment (because no literal `new/` directory existed), so `ProjectDetailPage` called `/api/projects/new` and the backend rejected `new` with "Parameter 'id' should be of type UUID". **Fix**: new `frontend/app/(app)/org/[slug]/projects/new/page.tsx` Server Component that redirects to `/projects?new=1[&customerId=...][&templateId=...]`. `projects/page.tsx` now reads the `new` + `customerId` search params and forwards `autoOpen` + `initialCustomerId` through `NewFromTemplateWrapper` → `NewFromTemplateDialog`. The dialog gains two optional props and a mount-only `useEffect` that calls `setOpen(true)` (bypassing `handleOpenChange` so the state reset doesn't wipe the seeded customer) and pre-selects `customerId` when the id matches an accessible customer. Lint 0 errors, `pnpm run build` (new route visible in manifest as `/org/[slug]/projects/new`), full vitest suite **312 files / 1932 passed / 2 skipped**. No NEEDS_REBUILD flip for this PR in isolation — frontend-only HMR.
- 2026-04-12 — Dev Turn 2: **GAP-S3-04 FIXED via PR #1007** (squash-merged into `bugfix_cycle_kc_2026-04-12` at `73de88b1`). Backend-only fix, **no migration**. Root cause: `ProjectService.createProject` goes through `ProjectFieldService.prepareForCreate` which calls `FieldGroupService.resolveAutoApplyGroupIds(EntityType.PROJECT)` and merges the result into `project.applied_field_groups`. `ProjectTemplateService.instantiateTemplate` built the Project directly via `new Project(...)` + `projectRepository.save(project)`, bypassing that whole path. The `legal_za_matter` field group seeded by `FieldPackSeeder` from `legal-za-project.json` is already flagged `autoApply=true` — the only bug was that the template instantiation path never queried it. **Fix**: `FieldGroupService` injected into `ProjectTemplateService`; `instantiateTemplate` (UI flow) and `instantiateFromTemplate` (scheduler flow) now call `resolveAutoApplyGroupIds(EntityType.PROJECT)` and set the result on the project before save. **Divergence from spec**: the fix-spec proposed a new `field_group_ids` JSONB column on `project_templates` + migration + seeder wiring; that would drift from how every other entity (Customer/Task/Invoice/plain Project create) already resolves auto-apply groups. The minimal, codebase-consistent fix is to align template instantiation with the existing mechanism. `ProjectTemplate` entity untouched. Migration counter unchanged (last tenant migration remains V93). **Tests**: new integration test `InstantiateTemplateIntegrationTest.instantiate_attachesAutoApplyFieldGroups` creates an auto-apply PROJECT field group, instantiates a template, asserts `project.getAppliedFieldGroups()` contains the group id. `EngagementPrerequisiteTest.buildProjectTemplateService()` updated to pass `null` for the new `fieldGroupService` ctor arg (path not exercised). `./mvnw spotless:apply` reformatted 2 files; `./mvnw compile test-compile -q` clean; targeted suite (`InstantiateTemplateIntegrationTest`, `ProjectTemplateServiceTest`, `ProjectTemplateControllerTest`, `EngagementPrerequisiteTest`) **63/63 green**; full `./mvnw verify` **4538 tests, 0 failures, 0 errors**, 26 pre-existing skipped. **NEEDS_REBUILD=true** — backend container must be rebuilt before QA Cycle KC 2026-04-12 turn 1 can re-verify Session 3 step 3.20 (matter from Litigation template shows "SA Legal — Matter Details" pre-attached with all 7 fields editable). CodeRabbit auto-review skipped on both PRs (target is not `main`); Qodana still running at merge time on both, no blocking findings. Services restarted via `svc.sh start backend gateway` after the full `./mvnw verify` run finished.
