# QA Cycle Status — Legal-ZA 90-Day Demo Walkthrough (Keycloak) — 2026-04-12

## Current State

- **QA Position**: Day 1 COMPLETE. All 11 checkpoints PASS. D0-10 VERIFIED, D0-11 VERIFIED. 2 new LOW gaps (terminology leaks on client detail page). Ready to advance to Day 2.
- **Cycle**: 2 (Day 1 complete)
- **Dev Stack**: READY (backend restarted from main with all Day 0 fixes)
- **NEEDS_REBUILD**: false
- **Branch**: `bugfix_cycle_demo_legal_day1_2026-04-12`
- **Scenario**: `qa/testplan/demos/legal-za-90day-keycloak.md`
- **Focus**: 90-day legal-ZA demo walkthrough executed end-to-end against the real Keycloak dev stack. Goal is to prove the scripted customer demo runs clean — access request → admin approval → KC registration → plan upgrade → legal-za profile → team invites → 3 client lifecycles (Litigation, Deceased Estate, RAF) → engagement letters, trust accounting, court calendar, adverse parties, activity feed, audit sign-off. Any sharp edges that break the demo narrative become fix-spec targets.
- **Auth Mode**: Keycloak (real OIDC — platform admin `padmin@docteams.local` is pre-seeded via `keycloak-bootstrap.sh`; all other users come through the onboarding flow).

## Environment

| Service | URL | Status |
|---------|-----|--------|
| Frontend (kc mode) | http://localhost:3000 | UP |
| Backend (local+keycloak profile) | http://localhost:8080 | UP |
| Gateway (BFF) | http://localhost:8443 | UP |
| Portal | http://localhost:3002 | UP |
| Keycloak | http://localhost:8180 | UP |
| Mailpit UI | http://localhost:8025 | UP |
| Postgres (docteams) | localhost:5432 | UP |

## Gap Tracker

| GAP_ID | Day / Checkpoint | Severity | Status | Summary | Owner | Retries |
|--------|------------------|----------|--------|---------|-------|---------|
| GAP-D0-01 | Day 0 / CP-0-15 | HIGH | VERIFIED | `KeycloakProvisioningClient.findOrganizationByAlias` broken — KC `/organizations?search=alias&exact=true` matches name not alias, so 409 retry path crashes with `IllegalStateException` whenever a stale KC org exists. Blocks idempotent re-approval. **Fix verified Turn 2**: client-side alias filtering works, approval succeeds with stale org present. Caveats: stale-org reuse path has sub-bugs (see GAP-D0-08, GAP-D0-09). | Dev | 0 |
| GAP-D0-02 | Day 0 / CP-0-06 & CP-0-18 | LOW | SPEC_READY | OTP + invitation emails still branded "DocTeams" (subject, from `noreply@docteams.local`, body); should be Kazi / `noreply@kazi.africa`. | Dev | 0 |
| GAP-D0-03 | Day 0 / CP-0-10 | LOW | SPEC_READY | Keycloak realm theme still says "DocTeams" (page title, heading "Sign in to DocTeams", footer "© 2026 DocTeams"). | Dev | 0 |
| GAP-D0-04 | Day 0 / CP-0-14 | LOW | WONT_FIX | No detail drill-down view on access-request rows; scenario step says "click into the request → verify detail view". Inline row exposes all fields, so content requirement satisfied. Deferred — new UI surface, out of scope for Cycle 1. | Product | 0 |
| GAP-D0-05 | Day 0 / CP-0-04 | LOW | VERIFIED | Industry dropdown says "Legal" instead of "Legal Services" — scenario mismatch (minor copy drift). **Fix verified Turn 2**: dropdown now shows "Legal Services". | Dev | 0 |
| GAP-D0-06 | Day 0 / CP-0-22 | MED | VERIFIED | Post-registration redirect lands on `/?code=...` marketing landing page instead of `/org/{slug}/dashboard`. **Turn 3 VERIFIED on clean path**: KC login as Thandi redirects `/dashboard` → `/org/mathebula-partners/dashboard`. JWT invite token `reduri = http://localhost:3000/dashboard`. PR #1014 fix confirmed working. | Dev | 0 |
| GAP-D0-07 | Day 0 / CP-0-23 | MED | VERIFIED | Sidebar + breadcrumb show org **slug** (`MATHEBULA-PARTNERS`) instead of display name (`Mathebula & Partners`). Breaks demo polish. **Fix verified Turn 2**: sidebar shows "Mathebula & Partners", breadcrumb shows "Mathebula & Partners > Dashboard". | Dev | 0 |
| GAP-D0-08 | Day 0 / V-D0-01 | MED | WONT_FIX | `inviteUser` throws 409 "User already a member" when re-approving with stale KC org that has existing member. Not idempotent. Also, stale-org reuse path does not PATCH the existing org's `redirectUrl` to include `/dashboard`. **WONT_FIX**: subsumed by pre-demo cleanup approach (GAP-D0-09). Stale-org reuse path never fires when demo starts clean. Production idempotency deferred to future epic. | Product | 0 |
| GAP-D0-09 | Day 0 / CP-0-17 | HIGH | VERIFIED | `INDUSTRY_TO_PROFILE` map key mismatch: frontend sends `"Legal Services"` (renamed in GAP-D0-05 PR #1016), but `AccessRequestApprovalService` line 31 maps `"Legal"` → `"legal-za"`. Lookup returns null, so `verticalProfile` is null, `setVerticalProfile()` is never called, and all vertical-specific packs are skipped by `AbstractPackSeeder` profile filter. **FIXED** via PR #1018: map key changed to `"Legal Services"`. **VERIFIED Turn 5**: clean org creation path seeds legal-za profile, ZAR currency, 4 legal modules, 4 matter templates, legal custom fields. | Dev | 1 |
| GAP-D0-10 | Day 0 / CP-0-36 | MED | VERIFIED | Member-role users (e.g., Carol) see generic terminology — sidebar shows "Projects" instead of "Matters", slug instead of org display name, no legal nav items. Console: `Failed to fetch org settings for terminology: Insufficient permissions for this operation`. The `/api/settings` endpoint returns 403 for non-owner/admin users. Sidebar falls back to generic terminology for Members. **Root cause**: `@RequiresCapability("TEAM_OVERSIGHT")` on GET endpoint, members have zero capabilities. **FIXED** via PR #1020: removed `@RequiresCapability("TEAM_OVERSIGHT")` from GET `/api/settings` only. Write endpoints retain capability check. Test updated. **VERIFIED Day 1**: Carol (member) sidebar shows "Matters", "Clients", org display name "Mathebula & Partners". | Dev | 0 |
| GAP-D0-11 | Day 0 / CP-0-46 | LOW | VERIFIED | Custom Fields page tab labels use generic entity names ("Projects", "Customers", "Invoices") instead of vertical terminology ("Matters", "Clients", "Fee Notes"). Page description text uses correct terms but tabs are hardcoded. **Root cause**: static `ENTITY_TYPE_TABS` array in `custom-fields-content.tsx:35-40`. **Fix**: import `useTerminology()`, translate labels via `t()`. **VERIFIED Day 1**: tabs now show "Matters", "Action Items", "Clients", "Fee Notes". Page description correct. | Dev | 0 |

| GAP-D1-01 | Day 1 / CP-1-05, CP-1-10 | LOW | OPEN | Client detail page tabs use generic terminology: "Projects" tab (should be "Matters"), "Invoices" tab (should be "Fee Notes"). Also Clients list empty state text says "No customers yet" and "Customers represent the organisations you work with" — should use "clients". Multiple locations in customer detail and list components. | Dev | 0 |
| GAP-D1-02 | Day 1 / CP-1-10 | LOW | OPEN | Client detail header subtitle says "0 projects" instead of "0 matters". Location: customer detail header component, project count text. | Dev | 0 |

## Legend

- **Status**: OPEN → SPEC_READY → FIXED → VERIFIED | REOPENED | STUCK | WONT_FIX
- **Severity**: HIGH (blocker) / MED (cascading bug) / LOW (cosmetic)
- **Owner**: QA / Product / Dev / Infra

## Log

- 2026-04-12 — Cycle 1 initialized. Prior cycle (KC 2026-04-12, legal-onboarding scenario) closed as ALL_DAYS_COMPLETE and archived to `qa_cycle/_archive_2026-04-12_legal-onboarding-kc/`. Branch `bugfix_cycle_demo_legal_2026-04-12` created from main. Scenario: `qa/testplan/demos/legal-za-90day-keycloak.md`.
- 2026-04-12 — Infra Turn 1: Verified KC dev stack health — all 4 Docker containers (b2b-postgres, b2b-keycloak, b2b-localstack, b2b-mailpit) reporting healthy (Up 17 hours). Keycloak `docteams` realm responds 200. svc.sh reports backend/gateway/frontend/portal all running and healthy (PIDs 55326/55489/90632/90679). Endpoint spot-checks: backend :8080 /actuator/health UP, gateway :8443 /actuator/health UP, frontend :3000 HTTP 200, portal :3002 HTTP 200, Mailpit API returning messages. No services required starting. Dev Stack READY for QA walkthrough.
- 2026-04-12 — QA Turn 1 (Day 0): 21 pass, 1 fail (CP 0.15 on first attempt), 3 partial. Pre-run cleanup found residual state from prior cycle (3 KC users, 1 KC org, 1 org row, 1 access_request, 1 tenant schema) — cleaned via Admin API + psql. New gaps: GAP-D0-01 (HIGH), GAP-D0-02 (LOW), GAP-D0-03 (LOW), GAP-D0-04 (LOW), GAP-D0-05 (LOW), GAP-D0-06 (MED), GAP-D0-07 (MED). Stopping point: CP 0.25 (dashboard wow-moment screenshot captured). Day 0 Phases D–K not executed this turn — blocked on GAP-D0-01 triage. Full details in `qa_cycle/checkpoint-results/day-00.md`.
- 2026-04-12 — Product Turn 1: triaged 7 Day 0 gaps. Verified every root cause by reading referenced source files. SPEC_READY: GAP-D0-01 (HIGH — alias lookup uses wrong KC search semantic, fix: list-and-match client-side), GAP-D0-02 (branding strings in AccessRequestService + application.yml + realm-export.json), GAP-D0-03 (Keycloak login theme TSX + index.html + realm displayName), GAP-D0-05 (one-word label change in lib/access-request-data.ts), GAP-D0-06 (KC org `redirectUrl` set to bare `frontendBaseUrl`; change to `frontendBaseUrl + "/dashboard"` so frontend middleware + gateway OAuth2 handle the code exchange), GAP-D0-07 (sidebar + breadcrumb need org display name piped through `/bff/me` → `AuthContext` → `DesktopSidebar`). WONT_FIX: GAP-D0-04 — introducing a new detail drawer/page is a scope-expanding UI add; inline row already exposes all fields required by the scenario, so content requirement is met. Priority order for Dev: GAP-D0-01 → GAP-D0-06 → GAP-D0-07 → GAP-D0-05 → GAP-D0-02 → GAP-D0-03. Total specs written: 6. Fix specs in `qa_cycle/fix-specs/`.
- 2026-04-12 — Dev Turn 1: GAP-D0-01 FIXED via PR #1013 (squash commit b62da54c). `KeycloakProvisioningClient.findOrganizationByAlias` now fetches orgs and filters on the `alias` field client-side, with a list-all fallback for the anomaly path. Added `KeycloakProvisioningClientTest` (WireMock-based, 4/4 green) covering alias match among name-similar orgs, list-all fallback, no-match error, and happy path. Backend changed → NEEDS_REBUILD=true.
- 2026-04-12 — Infra Turn 2: backend restarted after GAP-D0-01 merge (PR #1013). Old PID 55326 → new PID 83247. Health UP (`/actuator/health` → `{"status":"UP"}`). svc.sh status: backend/gateway/frontend/portal all running+healthy. KC orgs in `docteams` realm: `['mathebula-partners', 'moyo-dlamini-attorneys', 'qa-verify-corp', 'thornton-associates']` — note `mathebula-partners` is still present (QA Turn 1 cleanup did not remove it, or it was recreated); this gives QA a live stale-org scenario to re-verify GAP-D0-01 against. NEEDS_REBUILD cleared (false).
- 2026-04-12 — Dev Turn 2: GAP-D0-06 FIXED via PR #1014 (squash commit 9639a4f6). `KeycloakProvisioningClient.createOrganization` now sets KC org `redirectUrl` to `frontendBaseUrl + "/dashboard"` instead of bare `frontendBaseUrl`. New `organizationRedirectUrl` field derived in constructor with trailing-slash guard. 16 tests pass (AccessRequestApprovalServiceTest: 9, PlatformAdminControllerTest: 7). Backend changed → NEEDS_REBUILD=true.
- 2026-04-12 — Dev Turn 3: GAP-D0-07 FIXED via PR #1015 (squash commit 8d1cc50d). Added `orgName` field to `SettingsResponse` DTO, populated from `public.organizations` table via `OrganizationRepository.findByExternalOrgId(RequestScopes.ORG_ID)`. Frontend sidebar (desktop + mobile) and breadcrumbs now render `orgName` with fallback to slug. 7 files changed across backend + frontend. Build green: backend compile clean, frontend lint 0 errors, build green, 1932 tests pass. NEEDS_REBUILD=true (backend changed).
- 2026-04-12 — Dev Turn 4: GAP-D0-05 FIXED via PR #1016 (squash commit cab293bd). Renamed industry label "Legal" to "Legal Services" in `frontend/lib/access-request-data.ts` + updated test fixture. Frontend-only, HMR picks it up.
- 2026-04-12 — Infra Turn 3: backend restarted after D0-06+D0-07 fixes (PRs #1014, #1015). Old PID 83247 → new PID 95751. Health UP (`/actuator/health` → `{"status":"UP"}`). svc.sh status: backend/gateway/frontend/portal all running+healthy. NEEDS_REBUILD cleared.
- 2026-04-12 — QA Turn 2 (Day 0 Verification): 4 verification checks executed. GAP-D0-05 VERIFIED (industry label "Legal Services"). GAP-D0-07 VERIFIED (sidebar/breadcrumb show org display name). GAP-D0-01 VERIFIED (core alias-lookup fix works — approval succeeds with stale KC org present). GAP-D0-06 INCONCLUSIVE (stale-org reuse path doesn't update redirectUrl; fix cannot be tested without clean KC env). 2 new gaps: GAP-D0-08 (MED — inviteUser 409 not idempotent), GAP-D0-09 (HIGH — stale-org reuse skips vertical profile seeding, empty org_settings, 500 errors). Day 0 Phases D–K still NOT executed — blocked on GAP-D0-09. Recommendation: pre-run cleanup must delete stale KC orgs, not just users/DB rows. For clean demo: delete all KC orgs from prior cycles before Day 0.
- 2026-04-12 — Product Turn 2: GAP-D0-08 WONT_FIX (stale-org idempotency deferred — subsumed by cleanup approach). GAP-D0-09 SPEC_READY — pre-demo cleanup script (`compose/scripts/demo-cleanup.sh`). Strategy: clean env for demo runs, not production idempotency. GAP-D0-06 stays INCONCLUSIVE (will be verifiable after cleanup script enables clean-path testing).
- 2026-04-12 — Dev Turn 5: GAP-D0-09 FIXED via PR #1017 (squash commit 82a63ac3). `compose/scripts/demo-cleanup.sh` created and tested. Cleaned: 4 KC orgs deleted, 1 non-padmin user deleted, 4 tables truncated (subscriptions, org_schema_mapping, organizations, access_requests), 4 tenant schemas dropped, Mailpit inbox cleared. Idempotent re-run verified. No NEEDS_REBUILD — new script only, no application code changes.
- 2026-04-12 — QA Turn 3 (Day 0 Clean Re-Run): Full Day 0 re-run from pristine environment (0 KC orgs, 0 users except padmin, 0 DB rows). 25 checkpoints executed: 20 PASS, 3 FAIL, 2 PARTIAL. GAP-D0-06 VERIFIED (redirect to /dashboard works on clean path). GAP-D0-09 REOPENED (HIGH) — vertical profile seeder does NOT run on clean creation path either. org_settings empty: no vertical_profile, USD not ZAR, no terminology, no enabled modules, no legal packs. Only common/generic packs seeded. Root cause is NOT stale-org reuse — it's that the provisioning flow never invokes the vertical profile seeder. Cleanup script (PR #1017) masks but does not fix. Day 0 Phases D–K still blocked. Screenshot: `qa_cycle/screenshots/cycle-1/turn3-d0-dashboard-no-vertical-profile.png`.
- 2026-04-12 — Product Turn 3: GAP-D0-09 re-specced (v2). Root cause: `INDUSTRY_TO_PROFILE` map key mismatch. GAP-D0-05 (PR #1016) renamed frontend industry label from `"Legal"` to `"Legal Services"`, but `AccessRequestApprovalService.java` line 31 still maps `"Legal"` → `"legal-za"`. Lookup returns null, vertical profile seeder is never invoked. The wiring chain (approval → provisioning → setVerticalProfile → VerticalProfileRegistry → AbstractPackSeeder filtering) is correct — only the map key is wrong. Fix: one word change. Original cleanup-script approach (v1) was wrong fix. REOPENED → SPEC_READY.
- 2026-04-12 — Dev Turn 6: GAP-D0-09 FIXED (v2) via PR #1018. One-word fix: `"Legal"` → `"Legal Services"` in `INDUSTRY_TO_PROFILE` map key (`AccessRequestApprovalService.java`). Test fixtures updated in `AccessRequestApprovalServiceTest` and `PlatformAdminControllerTest`. 36/36 AccessRequest tests green. Root cause was cascading from GAP-D0-05 (PR #1016). NEEDS_REBUILD=true (backend changed).
- 2026-04-12 — Infra Turn 4: backend restarted after D0-09v2 fix (PR #1018). demo-cleanup.sh run. Clean state for QA Turn 4. NEEDS_REBUILD cleared.
- 2026-04-12 — QA Turn 4 (Day 0 Phases A–C): Full clean run from pristine state. Phases A–C all PASS: onboarding flow clean, vertical profile seeded correctly (legal-za, ZAR, en-ZA-legal, 4 modules), dashboard shows legal terminology. GAP-D0-09 VERIFIED. Turn ran out of context before committing or continuing to Phase D. 2 screenshots captured.
- 2026-04-12 — QA Turn 5 (Day 0 Phases D–K): Completed remaining Day 0 phases. Phase D (team invites): Bob (Admin) and Carol (Member) invited via KC, both registered and redirected to dashboard. Phase E: ZAR currency confirmed, brand colour #1B3A4B set and persisted. Phase G: 8 legal custom fields from SA Legal pack confirmed. Phase H: 4 matter templates from legal-za pack confirmed. Phase I: All 4 legal modules active in sidebar (Court Calendar, Conflict Check, Trust Accounting, Tariffs), no cross-vertical leaks. Phase K: Billing page shows flat subscription, no tier UI. 2 new gaps: GAP-D0-10 (MED — member role sees generic terminology due to 403 on org_settings), GAP-D0-11 (LOW — custom fields tab labels not localized). Day 0 COMPLETE.
- 2026-04-12 — Product Turn 4: Specced final 2 open gaps. GAP-D0-10 SPEC_READY — root cause is `@RequiresCapability("TEAM_OVERSIGHT")` on GET `/api/settings` (`OrgSettingsController.java:39`); members have zero capabilities per `V67` migration. Fix: remove annotation from GET method only (1 line, backend-only). GAP-D0-11 SPEC_READY — root cause is hardcoded `ENTITY_TYPE_TABS` array in `custom-fields-content.tsx:35-40`; fix: import `useTerminology()` hook and translate labels via `t()` (frontend-only, ~10 lines). Both XS effort. All 11 gaps now have final disposition: 5 VERIFIED, 4 SPEC_READY, 2 WONT_FIX. 0 OPEN gaps remain.
- 2026-04-12 — Dev Turn 7: GAP-D0-10 FIXED via PR #1020 (squash-merged). Removed `@RequiresCapability("TEAM_OVERSIGHT")` from GET `/api/settings` only. All write endpoints (PUT, PATCH, POST, DELETE) retain capability check. Updated `getSettings_memberGetsForbidden` test → `getSettings_memberCanReadSettings` (asserts 200 + `defaultCurrency` present). 56/56 settings tests green. NEEDS_REBUILD=true (backend changed).
- 2026-04-12 — QA Turn 6 (Day 1): All 11 Day 1 checkpoints PASS. Bob logged in, ran conflict check for "Sipho Dlamini" (CLEAR), created client with promoted fields inline, verified PROSPECT status + detail page. D0-10 VERIFIED (Carol member role sees legal terminology + org display name). D0-11 VERIFIED (Custom Fields tabs show "Matters/Clients/Fee Notes"). 2 new LOW gaps: GAP-D1-01 (client detail tabs + list empty state use generic "Projects"/"Customers" terms), GAP-D1-02 (header says "0 projects" not "0 matters"). Screenshot: `qa_cycle/screenshots/cycle-2/day01-cp1.4-conflict-check-clear.png`. Day 1 COMPLETE.
