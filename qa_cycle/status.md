# QA Cycle Status — Legal ZA Full Lifecycle (Firm + Portal Interleaved, Keycloak) — 2026-04-21

## Current State

- **ALL_DAYS_COMPLETE**: false
- **QA Position**: Day 4 — 4.8 (BLOCKED pending Dev fix for GAP-L-42; GAP-P-01 FIXED via PR #1100 + GAP-P-02 FIXED via PR #1101 both landed 2026-04-21 SAST; L-42 spec at `qa_cycle/fix-specs/GAP-L-42.md` is remaining backend blocker for Day 4 Phase A magic-link delivery).
- **Cycle**: 1
- **Dev Stack**: READY — all 4 services UP (8080/8443/3000/3002), KC `docteams` realm reachable, Bob sign-in verified.
- **NEEDS_REBUILD**: true (GAP-P-01 PR #1100 + GAP-P-02 PR #1101 both merged 2026-04-21 SAST — backend restart required so `PackReconciliationRunner` re-syncs `org_settings.enabled_modules` for `tenant_5039f2d497cf` to include `information_requests`; portal HMR normally picks up TSX edits but a portal restart is safer to guarantee P-02's new `/requests` + `/requests/[id]` routes are picked up)
- **Branch**: `bugfix_cycle_legal_full_2026-04-21`
- **Scenario**: `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`
- **Focus**: First unified firm + client-POV 90-day legal lifecycle on a single `mathebula-partners` / `legal-za` tenant. Interleaves firm-side Keycloak flows with portal-side magic-link flows across 23 scripted days (0, 1, 2, 3, 4, 5, 7, 8, 10, 11, 14, 15, 21, 28, 30, 45, 46, 60, 61, 75, 85, 88, 90). Includes a dedicated BLOCKER-severity **isolation check** on Day 15 (Sipho must not see Moroka Family Trust data) and a repeat isolation probe on Day 90.
- **Auth Mode**: Keycloak (real OIDC) for firm; magic-link + portal JWT for portal (`:3002`)

## Environment

| Service | URL | Status |
|---------|-----|--------|
| Frontend (kc mode) | http://localhost:3000 | UP — HTTP 200 (restarted post GAP-L-23 merge, PID 33007, ready in 3.1s, startup clean) |
| Backend (local+keycloak profile) | http://localhost:8080 | UP — `{"status":"UP"}` (restarted 2026-04-21 22:03 SAST post L-33/L-34 merges, PID 62516, ready in 27s) |
| Gateway (BFF) | http://localhost:8443 | UP — `{"status":"UP"}` (already running external, PID 71426) |
| Portal | http://localhost:3002 | UP — HTTP 307 root redirect expected (started fresh this cycle) |
| Keycloak | http://localhost:8180 | UP — `/realms/docteams` 200; bootstrap re-run idempotent, padmin user confirmed |
| Mailpit UI | http://localhost:8025 | UP — inbox purged (was 18 stale msgs) for Day 0 clean state |
| Postgres (docteams) | localhost:5432 | UP — `b2b-postgres` container healthy, DB `docteams` reachable |
| LocalStack (S3) | http://localhost:4566 | UP — container `b2b-localstack` healthy (4 days uptime) |

## Carry-Forward Watch List (from prior legal-za archives)

These are gaps/observations logged during `_archive_2026-04-21_legal-full-lifecycle` and earlier legal-za runs. If they reproduce, log fresh GAP IDs referencing the archive. Portal-specific watch items pulled forward because this is the first script to interleave firm + portal POVs on a single tenant.

### Firm-side (from 90-day legal archive)
- **Keycloak org-invite bounce-page mechanics** (prior GAP-L-01) — invite URL must route through `/accept-invite` to avoid same-profile cookie collision + single-use token consumption. Allow-list covers both `/login-actions/action-token` and `/protocol/openid-connect/registrations`. Regression risk if allow-list is tightened or KC endpoint shape changes.
- **Keycloak end-session confirmation page** (prior GAP-L-04, OPEN) — no `id_token_hint` causes one-click logout confirmation even without active session. Adds nuisance click to every invite-email recipient. Still LOW, but surfaces again on every fresh registration in Day 0.
- **Disbursements "Add / Log Expense" button missing** (prior GAP-L-06, OPEN, ADR-247) — Day 21 Phase B requires this button; scenario will hit it if not yet shipped.
- **Matter closure CLOSED state + pre-closure gates** (prior GAP-L-07, OPEN, ADR-248) — Day 60 Phase B requires all gates GREEN (unbilled time, trust balance, fee notes, tasks). Close-Matter flow must expose each gate.
- **Retention clock field + stamp-once semantics** (prior GAP-L-08, VERIFIED via V99 migration) — Day 60 Phase B Checkpoint 10.11 asserts `end_date = today + 5 years` per ADR-249. Partial-verified previously (code + DB only); Day 60/85 in this script will validate the UI path.
- **Statement of Account template** (prior GAP-L-09, OPEN, ADR-250) — Day 60/61 require the SoA PDF to render with Mathebula letterhead, banking details, trust ledger reconciliation, VAT. Fee-note template regression risk (prior runs used generic "Invoice" template).
- **Acceptance-eligible manifest flag on doc templates** (prior GAP-L-10, OPEN, ADR-251) — Day 7/8 proposal acceptance depends on this flag or its fallback (ENGAGEMENT_LETTER category).
- **Audit log UI** (prior GAP-L-11, OPEN) — Day 85 Checkpoint 85.4 requires filterable audit by matter + actor (including portal contacts). `/audit` / `/settings/audit` all returned 404 previously.
- **ZAR `/profitability` hydration via `@formatjs/intl-numberformat`** (prior GAP-L-12, VERIFIED) — polyfill depends on `pnpm install` being run post-merge. Infra-state regression risk after dependency updates.
- **Activity feed `"unknown"` task title** (prior GAP-L-05, VERIFIED via `putIfAbsent("title", ...)`) — regression risk on any `TaskService` refactor.
- **Untranslated Keycloak i18n keys** (prior GAP-L-02/L-03, VERIFIED) — `Info.tsx` / `Register.tsx` now call `advancedMsgStr(messageHeader)`. Regression risk on KC theme rebuilds.
- **Tariff-rate naming** (prior OBS-L-05) — VAT entry was seeded as "Standard"; verify in Day 1 settings walk that tariff names are explicit.
- **Post-registration session handoff** (prior OBS-L-06) — KC issued session_state for `account` client; explicit re-login required to land on `gateway-bff` session. May surface on Day 0 Phase C and Phase D.
- **Admin `APPROVE_TRUST_PAYMENT` capability** (prior OBS-L-13/L-26) — Admin role lacks this; only Owner can approve. Impacts Day 10 trust-deposit dual approval if flow uses Bob-as-Admin.
- **Matter closure gate error-text terminology leak** (prior Day 75 note) — "Project has 9 open task(s)" used `Project` terminology under legal-za profile. Check Day 60 Phase B gate-report copy.

### Portal-side (first QA run against this tenant — no prior legal-za portal archive; pulling generalised watch items)
- **Portal service staleness on `:3002`** (prior Day 68 BLOCKED — portal was stale, out of QA scope to restart). Infra Agent must confirm portal is UP in Session 0.
- **Portal magic-link email subject consistency** — Day 4/8/11/30/46/61/75 all depend on Mailpit delivering emails with stable subject keywords ("sign in", "action required", "proposal", "trust deposit", "fee note", "Statement of Account", "weekly update"). Regression risk if copy changes.
- **Portal org-slug override** — `PORTAL_ORG_SLUG=mathebula-partners` (not default `e2e-test-org`). Must be set if any portal E2E helper is used.
- **Portal currency rendering** — Day 11/30/46/61 all assert ZAR formatting on `/trust`, `/invoices`; uses same Node small-icu path as firm-side `/profitability`. If polyfill is not imported on the portal bundle, expect the same hydration issue.
- **Description sanitisation on portal `/trust`** — Day 11.5 asserts `[internal]` tags stripped, client-safe copy ≤ 140 chars. Regression risk.
- **Isolation (BLOCKER-severity)** — Day 15 Phases A–D + Day 90 final probe. Any portal list, URL, or API response that returns Moroka data to Sipho is an immediate BLOCKER. Verify at frontend (404/403/redirect), backend (`/portal/api/*` → 403/404), and digest-email levels.
- **Activity-trail / digest cross-entity leak** — Day 15 Phase D + Day 75 + Day 88 assert digest and portal activity view contain zero Moroka references.
- **Firm-side audit event for portal doc access** — Day 61 Checkpoint 61.9 asserts the audit log records Sipho's SoA download. Couples to GAP-L-11 (Audit log UI missing) — audit event must exist even if UI does not.

## Gap Tracker

| GAP_ID | Day / Checkpoint | Severity | Status | Summary | Owner | Retries |
|--------|------------------|----------|--------|---------|-------|---------|
| GAP-INFRA-01 | Session 0 / 0.C | HIGH | VERIFIED | Pre-existing `mathebula-partners` org (KC id `d49948bb-8dca-4ee3-8a87-4ff97e63ae2b`, registry id `3730e32a-5310-43e3-b001-495def2b524d`) + tenant schema `tenant_5039f2d497cf` remained from prior archived lifecycle run. Cleaned up 2026-04-21 19:22 SAST: deleted 3 KC users (Bob/Carol/Thandi), KC org, `subscriptions` row `ec8b9574…`, `access_requests` row `4b9c3030…`, `org_schema_mapping` row `bd05eca8…`, `organizations` row `3730e32a…`, and `DROP SCHEMA tenant_5039f2d497cf CASCADE` (103 objects). Post-checks: no mathebula org in KC, 0 `@mathebula-test.local` users, 0 registry rows, schema gone, other 3 tenant schemas intact, all services UP. | Infra | 0 |
| GAP-L-20 | Day 0 / 0.9 + 0.27 | LOW | OPEN | Two related copy/flow regressions: (a) `/dashboard` does not auto-redirect unauthenticated users to Keycloak — shows a "Waiting for Access" fallback card (frontend/app/(app)/dashboard/page.tsx catches auth failure and renders the card instead of redirecting). (b) Team page shows "1 of 10 members" seat counter despite user rule "No plan-tier subscriptions, unlimited members". Counter is display-only (invites still send), not a hard gate. **Deferred** — triage after QA unblocks and works through Day 1–5; not on Day 1 critical path. | frontend | 0 |
| GAP-L-21 | Day 0 / 0.13 | LOW | OPEN | Pending access-request row in `/platform-admin/access-requests` has no drill-down detail view. Clicking the row or any cell does not open a detail panel/page. All six submitted fields are visible inline (Org/Email/Name/Country/Industry/Submitted), which is sufficient for approval, but scenario expects a separate detail view before Approve. **Deferred** — cosmetic, Day 0 already PARTIAL-passed; spec later batch. | frontend | 0 |
| GAP-L-22 | Day 0 / 0.21 + 0.32 | MED | OPEN | Post-registration session handoff is broken. After KC registration completes and BFF code exchange runs, the BFF gateway cookie does not consistently reflect the newly registered user. Repro'd three times: (1) Thandi's registration redirected to padmin's `/platform-admin/access-requests` because padmin's BFF session was still active; (2) Bob's registration landed on dashboard but `/api/auth/me` fails; (3) after Carol's registration, dashboard sidebar RSC renders Thandi's user card despite Carol being the registered user. Explicit KC logout + fresh OIDC login is the reliable workaround. Escalated from prior OBS-L-06 because it surfaced for all three registrants, not just intermittently. **Deferred** — MED but workaround held cleanly on Day 1 entry; re-triage after GAP-L-23 fix lands so we can prioritise proper session-handoff fix without blocking the BLOCKER path. | both | 0 |
| GAP-L-23 | Day 1 / 1.1 | **HIGH / BLOCKER** | **VERIFIED** | `/settings/general` server-actions loader crashed with `ReferenceError: PortalDigestCadence is not defined` at module evaluation — every mutation on the page (Save Settings, Upload Logo, Vertical Profile, Portal Digest Cadence, Portal Retainer Member Display, Org Document upload) returned 500. Root cause: `frontend/app/(app)/org/[slug]/settings/general/portal-actions.ts` line 10 `export type { PortalDigestCadence, PortalRetainerMemberDisplay };` — Turbopack compiles this into a dangling value export in the server-actions bundle. Fixed via PR #1097 (merged `2a94de5f`): deleted the type re-export from `portal-actions.ts`; `components/settings/portal-settings-section.tsx` now imports those types directly from `@/lib/types/settings`. **QA VERIFIED 2026-04-21 20:50 SAST**: Thandi logged in clean → `/settings/general` → Save Settings, Upload Logo (mathebula-logo.png), Portal Digest Cadence (Weekly → Bi-weekly → Weekly), Vertical Profile combobox (4 options load, no "Failed to load profiles" card) all returned **200** over 12+ POSTs. DB row in `tenant_5039f2d497cf.org_settings` populated with `brand_color='#1B3358'`, `logo_s3_key='org/tenant_5039f2d497cf/branding/logo.png'`, `portal_digest_cadence='WEEKLY'`. Zero 500s observed; zero `ReferenceError` in `.svc/logs/frontend.log` during the turn. | frontend | 0 |
| GAP-L-24 | Day 1 / 1.1 | MED | **VERIFIED** | Cascade resolution of GAP-L-23. `fetchProfiles` was exported from the same crashing server-actions loader; with L-23 fixed the Vertical Profile card's loader runs normally. **QA VERIFIED 2026-04-21 20:50 SAST**: combobox dropdown now lists all 4 industry profiles ("South African Accounting Firm", "Consulting & Professional Services", "South African Agency & Consulting Firm", "Legal (South Africa)" [selected]) instead of the "Failed to load profiles" error card. Apply Profile button correctly disabled when the current profile matches the selection. | frontend | 0 |
| GAP-L-25 | Day 1 / 1.5 | LOW | OPEN | Trust Account create dialog at `/trust-accounting` Account Type dropdown offers only `General` and `Investment`. Scenario 1.5 expects `SECTION_86` (Legal Practice Act §86) classification for the legal-za vertical. Legal-za tenant cannot tag statutory trust classification. Not a cascade of GAP-L-23 — this is a pre-existing legal-za vertical coverage gap. Root-cause scope found: fix touches `backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/trustaccounting/TrustAccountType.java` (enum add), `backend/src/main/resources/db/migration/tenant/V*__*.sql` (new Flyway migration to relax `chk_trust_account_type` check constraint + evaluate primary-account partial unique index that currently only covers `GENERAL`), `frontend/lib/types/trust.ts`, `frontend/lib/schemas/trust.ts`, `frontend/app/(app)/org/[slug]/trust-accounting/actions.ts`, and `frontend/components/trust/CreateTrustAccountDialog.tsx`. **Deferred** — scope exceeds the <30-min bar (Flyway migration + 5 file edits + tests + primary-account index decision). Spec in a later batch after QA resumes; legal-za tenants can use `GENERAL` as the interim classification for Day 1.5. 2026-04-21 20:49 SAST — QA confirmed workaround: created "Mathebula Trust — Main" with `account_type=GENERAL`, `is_primary=t`. Day 1.5 logged as PARTIAL. | backend | 0 |
| GAP-L-26 | Day 1 / 1.2 | LOW | OPEN | Branding values persist server-side (verified via DB after GAP-L-23 fix: `brand_color='#1B3358'`, `logo_s3_key='org/tenant_5039f2d497cf/branding/logo.png'`) but the frontend app shell does not consume them. Computed sidebar `background-color` remains slate-950 default; `--brand-color` CSS custom property on `document.documentElement` is empty; no logo `<img>` rendered in sidebar header / navbar. Day-1 scenario checkpoint 1.2 ("brand colour applied to sidebar accent + logo renders at top of sidebar") therefore FAILs. Fix owner: frontend — likely `frontend/components/layout/AppSidebar.tsx` (or the org-layout root) needs to read `orgSettings.brandColor` / `orgSettings.logoUrl` and inject as CSS var + logo image src. Severity LOW because persistence works; UX polish missing. **Deferred** — not on Day 2+ critical path; batch with other LOW cosmetic gaps. | frontend | 0 |
| GAP-L-27 | Day 1 / 1.3 | LOW | OPEN | Tax rate seeded by `legal-za` pack (or common-tax seeder) as bare `"Standard"` at 15% rather than explicit `"VAT — Standard"` expected for the SA legal vertical. Also seeded: `Zero-rated` 0% and `Exempt` 0%. Tenant `org_settings.tax_label` is NULL — legal-za tenant init should probably set `tax_label='VAT'` and rename `Standard` -> `VAT — Standard`. Carry-forward of OBS-L-05 from prior legal-za archives; now formalised as a tracked GAP. Owner: backend (likely `ZaLegalTaxSeeder.java` or the common tax seeder with a legal-za override). Severity LOW — cosmetic copy only; does not block downstream billing / invoice / fee-note flows. **Deferred** — cosmetic. | backend | 0 |
| GAP-L-28 | Day 2 / 2.5 + 2.6 | MED | OPEN | Conflict-check engine at `/conflict-check` does NOT exclude the just-created subject from its match set, so the first conflict check on a newly-created client (scenario 2.5/2.6 happy-path) returns `CONFLICT_FOUND` with two 100% self-matches (ID + Name Match, both tagged `EXISTING CLIENT`) against the client's own record. Control probe with an unrelated identity (`Nontando Zulu`/`9001019999088`) correctly returned `NO_CONFLICT` — engine is real, registry query works, persisted rows in `tenant_5039f2d497cf.conflict_checks` confirm (Sipho=CONFLICT_FOUND, Nontando=NO_CONFLICT). Root cause is either (a) backend match logic should filter `subject.customer_id == candidate.customer_id` when `customer_id` provided on the request, OR (b) UX should hoist the conflict check into the Create Client wizard pre-insert (runs against adverse-parties + existing clients, then inserts the client only if CLEAR/waived). Severity MED because scenario's Day 2 demo moment is broken and GAP-P-XX (portal-side Day-15 isolation) depends on clean adverse-party matching later. Impact on Day 3 onward: negligible (matter creation, FICA request, trust deposit, etc. don't depend on conflict-check state). Owner: backend + frontend (product decision on approach). **Deferred** — scenario can proceed past Day 2; Resolve Conflict button is available as a workaround if Day 90 regression re-surfaces. | both | 0 |
| GAP-L-29 | Day 2 / 2.5 | LOW | OPEN | Conflict-check form at `/conflict-check` — the "Customer (optional)" and "Matter (optional)" select dropdowns show only `-- None --` even when the tenant has live clients/matters (verified with Sipho in the registry). Server-side render is not fetching the tenant's client/matter list to populate the options. Input path still works via free-text name+ID, so engine is reachable. Owner: frontend — the form server component needs to add `customers = await api.get('/api/customers')` + `projects = await api.get('/api/projects')` data fetches and pass them to the `<select>`. Severity LOW — cosmetic, not blocking. **Deferred** — small, batch with other LOW forms fixes. | frontend | 0 |
| GAP-L-30 | Day 2 / 2.8 | LOW | OPEN | KYC Verification integration is unconfigured out of the box under the legal-za vertical. `/settings/integrations` renders a "KYC Verification / Not Configured / Automated identity verification for FICA compliance" tile with a "Configure" button, and server-side default is `kycStatus = { configured: false, provider: null }` (confirmed in `frontend/app/(app)/org/[slug]/customers/[id]/page.tsx:244`). No provider stub nor mock adapter seeded for demo runs, so scenario Day 2 2.8/2.9/2.10 fall through to the "skip and log" branch. Owner: product — decide whether to ship a stub adapter for demo tenants (simplest), wire a real provider (heavier — Lexis Refinitiv / Truth SA / ComplyAdvantage are common SA-legal choices), or formally rescope the scenario to document KYC as skip-by-design until Phase N. **Deferred** — scenario permits skip. | product | 0 |
| GAP-L-31 | Day 2 / 2.1 | LOW | OPEN | `/customers` empty-state copy still reads "Customers represent the organisations you work with. Add your first customer to start managing relationships." under legal-za vertical. Page H1 and subnav say "Clients" / "New Client" (correct for legal), but the empty-state body copy leaks the generic "customer(s)" terminology. Cosmetic copy leak. Owner: frontend — likely in `frontend/app/(app)/org/[slug]/customers/page.tsx` empty-state branch; needs vertical-aware copy lookup (`copy.customer.pluralWordLower` vs `copy.client.pluralWordLower`). **Deferred** — batch with other terminology leaks identified during Day 0–2. | frontend | 0 |
| GAP-L-32 | Day 2 / 2.4 | LOW | OPEN | After clicking "Create Client" → 7× `POST` all return 200, dialog closes cleanly, but UX leaves the user on `/customers` (the list). Scenario 2.4 expects "redirected to client detail". Minor UX polish — user has to click the new list row to reach detail. Owner: frontend — the Create Client server action should return the created-customer ID and the dialog's `onSuccess` handler should `router.push('/customers/'+id)`. **Deferred** — workaround = click row. | frontend | 0 |
| GAP-L-33 | Day 3 / 3.8 | **HIGH / BLOCKER** | **VERIFIED** | FICA Onboarding Pack request template missing for legal-za vertical. `SELECT name FROM tenant_5039f2d497cf.request_templates` returns 5 platform templates (Annual Audit Document Pack, Company Registration, Conveyancing Intake (SA), Monthly Bookkeeping, Tax Return Supporting Docs) — none is FICA. `ls backend/src/main/resources/request-packs/` → 7 JSONs (annual-audit, company-registration, consulting-za-creative-brief, conveyancing-intake-za, monthly-bookkeeping, tax-return, year-end-info-request-za) — no `fica-onboarding-pack.json`. Scenario 3.8 hardcoded "FICA Onboarding Pack" template with 3 items (ID copy, Proof of residence ≤ 3 months, Bank statement ≤ 3 months); scenario 3.10 assumes pre-filled items; scenario 3.12 Send → status=Sent. **BLOCKER** for Day 3.8→3.14 and cascades into Day 4 (Sipho portal login via FICA magic link), Day 5 (upload), Day 8 (second request) etc. Owner: backend/product — author `backend/src/main/resources/request-packs/fica-onboarding-pack.json` + register in `pack.json` for legal-za + FICA-KYC compliance pack. | backend | 0 |
| GAP-L-34 | Day 3 / 3.8 + 3.9 + 3.13 | **HIGH / BLOCKER** | **VERIFIED** | No firm-side path to create a portal contact. `CustomerController.java` exposes `@GetMapping("/{id}/portal-contacts")` only (line 343) — no POST; `PortalContactService.createContact` is only invoked by `DevPortalController.generate-link` (line 115, dev tooling, not firm UX). Create Information Request dialog detects 0 contacts → shows "No portal contacts found for this customer. Please add a portal contact first." → Save as Draft + Send Now buttons both disabled. DB confirms: `SELECT count(*) FROM tenant_5039f2d497cf.portal_contacts` → 0. Customer email `sipho.portal@example.com` is on the `customers` row but not auto-propagated to `portal_contacts`. Scenario 3.9 "(portal contact auto-populated from client record)" implies one of: (a) auto-create on customer-create from `customers.email`, (b) auto-create on first info-request dispatch, (c) firm-side Add Portal Contact action. None exist. **BLOCKER** — every portal POV day (4/8/11/30/46/61/75 magic-link, 15 isolation probe) depends on at least one portal contact. Owner: backend (POST `/api/customers/{id}/portal-contacts` handler calling PortalContactService.createContact) + frontend ("Add Portal Contact" affordance in Create Information Request dialog Portal Contact empty state, OR simply call the new POST with `customers.email` as default). | both | 0 |
| GAP-L-35 | Day 3 / 3.3 | MED | OPEN (deferred — off Day 3/4 critical path) | Matter custom-field save blocked by PROSPECT lifecycle gate AFTER matter already successfully created. Sequence: Create Matter from "New from Template" → matter row inserted (status=ACTIVE, customer_id=Sipho) → detail page renders SA Legal — Matter Details field group → fill Court + Opposing Party → Save Custom Fields → 3 × POST return 200 at network level, but UI renders error string "Cannot create project for customer in PROSPECT lifecycle status" next to Save button. Scenario 3.3 expects promoted fields editable at creation. Gate appears to be on custom-field PATCH path (backend `MatterCustomFieldService` or `ProjectService#updateCustomFields`), not on matter-create (which succeeds). Error string also leaks "project" / "customer" terminology under legal-za profile. Fix either: (a) relax PROSPECT gate on custom-field updates (it's already OK on create), or (b) make Save Custom Fields transition the customer to ONBOARDING automatically, or (c) scope the gate to CLOSE-matter only. Owner: backend. | backend | 0 |
| GAP-L-36 | Day 3 / 3.2 + 3.3 | MED | OPEN (deferred — off Day 3/4 critical path) | "Litigation — Road Accident Fund" matter-type template missing. "New from Template" lists 5 legal templates (Collections (Debt Recovery), Commercial (Corporate & Contract), Deceased Estate Administration, Litigation (Personal Injury / General), Property Transfer (Conveyancing)). Scenario specifies RAF-specific template with accident date, third-party, insurer, summons, quantum task sections. Generic "Litigation (Personal Injury / General)" used as workaround (9 tasks auto-generated, LSSA tariff linkage not verified). Owner: backend/product — author `project-template-packs/legal-za-raf.json` OR extend existing `legal-za.json` pack with RAF preset OR formally reclassify scenario to use generic Litigation + SA Legal — Matter Details field group for RAF-specific data. | backend | 0 |
| GAP-L-37 | Day 3 / 3.6 | LOW | OPEN (deferred — off Day 3/4 critical path) | Matter detail auto-attaches "SA Conveyancing — Matter Details" field group to a Litigation matter regardless of template selection. Over-broad auto-attachment — conveyancing fields (Erf Number, Deeds Office, Purchase Price, Transfer Duty, Bond Institution) are irrelevant for RAF litigation. Matter creation path appears to attach every legal-za field group instead of only those declared in the selected project-template-pack. Owner: backend — filter attached field groups by `fieldGroup.appliesToMatterTypes` metadata, OR frontend — client-side filter the Field Groups list. **Deferred** — cosmetic, can be hidden with the Remove button (tested, functional). | frontend | 0 |
| GAP-L-38 | Day 3 / 3.5 | LOW | OPEN (deferred — off Day 3/4 critical path) | Matter detail tab bar shows 20 tabs (including **Disbursements twice**) and does not match scenario-specified 8 tabs (Overview/Tasks/Documents/Time/Fee Notes/Trust/Activity/Audit). Missing by exact label: "Tasks" (Action Items used instead), "Fee Notes" (Fee Estimate / Financials / Statements cover adjacent concerns), "Audit" (Activity is closest, GAP-L-11 still OPEN). Duplicate: "Disbursements" listed twice (refs e646 + e657 in snapshot). Owner: frontend — consolidate tabs, rename Action Items → Tasks for legal-za vertical, add Audit tab (couples to GAP-L-11). **Deferred** — scenario Days 21/28/60/75/85 will re-surface this drift; batch together once GAP-L-33/L-34 unblock day 3. | frontend | 0 |
| GAP-L-39 | Day 3 / 3.3 | LOW | OPEN (deferred — off Day 3/4 critical path) | "+ New Matter" CTA on client detail navigates to `/projects?new=1&customerId=<customerId>` but the "New from Template — Configure" dialog's Client dropdown defaults to "None", not to the customer in the URL. Minor UX polish — user manually re-selects. Owner: frontend — Configure step should read `customerId` from URL searchParams and pre-select it in the Client combobox. **Deferred** — workaround = re-select client. | frontend | 0 |
| GAP-L-40 | Follow-up | MED | OPEN (deferred — feature follow-up, unblocks only multi-contact customers) | Firm-side "Add Portal Contact" dialog on client detail. Carved out from GAP-L-34 fix-spec: L-34 ships the shortcut (auto-create a GENERAL portal contact on customer-create from `customers.email`), which covers the common one-contact-per-customer case and unblocks Day 3–90 portal POV. L-40 tracks the proper CRUD feature — named multiple contacts per customer with roles (PRIMARY / BILLING / GENERAL), suspend / archive actions, edit display name + email. Needed for customers like Moroka Family Trust (Day 14+) where 2–3 beneficial owners each need separate portal access. Owner: backend (POST `/api/customers/{id}/portal-contacts` wrapping `PortalContactService.createContact` + PUT / DELETE for suspend/archive) + frontend (Add Portal Contact button + dialog on client detail Portal Contacts section). **Deferred** — feature follow-up; L-34 shortcut sufficient for Day 3–90 single-contact path. | both | 0 |
| GAP-L-41 | Day 3 / 3.11 | LOW | OPEN (deferred — off Day 3/4 critical path) | Create Information Request dialog does not expose a Due Date field, and `tenant_*.information_requests` has no `due_date` column (only `reminder_interval_days`). Scenario 3.11 asks for Due = Day 10 (today + 7); request row created with no due-date tracking at all. Owner: backend (Flyway migration to add `due_date DATE`, DTO update) + frontend (date picker in Create Information Request dialog). **Deferred** — LOW, not blocking dispatch. | both | 0 |
| GAP-L-42 | Day 3 / 3.14 + Day 4 / 4.2 | **HIGH / BLOCKER** | **SPEC_READY** | Information-request email's "View Request" link points to `http://localhost:3000/portal` (firm host, no token, literal path) instead of the portal magic-link entry point. DB probe: the 20:15:43 info-request send did NOT mint a `magic_link_tokens` row — only the DevPortalController backfill did at 20:12. Cascade effect: Day 4 Phase A (magic-link landing) blocked, Days 4/8/11/30/46/61/75 portal POV all dependent on delivered magic-links. Workaround applied this turn: minted token via `POST /portal/dev/generate-link` (DevPortalController) + navigated to `http://localhost:3002/auth/exchange?token=…&orgId=mathebula-partners` → 200, portal JWT issued, localStorage populated. **Spec**: `qa_cycle/fix-specs/GAP-L-42.md` — inject `MagicLinkService` into `InformationRequestEmailEventListener.onRequestSent`, call `generateToken(portalContactId, null)`, extend `InformationRequestEmailService.sendRequestSentEmail` signature to accept `rawToken + orgId` and build the portal deep-link from `docteams.app.portal-base-url`. Est S–M (30–60 min). Owner: backend. | backend | 0 |
| GAP-P-01 | Day 4 / 4.4 | HIGH | **FIXED** | Portal home `InfoRequestsCard` queries `/portal/information-requests?status=PENDING` → **404** ("No static resource"). Backend exposes the feature under `/portal/requests` → 200 returning REQ-0001. Frontend/backend path mismatch at `portal/app/(authenticated)/home/page.tsx` line 62. Effect: home page silently shows no pending info requests even when REQ-0001 was dispatched minutes earlier — client has no discovery affordance. Also confirmed via code read: `legal-za.json`, `accounting-za.json`, `consulting-za.json` all omit `"information_requests"` from `enabledModules`, so the `modules.includes(...)` gate on both the home card AND the sidebar Requests nav item (nav-items.ts line 77–82) evaluates false — compounds the issue. **FIXED via PR #1100** (merged 2026-04-21 SAST, commit `6701c200`) — path corrected in `portal/app/(authenticated)/home/page.tsx` (client-side `status !== "COMPLETED"` filter); `"information_requests"` appended to `enabledModules` in `legal-za.json`, `accounting-za.json`, `consulting-za.json`. Portal build + backend compile clean. Needs backend restart so `PackReconciliationRunner` re-syncs tenant `org_settings.enabled_modules`. Couples to GAP-P-02 — card now loads, but clicking it still hits `/requests` which 404s until P-02 lands. | portal-fe + backend | 0 |
| GAP-P-02 | Day 4 / 4.8 | **HIGH / BLOCKER** | **FIXED** (stub scope) | Portal had **no UI route** for information-request pickup / upload. Probes: `/requests` → 404, `/information-requests` → 404. Backend `GET /portal/requests/{id}` returns complete FICA payload. Blocks entire portal POV across 90-day script. **FIXED via PR #1101** (merged 2026-04-21 SAST, commit `f1fd1073`) — two new files per spec: `portal/app/(authenticated)/requests/page.tsx` (list, 98 LOC, calls `/portal/requests`, renders request cards with status + submitted-count) + `portal/app/(authenticated)/requests/[id]/page.tsx` (detail, 216 LOC, renders item cards with per-item `<input type=file>` + presigned-URL upload + submit flow). Submit endpoint returns empty body so used `portalFetch` directly (instead of `portalPost`) for that one call to bypass JSON parse. Portal lint (0 errors, pre-existing warnings untouched) + build (✓ compiled in 5.7s, both `/requests` static and `/requests/[id]` dynamic routes present in Route summary) clean. No backend changes. **Stub scope**: no status badges, no filters, no progress indicators, no TEXT_RESPONSE — those are GAP-P-05. Portal restart needed for safety (HMR normally picks up TSX edits but new routes are safer with a restart). | portal-fe | 0 |
| GAP-P-05 | Follow-up | LOW | OPEN (deferred — polish after P-02 stub lands) | Portal requests UI polish after the GAP-P-02 stub ships: empty/loading skeletons, coloured status badges (SENT/IN_PROGRESS/SUBMITTED/ACCEPTED/REJECTED/COMPLETED), upload progress indicators, inline optional-note textarea for non-file items, per-item rejection-reason display with "re-upload" affordance, file-type client-side validation against `fileTypeHints`, pagination or filter on the list page. Also: `TEXT_RESPONSE` item support (scenario Day 8 will hit this — stub only handles FILE_UPLOAD). Owner: portal-fe. **Deferred** — not on the Day 4 blocker path. | portal-fe | 0 |
| GAP-P-03 | Day 4 / 4.8 | MED | OPEN | Portal Matters tab renders "No projects yet" and direct-URL `/projects/40881f2f-…` → "The requested resource was not found", despite Sipho being linked to that matter firm-side (ACTIVE matter, customer_id on project row). Either portal project scope filter is over-restrictive (possibly requires ACTIVE customer lifecycle — Sipho is PROSPECT until Day 7) or authorization rejects portal contacts as non-members. Owner: backend portal service + portal frontend fallback copy. Severity MED — doesn't directly block Day 4 Phase B (that's blocked by GAP-P-02), but will block Day 5+ matter deep-link flows from portal. | backend + portal-fe | 0 |

## Legend

- **Status**: OPEN -> SPEC_READY -> FIXED -> VERIFIED | REOPENED | STUCK | WONT_FIX | RESOLVED
- **Severity**: HIGH (blocker) / MED (cascading bug) / LOW (cosmetic)
- **Owner**: QA / Product / Dev / Infra

## Log

- 2026-04-21 18:59 SAST — Cycle 1 started. Archived prior legal-90day state. Fresh status seeded from legal-za-full-lifecycle-keycloak.md.
- 2026-04-21 19:04 SAST — Infra verification complete. Stack: Docker infra already up (KC, Postgres, LocalStack, Mailpit all healthy 3-4d uptime); gateway (PID 71426) + frontend (PID 62385) already running; backend + portal were stale and started fresh via `svc.sh start backend portal` (ready 30s + 3s). All four services pass health checks (8080/8443/3000/3002). Keycloak bootstrap re-run (idempotent) — padmin and gateway-bff mappers OK. Session 0 prep: 0.A ✓ (all svc UP), 0.B ✓ (portal 307 on `/`), 0.C BLOCKER (pre-existing `mathebula-partners` org + schema `tenant_5039f2d497cf` — see GAP-INFRA-01), 0.D ✓ (0 KC users matching `mathebula-test`), 0.E n/a (`public.portal_contacts` table does not exist — portal contacts live per-tenant, so dropping the mathebula schema in 0.C would cover this), 0.F ✓ (Mailpit purged, was 18 stale), 0.G pending QA/Product (PayFast sandbox vs stub — not infra), 0.H ✓ (backend logs confirm legal-za packs installed + reconciled for tenant_5039f2d497cf; 4 tenants reconciled 4 succeeded 0 failed). QA NOT cleared to start Day 0 until GAP-INFRA-01 resolved.
- 2026-04-21 19:22 SAST — Infra cleanup turn (GAP-INFRA-01 → VERIFIED). Re-verified live Keycloak state via admin REST (token from master realm). **Correction to prior snapshot**: live KC org id was `d49948bb-8dca-4ee3-8a87-4ff97e63ae2b` (not `3730e32a…` — that was the Postgres `organizations.id`); 3 users with `@mathebula-test.local` existed (not 0): Bob Ndlovu `79fb8ec4…`, Carol Mokoena `9110900e…`, Thandi Mathebula `e57609a8…`, all MANAGED members of the org. Deleted in order: (1) 3 KC users (HTTP 204 each), (2) KC org `d49948bb…` (HTTP 204), (3) `subscription_payments` (0 rows), `subscriptions` row `ec8b9574-028d-433f-a11e-b6e4823e5c34` (TRIALING, no payments — surfaced on first DELETE attempt via FK; expected for a provisioned org), `access_requests` row `4b9c3030-aed6-494c-aca0-f8fc3442fd73`, `org_schema_mapping` row `bd05eca8-1de1-4493-9645-4904eec5627e`, `organizations` row `3730e32a-5310-43e3-b001-495def2b524d` — all in one transaction, COMMIT OK, (4) `DROP SCHEMA tenant_5039f2d497cf CASCADE` — dropped 103 objects. Post-checks: KC org list empty for `mathebula`, 0 `@mathebula-test.local` users, registry counts all 0, schema gone, 3 other tenant schemas (`2a96bc3b208b`, `8ee5c5a6e45f`, `f6e34f99f3b9`) intact. Health: backend `{"status":"UP"}`, gateway `{"status":"UP"}`, KC realm `/realms/docteams` HTTP 200, frontend HTTP 200, portal HTTP 307, Mailpit purged (HTTP 200). Dev Stack → READY. QA cleared to start Day 0 Phase A.
- 2026-04-21 20:15 SAST — Day 0 QA turn: **32/32 checkpoints executed, 30 PASS, 2 PARTIAL (0.9, 0.13, 0.26, 0.27), 0 FAIL, 0 BLOCKED**. New gaps: GAP-L-20 (LOW, frontend — /dashboard fallback copy + "1 of 10 members" seat counter), GAP-L-21 (LOW, frontend — no access-request detail drill-down), GAP-L-22 (MED, both — post-registration session handoff broken for all three registrants). Carry-forward watch list: GAP-L-01 bounce page ✓ working; GAP-L-04 end-session confirmation nuisance still present (LOW, not re-logged); GAP-L-02/L-03 KC i18n ✓ clean; OBS-L-06 post-reg handoff re-observed → escalated to GAP-L-22. Real artefacts: KC users Thandi `c5693386-dc45-467a-b171-18713b94460f`, Bob `92823624-3e97-457d-8424-61381f89c81e`, Carol `b72c738b-686f-4bd2-8ba1-da7eeeddecab`; tenant schema `tenant_5039f2d497cf`; org registry id `6b404169-f142-436a-8e6c-354a66843a5a` (KC orgId in JWTs). Legal-ZA packs INSTALLED confirmed (legal-za v5 16 items + automation-legal-za v2 5 items + compliance-za + common + automation-common). Dashboard screenshot: `qa_cycle/checkpoint-results/day-00-firm-dashboard-legal.png`. Day 0 summary checks (org created, 3 KC users, legal terminology+nav, no tier upsell) all PASS with LOW copy leaks noted. QA Position advanced to Day 1 — 1.1. Next turn: QA continues with Day 1 firm-onboarding polish.
- 2026-04-21 20:27 SAST — Day 1 QA turn: **2/7 checkpoints executed, 0 PASS, 1 BLOCKED (1.1), 1 PARTIAL-discovery (1.5 dialog probed), 5 HALTED on BLOCKER rule**. New gaps: **GAP-L-23 (HIGH/BLOCKER, frontend — `/settings/general` server-actions loader `ReferenceError: PortalDigestCadence is not defined`; every Save / upload / profile mutation returns 500)**, GAP-L-24 (MED, frontend — "Failed to load profiles" card, cascade of L-23), GAP-L-25 (LOW, backend — Trust Account type dropdown missing SECTION_86 for legal-za). Workaround verification: GAP-L-22 explicit KC logout + fresh login held clean this turn (Thandi landed on her own dashboard, no padmin/Carol cross-session leak). Evidence: browser captured 17 × POST `/org/mathebula-partners/settings/general` → 500 in one turn; `.svc/logs/frontend.log` prints the ReferenceError at module evaluation of `.next-internal/server/app/(app)/org/[slug]/settings/general/page/actions.js` (server actions loader) line 15 `export {updatePortalDigestCadence} from 'ACTIONS_MODULE4'`; DB read-only `SELECT brand_color, logo_s3_key FROM tenant_5039f2d497cf.org_settings` returns empty row (confirms save never reached backend). Screenshot: `qa_cycle/checkpoint-results/day-01-1.1-save-settings-500.png`. Discovery probes (no state mutation) surfaced two independent scenario-vs-feature gaps for Day 1.3/1.5: (a) LSSA tariff schedule has NO firm-side UI route despite 19 items seeded in DB — `/settings/rates` is per-member billing rates, not tariff schedules; (b) Trust Account Type enum offers only `General` / `Investment`, no `SECTION_86` option. QA Position → `Day 1 — 1.1 (blocked pending GAP-L-23)`. Next-turn recommendation: **Dev fix** GAP-L-23 (likely swap `export type { PortalDigestCadence, PortalRetainerMemberDisplay }` re-export in `frontend/app/(app)/org/[slug]/settings/general/portal-actions.ts` line 10 for a local-consumption pattern OR split portal-actions.ts so the server-actions loader sees inline `export async function ...` without type re-exports) → then QA resumes Day 1 from 1.1.
- 2026-04-21 20:38 SAST — Product triage turn: specs written for GAP-L-23 (`qa_cycle/fix-specs/GAP-L-23.md`, est S <30 min). QA's hypothesis **confirmed** via code read of `frontend/app/(app)/org/[slug]/settings/general/portal-actions.ts` line 10 — file carries `"use server"` directive yet re-exports types, violating the server-action contract. Fix is a two-file edit: (1) delete line 10 of `portal-actions.ts`, (2) switch `frontend/components/settings/portal-settings-section.tsx` import lines 13–18 to import types directly from `@/lib/types/settings` (authoritative definition at lines 62–67). GAP-L-24 marked **GATED_ON_L-23** — confirmed cascade (`fetchProfiles` exported from the same broken loader); no separate Dev spec; re-verify after L-23 merges. GAP-L-25 **deferred** — root-cause scoped (enum + Flyway migration on `chk_trust_account_type` check constraint + primary-account partial unique index decision + 5 frontend files); exceeds <30-min bar so not specced this batch, will spec once QA is unblocked past Day 1. GAP-L-20, GAP-L-21, GAP-L-22 **deferred** — OBS/LOW/MED severity, none on Day 1 critical path; batch later so Dev's cycle focuses on the QA blocker. **Dev agent should tackle GAP-L-23 first and only** — spec is self-contained.
- 2026-04-21 20:43 SAST — Dev: GAP-L-23 fix merged (PR #1097 → `2a94de5f`). Two-file edit per spec — removed `export type { PortalDigestCadence, PortalRetainerMemberDisplay }` from `portal-actions.ts`, consumers in `components/settings/portal-settings-section.tsx` now import types directly from `@/lib/types/settings`. Grep confirmed only two files imported the types from `portal-actions` (the consumer component + the unit test which mocks the module and doesn't touch the re-export). `pnpm lint` 0 errors, `pnpm run build` succeeded (server-actions loader bundle regenerated cleanly; no `ReferenceError`, no "Only async functions allowed" turbopack error), 2046 unit tests pass including `portal-settings-cadence.test.tsx`. **GAP-L-24 resolved as cascade** (FIXED pending QA re-verify). NEEDS_REBUILD flagged true — Turbopack HMR should auto-pick up the source edit, but a targeted frontend restart is the safer guarantee before QA re-runs Day 1 1.1–1.4.
- 2026-04-21 20:44 SAST — Infra: frontend restart after GAP-L-23 fix merge. Startup clean. Cleared NEEDS_REBUILD. Pre-restart state: frontend UP on PID 62385, HTTP 200. `bash compose/scripts/svc.sh restart frontend` → stopped PID 62385, started new PID 33007, ready in 3s (svc.sh health-wait on port 3000). Post-restart verification: `curl http://localhost:3000/` → 200 (two GETs hit, `compile: 692ms, render: 317ms` first, `compile: 4ms, render: 53ms` second — server-actions bundle compiling cleanly). Frontend logs clean: Next.js 16.1.6 Turbopack, "Ready in 3.1s", no `ReferenceError`, no `SyntaxError`, only a benign multi-lockfile workspace-root warning (unrelated, pre-existing). Backend/gateway/portal untouched. QA cleared to resume Day 1 from 1.1 — re-run settings mutations (Save Settings, Brand Color, Logo Upload, Vertical Profile, Portal Digest Cadence, Portal Retainer Member Display, Org Document) and confirm all return 200 + values persist.
- 2026-04-21 20:55 SAST — Day 1 QA turn (resumed): verified L-23/L-24; **7/7 checkpoints executed, 4 PASS (1.1, 1.3, 1.6, 1.7), 1 FAIL (1.2), 2 PARTIAL (1.4, 1.5), 0 BLOCKED**. GAP-L-23 **VERIFIED** (12+ × 200 POSTs across Save Settings, Logo Upload, Portal Digest Cadence mutations; `.svc/logs/frontend.log` clean; DB row shows `brand_color='#1B3358'`, `logo_s3_key='org/tenant_5039f2d497cf/branding/logo.png'`, `portal_digest_cadence='WEEKLY'`). GAP-L-24 **VERIFIED** (Vertical Profile combobox lists all 4 industry profiles — no "Failed to load profiles" error). New gaps: **GAP-L-26** (LOW, frontend — branding saves server-side but sidebar/nav UI chrome doesn't consume `brand_color` or `logo_s3_key`: computed sidebar bg is slate-950, `--brand-color` CSS var empty, no logo `<img>` in shell), **GAP-L-27** (LOW, backend — tax rate seeded as bare "Standard" 15% not "VAT — Standard"; formalises OBS-L-05 carry-forward). Carry-forward verifications: GAP-L-22 workaround held (Thandi landed clean on her own dashboard); GAP-L-25 confirmed still open (Account Type dropdown lacks SECTION_86; workaround = GENERAL). Trust account "Mathebula Trust — Main / Standard Bank / 12345678" created as GENERAL, is_primary=t. LSSA tariff UI is at `/legal/tariffs` (not Settings > Rate Cards as scenario says); 1 schedule "LSSA 2024/2025 High Court Party-and-Party" with 19 items renders correctly; nearest Section 4 row to scenario ask is "Attendance at court (per day) R 7,800.00" — not the "per hour" row scenario expects; schedule effective 2024 not 2026. Screenshots: `day-01-1.1-save-settings-OK.png`, `day-01-1.2-dashboard-branding.png`, `day-01-trust-account-created.png`. QA Position advanced to `Day 2 — 2.1`. Next turn: QA context-swaps to Bob (Admin) — explicit KC logout + fresh OIDC login as `bob@mathebula-test.local`, then Day 2 client onboarding flow.
- 2026-04-21 21:22 SAST — Day 3 QA turn: **14/14 checkpoints executed, 2 PASS full (3.1, 3.7), 3 PASS partial (3.2, 3.3, 3.4), 2 FAIL (3.5, 3.6), 1 PARTIAL (3.3 custom-field save blocked by PROSPECT gate), 6 BLOCKED (3.8–3.13, 3.14)**. First BLOCKER hit at 3.8 — halted per rule. Matter `RAF-2026-001` (id `40881f2f-7cfc-45d9-8619-de18fd2d75bb`) created ACTIVE with 9 tasks auto-generated from "Litigation (Personal Injury / General)" template, linked to Sipho. **GAP-L-33** (HIGH/BLOCKER, backend — FICA Onboarding Pack template missing: 5 platform request templates seeded + 7 request-pack JSONs on filesystem, no `fica-onboarding-pack.json`). **GAP-L-34** (HIGH/BLOCKER, both — no firm-side portal-contact create path: GET-only endpoint, PortalContactService.createContact wired only to DevPortalController; Create Information Request dialog shows "No portal contacts found" with both submit buttons disabled; customers.email is NOT auto-propagated to portal_contacts). **GAP-L-35** (MED, backend — Save Custom Fields on matter detail returns "Cannot create project for customer in PROSPECT lifecycle status" despite matter already created; blocks Court+Opposing Party persistence; also leaks "project"/"customer" terminology). **GAP-L-36** (MED, backend/product — RAF-specific matter template missing; 5 legal templates shipped, no RAF variant). **GAP-L-37** (LOW, frontend/backend — over-broad field-group auto-attach: SA Conveyancing group applied to Litigation matter). **GAP-L-38** (LOW, frontend — 20-tab matter detail with Disbursements duplicated; missing Tasks/Fee Notes/Audit by exact labels). **GAP-L-39** (LOW, frontend — customerId URL param lost by Configure step). Carry-forward re-observations: GAP-L-22 not triggered (Bob session held clean); GAP-L-26 still present (sidebar slate-950, no logo); Day-75 "Project/Customer" terminology leak re-confirmed in backend error strings. Screenshots: `day-03-3.4-matter-raf-created.png`, `day-03-3.8-no-fica-template-no-portal-contact.png`. Evidence: DB reads confirmed matter row persisted + 0 portal_contacts + 0 information_requests + 5 request_templates with no FICA; filesystem ls confirmed no fica-onboarding-pack.json; source reads confirmed no POST /portal-contacts endpoint on CustomerController.java. QA Position → `Day 3 — 3.8 (blocked pending GAP-L-33 + GAP-L-34)`. Next-turn recommendation: **Dev fix** — (1) author `backend/src/main/resources/request-packs/fica-onboarding-pack.json` (3 items) + register in legal-za pack; (2) add POST `/api/customers/{id}/portal-contacts` wrapping `PortalContactService.createContact` + "Add Portal Contact" affordance in the Create Information Request dialog's empty-state (OR the simplest shortcut: auto-create a PortalContact row on customer-create from `customers.email`). Either fix alone is not sufficient — both HIGH blockers must land before QA can resume Day 3 from 3.8.
- 2026-04-21 21:09 SAST — Day 2 QA turn: **10/10 checkpoints executed, 6 PASS (2.1, 2.2, 2.3, 2.4, 2.5, 2.7), 1 FAIL (2.6), 3 SKIPPED-BY-DESIGN (2.8/2.9/2.10 — KYC adapter not configured; scenario permits skip-with-gap), 0 BLOCKED**. Context swap to Bob (Admin) via explicit KC logout → fresh OIDC login landed him cleanly on his own dashboard (**GAP-L-22 workaround held**). Sipho Dlamini created as INDIVIDUAL client (id `8fe5eea2-75fc-4df2-b4d0-267486df68bd`, lifecycle PROSPECT, status ACTIVE) with ID `8501015800088` + Preferred Correspondence=Email via SA Legal — Client Details vertical pack. Conflict-check engine confirmed real (not a stub): DB persisted `Sipho Dlamini | NEW_CLIENT | CONFLICT_FOUND` (2 × 100% self-match rows) AND `Nontando Zulu | NEW_CLIENT | NO_CONFLICT` control probe. KYC integration tile shows "Not Configured / Automated identity verification for FICA compliance" on `/settings/integrations`. New gaps: **GAP-L-28** (MED, both — conflict engine doesn't exclude the subject from its match set; new clients get false-positive self-match on 2.6), **GAP-L-29** (LOW, frontend — conflict-check form Customer/Matter selects never hydrate from tenant registry, only `-- None --`), **GAP-L-30** (LOW, product — KYC adapter unconfigured, no default provider wired for legal-za vertical), **GAP-L-31** (LOW, frontend — `/customers` empty-state copy leaks "customers" word under legal vertical), **GAP-L-32** (LOW, frontend — Create Client doesn't auto-redirect to new detail). Carry-forward re-observations: GAP-L-22 held, GAP-L-26 still present (Bob's sidebar also slate-950, no logo). Screenshots: `day-02-client-detail-sipho.png`, `day-02-2.7-conflict-check-unknown-clear.png`, `day-02-2.10-kyc-not-configured.png`. QA Position advanced to `Day 3 — 3.1`. Next turn: QA continues as Bob (no context swap) — create RAF matter RAF-2026-001 from Sipho's client detail, send FICA Onboarding Pack info request, verify Mailpit magic-link delivery.
- 2026-04-21 21:32 SAST — Product triage: specs written for GAP-L-33, GAP-L-34. Two blockers. Deferred: L-35/36/37/38/39. **GAP-L-33** spec (`qa_cycle/fix-specs/GAP-L-33.md`, est S <30 min) — pure asset add: author `backend/src/main/resources/request-packs/fica-onboarding-pack.json` (3 FICA items: ID copy / Proof of residence ≤ 3 months / Bank statement ≤ 3 months, all FILE_UPLOAD) + append `fica-onboarding-pack` to `packs.request` in `vertical-profiles/legal-za.json`. Confirmed via code read: `RequestPackSeeder` extends `AbstractPackSeeder`, auto-scans `classpath:request-packs/*.json`, filters by each pack's `verticalProfile` field against tenant's `org_settings.vertical_profile`; `PackReconciliationRunner` re-seeds all tenants on backend startup — no code changes, no migration, just backend restart after the file lands. **GAP-L-34** spec (`qa_cycle/fix-specs/GAP-L-34.md`, est S <30 min) — chose the **shortcut** (auto-create a PortalContact on customer-create via event listener) over the full Add Portal Contact dialog because: (a) scenario 3.9 literally specifies "portal contact auto-populated from client record", so auto-create IS the expected behaviour; (b) ≤60 LOC backend-only (one new `@EventListener @Component PortalContactAutoProvisioner` that hooks `CustomerCreatedEvent` → calls existing `PortalContactService.createContact` with `email` + `name` + `GENERAL` role); (c) no frontend work (existing dialog already auto-selects when `contacts.length === 1`, just needs ≥1 contact to unblock buttons); (d) idempotent via `ResourceConflictException` catch. The proper "Add Portal Contact" CRUD dialog (needed for Moroka Family Trust with 2–3 beneficial owners on Day 14+) is carved out as a separate follow-up **GAP-L-40** (MED, deferred — off Day 3/4 critical path; L-34 shortcut covers single-contact case for Day 3–90). Root cause for L-34 confirmed via code read: `CustomerController.java:343` exposes only `@GetMapping("/{id}/portal-contacts")`, no POST; `PortalContactService.createContact` only invoked from `DevPortalController.java:115` (dev tooling); `CustomerService.createCustomer` already publishes `CustomerCreatedEvent` carrying `customerId/name/email/orgId/tenantId` — an ideal hook point requiring no new plumbing. Deferred items (all OPEN, notes appended): L-35 (MED, backend — PROSPECT gate on custom-field PATCH), L-36 (MED, backend — RAF-specific template missing), L-37 (LOW, frontend — over-broad field-group auto-attach), L-38 (LOW, frontend — 20-tab matter detail), L-39 (LOW, frontend — customerId URL param lost by Configure step). **Dev agent should tackle both L-33 and L-34 in parallel** — specs are self-contained, neither depends on the other, and Day 3 resumes only when both land.
- 2026-04-21 21:38 SAST — Dev: GAP-L-33 fix merged (PR #1098 → `49027a45`). Two-file change per spec — created `backend/src/main/resources/request-packs/fica-onboarding-pack.json` (3 FICA items: ID copy / Proof of residence ≤ 3 months / Bank statement ≤ 3 months, all FILE_UPLOAD, required=true, sortOrder 1/2/3) + appended `"fica-onboarding-pack"` to `packs.request` in `backend/src/main/resources/vertical-profiles/legal-za.json`. Build: `./mvnw spotless:apply` BUILD SUCCESS, `./mvnw compile -q` clean exit 0, `./mvnw test -Dtest='*RequestPack*' -q` all 7 tests pass (ConsultingZaRequestPackTest 3/3, ConveyancingRequestPackTest 2/2, YearEndRequestPackTest 2/2), `./mvnw test-compile -q` clean. No code changes — pure asset addition picked up by `AbstractPackSeeder` classpath scan. **NEEDS_REBUILD flagged true** — backend restart required so `PackReconciliationRunner` seeds `fica-onboarding-pack` template into `tenant_5039f2d497cf` (and any other legal-za tenants). After restart, QA re-verify Day 3 3.8+: Create Information Request dialog on RAF-2026-001 should list "FICA Onboarding Pack (3 items)" — still gated on GAP-L-34 landing before dispatch is possible.
- 2026-04-21 22:03 SAST — Infra: backend restart post L-33/L-34 merges. `bash compose/scripts/svc.sh restart backend` stopped old PID 12688, started new PID 62516, ready in 27s (svc.sh health-wait on port 8080). Post-restart `/actuator/health` → `{"status":"UP"}`. L-33 pack seeded: **yes** — backend log line 196 confirms `Applied request pack fica-onboarding-pack v1 for tenant tenant_5039f2d497cf`; DB row present in `tenant_5039f2d497cf.request_templates` (id `1324776f-2ad3-459c-b2ba-049a5b3806c9`, name `FICA Onboarding Pack`, pack_id `fica-onboarding-pack`, source=PLATFORM, active=t); template count now 6 (was 5). L-34 listener registered: **yes** — `PortalContactAutoProvisioner.java` carries `@Component` + `@EventListener` on `CustomerCreatedEvent`; Spring component-scanned via `BackendApplication` (log shows "Started BackendApplication in 10.137 seconds" at 20:03:29 UTC with no classpath scan errors; no failed-bean messages for `io.b2mash.b2b.b2bstrawman.portal.*`). Cleared NEEDS_REBUILD. Other services untouched (gateway PID 71426 ext, frontend PID 33007, portal PID 12898). QA cleared to resume Day 3 from 3.8.
- 2026-04-21 22:35 SAST — Product triage turn (Day 4 blockers). Three HIGH gaps triaged; all three SPEC_READY. **GAP-P-01** (`qa_cycle/fix-specs/GAP-P-01.md`, est S <30 min) — dual fix: (1) correct the path in `portal/app/(authenticated)/home/page.tsx` line 62 from `/portal/information-requests?status=PENDING` to `/portal/requests` with client-side `status !== "COMPLETED"` filter, (2) append `"information_requests"` to `enabledModules` in `vertical-profiles/legal-za.json` / `accounting-za.json` / `consulting-za.json` — module gate currently hides both the home card AND the sidebar Requests nav item on every tenant. Confirmed via code read, not speculation. **GAP-P-02** (`qa_cycle/fix-specs/GAP-P-02.md`, est M 1.5–2hr) — admitted at the upper budget edge as a **stub-scope** fix: two new pages (list + detail with per-item presigned upload), ~260 LOC, reuses existing portalGet/portalPost + Card + document-list upload pattern. No backend changes — `GET /portal/requests`, `GET /portal/requests/{id}`, `POST /portal/requests/{id}/items/{itemId}/upload`, `POST /portal/requests/{id}/items/{itemId}/submit` all verified working via QA's direct-fetch probes. Polish (badges, progress indicators, filters, TEXT_RESPONSE item support) explicitly carved out as **GAP-P-05** (new LOW, deferred) so the stub scope is honest. Decision rationale documented in the spec: without P-02 every portal-POV day (4/8/11/15/30/46/61/75) is NOT_EXECUTED, so the cycle effectively ends unless the stub ships. **GAP-L-42** (`qa_cycle/fix-specs/GAP-L-42.md`, est S–M 30–60 min) — inject `MagicLinkService` into `InformationRequestEmailEventListener.onRequestSent`, mint token via existing `generateToken(portalContactId, null)` (reuses the same path `PortalEmailService` uses for magic-link dispatch), extend `InformationRequestEmailService.sendRequestSentEmail` signature to take `rawToken + orgId` and build `portalBaseUrl + "/auth/exchange?token=" + rawToken + "&orgId=" + orgId`. Option A chosen (accept the redundant portal-magic-link side-effect email) over Option B (expose a silent-mint method) for lower regression surface. Deferred (all OPEN, off Day 4 critical path): **GAP-L-41** (due-date field), **GAP-P-03** (portal Matters list empty — likely a portal-scope-filter bug that may self-resolve once Sipho transitions out of PROSPECT on Day 7). **Dev agent recommendation: tackle GAP-P-01 + GAP-L-42 in parallel (both backend/small-frontend, both self-contained), then GAP-P-02 (portal-fe, larger). GAP-P-01's JSON change + GAP-L-42 both require a backend restart; batch them.** QA resumes Day 4 from 4.2 (with L-42 fix the email deep-link works end-to-end without the DevPortalController workaround) and 4.4/4.8 (with P-01 + P-02 the portal UI renders and Sipho can complete the FICA upload flow).
- 2026-04-21 21:58 SAST — Dev: GAP-L-34 fix merged (PR #1099 → `f1edbc23`). Backend restart needed for event listener registration. Five-file change — new `backend/.../portal/PortalContactAutoProvisioner.java` (`@EventListener` on `CustomerCreatedEvent` — GENERAL contact auto-created from customer's email + name; idempotent pre-check via `existsByEmailAndCustomerId`; skips silently when email null/blank or orgId unavailable) + new integration test `PortalContactAutoProvisionerTest.java` (3 tests: happy path via POST /api/customers, replay idempotency, null/blank email skip). Consolidation: removed the redundant GAP-020 PROSPECT→ONBOARDING contact-creation from `CustomerLifecycleService.java` (superseded — auto-creation now happens at customer-create, unblocking portal flows before any lifecycle transition), and updated `PortalContactAutoCreationTest.java` expectations + `MagicLinkTokenIntegrationTest.java` (tenant B's PRIMARY contact now uses a distinct email to avoid the now-pre-provisioned GENERAL occupying the customer email). Build: `./mvnw spotless:apply` OK; `./mvnw compile -q` EXIT=0; `./mvnw test -Dtest='*PortalContact*'` 18/18 pass; `./mvnw test -Dtest='*CustomerService*,*CustomerLifecycle*'` 65/65 pass; `./mvnw test -Dtest='*Checklist*,*Compliance*,*Lifecycle*,*Customer*,*PortalContact*,*MagicLink*'` 384/384 pass. Pre-existing failures (unrelated, confirmed present on parent branch): `PortalProfileControllerTest`, `PortalNotificationPreferenceControllerTest`, `PortalCommentPostIntegrationTest`, `PortalProposalControllerTest`, `PortalContextControllerIntegrationTest`. **NEEDS_REBUILD flagged true** — backend restart required so the Spring context picks up the new `PortalContactAutoProvisioner` bean and registers its listener. After restart, QA re-verify Day 3 3.8+: re-create Sipho (or reseed) → DB should show one row in `tenant_5039f2d497cf.portal_contacts` (email=`sipho.portal@example.com`, role=GENERAL, status=ACTIVE), Create Information Request dialog auto-selects Sipho, Save as Draft + Send Now buttons enabled, Send → 200 + Mailpit magic-link delivered.
- 2026-04-21 22:42 SAST — Dev: GAP-P-01 fix merged (PR #1100 → `6701c200`). Backend+portal restart needed. Four-file change per spec — `portal/app/(authenticated)/home/page.tsx` path `/portal/information-requests?status=PENDING` → `/portal/requests` with client-side `status !== "COMPLETED"` filter; `"information_requests"` appended to `enabledModules` in `backend/src/main/resources/vertical-profiles/legal-za.json`, `accounting-za.json`, `consulting-za.json`. Portal `pnpm install` + `pnpm run lint` (0 errors, only pre-existing warnings) + `pnpm run build` (✓ compiled in 7.4s, 13 routes generated) all clean. Backend `./mvnw compile -q` EXIT=0. **NEEDS_REBUILD flagged true** — backend restart required so `PackReconciliationRunner` re-syncs `tenant_5039f2d497cf.org_settings.enabled_modules` to include `information_requests`; portal HMR normally picks up the TSX edit but restart is safer to guarantee the home-page bundle regenerates. After restart, QA re-runs Day 4 Phase A: InfoRequestsCard renders pending FICA request (REQ-0001) on `/home`; sidebar shows Requests nav item. Click-through to `/requests` still 404s until GAP-P-02 lands — expected.
- 2026-04-21 22:48 SAST — Dev: GAP-P-02 fix merged (PR #1101 → `f1fd1073`). Portal restart needed for new routes. Two new files per spec — `portal/app/(authenticated)/requests/page.tsx` (98 LOC, list page calling `GET /portal/requests` with skeleton loading, empty-state, request cards linking to `/requests/{id}`) + `portal/app/(authenticated)/requests/[id]/page.tsx` (216 LOC, detail page with per-item `ItemCard` component — FILE_UPLOAD renders `<input type=file>` + Upload and submit button, on submit initiates presigned URL via `POST /portal/requests/{id}/items/{itemId}/upload`, PUTs file to S3, calls `POST /portal/requests/{id}/items/{itemId}/submit` via `portalFetch` directly to bypass empty-body JSON parse on the `ResponseEntity<Void>` return; refresh-token pattern used for post-submit reload since `react-hooks/set-state-in-effect` lint rule disallows the spec's `refresh()` pattern). Portal `pnpm install` + `pnpm run lint` (0 errors, 5 pre-existing warnings untouched) + `pnpm run build` (✓ compiled in 5.7s, both `/requests` static and `/requests/[id]` dynamic routes present in Route summary) all clean. No backend changes. **Stub scope honoured** — no status badges, no filters, no progress indicators, no TEXT_RESPONSE support (deferred to GAP-P-05). **NEEDS_REBUILD flagged true** — portal uses HMR but safer to restart given new App Router routes. After restart, QA re-runs Day 4 Phase B 4.8–4.12: Sipho opens `/requests`, clicks REQ-0001, sees 3 FICA item cards, uploads PDFs, each flips to "Submitted — status: SUBMITTED", firm sees SUBMITTED status.
- 2026-04-21 22:25 SAST — Day 3 resume + Day 4 QA turn: verified L-33/L-34; Day 3 **7/7** resume checkpoints executed, **6 PASS** (3.8/3.9/3.10/3.12/3.13/3.14) **1 FAIL** (3.11 due-date missing); Day 4 **8/14** executed, **2 PASS** (4.3/4.6 via workaround), **3 FAIL** (4.2/4.4/4.5), **1 BLOCKER** (4.8) + **6 BLOCKED** (4.8–4.14 cascade). GAP-L-33 VERIFIED (FICA template now in dropdown + DB row + template items pre-filled on send). GAP-L-34 VERIFIED (fresh customer "Test Client FICA Verify" created → `PortalContactAutoProvisioner` listener fired synchronously, inserted GENERAL/ACTIVE portal_contact with correct email+name; separately backfilled Sipho's portal_contact via `POST /portal/dev/generate-link` Option C per scenario note). REQ-0001 dispatched successfully to Sipho (DB row, request_items with 3 FICA items, Sent status, email delivered). New gaps: **GAP-L-41** (LOW, backend+frontend — Create Info Request dialog has no Due Date field; table missing `due_date` column), **GAP-L-42** (HIGH/BLOCKER, backend — info-request email points to `http://localhost:3000/portal` with no token; no `magic_link_tokens` row minted by send path; Day 4 Phase A fundamentally broken without workaround), **GAP-P-01** (HIGH, portal-fe — home InfoRequestsCard queries `/portal/information-requests` (404) instead of backend's `/portal/requests` (200)), **GAP-P-02** (HIGH/BLOCKER, portal-fe — no UI route at all for info-request pickup/upload; `/requests` 404; backend is ready but portal pages are missing; blocks entire portal POV across the 90-day script), **GAP-P-03** (MED, both — Matters page empty / project detail 404 for Sipho despite firm-side linkage). Evidence: Mailpit message `kunWvRjbgFwpWHzveAQqrA` subject "Information request REQ-0001 from Mathebula & Partners" body href="http://localhost:3000/portal" (firm port, no token); DB `information_requests` row `06dc1a7e-…` status=SENT, portal_contact_id=d9ecf332-… (Sipho); DB `magic_link_tokens` 2 rows both from DevPortal backfill at 20:12 (none from the 20:15 send); `fetch('/portal/information-requests')` → 404 vs `fetch('/portal/requests')` → 200 with REQ-0001. Screenshots: `day-03-3.8-fica-template-present.png`, `day-03-3.9-dialog-fica-sipho-enabled.png`, `day-03-3.12-fica-request-sent.png`, `day-04-4.4-portal-no-requests-ui.png`. QA Position → `Day 4 — 4.8 (blocked pending GAP-P-01 + GAP-P-02 + GAP-L-42)`. Next-turn recommendation: **Dev fix** GAP-P-02 is the biggest lift — portal needs `app/(authenticated)/requests/page.tsx` (list, calls `/portal/requests`) + `app/(authenticated)/requests/[id]/page.tsx` (detail + 3 upload slots, calls existing presigned-upload endpoints) + `lib/nav-items.ts` entry. Also **Dev fix** GAP-P-01 (one-line path change in `home/page.tsx`) and **Dev fix** GAP-L-42 (mint magic-link token + re-template email URL to portal host) in the same cycle; they are independent fixes unblocking different parts of the Day-4 flow. Deferred: L-35/L-36/L-37/L-38/L-39/L-41 and P-03 (all LOW/MED, off critical path).
