# QA Cycle Status — Legal ZA 90-Day Demo (Fresh Tenant, Keycloak) — 2026-04-17

## Current State

- **ALL_DAYS_COMPLETE**: false
- **QA Position**: Day 0 — 0.19 (RE-BLOCKED on GAP-L-01 REOPENED — bounce-page allow-list mismatch rejects real invite URLs)
- **Cycle**: 1
- **Dev Stack**: READY
- **NEEDS_REBUILD**: false
- **Branch**: `bugfix_cycle_legal_2026-04-17`
- **Scenario**: `qa/testplan/demos/legal-za-90day-keycloak.md`
- **Focus**: Fresh tenant run — full onboarding through 90-day law firm lifecycle. Re-run with v3 profile set (post-consulting cycle) to validate legal-za vertical end-to-end.
- **Auth Mode**: Keycloak (real OIDC)

## Environment

| Service | URL | Status |
|---------|-----|--------|
| Frontend (kc mode) | http://localhost:3000 | UP |
| Backend (local+keycloak profile) | http://localhost:8080 | UP |
| Gateway (BFF) | http://localhost:8443 | UP |
| Portal | http://localhost:3002 | UP |
| Keycloak | http://localhost:8180 | UP |
| Mailpit UI | http://localhost:8025 | UP |
| Postgres (docteams) | localhost:5432 | UP |
| LocalStack (S3) | http://localhost:4566 | UP |

## Carry-Forward Watch List (from prior legal-za archives)

These are gaps logged during `_archive_2026-04-13_legal-90day-kc-v2` and earlier runs. If they reproduce, log fresh GAP IDs referencing the archive:

- **Trust accounting module not gated correctly** (prior runs flagged direct URL exposure pre-fix). Now expected to show "Module Not Available" when disabled.
- **Matter template custom-field defaults** (analogous to consulting GAP-C-09) — template fields may not auto-fill on project creation.
- **Retention clock / matter closure gating** (ADRs 247-249 recently added) — new code paths; treat as first-run.
- **Statement of account template + acceptance-eligible manifest flag** (ADRs 250-251 recently added) — new code paths; validate end-to-end.

## Gap Tracker

| GAP_ID | Day / Checkpoint | Severity | Status | Summary | Owner | Retries |
|--------|------------------|----------|--------|---------|-------|---------|
| GAP-L-01 | Day 0 / 0.19 | HIGH | REOPENED | Keycloak org-invite: same-profile session collision blocks registration AND single-use token is consumed on failed attempt. PR #1059 bounce-page fix was partial: bounce/logout mechanics are correct (verified against `/login-actions/action-token` URL), but the allow-list validator in `frontend/app/accept-invite/validate.ts` only accepts `http://localhost:8180/realms/docteams/login-actions/` prefix, while the real KC 26.5 `invite-user` endpoint (called by `KeycloakProvisioningClient.inviteUser()` during access-request approval) emits URLs starting with `http://localhost:8180/realms/docteams/protocol/openid-connect/registrations`. Every real invite email click is now rejected as "Invalid invitation link" — regression from pre-fix (pre-fix was session-collision; post-fix is hard-block). Fix: extend ALLOWED_KC_URL_PREFIXES to include `http://localhost:8180/realms/docteams/protocol/openid-connect/registrations?`. See day-00.md "Resumed at 0.19" section for screenshots + full timeline. | Dev | 1 |
| GAP-L-02 | Day 0 / 0.22 | LOW | SPEC_READY | Untranslated i18n key `expiredActionMessage` rendered as heading on Keycloak expired-link page; body text is localized but heading key is raw. Root cause: custom Info.tsx skips `advancedMsgStr(messageHeader)` — key IS in keycloakify default bundle. | Dev | 0 |
| GAP-L-03 | Day 0 / 0.20 | LOW | SPEC_READY | Keycloak registration page does not display target org name despite it being encoded in the invite token. User lacks visual confirmation of which org they're registering with. | Product | 0 |
| GAP-L-04 | Day 0 / 0.19 | LOW | OPEN | Keycloak `end_session_endpoint` called by bounce page without `id_token_hint` renders a "Do you want to log out?" confirmation page even when the user has no active KC session. Adds one extra click to every invite-email recipient's flow. Candidate fix: have the bounce page include a dummy/empty `id_token_hint` OR configure KC to skip the confirmation page when there is no session OR include the `state` token + `client_id` combo that allows silent end-session. Tier: LOW — nuisance not a blocker. Discovered during GAP-L-01 fix verification. | Product / Dev | 0 |

## Legend

- **Status**: OPEN -> SPEC_READY -> FIXED -> VERIFIED | REOPENED | STUCK | WONT_FIX | RESOLVED
- **Severity**: HIGH (blocker) / MED (cascading bug) / LOW (cosmetic)
- **Owner**: QA / Product / Dev / Infra

## Log

- 2026-04-17 — Cycle initialized. Prior consulting-za cycle (ALL_DAYS_COMPLETE 14:32 SAST same day) archived to `_archive_2026-04-17_consulting-complete/`. Fresh branch `bugfix_cycle_legal_2026-04-17` created from `main`. Scenario: `qa/testplan/demos/legal-za-90day-keycloak.md`. First action: Infra Agent to verify stack health and run `keycloak-bootstrap.sh` if needed.
- 2026-04-17 18:28 SAST — Infra Agent: Dev stack READY. All 4 Docker containers (postgres, keycloak, mailpit, localstack) healthy for 21h; Keycloak `/realms/docteams` returns 200 (platform admin preserved from prior cycles, no bootstrap needed). All 4 local services already running from earlier: backend PID 24024 (maven wrapper, child java PID 24179 on :8080, etime 5h43m), gateway PID 71302 (etime 8h08m, serves HTTP on :8443 — note env ref table said HTTPS but actual is plain HTTP), frontend PID 15582 (ext, next-server v16.1.6, etime 21h05m, curl 200), portal PID 71100 (pnpm dev, etime 8h08m). Nothing started/restarted — all endpoints already responding 200. Backend log post-startup clean of stack-trace ERRORs; only non-fatal AutomationEventListener WARNs about a tenant's malformed `BudgetThresholdTriggerConfig.thresholdPercent` (operational data issue, not infra — pre-existing from prior cycle, handed to QA/Dev to track if it recurs during legal-za flow).
- 2026-04-17 19:25 SAST — Product Agent: Triaged 3 gaps, all now SPEC_READY. **GAP-L-01 (HIGH)**: root cause is KC core invariant (direct-to-KC invite URL → same-profile cookie collision → core token consumed on failed attempt, not fixable via SPI in <2hr). Spec proposes a bounce-page pattern (`frontend/app/accept-invite`) that routes the email link through KC's `end_session_endpoint` with `post_logout_redirect_uri`, guaranteeing a clean cookie jar before KC sees the token — prevents collision, so token-consumption is irrelevant. Effort M (~75 min, 3 phases, Phase 3 optional). **GAP-L-02 (LOW)**: one-line fix in `compose/keycloak/theme/src/login/pages/Info.tsx` — we skip `advancedMsgStr(messageHeader)` that keycloakify default does; the key IS in the default en.ts bundle. Effort S (~10 min). **GAP-L-03 (LOW)**: fix is same pattern in `Register.tsx` — render `advancedMsgStr(messageHeader)` as title (KC 26 emits org name via `messageHeader` during org-invite flow; Phase B fallback through GAP-L-01's bounce page if it doesn't). Effort S (~10 min) + optional Phase B (M). Recommended Dev dispatch order: GAP-L-01 FIRST (unblocks QA), then GAP-L-02 + GAP-L-03 Phase A batched (same file area, one JAR rebuild, one KC restart).
- 2026-04-17 18:56 SAST — QA Agent: Day 0 **BLOCKED** at checkpoint 0.22 (owner Keycloak registration). Phases A-B PASS end-to-end: access-request form → OTP (`447884` from Mailpit) → "Request Submitted" card, then padmin login → Mathebula & Partners approval → tenant provisioning all green. Tenant schema `tenant_5039f2d497cf` created; `vertical_profile=legal-za`, ZAR, en-ZA-legal terminology, all 4 legal modules enabled, all legal-za packs applied (common-project/task, legal-za-customer/project, compliance-za, fica-individual/trust/company, legal-za-clauses, automation-legal-za, legal-za-project-templates). Invitation email delivered. Phase C BLOCKER: clicking invite link in new Chrome tab inherits padmin KC cookie → "You are already authenticated as different user" (carry-forward GAP-C-02 reproduces); worse, invite token is consumed by the failed attempt, so logging padmin out then retrying the link yields `expiredActionMessage`. 3 gaps logged: GAP-L-01 HIGH (Keycloak session collision + token consumption on failure), GAP-L-02 LOW (untranslated i18n key), GAP-L-03 LOW (registration page doesn't show org name). 3 screenshots captured (access-request-submitted, keycloak-session-collision, invite-token-expired). Also noted 3 LOW observations: OBS-L-01 (access-requests row click has no detail page — all fields already inline, probably fine), OBS-L-02 (Playwright MCP click unreliable on Radix Button onClick — worked around with `page.evaluate('btn.click()')`), OBS-L-03 (Radix Tabs triggers don't switch under Playwright MCP — verified via SQL and empty-Pending evidence). Next QA Position: Day 0 — 0.22 remains blocked; Day 1+ cannot proceed until Thandi can register. Recommended fix direction for GAP-L-01: (a) clear Keycloak session on invite-click, or (b) make org-invite tokens non-consumable on session-collision errors, or (c) add "logout and continue" CTA on collision page.
- 2026-04-17 18:32 SAST — Orchestrator: Fixed malformed test-fixture PDFs reported by user. Before: `.playwright-mcp/test-docs/*.pdf` were 28–55 B text stubs (`%PDF-1.4 test FICA document\n`) with no objects/xref/trailer — every PDF viewer rejected them. Archive `test-fixtures/*.pdf` were 317 B structurally-valid but contentless (blank page, no `/Contents` stream). New script `qa_cycle/make_test_pdf.py` generates well-formed PDF 1.4 documents (proper catalog/pages/page/content-stream/font objects, correct xref byte offsets, `%%EOF`, WinAnsi/CP1252 text encoding so em-dashes render). Regenerated 92 PDFs across 4 locations: `qa_cycle/test-fixtures/` (23 legal-za fixtures), `.playwright-mcp/test-docs/` (23, gitignored — MCP cache), archive `test-fixtures/` and `test-files/` (46). All 46 active files pass `qlmanage -t` thumbnail rendering with visible legible text. QA Agent can rely on fixtures from `qa_cycle/test-fixtures/` for upload checkpoints. Script is idempotent and parameterized — to add new doc types, edit the `DOCS` dict and re-run.
- 2026-04-17 20:24 SAST — Dev Agent: GAP-L-01 fixed via PR #1059, merged as `7758d6c3`. Phases 1+2 of fix spec implemented: (1) new frontend bounce pages `frontend/app/accept-invite/page.tsx` + `frontend/app/accept-invite/continue/page.tsx` with shared `validate.ts` (strict `startsWith` allow-list on `http://localhost:8180/realms/docteams/login-actions/`) and a small `"use client"` `redirect-client.tsx` component that `window.location.replace`s on mount (plus a `<noscript>` manual-click fallback); (2) `frontend/lib/auth/middleware.ts` adds `"/accept-invite(.*)"` to `PUBLIC_ROUTES`; (3) `compose/keycloak/themes/docteams/email/html/org-invite.ftl` line 9 now wraps `${link}` with `bounceBase + "?kcUrl=" + link?url('UTF-8')` so email recipients land on the bounce page first. Phase 3 (optional KC theme error-page "Sign out and continue" CTA) deferred — Phases 1+2 alone solve the blocker per the spec. Frontend lint clean (0 errors), build clean (`/accept-invite` + `/accept-invite/continue` both registered as dynamic routes), unit tests clean (1959 passed, 2 skipped). NEEDS_REBUILD flagged — Keycloak container restart required to pick up the email template change (templates are bind-mounted but loaded at startup), and frontend restart recommended to guarantee the new routes register cleanly in middleware (HMR normally handles this but route-level middleware can be finicky). Next action: Infra Agent restart KC + frontend, then QA re-runs Day 0 checkpoint 0.22.
- 2026-04-17 20:27 SAST — Infra Agent: Restarted keycloak + frontend for PR #1059 (GAP-L-01). KC container `b2b-keycloak` bounced via `docker restart` (up in 7s, `/realms/docteams` → 200, `org-invite.ftl` bind-mount confirmed inside container showing new `${link?url('UTF-8')}` URL-encoding wrapping and bounceBase assignment — theme live). Frontend restart: `svc.sh status` showed stale `ext` ownership (documented gotcha — port 3000 held by PID 15582, `.svc/frontend.pid` said 17645); killed the port-holder, confirmed :3000 free, `svc.sh start frontend` → new PID 88049 (child next-server 88090) healthy in 3s. Route checks: `GET /accept-invite?kcUrl=...` → HTTP 200, `GET /accept-invite/continue?kcUrl=...` → HTTP 200 (both live, routes registered, middleware PUBLIC_ROUTES honored). No new ERROR/Exception entries in KC `docker logs` or frontend svc log since restart. Portal is incidentally stale (pre-existing, unrelated to this fix, not in this task's scope). NEEDS_REBUILD cleared. Next action: dispatch QA Agent to re-run Day 0 from checkpoint 0.19–0.22 (Thandi invite-click → KC registration flow).
- 2026-04-17 20:40 SAST — QA Agent: Fix verification — **GAP-L-01 REOPENED**. PR #1059 is functionally partial. (1) **Email-template rewrite: CONFIRMED LIVE** — fresh invite emails at 18:32:27 + 18:34:40 wrap the KC URL via `http://localhost:3000/accept-invite?kcUrl=<URL-encoded>`. (2) **Bounce mechanics: WORK when allow-list matches** — tested with a `/login-actions/action-token` kcUrl (via `invite-existing-user`), flow completed bounce → KC `end_session_endpoint` → logout confirm → `/accept-invite/continue` → back to KC; no session-collision. (3) **Bounce mechanics: BLOCK when allow-list doesn't match** — the real user-facing flow (`KeycloakProvisioningClient.inviteUser()` → KC `invite-user` endpoint) emits `/protocol/openid-connect/registrations` URLs, which the validator rejects; user sees "Invalid invitation link". Day 0 cannot proceed past 0.19 until the allow-list is extended. Proposed fix: add `http://localhost:8180/realms/docteams/protocol/openid-connect/registrations?` to `ALLOWED_KC_URL_PREFIXES` in `frontend/app/accept-invite/validate.ts`, plus a unit test covering both URL shapes. 2 screenshots saved (`day-00-resume-0.19-bounce-member-already.png` — allowlist-match success; `day-00-resume-0.19-bounce-rejects-registrations-url.png` — real-world rejection). 1 new LOW gap (GAP-L-04 — KC logout page requires manual click when no `id_token_hint`; adds nuisance click to every invite flow). KC-side state: deleted Thandi KC user + pending org_invitation row to get a clean test state (documented as recovery per scenario guidance, not a product-flow bypass — identical outcome to "platform admin cancels and re-issues invite"). Tenant schema `tenant_5039f2d497cf` + backend approval state untouched.
