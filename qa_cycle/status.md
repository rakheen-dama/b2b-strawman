# QA Cycle Status — Legal ZA Full Lifecycle (VERIFY CYCLE, Keycloak) — 2026-04-24

## Current State

**Purpose**: Re-validate that the 40+ fixes merged to `main` between 2026-04-21 and 2026-04-24 actually work end-to-end on the Keycloak dev stack. Prior cycle state archived to `qa_cycle/_archive_2026-04-24_pre-verify/status.md`.

- **ALL_DAYS_COMPLETE**: false
- **QA Position**: **Day 85 PARTIAL COMPLETE; Day 88 next.** Day 85 cycle-1 verify 2026-04-25 22:55-23:05 SAST (~10 min): 85.1 PASS (closure letter `matter-closure-letter-dlamini-v-road-accident-fund-2026-04-25.pdf` 1.6 KB attached + downloadable on Documents tab; second `-2026-04-26.pdf` exists from re-close cycle); 85.2 N/A (no additional firm-side correspondence required by tenant workflow beyond closure letter); 85.3 PASS at DB (`projects.retention_clock_started_at=2026-04-25 20:44:20`, computed `end_date=2031-04-24 = clock_start + 1825 days` per ADR-249) but FAIL at UI surface — no per-matter retention card on matter Overview/header (only global `retention_policies` table at `/settings/data-protection` shows CUSTOMER 1825d + AUDIT_EVENT 2555d); reconfirms pre-existing OBS-Day60-RetentionShape Sprint-2 followup. 85.4 PARTIAL — matter Activity tab renders full firm-side audit feed with type filters (All/Tasks/Documents/Comments/Members/Time) but **NO actor filter UI**; portal-actor write events not emitted to `audit_events` (DB confirms only 2 PORTAL_CONTACT rows tenant-wide — `acceptance.viewed/accepted` from Day 8 — Sipho's FICA upload, fee-note pay, doc downloads, info-req submits all MISSING). NEW **GAP-L-75 HIGH** opened — cross-actor audit log surface missing (Phase 69 page not shipped + matter Activity actor filter missing + portal-contact write events not persisted). Suggested fix: Phase 69 = M-L (~1 sprint, net-new); portal audit emission = S-M (~3-4 hr, extend `PortalDocumentService` + `PortalInvoiceService` + `PortalTrustService` + `PortalInformationRequestService`). 0 console errors on matter Documents/Activity/Overview + Data Protection. **Demo impact**: directly blocks exit checkpoint **E.14** (audit-trail completeness — both firm-user + portal-contact events over 90 days). Day 88 (firm + portal activity-feed wow moment) likely BLOCKS on portal `/activity` surface absence (= OBS-Day75-NoPortalActivityTrail) + portal-actor `audit_events` emission gap (= GAP-L-75c). Original prior position (preserved): **Day 75 PARTIAL COMPLETE — late-cycle isolation HOLDS (75.6 a/b/c PASS); weekly digest sub-flow (75.1-75.4) DEFERRED on environmental limitation (no manual trigger for cron-only `PortalDigestScheduler`); activity-trail surface (75.5) absent — defer to Day 88. Day 85 next (firm final closure paperwork).** Day 75 cycle-1 verify 2026-04-25 22:48-23:02 SAST (~12 min): magic-link self-service Sipho portal session → `/home` (no Moroka entries, only Sipho invoices INV-0001/INV-0002), `/trust` (R 0,00 balance + 3 transactions all RAF-2026-001 — REFUND R 70k, 2 DEPOSITs R 50k+R 20k; zero R 25,000 Moroka leak), `/projects` (3 matters all customer=Sipho Dlamini DB-confirmed: Dlamini RAF CLOSED + 2 L-37 regression-test probes — NO Moroka cross-link), Dlamini matter-detail header status badge **CLOSED** (L-73 still working). Weekly digest path: `PortalDigestScheduler` cron `0 0 8 ? * MON`, today is Sun 2026-04-26 00:48 SAST so next fire ~31h away; **no manual REST/admin trigger exists** (`grep -r "runWeeklyDigest" backend/src/main` confirms only the @Scheduled cron entry point); Mailpit search "weekly update" → 0 hits. Defensible by code path (per-`PortalContact.id` assembler is tenant-scoped; cross-contact leak would require Hibernate filter bypass). Four NEW informational observations (none HARD blockers, all Sprint 2): **OBS-Day75-NoManualDigestTrigger** (~30 min S-fix: expose `/internal/portal/digest/run-weekly` admin endpoint), **OBS-Day75-NoPortalActivityTrail** (no `/activity` surface — expected to gap on Day 88.3-88.5), **OBS-Day75-NoPastTabForClosedMatters** (already triaged as L-73-followup), **OBS-Day75-PortalTrustMovementsEndpoint404** (LOW cosmetic — Home tile fetches missing endpoint, degrades gracefully). 0 console errors on portal pages other than the pre-existing trust-movements 404. Wall-clock ~12 min, well under 45 min target. Original prior position (preserved): **Day 61 COMPLETE — GAP-L-73 + GAP-L-74 VERIFIED end-to-end. Day 75 next (weekly digest + late-cycle isolation spot-check).** Day 61 re-walk 2026-04-25 ~22:30-22:38 SAST: backfill via re-generate SoA (new `documents.8e9eff16-… visibility=SHARED`) + reopen→re-close matter (new closure letter `documents.6e2ea5ee-… visibility=SHARED`, auto-flipped from INTERNAL by L-74b code path); `portal_projects.status='CLOSED'` confirmed by L-73 fix path; Sipho self-service magic-link → portal matter detail header shows status badge **CLOSED** (was ACTIVE pre-fix) → Documents tab lists both new PDFs alongside FICA uploads → Download SoA + closure letter both fire LocalStack S3 presigned URLs returning HTTP 200 with byte-sizes 4110 + 1643 matching DB exactly. Scenario 61.1-61.9 all PASS (61.1 via L-72 magic-link workaround per deferral; 61.4 content reconciliation note: SoA Fees/Trust loops still empty per pre-existing OBS-Day60-SoA-Fees/Trust-Empty Sprint 2 followup; 61.9 portal-side audit not emitted — new informational OBS-Day61-NoPortalDocAuditEvent, deferred to Sprint 2). 0 console errors. **L-73 + L-74 → VERIFIED.** Original prior position (preserved): **Day 61 BLOCKED on GAP-L-74 (SPEC_READY 2026-04-26 — Dev next).** Product triage 2026-04-26 SAST: GAP-L-74 → SPEC_READY (fix-in-cycle, M ~3 hr, 2-part backend fix at `qa_cycle/fix-specs/GAP-L-74.md` — `StatementService.generate` persists paired `Document` row + `MatterClosureService.generateClosureLetterSafely` flips closure letter to SHARED); GAP-L-73 → SPEC_READY (fix-in-cycle, S ~1.5 hr, spec at `qa_cycle/fix-specs/GAP-L-73.md` — extend `MatterClosureNotificationHandler` to sync portal projection status); GAP-L-72 → DEFERRED to Sprint 2 as L-72-followup (workaround proven; magic-link self-service path authorised by scenario; no demo flow gates). After Dev merges L-74 + L-73 and Infra restarts backend, QA re-walks Day 61 from 61.1 (magic-link self-service per L-72 deferral) → 61.8 (closure letter visibility) → 61.9 (firm-side audit log of portal access). Day 88 next AFTER Day 61 verify clean. Original prior position (preserved): **Day 60 SoA re-gen COMPLETE — GAP-L-71 VERIFIED. Day 61 (Sipho portal SoA download + review) BLOCKED on NEW GAP-L-74 (closure-pack documents not surfaced to portal). Day 88 next AFTER L-74 triage.** Cycle-1 SoA re-gen 2026-04-25 ~21:39 SAST: Thandi → closed Dlamini matter `e788a51b-…` toolbar `Generate Statement of Account` → Preview & Save → backend 200, preview iframe rendered, no ClassCastException, backend log `2026-04-25T21:39:48.262 StatementService — Generated Statement of Account: project=e788a51b-…, generatedDoc=c0931e79-…` (4 109-byte PDF persisted to `generated_documents`, downloadable via Statements tab). **GAP-L-71 → VERIFIED**. Day 61 (~21:42 SAST): Sipho self-service magic-link → portal `/projects/e788a51b-…` matter detail → Documents tab lists ONLY 6 FICA upload duplicates; closure letter (`documents.2bad9b06-…`) and SoA (`generated_documents.c0931e79-…` `document_id=NULL`) BOTH MISSING from portal Documents tab → Sipho cannot download/reconcile SoA per scenario 61.2-61.8. **NEW GAP-L-74 HIGH/BLOCKER** (closure-pack docs not surfaced to portal: SoA structurally orphaned with `document_id=NULL`; closure letter has `documents` row but portal API filters it out — likely visibility/scope filter). Two side-gaps: **GAP-L-73 LOW** (portal matter status badge shows ACTIVE for closed matter — cosmetic, projects.status flip not picked up by portal API/frontend); **GAP-L-72 MED** (no notification email to portal contact on closure or SoA generation — only trust-activity hook fires; closure + statement events do not enqueue email-notifications). Two informational obs from L-71 SoA preview content (NOT new gaps — separate from renderer-crash scope): **OBS-Day60-SoA-NotInDocumentsTable** (the structural cause of L-74 — SoA never gets a `documents` row), **OBS-Day60-SoA-Fees/Trust-Empty** (rendered SoA preview body has empty fee+trust loops despite real ledger data; disbursements loop populates correctly). Side-evidence — portal Trust ledger and portal Invoices both render Sipho's data correctly with no leakage, so portal data-pipe is healthy generally; this is a Documents-surface filter+structural gap specifically. 0 console errors throughout. Wall-clock ~8 min total, well under 75 min budget. Original prior position (preserved): **Day 60 COMPLETE — matter CLOSED. Day 61 (Statement of Account portal-side review) BLOCKED on NEW GAP-L-71 (SoA generation backend ClassCastException).** PRE-FLIGHT-C re-walk per L-69 fix-spec verification path executed end-to-end browser-driven 2026-04-25 ~20:35-20:46 SAST: Bob reversed both dirty rows (`f9f3f7a4` FEE_TRANSFER + `f215423c` REFUND, both `project_id=NULL`) → ledger card returned to R 70 000 → Bob attempted re-record fee transfer R 1 250 against INV-0001 → **BLOCKED by NEW GAP-L-70** (invoice still PAID from original FEE_TRANSFER's auto-flip, reversal does NOT cascade to invoice status). Recovery: skip Fee Transfer (no unpaid invoices remain), record FULL R 70 000 REFUND with new Matter (Optional) field via the L-69-fixed dialog → DB confirms `project_id=e788a51b-…` populated → Thandi approved → matter gate calc returned **R 0,00**. **Closure-gate dialog: ALL 9 GATES GREEN ✅** (L-69 verify exit checkpoint passed). CLOSURE-EXECUTE: Continue → reason CONCLUDED + notes + Generate closure letter checkbox → Confirm Close → matter ACTIVE→**CLOSED** at `2026-04-25 20:44:20`, retention_clock_started_at populated (per ADR-249), `matter_closure_log` row inserted with full notes, closure letter PDF `matter-closure-letter-dlamini-v-road-accident-fund-2026-04-25.pdf` (`generated_documents.a48e1bdb-…` → `documents.2bad9b06-…`) generated and attached. SoA generation FAILED with NEW GAP-L-71 (`ClassCastException: DisbursementStatementDto cannot be cast to Map` at `TiptapRenderer.renderLoopTable:309`). **GAP-L-69 → VERIFIED** (REFUND path proven; FEE_TRANSFER backend code in place but not exercise-able due to L-70). NEW: GAP-L-70 (FEE_TRANSFER reversal doesn't cascade invoice status; HIGH for verify-cycle re-walks, MED for prod). NEW: GAP-L-71 (SoA backend ClassCastException; HIGH/BLOCKER for E.13 SoA exit + Day 61 portal download). 0 console errors. **Original prior position (preserved):** GAP-L-69 → SPEC_READY (Product triage 2026-04-25 SAST; spec at `qa_cycle/fix-specs/GAP-L-69.md`; Dev next). Hybrid fix chosen: backend infers `projectId` for FEE_TRANSFER from `InvoiceLine`, REFUND gains optional `projectId` on the request DTO + a Matter UUID field on the dialog (mirrors `RecordDepositDialog`). Est S-M (~1.5-2 hr). NEEDS_REBUILD=true (backend Java change). After Dev lands, QA re-walks PRE-FLIGHT-C per spec verification path (UI-driven reverse of dirty rows `f9f3f7a4-…`+`f215423c-…` → re-record with fix → 9-GREEN gate → CLOSURE-EXECUTE). Original prior position (preserved): **Day 60 PRE-FLIGHT-A+B complete; PRE-FLIGHT-C HALTED on NEW HIGH GAP-L-69 (Fee Transfer + Refund dialogs lack matter/project_id field).** Disposition was driven end-to-end browser-driven: Bob recorded Fee Transfer R 1 250 → INV-0001 (sidesteps the recorder-sole-approver rule because only Thandi has APPROVE_TRUST_PAYMENT) → Thandi approved → INV-0001 SENT→PAID + Sipho ledger R 70 000→R 68 750. Then Bob recorded Refund R 68 750 → Thandi approved → Sipho ledger R 68 750→**R 0,00**. Matter Trust tab card shows `R 0,00 / No Funds`. **However the closure-gate dialog STILL renders trust gate RED at "R 70 000,00"** because the per-matter gate computation (`TrustBalanceZeroGate.calculateBalanceByProjectId`) sums over `trust_transactions WHERE project_id = :projectId`, but the FEE_TRANSFER and REFUND rows have `project_id = NULL` — the dialogs (`RecordFeeTransferDialog`, `RecordRefundDialog`) don't capture a Matter field, so the per-matter view never sees the disposition. Customer ledger card (customer-scoped) reconciles correctly to R 0; per-matter gate (project-scoped) cannot be cleared via UI. **GAP-L-69 OPENED (HIGH/BLOCKER for matter closure)** — fix M (~3-4 hr): add `Matter` (project_id) field to both dialogs + auto-fill from matter-Trust-tab context + optionally derive from invoice when `invoiceId` provided. PRE-FLIGHT-C HALTED per dispatch hard rule "If a UI affordance is missing, log gap and exit — don't power through with REST or SQL." Closing gate state: 8 GREEN, 1 RED (trust). 0 console errors. **Side-finding** — Single-owner firms cannot approve any trust transaction they record (Thandi is the only `APPROVE_TRUST_PAYMENT` holder; combined with "recorder cannot be sole approver" rule = no path forward without dual approval enabled or admin role grant). Worked around by having Bob (admin/MANAGE_TRUST) record + Thandi approve. Logged as observation, not separate gap, since Mathebula has both roles available. Original prior position (preserved): Day 60 PRE-FLIGHT-A complete; PRE-FLIGHT-B already complete (info requests gate GREEN). Closure-gate dialog re-walked at 2026-04-25 ~21:21 SAST: 8 of 9 gates GREEN (court dates ✅, tasks ✅, info requests ✅, disbursements approved/settled ✅, final bill ✅, prescription timers ✅, document acceptances ✅), only 1 RED gate remaining: `Matter trust balance is R70000.00. Transfer to client or office before closure.` (Fix-this deep-link → `?tab=trust`). DB confirms: `court_dates.d4cd7dcd-… status=CANCELLED`, `tasks WHERE project_id='e788a51b-…' GROUP BY status → CANCELLED 9 (zero non-terminal)`, `information_requests` 6 rows all CANCELLED/COMPLETED. Dispatch's expected "4 RED gates" intel was stale — prior timed-out agent had also driven info-request closure through UI before timing out. Recommend orchestrator skip PRE-FLIGHT-B and dispatch PRE-FLIGHT-C directly. Evidence: `qa_cycle/checkpoint-results/day-60.md` + 5 PNG snapshots (`day-60-cycle1-court-date-closed.png`, `day-60-cycle1-tasks-closed.png`, `day-60-cycle1-gate-court-date-clear.png`, `day-60-cycle1-gate-tasks-clear.png`, `day-60-cycle1-gates-after-A.png`). 0 console errors. No new gaps opened (informational `OBS-Day60-TasksAllFilterHidesCancelled` only — cosmetic). **Original prior position (preserved):** Day 46 cycle-1 verify COMPLETE; 5/5 substantive PASS + 1 PARTIAL (46.7 home-tile pending count semantics — design-intent question, not regression) + 0 BLOCKER. Sipho self-served magic-link (token `d-1Edcm_PHwdklk9Lb3HKTJqvmUjHwEGCnDZawr6iew`); REQ-0007 walked SENT → IN_PROGRESS with all 3 items SUBMITTED (3 portal-uploaded documents `4d8e6125-… / 68d9b68e-… / 8ca47203-…`). Portal `/trust/{RAF-matter}` shows balance **R 70 000,00** with both deposits + correct running balance — **L-52 VERIFIED for second deposit (DEP-2026-002)**. Late-cycle isolation HOLDS at all 3 layers: list-render (no Moroka in projects/requests/trust), direct-URL probe (`/trust/{moroka-matter}` renders "No trust balance" with 404 backend), API hard-negative (backend rejects Sipho JWT against Moroka resources). NEW gap **GAP-L-68 LOW** opened — portal Home "Last trust movement" tile shows "No recent activity" because backend lacks `GET /portal/trust/movements` endpoint that frontend `home/page.tsx:239` calls (404). Frontend-vs-backend route mismatch; cosmetic only; suggested fix S (~1 hr). L-43 + L-47 portal listener + parent-status sync re-VERIFIED. **Original prior position (preserved):** Day 45 cycle-1 verify COMPLETE; 5/5 substantive PASS + 1 PARTIAL (45.1 substituted FICA template for free-form due to NEW GAP-L-67); 0 BLOCKER. New trust deposit `ce6767c1-…` R 20 000 RECORDED on Sipho/RAF (DEP-2026-002); Sipho client_ledger_card now R 70 000,00; Trust Accounting overview R 95 000,00. NEW gap **GAP-L-67 HIGH** opened — no UI affordance to add items to ad-hoc DRAFT info requests AND no Send button on 0-item drafts; backend `POST /api/information-requests/{id}/items` exists but frontend has zero callers; means free-form info requests can't be created via firm UI today (must use a pack template). NEW gap **GAP-L-66 LOW** logged from Day 30 side-finding (MockPaymentGateway uses Next.js host for backend-controller URL → Pay Now 404 on direct click). Magic-link L-42 fix HOLDS for REQ-0007 (port 3002, token, orgId all correct). MINOR-Trust-Nudge-Email-Missing carries forward unchanged (no nudge email after deposit). Original prior position (preserved): **Day 30 cycle-1 verify COMPLETE.** GAP-L-64 VERIFIED end-to-end browser-driven 2026-04-25 14:09 SAST via plugin Playwright tools (Option B — fresh fee-note from new R 100 Court Fees disbursement on Dlamini matter; INV-0002 walked Draft → Approved → Sent (payment_url populated) → portal Pay Now visible → Mock checkout Simulate-Successful → portal payment-success page → firm SENT→PAID with payment_events Created+Completed). Side-finding (NOT a Day 30 blocker, recommend new GAP-L-66 LOW): MockPaymentGateway uses `appBaseUrl=http://localhost:3000` but controller is on backend :8080 → direct Pay Now click 404s in the Next.js frontend. Tab 0 Bob firm + Tab 1+2 Sipho portal preserved. Original prior position:  PR #1134 (merge SHA `34493c79`) merged 2026-04-25 SAST. Backend restarted PID 41837, MockPaymentIntegrationSeeder confirmed seeded `tenant_5039f2d497cf.org_integrations` row PAYMENT/mock/enabled (id `9775a954-…`). DB pre-state re-confirmed this turn (read-only SELECT): INV-0001 still SENT/NULL payment_url (created pre-seeder); seeded mock row intact. Next browser-available turn drives Option A (firm-side regenerate) or Option B (fresh fee-note) before re-walking 30.1/30.5/30.6/30.7. **GAP-L-64 → SPEC_READY (fix-in-cycle, dev `MockPaymentGateway` per scenario step 0.G stub-path + email portalUrl fix; full spec at `qa_cycle/fix-specs/GAP-L-64.md`)**. **GAP-L-65 → WONT_FIX (this verify cycle) / DEFERRED to Sprint 2** — exit gate E.9 only requires portal internal consistency (which holds), not firm↔portal seam. Day 30 phases 30.1 / 30.5 / 30.6 / 30.7 / 30.8 / 30.10 await re-walk after L-64 fix lands; 30.2 / 30.4 / 30.11 already PASS. **Day 45 cleared to proceed in parallel** (does not hard-depend on Day 30 PAID; firm-side flow). Original triage context: **NEW HIGH GAP-L-64 opened**: Mathebula has zero `org_integrations` rows → `IntegrationRegistry.resolve(PAYMENT)` falls back to `NoOpPaymentGateway` → invoice `payment_url=NULL` → portal renders "Contact Mathebula & Partners to arrange payment" (no Pay button) AND email "View Invoice" CTA has empty `href=""`. Per scenario hard rule, did NOT fake payment via SQL/REST. Two fixable surfaces: (a) seed PayFast sandbox `OrgIntegration` on legal-za pack install; (b) email "View Invoice" link should always render the portal URL even when no PSP configured. Scenario step 0.G acknowledged this PSP prerequisite. Also opened **LOW GAP-L-65** — portal terminology override does not propagate ("Invoices"/"Back to invoices" on portal vs "Fee Note"/"fee notes" on firm; email subject "Invoice INV-0001..."). Day 30 PASS items: 30.2 portal invoice detail renders correctly (Sheriff Fees line, R 1 250,00 totals), 30.4 screenshot captured, 30.11 Sipho portal isolation holds (only INV-0001 visible). DB final state: invoice still SENT, 0 payment_events. Tab 0 Bob firm + Tab 1 Sipho portal both preserved (fresh magic-link issued via portal `/login` self-service). Day 28 cycle-1 verify COMPLETE; **GAP-L-62 + GAP-L-63 BOTH VERIFIED end-to-end browser-driven.** L-63: Sheriff Fees R 1 250 disbursement surfaces in fee-note dialog under "Disbursements" section, auto-checked, total R 1 250,00. L-62: soft-warn at draft (amber banner "Tax number missing — This draft can be saved, but the invoice cannot be sent until a tax number is added", button "Create Draft (1 issues)" still saves) + hard-enforce at send (CRITICAL validation panel "Tax Number is required to send an invoice" with admin Send Anyway override + Cancel; Send blocked without override). Happy-path: set tax_number='8501015800088' via Edit Customer → re-Send → status flipped APPROVED→**SENT**, invoice_number=**INV-0001**. DB confirms `legal_disbursements.bb9ee2ac-…` flipped `billing_status` UNBILLED→**BILLED** with `invoice_line_id=326fb6c5-…` linked to new invoice `8f718728-5fb6-40fe-abf1-2cafc86c0f10`. Also L-60 (PROSPECT-lifecycle gate on CREATE_INVOICE) HOLDS — draft created against PROSPECT customer without lifecycle gate firing.
- **Cycle**: 1 (verify)
- **Dev Stack**: READY — backend PID 41837 (V112 + resolver overload + MockPaymentGateway live, restarted 2026-04-25 SAST post-PR #1134), portal PID 5677, frontend PID 5771, gateway external PID 71426.
- **NEEDS_REBUILD**: false
- **Branch**: `bugfix_cycle_2026-04-24`
- **Scenario**: `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`
- **Auth Mode**: Keycloak (real OIDC) for firm; magic-link + portal JWT for portal (`:3002`)
- **Next action**: **QA — Day 85 (firm final closure paperwork, audit log filters) per scenario** OR **Dev/Product — triage OBS-Day75-NoManualDigestTrigger** (S-fix to enable on-demand digest verification; otherwise Day 75 75.1-75.4 stay structurally-defensible-only until Monday cron fires naturally). Day 75 isolation invariant (75.6) verified PASS browser-driven. Scope for Day 85: `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md` Day 85 section (verify closure letter still attached, retention policy `end_date ≈ today + 5 years - 25 days`, audit log filter by matter/actor — Phase 50 + Phase 69 readiness). Original prior position (preserved): **QA — Day 75 (weekly digest + late-cycle isolation spot-check) per scenario.** Original prior position (preserved): **Dev — implement GAP-L-74 + GAP-L-73 per fix specs.** L-74 spec at `qa_cycle/fix-specs/GAP-L-74.md` (M ~3 hr, two-part backend: `StatementService.generate` paired Document persistence + `MatterClosureService.generateClosureLetterSafely` visibility flip to SHARED); L-73 spec at `qa_cycle/fix-specs/GAP-L-73.md` (S ~1.5 hr, single-file backend: extend `MatterClosureNotificationHandler` to sync portal projection status on close + reopen). Both touch the closure→portal seam — same code path, package together for one PR or two tightly-coupled PRs. After Dev merges + Infra restarts backend, QA re-walks Day 61 from 61.1 (magic-link self-service per L-72 deferral) → 61.2 portal Documents tab list with SoA + closure letter visible → 61.3 download cleanly → 61.4 PDF reconciliation → 61.7 title check → 61.8 closure letter visibility → 61.9 firm-side audit log. Existing closure letter `documents.2bad9b06-…` will need a one-time backfill to `visibility=SHARED` (firm-side toggle UI or SQL); fresh SoA regen (existing `generated_documents.c0931e79-…` was created pre-fix with `document_id=NULL` — won't appear on portal even after fix; regenerate to exercise new path). Original prior position (preserved): **Day 60 cycle-1 verify dispatch CLEARED — firm matter closure + generate Statement of Account (actor Thandi on Tab 0 firm).** Product triage 2026-04-25 SAST resolved both blocking questions: (i) **GAP-L-67 DEFERRED to Sprint 2 as L-67-followup** (workaround verified through Day 46; L-67 doesn't gate Day 60+); (ii) **Day 60.5 INV-0001 question — Option C primary (accept INV-0001 as documented pre-seeder edge case carry-forward) with Option B fallback (firm-side Record Payment on INV-0001 if closure-gate UI hard-blocks)**. Day 60 dispatch instruction: attempt closure dialog first; if Day 60.5 gate hard-blocks on INV-0001 outstanding, invoke `Record Payment` on INV-0001 detail page with reference `EFT-OFFLINE-INV-0001` (legitimate firm workflow, doesn't require `payment_url`). Either path is scenario-compliant. Day 46 cycle-1 verify **COMPLETE** — REQ-0007 closed by Sipho (3/3 SUBMITTED), L-52 second-deposit propagation VERIFIED, isolation HOLDS, GAP-L-68 LOW opened (Home tile endpoint mismatch). Day 30 cycle-1 verify previously COMPLETE — GAP-L-64 VERIFIED end-to-end via INV-0002 (R 100 court-fee fresh fee-note path). Day 46 cycle-1 verify **COMPLETE** — REQ-0007 closed by Sipho (3/3 SUBMITTED), L-52 second-deposit propagation VERIFIED, isolation HOLDS, GAP-L-68 LOW opened (Home tile endpoint mismatch). Day 30 cycle-1 verify previously COMPLETE — GAP-L-64 VERIFIED end-to-end. Day 28 cycle-1 verify COMPLETE — L-62 + L-63 both VERIFIED. Day 28 cycle-1 verify **COMPLETE** — L-62 + L-63 both VERIFIED end-to-end browser-driven (PR #1134 unblocks Day 30 chain). Day 21 cycle-1 verify **COMPLETE** — disbursement create + L-57 + L-61 all VERIFIED browser-driven. GAP-L-61 FIXED via PR #1133 and VERIFIED 2026-04-25 11:57 UTC end-to-end (browser-driven submit + approve, DB confirms APPROVED+UNBILLED, approver Bob Ndlovu). Day 15 cycle-1 verify COMPLETE — Sipho portal isolation HOLDS at all four layers (UI list, UI direct-URL, API, audit). 18/18 PASS, 0 BLOCKER. Method: Playwright MCP browser-driven for 15.1+15.2 (4 list pages + 4 direct-URL probes — 0 Moroka tokens visible anywhere); REST curl for 15.3 (API as SUT — 8 hard-negatives 404/401, plus positive control 200, plus 3 list-endpoint scans Moroka-clean); read-only SQL SELECT for 15.4 (zero Sipho-actor audit rows against Moroka entities). Sipho JWT extracted from Tab 1 `localStorage.portal_jwt` (`sub=c3ad51f5-…/type=customer/org_id=mathebula-partners`). L-52 portal trust ledger does NOT leak Moroka R 25 000,00 — Sipho sees only his own R 50 000,00. Tab 0 (Bob firm) and Tab 1 (Sipho portal) preserved. No new gaps. Day 14 cycle-1 verify COMPLETE — Moroka Family Trust onboarded firm-side (TRUST type, EST-2026-002 deceased estate matter, REQ-0005 Liquidation pack SENT, 1 doc uploaded, R 25,000 trust deposit RECORDED). 11/11 PASS, 0 BLOCKER. Method: **Playwright MCP browser-driven** — actor Bob (admin) on Tab 0; Tab 1 Sipho portal session preserved untouched for Day 15 reuse. Verify-focus this day: L-32, L-34 (TRUST type too), L-37 customer+project narrowing, L-39 propagation, L-41, L-53 all VERIFIED; L-37-regression V112 fix HOLDS (no conveyancing pack on deceased-estate matter). Pending UI re-walks still carried forward: L-48 matter-level "+ New EL" CTA placement, L-58 Overview "Upcoming Deadlines" tile.

## Verification Focus — fixes to re-check as QA proceeds

These gaps were **FIXED and merged to main** since 2026-04-21. QA should re-verify them during the lifecycle run (not as separate probes — just notice whether the underlying functionality works during the normal flow). If any of these REOPEN, log as regression.

### Shipped in the 2026-04-21 cycle PR #1114 (bundled; verified end-to-end up to Day 28.3 before merge to main)
- L-23 (settings general 500 error), L-33 (FICA onboarding pack), L-34 (portal-contact auto-provision), L-42 (info-request magic-link), L-43 (portal request-item submitted listener), L-50 (acceptance email portal base URL), L-56 (time-entry PROSPECT gate), L-57 (disbursement Matter combobox), L-60 (invoice PROSPECT gate), P-01 (portal InfoRequestsCard path), P-02 (portal requests UI route), P-06 (portal acceptance status handling)

### Shipped post-cycle-merge (2026-04-23 → 2026-04-24) — NEW functionality / reopened code paths
- **L-21** — Access-request detail: WONT_FIX (PR #1128, scenario rescope, no code).
- **L-22** — BFF session handoff cleaner middleware (PR #1125). Verify: after Thandi registers while padmin session is live, Thandi lands clean without padmin sidebar leak.
- **L-25** — SECTION_86 trust account type (PR #1123). Verify: Day 1 Trust Account dialog offers SECTION_86 option.
- **L-27** — Tax rate label "VAT — Standard" + `tax_label='VAT'` on legal-za (PR #1124).
- **L-28** — Conflict-check self-exclusion (PR #1118). Verify: Day 2 conflict check on newly-created client returns NO_CONFLICT (not CONFLICT_FOUND self-match).
- **L-29** — Conflict-check form dropdowns hydrated (PR #1122).
- **L-31** — Customers empty-state vertical copy (PR #1122).
- **L-32** — Create Client redirects to new detail (PR #1122).
- **L-35** — Custom-field update on PROSPECT customers allowed (PR #1119). Verify: Day 3 matter Save Custom Fields on PROSPECT customer returns 200.
- **L-37** — Field-group over-attach narrowed (PR #1122).
- **L-38** — Matter detail tab cleanup (PR #1122).
- **L-39** — customerId URL-param propagation to Configure step (PR #1122).
- **L-41** — Information request due_date column + picker (PR #1123).
- **L-44** — PackReconciliationRunner syncs enabled_modules + enabled_features (PR #1124).
- **L-45** — Per-item Download on firm info-request detail (PR #1122).
- **L-46** — FICA status tile on matter Overview (PR #1127).
- **L-47** — Portal parent-request status sync (PR #1124).
- **L-48** — Matter-level "+ New Engagement Letter" CTA + `/api/customers` 404 fix (PR #1127).
- **L-51** — "Send for Acceptance" email subject keywords (PR #1124).
- **L-52** — Portal trust-ledger sync for RECORDED deposits (PR #1117). Verify: Day 11 Sipho portal `/trust` shows the Day-10 deposit (R 50 000).
- **L-53** — Liquidation & Distribution request pack (PR #1123).
- **L-55** — Portal trust error-message alignment (PR #1124).
- **L-58** — Court dates union into Overview deadlines (part of PR #1124 — listed as SKIPPED there; re-check).
- **L-62** — Tax number hybrid: INDIVIDUAL auto-populate, soft-warn at draft, hard-enforce at send (PR #1126). Verify: Day 28 fee-note draft succeeds with tax_number warning, send fails without.
- **L-63** — Fee-note dialog surfaces unbilled disbursements (PR #1116). Verify: Day 28 fee-note dialog shows the Day-21 Sheriff Fees R 1 250 (after it's been Approved).
- **P-03** — Portal projects visible to portal contacts linked via customer (PR #1120). Verify: Day 4 Sipho portal `/projects` shows his RAF matter (not empty).
- **P-07** — Portal `/documents` nav hidden or stubbed (PR #1123).

### CodeRabbit follow-ups (all merged)
- PR #1115, #1121 — tenantId guard + PII log redaction + a11y label + scheduler-path metadata + test hardening.

## Environment

| Service | URL | Status (post-infra-ready 2026-04-24 23:05 SAST) |
|---------|-----|--------------------------------------------------|
| Frontend (kc mode) | http://localhost:3000 | UP PID 5771 — healthy |
| Backend (local+keycloak profile) | http://localhost:8080 | UP PID 41678 (was 41837 in prior log; PID may have rotated between L-64 verify and Day 45 — no action needed, /actuator/health UP, all features intact incl. V112 + MockPaymentGateway + seeder) |
| Gateway (BFF) | http://localhost:8443 | UP external PID 71426 — healthy |
| Portal | http://localhost:3002 | UP PID 5677 — healthy, root returns 307 |
| Keycloak | http://localhost:8180 | UP — realm `docteams` OK |
| Mailpit UI | http://localhost:8025 | UP — inbox purged |
| Postgres (docteams) | localhost:5432 | UP (8 days healthy) |
| LocalStack (S3) | http://localhost:4566 | UP |

Dev Stack READY. QA cleared to start Day 0.

## Gap Tracker

| GAP_ID | Day / Checkpoint | Severity | Status | Summary | Owner | Retries |
|--------|------------------|----------|--------|---------|-------|---------|
| GAP-L-22 | Day 0 — 0.21 / 0.22 | HIGH (BLOCKER) | VERIFIED | Accept-invite flow completes KC registration but browser lands on padmin's `/platform-admin/access-requests` (stale BFF session) instead of Thandi's firm dashboard. **VERIFIED 2026-04-25 03:55 SAST** end-to-end on 3 KC invites (Thandi/Bob/Carol). PR #1129 merge SHA `05d05f48`. | Dev → QA | 0 |
| GAP-L-25 | Day 1 — 1.5 | MED | VERIFIED | SECTION_86 trust account type (PR #1123). **VERIFIED 2026-04-25 04:20 SAST**: dialog now offers Section 86 / General / Investment; persisted as `account_type=SECTION_86`. | Dev → QA | 0 |
| GAP-L-26 | Day 1 — 1.2 | MED | VERIFIED | Brand colour + logo apply to sidebar chrome. **VERIFIED 2026-04-25 04:20 SAST**: `--brand-color=#1B3358` set on `:root`; sidebar `<img>` renders the uploaded logo at 32×32. | Dev → QA | 0 |
| GAP-L-29-regression | Day 2 — 2.5 / 2.6 | MED (cascading on L-28) | VERIFIED | **NEW REGRESSION found in Cycle 1 verify on 2026-04-25 04:30 SAST.** Conflict-check form's Customer dropdown is empty because backend `/api/customers` returns raw `List<CustomerResponse>` (CustomerController.listCustomers line 92) but frontend server action `fetchCustomers` in `frontend/app/(app)/org/[slug]/conflict-check/actions.ts:108-112` treats response as `PaginatedResponse<>` and reads `result.content`. Result: `result?.content ?? []` always `[]`, dropdown never populates. Without customerId, conflict-check backend cannot self-exclude → L-28 fix's NO_CONFLICT outcome is unreachable from UI. Same shape mismatch latent in `fetchProjects` (lines 102–106) — `ProjectController.listProjects` also returns raw `List<ProjectResponse>`. **FIXED 2026-04-25 05:59 SAST in PR #1131 (merge SHA `74164d0d`)**: defensive parse `Array.isArray(result) ? result : (result?.content ?? [])` applied to both `fetchCustomers` and `fetchProjects` (latent project bug pre-empted in same change). New Vitest at `frontend/app/(app)/org/[slug]/conflict-check/actions.test.ts` covers raw-array, paginated-wrapper, null, and empty-content shapes for both functions (8/8 pass). Frontend HMR picks up — no service restart needed. **VERIFIED 2026-04-25 06:04 SAST**: Customer dropdown hydrates with Sipho Dlamini option; selecting Sipho + clicking Run Conflict Check returns NO_CONFLICT (green badge, DB row `customer_id=c3ad51f5-… / NO_CONFLICT`). Bonus: Matter dropdown also hydrated (`Dlamini v Road Accident Fund`) — latent fetchProjects bug pre-empted. | Product → Dev → QA | 0 |
| GAP-L-28 | Day 2 — 2.6 | MED | VERIFIED | Conflict-check self-exclusion (PR #1118). Was VERIFIED-BACKEND-only in prior cycle (UI path blocked by L-29-regression). **VERIFIED end-to-end 2026-04-25 06:04 SAST** post-L-29-regression fix — selecting just-created Sipho via the now-hydrated dropdown causes backend to self-exclude on `customer_id`; result = NO_CONFLICT. | Dev → QA | 0 |
| GAP-L-33 | Day 3 — 3.8 | MED | VERIFIED | FICA Onboarding Pack template present in legal-za request packs. **VERIFIED 2026-04-25 SAST (Day 3 cycle-1)**: Create Information Request dialog template combobox lists 7 options including "FICA Onboarding Pack (3 items)". Snapshot `qa_cycle/checkpoint-results/day-03-cycle1-template-options.yml`. | Dev → QA | 0 |
| GAP-L-34 | Day 3 — 3.9, 3.13 | MED | VERIFIED | Portal-contact auto-provision (PortalContactAutoProvisioner). **VERIFIED Day 2 cycle-1; re-confirmed Day 3 cycle-1 (2026-04-25 SAST)**: Sipho's `portal_contacts` row `127d1c7d-…` ACTIVE/GENERAL; REQ-0001 dispatched against this portal_contact. | Dev → QA | 0 |
| GAP-L-37-regression-2026-04-25 | Day 3 — 3.6 | **HIGH (BLOCKER)** | **VERIFIED** | **NEW REGRESSION found Day 3 cycle-1 verify (2026-04-25 SAST)**. RAF litigation matter `e788a51b-…` has THREE auto_apply field groups attached: SA Conveyancing — Matter Details (Conveyancing Type, Erf Number, Deeds Office, Lodgement Date, Transfer Duty, Bond Institution — entirely inappropriate for a litigation matter), SA Legal — Matter Details (correct), Project Info. DB: `applied_field_groups` jsonb on `tenant_5039f2d497cf.projects` contains both `2b892529-… (conveyancing_za_matter)` and `2ce9428d-… (legal_za_matter)`. **Root cause**: at PROJECT entity_type, both `conveyancing-za-project` and `legal-za-project` field-group rows have `auto_apply=true` and `depends_on=NULL`, so the auto-apply runner attaches both unconditionally to every legal-za matter regardless of tenant vertical or matter work_type. Customer level was VERIFIED Day 2 because at CUSTOMER entity_type only `legal-za-customer` has `auto_apply=true` (the FICA + conveyancing customer field groups are `auto_apply=false`). Fix candidates: (a) populate `field_groups.depends_on` with vertical_profile predicate for vertical-specific packs and have the auto-apply runner respect it; (b) gate field-group seeding behind a per-tenant pack install whitelist so `conveyancing-za-project` is not present in legal-za tenants; (c) tie auto_apply to `projects.work_type` matching a per-pack target list. PR #1122 (the L-37 fix) appears to have addressed only the customer scope. Snapshot `qa_cycle/checkpoint-results/day-03-cycle1-matter-detail.yml` lines 139–270 + image `day-03-cycle1-3.5-matter-overview-with-fica.png`. **Halt cycle.** **FIXED 2026-04-25 SAST in PR #1132 (merge SHA `bb45e85f`)**: added `applicable_work_types` jsonb predicate to `field_groups`; auto-apply runner filters project-scope groups by `Project.workType`; conveyancing-za-project pack opted in to `[CONVEYANCING]`; legal-za-project pack stays unscoped; V112 tenant migration backfills. Three call sites updated (`ProjectFieldService.prepareForCreate`, `ProjectTemplateService.instantiateTemplate`, `ProjectTemplateService.instantiateFromTemplate`). NEEDS_REBUILD: backend must restart for V112 + resolver overload. | Product → Dev | 0 |
| GAP-L-38 | Day 3 — 3.5 | LOW | VERIFIED | Matter detail tab cleanup. **VERIFIED 2026-04-25 SAST (Day 3 cycle-1)**: 19 tabs visible, ONE Disbursements tab (no duplicate). Tabs: Overview, Documents, Members, Clients, Tasks, Time, Fee Estimate, Financials, Staffing, Rates, Generated Docs, Requests, Client Comments, Court Dates, Adverse Parties, Trust, Disbursements, Statements, Activity. Snapshot `day-03-cycle1-matter-detail.yml`. | Dev → QA | 0 |
| GAP-L-39 | Day 3 — 3.1, 3.3 | MED | VERIFIED | customerId URL-param propagation to Configure step. **VERIFIED 2026-04-25 SAST (Day 3 cycle-1)** indirectly via DB binding: matter `e788a51b-…` was created in the prior partial turn from Sipho's customer detail page and DB shows `customer_id=c3ad51f5-…` (Sipho) bound correctly — proves the customerId param flowed end-to-end. | Dev → QA | 0 |
| GAP-L-41 | Day 3 — 3.11 | LOW | VERIFIED | Information request due_date column + picker. **VERIFIED 2026-04-25 SAST (Day 3 cycle-1)**: `tenant_5039f2d497cf.information_requests.due_date` column exists (type DATE, indexed); Create dialog exposes "Due Date (optional)" textbox; REQ-0001 has `due_date=2026-05-02` (today + 7d = Day 10). Snapshots `day-03-cycle1-new-request-dialog.yml` line 350-352 + `day-03-cycle1-dialog-2.yml` line 363-366. | Dev → QA | 0 |
| GAP-L-42 | Day 3 — 3.14 | HIGH | VERIFIED | Information-request magic-link to portal. **VERIFIED 2026-04-25 SAST (Day 3 cycle-1) — re-confirmed**: Mailpit message `ize9ah2EATLQGhwNJuYkdi` for REQ-0001 contains URL `http://localhost:3002/auth/exchange?token=YuMT4sl1mkY_QyPVe4CMW1kz8Ef7p6tnFAJo5DGWgv4&orgId=mathebula-partners`. Port 3002 ✓, token ✓, orgId=mathebula-partners ✓. | Dev → QA | 0 |
| MINOR-Doc-Drift-26 | Day 0 — 0.26 | LOW (cosmetic/docs) | OPEN | Scenario step 0.26 references `/settings/team` but actual route is `/org/{slug}/team`. `/settings/team` returns 404. Update scenario doc or add a redirect. Does not block demo. | Product | 0 |
| MINOR-Copy-Projects | Day 0 — dashboard | LOW (cosmetic) | OPEN | Dashboard Recent Activity empty-state reads "Activity will appear as your team works on **projects**." Legal-za terminology should say "matters". One-string leak in empty-state copy. | Product → Dev | 0 |
| GAP-L-35 | Day 3 — 3.3 | MED | VERIFIED | Custom-field update on PROSPECT customer's matter. **VERIFIED 2026-04-25 SAST (Day 3 cycle-1 cont.)**: PUT `/api/projects/e788a51b-…` with `customFields:{case_number,opposing_party}` on matter linked to PROSPECT customer Sipho → 200 OK; DB confirms persistence. PR #1119 fix holds. | Dev → QA | 0 |
| GAP-L-43 | Day 4 — 4.12 | HIGH | VERIFIED | Portal request-item submitted listener. **VERIFIED 2026-04-25 SAST (Day 4 cycle-1 snapshot-replay)**: snapshots `day-04-cycle1-after-upload-1.yml` / `after-upload-2.yml` / `portal-3of3-submitted.png` show progressive `N/3 submitted • status IN_PROGRESS` rendering. Listener projects state correctly to read-model `portal.portal_request_items` after each upload. | Dev → QA | 0 |
| GAP-L-44 | Day 4 — 4.4 / 4.7 | LOW | VERIFIED | PackReconciliationRunner enabled_modules sync. **VERIFIED 2026-04-25 SAST (Day 4 cycle-1 snapshot-replay)**: portal sidebar "Requests" link rendered in all snapshots (was previously hidden by module-gate). | Dev → QA | 0 |
| GAP-L-45-regression-2026-04-25 | Day 5 — 5.3 | LOW | OPEN | Per-item Download button on firm info-request detail. PR #1122 (L-45) was frontend-only — added `ItemDocumentDownloadButton` gated on `item.documentId && item.documentFileName` (`request-detail-client.tsx:342-356`) — but backend `RequestItemResponse.from()` (`InformationRequestDtos.java:149-169`) doesn't include `documentFileName`, so the gate fails and the Download button never renders. Verified via REST: `GET /api/information-requests/b78cb730-…` returns `documentFileName: null` for ACCEPTED items even though the underlying `documents.file_name` exists. Snapshot `day-05-cycle1-firm-req-detail.yml` shows ACCEPT/REJECT only, no Download. **Severity LOW** — workaround is matter Documents tab. Fix: add `documentFileName` to `RequestItemResponse` record + populate via `Document` lookup in `from()` (mirror pattern in `AcceptanceService.java:822`). Not a blocker for Day 7+ proposal flow — defer to next dispatch. | Product → Dev | 0 |
| GAP-L-46 | Day 5 — 5.5 | LOW | VERIFIED | FICA status tile on matter Overview. **VERIFIED 2026-04-25 SAST (Day 5 cycle-1)**: REST call `/api/customers/c3ad51f5-…/fica-status` returns `{status: "DONE", lastVerifiedAt: "2026-04-25T06:28:35Z", requestId: "b78cb730-…"}` after REQ-0003 closure. Frontend `FicaStatusCard` wired into Overview tab (`overview-tab.tsx:561`, conditional on customerId). PR #1127 holds. | Dev → QA | 0 |
| GAP-L-47 | Day 4/5 — 4.13 / 5.4 | LOW | VERIFIED | Portal parent-request status sync. **VERIFIED 2026-04-25 SAST**: `portal.portal_requests` REQ-0003 row carries `status=COMPLETED, completed_at=…, accepted_items=3` matching tenant `tenant_5039f2d497cf.information_requests` REQ-0003 row. After-upload snapshots also show parent transitioning SENT→IN_PROGRESS during item submissions. | Dev → QA | 0 |
| GAP-P-01 | Day 4 — 4.4 | HIGH | VERIFIED | Portal home InfoRequestsCard path. **VERIFIED 2026-04-25 SAST (via L-44)**: sidebar "Requests" link renders in all portal snapshots (path was already fixed in earlier turn; module-gate now also resolved via L-44). | Dev → QA | 0 |
| GAP-P-02 | Day 4 — 4.8 | HIGH | VERIFIED | Portal /requests UI route. **VERIFIED 2026-04-25 SAST**: snapshots `day-04-cycle1-portal-requests-list.yml` (list page) + `day-04-cycle1-portal-req-detail.yml` (detail page) show full functional flow — list of REQ-0001/0002/0003 cards, detail page with 3 FICA items + Upload buttons + per-item state rendering. | Dev → QA | 0 |
| GAP-P-03 | Day 4 — 4.4 / 4.7 | MED | VERIFIED | Portal projects visible to portal contacts linked via customer. **VERIFIED 2026-04-25 SAST**: snapshot `day-04-cycle1-portal-projects.yml` shows Sipho's `Dlamini v Road Accident Fund` matter rendered on `/projects` (was "No projects yet" before this fix). PR #1120. | Dev → QA | 0 |
| GAP-L-48 | Day 7 — 7.1 | HIGH | **PARTIAL-VERIFIED** | Matter-level "+ New Engagement Letter" CTA + `/api/customers` 404 fix (PR #1127). **Cycle-1 verify 2026-04-25 SAST (REST-only this turn)**: Backend capability proven — `POST /api/templates/0b786248-…/generate` with `entityId=<RAF projectId>` succeeds (HTTP 201, generated_doc `b1f81ae2-…`); `GET /api/customers` returns 200 array (no 404). UI affordance (CTA placement on matter detail header) NOT browser-verified this turn — chrome MCP extension was disconnected. Recommend final UI walk in next chrome-MCP-available QA turn before closing the row. | Dev → QA | 0 |
| GAP-L-50 | Day 7 — 7.11 | HIGH | VERIFIED | "Send for Acceptance" email URL points at portal :3002 (PR #1124, was :3001 in earlier cycle). **VERIFIED 2026-04-25 SAST**: Mailpit message `jQafLva6oWCinjMkfpF78A` href = `http://localhost:3002/accept/sVv_daLWLnSD1xYtkp8iw6I4780NIaQjc4bS02KRVFY` (HTML and text bodies match). | Dev → QA | 0 |
| GAP-L-51 | Day 7 — 7.10 | LOW | VERIFIED | "Send for Acceptance" email subject keywords (PR #1124). **VERIFIED 2026-04-25 SAST**: Subject = `"Mathebula & Partners -- Please review engagement letter for acceptance: engagement-letter-litigation-…pdf"` — contains "review", "engagement letter", "acceptance" (was just "Document for your acceptance" before). | Dev → QA | 0 |
| GAP-P-06 | Day 8 — 8.1 / 8.7 | HIGH | VERIFIED | Portal `/accept/[token]` page handles status correctly (PR #1124). **VERIFIED 2026-04-25 SAST**: (a) Backend `GET /api/portal/acceptance/{token}` returns 200 with VIEWED status (auto SENT→VIEWED on portal fetch); (b) Portal `:3002/accept/{token}` SSR returns 200 OK / 37 KB (no "Unable to process" error); (c) `POST /api/portal/acceptance/{token}/accept` body `{name:"Sipho Dlamini"}` → 200 `{status:"ACCEPTED", acceptedAt:09:56:48 Z, acceptorName:"Sipho Dlamini"}`; DB row flipped clean SENT→VIEWED→ACCEPTED with timestamps + certificate generated. | Dev → QA | 0 |
| GAP-L-58 | Day 7 / 21.12 | LOW | OPEN (NOT-RE-VERIFIED-THIS-TURN) | Court dates union into matter Overview "Upcoming Deadlines" tile (claimed in PR #1124, listed there as SKIPPED; status.md verify-focus said "re-check"). **Cycle-1 2026-04-25 SAST**: backend setup confirmed — `POST /api/court-dates` returns 201 row `d4cd7dcd-…` PRE_TRIAL 2026-05-15 against the RAF matter. Matter Overview tile is a UI element with no single REST aggregator (`/api/deadlines/*` is `regulatory_deadlines`-module-gated which legal-za doesn't enable). UI render NOT verified this turn (chrome MCP unavailable). Defer to next chrome-MCP-available QA turn. | backend / frontend | 0 |
| GAP-L-52 | Day 11 — 11.3/11.4/11.5 | HIGH | **VERIFIED** | Portal trust-ledger sync for RECORDED deposits (PR #1117). **VERIFIED end-to-end 2026-04-25 SAST (Day 11 cycle-1) browser-driven via Playwright MCP**: Sipho's portal `/trust/{matterId}` page (`/trust/e788a51b-…`) renders the Day-10 RECORDED deposit row (`25 Apr 2026 / DEPOSIT / "Initial trust deposit — RAF-2026-001" / R 50 000,00 / R 50 000,00`); trust balance card at top reads `R 50 000,00` "As of 25 Apr 2026"; ZAR currency throughout; 0 console errors; 1 cosmetic Next.js warning unrelated. Sanity SQL confirms `tenant_5039f2d497cf.trust_transactions` row `0a6d1d60-…` still RECORDED (no write occurred). Read-model projection (previously empty per prior cycle's blocker) now populates correctly. Old REQ-0003 magic-link expired; re-issued via firm UI (REQ-0004 created → Mailpit `X3oEh2xXMdLE9qmqLL3DHt` href `http://localhost:3002/auth/exchange?token=…&orgId=mathebula-partners`) — L-42 fix still intact. Evidence: `qa_cycle/checkpoint-results/day-11.md` §"Day 11 Re-Verify — Cycle 1", `day-11-cycle1-portal-trust-view.yml`, `day-11-cycle1-portal-trust-view.png`. | Dev → QA | 0 |
| MINOR-Trust-Nudge-Email-Missing | Day 11 — 11.1 | LOW | OPEN | Day-10 RECORDED trust deposit did NOT dispatch a "View trust balance" / "trust deposit" / "funds received" nudge email to Sipho's portal address. Scenario 11.1 expects one. Possible causes: (a) email template not wired to `TrustTransactionRecordedEvent`; (b) email only fires on UI-driven `recordDeposit` and Day 10 deposit was REST-driven. Workaround: clients use sidebar Trust link (works perfectly). Not a demo blocker; downgraded from prior cycle's HIGH (which was about read-model sync — that part of L-52 is now FIXED in PR #1117). | Product → Dev | 0 |
| GAP-L-62 | Day 28 — 28.4 / 28.6 (tax_number hybrid) | MED | **VERIFIED** (2026-04-25 SAST, browser-driven). PR #1126 hybrid B+C: at draft creation `InvoiceCreationService.createDraft` no longer raises STRUCTURAL violation for missing tax_number; instead surfaces `customer_tax_number` WARNING-severity check + non-blocking `warnings: ["tax_number_missing"]` on response. UI dialog renders amber banner "Tax number missing — This draft can be saved, but the invoice cannot be sent until a tax number is added to the customer profile" with button "Create Draft (1 issues)" still enabled. At send `InvoiceValidationService` fires CRITICAL `customer_tax_number` check → backend returns 422 → UI shows "Validation issues found / ✗ Tax Number is required to send an invoice / Send Anyway / Cancel" (admin/owner Send Anyway override allowed by spec; non-admin would be hard-blocked). Happy-path: Edit Customer dialog → set tax_number='8501015800088' → Save → re-Send → status APPROVED→SENT. Auto-populate path (Create Client wizard auto-fills `taxNumber=idNumber` for INDIVIDUAL) NOT verified this turn (Sipho was created pre-L-62; will surface on next fresh-INDIVIDUAL Day-2 walk). Evidence: `qa_cycle/checkpoint-results/day-28-cycle1-fee-note-dialog-soft-warn.{yml,png}` + `day-28-cycle1-fee-note-send-blocked.{yml,png}` + `day-28-cycle1-fee-note-sent.{yml,png}`. DB pre/post state captured. | Dev → QA | 0 |
| GAP-L-63 | Day 28 — 28.2 (disbursements in fee-note dialog) | MED | **VERIFIED** (2026-04-25 SAST, browser-driven). PR #1116 surfaces APPROVED+UNBILLED disbursements on the customer in the per-customer Generate Fee Note dialog as a "Disbursements" section in the Select Unbilled Items step. Verified end-to-end on Sipho's RAF matter: From=2026-04-01 / To=2026-04-30 / Currency=ZAR → Fetch Unbilled Time → dialog showed "No unbilled time entries found for this period" + **Disbursements section** containing `Sheriff service of summons on RAF — Day 21 cycle-1 verify / Sheriff Fees / Sheriff Pretoria · Apr 25, 2026 / R 1 250,00` (auto-checked, Deselect All option). "1 items selected for Sipho Dlamini / Total R 1 250,00". Created draft → Approve → Send → DB confirms `legal_disbursements.bb9ee2ac-…` flipped `billing_status=UNBILLED → BILLED` with `invoice_line_id='326fb6c5-d25b-4911-9ced-b75a6c5a23ee'` linked to invoice `8f718728-…` (INV-0001, SENT, R 1 250,00 ZAR). Predicate `findUnbilledBillableByCustomerId` (filters APPROVED+UNBILLED) is now reachable end-to-end thanks to L-61 (Submit for Approval CTA) landing first. Evidence: `qa_cycle/checkpoint-results/day-28-cycle1-fee-note-dialog-disbursement-visible.{yml,png}`. | Dev → QA | 0 |
| GAP-L-64 | Day 30 — 30.5 (PayFast Pay button missing) + 30.1 (email "View Invoice" empty href) | **HIGH (BLOCKER for Day 30 PayFast flow)** | **VERIFIED end-to-end browser-driven 2026-04-25 14:09 SAST.** Day 30 cycle-1 re-walk via `mcp__plugin_playwright_playwright__*`. Option B path (new R 100 Court Fees disbursement on Dlamini matter → Submit-for-Approval → Approve → New Fee Note → Approve → Send → INV-0002 status `Sent` with populated payment_url + Online Payment Link panel + Pay button on portal + Mock checkout Simulate-Successful → portal payment-success page → firm flips SENT→PAID with payment_events Created+Completed rows). Final DB: INV-0002 `PAID, paid_at=2026-04-25 14:08:43+00, payment_reference=MOCK-PAY-b98dcf15-…`. Mailpit confirms email "View Invoice" CTA href=`http://localhost:3002/invoices/62c56974-…` (non-empty, fixes Surface B). All 6 L-64 checkpoints PASS (30.1, 30.2, 30.4, 30.5, 30.6, 30.7, 30.11). Side-finding (NOT a Day 30 blocker): MockPaymentGateway uses `appBaseUrl=http://localhost:3000` (Next.js frontend) for the hosted-checkout URL but the controller is mapped on backend port 8080 → direct user click on Pay Now 404s on the frontend; bypassed by Phase 5 by hitting `localhost:8080/portal/dev/mock-payment?…` directly. Recommend new GAP-L-66 LOW (mock-payment URL host config). Evidence: `day-30.md` §"Day 30 Re-walk after L-64 fix — Cycle 1 — RETRY"; snapshots `day-30-cycle1-l64-verified-firm-inv0002-sent-with-payment-link.png`, `day-30-cycle1-l64-verified-portal-pay-now-rendered.png`, `day-30-cycle1-l64-verified-mock-payment-checkout.png`, `day-30-cycle1-l64-verified-portal-payment-success.png`, `day-30-cycle1-l64-verified-firm-inv0002-paid.png`. Original prior status (FIXED + code merged): MockPaymentGateway @Profile({local,dev,keycloak,test}) + MockPaymentController @Profile({local,dev,keycloak}) + MockPaymentIntegrationSeeder (runtime profile-gate via `Environment`, idempotent, hooked into PackReconciliationRunner) + InvoiceEmailService portalUrl-always-set fix. 11/11 MockPaymentGatewayTest pass; backend compile clean. Backend restarted (PID 80950→41837, ready 27s). Seeder log confirms "Seeded mock PAYMENT integration for legal-za tenant tenant_5039f2d497cf (dev profile only)". DB confirms `tenant_5039f2d497cf.org_integrations` row id `9775a954-96b8-4db1-89c6-ad5c7dff7fb9` PAYMENT/mock/enabled=true (re-verified read-only SELECT 2026-04-25). NEEDS_REBUILD=false (just done). **End-to-end browser-driven re-walk of Day 30 phases 30.1/30.5–30.10 NOT YET EXECUTED** — Playwright MCP + Claude-in-Chrome bridges both unavailable on the dispatch turn (wedged Chrome user-data dir + extension disconnect). HALTED per HARD rule #4 rather than substituting REST. Pre-existing INV-0001 still has NULL payment_url (was created pre-seeder); next QA turn must drive Option A (firm-side regenerate) or Option B (fresh fee-note) before re-walk. **Original SPEC_READY context preserved:** Spec at `qa_cycle/fix-specs/GAP-L-64.md`. Chosen approach: dev-only `MockPaymentGateway` adapter (slug=`mock`, `@Profile({local,dev,keycloak,test})`) + Java-side seed of `OrgIntegration(domain=PAYMENT, provider_slug=mock, enabled=true)` in `PackReconciliationRunner` on legal-za install (gated on non-prod profile, idempotent) + dev `/dev/mock-payment` page that POSTs to a profile-guarded `/api/payments/mock/complete` to drive `PaymentWebhookService` synchronously + email template `portalUrl` always populated regardless of PSP state. Authority: scenario step 0.G verbatim "Confirm PayFast sandbox credentials are configured in `OrgIntegration` seed data **OR set up a firm-side stub adapter for Day 30**" — explicitly authorises stub-adapter path. Why fix-in-cycle: blast radius cascades through Day 60.5 closure gate ("no unpaid fee notes" GREEN required), Day 61.4 SoA reconciliation ("Day 30 fee note paid"), Day 88.4 activity trail, exit checkpoint E.12 (demo-blocking). Real PayFast sandbox (`merchant_id`/`merchant_key`/`passphrase` + ngrok-tunneled `notify_url`) deferred to Sprint 2 as "L-64-followup". Est M (~3–4 hr; 2 NEW Java classes, 1 NEW frontend dev page, 1 hook in PackReconciliationRunner, 1 email-service one-line fix, 2 tests). NEEDS_REBUILD: true. **Original triage:** OPEN — NEW (2026-04-25 SAST, Day 30 cycle-1 verify, browser-driven). Mathebula tenant `tenant_5039f2d497cf` has **0 rows** in `org_integrations` for any domain. `IntegrationRegistry.resolve(IntegrationDomain.PAYMENT, PaymentGateway.class)` therefore falls back to `NoOpPaymentGateway` (`@IntegrationAdapter(domain=PAYMENT, slug="noop")` in `integration/payment/NoOpPaymentGateway.java`). NoOp's `createCheckoutSession()` returns `CreateSessionResult.notSupported(...)`. `PaymentLinkService.generatePaymentLink()` (lines 62-68) then exits early with `if (!result.success() || !result.supported()) return;` — `Invoice.payment_url` and `payment_session_id` stay NULL on Send. **Impact (a)**: Portal `/invoices/[id]` render path `invoice.status === "SENT" && !invoice.paymentUrl` shows the fallback panel "Contact Mathebula & Partners to arrange payment" (`portal/app/(authenticated)/invoices/[id]/page.tsx:155-160`) instead of the Pay Now button → Day 30 PayFast sandbox redirect cannot be triggered. **Impact (b)**: Email "View Invoice" CTA renders with `href=""` (empty string) — the link should always go to the portal invoice URL even when no PSP is configured (template currently appears to gate the entire CTA on `paymentUrl` instead of falling back to the portal URL when payment is not configured). DB confirmed: `SELECT count(*) FROM tenant_5039f2d497cf.org_integrations` = 0; `invoices.payment_url` for INV-0001 = NULL; `payment_events` for the invoice = 0 rows. Scenario step 0.G ("Confirm PayFast sandbox credentials are configured in OrgIntegration seed data OR set up a firm-side stub adapter for Day 30") acknowledges this is a known prerequisite that was never satisfied. Two fixable surfaces: (1) seed PayFast sandbox `OrgIntegration` row on legal-za pack install (so `payment_url` populates on Send and `domain=PAYMENT, slug=payfast, enabled=true` resolves correctly), or wire a development-only `MockPaymentGateway` that simulates the full PayFast round-trip locally; (2) email template's "View Invoice" link should always render the portal `/invoices/[id]` URL regardless of `paymentUrl`. Per scenario hard rule, did NOT fake payment via SQL/REST. **Halted Day 30 here**. Evidence: `qa_cycle/checkpoint-results/day-30-cycle1-portal-invoice-detail.{png,yml}` (showing fallback panel, no Pay button), `day-30-cycle1-firm-invoice-still-sent.png` (firm side still SENT, "Payment History — No payment events yet"), `day-30-cycle1-portal-invoices-list.{png,yml}`. | Product → Dev → QA | 0 |
| GAP-L-65 | Day 30 — 30.3 (terminology drift portal vs firm) | LOW | **WONT_FIX (this verify cycle) — DEFERRED to Sprint 2 (2026-04-25 SAST).** Reasoning: exit checkpoint E.9 reads "Terminology sweep passed — zero 'Project/Customer/Invoice' leaks **firm-side**; **portal terminology consistent within itself**" — the portal IS internally consistent (renders "Invoice"/"Invoices" everywhere on portal-side; no firm-side legal vocabulary leaked into portal). The terminology divergence is only across the firm↔portal seam, which E.9 does not gate on. Verify cycle scope is "re-validate 40+ shipped fixes" — propagating the `legal-za` `terminology.invoice` override into portal layout + email templates is genuinely new functionality (portal-side terminology resolver did not exist before; the firm-side wiring is in `(app)/org/[slug]` chrome and was scoped firm-only by design). A correct fix needs a portal-side query for org's terminology overrides + `<Trans>`-style helper + email-template token expansion — at least M-effort; doesn't fit verify-cycle scope. Tracked for Sprint 2 as "L-65-followup: portal/email vertical terminology propagation". **Original triage:** OPEN — NEW (2026-04-25 SAST, Day 30 cycle-1 verify). Portal renders "Invoices" / "INV-0001" / "Back to invoices" / "Contact Mathebula & Partners to arrange payment" while firm side correctly applies the legal-za terminology override "Fee Note" / "fee notes" / "Generate Fee Note" / "New Fee Note". Email subject also says "Invoice INV-0001 from Mathebula & Partners" not "Fee Note INV-0001 ...". The terminology override appears to be wired into the firm-side `(app)/org/[slug]` chrome but not into the portal `(authenticated)/invoices/*` chrome nor into the invoice email template. URL path `/invoices/[id]` is consistent both sides — no path-level rename needed, just label/copy. Suggested scope: (a) propagate vertical's `terminology.invoice` override into portal layout (likely needs portal-side query for org's terminology overrides and a `<Trans>`-style helper), (b) update invoice email subject/body templates to use `{terminology.invoice}` token. Recovery: not required to advance scenario — labels only. Evidence: `qa_cycle/checkpoint-results/day-30-cycle1-portal-invoice-detail.{png,yml}` (portal copy) + Mailpit message ID `DB9hqMbLXzRzDdRgB7jQGh` (subject). | Product → Dev | 0 |
| GAP-L-66 | Day 30 — 30.6 (mock-payment URL host) | LOW | OPEN | Side-finding from Day 30 L-64 verify (2026-04-25 SAST). `MockPaymentGateway` uses `appBaseUrl=${APP_BASE_URL:http://localhost:3000}` (Next.js frontend origin) for the hosted-checkout URL but the Spring controller is mapped at `/portal/dev/mock-payment` on backend port 8080 → direct user click on Pay Now in the portal opens `localhost:3000/portal/dev/mock-payment?…` and 404s in Next.js (no rewrite to backend). Workaround: rewrite host to :8080 manually. Pre-dates L-64; would have hit on first end-to-end click even if `org_integrations` seed had been correct from day one. Fix: (a) change `MockPaymentGateway` to use a backend/gateway origin (e.g. `BACKEND_URL` or `${GATEWAY_URL}`), or (b) add a Next.js rewrite for `/portal/dev/mock-payment*` → backend. Severity LOW (dev-only stub adapter; real PayFast Sprint-2 won't use this code path). Owner: Product → Dev. | Product → Dev | 0 |
| GAP-L-67 | Day 45 — 45.1 (ad-hoc info request free-form items) | MED (downgraded from HIGH — viable workaround proven through Day 46) | **DEFERRED to Sprint 2 as L-67-followup (Product triage 2026-04-25 SAST)** | NEW Day 45 cycle-1 verify (2026-04-25 SAST). Firm UI has **no affordance** to (a) add items to a DRAFT ad-hoc information request, or (b) Send a 0-item DRAFT (action menu only contains Cancel Request). The Create Information Request dialog (`frontend/components/information-requests/create-request-dialog.tsx`) lacks Title and Items inputs — selecting "Ad-hoc (no template)" creates a request with `name=''` and 0 items, which then becomes un-sendable via UI. The detail-page `request-detail-client.tsx` renders no "Add Item" button for DRAFT status (canResend gate is SENT/IN_PROGRESS only at line 133; canCancel covers DRAFT but no Send action exists). **Backend supports it**: `POST /api/information-requests/{id}/items` exists at `InformationRequestController.java:73` (`addItem(...)` → `InformationRequestService.addItem(...)` line 626). Net effect: free-form info requests cannot be created via firm UI today; ALL info requests must use a pack template. **Product triage 2026-04-25 SAST — DEFER to Sprint 2:** (1) Workaround (substitute FICA pack template) was used Day 45 to create REQ-0007 and **VERIFIED end-to-end through Day 46** — Sipho closed REQ-0007 with 3/3 SUBMITTED items, L-43+L-47 listener+parent-status sync re-VERIFIED on the substituted request. Lifecycle is not blocked. (2) Forward QA position is Day 60 (firm matter closure + SoA) — closure flow does not require creating new ad-hoc info requests; existing requests REQ-0001..REQ-0007 cover the lifecycle through closure + Day 88 audit trail. (3) Verify cycle scope is "re-validate 40+ shipped fixes" — L-67 surfaces NEW missing functionality (free-form ad-hoc UI), not a regression of a previously-shipped fix; expanding scope dilutes verify-cycle purpose. (4) Fix scope M (~2-3 hr) — add Title + Items multi-input to `create-request-dialog.tsx` for ad-hoc path, plus "+ Add Item" + "Send Now" actions on `request-detail-client.tsx` DRAFT state — exceeds the < 2 hr SPEC_READY bar for this verify cycle. Severity downgraded HIGH→MED because workaround is verified-good. Evidence: `qa_cycle/checkpoint-results/day-45.md` §"NEW gaps opened"; snapshots `day-45-cycle1-new-request-dialog.yml`, `day-45-cycle1-req-0006-detail.yml`, `day-45-cycle1-req-action-menu.yml`. DB row `44d6d8df-…` REQ-0006 left Cancelled as evidence. | Product → Sprint 2 | 0 |
| GAP-L-68 | Day 46 — 46.7 (portal Home "Last trust movement" tile) | LOW | OPEN | NEW Day 46 cycle-1 verify (2026-04-25 SAST). Portal `/home` "Last trust movement" tile renders "No recent activity" even though Sipho has 2 RECORDED trust deposits (R 50K + R 20K = R 70K balance, both visible on `/trust/{matterId}`). **Root cause** (grep'd): frontend caller `portal/app/(authenticated)/home/page.tsx:239` invokes `portalGet<TrustMovement[]>("/portal/trust/movements?limit=1")` but backend `PortalTrustController` (`backend/src/main/java/io/b2mash/b2b/b2bstrawman/customerbackend/controller/PortalTrustController.java`) maps `/portal/trust` and exposes only `GET /summary`, `GET /matters/{matterId}/transactions`, `GET /matters/{matterId}/statement-documents` — **no `/movements` endpoint exists**. Browser console: `Failed to load resource: 404 @ http://localhost:8080/portal/trust/movements?limit=1` (twice on two `/home` visits). Severity LOW — purely cosmetic Home tile; trust transactions display correctly on matter trust page. Scenario doesn't gate on Home tile content. **Suggested fix scope: S (~1 hr)** — Option A: change frontend to call existing `/portal/trust/summary` and read latest transaction from each `PortalTrustMatterSummary.lastTransactionDate/Amount`, picking the max (smallest change, reuses existing endpoint). Option B: add new `GET /portal/trust/movements?limit=N` endpoint returning latest N transactions across all matters of caller's portal-contact. Evidence: `qa_cycle/checkpoint-results/day-46.md` §"NEW gaps opened"; snapshots `day-46-cycle1-portal-home-initial.yml` line 91, `day-46-cycle1-portal-home-final.png`. | Product → Dev | 0 |
| GAP-L-69 | Day 60 PRE-FLIGHT-C — 60.2/60.3 (matter-trust closure gate cannot clear) | **HIGH (BLOCKER for matter closure)** | **VERIFIED** | **VERIFIED end-to-end browser-driven 2026-04-25 ~20:35-20:46 SAST through PRE-FLIGHT-C re-walk after PR #1136 merge (`8580e466`).** Bob reversed both dirty rows via Trust → Transactions `Reverse` button (Reason "L-69 cleanup..."): `f9f3f7a4` FEE_TRANSFER + `f215423c` REFUND both flipped APPROVED→**REVERSED**, paired REVERSAL rows created in RECORDED state, customer ledger card balance returned to R 70 000. **REFUND path verified end-to-end**: Bob: Trust → Record Transaction → Record Refund dialog **shows new "Matter (Optional)" FormField** mirroring deposit dialog convention (per L-69 fix-spec §"Frontend — Matter field on RecordRefundDialog"). Bob filled customer Sipho UUID + matter UUID `e788a51b-…` + R 70 000 + reference `REFUND-DLAMINI-CLOSURE-DAY60-FIXED` + description → submit → DB `b7c87d27-de02-4d4b-9ff8-917a8354d121 REFUND 70000.00 AWAITING_APPROVAL` with **`project_id=e788a51b-…` populated** (was NULL on the pre-fix dirty row, proving the L-69 frontend + REFUND DTO change works). Thandi approved → matter gate calc returned **R 0,00**. Closure-gate dialog: ALL 9 GATES GREEN ✅. CLOSURE-EXECUTE proceeded successfully — matter `e788a51b-…` `status='CLOSED' / closed_at='2026-04-25 20:44:20' / retention_clock_started_at='2026-04-25 20:44:20'`; `matter_closure_log` row inserted with full notes; closure letter PDF generated and attached. **FEE_TRANSFER path** (the backend-inference half of the L-69 fix) — backend code in place but NOT exercise-able in this verify cycle due to NEW GAP-L-70 (FEE_TRANSFER reversal doesn't cascade to invoice payment status; INV-0001 stuck PAID after the original FEE_TRANSFER was reversed, blocks re-recording fresh fee transfer). 0 console errors. Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — PRE-FLIGHT-C re-walk (after L-69 fix) — 2026-04-25 SAST" + `.playwright-mcp/page-2026-04-25T20-{35-58,36-21,37-22,40-17,41-44,43-24,43-58}*.yml`. **Original prior triage**: Product triage 2026-04-25 SAST — fix-in-cycle, hybrid backend-inference + frontend-Matter-on-REFUND approach. Spec at `qa_cycle/fix-specs/GAP-L-69.md`. Chosen over QA's all-frontend Matter-combobox-on-both-dialogs suggestion because: (a) FEE_TRANSFER is intrinsically tied to one invoice and `InvoiceLine.projectId` already carries the matter — backend inference is one `findDistinctProjectIdByInvoiceId` query, no UX surface, no scope creep, no risk of user mismatch (the invoice is the source of truth); (b) REFUND has no inherent matter binding so UI is the right place — mirror `RecordDepositDialog`'s existing free-text Matter UUID Input verbatim (no new combobox component, consistent with the other 4 trust dialogs); (c) **CORRECTION to QA's note** — `RecordFeeTransferRequest` and `RecordRefundRequest` records (`TrustTransactionService.java:101-109`) do NOT currently accept `projectId`; both service methods hardcode `null` into the constructor's projectId positional. Spec adds projectId to `RecordRefundRequest` only; FEE_TRANSFER stays request-shape-stable and gets the value from invoice inference. Est S-M (~1.5-2 hr): ~30 min backend (record DTO + 2 one-line service edits + 1 new repository query + 4 unit tests), ~20 min frontend (1-line schema + 1 FormField block copied from deposit dialog), ~10 min restart+smoke, ~30 min buffer for QA's pre-rewalk cleanup of the existing 2 NULL-projectId rows via UI-driven reversal pair (Bob reverse → Thandi approve, twice). NEEDS_REBUILD=true (backend Java change). 4 explicit Sprint-2 follow-ups carved out: typeahead Matter combobox (UX polish across all trust dialogs), `?tab=trust` matter auto-fill on refund dialog, multi-matter-invoice FEE_TRANSFER allocation, APPROVE_TRUST_PAYMENT-on-admin capability question. Why fix-in-cycle (not deferred): blocks Day 60 CLOSURE-EXECUTE → blocks E.13 (matter closed) → blocks E.14 (Day 88 audit trail through closure); without fix the verify cycle cannot reach Day 60 CLOSED state. Verification path: cleanup-via-reversal of existing dirty rows `f9f3f7a4-…`+`f215423c-…`, then re-record with fix in place, then closure-gate dialog must show all 9 GREEN. Original triage: NEW Day 60 cycle-1 PRE-FLIGHT-C (2026-04-25 SAST). Closure-gate `TrustBalanceZeroGate` (`backend/.../verticals/legal/closure/gates/TrustBalanceZeroGate.java:38`) computes balance via `trustTransactionRepository.calculateBalanceByProjectId(project.getId())` — strictly per-matter using `trust_transactions.project_id`. But **`RecordFeeTransferDialog` (`frontend/components/trust/RecordFeeTransferDialog.tsx`) and `RecordRefundDialog` (`frontend/components/trust/RecordRefundDialog.tsx`) have NO Matter / project_id field** in their schema or form. Even when opened from the matter Trust-tab "Fee Transfer" button, projectId is not auto-injected. So FEE_TRANSFER and REFUND rows write `project_id = NULL`, the per-matter gate calculation never sees the disposition, and the gate is **physically unrouteable through the UI in its current form**. Verified end-to-end this slice: Sipho ledger card R 70 000 → R 1 250 fee transfer → R 68 750 refund → **R 0,00** (customer-scoped balance correct, matter Trust tab card displays R 0,00); but closure-gate dialog still renders trust gate RED at `Matter trust balance is R70000.00`. DB: APPROVED FEE-TRANSFER-INV-0001-DAY60-RETRY R 1 250 + APPROVED REFUND-DLAMINI-CLOSURE-DAY60 R 68 750 — both with `project_id=NULL`. Suggested fix M (~3-4 hr, mostly frontend): (1) add optional `matterId` field with combobox/autocomplete to RecordFeeTransferDialog + RecordRefundDialog; (2) auto-fill matterId from matter-Trust-tab context (URL `/projects/{id}?tab=trust`); (3) consider adding a Trust → Refund affordance to matter Trust tab (today only Deposit/Payment/FeeTransfer surface there); (4) backend `RecordFeeTransferRequest`/`RecordRefundRequest` already accept project_id (no schema change needed); (5) optional follow-up — backend derives projectId from invoice's matter when `invoiceId` is supplied for FeeTransfer. Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — PRE-FLIGHT-C — 2026-04-25 SAST"; snapshots `day-60-cycle1-c-{pre-state-3-duplicate-fee-transfers,trust-transfer-fee,trust-refund-client,trust-balance-zero,gates-still-red-l69}.png`. **Side-finding (NOT a separate gap)**: only `owner` role has `APPROVE_TRUST_PAYMENT` — combined with the "recorder cannot be sole approver" rule, single-owner firms cannot approve any trust transaction. Worked around here by having Bob (admin/MANAGE_TRUST) record + Thandi (owner/APPROVE_TRUST_PAYMENT) approve. | Product → Dev → QA | 0 |
| GAP-L-70 | Day 60 PRE-FLIGHT-C re-walk — C-RW2 (FEE_TRANSFER reversal does not cascade invoice payment status) | **HIGH (verify-cycle re-walks); MED (prod)** | **DEFERRED to Sprint 2 as L-70-followup (Product triage 2026-04-25 SAST)** | **Product triage 2026-04-25 SAST — DEFER to Sprint 2:** (1) QA proved a clean workaround in the PRE-FLIGHT-C re-walk — recorded a full R 70 000 REFUND with the new L-69 Matter (Optional) field instead of the FEE_TRANSFER + REFUND split, matter trust gate still cleared to R 0,00, all 9 closure gates went GREEN, matter CLOSED at 2026-04-25 20:44:20. Verify cycle reached its headline outcome (matter CLOSED) without this fix. (2) No remaining Day 60–90 demo flow depends on the FEE_TRANSFER reversal path being lossless — Day 61 (portal SoA download) is gated on L-71 not L-70; Day 88 (audit trail) reads from `audit_log` which already captures the reversal correctly; closure letter is unaffected. (3) Fix scope M (~3-4 hr backend) — exceeds the <2 hr SPEC_READY bar this verify cycle; touching `TrustTransactionService.reverseTransaction` cross-cuts into `InvoiceService` payment-state inversion + `payment_events` reversal entry, plus needs care for the multi-payment case (reverse one of N payments → invoice stays PAID, just rolls back this payment row). (4) Scenario re-walks of L-69's FEE_TRANSFER half can be scripted in Sprint 2 once the cascade is implemented; not needed for verify cycle exit. **L-70-followup carved out**: trust reversal cascade to invoice + payment_events for FEE_TRANSFER (and analogous review for other debit-reversal types that may have downstream side-effects). Severity for verify cycle drops effectively to N/A (not blocking); production severity remains MED (genuine accounting incorrectness — a real firm reversing a fee transfer would have the invoice stuck PAID even though the trust ledger nets back to pre-transfer state, requiring manual void + recreate). Original triage: **NEW Day 60 cycle-1 PRE-FLIGHT-C re-walk (2026-04-25 ~20:40 SAST).** When a FEE_TRANSFER is approved, backend auto-flips the linked invoice from SENT → PAID (`paid_at` populated). When that FEE_TRANSFER is later reversed via UI `Reverse` button, `TrustTransactionService.reverseTransaction` (`backend/.../trustaccounting/transaction/TrustTransactionService.java:959`) cleans up the trust-ledger half (debit reversal: original→REVERSED, ledger card refunded R 1 250) but does NOT call back into `InvoiceService` to undo the invoice payment status. INV-0001 remains stuck PAID with `paid_at='2026-04-25 19:42:07'` even after the originating fee transfer is REVERSED. Attempting to re-record a fresh FEE_TRANSFER against INV-0001 fails with backend validation `"Invoice must be in APPROVED or SENT status, current status: PAID"`. Repro: Day 60 PRE-FLIGHT-C re-walk attempt this slice. **Severity HIGH for verify-cycle** (blocks re-walking the FEE_TRANSFER half of L-69 verification path; QA had to fall back to a REFUND-only disposition to clear closure gate). **Severity MED for production** (genuine accounting incorrectness — a real firm reversing a fee transfer would have the invoice stuck PAID even though trust ledger nets back to pre-transfer state; customer's account would show balance owing OR invoice would need to be voided + recreated). Suggested fix M (~3-4 hr, backend-only): in `TrustTransactionService.reverseTransaction` debit-reversal branch (~line 1055-1075), when `original.transactionType == "FEE_TRANSFER" AND original.invoiceId != null`, call `invoiceService.unmarkPaid(invoiceId, paymentId)` (or similar inverse op) which clears `payment_events.completed`, resets `invoices.status` SENT/APPROVED, clears `paid_at`/`payment_reference`. Test cases: reverse fee transfer that was the only payment on an invoice → invoice goes back to SENT; reverse fee transfer that was one of multiple payments → invoice stays PAID, just rolls back this specific payment. Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — PRE-FLIGHT-C re-walk" Step C-RW2; snapshot `.playwright-mcp/page-2026-04-25T20-40-17-265Z.yml`. | Product → Dev | 0 |
| GAP-L-75a | Day 85 — 85.4 / Phase 69 firm-side audit-log page (`/org/[slug]/audit-log`) returns 404 | **HIGH (verify-cycle blocker for E.14); LOW (prod — intentional roadmap deferral)** | **DEFERRED-Sprint-2 (Phase 69)** | **Triaged 2026-04-25 SAST.** Phase 69 spec already exists at `requirements/claude-code-prompt-phase69.md` (read-side only, ~1 sprint, M-L) — facet endpoints, CSV/PDF export, event-type metadata registry, DSAR integration, generic JSON-diff viewer. Phase 69 Constraints section explicitly carves OUT portal-side activity trail (deferred per Phase 68 ideation 2026-04-18; founder decision 2026-04-20). Decision: **DEFER to Sprint 2 / Phase 69**. Building the page in-cycle would either (a) require shipping a stub that skips the spec'd facet endpoints + export + metadata registry — wasted work overwritten by Phase 69; or (b) pull the full Phase 69 sprint into the verify cycle — out of scope for "re-validate 40+ shipped fixes". For E.14 in this cycle: rely on the matter Activity tab as the firm-side surface (already renders 85-day history per 85.4a evidence) plus DB evidence for the portal-contact rows (depends on L-75c). | Product (deferred) | 0 |
| GAP-L-75b | Day 85 — 85.4 / Matter Activity tab missing actor filter UI | **MED (verify-cycle); LOW (prod)** | **DEFERRED-Sprint-2** | **Triaged 2026-04-25 SAST.** Frontend-only. Matter Activity tab (`frontend/components/activity/activity-feed.tsx` + `activity-filter.tsx`) currently has 6 type-filter buttons (All/Tasks/Documents/Comments/Members/Time, hardcoded `FILTERS` array) but no actor combobox. Backend `AuditEventRepository.findByProjectId` does not currently expose an `actorId` parameter — adding the filter would require: (a) backend repo overload with optional `actorId` + `actorType` params, (b) DTO + controller surface to enumerate distinct actors per matter, (c) frontend Combobox component bound to that endpoint, (d) URL-param plumbing. Estimate M (~3-4 hr), not S as originally guessed — actor enumeration endpoint is non-trivial because Phase 69's `/api/audit-events/facets/actors` is not yet shipped. Decision: **DEFER to Sprint 2** as part of Phase 69 (the facets endpoint is on Phase 69's critical path anyway — section 1.1 in the Phase 69 spec). For E.14 in this cycle: workaround = eyeball-scroll the Activity tab + DB query for portal-contact rows (covers the actor-filter intent at the data level once L-75c lands). | Product (deferred) | 0 |
| GAP-L-75c | Day 85 — 85.4 / Portal-contact write events not persisted to `audit_events` | **HIGH (verify-cycle blocker for E.14); MED (prod, Phase 50 readiness defect)** | **SPEC_READY** | **Triaged 2026-04-25 SAST. Spec at `qa_cycle/fix-specs/GAP-L-75c.md`.** This is the critical path of the L-75 triple — without portal-contact rows in `audit_events`, no amount of L-75a/L-75b surface work can satisfy E.14. Code investigation confirms: `AcceptanceService.markViewed` (line 310) + `AcceptanceService.accept` (line 377) are the ONLY emitters using `actorType("PORTAL_CONTACT")` — explains the 2 rows seen in DB. `PortalCommentService.java:72` emits with non-canonical `actorType("PORTAL_USER")` (drive-by cleanup). All other portal write paths missing emission: `PortalInformationRequestService.initiateUpload` + `submitItem` + `submitTextResponse` (Days 4 / 46), `PortalQueryService.getPresignedDownloadUrl` (Day 61 SoA + closure-letter download), `PaymentWebhookService.processWebhook` (Day 30 mock-payment complete). Fix is additive (4 new `auditService.log(...)` call-sites + 1 `AuditService` constructor injection per service + 1 line pass-through in `PortalDocumentController` + 4 integration tests + optional `PORTAL_USER`→`PORTAL_CONTACT` cleanup in `PortalCommentService`). Pattern is well-established (canonical `AuditEventBuilder.builder().actorType("PORTAL_CONTACT").source("PORTAL")...build()` shape from `AcceptanceService.java:310`). `details.project_id` field is load-bearing for matter-scoped queries (`AuditEventRepository.findByProjectId` filters on `details->>'project_id'`). NEEDS_REBUILD: true (Java change, backend restart). NEEDS_DB_MIGRATION: false (`audit_events.details` is JSONB, additive). Estimated S–M (~3 hr). After Dev lands, QA re-walks one portal write of each kind (info-req item submit, mock-payment complete, SoA download) on the existing Dlamini matter via Sipho self-service magic-link, then runs the DB count query (Verification step 4 in spec) → expect ≥ 6 distinct PORTAL_CONTACT event_types and ≥ 4 rows for matter `e788a51b-…`. With this in place, E.14 can be satisfied in-cycle via DB evidence even with L-75a/L-75b deferred. | Product → Dev | 0 |
| GAP-L-74 | Day 61 — Portal Documents tab does not surface closure pack (closure letter + SoA) to portal contact | **HIGH (BLOCKER for Day 61 entire flow)** | **VERIFIED** | **VERIFIED 2026-04-25 22:30-22:38 SAST end-to-end browser-driven (PR #1138 merge SHA `2640d868`).** Backfill executed via two paths since pre-fix rows are unreachable: (Part A SoA) Thandi → closed Dlamini matter → toolbar `Generate Statement of Account` → Preview & Save → DB confirms new `generated_documents.a9c8d1ac-… document_id=8e9eff16-… → documents.8e9eff16-… visibility=SHARED, size=4110, content_type=application/pdf, file_name=statement-of-account-dlamini-v-road-accident-fund-2026-04-25.pdf` — **L-74a code path proven**: `StatementService.generate` now persists paired `Document` SHARED via mirrored `GeneratedDocumentService.createLinkedDocument` PROJECT branch. (Part B closure letter) Reopen Matter → notes "L-74 verification: re-open to trigger fresh closure-letter generation through new SHARED-visibility code path" → matter ACTIVE → Close Matter → 9 closure gates GREEN → reason=Concluded + Generate closure letter checkbox checked → Confirm Close → DB confirms new `generated_documents.d0e347e5-… document_id=6e2ea5ee-… → documents.6e2ea5ee-… visibility=SHARED, size=1643, content_type=application/pdf, file_name=matter-closure-letter-dlamini-v-road-accident-fund-2026-04-26.pdf` — **L-74b code path proven**: `MatterClosureService.generateClosureLetterSafely` flips visibility INTERNAL→SHARED post-creation. Sipho session: portal `/login?orgId=mathebula-partners` self-service magic-link → token `NLiaMMmaB1nZ4GivRh_KwJgmcrHdQh-roVbMwFx30Vs` → `/auth/exchange` → `/projects` → click Dlamini matter → portal `/projects/e788a51b-…` → Documents tab now lists **8 rows**: 6 FICA + SoA (4.0 KB) + closure letter (1.6 KB). Both new docs visible (was completely missing in cycle-1 pre-fix). Download SoA: presigned URL fired against LocalStack `http://localhost:4566/docteams-dev/org/tenant_5039f2d497cf/generated/statement-of-account-…pdf?X-Amz-…`; `curl` confirmed HTTP 200, 4110 bytes, valid PDF v1.6 (size matches DB exactly). Download closure letter: HTTP 200, 1643 bytes, valid PDF v1.6 (size matches DB exactly). 0 console errors. Pre-fix orphan row `c0931e79-…` (`document_id=NULL`) coexists in DB but is correctly ignored by portal documents API (joins through `documents`). Three informational obs surfaced (NOT new gaps — all deferred Sprint 2): **OBS-Day61-NoProjectDocVisibilityToggle** (no firm UI to flip PROJECT-scoped doc visibility post-creation; not needed since L-74 fixes set SHARED at creation), **OBS-Day61-PortalDocumentsProjectionPartial** (newly-generated SoA missing from `portal.portal_documents` projection, but `PortalQueryService.listProjectDocuments` reads from tenant `documents` table directly so portal still surfaces it), **OBS-Day61-NoPortalDocAuditEvent** (portal-side download/view doesn't emit `audit_events` row — Sprint 2 followup, may surface as gap on Day 88.4 activity-trail). Evidence: `qa_cycle/checkpoint-results/day-61.md` § "Day 61 Re-Walk (after L-73+L-74 fix) — 2026-04-25 SAST"; YAML snapshots `day-61-cycle1-rewalk-{soa-regen-saved,after-reopen,close-step2,after-reclose,portal-projects,portal-matter-detail}.yml`; PNGs `day-61-cycle1-rewalk-{soa-regen-saved,portal-matter-status-closed,portal-documents-with-closure-and-soa}.png`; downloaded PDFs `day-61-cycle1-rewalk-portal-{soa,closure}.pdf` (4110 + 1643 bytes). Original triage: **Product triage 2026-04-25 SAST — fix-in-cycle, two-part backend-only fix, contained to `StatementService` + `MatterClosureService`.** Spec at `qa_cycle/fix-specs/GAP-L-74.md`. Root cause grep-confirmed both halves: **L-74a (closure letter)** — `GeneratedDocumentService.createLinkedDocument` (line 644) for PROJECT entityType uses 5-arg `Document(projectId, fileName, contentType, size, uploadedBy)` constructor (line 653-659); that constructor at `Document.java:71` defaults `visibility=INTERNAL`; portal filter at `PortalQueryService.listProjectDocuments:83` requires `Visibility.SHARED`, drops INTERNAL. So closure letter `documents.2bad9b06-…` row exists with proper `project_id` linkage but visibility=INTERNAL → portal API filter excludes it. **L-74b (SoA)** — `StatementService.generate` (lines 95-175) bypasses `GeneratedDocumentService.generateDocument` (per existing Javadoc lines 39-50 — period-bound context doesn't fit standard `TemplateContextBuilder` interface) and persists ONLY a `GeneratedDocument` row directly via `generatedDocumentRepository.save(...)` at line 139; never creates a paired `Document` row, never calls `linkToDocument(...)`. So SoA is structurally orphaned from the documents pipeline — even firm-side Documents tab can't see it (only Statements tab does, which doesn't exist on portal at all). Chosen fix mirrors `GeneratedDocumentService.createLinkedDocument` PROJECT branch inline in `StatementService.generate` (Part A: ~5 LOC inject `DocumentRepository` + `new Document(projectId, …) + setVisibility(SHARED) + assignS3Key + confirmUpload + save + linkToDocument`) AND inline visibility flip in `MatterClosureService.generateClosureLetterSafely` (Part B: inject `DocumentService`, call `documentService.toggleVisibility(linkedDocId, SHARED)` wrapped in try/catch — best-effort matching the existing letter-generation REQUIRES_NEW contract). No `PortalQueryService` change — `SHARED` is the right gate (closure pack is by-definition client-facing per scenario 60.10 + 61.2 + 61.8). Sprint 2 follow-ups carved out: (i) **L-74-followup** generic `Visibility` enum extension (`PORTAL`/`CLIENT_VISIBLE` distinct from manually-toggled `SHARED`) for audit clarity; (ii) **L-74-followup-2** refactor `StatementService` to reuse `GeneratedDocumentService.generateDocument` once period-bound context is generalised (larger refactor; bypass duplication is small + self-contained today). ~30 prod + ~80 test LOC across 4 files. Est M (~3 hr): 35 min code + 60 min tests + 15 min restart smoke + 30 min fixture buffer. NEEDS_REBUILD=true. Why fix-in-cycle (not deferred): L-74 is the single hard blocker for Day 61 portal walk; cannot be worked around with REST/SQL per dispatch hard rule. **Blocks Day 61 entire flow (61.2 → 61.8), Day 88.4 portal activity-trail (which expects "SoA download (Day 61)" entry — impossible if Sipho can't reach the SoA), and exit checkpoint E.13 (closure-pack delivered to client).** Verification post-fix: Dev rebuild + restart → re-generate SoA via Statements tab toolbar (existing `c0931e79-…` was created pre-fix and still has `document_id=NULL` — backfill or regenerate to get fresh row through new code path) → backfill closure-letter `documents.2bad9b06-…` to `visibility=SHARED` via firm-side toggle UI (or close a fresh test matter) → drive Sipho's portal session via portal `/login` magic-link self-service → matter detail Documents tab → expect both PDFs listed alongside FICA uploads → click Download SoA → presigned-URL roundtrip → byte-size match. Original triage: **NEW 2026-04-25 SAST — Day 61 cycle-1 verify, Sipho session.** Portal matter detail Documents tab on Dlamini matter `e788a51b-…` lists ONLY 6 FICA upload duplicates (`fica-id.pdf` / `fica-address.pdf` / `fica-bank.pdf`, 344 B each, status Pending). Two firm-side closure-pack docs are MISSING from portal: (a) `matter-closure-letter-dlamini-…2026-04-25.pdf` (1.6 KB, `documents.2bad9b06-…`, scope=PROJECT, status=UPLOADED) is in `documents` table with proper `project_id` linkage but NOT exposed on portal — likely portal Documents API filters on `visibility='PORTAL'` or scope=CLIENT_VISIBLE that closure letters don't satisfy; (b) `statement-of-account-dlamini-…2026-04-25.pdf` (4 109 B, `generated_documents.c0931e79-…`, `document_id=NULL`) is structurally orphaned — `StatementService.generate` writes only to `generated_documents` and never creates a paired `documents` row, so the SoA can NEVER appear in any Documents tab (firm-side OR portal-side). Statements tab (which surfaces from `generated_documents`) does not exist on portal at all. Sipho cannot see, download, or reconcile the SoA per scenario steps 61.2 / 61.3 / 61.4 / 61.7 / 61.8. Repro: any closed matter + SoA generated, log in as portal contact, navigate to matter detail Documents tab. **Blocks Day 61 entirely** (re-walk requires this fix; cannot be worked around with REST/SQL per dispatch hard rule). Suggested fix M (~3-4 hr, backend + portal-API): (a) `StatementService.generate` should also write a paired `documents` row (with appropriate scope/visibility) and link via `generated_documents.document_id`, mirroring `MatterClosureService.generateClosureLetter` which already does this; (b) `MatterClosureService` set closure-letter `documents.visibility='PORTAL'` (or whatever filter the portal Documents API applies); (c) review portal Documents API filter to ensure scope=PROJECT + visibility=PORTAL flows through. Unblocks 61.2 (SoA listing), 61.3 (download), 61.4 (PDF contents reconciliation), 61.7 (title check), 61.8 (closure letter visibility) in one shot. Evidence: `qa_cycle/checkpoint-results/day-61.md` §"Day 61 Re-Verify — Cycle 1 — 2026-04-25 SAST" Step 61.2 — Documents tab; YAML `day-61-cycle1-portal-matter-detail.yml` lines 117-186; `day-61-cycle1-portal-projects.yml`. Side-evidence — portal Trust ledger (`day-61-cycle1-portal-trust.yml`) and portal Invoices (`day-61-cycle1-portal-invoices.yml`) both render Sipho's data correctly with no leakage and no blockers, so portal data-pipe is healthy generally — this is specifically a Documents-surface filter+structural gap. | Product → Dev | 0 |
| GAP-L-73 | Day 61 — Portal matter detail status badge shows ACTIVE for closed matter | **LOW (cosmetic; confusing to client)** | **VERIFIED** | **VERIFIED 2026-04-25 22:30-22:38 SAST end-to-end browser-driven (PR #1138 merge SHA `2640d868`).** DB confirms `portal.portal_projects.id='e788a51b-…' status='CLOSED' updated_at='2026-04-26 00:35:14'` (was 'ACTIVE' pre-fix). Portal `/projects/e788a51b-…` matter detail header now renders status badge **CLOSED** (was ACTIVE in cycle-1 pre-fix). `MatterClosureNotificationHandler` correctly fires on `MatterClosedEvent` and updates portal projection per spec — verified by reopen→re-close cycle which exercised both `MatterReopenedEvent` (ACTIVE) and `MatterClosedEvent` (CLOSED) paths cleanly. Sipho session: portal `/login?orgId=mathebula-partners` self-service magic-link → `/projects` → click Dlamini matter → status badge **CLOSED** visible in matter header (`day-61-cycle1-rewalk-portal-matter-detail.yml` line 124, `day-61-cycle1-rewalk-portal-matter-status-closed.png`). Evidence: `qa_cycle/checkpoint-results/day-61.md` § "Day 61 Re-Walk (after L-73+L-74 fix)" Slice 1 B3; DB select on `portal.portal_projects`. **L-73-followup** still carved out for Sprint 2 (frontend `/projects/[id]/page.tsx` could render CLOSED with a distinct badge variant — currently uses default styling; backend now emits correct value). Original triage: **Product triage 2026-04-25 SAST — fix-in-cycle (S effort, ~1.5 hr), backend-only.** Spec at `qa_cycle/fix-specs/GAP-L-73.md`. Root cause grep-confirmed: portal matter detail reads from denormalised projection `portal.portal_projects.status`, populated by `PortalEventHandler` listeners. The status column is updated only on `ProjectUpdatedEvent` (`PortalEventHandler.onProjectUpdated:178-201` calls `updatePortalProjectDetails(...)` with `status = event.getStatus() ?? "ACTIVE"`). `MatterClosureService.performClose` (line 189-247) transitions the project status via `project.closeMatter(...) + projectRepository.save(...)` (lines 205-206) and publishes `MatterClosedEvent` (line 237-239) — but **never** publishes `ProjectUpdatedEvent`. Same for `MatterClosureService.reopen` (publishes `MatterReopenedEvent`, not `ProjectUpdatedEvent`). Result: portal projection stays at the initial "ACTIVE" value written by `PortalEventHandler.onCustomerProjectLinked:146` (hardcoded `"ACTIVE"`) on first customer-project link, forever after closure. **Chosen fix**: extend the existing `MatterClosureNotificationHandler` (the only listener on `MatterClosedEvent` today) to also call `portalReadModelRepository.updatePortalProjectDetails(...)` with the new status for every customer linked to the project — mirrors `PortalEventHandler.onProjectUpdated` row-by-row pattern. Mirror in `onMatterReopened` for the close→reopen→close cycle. Considered alternatives rejected: (Alt A) publish `ProjectUpdatedEvent` from `MatterClosureService` in addition to `MatterClosedEvent` — semantic conflation, payload mismatch; (Alt B) new separate `PortalClosureProjectionHandler` listener — over-engineering when one helper inside the existing handler suffices. ~25 prod + ~40 test LOC across 1 file (`MatterClosureNotificationHandler.java`) + 1 test. Est S (~1.5 hr): 25 min code + 30 min tests + 10 min restart + SQL backfill on Dlamini matter for QA + 25 min fixture buffer. NEEDS_REBUILD=true. **L-73-followup** (Sprint 2): portal frontend `/projects/[id]/page.tsx` "CLOSED" badge variant (greyed-out / archived styling, "Past" tab grouping in `/projects` list per scenario step 75.6) — backend now emits correct value, frontend just renders whatever it gets. Original triage: **NEW 2026-04-25 SAST — Day 61 cycle-1 verify, Sipho session.** Portal `/projects/e788a51b-…` matter detail header renders status badge **"ACTIVE"** even though firm-side + DB confirm `tenant_5039f2d497cf.projects.status='CLOSED' / closed_at='2026-04-25 20:44:20' / retention_clock_started_at='2026-04-25 20:44:20'`. Portal API does not pick up the status flip OR portal frontend hardcodes status display. Severity LOW because matter is read-only-effective on portal anyway (all 9 tasks CANCELLED, no portal actions available); but causes confusion for the client who will see "ACTIVE" label while firm has actually concluded the matter. Repro: any closed matter, log in as portal contact, navigate to matter detail. Suggested fix S (~1 hr): portal API endpoint (`PortalProjectController.getMatterDetail` or equivalent) should pass `projects.status` directly into response payload status field; portal frontend `/projects/[id]/page.tsx` should render `CLOSED`/`Closed` as a non-default badge variant. Probable root cause grep: portal API hardcodes status to 'ACTIVE' or backs it off `is_active`/some other field that wasn't toggled by closure. Evidence: `qa_cycle/checkpoint-results/day-61.md` §Step 61.2 matter-detail status; `day-61-cycle1-portal-matter-detail.yml` line 61. | Product → Dev | 0 |
| GAP-L-72 | Day 60/61 — No notification email to portal contact on matter closure or SoA generation | **MED (UX gap; verify-cycle workaround = portal magic-link self-service)** | **DEFERRED to Sprint 2 as L-72-followup (Product triage 2026-04-26 SAST)** | **Product triage 2026-04-26 SAST — DEFER to Sprint 2:** (1) QA proved a clean workaround in this slice and reused it on Day 46/61 — Sipho self-services a fresh magic-link via portal `/login?orgId=mathebula-partners` whenever he needs portal access (already exercised 3+ times in cycle-1 verify; portal returns dev-mode link inline); no demo flow gates on auto-notification. (2) Scenario re-walk plan for Day 61 (per status.md verify focus) explicitly authorises the magic-link self-service path as the primary method post-L-72 acknowledgement: "Mailpit notification path if L-72 fixed; magic-link self-service path otherwise" (`day-61.md` line 56). (3) Day 75 weekly digest IS scheduled to fire (separate path via existing digest scheduler) — reduces production-UX severity slightly: even without per-event notifications, Sipho gets a digest summarising the closure pack within 7 days. (4) Fix scope S-M (~2-3 hr) is acceptable, but blast radius decision (option a/b/c) needs product/UX call: (a) targeted enqueue from `MatterClosureService.confirmClose` + `StatementService.generate` is mechanical but proliferates email-trigger sites across services; (b) ditto for SoA; (c) generic `documents.created` listener on visibility=SHARED + scope=PROJECT documents is the most general but biggest blast radius — could spam clients on every internal-doc-promoted-to-SHARED action. Option (c) needs a notification-policy decision (per-doc-type allowlist? throttle window?) that's a Sprint 2 product call, not a 3-hr verify-cycle patch. (5) No Day 61 / 75 / 85 / 88 / 90 scenario step or exit checkpoint is hard-gated by per-event email notification — all gated steps either use the magic-link path (Day 61) or the digest path (Day 75) or are firm-side (Day 85, 88-firm half) or sweep-style (Day 90 + exit). **L-72-followup carved out**: per-event portal-contact notification on matter closure + SoA generation, with notification-policy decision (option a/b/c picked + per-doc-type allowlist if c). Severity for verify cycle: workaround-proven N/A (not blocking); production severity remains MED (genuine UX gap — clients should be notified when the closure pack lands without waiting for the weekly digest). Original triage: **NEW 2026-04-25 SAST — Day 61 cycle-1 verify.** Per scenario step 60 ("Mailpit → notification email to `sipho.portal@example.com`: 'Your Statement of Account is ready'") and step 61.1 ("Mailpit → open 'Statement of Account ready' email → click link → lands on portal `/projects/[matterId]`"), the matter closure (via `MatterClosureService.confirmClose`) and SoA generation (via `StatementService.generate`) should both fire customer-notification emails to the portal contact. Mailpit `GET /api/v1/messages?query=to:sipho.portal@example.com` confirms NEITHER fires: last email to Sipho before this slice's portal `/login` self-service request is `Subject: "Mathebula & Partners: Trust account activity"` at `2026-04-25T20:43:23.29Z` (Day 60 PRE-FLIGHT-C REFUND notification, fires from `TrustTransactionService` via existing trust-activity hook). Nothing fires from closure or SoA generation. Severity MED for verify cycle (workaround = portal `/login?orgId=…` self-service magic-link, which Sipho can do anytime — already exercised in this slice to gain portal access); HIGH for production UX (clients must be notified or they'll never know the closure pack is ready). Suggested fix S-M (~2-3 hr): (a) `MatterClosureService.confirmClose` enqueue email-notification with closure-letter doc link, OR (b) `StatementService.generate` enqueue email-notification with SoA doc link, OR (c) generic `documents.created` listener in email-notification module that surfaces visibility=PORTAL or scope=PROJECT documents to the matter's portal contacts. Option (c) is most general but biggest blast radius; (a)+(b) are targeted. Evidence: `qa_cycle/checkpoint-results/day-61.md` §Step 61.1a; Mailpit JSON dump in cycle log. | Product → Dev | 0 |
| GAP-L-71 | Day 60 CLOSURE-EXECUTE — CE3 (Statement of Account generation crash) | **HIGH (BLOCKER for E.13 SoA + Day 61 portal SoA download)** | **VERIFIED** | **2026-04-25 SAST — VERIFIED end-to-end browser-driven on closed Dlamini matter `e788a51b-…` post PR #1137 (`23bb43d8`).** Thandi → matter detail (status Closed) → toolbar `Generate Statement of Account` → dialog default period 2026-04-01..2026-04-25 → `Preview & Save` → backend returned 200, preview iframe rendered, buttons reshape to Close/Download PDF/Regenerate. Backend log line `2026-04-25T21:39:48.262018Z … StatementService — Generated Statement of Account: project=e788a51b-…, period=2026-04-01..2026-04-25, generatedDoc=c0931e79-f185-42c8-af70-33528d5e0d69` (requestId `986b9f0d-…`). NO ClassCastException, NO error stack trace anywhere in `bash svc.sh logs backend | tail -30` post-click (compared to prior failure at `2026-04-25T20:46:08.033`). DB confirms `tenant_5039f2d497cf.generated_documents.c0931e79-…` exists with `file_name='statement-of-account-dlamini-v-road-accident-fund-2026-04-25.pdf'`, `file_size=4109`, `generated_at='2026-04-25 21:39:48.251717+00'`. Statements tab on matter renders new row Apr 25 / R 1 250,00 / R 0,00 / Download (downloads 4 109-byte PDF cleanly). Activity feed shows `Thandi Mathebula performed statement.generated on generated_document`. 0 console errors. Two informational observations carved out (NOT new gaps — separate from L-71's renderer-crash scope): **OBS-Day60-SoA-NotInDocumentsTable** (SoA `document_id` is NULL → SoA only visible via Statements tab not Documents tab firm-side AND not visible at all portal-side; this is the structural cause of GAP-L-74 below), and **OBS-Day60-SoA-Fees/Trust-Empty** (rendered SoA preview body has empty Fee Notes loop AND empty Trust deposits/payments loops despite real ledger data on the matter; disbursements loop populates correctly with R 100 + R 1 250). Both worth Sprint-2 SoA-content-reconciliation followup. Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — SoA re-gen (after L-71 fix) — 2026-04-25 SAST"; YAML snapshots `day-60-cycle1-ce-soa-{regen-dialog,regen-after-click,statements-tab,documents-tab,after-close}.yml`; PDF at `day-60-cycle1-ce-soa-statement.pdf` (4 109 bytes). L-71's Sprint-2 follow-up (generic renderer hardening Option B so future templates don't need per-builder adapters) carried into backlog. Original triage: **Product triage 2026-04-25 SAST — fix-in-cycle, Option C (DTO→Map adapter at SoA model-build, contained to `StatementOfAccountContextBuilder`).** Spec at `qa_cycle/fix-specs/GAP-L-71.md`. Root cause grep-confirmed: `TiptapRenderer.renderLoopTable:309` enhanced-for casts each row to Map (via the unchecked cast at `resolveDataSource:336`), but `StatementOfAccountContextBuilder.build:148/157/164` puts typed Java records directly into the loop data sources — `List<FeeLineDto>`, `List<DisbursementStatementDto>`, `List<TrustTxDto>`. QA's trace blew up on the disbursements path first because that's the only loop-table the SoA template currently exercises in the Dlamini matter, but `fees.entries` and `trust.deposits/payments` would crash too once a richer matter is processed. Chosen Option C (adapter at model-build, smallest blast radius — wrap the 3 typed lists via `objectMapper.convertValue(list, new TypeReference<List<Map<String,Object>>>(){})` before context.put) over Option A (template syntax migration — requires Flyway + tenant-installed-template migration, too much surface) and Option B (extend renderer to reflectively introspect non-Map elements — broader blast radius across all reports + per-row CPU cost, worth doing in Sprint 2 hardening but not in verify cycle). Files touched (~25 prod + ~50 test LOC): `StatementOfAccountContextBuilder.java` (+ ObjectMapper inject + `toMapList(...)` helper + 3 wrappings), `TiptapRenderer.java` (comment-only update on lines 334-337 documenting renderer caller contract), new render-roundtrip integration test asserting first-row-is-Map shape AND end-to-end render against the real installed SoA template. Est S-M (~2-3 hr): 30 min code + 45 min test + 10 min restart smoke + 30 min test-setup buffer. NEEDS_REBUILD=true (backend Java change). After Dev lands, QA re-walks SoA generation against the already-CLOSED Dlamini matter `e788a51b-…` (toolbar button still available on closed matters per scenario step 60.6) → expect 200, PDF download non-empty, contains "Sheriff Fees R 1 250,00" disbursement row. Then Day 61 (Sipho portal SoA download) unblocks. Why fix-in-cycle (not deferred): blocks E.13 (SoA half of Day 60 exit checkpoint — the only Day 60 deliverable still missing now that matter is CLOSED) AND Day 61 entirely. Sprint-2 follow-up carved out: generic renderer hardening (Option B) so future templates with typed-DTO loop sources don't need per-builder adapters. Original triage: **NEW Day 60 cycle-1 CLOSURE-EXECUTE (2026-04-25 ~20:46 SAST).** Statement of Account generation throws `java.lang.ClassCastException: class io.b2mash.b2b.b2bstrawman.verticals.legal.disbursement.dto.DisbursementStatementDto cannot be cast to class java.util.Map` at `io.b2mash.b2b.b2bstrawman.template.TiptapRenderer.renderLoopTable(TiptapRenderer.java:309)`. Stack: `StatementService.renderHtml:276` → `StatementService.generate:110` → `StatementController.generate:42`. The Tiptap template engine's loop-table renderer does `(Map<String,Object>) item` for each iterable element, but `StatementService` provides typed `DisbursementStatementDto` records into the loop's data context. Frontend renders alert "An unexpected error occurred"; backend logs the full ClassCastException at `2026-04-25T20:46:08.033` (requestId `e9fc7a75-d2ae-452f-94b2-e85426e6c4bc`). Repro: any matter with disbursements + click "Generate Statement of Account" toolbar button → Period start/end → Preview & Save. Tested on closed RAF matter `e788a51b-…` post Day 60 CLOSURE-EXECUTE. **Blocks E.13 (SoA half of Day 60 exit checkpoint) AND Day 61 (Sipho portal SoA download)** — no SoA artifact exists for the portal to render. Closure letter generation is unaffected (it uses a different template that doesn't hit the loop-table path). Suggested fix S-M (~2-3 hr): either (a) `TiptapRenderer.renderLoopTable` reflectively converts non-Map items to Maps via Jackson `objectMapper.convertValue(item, Map.class)` before iterating (general fix; works for all DTO types in any future template), OR (b) `StatementService.renderHtml` builds the disbursement-list as `List<Map<String,Object>>` already converted before passing into the renderer context (targeted fix; doesn't address future template DTO usage). Option (a) preferred. Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — CLOSURE-EXECUTE" Step CE3; backend log line at `.svc/logs/backend.log` 2026-04-25T20:46:08.033Z. | Product → Dev | 0 |
| GAP-L-61 | Day 21 — Phase D (approval transition) | **HIGH (BLOCKER for L-63 / Day 28)** | **VERIFIED** (2026-04-25 11:57 UTC) — browser-driven end-to-end on disbursement `bb9ee2ac-b1e5-4e2f-bf43-e40a63809530`: (a) "Submit for Approval" CTA visible in detail-page header on DRAFT (gated on `approvalStatus==='DRAFT'`, placed before Upload/Edit, `data-testid="disbursement-submit-for-approval-button"`); (b) browser-click on CTA → page revalidates, badge transitions Draft→Pending, CTA disappears, `DisbursementApprovalPanel` ("Approval Required" + Approve/Reject) renders; (c) browser-click Approve → modal "Approve Disbursement" dialog with optional notes; (d) browser-click confirm → status flips Pending→Approved, panel disappears. DB confirms `approval_status='APPROVED' / billing_status='UNBILLED' / approved_at=2026-04-25 11:57:03.055511+00 / approved_by='5fabd245-…' (Bob Ndlovu, bob@mathebula-test.local)`. Console: 0 errors / 0 warnings during all transitions. L-63 prerequisite (`findUnbilledBillableByCustomerId` filters APPROVED+UNBILLED) now satisfied. Evidence: `qa_cycle/checkpoint-results/day-21-cycle1-l61-fixed-{detail-draft,submitted,approved}.{yml,png}`; `qa_cycle/checkpoint-results/day-21.md` §"Day 21 Phase D Re-Verify (after L-61 fix) — Cycle 1". Method confirmed browser-driven for every state mutation; only DB read was `SELECT`. Was FIXED (PR #1133, merge SHA `9a7fcad2`, 2026-04-25 SAST) — frontend-only two-file edit per spec: added `submitForApprovalAction` server action in `legal/disbursements/actions.ts` gated on `MANAGE_DISBURSEMENTS`; added "Submit for Approval" CTA in `detail-client.tsx` header gated on `approvalStatus === "DRAFT"`. Build matrix: lint 0 errors / 85 pre-existing warnings, build PASS (23.0s), Vitest 332 files / 2072 tests pass / 2 skipped. NEEDS_REBUILD=false (frontend HMR). Original spec context: | Spec at `qa_cycle/fix-specs/GAP-L-61.md` (2026-04-25 SAST). Frontend-only fix: add `submitForApprovalAction` server action in `legal/disbursements/actions.ts` (mirrors `approveDisbursementAction`, gated on `MANAGE_DISBURSEMENTS`); add "Submit for Approval" CTA in `detail-client.tsx` header gated on `approvalStatus === "DRAFT"` (NOT the existing `editable` flag, to avoid overlap with the Approve/Reject panel on PENDING_APPROVAL). Backend route is `POST /api/legal/disbursements/{id}/submit` (verb-only, NOT `/submit-for-approval` as in dispatch) at `DisbursementController.java:82-86`; capability `MANAGE_DISBURSEMENTS`. Est S (≤30 min, ~65 lines, two files); NEEDS_REBUILD=false (frontend HMR). Original triage: **NEW Cycle-1 verify 2026-04-25 SAST.** DRAFT disbursement has NO UI affordance to transition to PENDING_APPROVAL. `frontend/components/legal/disbursement-approval-panel.tsx:55-57` only renders the Approve/Reject panel when `approvalStatus === "PENDING_APPROVAL"`; no button anywhere on detail-client (`disbursement-detail-client.tsx`), matter-tab list, or org-level list lets a user submit DRAFT for approval. Detail page action buttons = `["Upload receipt", "Edit"]` only (verified via DOM enumeration). Backend `POST /api/legal/disbursements/{id}/submit-for-approval` exists (`DisbursementController.java:85`); API client helper `submitForApproval(id)` exists in `frontend/lib/api/legal-disbursements.ts:167` — but **zero callers** in `frontend/app` or `frontend/components`. **Impact**: `DisbursementRepository.findUnbilledBillableByCustomerId` filters on `approvalStatus='APPROVED' AND billingStatus='UNBILLED'` (lines 40–47), so disbursements created via the L-57 dialog cannot reach the Day-28 fee-note dialog. **L-63 verify on Day 28 is gated on this fix.** Suggested fix scope (<1hr, frontend-only): (a) add "Submit for Approval" button on `disbursement-detail-client.tsx` rendered when `approvalStatus==="DRAFT" && (canApprove || canEdit)`; (b) wire it to a new `submitForApprovalAction` server action in `legal/disbursements/actions.ts` calling the existing `submitForApproval(id)` API helper; (c) on success, mutate state — `DisbursementApprovalPanel` then takes over for the approve/reject leg. Day-21 evidence row at DRAFT/UNBILLED in DB awaiting transition: `bb9ee2ac-b1e5-4e2f-bf43-e40a63809530` (Sipho `c3ad51f5-…`, RAF `e788a51b-3a73-…`, R 1 250,00 SHERIFF_FEES). | Product → Dev → QA | 0 |

## Deferred to future phases (do NOT re-log as new gaps if observed)

- **L-30** — KYC adapter (needs provider decision; mock-adapter wiring planned for Sprint 2).
- **L-36** — RAF-specific matter template (Sprint 2).
- **L-40** — Multi-role portal-contact CRUD (Sprint 2).
- **L-49** — Proposal fee-estimate side-table + line-item rendering (Sprint 3 — scenario 7.3/7.4/8.2/8.3 carry SKIPPED-BY-DESIGN).
- **L-54** — Beneficial-owners structured field group for TRUST/COMPANY (Sprint 3).
- **L-61** — Bulk Billing Runs default ON for legal-za (Sprint 2).
- **L-64-followup** — Real PayFast sandbox integration test (Sprint 2). Requires `merchant_id`/`merchant_key`/`passphrase` env wiring + ngrok-tunneled `notify_url` reachable from `sandbox.payfast.co.za`. Out-of-scope for in-session QA cycle. The dev `MockPaymentGateway` (GAP-L-64 spec) covers the IntegrationRegistry resolution path, payment_url plumbing, portal Pay-button render, redirect roundtrip, and webhook reconciliation — but NOT real PayFast cryptographic signature verification.
- **L-65** — Portal/email vertical terminology propagation (Sprint 2). Verify cycle has portal "Invoices" + email "Invoice INV-…" while firm side correctly says "Fee Note". Exit checkpoint E.9 only gates portal internal consistency (which holds) — not firm↔portal seam consistency. Needs a portal-side terminology resolver + email-template token expansion.
- **L-70-followup** — Trust reversal cascade to invoice / payment_events for FEE_TRANSFER (Sprint 2). When `TrustTransactionService.reverseTransaction` reverses a debit-type trust transaction whose original side-effect was to flip an invoice to PAID (currently FEE_TRANSFER → invoice paid via `paymentEvent` chain), the reversal must call back into `InvoiceService` to undo the payment-state change: clear `payment_events.completed`, reset `invoices.status` PAID→SENT/APPROVED, clear `paid_at`/`payment_reference`. Multi-payment case: reverse one of N payments → invoice stays PAID, just rolls back this specific payment row. Fix scope M (~3-4 hr backend + cross-service tests). Workaround used in this verify cycle: full-amount REFUND with the L-69 Matter (Optional) field instead of FEE_TRANSFER+REFUND split — proven good through matter CLOSED state. No Day 60–90 demo flow gates on the lossless-reversal property in production today.
- **L-72-followup** — Per-event portal-contact email notification on matter closure + SoA generation (Sprint 2). Today only the existing weekly digest path + on-demand portal `/login` magic-link self-service surface the closure pack to portal contacts. Verify cycle workaround proven (Sipho self-services magic-link, used 3+ times in cycle-1; demo flows do not gate on auto-notification). Sprint 2 work: (i) product/UX decision on enqueue site — option (a) targeted from `MatterClosureService.confirmClose` + `StatementService.generate`, OR (c) generic `documents.created` listener on `visibility=SHARED + scope=PROJECT` with per-doc-type allowlist + throttle to avoid spam; (ii) implement chosen path; (iii) re-test Day 60.11 + Day 61.1 against scenario notification expectations. Production severity MED (genuine UX gap — clients shouldn't have to wait for weekly digest to learn the matter closed).
- **L-73-followup** — Portal frontend `/projects/[id]/page.tsx` "CLOSED" badge variant (greyed-out / archived styling) + "Past" tab grouping in `/projects` list per scenario step 75.6 (Sprint 2). Backend L-73 fix surfaces the correct `status='CLOSED'` value through the portal API; the frontend just needs a non-default badge variant + list-grouping rule. Pure portal-side polish.
- **L-74-followup** — Generic `Document.Visibility` enum extension (Sprint 2). Add a distinct `PORTAL` / `CLIENT_VISIBLE` value separate from manually-toggled `SHARED` so audit can distinguish "auto-shared by closure-pack generator" vs "manually shared by firm user". Also broaden `PortalQueryService.listProjectDocuments` filter to admit both. Not needed for verify-cycle exit (current `SHARED` semantics are correct for closure-pack artefacts).
- **L-74-followup-2** — Refactor `StatementService.generate` to reuse `GeneratedDocumentService.generateDocument` once the period-bound context constraint is generalised — e.g. via a `PeriodContextBuilder` interface variant (Sprint 2). Removes the bypass duplication; deferred because today's inline Document persistence in `StatementService` is small + self-contained.
- **OBS-Day75-NoManualDigestTrigger** (Sprint 2, S ~30 min) — Expose `POST /internal/portal/digest/run-weekly` admin endpoint (API-key gated, like other `/internal/*` routes) OR add to dev-portal harness (`@Profile({"local","dev"})`) so QA can invoke `PortalDigestScheduler.runWeeklyDigest()` on demand. Today the cron `0 0 8 ? * MON` is the only entry point — verify cycles cannot test 75.1-75.4 in-session unless they happen to fall on a Monday morning. Discovered Day 75 cycle-1 verify 2026-04-25 SAST (Sunday).
- **OBS-Day75-NoPortalActivityTrail** (Sprint 2 / Day 88-blocker, M) — Portal has no `/activity`, `/timeline`, or activity-feed surface. Profile shows contact-info only; home shows tile dashboard. Scenario steps 75.5 + 88.3 + 88.4 expect a portal-side activity trail with FICA submit / proposal accept / fee-note pay / SoA download events. Will surface as an explicit gap when Day 88 dispatches. Sprint 2 work: portal-side `PortalActivityFeedService` consuming `PortalEvent` projection + `/profile` or `/home` activity-trail component. Discovered Day 75 cycle-1 verify 2026-04-25 SAST.
- **OBS-Day75-NoPastTabForClosedMatters** — see L-73-followup above (same surface — portal `/projects` list flat, no Closed/Past grouping, closed matter only differentiated by detail-header CLOSED badge).
- **OBS-Day75-PortalTrustMovementsEndpoint404** (Sprint 2, S ~1 hr — duplicate of GAP-L-68 LOW open) — `/home` "Last trust movement" tile fetches `GET http://localhost:8080/portal/trust/movements?limit=1` → 404; UI degrades to "No recent activity". Either implement the endpoint or remove the tile. Re-confirmed Day 75 cycle-1 verify 2026-04-25 SAST.
- **OBS-L-27** — Portal PDF iframe cross-origin (infra decision).

## Legend

- **Status**: OPEN → SPEC_READY → FIXED → VERIFIED | REOPENED | STUCK | WONT_FIX | RESOLVED
- **Severity**: HIGH (blocker) / MED (cascading bug) / LOW (cosmetic)
- **Owner**: QA / Product / Dev / Infra

## Log

- 2026-04-24 SAST — VERIFY CYCLE started. Prior cycle archived to `qa_cycle/_archive_2026-04-24_pre-verify/status.md`. Branch: `bugfix_cycle_2026-04-24` off main. Purpose: re-validate 40+ shipped fixes end-to-end before declaring Day 0–90 scenario green. Next action: Infra agent — restart backend for post-L-60 merges + start portal.
- 2026-04-24 21:26 SAST — Infra: stack readied for verify cycle. Backend PID 5458 ready in 27s (post-L-60-through-L-48 merges). Portal PID 5677 ready in 3s. Frontend PID 5771 ready in 3s (restarted for clean HMR state). Gateway external PID 71426 healthy. Mailpit inbox purged. Docker infra (Postgres, Keycloak, Mailpit, LocalStack) all green; KC realm `docteams` verified. All 4 svc.sh services RUNNING=yes, HEALTHY=yes. QA cleared to start Day 0.
- 2026-04-24 21:38 SAST — QA: Day 0 run executed. Pre-flight wiped prior-cycle state (tenant schema `tenant_5039f2d497cf`, KC org + users, access_request, subscriptions). 0.1–0.20 PASS (OTP flow, padmin approval, KC invitation, L-22 middleware logout bounce, KC registration submission all work). **0.21 FAIL — BLOCKER**: post-registration browser lands on padmin's `/platform-admin/access-requests` with stale BFF session instead of Thandi's firm dashboard. GAP-L-22 **REOPENED**. L-27 VAT/ZAR labels verified via `org_settings`. 10 downstream checkpoints (0.23–0.32) NOT EXECUTED. Full report: `qa_cycle/checkpoint-results/day-00.md`. Next action: Product/Dev triage on GAP-L-22.
- 2026-04-24 22:24 SAST — Product triaged GAP-L-22 regression → SPEC_READY. Spec at `qa_cycle/fix-specs/GAP-L-22-regression.md`. Chose Option A (route post-registration through gateway-bff OAuth2 via new `/accept-invite/complete` frontend bounce page + flip `KeycloakProvisioningClient.organizationRedirectUrl` to target it). Root cause confirmed in code: KC org's `redirectUrl` sends the `account`-client auth code straight to the frontend, which has no OAuth2 handler → gateway success handler never fires → no `KC_LAST_LOGIN_SUB` → middleware's L-22 check is never triggered. PR #1125 middleware is correct; only the trigger path is broken. Est S ≤30 min; one backend line-change + ~40-line frontend page clone; no migrations, no realm-export changes. Dev is cleared to implement.
- 2026-04-24 22:38 SAST — Dev: GAP-L-22 regression **FIXED** in PR #1129 (merge SHA `05d05f48`, squash-merged into `bugfix_cycle_2026-04-24`). Implementation matches spec verbatim — `KeycloakProvisioningClient.organizationRedirectUrl` flipped from `<frontend>/dashboard` to `<frontend>/accept-invite/complete`, new `frontend/app/accept-invite/complete/page.tsx` (40-line clone of `/accept-invite/continue` reusing `AcceptInviteRedirect`) forwards to `${NEXT_PUBLIC_GATEWAY_URL || http://localhost:8443}/oauth2/authorization/keycloak`. New backend tests assert the POST body's `redirectUrl` ends with `/accept-invite/complete` (incl. trailing-slash normalisation case); Vitest covers the bounce-page render + `window.location.replace` on mount. CI green (qodana pass; CodeRabbit pass). Build matrix: backend `compile/test-compile=0`, targeted tests `*KeycloakProvisioning*,*Invitation*,*Organization*Provision*` `=0`; frontend `install=0`, `format` clean, `lint=0` (only pre-existing warnings), `build=0` (`/accept-invite/complete` listed as static prerender), `test=0` (2064 pass / 2 skipped, including 2 new tests in `app/accept-invite/complete/page.test.tsx`). **NEEDS_REBUILD: true** — `KeycloakProvisioningClient.organizationRedirectUrl` is set in the constructor from `@Value`, so the running backend (PID 5458) still serves the old `/dashboard` redirectUrl until restarted. Next action: Infra — restart backend + run the standard pre-flight wipe (tenant schema + KC org + access_request + Mailpit) so QA can resume Day 0 from 0.1 with a freshly provisioned `mathebula-partners` org carrying the new redirectUrl.
- 2026-04-24 23:05 SAST — Infra: pre-flight wipe complete. Deleted 1 KC user (`thandi@mathebula-test.local` id `713ab9d6-654e-4aed-962d-491c7526a5f3`), KC org `61101fc9-cbc7-40e3-8912-dcf44f17ba4b` (`Mathebula & Partners`), 4 registry rows across organizations/access_requests/org_schema_mapping/subscriptions (organizations `2c3a4b3b-ab11-4f10-99f0-76441fffcc2f`, access_requests `d009c550-92ae-479e-97bb-0572007af638`, org_schema_mapping `604bb9ff-26f8-4901-a7e5-7f29a9b64be5`, subscriptions `309af2a6-6fc6-4c12-9fe0-4d00354f58d1` TRIALING with 0 payments), dropped tenant schema `tenant_5039f2d497cf` (103 objects CASCADE). Post-checks all 0 for mathebula; 3 unrelated tenant schemas preserved (`tenant_2a96bc3b208b`, `tenant_8ee5c5a6e45f`, `tenant_f6e34f99f3b9`). Backend restarted PID 25298 ready in 27s (L-22 regression fix loaded — source confirms `/accept-invite/complete` at `KeycloakProvisioningClient.java:80`; accept-invite redirect URL now points at `/accept-invite/complete`). Mailpit inbox purged. All 4 svc.sh services RUNNING=yes, HEALTHY=yes (backend 25298 / gateway ext 71426 / frontend 5771 / portal 5677). QA cleared to re-run Day 0 Phase A–D fresh.
- 2026-04-25 03:55 SAST — QA: Day 0 VERIFY re-run COMPLETE. 30/32 PASS, 1 N/A (0.13 L-21 WONT_FIX), 1 PARTIAL (0.26 doc drift `/settings/team` vs `/team` — non-blocker). **GAP-L-22 VERIFIED end-to-end on 3 KC invites**: Thandi (owner, 0.21), Bob (admin, 0.31), Carol (member, 0.32) all successfully reached `/org/mathebula-partners/dashboard` via the new `/accept-invite/complete` bounce → gateway OAuth2 success handler → L-22 middleware handoff. ORGIVT `reduri` claim confirmed = `http://localhost:3000/accept-invite/complete`. No padmin session leak. L-27 VAT/ZAR labels VERIFIED (tenant `org_settings`: `vertical_profile=legal-za`, `default_currency=ZAR`, `tax_label=VAT`). New access_requests row `status=APPROVED` `keycloak_org_id=3d2c66a0-516d-4357-908e-2ab76351d473`; organizations `provisioning_status=COMPLETED`; tenant schema `tenant_5039f2d497cf` recreated. 2 LOW cosmetic findings logged as new Minor gaps (doc drift, "projects" copy leak). Full results appended to `qa_cycle/checkpoint-results/day-00.md`. Next action: QA — proceed to Day 1.
- 2026-04-25 04:20 SAST — QA: Day 1 VERIFY CLEARED. 7/7 substantive checkpoints PASS + 3/3 summary PASS. Re-logged in as Thandi (`thandi@mathebula-test.local` / `SecureP@ss1`) post-Carol-logout. **GAP-L-25 VERIFIED end-to-end** — Trust Account dialog now offers Section 86 / General / Investment; persisted as `account_type=SECTION_86`, primary, ACTIVE. **GAP-L-26 VERIFIED end-to-end** — `--brand-color` CSS var emits `#1B3358`; sidebar `<img>` renders uploaded logo at 32×32. GAP-L-23 (settings 500), GAP-L-24 (vertical profile loader), L-27 (ZAR/VAT labels) all re-VERIFIED. 19 LSSA tariff items pre-populated. 3 minor non-blocking findings logged informationally (rate-cards doc drift, raw enum badge label, branch-code required UX). Day 1 evidence: `qa_cycle/checkpoint-results/day-01.md` §"Day 1 Re-Run — Cycle 1 Verify".
- 2026-04-25 05:52 SAST — Product triaged L-29-regression → SPEC_READY. Spec at `qa_cycle/fix-specs/GAP-L-29-regression.md`. Chose option (1) — frontend defensive parse in `fetchCustomers` AND `fetchProjects` (latent same bug; `ProjectController.listProjects` also returns raw `List<ProjectResponse>`). Validated against alternatives: audited 15 callers of `/api/customers` across the frontend — 13 expect raw `Customer[]`, 2 (`legal/disbursements/actions.ts:273-278`, `legal/adverse-parties/actions.ts:140-147`) already use the exact `Array.isArray(result) ? result : (result?.content ?? [])` pattern with an inline comment confirming `/api/customers` is non-paginated by contract. Option (2) backend wrap would break 13 non-defensive callers; option (3) dedicated combobox endpoint adds surface area for no benefit. Backend `CustomerController.listCustomers` is intentionally non-paginated (no `Pageable` parameter, returns `ResponseEntity<List<CustomerResponse>>` at line 92-93). Est S (≤30 min): one file edit (`conflict-check/actions.ts`) + new Vitest covering both response shapes; no backend, no migration, no service restart. Dev cleared to implement.
- 2026-04-25 05:59 SAST — Dev: GAP-L-29-regression **FIXED** in PR #1131 (merge SHA `74164d0d`, squash-merged into `bugfix_cycle_2026-04-24`). Frontend-only defensive parse on `fetchCustomers` + `fetchProjects` in `frontend/app/(app)/org/[slug]/conflict-check/actions.ts` — applied `Array.isArray(result) ? result : (result?.content ?? [])` matching the convention in `legal/disbursements/actions.ts:273-278` and `legal/adverse-parties/actions.ts:140-147`. New Vitest at `frontend/app/(app)/org/[slug]/conflict-check/actions.test.ts` covers raw-array AND paginated-wrapper shapes (+ null + empty-content) for both functions — 8/8 cases pass. Build matrix: lint=0 errors (warnings unchanged), typecheck unchanged from main (180 pre-existing errors, 0 new from this change), targeted test=8/8 pass, full vitest=2072 pass / 2 skipped (332 files), build=0. **NEEDS_REBUILD: false** — frontend HMR picks up the change automatically; no backend restart needed (backend untouched). Next action: QA resume Day 2 — 2.6 to verify L-29-regression fix end-to-end (Customer dropdown should populate; Sipho conflict check should return NO_CONFLICT proving L-28 self-exclusion is reachable through UI); if green, mark FIXED → VERIFIED and continue Day 3+.
- 2026-04-25 04:33 SAST — QA: Day 2 VERIFY PARTIAL — **NEW REGRESSION GAP-L-29-regression OPENED**. Switched actor to Bob via in-app sign-out → KC re-login. 2.1–2.5 PASS (client Sipho Dlamini created, redirected to detail at `/customers/c3ad51f5-…`), but **2.6 FAIL** — conflict-check ran with empty Customer dropdown, returned CONFLICT_FOUND (self-match) instead of CLEAR. Root cause traced to API shape mismatch (see Tracker row). 2.7 PARTIAL (screenshot taken, shows wrong outcome). 2.8–2.10 N/A (KYC adapter L-30 deferred to Sprint 2 per scenario permission). Verified during run: GAP-L-31 (empty-state copy "Clients"), GAP-L-32 (Create Client redirect), GAP-L-34 (portal-contact auto-provision via PortalContactAutoProvisioner — backend log captured), GAP-L-37 (field-group narrowing — exactly 1 applied group: SA Legal — Client Details). **GAP-L-28 cannot be UI-verified** until L-29-regression fixed (backend fix may be working but unreachable). Day 2 evidence: `qa_cycle/checkpoint-results/day-02.md` §"Day 2 Re-Run — Cycle 1 Verify". QA agent timed out before status.md update; orchestrator updated this turn. Next action: Product — triage L-29-regression to SPEC_READY (frontend one-line fix candidate already identified).
- 2026-04-25 06:04 SAST — QA: Day 2 CLOSED — **GAP-L-29-regression VERIFIED end-to-end**. Re-ran scenario 2.6 post-PR #1131 merge: Customer dropdown now hydrated with `Sipho Dlamini` option (was empty before). Selected Sipho, ran conflict check → green "No Conflict" badge, DB row `tenant_5039f2d497cf.conflict_checks / NEW_CLIENT / NO_CONFLICT / customer_id=c3ad51f5-…` at 04:03:59 UTC. **GAP-L-28 also VERIFIED end-to-end** — self-exclusion now reachable through UI (was VERIFIED-BACKEND-only before). Bonus: Matter dropdown also hydrated (`Dlamini v Road Accident Fund`) — Dev's two-function fix pre-empted the latent fetchProjects same-shape-mismatch. Day 2 final tally: 7/7 PASS, 3 N/A (L-30 deferred), 0 FAIL. Screenshot: `qa_cycle/checkpoint-results/day-02-2.6-clear-after-l29-fix.png`. Day 2 evidence appended to `day-02.md` §"Day 2 Re-Verify after L-29 fix — Cycle 1 Verify". QA agent stream timed out after also exploring Day 3 (matter + REQ-0001 already exist from prior turn) but did not write Day 3 verify section before timeout. Orchestrator updated status this turn. Next action: QA — write Day 3 cycle-1 verify section using existing data; tighter per-day scope to avoid 3-hour stream timeouts.
- 2026-04-25 SAST — Product triaged L-37-regression-2026-04-25 → SPEC_READY. Spec at `qa_cycle/fix-specs/GAP-L-37-regression-2026-04-25.md`. Chose option (c) — add `applicable_work_types` jsonb predicate to `field_groups` and overload `FieldGroupService.resolveAutoApplyGroupIds(EntityType, String workType)` to filter project-scope auto-apply by `Project.workType`. Validated against alternatives: option (a) `dependsOn` repurposing collides with the column's existing "depends on these other field group IDs" semantics (`FieldGroupService.validateDependsOn` enforces same-entity FK existence) — higher risk; option (b) "per-tenant pack-install whitelist" is heavier than QA realised because field packs are NOT integrated into the formal `PackInstallService` pipeline (which handles only `DOCUMENT_TEMPLATE`/`AUTOMATION_TEMPLATE`) — there is no existing whitelist pattern at the field-pack layer to mirror, and `legal-za.json` profile DOES list conveyancing as a legal-za pack by design (mixed-practice firms genuinely need the pack available, just not auto-attached). Also clarified the QA hypothesis "PR #1122 narrowed at customer scope only" — actually PR #1122 explicitly **skipped** L-37 ("Deferred pending backend schema change"); the customer scope passed at QA Day 2 only because no `conveyancing-za-customer.json` pack exists, not because of any narrowing. Est M (8 file edits + 1 tenant Flyway migration `V112__add_field_group_applicable_work_types.sql` with backfill + 1 new integration test class). NEEDS_REBUILD: true (Java + Flyway). Dev cleared to implement. Next action: Dev — implement L-37-regression-2026-04-25 fix per spec.
- 2026-04-25 SAST — QA: Day 3 cycle-1 verify **HALT — `GAP-L-37-regression-2026-04-25` HIGH/blocker at 3.6**. Method: DB read-only state verification + reuse of prior agent's accessibility snapshots (`day-03-cycle1-*.yml` + matter-overview PNG); no browser drive this turn. Tally: 13/14 PASS/VERIFIED/N/A; 1 FAIL at 3.6. Verify-focus tally: L-33 VERIFIED (FICA Onboarding Pack template present, 3 items), L-34 VERIFIED (Sipho portal_contact still ACTIVE, REQ-0001 dispatched against it), L-37 **REGRESSED at matter level** (both `conveyancing-za-project` and `legal-za-project` field groups have `auto_apply=true` + `depends_on=NULL` → both attach to every legal-za matter; customer-level fix from PR #1122 didn't cover the project scope), L-38 VERIFIED (19 tabs, no duplicate Disbursements), L-39 VERIFIED (matter `customer_id` correctly bound from create flow), L-41 VERIFIED (`due_date` column + picker present, REQ-0001 has Day 10 due_date), L-42 VERIFIED (magic-link URL = `http://localhost:3002/auth/exchange?token=…&orgId=mathebula-partners`). Day 3 evidence: `qa_cycle/checkpoint-results/day-03.md` §"Day 3 Re-Verify — Cycle 1". Next action: Product — triage `GAP-L-37-regression-2026-04-25` to SPEC_READY.
- 2026-04-25 SAST — Dev: GAP-L-37-regression-2026-04-25 **FIXED** in PR #1132 (merge SHA `bb45e85f`). Backend changes (Java + V112 tenant Flyway): added `applicable_work_types` jsonb predicate to `field_groups`; `FieldGroupService.resolveAutoApplyGroupIds(EntityType, String workType)` overload filters project-scope auto-apply by `Project.workType`; conveyancing-za-project pack opts in with `[CONVEYANCING]`; legal-za-project pack stays unscoped; V112 tenant migration backfills existing rows. Three call sites updated (`ProjectFieldService.prepareForCreate`, `ProjectTemplateService.instantiateTemplate`, `ProjectTemplateService.instantiateFromTemplate`). Build matrix: spotless=clean, compile=0, target tests=94 pass (`*FieldGroup*,*ProjectField*,*ProjectTemplate*`), unit suite=3494 pass, new IT=6 pass (`FieldGroupAutoApplyWorkTypeIntegrationTest`). **NEEDS_REBUILD: true**. Next action: Infra — restart backend; then QA resumes Day 3 — 3.6.
- 2026-04-25 SAST — Infra: backend restarted (PID 25298→80950, ready in 27s; old PID etime 9h13m, new PID Spring start time 9.66s). V112 migration ran on 4 tenants (`tenant_8ee5c5a6e45f`, `tenant_f6e34f99f3b9`, `tenant_2a96bc3b208b`, `tenant_5039f2d497cf` — mathebula-partners). Verified: `tenant_5039f2d497cf.field_groups` now has `applicable_work_types jsonb` column; conveyancing-za-project pack scoped to `["CONVEYANCING"]`, legal-za-project / common-project / common-task / legal-za-customer packs unscoped (NULL = applies to all). New resolver overload loaded (no startup errors, only 1 harmless CglibAopProxy WARN). All 4 svc.sh services RUNNING=yes, HEALTHY=yes (backend 80950 / gateway ext 71426 / frontend 5771 / portal 5677). NEEDS_REBUILD=false. QA cleared to re-run 3.6.
- 2026-04-25 SAST — QA: **Day 3 CLOSED + Day 4–5 cycle-1 verify COMPLETE**. Method: REST API (Bob KC password-grant — Chrome MCP unavailable; per dispatch this is allowed) + READ-ONLY DB queries + cross-reference against committed accessibility snapshots from prior agent. **GAP-L-37-regression-2026-04-25 VERIFIED** end-to-end: created NEW litigation matter via `POST /api/project-templates/998ba76c-…/instantiate` body `{workType:"LITIGATION"}` → matter `af9b14b2-…` with `applied_field_groups=[common-project, legal-za-project]` only (conveyancing pack correctly excluded). Control probe with `workType:"CONVEYANCING"` (matter `db5ff54a-…`, archived after) confirms conveyancing pack still attaches to matters that need it — fix is precise, not over-broad. **GAP-L-35 VERIFIED**: PUT `/api/projects/e788a51b-…` `customFields:{case_number,opposing_party}` on PROSPECT-customer matter → 200, persisted. **Day 4 cycle-1 (snapshot-replay)**: GAP-L-42 / L-43 / L-44 / L-47 / P-01 / P-02 / P-03 all VERIFIED via snapshots `day-04-cycle1-*.yml,png`; portal flow end-to-end works (REQ-0003 SENT→IN_PROGRESS→COMPLETED, all 3 items SUBMITTED then ACCEPTED with document_ids; portal read model in sync). **Day 5 cycle-1**: GAP-L-46 VERIFIED (REST `/api/customers/c3ad51f5-…/fica-status` returns `status=DONE` after REQ-0003 closure; FicaStatusCard component wired). **NEW gap GAP-L-45-regression-2026-04-25 (LOW)**: PR #1122 L-45 fix was frontend-only — added `ItemDocumentDownloadButton` gated on `documentFileName`, but backend `RequestItemResponse.from()` (`InformationRequestDtos.java:149-169`) doesn't populate `documentFileName`, so Download button never renders. REST verifies `documentFileName: null` for all REQ-0003 ACCEPTED items. Workaround: matter Documents tab. NOT a blocker for Day 7+. Final tally: Day 3 14/14, Day 4 14/14 (1 SKIPPED-by-design 4.11 note polish), Day 5 6/6 (1 PARTIAL 5.3). Probe matter `af9b14b2-…` left in tenant as evidence. Next action: Day 7 separate dispatch — Thandi proposal flow.
- 2026-04-25 SAST — QA: **Day 7 + Day 8 + Day 10 cycle-1 verify COMPLETE**. Method: REST API end-to-end (Thandi via Keycloak password-grant, gateway-bff confidential client + organization scope) + Mailpit body inspection + READ-ONLY DB queries. Chrome MCP extension was disconnected this turn — REST-only is permitted by dispatch ("ALL state mutations through REST API or browser UI"). **GAP-L-50 VERIFIED** — acceptance email href = `http://localhost:3002/accept/<token>` (Mailpit `jQafLva6oWCinjMkfpF78A`). **GAP-L-51 VERIFIED** — subject = `"Please review engagement letter for acceptance: …"` (keyword match on review + engagement letter + acceptance). **GAP-P-06 VERIFIED** — portal `:3002/accept/<token>` SSR returns 200 OK; backend `POST /api/portal/acceptance/{token}/accept` accepts `{name:"Sipho Dlamini"}` → 200 ACCEPTED with timestamps + certificate generated; idempotent re-GET renders ACCEPTED state cleanly. **GAP-L-48 PARTIAL-VERIFIED** — backend tier (engagement-letter generation + `/api/customers` 200) proven; UI CTA placement deferred to next chrome-MCP-available turn. **GAP-L-58 NOT-RE-VERIFIED** — court_dates row created but Overview tile is UI-only; deferred. **Day 7 7/11+3 SKIPPED-BY-DESIGN (L-49 Sprint 3)/1 PARTIAL**. **Day 8 6/9+2 SKIPPED-BY-DESIGN (L-49)/1 N/A (no /proposals route)**. **Day 10 7/9 PASS, 1 SKIPPED-BY-DESIGN (10.6 dual-approval), 1 N/A (10.9 screenshot)**. Trust state at end-of-turn: account `45581e7d-…` Mathebula Trust — Main SECTION_86 R 50 000,00; trust_transactions row `0a6d1d60-…` DEPOSIT RECORDED 2026-04-25; client_ledger_cards row `28326991-…` Sipho R 50 000,00 / total_deposits R 50 000,00. RAF matter has live ACCEPTED engagement-letter `276d7b95-…` with certificate `Vt9z75ZUfxWWUTXrToTaov` confirmation email dispatched. Court date `d4cd7dcd-…` PRE_TRIAL 2026-05-15 created in support of L-58 setup (UI verification deferred). No new gaps. Day 7+8+10 evidence in `qa_cycle/checkpoint-results/day-07.md` / `day-08.md` / `day-10.md` (each appended with "## Day N Re-Verify — Cycle 1 — 2026-04-25 SAST" section). Next action: **Day 11 dispatch — Sipho portal `/trust` view (L-52 verify)**. **Note: chrome MCP extension is currently disconnected** — reconnect to claude.ai before next QA turn if it requires UI walks; REST-only verify works for many scenarios but not for visual checkpoints (screenshots, tile placement) or genuine portal-page interaction.
- 2026-04-25 SAST — Orchestrator: **Pause-and-resume marker**. User has reinforced HARD rule (saved to memory `feedback_qa_must_use_browser_not_rest.md`): **REST-as-substitute-for-UI-verification is forbidden**. The ONLY legitimate REST in QA is the Mailpit `/api/v1/messages` API. READ-ONLY SQL `SELECT` for state verification still allowed; WRITE SQL forbidden. **Implication for the Day 7+8+10 cycle-1 verify above**: every checkpoint marked PASS via `POST/GET /api/...` (engagement-letter generate, Send for Acceptance dialog, portal accept form, trust deposit dialog) is **UNVERIFIED for the UI tier** — must be re-walked through the browser before declared green. Mailpit-derived L-50 (href) and L-51 (subject) verifications **stand** (legitimate REST). Browser MCP availability: chrome `claude-in-chrome` extension — `tabs_context_mcp` and `switch_browser` both return "Browser extension is not connected" despite user enabling it; "Reconnect extension" via `/chrome` opens claude.ai page then closes immediately (likely account mismatch or Chrome profile/cookie issue, not yet root-caused). Playwright MCP — `claude mcp list` shows `playwright ✓ Connected` and `plugin:playwright:playwright ✓ Connected` BUT ToolSearch in this session returns no Playwright tool schemas (subagents don't see them). Both MCP paths are blocked this turn. Day 11+ cannot proceed honestly until one browser MCP is restored. **Resume in next session after Chrome restart / reconnect**. Pending UI re-walks (carried forward): L-48 matter-level "+ New Engagement Letter" CTA placement, L-58 Overview "Upcoming Deadlines" tile, Day 7 Generate EL + Send for Acceptance dialogs, Day 8 portal `/accept/{token}` form + idempotent re-render + firm-side status reflection, Day 10 Trust Accounting Record Deposit dialog (R 25,000 fresh deposit on top of existing R 50,000 to keep evidence separate). Live tenant state preserved (no writes since the REST verify): Sipho PROSPECT, RAF matter `e788a51b-…` ACTIVE, probe matter `af9b14b2-…` ACTIVE (clean post-V112), Mathebula Trust — Main R 50,000.00, accepted EL `276d7b95-…` with cert, court date `d4cd7dcd-…` PRE_TRIAL 2026-05-15.
- 2026-04-25 SAST — QA: **Day 14 cycle-1 verify COMPLETE — Moroka Family Trust onboarded firm-side via Playwright MCP browser walk**. Tab 0 firm session (Bob Ndlovu admin); Tab 1 Sipho portal preserved untouched. **11/11 PASS** (14.4 SKIPPED-BY-DESIGN per scenario). Created Moroka client (`2b454c42-…`, TRUST, IT 001234/2024, moroka.portal@example.com) → matter from Deceased Estate template (`89201af5-…`, EST-2026-002, lead Thandi) → Liquidation and Distribution Account Pack info request REQ-0005 SENT (`83428106-…`, due 2026-05-11) → uploaded death-certificate-moroka.pdf (`8d92037c-…`, 298 B test PDF) → recorded R 25,000 DEPOSIT (`446fa97c-…`, ref DEP/2026/002, RECORDED). Trust Balance card on Moroka matter renders **R 25 000,00** ZAR; per-client ledger card `182621ca-…` separate from Sipho's `28326991-…` (DB-layer per-customer isolation already in place — good substrate for Day 15 portal probes). **Verify-focus**: L-27 VAT/ZAR labels HOLD (R 25 000,00 with comma decimal), L-32 redirect-to-detail VERIFIED for Moroka, L-34 portal-contact auto-provision VERIFIED for TRUST type (Moroka pre-populated REQ-0005 Portal Contact combobox), L-37 customer-scope narrowing HOLDS (only `SA Legal — Client Details` shown in step 2 — no conveyancing pack), **L-37-regression V112 fix HOLDS at project scope** (Moroka matter `applied_field_groups=[Project Info, SA Legal — Matter Details]` — `conveyancing_za_matter` correctly excluded because deceased-estate template's empty work_type doesn't match the `["CONVEYANCING"]` predicate), L-39 customerId param propagation VERIFIED (`+ New Matter` link from Moroka detail → Configure step Client combobox pre-selected Moroka), L-41 due_date picker VERIFIED, L-53 Liquidation pack template VERIFIED. Note on dispatch-vs-scenario: dispatch instructions described Day 14 as a separate-tenant onboarding (padmin → KC org); the scenario file (lines 421–461) describes it as a second-client-of-same-tenant flow. Followed the scenario file (authoritative). No new tenant schema created — both clients share `tenant_5039f2d497cf`. Tenant state: 2 customers (Sipho INDIVIDUAL + Moroka TRUST), 2 substantive matters + 1 prior probe matter, Trust account total R 75,000 (Sipho 50K / Moroka 25K). Evidence: `qa_cycle/checkpoint-results/day-14.md` §"Day 14 Re-Verify — Cycle 1", 8 yml snapshots + 1 png screenshot prefixed `day-14-cycle1-*`. No new gaps. Next action: **Day 15 dispatch — Sipho portal isolation probes** using captured probe IDs.
- 2026-04-25 SAST — QA: **Day 15 cycle-1 verify COMPLETE — Sipho portal isolation HOLDS at all four layers — 18/18 PASS, 0 BLOCKER, NO LEAKS**. Method: Playwright MCP browser-driven for 15.1 list-view + 15.2 direct-URL probes; REST curl for 15.3 API hard-negative (API is the SUT per dispatch — allowed); read-only SQL SELECT for 15.4 audit. **Phase 15.1 (4 list pages browser-walked)**: `/home` empty cards "Sipho Dlamini" header (clean); `/projects` shows 3 matters all `customer_id=c3ad51f5-… (Sipho)` confirmed via DB join — Moroka matter `89201af5-…` ABSENT; `/requests` shows REQ-0001/0002/0003/0004 all `projectName=Dlamini v Road Accident Fund` — REQ-0005 (Moroka Liquidation pack) ABSENT; `/trust` auto-redirected to Sipho's RAF matter showing R 50 000,00 — no R 25 000,00 / no R 75 000,00 aggregate leak. Token-grep for `Moroka|REQ-0005|Liquidation|Estate Late|2b454c42|89201af5|83428106|8d92037c|446fa97c|182621ca|25 000|75 000|death-cert` across all 4 list yml snapshots returned 0 hits per file. **Phase 15.2 (4 direct-URL probes browser-walked)**: `/projects/89201af5-…` "The requested resource was not found." denial card; `/requests/83428106-…` "The requested resource was not found"; `/trust/89201af5-…` "No trust balance is recorded for this matter" + nested "not found" for transactions/statements (no R 25 000,00 anywhere); `/documents/8d92037c-…` Next.js 404 (route absent on portal — pre-existing GAP-P-07, functionally a denial). **Phase 15.3 (REST 8 hard-negatives + 1 positive control)**: JWT extracted from `localStorage.portal_jwt` (decoded `sub=c3ad51f5-…/type=customer/org_id=mathebula-partners/exp=1777118542`); positive control `GET /portal/projects/e788a51b-…` → 200 RAF matter (proves JWT works); Moroka probes — `GET /portal/projects/89201af5-…` 404, `GET /portal/requests/83428106-…` 404, `GET /portal/trust/matters/89201af5-…/transactions` 404, `GET /portal/documents/8d92037c-…` 404, `GET /portal/projects/89201af5-…/tasks` 404, `GET /api/projects/89201af5-…` 401 (firm endpoint correctly rejects portal JWT); list scans — `GET /portal/requests` 200 with 4 Sipho REQs only (no REQ-0005), `GET /portal/projects` 200 with 3 Sipho-bound matters only (no `89201af5`), `GET /portal/trust/summary` 200 `{"matters":[{"matterId":"e788a51b-…","currentBalance":50000.00}]}` exactly 1 matter — no `89201af5`, no `25000`, no `446fa97c`. **Phase 15.4 (read-only SQL)**: `audit_events` query for `actor_id=Sipho OR entity_id IN (Moroka entity IDs)` returns 6 rows — ALL Bob USER/API actions creating Moroka entities on Day 14; zero Sipho-actor rows against Moroka entities. **L-52 portal trust ledger does NOT leak Moroka deposit into Sipho's view** (re-confirmed). Tab 0 Bob firm + Tab 1 Sipho portal both preserved untouched on exit. Sipho JWT still live (~50 min remaining). No new gaps. Evidence: `qa_cycle/checkpoint-results/day-15.md` §"Day 15 Re-Verify — Cycle 1 — 2026-04-25 SAST"; 8 yml snapshots + 1 png screenshot prefixed `day-15-cycle1-*`. Next action: **Day 21 dispatch — firm-side time entry + disbursement + court date on RAF-2026-001**.
- 2026-04-25 SAST — QA: **Day 11 cycle-1 verify COMPLETE — GAP-L-52 VERIFIED via Playwright MCP browser walk**. Method: Tab 0 kept Thandi firm session alive; Tab 1 opened Sipho portal session via fresh REQ-0004 magic-link (old REQ-0003 link expired → re-issued via firm UI New Request → FICA Onboarding Pack → Send Now → Mailpit `X3oEh2xXMdLE9qmqLL3DHt` href `http://localhost:3002/auth/exchange?token=…&orgId=mathebula-partners`). Sipho landed at `/projects`, navigated to `/trust` → auto-redirect to `/trust/e788a51b-…`. **Trust balance card renders R 50 000,00 "As of 25 Apr 2026" / "Matter e788a51b"**; transactions table shows `25 Apr 2026 / DEPOSIT / "Initial trust deposit — RAF-2026-001" / R 50 000,00 / R 50 000,00`; ZAR currency throughout (R + comma decimal); 0 console errors (1 cosmetic Next.js scroll-behavior warning). Sanity SELECT confirms `tenant_5039f2d497cf.trust_transactions` row `0a6d1d60-…` still RECORDED (no write occurred). Cycle-1 tally: 7/8 PASS, 1 STILL-FAIL (11.1 nudge email — downgraded to LOW MINOR). PR #1117 (read-model sync) is the actual L-52 fix and it works end-to-end. **NEW GAP `MINOR-Trust-Nudge-Email-Missing` (LOW)** — no "View trust balance" / "trust deposit" email dispatched after Day-10 RECORDED deposit; possible (a) template not wired to `TrustTransactionRecordedEvent`, (b) only fires on UI-driven `recordDeposit`. Workaround: clients use sidebar Trust link. Minor portal nav (informational, not new gap): `/trust/{matterId}` "← Back to trust" link is no-op. Evidence: `qa_cycle/checkpoint-results/day-11.md` §"Day 11 Re-Verify — Cycle 1", `day-11-cycle1-portal-trust-view.yml`, `day-11-cycle1-portal-trust-view.png`, `day-11-cycle1-firm-customer-detail.yml`. Live state at end-of-turn unchanged except: REQ-0004 row added on RAF matter (FICA pack, status SENT) — used solely to obtain a fresh magic-link; can be cancelled or left as-is. Next action: **Day 14 dispatch — Thandi onboards Moroka Family Trust (firm-side seeding for Day 15 isolation probe)**.
- 2026-04-25 SAST — QA: **Day 21 cycle-1 verify PARTIAL — L-57 VERIFIED browser-driven; new HIGH/BLOCKER GAP-L-61 opened on approval transition**. Method: Playwright MCP browser-driven for all UI; read-only SQL `SELECT` for state confirmation. Tab 0 (Bob firm KC session) alive throughout; Tab 1 (Sipho portal) preserved untouched. **Phase A (matter nav)**: `/projects` → "Dlamini v Road Accident Fund" card → matter detail loaded clean (full UUID resolved as `e788a51b-3a73-456c-b932-8d5bd27264c2`; dispatch's `e788a51b-3027-…` was wrong — that ID 500s on detail load). **Phase B (L-57 dialog)**: Disbursements tab → "+ New Disbursement" → dialog hydrates with Matter combobox showing all 3 matters (Estate Late Peter Moroka / L-37 Regression Probe / Dlamini v Road Accident Fund) and RAF auto-pre-selected (combobox `[disabled]` because matter is locked from matter-tab context — correct UX); Customer combobox lists Sipho + Moroka; Category lists 9 SA-legal categories; VAT auto-flips to "Zero-rated pass-through" on Sheriff Fees (correct SA legal-disbursement treatment). Filled form, zero `/api/projects` 404s in console. **Phase C (submit)**: Create Disbursement → dialog closes → list shows new row "2026-04-25 / — / Sheriff Fees / R 1 250,00 / Draft / Unbilled". DB confirms `legal_disbursements` row `bb9ee2ac-b1e5-4e2f-bf43-e40a63809530` inserted (project_id=`e788a51b-3a73-…`, customer_id=`c3ad51f5-…` Sipho, amount=1250.00, vat_treatment=ZERO_RATED_PASS_THROUGH, approval_status=DRAFT, billing_status=UNBILLED, supplier_name="Sheriff Pretoria", supplier_reference="SHF-RAF-2026-001"). **Phase D (approval BLOCKED)**: walked detail page + matter-tab Actions menu + org-level list — **NO "Submit for Approval" / "Approve" / "Submit" button anywhere**. Detail-page header buttons enumerated via DOM = `["Upload receipt", "Edit"]`. Source confirms: `disbursement-approval-panel.tsx:55-57` only renders for `approvalStatus==="PENDING_APPROVAL"`; `submitForApproval(id)` API client helper exists in `frontend/lib/api/legal-disbursements.ts:167` but has zero callers in `frontend/app` or `frontend/components`. Backend route + entity method are wired. **GAP-L-61 OPENED (HIGH/BLOCKER for L-63 / Day 28 fee-note)** — disbursement stuck at DRAFT/UNBILLED; `DisbursementRepository.findUnbilledBillableByCustomerId` requires `approvalStatus='APPROVED'`, so the Day-28 fee-note dialog will see zero unbilled disbursements. **L-63 verify on Day 28 is gated on L-61 fix.** Approval was NOT performed via REST per HARD rule (state mutations through browser UI only). Suggested fix scope <1hr frontend-only: add "Submit for Approval" CTA on disbursement-detail-client.tsx visible when `approvalStatus==="DRAFT"`, wire to new `submitForApprovalAction` server action. Verify-focus this turn: **L-57 VERIFIED end-to-end browser-driven** (was REST-only in prior log); **L-63 CANNOT VERIFY** — gated on L-61. No regressions on L-22 / L-26 / L-37-regression / L-58. **Time entries (21.1–21.5) and Court Date (21.10–21.12) phases NOT re-walked this turn** — already VERIFIED in prior log on different evidence rows; this turn was scoped to disbursement flow only per dispatch (L-57 + L-63 setup). Day 21 evidence row in DB awaiting L-61 fix: `bb9ee2ac-b1e5-4e2f-bf43-e40a63809530`. Evidence: `qa_cycle/checkpoint-results/day-21.md` §"Day 21 Re-Verify — Cycle 1 — 2026-04-25 SAST"; 7 yml snapshots + 3 png screenshots prefixed `day-21-cycle1-*`. Next action: **Product → Dev triage GAP-L-61** → after fix lands, QA re-runs Day-21 Phase D to approve `bb9ee2ac-…` browser-driven, then proceeds to Day 28 fee-note dispatch.
- 2026-04-25 SAST — Product triaged GAP-L-61 → SPEC_READY. Spec at `qa_cycle/fix-specs/GAP-L-61.md`. Chosen approach: **frontend-only, two-file** edit — (a) add `submitForApprovalAction` server action in `frontend/app/(app)/org/[slug]/legal/disbursements/actions.ts` mirroring `approveDisbursementAction`'s shape but gated on `MANAGE_DISBURSEMENTS` (submitter capability) not `APPROVE_DISBURSEMENTS` (approver capability); (b) add "Submit for Approval" button in `frontend/app/(app)/org/[slug]/legal/disbursements/[id]/detail-client.tsx` header rendered when `approvalStatus === "DRAFT"` (NOT the existing `editable` flag — `editable` covers both DRAFT and PENDING_APPROVAL, and the Approve/Reject panel already takes over for PENDING_APPROVAL, so reusing `editable` would double-render). Validated against alternatives: (1) reusing `editable` flag rejected (overlap with approval-panel render gate at PENDING_APPROVAL); (2) adding the CTA on the list-view rejected as out-of-scope (detail-page CTA is sufficient to unblock L-63; list CTAs are a Sprint-2 UX nicety); (3) putting submit logic in the approval-panel component rejected (panel's purpose is approver-side actions, not submitter-side — separation of concerns matches backend's distinct capabilities). Dispatch wording said path was `/submit-for-approval`; confirmed via direct read of `DisbursementController.java:82-86` that the actual route is `/submit` (verb-only) — the `submitForApproval(id)` API helper at `legal-disbursements.ts:167-169` already targets `/submit` correctly, no backend or helper changes needed. Est S (≤30 min, ~65 lines, two files); NEEDS_REBUILD=false (frontend HMR picks up automatically; no Java change, no migration). Dev cleared to implement. Next action: Dev → implement L-61 fix per spec → after merge, QA re-runs Day-21 Phase D against `bb9ee2ac-…` browser-driven and proceeds to Day 28 fee-note dispatch.
- 2026-04-25 SAST — Dev: GAP-L-61 **FIXED** in PR #1133 (merge SHA `9a7fcad2`, squash-merged into `bugfix_cycle_2026-04-24`). Frontend-only two-file edit per spec verbatim: (a) `frontend/app/(app)/org/[slug]/legal/disbursements/actions.ts` — added `submitForApproval as submitForApprovalApi` import, `hasManageDisbursementCapability` helper, and `submitForApprovalAction(slug, id)` server action gated on `MANAGE_DISBURSEMENTS` and revalidating list/detail/project paths; (b) `frontend/app/(app)/org/[slug]/legal/disbursements/[id]/detail-client.tsx` — added `Send` lucide icon import + `submitForApprovalAction` import; added `isSubmittingForApproval` / `submitError` state and `handleSubmitForApproval` callback; rendered "Submit for Approval" button in header gated on `approvalStatus === "DRAFT"` (NOT `editable`, per spec) with `data-testid="disbursement-submit-for-approval-button"`; inline `role="alert"` error below the button row. Build matrix: lint=0 errors / 85 pre-existing warnings unchanged; build=PASS (`Compiled successfully in 23.0s`); Vitest=332 files / 2072 tests pass / 2 skipped. **NEEDS_REBUILD: false** — frontend HMR picks the change up automatically; no backend, no migration, no service restart. Next action: **QA re-runs Day-21 Phase D + proceeds to Day 28** — submit `bb9ee2ac-…` for approval browser-driven → Bob approves → status flips to APPROVED → Day 28 fee-note dispatch (L-63 verify) unblocked.
- 2026-04-25 SAST — QA: **Day 28 cycle-1 verify COMPLETE — GAP-L-62 + GAP-L-63 BOTH VERIFIED end-to-end browser-driven; fee-note flow works through draft → approve → send**. Method: Playwright MCP browser-driven for every state mutation (open dialog, fetch unbilled, save draft, approve, send, edit customer); read-only SQL `SELECT` for state confirmation; **no REST mutations**. Tab 0 (Bob KC firm session) reused from prior L-61 verify; Tab 1 (Sipho portal) preserved untouched. **Pre-state**: Sipho `c3ad51f5-2bda-4a27-b626-7b5c63f37102` INDIVIDUAL/PROSPECT with `tax_number=NULL` (the L-62 hybrid condition); disbursement `bb9ee2ac-…` APPROVED+UNBILLED R 1 250 Sheriff Fees (from L-61 verify); 0 invoices, 0 time entries on RAF matter. **Phase C (open dialog)**: `/customers/c3ad51f5-…?tab=invoices` → Fee Notes subtab → New Fee Note → Generate Fee Note dialog opens (no L-62 hard-block prerequisite, regression vs Day-28 cycle-0 where this was the gap). **Phase D (L-63 verified)**: Filled From=2026-04-01 / To=2026-04-30 / Currency=ZAR → Fetch Unbilled Time → Select Unbilled Items step shows "No unbilled time entries found for this period" + **"Disbursements" section** with Sheriff Fees R 1 250,00 auto-checked (L-63 PR #1116 fix CONFIRMED). **Phase E (L-62 soft-warn verified)**: Click Validate & Create Draft → "Pre-generation checks" panel: ✓ Organization name set / ✓ Customer required fields filled / ⚠ "Tax Number is required to send an invoice" + amber banner "Tax number missing — This draft can be saved, but the invoice cannot be sent until a tax number is added to the customer profile" + button "Create Draft (1 issues)" still enabled. Click → draft saved as DRAFT, R 1 250,00 ZAR (L-62 soft-warn semantics PR #1126 CONFIRMED). DB: invoice `8f718728-5fb6-40fe-abf1-2cafc86c0f10` DRAFT, customer=Sipho. **Phase F (L-62 hard-enforce verified)**: Navigated to `/invoices/8f718728-…` → click Approve → status DRAFT→APPROVED (`INV-0001` assigned) → click Send Fee Note → in-page validation panel "Validation issues found / The following issues were found. As an admin/owner, you can override and send anyway. / ✗ Tax Number is required to send an invoice / Send Anyway / Cancel". Send blocked at UI level (admin override available per spec; backend `/api/invoices/{id}/send` would 422 for non-admin). L-62 hard-enforce PR #1126 CONFIRMED. **Phase G (auto-populate)**: N/A this turn — auto-populate fires only in Create Client wizard for new INDIVIDUAL with idNumber; Sipho was created pre-L-62 and the Edit Customer dialog correctly does NOT auto-populate (would be unwanted side-effect on existing rows). Defer to next fresh-INDIVIDUAL Day-2 walk. **Phase H (happy-path)**: Cancel validation banner → Edit Customer dialog → typed `8501015800088` (SA ID number, also serves as tax_number for natural persons) into Tax Number field → Save Changes → DB confirms persisted. Re-navigated to invoice → click Send Fee Note → no validation banner → status APPROVED→**SENT**, header buttons now Preview/Record Payment/Void; "Issued: Apr 25, 2026". **Phase I (DB confirmation)**: `invoices.8f718728-…` = SENT / INV-0001 / R 1 250,00 ZAR. `legal_disbursements.bb9ee2ac-…` = approval_status=APPROVED, billing_status=**BILLED** (flipped from UNBILLED), invoice_line_id=`326fb6c5-d25b-4911-9ced-b75a6c5a23ee` (linked to the new invoice). GAP-L-62 transitions FIXED → VERIFIED. GAP-L-63 transitions FIXED → VERIFIED. GAP-L-60 (PROSPECT-lifecycle gate on CREATE_INVOICE) HOLDS — draft created against PROSPECT customer without lifecycle gate firing (PR #1114 fix intact). **L-27 VAT/ZAR labels HOLD** end-to-end (R 1 250,00 with comma decimal; ZAR currency). **Day 28 summary**: 7/8 PASS (Phase G N/A by design; Mailpit email-on-send body inspection deferred to Day 30 verify). No new gaps. State at end-of-turn: Sipho tax_number=8501015800088, invoice INV-0001 SENT, disbursement BILLED, RAF matter `e788a51b-…` ACTIVE, Tab 0+Tab 1 preserved. Evidence: `qa_cycle/checkpoint-results/day-28.md` §"Day 28 Re-Verify — Cycle 1"; 6 yml + 5 png snapshots prefixed `day-28-cycle1-*`. Next action: **Day 30 dispatch — Sipho portal pays fee note via PayFast sandbox**.
- 2026-04-25 11:57 UTC — QA: **GAP-L-61 VERIFIED end-to-end browser-driven; Day 21 cycle-1 verify COMPLETE**. Method: Playwright MCP browser-driven for every state mutation; only DB read was a `SELECT`; **no REST mutations** per HARD rule. Tab 0 (Bob KC firm session) reused from prior turn — no re-login needed. Tab 1 (Sipho portal) untouched. **Phase A**: detail page at `/org/mathebula-partners/legal/disbursements/bb9ee2ac-…` loaded clean — status badges `Draft / Unbilled`, supplier "Sheriff Pretoria · Ref SHF-RAF-2026-001". **Phase B+C ("Submit for Approval" CTA visible)**: header now shows three buttons `["Submit for Approval", "Upload receipt", "Edit"]` (was `["Upload receipt", "Edit"]` pre-fix). PR #1133's `data-testid="disbursement-submit-for-approval-button"` confirmed. **Phase D (Submit)**: browser-click on CTA → page revalidated, badge transitioned `Draft → Pending`, "Submit for Approval" button disappeared, `DisbursementApprovalPanel` rendered with copy "This disbursement is pending approval. Review the amount and supplier details before approving, or reject with a reason." + Approve/Reject buttons. 0 console errors during transition. **Phase E (Approve)**: browser-click Approve → modal dialog "Approve Disbursement" opened with optional Notes textarea + Cancel/Approve Disbursement buttons; browser-click confirm "Approve Disbursement" → status flipped `Pending → Approved`, panel disappeared. **Phase F (DB)**: `SELECT id, approval_status, billing_status, approved_at, approved_by FROM tenant_5039f2d497cf.legal_disbursements WHERE id='bb9ee2ac-b1e5-4e2f-bf43-e40a63809530'` → `approval_status='APPROVED' / billing_status='UNBILLED' / approved_at=2026-04-25 11:57:03.055511+00 / approved_by='5fabd245-0cc3-4f70-8605-3f4e2a369140'`. Approver resolved via `tenant_5039f2d497cf.members` → Bob Ndlovu (`bob@mathebula-test.local`). **Phase G (L-63 setup)**: predicate `findUnbilledBillableByCustomerId` (filters APPROVED+UNBILLED) now satisfied — Day 28 fee-note dialog L-63 verify is unblocked. Console: 0 errors / 0 warnings across 122 messages during all transitions. GAP-L-61 transitions FIXED → VERIFIED. 9/9 Phase D checkpoints PASS. Evidence: `qa_cycle/checkpoint-results/day-21-cycle1-l61-fixed-{detail-draft,submitted,approved}.{yml,png}` (3 yml + 3 png); `qa_cycle/checkpoint-results/day-21.md` §"Day 21 Phase D Re-Verify (after L-61 fix) — Cycle 1 — 2026-04-25 SAST". Next action: **Day 28 fee-note dispatch — L-62 verify (tax_number hybrid: INDIVIDUAL auto-populate, soft-warn at draft, hard-enforce at send) + L-63 verify (fee-note dialog should surface the now-APPROVED Sheriff Fees R 1 250 unbilled disbursement on Sipho's RAF matter)**.
- 2026-04-25 14:30 SAST — QA: **Day 30 cycle-1 verify PARTIAL — HALTED on PayFast prerequisite per scenario hard rule. NEW HIGH GAP-L-64 (PayFast not configured) + LOW GAP-L-65 (portal terminology drift) opened.** Method: Playwright MCP browser-driven for all UI; Mailpit GET for email content (legitimate); read-only SQL `SELECT` for DB confirmation; **no payment fakery via REST or SQL**. Tab 0 (Bob KC firm session) preserved. Tab 1 (Sipho portal) was JWT-expired — re-issued fresh magic-link via portal `/login?orgId=mathebula-partners` → typed `sipho.portal@example.com` → "Send Magic Link" → portal returned dev-mode link inline → clicked → fresh portal JWT. **Phase A (email arrival, 30.1)**: Mailpit shows `Invoice INV-0001 from Mathebula & Partners` to `sipho.portal@example.com` at 12:12 UTC. Body lists Invoice Number `INV-0001`, Due Date `N/A`, Amount Due `ZAR 1250.00`. Pay-Now block correctly suppressed (`paymentUrl` null). **However, "View Invoice" CTA renders with `href=""` (empty)** — link should always go to portal URL even when no PSP configured (logged as part of GAP-L-64). Subject says "Invoice" not "Fee Note" → terminology drift (GAP-L-65). **Phase B (portal nav, 30.2)**: `/invoices` renders one row `INV-0001 SENT 25 Apr 2026 R 1 250,00 View Download`. Click into detail → header `INV-0001` SENT, line item "Sheriff fees: Sheriff service of summons on RAF — Day 21 cycle-1 verify (Sheriff Pretoria, 2026-04-25)" / Qty 1 / R 1 250,00 / Zero-rated 0% / R 1 250,00. Subtotal R 1 250,00 / Zero-rated (0%) R 0,00 / **Total R 1 250,00**. Download PDF button present. **Phase C (Pay button MISSING, 30.5 BLOCKER)**: instead of Pay/Pay Now, page shows fallback panel "Contact Mathebula & Partners to arrange payment". Source: `portal/app/(authenticated)/invoices/[id]/page.tsx:155-160` `{invoice.status === "SENT" && !invoice.paymentUrl && (...)}`. **Root cause**: `tenant_5039f2d497cf.org_integrations` has 0 rows → `IntegrationRegistry.resolve(IntegrationDomain.PAYMENT, PaymentGateway.class)` falls back to `NoOpPaymentGateway` (`@IntegrationAdapter(domain=PAYMENT, slug="noop")`). NoOp's `createCheckoutSession()` returns `CreateSessionResult.notSupported(...)`. `PaymentLinkService.generatePaymentLink` (lines 62-68) exits early without setting `payment_url` → `Invoice.payment_url=NULL` → portal hides Pay button. Scenario step 0.G ("Confirm PayFast sandbox credentials are configured in OrgIntegration seed data OR set up a firm-side stub adapter for Day 30") confirms this is a known prerequisite that was never satisfied. **Phase D (firm-side sanity, 30.7-cross-check)**: Tab 0 → `/org/mathebula-partners/invoices/8f718728-...` shows INV-0001 still SENT; "Payment History — No payment events yet"; firm-side actions "Preview / Record Payment / Void" available (manual record-payment would bypass the scenario-mandated PayFast flow — NOT used). **Phase E (DB confirmation)**: `invoices.8f718728-… payment_url=NULL, payment_session_id=NULL, paid_at=NULL, status=SENT` ; `payment_events count=0` for this invoice; `org_integrations count=0` for the tenant. Schema note: actual table is `payment_events` (not `invoice_payments` from prompt). **Phase F (isolation 30.11)**: PASS — portal `/invoices` shows only Sipho's INV-0001; no Moroka invoices visible (tenant has no other invoices yet at this scenario point). **Decision**: PER SCENARIO HARD RULE ("If PayFast not configured / sandbox unreachable, log as gap and HALT — don't fake payment via SQL or REST") — HALTED here. **GAP-L-64 OPENED (HIGH/BLOCKER for Day 30 PayFast flow)** — fixable surfaces: (1) seed PayFast sandbox `OrgIntegration` row on legal-za pack install OR wire dev-only `MockPaymentGateway`; (2) email "View Invoice" link should always render portal URL regardless of `paymentUrl`. **GAP-L-65 OPENED (LOW — portal terminology drift)** — portal/email render "Invoice"/"Invoices" while firm side correctly uses vertical's "Fee Note" override. Suggested scope: propagate `terminology.invoice` override into portal layout + email templates. Day 30 PASS items: 30.2 detail render, 30.4 screenshot, 30.11 isolation. Tab 0 + Tab 1 preserved. Evidence: `qa_cycle/checkpoint-results/day-30.md` §"Day 30 Re-Verify — Cycle 1"; 4 yml + 3 png snapshots prefixed `day-30-cycle1-*`. Next action: **Product → Dev triage GAP-L-64** → after fix lands, QA re-runs Day 30 from 30.5 to walk PayFast sandbox redirect end-to-end. Day 45 (firm: second info request + second trust deposit) is next sequential scenario step and does NOT depend on Day 30 payment landing — could proceed in parallel if Product prefers a parallel track. Per dispatch instruction "Do NOT advance past Day 30", staying paused awaiting triage.

- 2026-04-25 SAST — Product triaged GAP-L-64 → **SPEC_READY**; GAP-L-65 → **WONT_FIX (this verify cycle) / DEFERRED to Sprint 2**. Spec at `qa_cycle/fix-specs/GAP-L-64.md`. **L-64 decision: FIX-IN-CYCLE.** Authority: scenario step 0.G verbatim — "Confirm PayFast sandbox credentials are configured in `OrgIntegration` seed data **OR set up a firm-side stub adapter for Day 30** — document which path is in use" (`qa/testplan/demos/legal-za-full-lifecycle-keycloak.md:105`). The stub-adapter path is explicitly authorised. Chose dev-only `MockPaymentGateway` (slug=`mock`, `@Profile({local,dev,keycloak,test})`) + Java-side seed of `OrgIntegration(domain=PAYMENT, provider_slug=mock, enabled=true)` in `PackReconciliationRunner` on legal-za install (gated on non-prod profile, idempotent — no Flyway migration since Flyway can't read profiles) + dev `/dev/mock-payment` page POSTing to a profile-guarded `/api/payments/mock/complete` that drives `PaymentWebhookService.handleWebhook(...)` synchronously → invoice flips SENT→PAID with a real `payment_events` row. Bundled Surface B: email template `portalUrl` always populated regardless of `paymentUrl` state (templates/email/invoice-delivery.html:51 `th:href="${portalUrl}"` is currently empty because the model passes empty `portalUrl` — fix in `InvoiceDeliveryEmailService` to always set `portalBaseUrl + "/invoices/" + invoiceId`, mirroring L-50 portal-base-url resolution). **Why fix-in-cycle, not defer:** blast radius cascades through Day 60.5 closure gate ("no unpaid fee notes" GREEN required → unpaid INV-0001 fails → matter cannot CLOSE clean-path), Day 61.4 SoA reconciliation ("Day 30 fee note paid (amount)" line item), Day 88.4 activity trail ("fee-note paid (Day 30)"), and exit checkpoint **E.12** ("Fee note + payment flow — Day 28 generation + Day 30 PayFast sandbox payment completes end-to-end; firm PAID status reflects within 60s") — demo-blocking gate. The mock adapter exercises the full IntegrationRegistry resolution path, payment_url plumbing, portal Pay-button render, redirect roundtrip, and webhook reconciliation — only PayFast cryptographic signature verification is NOT covered (acceptable for a verify cycle whose scope is "re-validate 40+ shipped fixes"). Real PayFast sandbox (`merchant_id`/`merchant_key`/`passphrase` env wiring + ngrok-tunneled `notify_url` reachable from `sandbox.payfast.co.za`) deferred to **Sprint 2 as L-64-followup**. Est M (~3–4 hr; 2 NEW Java classes, 1 NEW frontend dev page, 1 hook in PackReconciliationRunner, 1 email-service fix, 2 tests). NEEDS_REBUILD: true. **L-65 decision: DEFER.** Reasoning: exit checkpoint **E.9** reads "Terminology sweep passed — zero `Project/Customer/Invoice` leaks **firm-side**; **portal terminology consistent within itself**". Portal IS internally consistent (renders "Invoice"/"Invoices" everywhere on portal-side, no firm-side legal vocabulary leaked into portal). The firm↔portal seam divergence is not gated by E.9. Verify-cycle scope is "re-validate 40+ shipped fixes" — propagating `legal-za` `terminology.invoice` override into portal layout + email templates is genuinely NEW functionality (portal-side terminology resolver does not exist; firm-side wiring in `(app)/org/[slug]` chrome was scoped firm-only by design). Fix needs portal-side org-terminology query + `<Trans>`-style helper + email-template token expansion — at least M effort; doesn't fit verify-cycle scope. Tracked as Sprint 2 "L-65-followup". Both gaps moved on the tracker; both Sprint-2 followups added to "Deferred to future phases" section. Next action: **Dev — implement GAP-L-64 per spec**. After Dev merges + Infra restarts backend, QA re-runs Day 30 from 30.1 (email href) → 30.5 (Pay button) → 30.6 (mock-payment success page) → 30.7 (firm flip to PAID within 60s) → 30.10 → 30.11. Day 45 (firm: second info-req + second trust deposit) does NOT hard-depend on Day 30 PAID — can run in parallel if backend rebuild is in flight. Per dispatch instruction, advancing scenario position to Day 45 is reasonable since L-64 fix-spec is captured; QA should pick up Day 45 next while Dev implements.

- 2026-04-25 SAST — Dev + Infra (orchestrator-driven completion): **GAP-L-64 FIXED in PR #1134 (merge SHA `34493c79`) + backend restarted + seeder confirmed**. Implementation per spec: `MockPaymentGateway.java` (`@Profile({local,dev,keycloak,test})`, 185 lines, implements PaymentGateway, generates MOCK-SESS- session URLs pointing to `/dev/mock-payment`); `MockPaymentController.java` (`@Profile({local,dev,keycloak})`, excludes test profile, 178 lines, drives `PaymentWebhookService.handleWebhook` synchronously); `MockPaymentIntegrationSeeder.java` (runtime profile-gate via `Environment.getActiveProfiles`, idempotent — never overwrites configured PSP, hooked into `PackReconciliationRunner.reconcileForLegalZa`); `templates/portal/mock-payment.html` (53-line sandbox page with Complete + Fail buttons); `MockPaymentGatewayTest` 11/11 PASS; `InvoiceEmailService` modified to always set `portalUrl = portalBaseUrl + "/invoices/" + invoiceId` regardless of paymentUrl state (fixes empty `href=""` CTA). Backend restarted via `svc.sh restart backend` (PID 80950→41837, ready 27s). Seeder log confirmed: "Seeded mock PAYMENT integration for legal-za tenant tenant_5039f2d497cf (dev profile only)". DB verified: `tenant_5039f2d497cf.org_integrations` now has 1 row id `9775a954-96b8-4db1-89c6-ad5c7dff7fb9` PAYMENT/mock/enabled=true (was 0 rows before). NEEDS_REBUILD=false (just done). PR #1134 needed conflict resolution on parent merge (qa_cycle/{status.md, day-00.md} took --theirs from parent — Dev worktree was based on older parent commit and parent had advanced through 4 QA dispatches in this session). Profile gating audit: Mock paths cannot be reached in `prod` profile (gateway+controller `@Profile`, seeder runtime check). Real PayFast (Sprint 2 L-64-followup) tracked in deferred section. Next action: **QA re-walks Day 30 phases 30.1 (email `href` non-empty) → 30.5 (Pay button visible) → 30.6 (`/dev/mock-payment` redirect + Complete) → 30.7 (firm flip to PAID within 60s) → 30.8 / 30.10**.

- 2026-04-25 SAST — QA: **Day 45 cycle-1 verify COMPLETE.** 5/5 substantive PASS + 1 PARTIAL (45.1 forced template-substitute path due to NEW GAP-L-67); 0 BLOCKER. Method: Playwright MCP browser-driven for every state mutation; READ-ONLY SQL `SELECT` for state confirmation; Mailpit `/api/v1/messages` for email body (legitimate REST). **No SQL/REST mutations.** Tab 0 (Bob Ndlovu KC firm session) used throughout — re-authenticated mid-session after browser/MCP closed connection (cleaned stale `mcp-chrome-5d273ba` SingletonLock and reattached via `mcp__playwright__*` main namespace). **Phase A (45.1)**: tried free-form first — Save as Draft on Ad-hoc template created REQ-0006 (`44d6d8df-…`) with no title/items; detail page exposed only Contact/Reminder/Sent/Due metadata and a Cancel-only action menu — **no UI affordance to add items or to Send a 0-item draft**. Cancelled REQ-0006 → opened **GAP-L-67 HIGH**. Pivoted to FICA Onboarding Pack template (3 items) + due 2026-05-02 + Send Now → **REQ-0007 (`454dea5d-…`) SENT** to RAF/Sipho. **Phase B (45.2)**: Mailpit `cjjViD23s8SxAvfGJpL7Ln` "Information request REQ-0007 from Mathebula & Partners" → `sipho.portal@example.com` href = `http://localhost:3002/auth/exchange?token=g1Zpu-G-CYVAKXnoMvlQvCbmiSfHBNyODeW1Oy1TAbA&orgId=mathebula-partners` — port 3002 ✓ token ✓ orgId ✓. **L-42 magic-link fix HOLDS for second info request.** **Phase C (45.3)**: Trust Accounting → Record Transaction → Record Deposit → filled Client `c3ad51f5-…` Matter `e788a51b-…` Amount 20000 Reference DEP-2026-002 Description "Top-up per engagement letter — RAF-2026-001" → Record Deposit → DB confirms `trust_transactions.ce6767c1-4433-4f76-b5e6-acda100ef293` DEPOSIT R 20 000,00 RECORDED. (Cosmetic UX-debt: dialog uses raw UUID textboxes, no autocomplete combobox.) **Phase D (45.4)**: Client Ledgers page → Sipho row `R 70 000,00 / R 70 000,00 / 25 Apr 2026`, Moroka row unchanged `R 25 000,00`. Sipho ledger detail Transaction History "2 transactions found" — DEP-2026-001 R 50 000 + DEP-2026-002 R 20 000 both visible RECORDED. Trust Accounting overview total R 95 000,00 (50K + 20K + 25K Moroka unchanged). **Phase E (45.5)**: RAF matter detail → Trust tab → Trust Balance card R 70 000,00 / Funds Held / Deposits R 70 000,00. **Phase F (cross-checks)**: Console 0 errors (1 cosmetic warning unrelated); Moroka client_ledger_card unchanged (cross-customer isolation HOLDS); MINOR-Trust-Nudge-Email-Missing carries forward unchanged (Mailpit shows ONLY the REQ-0007 email — no trust-deposit nudge). **NEW gaps opened this turn**: (1) **GAP-L-67 HIGH** — no UI affordance for ad-hoc info request items (frontend-only fix, M ~2-3 hr); (2) **GAP-L-66 LOW** logged from Day 30 side-finding (MockPaymentGateway `appBaseUrl` host config mismatch). Verify-focus this turn: L-41 (due_date) re-VERIFIED, L-42 (magic-link) re-VERIFIED. Backend PID rotated 41837→41678 between L-64 verify and Day 45 (noted in environment row). DB final: 3 trust_transactions, 2 client_ledger_cards (Sipho R 70K / Moroka R 25K), 7 information_requests (REQ-0001..0007 with REQ-0006 Cancelled). Day 45 evidence: `qa_cycle/checkpoint-results/day-45.md` + 13 yml/png snapshots prefixed `day-45-cycle1-*`. Next action: **Day 46 dispatch — Sipho portal responds to REQ-0007 + portal trust-ledger re-check showing R 70 000,00 + late-cycle isolation spot-check (Moroka data should remain invisible)**. Day 45 outcome does NOT block Day 46 progression; GAP-L-67 is firm-side only and can be triaged in parallel.

- 2026-04-25 14:09 SAST — QA: **Day 30 cycle-1 re-walk COMPLETE — GAP-L-64 VERIFIED end-to-end browser-driven.** Tooling: `mcp__plugin_playwright_playwright__*` (plugin Playwright; main `mcp__playwright__*` namespace remained wedged from prior session). Re-auth: Keycloak Bob `bob@mathebula-test.local` / `SecureP@ss2` on Tab 0 (note prior status had wrong username `bob.ndlovu@…`); Sipho via portal `/login?orgId=mathebula-partners` magic-link self-service on Tab 1 (token `gLoUu3jzZrGirEIPhFcWfLU4gfQXu9rF4RB2xAsM29o`). **Path taken: Option B (clean test).** Option A discarded — firm-side INV-0001 detail page exposes only Preview/Record-Payment/Void; no Resend/Regenerate-Link affordance; INV-0001 created pre-seeder so `payment_url` permanently NULL. Option B end-to-end: (1) Dlamini matter → Disbursements → "New Disbursement" R 100 Court Fees / Magistrate Court Pretoria / 2026-04-25, customer Sipho, payment-source Office, VAT zero-rated → disb id `361c56c4-…` Draft/Unbilled. (2) Disb detail → Submit-for-Approval → Pending → Approve modal → Approved/Unbilled. (3) Sipho customer → Fee Notes → New Fee Note → Fetch Unbilled Time → R 100 disbursement auto-included → Validate & Create Draft → Create Draft → INV-0002 Draft R 100. (4) INV-0002 → Approve → Approved → Send Fee Note → **Sent**. Sent state renders: header chip `Sent`, "Online Payment Link" panel with full URL `http://localhost:3000/portal/dev/mock-payment?sessionId=MOCK-SESS-11530fc8-eea7-4bb1-ad0d-7c5bb60093a1&invoiceId=62c56974-b633-43ec-b2d7-73406a43df08&amount=100.00&currency=ZAR&returnUrl=...payment-success`, Copy Link / Regenerate buttons, Payment History row `Created mock — R 100,00`. **Phase 3 (30.1) PASS**: Mailpit msg `kCG5QGqqQdquPPVqdmL2xQ` (`Invoice INV-0002 from Mathebula & Partners`) HTML body `<a href="http://localhost:3002/invoices/62c56974-…">View Invoice</a>` (non-empty) + Pay-Now block with mock-payment URL. **Phase 4 (30.5) PASS**: Sipho portal `/invoices/62c56974-…` renders green "Pay Now" link with mock-payment URL. **Phase 5 (30.6) PASS**: Pay Now opens new tab to `localhost:3000/portal/dev/mock-payment?…` which **404s on Next.js frontend** (side-finding — Mock checkout controller is on backend :8080 not frontend :3000; appBaseUrl misconfigured). Bypassed by browsing directly to `localhost:8080/portal/dev/mock-payment?<same query>` — Mock Payment Checkout DEV ONLY page renders (Invoice 62c56974, ZAR 100.00, Session MOCK-SESS-11530fc8, Simulate Successful + Simulate Failed buttons). Click Simulate Successful → 302 redirect to portal `/invoices/62c56974-…/payment-success` → "Payment confirmed — Payment received — thank you! Paid on 25 Apr 2026" + View Invoice CTA. **Phase 6 (30.7) PASS**: firm-side INV-0002 detail refresh → status flipped `Sent → Paid`; "Payment Received" banner `Paid on: Apr 25, 2026`, `Reference: MOCK-PAY-b98dcf15-25c3-4aca-ae44-6d8e6dc5d149`; Payment History 2 rows (`Completed mock MOCK-PAY-b98dcf15-… R 100,00 Apr 25, 2026` + `Created mock — R 100,00 Apr 25, 2026`). **Phase 7 (DB confirm)**: `invoices.62c56974-… status=PAID, payment_url=<mock URL>, paid_at=2026-04-25 14:08:43.863155+00`; `payment_events` 2 rows: `81cccfb0-… CREATED 100.00 14:04:42.166262+00`, `01e0c0bc-… COMPLETED 100.00 payment_reference=MOCK-PAY-b98dcf15-… 14:08:43.877885+00`. **Phase 8 (console + isolation) PASS**: 0 errors via `browser_console_messages level=error`; portal `/invoices` lists exactly Sipho's INV-0001 (SENT) + INV-0002 (PAID); tenant has 0 invoices for any other customer (verified by `SELECT id, status, name FROM invoices JOIN customers …`). All 6 L-64 checkpoints PASS (30.1, 30.2, 30.4, 30.5, 30.6, 30.7, 30.11). **Side-finding (NOT a Day 30 blocker, recommend new GAP-L-66 LOW)**: `MockPaymentGateway` uses `appBaseUrl=${APP_BASE_URL:http://localhost:3000}` (Next.js frontend origin) for the hosted-checkout URL but the Spring controller is mapped at `/portal/dev/mock-payment` on backend port 8080 → direct user click on Pay Now 404s in the Next.js frontend (no rewrite). Fix is one of: (a) change `MockPaymentGateway` to use a backend/gateway origin, or (b) add a Next.js rewrite. This bug pre-dates L-64 — would have hit on first end-to-end click even if the org_integrations seed had been correct from day one. Workaround: open URL after rewriting host to :8080 (used by QA this turn). Logged here for follow-up. INV-0001 left unchanged with NULL payment_url as evidence of pre-fix state; INV-0002 PAID as evidence of post-fix state. Evidence: `qa_cycle/checkpoint-results/day-30.md` §"Day 30 Re-walk after L-64 fix — Cycle 1 — RETRY" + 4 PNG snapshots (`day-30-cycle1-l64-verified-firm-inv0002-sent-with-payment-link.png`, `day-30-cycle1-l64-verified-portal-pay-now-rendered.png`, `day-30-cycle1-l64-verified-mock-payment-checkout.png`, `day-30-cycle1-l64-verified-portal-payment-success.png`, `day-30-cycle1-l64-verified-firm-inv0002-paid.png`). Next action: QA — Day 45 (firm: second info-req + second trust deposit on Moroka).

- 2026-04-25 (earlier) SAST — QA: **Day 30 cycle-1 re-walk attempt — HALTED on tooling unavailability.** Pre-state DB confirmation completed (read-only SELECT, no mutations): `tenant_5039f2d497cf.invoices.8f718728-…` still `SENT, payment_url=NULL, payment_session_id=NULL`; `tenant_5039f2d497cf.org_integrations` has the seeded `9775a954-… PAYMENT/mock/enabled=true` row from PR #1134 (confirms seeder ran). Existing INV-0001 was created BEFORE the seeder so its payment_url remains NULL — re-walk would have driven Option A (firm-side regenerate via "Resend Invoice" / view-trigger) or Option B (fresh fee-note via new disbursement-billing) per dispatch. Browser-driven phases NOT EXECUTED because **all three browser bridges are unavailable**: `mcp__playwright__browser_*` returns `Target page, context or browser has been closed`; `mcp__plugin_playwright_playwright__browser_*` returns `Browser is already in use for /Users/rakheendama/Library/Caches/ms-playwright/mcp-chrome-5d273ba` (wedged user-data dir held by Chrome PID 5426, can't release without external intervention); `mcp__claude-in-chrome__tabs_context_mcp` returns `Browser extension is not connected`. Same dual-bridge wedge as commit `b2bde0c8` ("status: pause-and-resume marker — chrome+playwright MCP both blocked this turn"). Per dispatch HARD rule #4 ("If Playwright MCP unavailable, **HALT immediately**") + Memory feedback rule "QA must drive browser, not REST" — HALTED here, did NOT substitute REST for UI checkpoints, did NOT click `/dev/mock-payment`, did NOT mutate any state. **Phase status post-halt**: 30.1 / 30.5 / 30.6 / 30.7 / 30.8 / 30.10 = NOT_RE-WALKED; 30.2 / 30.4 / 30.11 already PASS from prior turn. **Tab state IS LOST** — wedged Chrome session is unusable; next turn must fresh-login Bob (Keycloak port 3000) and Sipho (portal `/login` magic-link port 3002) before resuming. Backend stack remains healthy (`/actuator/health` 8080=200, 8443=200, 3000=200, 3002=307; backend PID 41837 with MockPaymentGateway loaded; seeded org_integrations row intact). GAP-L-64 status remains **FIXED (code merged) / VERIFY-PENDING (browser-driven re-walk)** — NOT marked VERIFIED yet. Evidence: `qa_cycle/checkpoint-results/day-30.md` §"Day 30 Re-walk after L-64 fix — Cycle 1". Next action: external intervention to release the wedged Chrome user-data dir (kill Chrome PID 5426 / restart Claude Code with a fresh `--isolated` Playwright config / re-connect Claude-in-Chrome extension), then re-dispatch QA for Day 30 re-walk from a fresh browser.

- 2026-04-25 SAST — QA: **Day 46 cycle-1 verify COMPLETE.** 5/5 substantive PASS + 1 PARTIAL (46.7 portal Home pending-tile semantics — design question, not regression) + 0 BLOCKER. Method: `mcp__playwright__*` browser-driven for every state mutation and UI checkpoint (plugin namespace `mcp__plugin_playwright_playwright__*` was wedged this session — `Target page, context or browser has been closed` — main namespace worked end-to-end). READ-ONLY SQL `SELECT` for state confirmation. **No SQL/REST mutations.** **Phase A (46.1)**: Sipho self-served fresh magic-link via portal `/login?orgId=mathebula-partners` → typed `sipho.portal@example.com` → "Send Magic Link" → portal returned dev-mode link inline (`http://localhost:3002/auth/exchange?token=d-1Edcm_PHwdklk9Lb3HKTJqvmUjHwEGCnDZawr6iew&orgId=mathebula-partners`) → clicked → landed on `/projects` with valid portal JWT (Sipho Dlamini in user menu). **L-42 magic-link self-service HOLDS for third use this cycle.** **Phase B (46.2 + 46.3)**: `/home` → "Pending info requests 4" tile + Recent invoices showing INV-0001 + INV-0002 → click Requests → list shows REQ-0007 SENT 0/3 at top alongside REQ-0001/0002/0003/0004 (NO Moroka EST request visible). Click REQ-0007 → detail page shows 3 PENDING items (ID copy, Proof of residence, Bank statement — FICA Onboarding Pack template per Day 45 GAP-L-67 substitution). Upload `qa_cycle/fixtures/fica-id.pdf` → ID copy SUBMITTED 16:46:51 UTC, parent flipped SENT→IN_PROGRESS automatically (**L-43 + L-47 listener + parent-status sync HOLDS**). Upload `fica-address.pdf` → Proof of residence SUBMITTED 16:47:17 UTC. Upload `fica-bank.pdf` → Bank statement SUBMITTED 16:47:36 UTC. Final state "3/3 submitted • status IN_PROGRESS" rendered. DB confirms 3 SUBMITTED items with `document_id` linked to docs `4d8e6125-… / 68d9b68e-… / 8ca47203-…` and `submitted_at` populated; parent `information_requests.454dea5d-…` status `IN_PROGRESS`. **Phase C (46.4 + 46.5)**: `/trust` auto-redirected to single-matter view `/trust/e788a51b-…` (RAF). Trust balance card R 70 000,00 / "As of 25 Apr 2026" / "Matter e788a51b". Transactions table 2 rows (descending): `25 Apr 2026 / DEPOSIT / Top-up per engagement letter — RAF-2026-001 / R 20 000,00 / running R 70 000,00` then `25 Apr 2026 / DEPOSIT / Initial trust deposit — RAF-2026-001 / R 50 000,00 / running R 50 000,00`. Running balance correct at every step. **L-52 portal trust-ledger sync VERIFIED for second deposit (DEP-2026-002)** — multi-deposit accumulation works correctly. **Phase D (46.6 isolation spot-check)**: 3-layer hard-negative test. (1) UI list-render: `/projects`, `/requests`, `/trust`, `/home` snapshots all grep-clean for "moroka", "EST-2026", "deceased", "liquidation" (zero tokens). (2) Direct-URL probe: navigated to Moroka matter `/trust/89201af5-f6e0-4d9a-952e-a2af6e5b70ee` → page renders "No trust balance is recorded for this matter" + "The requested resource was not found." for transactions and statements sections. (3) API hard-negative: 2x browser-console 404 on `/portal/trust/matters/89201af5-…/transactions` and `/statement-documents` — backend correctly rejects Sipho JWT against Moroka resources. **Isolation HOLDS at all 3 layers, 31 days after explicit Day 15 check.** **Phase E (46.7 + cross-checks)**: Home tile "Pending info requests" still shows count "4" after all 3 SUBMITTED — request remains IN_PROGRESS until firm-side review/accept; semantic question logged as **OBS-Day46-PendingTileSemantics** (not a gap, awaiting Product clarification on whether "pending" means "open from portal-contact perspective" or "open from firm-review perspective"). **NEW gap GAP-L-68 LOW opened**: portal Home "Last trust movement" tile renders "No recent activity" because frontend `home/page.tsx:239` calls `GET /portal/trust/movements?limit=1` but backend `PortalTrustController` exposes only `/summary`, `/matters/{id}/transactions`, `/matters/{id}/statement-documents` — **no `/movements` endpoint exists**. 2x 404 in browser console. Cosmetic only (correct trust data displays on `/trust/{matterId}`). Suggested fix S (~1 hr): swap frontend to existing `/portal/trust/summary` and read latest from `lastTransactionDate` per matter. DB final: REQ-0007 IN_PROGRESS with 3 SUBMITTED items + 3 portal-uploaded documents; trust state unchanged (Sipho R 70K / Moroka R 25K). Verify-focus this turn: L-42 (magic-link), L-43 (portal listener), L-47 (parent-status sync), L-52 (second-deposit portal sync), P-01/P-02/P-03 all VERIFIED. Day 46 evidence: `qa_cycle/checkpoint-results/day-46.md` + 8 yml/png snapshots prefixed `day-46-cycle1-*`. Next action: **Day 60 dispatch — firm matter closure + Statement of Account generation (actor Thandi)**. **Pre-flight question for Product**: Day 60.5 closure gate "no unpaid fee notes" — INV-0001 is still SENT (no payment_url, pre-dates L-64 seeder); INV-0002 is PAID. Does Day 60 need INV-0001 walked-to-PAID first (tricky given Day 30 verify already confirmed Option A regenerate path is unavailable on existing invoices), or can the Day 60 closure proceed with one outstanding fee note as a known edge case carried forward from L-64's pre-seeder cohort?

- 2026-04-25 SAST — Product triaged L-67 + Day 60.5 INV-0001 closure-gate question. **Item 1 (GAP-L-67): DEFERRED to Sprint 2.** Severity downgraded HIGH→MED, status OPEN→DEFERRED-Sprint-2 (L-67-followup). Reasoning: (a) workaround (substitute pack template — used FICA pack as REQ-0007) was used Day 45 and verified end-to-end through Day 46 (Sipho closed REQ-0007 with 3/3 SUBMITTED, L-43+L-47 listener+parent-status sync re-verified on the substituted request); (b) forward QA position is Day 60 (firm matter closure + SoA) and closure flow does not require creating new ad-hoc info requests — REQ-0001..REQ-0007 already cover the full lifecycle through Day 88 audit trail; (c) verify-cycle scope is "re-validate 40+ shipped fixes", and L-67 is NEW missing functionality (free-form ad-hoc UI never shipped) not a regression — out of scope; (d) fix scope M (~2-3 hr) for two-surface frontend change (`create-request-dialog.tsx` Title+Items inputs for ad-hoc path, plus `request-detail-client.tsx` "+ Add Item" + "Send Now" actions on DRAFT state) exceeds the < 2 hr SPEC_READY bar. Tracker row updated. **Item 2 (Day 60.5 INV-0001 closure gate): Option C primary (accept INV-0001 as pre-seeder edge case with documented carry-forward), Option B (Record Payment) as fallback if Day 60.5 closure-gate UI hard-blocks.** Reasoning: (1) **Option A discarded** — confirmed by Day 30 agent that INV-0001 detail page exposes only Preview / Record Payment / Void; no Resend/Regenerate-Link affordance. Backend `POST /api/invoices/{id}/refresh-payment-link` exists at `InvoiceController.java:195` but no UI button calls it; firm-side regeneration is unreachable through the UI. Adding the button is a S (~1 hr) fix but creates code-spec scope-creep into a verify-only cycle; the existing INV-0002 PAID artifact already proves the L-64 fix end-to-end. (2) **Option D discarded** — Voiding INV-0001 + creating replacement would erase the Day 28 chain-of-custody (the actual disbursement-billing artifact for Sheriff Fees R 1 250) and substitute a synthetic narrative. (3) **Option C preferred** — INV-0001 is a documented pre-fix-state artifact: created Day 28 BEFORE the L-64 MockPaymentGateway seeder ran (verified twice in DB: `payment_url=NULL`, `payment_session_id=NULL`, created_at < seeder run time). The L-64 fix is verified end-to-end by INV-0002 (R 100 court-fee disbursement → Draft→Approved→Sent with payment_url populated → portal Pay Now → mock checkout → SENT→PAID + payment_events Created+Completed). INV-0001 SENT/UNPAID is the ONLY pre-fix-cohort invoice on this matter and belongs to a known migration boundary (L-64 fix path is forward-only; existing pre-seeder invoices were never meant to be back-filled by the seeder per the L-64 spec at `qa_cycle/fix-specs/GAP-L-64.md`). Day 60.5 scenario gate "no unpaid fee notes" interpretation: in production, every invoice would post-date the L-64 seeder; the residual pre-fix invoice is a verify-cycle-only artifact. **Day 60 dispatch instruction**: QA agent should attempt Option C first — proceed with closure dialog, observe whether Day 60.5 gate report renders RED for INV-0001 outstanding. If gate is informational/soft (allows Continue with override or warning), document the carry-forward + proceed. If gate hard-blocks closure (no Continue button when an unpaid fee note exists), fall back to **Option B** — invoke `Record Payment` action on INV-0001 detail page (button confirmed available by Day 30 agent), provide payment reference like `EFT-OFFLINE-INV-0001` (legitimate firm workflow for cash/EFT receipts that bypass PSP roundtrip; backend `POST /api/invoices/{id}/payment` at `InvoiceController.java:181-187` requires only SENT status, doesn't require `payment_url`; transitions to PAID via `gateway.recordManualPayment()` which works for any gateway including NoOp). Either path is scenario-compliant — Option B uses a legitimate firm UI affordance (NOT a SQL/REST hack). **No new GAP opened, no new fix spec written** — this is a scenario-interpretation decision, not a code defect; the underlying L-64 fix is verified working for new invoices and the residual pre-seeder INV-0001 is documented evidence of pre-fix state. Carry-forward note: any future tenant onboarded after legal-za pack install will have the seeder row from PR #1134 in place from day one, so this edge case is bounded to verify-cycle artifacts only.

- 2026-04-25 SAST — QA: **Day 60 cycle-1 verify HALTED — stream timeout (~2.2 hr, 264 tool uses, 0 written results).** Agent did substantial browser work (9 PNG snapshots preserved at `qa_cycle/checkpoint-results/day-60-partial/`) but never wrote a `day-60.md` results file or updated this status before timeout. **Useful intel from preserved screenshots**: `day-60-cycle1-60.5-gate-report-initial.png` shows the actual closure-gate dialog. INV-0001's pre-flight concern is **MOOTED** — the gate reads "Final bill issued with no unbilled items" (✅ GREEN, INV-0001 SENT with billed disbursement satisfies it), NOT "no unpaid fee notes". The Product Option B/C strategy is therefore not needed for closure. **Actual 4 RED gates** preventing closure of Dlamini RAF matter: (1) `Matter trust balance is R 70 000.00. Transfer to client or office before closure.` (2) `1 court date scheduled for today or later.` (3) `9 tasks remain open.` (4) `4 client information requests outstanding.` Other gates GREEN: disbursements approved + settled, no prescription timers, no document acceptances pending. **QA Position remains Day 60** but with refined understanding — closure is multi-step pre-flight resolution (trust disposition + court-date close + task close + info-request close), then closure form, then SoA. Each is non-trivial; recommend tighter per-step scoping on next dispatch (e.g., one dispatch per gate-clearance group + one for closure execute/SoA). Tab state likely lost (Chrome may need re-login). Backend stack still healthy (no restart since PID 41678/41837). NEEDS_REBUILD=false. **Next action options for orchestrator**: (a) dispatch Day 60 PRE-FLIGHT-A (clear court date + tasks); (b) dispatch Day 60 PRE-FLIGHT-B (close 4 outstanding info requests); (c) dispatch Day 60 PRE-FLIGHT-C (trust disposition); then (d) dispatch Day 60 CLOSURE-EXECUTE (form + SoA). Suggest sequential a→b→c→d, each ~30-60 min focused dispatch, to avoid re-timeout. Evidence: `qa_cycle/checkpoint-results/day-60-partial/*.png` (9 files).

- 2026-04-25 SAST — QA: **Day 60 PRE-FLIGHT-C HALTED on NEW HIGH GAP-L-69 (matter-trust closure gate cannot be cleared via UI — Fee Transfer + Refund dialogs lack project_id field).** Disposition was driven end-to-end browser-driven through plugin Playwright. Pre-state: 3 leftover AWAITING_APPROVAL fee transfers from prior timed-out agent (`cbf1c21b-…`, `dc63acf6-…`, `3b6ddf61-…`, all R 1 250 reference `FEE-TRANSFER-INV-0001-DAY60`, all recorded by Thandi at 17:51, 18:06, 18:42 UTC). **Phase 1 (cleanup duplicates)**: as Thandi rejected `cbf1c21b-…` (reason "D" — typed via JS DOM-set; cosmetic data-loss because React-controlled textarea didn't capture, but transition itself succeeded); rejected `dc63acf6-…` properly via `browser_type` (reason "Duplicate of accidental retry — keeping the most recent for approval"); attempted to approve `3b6ddf61-…` but blocked by `TrustTransactionService.approveSingleMode` rule "the transaction recorder cannot be the sole approver". **Phase 2 (capability discovery)**: signed in as Bob (admin) to attempt approval — blocked with "Insufficient permissions for this operation" because admin role lacks APPROVE_TRUST_PAYMENT (only owner has it; verified via `org_role_capabilities`). Side-finding: single-owner firms cannot approve any trust transaction they record (combined recorder-sole-approver rule + only-owner-has-capability lock). Logged as observation, not a separate gap. **Phase 3 (workaround — Bob records, Thandi approves)**: signed in as Bob → matter Trust tab → Fee Transfer dialog → recorded fresh R 1 250 fee transfer with reference `FEE-TRANSFER-INV-0001-DAY60-RETRY` (id `f9f3f7a4-dc7f-4eaa-a594-b03374a9d80c`). Signed back in as Thandi → rejected the lingering `3b6ddf61-…` Thandi-recorded duplicate → approved Bob's `f9f3f7a4-…` → DB confirms APPROVED at 19:42:07; **INV-0001 flipped SENT→PAID** at the same instant (`paid_at=19:42:07.488`); Sipho `client_ledger_cards.balance` 70000.00 → 68750.00. **This satisfies scenario step 60.2 (Fee Transfer Out / pay final fee note from trust) AND clears the L-64 pre-seeder edge case** that Product had earlier flagged for Option C carry-forward — INV-0001 is now PAID via legitimate Trust → Fee Transfer flow, not a bypass. **Phase 4 (refund residual to client)**: signed in as Bob → Trust Accounting → Record Transaction → Record Refund dialog → R 68 750 with reference `REFUND-DLAMINI-CLOSURE-DAY60` description "Refund residual trust balance to client on matter closure (RAF-2026-001)" (id `f215423c-bfbc-4a07-981a-4e0a90a8af48`). Signed back in as Thandi → approved → DB confirms APPROVED at 19:45:34; Sipho ledger card balance 68750.00 → **0.00**. **Phase 5 (matter Trust tab card)**: navigated to RAF matter Trust tab → card renders `Trust Balance · No Funds · R 0,00` (Deposits R 70 000,00 / Payments R 0,00 / Fee Transfers R 1 250,00) — confirms customer-scoped view is correct. **Phase 6 (closure-gate re-walk — FAIL)**: clicked Close Matter → closure-gate dialog STILL renders trust gate RED at `Matter trust balance is R70000.00. Transfer to client or office before closure.` despite ledger card showing R 0,00. Cancelled + re-opened to bust any caching → unchanged. **Root cause** (read-only via grep + DB inspection): `TrustBalanceZeroGate` (`backend/src/main/java/io/b2mash/b2b/b2bstrawman/verticals/legal/closure/gates/TrustBalanceZeroGate.java:38`) computes via `trustTransactionRepository.calculateBalanceByProjectId(project.getId())` — strictly per-matter using `trust_transactions.project_id`. **`RecordFeeTransferDialog` (`frontend/components/trust/RecordFeeTransferDialog.tsx`) and `RecordRefundDialog` (`frontend/components/trust/RecordRefundDialog.tsx`) have NO Matter / project_id field** in their schema or form — even when opened from matter Trust-tab "Fee Transfer" button, projectId is not auto-injected. So FEE_TRANSFER and REFUND rows write `project_id = NULL`, the per-matter gate calculation never sees the disposition, the gate is **physically unrouteable through the UI in its current form**. DB confirms: `f9f3f7a4-…` and `f215423c-…` both have `project_id IS NULL`; only the 2 deposits (recorded via deposit-dialog which DOES have a Matter field) carry `project_id=e788a51b-…`. **GAP-L-69 OPENED (HIGH/BLOCKER for matter closure)**. Per dispatch hard rule "If a UI affordance is missing, log gap and exit — don't power through with REST or SQL", PRE-FLIGHT-C HALTED here. Closing gate state: 8 GREEN, 1 RED (trust). 0 console errors. Suggested fix M (~3-4 hr, mostly frontend): add Matter combobox to both dialogs + auto-fill from matter context + optional invoice→matter inference on backend. After fix lands, simplest re-walk: Reverse the existing R 68 750 REFUND + R 1 250 FEE_TRANSFER + re-record both with proper matter context. Day 60 CLOSURE-EXECUTE remains BLOCKED. Tab state: Thandi KC session active in browser. Backend stack healthy (no restart since PID 41678). NEEDS_REBUILD=false. **Wall-clock ~30 min, ~80 tool uses (well under 75 min budget).** Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — PRE-FLIGHT-C — 2026-04-25 SAST" + 6 PNG snapshots `day-60-cycle1-c-{pre-state-3-duplicate-fee-transfers,final-fee-note-draft,trust-transfer-fee,trust-refund-client,trust-balance-zero,gates-still-red-l69}.png`. **Next action**: Product → Dev triage GAP-L-69. After fix, QA re-walks PRE-FLIGHT-C (reverse + re-record) → re-verify closure-gate dialog all 9 GREEN → dispatch CLOSURE-EXECUTE.

- 2026-04-25 SAST — Product triaged GAP-L-69 (matter-trust closure gate cannot clear via UI — Fee Transfer + Refund dialogs lack matter context). **Conclusion: SPEC_READY (fix-in-cycle, hybrid backend-inference + frontend-Matter-on-REFUND).** Spec at `qa_cycle/fix-specs/GAP-L-69.md`. **Chosen approach (Option 3 hybrid, NOT QA's all-frontend Option 1)**: (1) **FEE_TRANSFER** — backend `recordFeeTransfer` infers `projectId` from the linked invoice's lines via a new `InvoiceLineRepository.findDistinctProjectIdByInvoiceId` query (symmetric to existing `findDistinctInvoiceIdsByProjectId`), and writes the inferred value into the new `TrustTransaction` row; multi-matter invoice edge case falls through to `null` (preserves today's behaviour, deferred to L-69-followup-C). No frontend change for FEE_TRANSFER. (2) **REFUND** — backend `RecordRefundRequest` record gains optional `UUID projectId` field (mirrors `RecordDepositRequest`); service writes `request.projectId()` into the new row. Frontend `recordRefundSchema` (`lib/schemas/trust.ts:63`) gains `projectId: z.string().uuid().optional().or(z.literal(""))` and `RecordRefundDialog.tsx` adds a `<FormField name="projectId">` block copying `RecordDepositDialog.tsx:118-130` verbatim (free-text Matter UUID input, "Optional"). **CORRECTION to QA's tracker note** — backend `RecordFeeTransferRequest` and `RecordRefundRequest` records do NOT currently accept `projectId`; both service methods hardcode `null` into the constructor's projectId positional argument (`TrustTransactionService.java:559, 629`). Spec adds it to REFUND only; FEE_TRANSFER stays request-shape-stable and gets the value via inference. **Why fix-in-cycle (not deferred)**: blocks Day 60 CLOSURE-EXECUTE → blocks E.13 (matter closed) → blocks E.14 (Day 88 audit trail through closure); the verify cycle cannot reach Day 60 CLOSED state without this fix. Also blocks scenario step 60.3 (refund residual on closure) from being a clean UI-driven flow rather than a SQL/REST patch. **Why hybrid over all-frontend Matter combobox**: (a) FEE_TRANSFER's matter is intrinsically determined by the invoice — having the user re-pick it adds zero information and creates a UX surface where they could mismatch (the invoice is the source of truth via `InvoiceLine.projectId`); (b) no Matter combobox component exists yet in `frontend/components/trust/` — building one + autocomplete pushes past the < 2 hr verify-cycle SPEC_READY bar (cf. L-67's M-effort deferral on similar grounds); free-text UUID is the existing dialog convention across deposit/payment/transfer dialogs; (c) refund has no inherent matter binding so UI is the right place — mirror the proven `RecordDepositDialog` pattern verbatim. Est S-M (~1.5-2 hr): ~30 min backend (record DTO + 2 one-line service edits + 1 new repository query + 4 unit tests covering single-matter inference, multi-matter null-fallback, refund-with-projectId, refund-without-projectId), ~20 min frontend (1-line schema + 1 FormField block), ~10 min restart+smoke (record one fee transfer through Trust UI, confirm `project_id` populates in DB), ~30 min buffer. **NEEDS_REBUILD=true** (backend Java change → `bash compose/scripts/svc.sh restart backend`; frontend HMR picks up TSX changes automatically). **Verification path** (in spec): (i) UI-driven cleanup of the 2 existing dirty rows from PRE-FLIGHT-C — Bob clicks `Reverse` on `f9f3f7a4-…` (FEE_TRANSFER R 1 250) → Thandi approves the reversal; same for `f215423c-…` (REFUND R 68 750). FEE_TRANSFER + REFUND are both in `REVERSIBLE_TYPES` per `TrustTransactionService.java:941`. Net DB state returns to legitimate pre-disposition (R 70 000 ledger, gate RED at R 70 000). (ii) Re-walk the disposition with the fix in place: fresh fee transfer (backend infers projectId from INV-0001's line) + fresh refund (form supplies matterId=`e788a51b-…`). (iii) Closure-gate dialog must show all 9 GREEN → close matter → CLOSURE-EXECUTE unblocked. **4 explicit Sprint-2 follow-ups carved out** (in spec, NOT in this verify cycle): L-69-followup-A typeahead Matter combobox (UX polish across all trust dialogs); L-69-followup-B `?tab=trust` matter auto-fill on refund dialog + add Refund quick-action to `TrustBalanceCard`; L-69-followup-C multi-matter-invoice FEE_TRANSFER amount-allocation across matters; L-69-followup-D capability question (only `owner` has APPROVE_TRUST_PAYMENT — single-owner firms cannot approve any trust transaction they record; not opened as separate gap because Mathebula has both roles available and QA worked around it cleanly with Bob-records-Thandi-approves split). L-69 tracker row updated OPEN→SPEC_READY. **Next dispatch: Dev** (implement spec; restart backend; smoke check; PR + merge). After Dev lands, QA re-walks PRE-FLIGHT-C per spec verification path → CLOSURE-EXECUTE.

- 2026-04-25 SAST — QA: **Day 60 PRE-FLIGHT-A COMPLETE; PRE-FLIGHT-B also already complete (out-of-scope bonus from prior timed-out agent).** Slice was effectively a verify-only — when this dispatch opened, DB state for all 4 PRE-FLIGHT gates was already in target: court_date `d4cd7dcd-…` CANCELLED, all 9 tasks CANCELLED, all 6 information_requests CANCELLED/COMPLETED. The prior agent at 2026-04-25 ~19:07–19:51 had successfully driven these mutations through the UI before timing out without writing results (9 partial PNGs at `day-60-partial/` corroborate the in-flight UI work). This slice re-walked the **closure-gate dialog** browser-driven via plugin Playwright (`mcp__plugin_playwright_playwright__*`), confirmed gate report renders correctly, captured 5 fresh evidence snapshots, and proved A1+A2 gates flip GREEN. Tooling: plugin Playwright; Docker `psql` exec for read-only DB confirms; 0 SQL/REST mutations. Auth: Thandi already had a live KC session from prior turn (no re-login). Method: `/dashboard` → projects/`e788a51b-…` (route is `/projects/[id]` not `/legal/matters/[id]` — dispatch URL was wrong) → Court Dates tab → confirmed `Cancelled` chip → snapshot → Tasks tab → "No tasks yet" empty state (All saved-view filter excludes CANCELLED) → snapshot → click `Close Matter` → closure-gate dialog renders 9 gates with **8 GREEN ✅ and 1 RED ❌**. **GREEN gates**: all disbursements approved + settled; final bill issued with no unbilled items; **no court dates scheduled for today or later** (this slice's A1 win); no prescription timers; **all tasks resolved** (this slice's A2 win); **all client information requests closed** (PRE-FLIGHT-B bonus — already done by prior agent); no document acceptances pending. **RED gate (single remaining)**: `Matter trust balance is R70000.00. Transfer to client or office before closure.` with `Fix this` deep-link → `?tab=trust`. Cancel-clicked dialog (no submission). 0 console errors / 0 warnings. **DB confirms (read-only SELECT)**: `court_dates.d4cd7dcd-… 2026-05-15 CANCELLED PRE_TRIAL`; `tasks WHERE project_id='e788a51b-…' GROUP BY status → CANCELLED 9` (zero non-terminal); `information_requests WHERE project_id='e788a51b-…' → REQ-0001/0002/0004/0006 CANCELLED + REQ-0003/0007 COMPLETED` (6 rows total, all closed). **No new GAP opened.** Informational observation only — `OBS-Day60-TasksAllFilterHidesCancelled` (cosmetic): Tasks tab `All` saved-view excludes CANCELLED so empty state "No tasks yet" is mildly misleading when 9 cancelled rows actually exist; the partial screenshot at 19:12 showed an additional `Cancelled` filter pill that's no longer visible — possibly auto-removed when DB count of non-cancelled hit 0. Not a regression. **Dispatch's expected "4 RED gates + 4 outstanding info requests" intel was stale** — prior timed-out agent had also driven PRE-FLIGHT-B through UI before timing out. **Next action**: skip PRE-FLIGHT-B, dispatch **PRE-FLIGHT-C (trust disposition R 70K via Fee Transfer Out per scenario step 60.2)** directly. After PRE-FLIGHT-C lands trust-balance gate GREEN, dispatch **CLOSURE-EXECUTE (Step 2 form + reason CONCLUDED + generate closure letter + generate SoA + Confirm Close)** to actually drive matter status ACTIVE→CLOSED + retention policy row insert. INV-0001 pre-seeder edge case is MOOTED (closure-gate copy is "no unbilled items" not "no unpaid fee notes" → already GREEN). Time: ~7 min wall-clock, 14 tool uses (well under 75 min budget). Backend stack healthy (no restart). Evidence: `qa_cycle/checkpoint-results/day-60.md` + `day-60-cycle1-{court-date-closed,tasks-closed,gate-court-date-clear,gate-tasks-clear,gates-after-A}.png`.

- 2026-04-25 SAST — Dev + Infra: **GAP-L-69 FIXED in PR #1136 (merge SHA `8580e466`).** Implementation per spec (hybrid Option 3): backend `TrustTransactionService.recordFeeTransfer` now infers `projectId` from invoice's lines via new `InvoiceLineRepository` lookup; `RecordRefundRequest` accepts new `projectId` field; `RecordRefundDialog.tsx` exposes Matter UUID input mirroring deposit dialog. 6 files changed, +332/-2 lines (3 backend, 3 frontend). Test matrix: `mvn verify` ran 588+ test classes green (TrustTransactionServiceTest 46/46 PASS); pnpm test 332 files / 2072 PASS / 2 skipped; pnpm build clean; pnpm lint 0 errors / 85 pre-existing warnings. Backend restarted via `svc.sh restart backend` (PID 41678→14088, ready in 30s). Gateway also restarted PID 71426→5812 (external trigger, separate from this cycle). All 4 svc.sh services RUNNING=yes, HEALTHY=yes. NEEDS_REBUILD=false. **Next action**: QA re-walks PRE-FLIGHT-C per fix-spec verification path — Bob reverses dirty `f9f3f7a4-…` FEE_TRANSFER + `f215423c-…` REFUND, Thandi approves; re-record both via fixed dialogs (FEE_TRANSFER auto-infers project_id from invoice; REFUND requires new Matter field); re-verify closure-gate trust balance flips RED→GREEN; then dispatch CLOSURE-EXECUTE.

- 2026-04-25 SAST — Product triaged GAP-L-70 + GAP-L-71. **GAP-L-71 → SPEC_READY** (fix-in-cycle, Option C — DTO→Map adapter at SoA model-build, contained to `StatementOfAccountContextBuilder`). Spec at `qa_cycle/fix-specs/GAP-L-71.md`. Root cause grep-confirmed: `TiptapRenderer.renderLoopTable:309` enhanced-for casts each row to Map (via unchecked cast at `resolveDataSource:336`), but `StatementOfAccountContextBuilder.build:148/157/164` puts typed records (`List<FeeLineDto>`, `List<DisbursementStatementDto>`, `List<TrustTxDto>`) directly into the loop data sources. Chosen Option C (adapter at producer side via `objectMapper.convertValue(list, new TypeReference<List<Map<String,Object>>>(){})` for the 3 typed lists before `context.put`) over Option A (template syntax migration — too much surface) and Option B (extend renderer with reflective per-row conversion — broader blast radius, defer to Sprint 2 generic hardening). ~25 prod + ~50 test LOC across 3 files (`StatementOfAccountContextBuilder.java` + comment-only `TiptapRenderer.java` + new render-roundtrip integration test). Est S-M (~2-3 hr); NEEDS_REBUILD=true. Dev next; QA re-walks SoA generation against closed Dlamini matter `e788a51b-…` after fix lands. **GAP-L-70 → DEFERRED to Sprint 2 as L-70-followup** (trust reversal cascade to invoice + payment_events for FEE_TRANSFER). Workaround proven clean: full R 70 000 REFUND with L-69 Matter (Optional) field instead of FEE_TRANSFER+REFUND split — matter still CLOSED, all 9 gates GREEN. No Day 60–90 demo flow gates on lossless-reversal property. Fix scope M (~3-4 hr backend cross-cuts InvoiceService + payment_events) exceeds <2 hr SPEC_READY bar; production severity stays MED (genuine accounting incorrectness). L-70-followup added to "Deferred to future phases" section. Recommended next dispatch: Dev for L-71.

- 2026-04-25 SAST — QA: **Day 60 PRE-FLIGHT-C re-walk + CLOSURE-EXECUTE COMPLETE — matter CLOSED.** Tooling: plugin Playwright (`mcp__plugin_playwright_playwright__*`) for all browser-driven UI mutations + Keycloak auth flow (sign-out via gateway `POST /logout` with CSRF token from `/bff/csrf`); Docker `psql` exec on `b2b-postgres` (DB=`docteams`, NOT `app`) for read-only state confirmation. Zero SQL/REST mutations. Screenshots tooling timed-out repeatedly so YAML accessibility snapshots auto-saved by `browser_snapshot` (under `.playwright-mcp/page-2026-04-25T20-XX-XX.yml`) serve as primary visual evidence. **PRE-FLIGHT-C re-walk** (~10 min): Bob signed in via KC → Trust → Transactions filter status=APPROVED → clicked `Reverse` on `f9f3f7a4-…` (FEE_TRANSFER R 1 250) with reason "L-69 cleanup..." → DB confirmed REVERSED + paired REVERSAL row `8497d634-…` RECORDED + ledger 0→1250. Repeated for `f215423c-…` (REFUND R 68 750) → REVERSED + REVERSAL `423f1ed0-…` RECORDED + ledger 1250→70000. Matter gate calc returned to legitimate pre-disposition R 70 000. Attempted re-record FEE_TRANSFER R 1 250 → INV-0001 (per fix-spec) but **BLOCKED by NEW GAP-L-70**: backend rejected with `"Invoice must be in APPROVED or SENT status, current status: PAID"` — INV-0001 stuck PAID after the original FEE_TRANSFER's auto-flip; reversal does NOT cascade to invoice payment status. Recovery (scenario-interpretation): since no unpaid invoices remain (Phase 60.2 fee-transfer-out is no-op), record FULL R 70 000 REFUND with new Matter field via the L-69-fixed dialog. **L-69 frontend fix CONFIRMED in dialog**: Refund dialog renders `Client ID / Matter (Optional) / Amount / Reference / Description / Transaction Date` with Matter placeholder "Matter UUID (optional)" matching `RecordDepositDialog` convention exactly per fix-spec §"Frontend — Matter field on RecordRefundDialog". Bob filled customer Sipho UUID + matter `e788a51b-…` + R 70 000 + reference `REFUND-DLAMINI-CLOSURE-DAY60-FIXED` → submit → DB row `b7c87d27-de02-4d4b-9ff8-917a8354d121 REFUND 70000.00 AWAITING_APPROVAL` with **`project_id=e788a51b-…` populated** (was NULL on the pre-fix dirty row, proving L-69 frontend + REFUND DTO change works end-to-end). Sign out Bob → sign in Thandi → Approve → matter gate calc returned **R 0,00**. **Closure-gate dialog: ALL 9 GATES GREEN ✅** (L-69 verify exit checkpoint passed). **CLOSURE-EXECUTE** (~7 min): Continue → Step 2 closure form (reason `CONCLUDED` default + notes "Settlement reached with Road Accident Fund. All trust funds disposed: R 70,000 refunded to client. INV-0001 final fee note paid. Statement of Account to be generated for client closure pack." + `Generate closure letter` checkbox checked) → Confirm Close → matter `e788a51b-…` `status=CLOSED / closed_at=2026-04-25 20:44:20 / retention_clock_started_at=2026-04-25 20:44:20` (per ADR-249); `matter_closure_log` row `f0c26aaf-…` inserted (closed_by=Thandi member id `427485fe-…`, reason=CONCLUDED, notes verbatim, override_used=false, closure_letter_document_id=`a48e1bdb-…`); closure letter PDF `matter-closure-letter-dlamini-v-road-accident-fund-2026-04-25.pdf` generated as `generated_documents.a48e1bdb-…` linked to `documents.2bad9b06-…` (`application/pdf`, UPLOADED, scope=PROJECT). Status badge in matter detail header flipped Active→**Closed**, toolbar "Close Matter" replaced with "Reopen Matter". **SoA generation FAILED with NEW GAP-L-71**: `Generate Statement of Account` toolbar button → dialog default period 2026-04-01 to 2026-04-25 → Preview & Save → backend returned alert "An unexpected error occurred"; backend log shows `java.lang.ClassCastException: class io.b2mash.b2b.b2bstrawman.verticals.legal.disbursement.dto.DisbursementStatementDto cannot be cast to class java.util.Map` at `TiptapRenderer.renderLoopTable:309` (stack: StatementService.renderHtml:276 → StatementService.generate:110 → StatementController.generate:42; requestId `e9fc7a75-d2ae-452f-94b2-e85426e6c4bc`). 0 console errors throughout. **GAP-L-69 → VERIFIED**. **NEW GAP-L-70** (FEE_TRANSFER reversal doesn't cascade invoice payment status; HIGH for verify-cycle re-walks, MED for prod; suggested fix M ~3-4 hr backend-only). **NEW GAP-L-71** (SoA backend ClassCastException; HIGH/BLOCKER for E.13 SoA + Day 61 portal SoA download; suggested fix S-M ~2-3 hr — option (a) `TiptapRenderer.renderLoopTable` reflectively converts non-Map items via Jackson `objectMapper.convertValue`, or option (b) `StatementService.renderHtml` builds DTO list as `List<Map<String,Object>>` already converted; option (a) preferred). **OBS-Day60-RetentionShape** (informational): ADR-249 implementation uses `projects.retention_clock_started_at` column, not separate per-matter `matter_retention_policies` rows (table doesn't exist in schema). Two GLOBAL rules in `retention_policies` (CUSTOMER 1825 days FLAG, AUDIT_EVENT 2555 days FLAG) — confirm with Product whether column-based design is the intended ADR-249 implementation. **Headline outcome**: Day 60 matter closure executed end-to-end via canonical UI flow; matter genuinely CLOSED; closure letter attached. SoA half failed → Day 61 BLOCKED on L-71. **Wall-clock ~17 min total, well under 75 min budget**. Evidence: `qa_cycle/checkpoint-results/day-60.md` §§ "Day 60 — PRE-FLIGHT-C re-walk (after L-69 fix)" + "Day 60 — CLOSURE-EXECUTE" (both 2026-04-25 SAST); 7 key YAML snapshots under `.playwright-mcp/page-2026-04-25T20-{35-58,36-21,37-22,40-17,41-44,43-24,43-58}*.yml`; backend log line at `.svc/logs/backend.log` 2026-04-25T20:46:08.033Z. Next action: Product → Dev triage GAP-L-70 + GAP-L-71. After L-71 fixed, re-run SoA generation alone (closed-matter toolbar button remains available); then dispatch Day 61 portal SoA download.

- 2026-04-25 SAST — Dev + Infra: **GAP-L-71 FIXED in PR #1137 (merge SHA `23bb43d8`).** Implementation per spec Option C: `StatementOfAccountContextBuilder` converts typed DTO lists (`FeeLineDto`, `DisbursementStatementDto`, `TrustTxDto`) to `List<Map<String,Object>>` via `ObjectMapper.convertValue` at model-build (3 loop sources: fees.entries / disbursements / trust.deposits/payments). `TiptapRenderer.renderLoopTable` documented to require Map-shaped rows. Render-roundtrip tests added. Build: `mvnw test -Dtest='*StatementOfAccount*'` 13/13 PASS, BUILD SUCCESS. 3 files changed, +256/-17. Backend restarted via `svc.sh restart backend` (PID 14088→new, ready in 27s). NEEDS_REBUILD=false. **Next action**: QA — re-trigger SoA generation from already-CLOSED Dlamini matter `e788a51b-…` toolbar (matter is CLOSED but toolbar Generate Statement of Account button still available). Verify SoA PDF generates without ClassCastException, contents match scenario (trust ledger zero, fee notes paid, disbursements billed, retention started, balance R 0). Then proceed to Day 61 (Sipho portal SoA download + portal-side review).

- 2026-04-25 SAST — QA: **Day 60 SoA re-gen COMPLETE (GAP-L-71 → VERIFIED) + Day 61 portal-side BLOCKED on NEW GAP-L-74.** Tooling: plugin Playwright (`mcp__plugin_playwright_playwright__*`) for all browser-driven UI mutations + magic-link self-service via portal `/login`; Mailpit `GET /api/v1/messages` for token extraction (only legitimate REST use per dispatch); Docker `psql` exec on `b2b-postgres` (DB=`docteams`, NOT `app`) for read-only state confirmation. Zero SQL/REST mutations. Screenshots tooling timed-out repeatedly so YAML accessibility snapshots auto-saved by `browser_snapshot` serve as primary visual evidence. **SoA re-gen** (~3 min): Thandi already KC-authenticated → matter `/projects/e788a51b-…` (status badge **Closed**) → toolbar `Generate Statement of Account` → dialog default period 2026-04-01..2026-04-25 → `Preview & Save` → backend returned 200, modal renders embedded `<iframe>` with rendered HTML SoA + buttons reshape Close/Download PDF/Regenerate. Backend log line `2026-04-25T21:39:48.262 StatementService — Generated Statement of Account: project=e788a51b-…, period=2026-04-01..2026-04-25, generatedDoc=c0931e79-f185-42c8-af70-33528d5e0d69` (requestId `986b9f0d-…`). NO ClassCastException, NO error stack trace anywhere in `bash svc.sh logs backend | tail -30`. DB confirms `tenant_5039f2d497cf.generated_documents.c0931e79-…` 4 109-byte PDF; Statements tab on matter renders new row Apr 25 / R 1 250,00 / R 0,00 / Download (downloads cleanly to `qa_cycle/checkpoint-results/day-60-cycle1-ce-soa-statement.pdf`); Activity feed shows `Thandi Mathebula performed statement.generated on generated_document`. **GAP-L-71 → VERIFIED.** Two informational obs from SoA preview content (NOT new gaps — separate from L-71's renderer-crash scope): **OBS-Day60-SoA-NotInDocumentsTable** (SoA `document_id=NULL` so SoA only visible via Statements tab not Documents tab firm-side AND not visible at all portal-side — this is the structural cause of GAP-L-74 below), **OBS-Day60-SoA-Fees/Trust-Empty** (rendered SoA preview body has empty Fee Notes loop AND empty Trust deposits/payments loops despite real ledger data; disbursements loop populates correctly with R 100 + R 1 250 — Sprint-2 SoA-content-reconciliation followup). **Day 61 portal walk** (~5 min): Mailpit confirmed NO closure/SoA notification email fired to Sipho between closure timestamp `20:44:20` and now (latest mail to Sipho is `Trust account activity` at `20:43:23` — pre-closure REFUND notification only) → **NEW GAP-L-72 MED** (no notification email on closure or SoA generation). Workaround: Sipho self-service magic-link via portal `/login?orgId=mathebula-partners` → token `wbLQXUsXPoHBpuF2bWTXmHsY_-…` → `/auth/exchange` → portal `/projects` rendered. Matters list shows Dlamini matter as 1 of 3 (incidental: each matter shows "0 documents" badge — firm-vs-portal counter mismatch). Click into Dlamini → matter detail header shows status badge **"ACTIVE"** despite firm-side + DB CLOSED → **NEW GAP-L-73 LOW** (portal status badge stale; cosmetic). **Day 61 BLOCKER on Documents tab** — portal Documents table on matter detail lists ONLY 6 FICA upload duplicates (`fica-id.pdf` / `fica-address.pdf` / `fica-bank.pdf`, 344 B, status Pending). Both firm-side closure-pack docs MISSING from portal: closure letter `documents.2bad9b06-…` (1.6 KB, has proper `project_id` linkage but portal API filters it out — likely visibility/scope filter) AND SoA `generated_documents.c0931e79-…` (4 109 B, structurally orphaned because `document_id=NULL` — `StatementService.generate` writes only to `generated_documents`, never creates paired `documents` row, so SoA can NEVER appear in any Documents tab). Statements tab does not exist on portal at all. Sipho cannot see/download/reconcile SoA per scenario steps 61.2-61.8. **NEW GAP-L-74 HIGH/BLOCKER** for Day 61 entire flow. Suggested fix M (~3-4 hr backend + portal-API): (a) `StatementService.generate` should also create paired `documents` row (with appropriate visibility/scope) and link via `generated_documents.document_id`, mirroring `MatterClosureService.generateClosureLetter` which already does this; (b) `MatterClosureService` set closure-letter `documents.visibility='PORTAL'`; (c) review portal Documents API filter to ensure scope=PROJECT + visibility=PORTAL flows through. STOPPED per dispatch hard rule "If a Day 61 checkpoint blocks (e.g., SoA not in portal contact's view, download 404, etc.), STOP, log gap, exit." Side-evidence — portal Trust ledger (`day-61-cycle1-portal-trust.yml` shows balance R 0,00 + 3 transactions REFUND/DEPOSIT×2 reconciling to firm-side) and portal Invoices (`day-61-cycle1-portal-invoices.yml` shows INV-0001 PAID R 1 250 + INV-0002 PAID R 100 with View+Download) BOTH render Sipho's data correctly with no leakage and no blockers, so portal data-pipe is healthy generally — this is a Documents-surface filter+structural gap specifically. 0 console errors throughout. **Headline outcome**: L-71 (the renderer crash) is GONE — SoA generates end-to-end firm-side. Day 61 portal walk hit a separate document-visibility/persistence gap that L-71 spec did not contemplate. **Wall-clock ~8 min total, well under 75 min budget**. Evidence: `qa_cycle/checkpoint-results/day-60.md` §"Day 60 — SoA re-gen (after L-71 fix) — 2026-04-25 SAST" + `qa_cycle/checkpoint-results/day-61.md` §"Day 61 Re-Verify — Cycle 1 — 2026-04-25 SAST"; YAML snapshots `day-60-cycle1-ce-soa-{regen-dialog,regen-after-click,statements-tab,documents-tab,after-close}.yml` + `day-61-cycle1-portal-{projects,matter-detail,trust,invoices}.yml`; PDF `day-60-cycle1-ce-soa-statement.pdf` (4 109 bytes). Next action: Product → Dev triage GAP-L-74 (BLOCKER for Day 61 re-execution), GAP-L-73 (cosmetic portal badge), GAP-L-72 (notification email on closure/SoA). After L-74 fixed, re-walk Day 61 from 61.1 (Mailpit notification path if L-72 fixed; magic-link self-service path otherwise) → 61.2 portal Documents tab list with SoA + closure letter → 61.3 download cleanly → 61.4 PDF reconciliation → 61.9 firm-side audit log of portal access event. Day 88 (audit trail review) blocked behind Day 61 verification per scenario sequence.

- 2026-04-26 SAST — Product triaged GAP-L-72 + GAP-L-73 + GAP-L-74. **GAP-L-74 → SPEC_READY (FIX-IN-CYCLE).** Spec at `qa_cycle/fix-specs/GAP-L-74.md`. Two-part backend-only fix, contained to two services, ~30 prod + ~80 test LOC, est M (~3 hr), NEEDS_REBUILD=true. Root cause grep-confirmed both halves: **L-74a (closure letter)** — `GeneratedDocumentService.createLinkedDocument:644` PROJECT branch uses 5-arg `Document(projectId,…)` constructor (line 653-659) → `Document.java:71` defaults `visibility=INTERNAL` → portal filter at `PortalQueryService.listProjectDocuments:83` requires `Visibility.SHARED`, drops INTERNAL. Closure-letter `documents.2bad9b06-…` row exists with proper `project_id` linkage but is INTERNAL → portal filter excludes it. **L-74b (SoA)** — `StatementService.generate:95-175` bypasses `GeneratedDocumentService.generateDocument` per its own Javadoc rationale (lines 39-50: period-bound context doesn't fit standard `TemplateContextBuilder` interface) and persists ONLY a `GeneratedDocument` row directly via `generatedDocumentRepository.save(...)` at line 139; never creates paired `Document` row. SoA structurally orphaned from documents pipeline — even firm-side Documents tab can't see it (only Statements tab does, which doesn't exist on portal at all). Chosen fix: (Part A) inline paired `Document` persistence in `StatementService.generate` mirroring `GeneratedDocumentService.createLinkedDocument` PROJECT branch — `new Document(projectId, fileName, "application/pdf", size, memberId) + assignS3Key + confirmUpload + setVisibility(SHARED) + save + linkToDocument`; (Part B) inline visibility flip in `MatterClosureService.generateClosureLetterSafely:264-286` after letter-id capture — `documentService.toggleVisibility(linkedDocId, SHARED)` wrapped in try/catch matching the existing best-effort REQUIRES_NEW contract. No `PortalQueryService` change — `SHARED` is the right gate (closure pack is by-definition client-facing per scenario steps 60.10 + 61.2 + 61.8). Considered alternatives rejected: (i) extending `Visibility` enum to add `PORTAL`/`CLIENT_VISIBLE` distinct from manually-toggled `SHARED` (audit-clarity nice-to-have but bigger blast radius — defer to L-74-followup); (ii) refactoring `StatementService` to reuse `generateDocument` once period-bound context is generalised (larger refactor — defer to L-74-followup-2). Why fix-in-cycle: L-74 is the SOLE hard blocker for Day 61 portal walk; cannot be worked around with REST/SQL per dispatch hard rule "If a Day 61 checkpoint blocks (e.g., SoA not in portal contact's view, download 404, etc.), STOP, log gap, exit"; blocks Day 88.4 portal activity-trail (which expects "SoA download (Day 61)" entry — impossible if Sipho can't reach the SoA) AND exit checkpoint **E.13** (closure-pack delivered to client). Verification post-fix: rebuild + restart → re-generate SoA via Statements tab toolbar (existing `c0931e79-…` was created pre-fix and still has `document_id=NULL` — backfill or regenerate to get fresh row through new code path) → backfill closure-letter `documents.2bad9b06-…` to `visibility=SHARED` via firm-side toggle UI (or close a fresh test matter to exercise new path end-to-end) → drive Sipho's portal session via portal `/login` magic-link self-service → matter detail Documents tab → expect both PDFs listed alongside FICA uploads → click Download SoA → presigned-URL roundtrip → byte-size match. **GAP-L-73 → SPEC_READY (FIX-IN-CYCLE).** Spec at `qa_cycle/fix-specs/GAP-L-73.md`. Backend-only, ~25 prod + ~40 test LOC across 1 file (`MatterClosureNotificationHandler.java`) + 1 test, est S (~1.5 hr), NEEDS_REBUILD=true. Root cause grep-confirmed: portal matter detail reads from denormalised projection `portal.portal_projects.status` (populated by `PortalEventHandler` listeners). Status column is updated only on `ProjectUpdatedEvent` (`PortalEventHandler.onProjectUpdated:178-201` calls `updatePortalProjectDetails(...)` with `status = event.getStatus() ?? "ACTIVE"`). `MatterClosureService.performClose:189-247` transitions status via `project.closeMatter(...)` + `projectRepository.save(...)` (lines 205-206) and publishes `MatterClosedEvent` (line 237-239) — but **never** publishes `ProjectUpdatedEvent`. Same for `MatterClosureService.reopen` (`MatterReopenedEvent`, not `ProjectUpdatedEvent`). Result: portal projection stays at the initial "ACTIVE" value written by `PortalEventHandler.onCustomerProjectLinked:146` (hardcoded `"ACTIVE"`) on first customer-project link, forever after closure. Chosen fix: extend `MatterClosureNotificationHandler` (the only existing `MatterClosedEvent` listener) to also call `portalReadModelRepository.updatePortalProjectDetails(...)` with new status for every customer linked to the project — mirrors `PortalEventHandler.onProjectUpdated` row-by-row pattern. Mirror in `onMatterReopened` so close→reopen cycle correctly flips back to "ACTIVE". Considered alternatives rejected: (Alt A) publish `ProjectUpdatedEvent` from `MatterClosureService` in addition to `MatterClosedEvent` — semantic conflation, payload mismatch with closure-specific fields; (Alt B) new separate `PortalClosureProjectionHandler` listener — over-engineering when one helper inside existing handler suffices. Why fix-in-cycle (not deferred to Sprint 2 with L-72): L-73 is genuinely a 1-helper expansion (~25 prod LOC) of an existing handler that already has all the right AFTER_COMMIT + REQUIRES_NEW + try/catch idioms wired; cleaner to land alongside L-74 since both touch the closure→portal seam and Dev will be in this code path. **L-73-followup** (Sprint 2): portal frontend `/projects/[id]/page.tsx` "CLOSED" badge variant (greyed-out / archived styling, "Past" tab grouping in `/projects` list per scenario step 75.6) — backend now emits correct value, frontend just renders whatever it gets. **GAP-L-72 → DEFERRED to Sprint 2 as L-72-followup.** Reasoning: (1) Workaround proven clean — Sipho self-services magic-link via portal `/login?orgId=…` whenever needed; already exercised 3+ times across cycle-1 verify; portal returns dev-mode link inline. (2) Scenario re-walk plan for Day 61 explicitly authorises magic-link self-service path post-L-72-acknowledgement (`day-61.md` line 56). (3) Day 75 weekly digest covers the closure-pack notification within 7 days via existing scheduler. (4) Fix scope S-M (~2-3 hr) is mechanical IF the option (a/b/c) decision is product-given; option (c) generic `documents.created` listener has the biggest blast radius and needs a notification-policy decision (per-doc-type allowlist? throttle window?) that is itself a Sprint 2 product call, not a 3-hr verify-cycle patch. (5) NO Day 61 / 75 / 85 / 88 / 90 scenario step or exit checkpoint hard-gates on per-event email notification — all gated steps either use magic-link (Day 61) or digest (Day 75) or are firm-side (Day 85, 88-firm) or sweep-style (Day 90 + exit). L-72-followup carved out: per-event portal-contact notification on closure + SoA generation, with notification-policy decision (option a/b/c picked + per-doc-type allowlist if c). Severity for verify cycle drops effectively to N/A; production severity remains MED. **Tracker updates**: L-74 OPEN→SPEC_READY; L-73 OPEN→SPEC_READY; L-72 OPEN→DEFERRED. **Deferred section additions**: L-72-followup, L-73-followup, L-74-followup, L-74-followup-2 — all carved out for Sprint 2. **Recommended next dispatch**: Dev for GAP-L-74 (the BLOCKER) + GAP-L-73 (small follow-on, same code path — closure→portal seam). After Dev merges + Infra restarts backend, QA re-walks Day 61 from 61.1 (magic-link self-service path per L-72 deferral) → 61.2 portal Documents tab list with SoA + closure letter visible → 61.3 download cleanly → 61.4 PDF reconciliation → 61.7 title check → 61.8 closure letter visibility → 61.9 firm-side audit log of portal access event. Then Day 75 (weekly digest), Day 85 (firm final closure paperwork), Day 88 (firm + portal activity-feed wow moment), Day 90 + exit checkpoints. Both L-73 + L-74 SPEC_READY decisions were authority-checked against scenario hard rule (no REST/SQL workarounds for Day 61 portal-side blockers) and exit-checkpoint coverage (E.13 closure-pack delivery to client requires both fixes).

- 2026-04-25 SAST — Dev + Infra: **GAP-L-74 + GAP-L-73 FIXED in PR #1138 (merge SHA `2640d868`).** Bundled fix per both specs. L-74 part A: `StatementService.generate` persists paired Document (visibility SHARED) alongside GeneratedDocument so SoA enters standard documents pipeline. L-74 part B: `MatterClosureService.generateClosureLetterSafely` toggles linked Document visibility INTERNAL→SHARED so closure letter is portal-visible. L-73: `MatterClosureNotificationHandler` syncs `portal_projects.status` to CLOSED on `MatterClosedEvent` (and ACTIVE on `MatterReopenedEvent`), mirroring `PortalEventHandler.onProjectUpdated`. 6 files changed, +451/-2 (3 prod, 3 test incl. NEW `StatementServicePortalDocumentTest`). Build: `mvn test -Dtest='*Statement*,*Closure*,*PortalEvent*,*PortalQuery*,*MatterClosureNotification*,*Document*'` 235/235 PASS, BUILD SUCCESS. Backend restarted via `svc.sh restart backend` (PID 33794→new, ready in 30s). NEEDS_REBUILD=false. **Next action**: QA — (1) one-time backfill: existing closure letter `documents.2bad9b06-…` needs visibility flipped SHARED via firm UI (pre-fix row); existing SoA `generated_documents.c0931e79-…` was created pre-fix with `document_id=NULL` and won't appear on portal even after the fix → re-generate SoA from closed Dlamini matter (toolbar still available) to exercise new code path. (2) Day 61 re-walk: portal Documents tab should now show closure letter + new SoA, matter status badge should flip CLOSED, full Sipho portal-side review per scenario 61.1-61.9. (3) Mark L-73 + L-74 VERIFIED with evidence.

- 2026-04-25 SAST — QA: **Day 75 cycle-1 verify PARTIAL COMPLETE — late-cycle isolation HOLDS (75.6 a/b/c PASS); weekly digest (75.1-75.4) DEFERRED on environmental limitation; activity-trail (75.5) absent — defer to Day 88. Day 85 next.** Tooling: plugin Playwright (`mcp__plugin_playwright_playwright__*`) for browser-driven portal navigation; magic-link self-service via portal `/login` (per L-72 deferral); Mailpit `GET /api/v1/messages` + `/api/v1/search?query=weekly+update` for digest-email lookup; Docker `psql` exec on `b2b-postgres` (DB=`docteams`) for read-only DB state confirmation. Zero SQL/REST mutations. **Pre-flight DB state**: Sipho's customer (`Sipho Dlamini`, 127d1c7d-…) owns 3 matters all DB-confirmed customer-linked: Dlamini RAF (CLOSED), L-37 Regression Probe (ACTIVE), L-37 Conveyancing Probe (ARCHIVED). Moroka matter `89201af5-… Estate Late Peter Moroka` (ACTIVE) exists separately, customer Moroka Family Trust portal contact `881e5f2f-…` distinct. **Digest path analysis**: `PortalDigestScheduler.scheduledRun()` cron `0 0 8 ? * MON`; today is Sunday 2026-04-26 00:48 SAST; next fire ~31h. **No manual REST/admin trigger exists** (`grep -rn "runWeeklyDigest" backend/src/main` returns only the @Scheduled cron entry point + the public method called by it; zero `Controller`/`Endpoint` references). Mailpit search "weekly update" + "Your week" → 0 hits. Backend log zero `digest sweep` / `digest sent` lines. Defensible by code path (per-`PortalContact.id` assembler is tenant-ScopedValue scoped; cross-contact leak would require Hibernate filter bypass not present in code grep). **Isolation walk (~5 min)**: Sipho self-service magic-link via portal `/login?orgId=mathebula-partners` → token `Q9oxUoLn2nlnyb-1YPbgWd_KG-2aOY5BMWf1XXOGkQI` extracted from inline dev-mode link → `/auth/exchange` → `/projects` → 3 matter cards (all Sipho-linked, NO Moroka leak); → `/home` → tile dashboard "Pending info requests: 3" + "Upcoming deadlines: 0" + Recent invoices INV-0001 R 1 250,00 + INV-0002 R 100,00 (both Sipho-only) + "Last trust movement: No recent activity" (degraded gracefully due to pre-existing 404 endpoint); → `/trust` → auto-redirected to `/trust/e788a51b-…` matter view → balance **R 0,00** + 3 transactions (REFUND R 70k, 2 DEPOSITs R 50k+R 20k, all RAF-2026-001 referenced); → click into Dlamini matter → header status badge **CLOSED** (L-73 still working from Day 61 re-walk). 0 Moroka tokens visible anywhere. Zero R 25,000 leak. **`/profile`** has no activity trail — only contact info card (Name, Email, Role, Customer). **Console errors**: 2 errors on `/home` only — repeated `GET http://localhost:8080/portal/trust/movements?limit=1 → 404` (pre-existing, same as GAP-L-68 LOW from Day 46; UI degrades to "No recent activity"). All other portal pages → 0 errors. **Per-step results**: 75.1-75.4 DEFERRED (environmental, no manual trigger); 75.5 PARTIAL (no portal activity-trail surface — defer to Day 88); 75.6a PASS (`/home` no Moroka); 75.6b PASS (`/trust` R 0,00 RAF-only, no R 25k Moroka leak); 75.6c PARTIAL (matters list flat, no Past tab — already L-73-followup; CLOSED badge in matter detail header confirmed); 75.7 N/A (digest deferred, captured isolation screenshots instead). **Headline outcome**: late-cycle isolation invariant **HOLDS**; weekly-digest path is structurally wired but not on-demand-testable without next Monday's cron fire OR a Sprint-2 admin-trigger endpoint. **NEW informational obs (4, all Sprint 2 / NOT new gaps)**: OBS-Day75-NoManualDigestTrigger (S ~30 min — expose `/internal/portal/digest/run-weekly` admin endpoint OR dev-portal harness wiring), OBS-Day75-NoPortalActivityTrail (M — no portal `/activity` surface, expected to gap-out Day 88.3-88.5), OBS-Day75-NoPastTabForClosedMatters (= L-73-followup), OBS-Day75-PortalTrustMovementsEndpoint404 (= GAP-L-68 LOW reconfirmed). **Wall-clock ~12 min total, well under 45 min target**. Evidence: `qa_cycle/checkpoint-results/day-75.md` § "Day 75 Re-Verify — Cycle 1 — 2026-04-25 SAST"; YAML snapshots `day-75-cycle1-portal-{projects,home,trust,profile,matter-detail}.yml`; PNGs `day-75-cycle1-portal-{matters-list,trust-balance,matter-detail-closed}.png`. Next action: QA — Day 85 (firm final closure paperwork — verify retention policy `end_date ≈ today + 5 years - 25 days`; audit log filters by matter+actor for Phase 50 + Phase 69 readiness).

- 2026-04-25 SAST — QA: **Day 61 re-walk + Slice 1 (L-73 + L-74 verify) + Slice 2 (Day 61 scenario 61.1-61.9) COMPLETE. L-73 + L-74 → VERIFIED end-to-end browser-driven. Day 75 next.** Tooling: plugin Playwright (`mcp__plugin_playwright_playwright__*`) for all browser-driven UI mutations + magic-link self-service via portal `/login`; Docker `psql` exec on `b2b-postgres` (DB=`docteams`) for read-only DB state confirmation; `curl` against LocalStack S3 for presigned-URL byte-size verification (legitimate QA use — Sipho's actual download path). Zero SQL/REST mutations. **Backfill (B1+B2, ~3 min)**: After ruling out a project-scoped doc visibility-toggle UI (`VisibilityToggle` component exists at `frontend/components/documents/visibility-toggle.tsx` but only wires to `/customers/[id]/actions.ts toggleDocumentVisibility` — surfaced exclusively in `CustomerDocumentsPanel`, not on matter Documents tab; logged as informational OBS-Day61-NoProjectDocVisibilityToggle, not a new gap since L-74 fix sets SHARED at creation), executed two paths: (a) toolbar Generate Statement of Account → period 2026-04-01..2026-04-25 → Preview & Save → DB confirms new SoA `generated_documents.a9c8d1ac-… document_id=8e9eff16-… → documents.8e9eff16-… visibility=SHARED, size=4110, content_type=application/pdf` (L-74a code path proven); (b) Reopen Matter → close gates 9/9 GREEN → Confirm Close with Generate closure letter checkbox → DB confirms new closure letter `generated_documents.d0e347e5-… document_id=6e2ea5ee-… → documents.6e2ea5ee-… visibility=SHARED, size=1643` (L-74b code path proven; auto-flipped INTERNAL→SHARED). Pre-fix orphan rows (`c0931e79-…` SoA `document_id=NULL` + `2bad9b06-…` closure letter `visibility=INTERNAL`) coexist in DB but correctly ignored by portal Documents API. **Slice 1 verify (B3+B4, ~2 min)**: Sipho self-service magic-link via portal `/login?orgId=mathebula-partners` → token `NLiaMMmaB1nZ4GivRh_KwJgmcrHdQh-roVbMwFx30Vs` extracted from inline dev-mode link → `/auth/exchange` → `/projects` (Dlamini matter shows "1 document" — was "0 documents" pre-fix) → click Dlamini → portal matter detail. **L-73 VERIFIED**: status badge renders **CLOSED** in matter header (was ACTIVE pre-fix); DB `portal.portal_projects.status='CLOSED'` (was 'ACTIVE' pre-fix) — `MatterClosureNotificationHandler` correctly fires on `MatterClosedEvent`/`MatterReopenedEvent`. **L-74 VERIFIED**: Documents tab now lists 8 rows (6 FICA + new SoA 4.0 KB + new closure letter 1.6 KB; 2 firm-side closure pack PDFs visible to portal contact). Click Download SoA → presigned URL fired against LocalStack `http://localhost:4566/docteams-dev/org/tenant_5039f2d497cf/generated/statement-of-account-…pdf?X-Amz-…` → `curl` confirms HTTP 200 + 4110 bytes + valid PDF v1.6 (matches DB exactly). Click Download closure letter → HTTP 200 + 1643 bytes + valid PDF v1.6 (matches DB exactly). **Slice 2 Day 61 walk (~2 min)**: 61.1 PASS via L-72 magic-link workaround per deferral; 61.2 PASS (both files listed); 61.3 PASS (downloads clean); 61.4 PARTIAL (PDF saved, content reconciled vs cycle-1 firm-side preview — sub-gap reconfirmed: SoA Fees/Trust loops still empty per pre-existing OBS-Day60-SoA-Fees/Trust-Empty Sprint 2 followup; disbursements loop populates correctly); 61.5 PASS (full-page screenshot); 61.6 PASS (byte-sizes match exactly: 4110+1643); 61.7 PASS (both titles render with full descriptive names — no "Untitled" leak); 61.8 PASS (closure letter visible + downloads); 61.9 PARTIAL — **NEW informational OBS-Day61-NoPortalDocAuditEvent** (portal-side document download/view doesn't emit `audit_events` row; portal-side audit currently only fires for `acceptance.viewed`/`acceptance.accepted`; scenario 61.9 + 88.4 expect this for Phase 50 data-protection traceability — defer to Sprint 2, may surface as gap on Day 88.4). 0 console errors throughout. 1 warning unrelated. **Headline outcome**: L-73 + L-74 fully VERIFIED — closure-pack delivery flow (E.13 exit checkpoint) functionally proven end-to-end. Three informational obs surfaced (NOT new gaps; all deferred Sprint 2): OBS-Day61-NoProjectDocVisibilityToggle (no firm UI for PROJECT-scope visibility flip), OBS-Day61-PortalDocumentsProjectionPartial (`portal.portal_documents` projection partial, but `PortalQueryService` reads tenant `documents` directly so portal still surfaces correctly), OBS-Day61-NoPortalDocAuditEvent (above). **Wall-clock ~7 min total, well under 60 min target**. Evidence: `qa_cycle/checkpoint-results/day-61.md` § "Day 61 Re-Walk (after L-73+L-74 fix) — 2026-04-25 SAST"; YAML snapshots `day-61-cycle1-rewalk-{soa-regen-saved,after-reopen,close-step2,after-reclose,portal-projects,portal-matter-detail}.yml`; PNGs `day-61-cycle1-rewalk-{soa-regen-saved,portal-matter-status-closed,portal-documents-with-closure-and-soa,firm-documents-{after-fix,with-new}}.png`; downloaded PDFs `day-61-cycle1-rewalk-portal-{soa,closure}.pdf` (4110+1643 bytes). Next action: QA — Day 75 (weekly digest + late-cycle isolation spot-check) per scenario.

- 2026-04-25 SAST — Product: **Triaged GAP-L-75 — split into a/b/c sub-rows.** **L-75a (Phase 69 audit-log page 404) → DEFERRED-Sprint-2 (Phase 69)**: spec already exists at `requirements/claude-code-prompt-phase69.md` (~1 sprint, M-L; explicit founder decision 2026-04-20 carves OUT portal-side activity trail from Phase 69). Building in-cycle either ships a throwaway stub or pulls a full sprint into a verify cycle — out of scope. **L-75b (matter Activity actor filter UI) → DEFERRED-Sprint-2**: revised estimate M (~3-4 hr, not S as originally guessed) because Phase 69's `/api/audit-events/facets/actors` endpoint isn't shipped yet, and adding it ad-hoc would duplicate Phase 69 scope. **L-75c (portal-contact write events not in `audit_events`) → SPEC_READY** (`qa_cycle/fix-specs/GAP-L-75c.md`): this is the critical path of the triple — without portal-contact rows, no L-75a/L-75b surface work can satisfy E.14. Code investigation grep-confirmed: `AcceptanceService.markViewed:310` + `AcceptanceService.accept:377` are the ONLY `actorType("PORTAL_CONTACT")` emitters in the codebase (matches the 2 rows seen in DB). `PortalCommentService.java:72` uses non-canonical `actorType("PORTAL_USER")` (drive-by cleanup in spec). All other portal write paths missing emission: `PortalInformationRequestService.initiateUpload` (~line 108) + `submitItem` (line 171) + `submitTextResponse` (line 201) + `PortalQueryService.getPresignedDownloadUrl` (called from `PortalDocumentController.presignDownload`) + `PaymentWebhookService.processWebhook` (called by `MockPaymentController.completeMockPayment` line 121). Fix is purely additive (4 emit sites + 1 `AuditService` constructor injection per service + 1-line pass-through in `PortalDocumentController` to forward `portalContactId` + 4 integration tests). Pattern is canonical (`AuditEventBuilder.builder().eventType(...).actorType("PORTAL_CONTACT").source("PORTAL").details(Map.of("project_id", projectId.toString(), ...)).build()` per `AcceptanceService` line 310). `details.project_id` is load-bearing (`AuditEventRepository.findByProjectId` filters on `details->>'project_id'` — without this field new portal rows wouldn't appear on matter Activity feed even after L-75b lands). NEEDS_REBUILD=true (Java change, backend restart). NEEDS_DB_MIGRATION=false (`audit_events.details` is JSONB, additive). Estimated S–M (~3 hr): 30 min wiring + 60 min emit code + 60 min tests + 30 min smoke walk. **E.14 fallback path with this triage**: matter Activity tab covers firm-side audit (already verified); DB SELECT covers portal-side (after L-75c lands). L-75a/L-75b pure-UI gaps documented as "Phase 69 carve-out" in the cycle write-up. **Day 88 implication**: L-75c lands → Day 88.4 portal activity-trail items can be evidenced via DB even with portal `/activity` surface still absent (= OBS-Day75-NoPortalActivityTrail, separate Sprint-2 followup). Status tracker updated: L-75 row split into L-75a + L-75b + L-75c; original L-75 wording preserved verbatim in this log for traceability. Next action: **Dev → implement L-75c per spec** (`qa_cycle/fix-specs/GAP-L-75c.md`); after merge + backend restart, QA re-walks one portal write of each kind on the existing Dlamini matter via Sipho self-service magic-link, runs DB count assertion (Verification step 4), then proceeds to Day 88 (firm activity feed surface OK; portal half evidenced via DB + matter Activity tab as fallback for missing portal `/activity`).

- 2026-04-25 SAST — QA: **Day 85 cycle-1 verify PARTIAL COMPLETE — 85.1 PASS (closure letter attached + downloadable); 85.2 N/A (no additional firm-side correspondence required); 85.3 PASS at DB / FAIL at UI surface (no per-matter retention card; reconfirms OBS-Day60-RetentionShape); 85.4 PARTIAL — firm Activity feed renders but NEW GAP-L-75 HIGH opened on cross-actor audit-trail completeness. Day 88 next (with caveat — likely BLOCKS on portal activity-trail surface absence + portal-actor `audit_events` emission gap).** Tooling: plugin Playwright (`mcp__plugin_playwright_playwright__*`); Docker `psql` exec on `b2b-postgres` (DB=`docteams`) for read-only retention + audit_events SELECTs. Zero SQL/REST mutations. Auth: Thandi already had a live KC session from prior turn. **85.1 — closure letter** (~2 min): `/projects/e788a51b-…?tab=documents` lists `matter-closure-letter-dlamini-v-road-accident-fund-2026-04-25.pdf` (1.6 KB, status Uploaded, Apr 25) + a second `-2026-04-26.pdf` from Day 61 re-close cycle + statement-of-account PDF (4.0 KB). Status badge **Closed**, toolbar "Reopen Matter" + "Generate Statement of Account" buttons available. **85.3 — retention math** (~1 min, DB-side): `SELECT id, status, closed_at, retention_clock_started_at, retention_clock_started_at + INTERVAL '1825 days' AS computed_end_date FROM tenant_5039f2d497cf.projects WHERE id='e788a51b-…'` → `status=CLOSED, closed_at=2026-04-25 22:35:14 (re-close), retention_clock_started_at=2026-04-25 20:44:20, computed_end_date=2031-04-24 20:44:20` (= clock_start + 1825 days per ADR-249). Scenario 85.3 wording "today + 5 years - 25 days" assumes scenario-time of 25 days post-closure; in real-time walk closure was ~2 hours ago (`elapsed=02:14:17.582736`), so the 5-year forward calc is correct: `end_date = clock_start + 1825 days`. UI surface: `/settings/data-protection` exposes 2 GLOBAL `retention_policies` rows (CUSTOMER 1825d + AUDIT_EVENT 2555d, both Flag action, both Enabled, "Last Evaluated: Never") via `RetentionPolicyTable` component; **NO per-matter retention card** in matter Overview/header/Settings (column-based design per ADR-249 — `projects.retention_clock_started_at` not exposed in any UI surface). Reconfirms pre-existing OBS-Day60-RetentionShape Sprint-2 followup. **85.4 — audit log filters** (~3 min): `/org/mathebula-partners/audit-log` returns 404 (Phase 69 page not yet shipped — `find frontend/app -name "audit*"` returns zero matches; scenario explicitly says "platform admin view — requires Phase 69 if shipped, else firm-side activity feed"). Fallback to matter Activity tab `/projects/e788a51b-…?tab=activity` → tabpanel renders 6 type-filter buttons (All / Tasks / Documents / Comments / Members / Time) but **zero actor-filter UI**. Activity feed renders full firm-side audit history: `Thandi generated document "matter-closure-letter-…2026-04-26.pdf" from template "Matter Closure Letter"` (most recent), `Thandi performed statement.generated on generated_document` (×2), `Thandi generated document "matter-closure-letter-…2026-04-25.pdf"`, `Bob performed trust_refund.recorded on trust_transaction` (Day 60 PRE-FLIGHT-C cleanup), `Thandi performed task.cancelled on task` (×9 — Day 60 PRE-FLIGHT-A), `Thandi accepted "Bank statement (≤ 3 months)" for unknown` (×3 — Day 60 PRE-FLIGHT-B item acceptances), `Thandi cancelled information request REQ-0004 / REQ-0002` etc. **Sipho's portal-side actions are NOT in the feed**. DB confirms scope: `SELECT event_type, actor_type, COUNT(*) FROM tenant_5039f2d497cf.audit_events WHERE entity_id::text='e788a51b-…' OR (details->>'projectId')='e788a51b-…' GROUP BY event_type, actor_type` returns 4 rows all `actor_type=USER` (matter_closure.closed×2, matter_closure.reopened×1, project.created_from_template×1, project.updated×3). Tenant-wide `WHERE actor_type='PORTAL_CONTACT'` returns only **2 rows** total (`acceptance.viewed` + `acceptance.accepted` from Day 8 proposal) — Sipho's FICA upload (Day 4), trust balance view (Day 11), fee-note pay (Day 30 mock-payment webhook fires `payment_events` not `audit_events`), info-req item submit (Day 46), SoA download (Day 61) all MISSING from `audit_events`. **NEW GAP-L-75 HIGH (verify-cycle blocker for E.14); MED (prod)** — three-layer audit-trail completeness gap: (a) no firm-side audit-log UI with filter-by-matter + filter-by-actor (Phase 69 not shipped); (b) matter Activity tab fallback has type filter only, no actor filter; (c) portal-contact write events not emitted to `audit_events`. Reconfirms + widens scope of pre-existing OBS-Day61-NoPortalDocAuditEvent (which was only about doc downloads). **Suggested fix split**: (i) Phase 69 audit-log page — M-L (~1 sprint, net-new); (ii) portal-contact audit emission — S-M (~3-4 hr, extend `PortalDocumentService`+`PortalInvoiceService`+`PortalTrustService`+`PortalInformationRequestService`); (iii) matter Activity actor filter — S (~1.5 hr, frontend combobox + query param plumbing). 0 console errors on matter Documents/Activity/Overview + Data Protection page; 1 expected error on `/audit-log` 404 probe. **Headline outcome**: Day 85 cleared at the structural level (closure paperwork attached, retention clock running per ADR-249), but the audit-trail-completeness exit gate **E.14** is FAILED — both because the surface UI does not exist (Phase 69 carve-out) and because the underlying data plane (portal-contact write events) is not being captured. **Demo-impact**: Day 88 ("firm + portal activity-feed wow moment side-by-side") will likely also BLOCK on (a) portal `/activity` surface absence (per OBS-Day75-NoPortalActivityTrail) and (b) `audit_events` portal-actor emission gap (this gap, c). **Wall-clock ~10 min total, well under 45 min target**. Evidence: `qa_cycle/checkpoint-results/day-85.md` §"Day 85 Re-Verify — Cycle 1 — 2026-04-25 SAST"; YAML snapshots `day-85-cycle1-{firm-documents-tab,firm-activity-tab,firm-activity-panel,firm-overview,data-protection,matter-documents-tab}.yml`; PNGs `day-85-cycle1-{firm-activity-feed,data-protection-retention,matter-overview-closed}.png`. Next action: Product → triage GAP-L-75 (decide which of (i)-(iii) are fix-in-cycle vs Sprint-2 followup) BEFORE Day 88 dispatch. After L-75 triage, QA dispatches Day 88 (wow-moment side-by-side) — expect partial-block on portal activity surface; close out with whatever firm-side coverage remains, then Day 90 + exit checkpoints sweep.

- 2026-04-25 SAST — Dev + Infra: **GAP-L-75c FIXED in PR #1139 (merge SHA `a76b6a93`).** 5 portal write paths now emit `audit_events` with `actorType=PORTAL_CONTACT` + load-bearing `details.project_id`: PortalInformationRequestService initiateUpload + submitItem + submitTextResponse, PortalQueryService.getPresignedDownloadUrl (portal SoA + closure-letter + info-req item downloads), PaymentReconciliationService.handlePaymentCompleted (portal mock-payment complete — moved from PaymentWebhookService per Dev's correct layering call). Drive-by: PortalCommentService now emits canonical `PORTAL_CONTACT` (was `PORTAL_USER`). Build: 370 targeted tests PASS (`*AuditEvent*,*Portal*,*Acceptance*,*PaymentWebhook*,*MockPayment*,*PaymentReconciliation*`); BUILD SUCCESS. 7 files changed, +267/-6. Backend restarted via `svc.sh restart backend` (PID 47053→new, ready in 27s). NEEDS_REBUILD=false. **Next action**: QA — (1) re-walk one portal write of each kind on existing Dlamini matter (info-req item submit, doc download, mock-payment complete) via Sipho self-service magic-link; assert DB `audit_events` count for tenant goes from 2→≥6 with PORTAL_CONTACT actor + Dlamini project_id. Mark L-75c VERIFIED. (2) Day 88 firm + portal activity-feed wow-moment per scenario (firm half via matter Activity tab, portal half via DB evidence since portal `/activity` UI absent — separate Sprint 2 followup). (3) E.14 final verdict.
