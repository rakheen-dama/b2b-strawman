# QA Cycle Status — AI Core Live-Claude Verification (Keycloak) — Cycle 2026-06-14

- **Branch**: `bugfix_cycle_2026-06-14`
- **Scenario**: `qa/testplan/demos/ai-core-verification-keycloak.md`
- **Stack**: Keycloak dev stack (frontend :3000, backend :8080, gateway :8443, KC :8180, Mailpit :8025)
- **Started**: 2026-06-14
- **Mode**: Clean slate. Session 0 = dev-up + keycloak-bootstrap + svc start all, then drop any `tenant_verifain*` schema and delete `@verifain-test.local` KC users. Orgs/users created through real onboarding flow.
- **Context**: Phases 72 & 74 shipped the AI core code-complete but never exercised against live Claude (only the `NoOpAiProvider` stub + stubbed backend tests). This cycle verifies the whole AI substrate end-to-end against **real Claude**. It is the blocker to clear before any MCP-server work (`.claude/ideas/mcp-plugin-strategy-2026-06-14.md`).

## 🔴 Human-in-the-loop dependency (RESOLVED for this run)

- A funded `sk-ant-…` Anthropic key is required. **Decision**: the human enters it themselves.
- **Flow**: QA runs V0 onboarding → orchestrator PAUSES → human logs in as Nomsa (`nomsa@verifain-test.local` / `SecureP@ss1`) in their OWN browser at http://localhost:3000 → Integrations UI → enters funded key (V1 steps 1.1–1.7), enables, runs connection test green → tells orchestrator "done" → QA resumes verifying V1 then V2–V12.
- Estimated live spend for a clean run: **< R30**. Stub-vs-live proof obligation: every skill PASS needs non-empty `output` + `costCents > 0` + non-zero token usage in backend log. `costCents == 0` / empty output = stub answered = **FAIL**.

## Per-Stage Workflow (NON-NEGOTIABLE)

For each verification stage V0–V12:

1. **QA agent walks the stage end-to-end** via Playwright MCP. Records the **Observed PASS** criterion result with evidence (browser state + backend log excerpt + gate id / audit id / costCents).
2. **Triage every gap.** Product agent decides: SPEC_READY, WONT_FIX-EXEMPT, or scenario-amendment (requires authorization).
3. **Fix every spec.** Dev agent: reproduce → full `./mvnw verify` → marker → commit → PR → review → merge.
4. **PR each bugfix into `main`** (its own PR). Pre-merge gate hook blocks unless verify markers green.
5. **Retest each fix on main** with QA agent. Mark VERIFIED only after observed end-to-end PASS.
6. **Only then advance.** Next stage starts when the current stage's blocking gaps are VERIFIED.

## Mandate
- Only acceptable open gaps: **KYC** and **Payments** integrations not yet wired (per CLAUDE.md §6 WONT_FIX exemptions). Everything else is own/fix.
- No production data; all data disposable. Backward compatibility not a priority.
- No SQL shortcuts — all operations via REST API or browser UI. Only legitimate REST use is the Mailpit email API.

## QA Position

- **Stage (current, 2026-06-15)**: **V9 + V10 + V11 + V12 complete. Scenario substantively COMPLETE except V11.1 (human-key bad-key test) + previously-deferred items (AIVERIFY-003/004/009, CORS) + the new Low AIVERIFY-013 (audit event-type naming, product call).** **V11.3 = ✅ PASS** (Anthropic prompt caching implemented on the system prompt; live cache WRITE today + a historical cache-HIT pair: compliance-audit 23:10→23:13 `cache_read=1848`, cost 125c vs cold 167–200c). **V11.4 = ✅ PASS** (matter-intake <20-char → pre-LLM 400 + UI button disabled; FICA no-docs/no-pending → pre-LLM 400; unreadable-PDF degrades to NEEDS-REVIEW not 500). **V11.1 DEFERRED** (invalidates human key). **V12 = ✅ PASS** (27 `ai.skill.invoked` + 2 `ai.gate.approved` + 2 `ai.gate.rejected` + 1 `ai.specialist.failed`; actor attribution correct — Pieter's V10.1 matter-intake attributed to Pieter, Nomsa's gate approvals/rejections to Nomsa with notes "Wrong template"; UI filters by event-type/actor/entity all work live, reconciled to DB). **⚠️ ENV: HikariPool thread-starvation re-appeared on a 2nd back-to-back live call — recommend a backend restart before further live-call-heavy work.** Evidence: `V11-final.md`, `V12-final.md`, `V9-final.md`, `V10-final.md`. (Prior:) **V9 + V10 fully complete.** **V9 = ✅ PASS (9.1–9.5).** AIVERIFY-012 VERIFIED (budget input `step="1"`; R20 saved → DB 2000c/`profile_version 11`; invalid `-3` → visible FormMessage, not silent drop). **V9.4/9.5 PASS**: with budget R20 < spend R23.99, FICA-on-Sipho rejected with a **pre-LLM 403** — backend `GlobalExceptionHandler Forbidden: ...reason=Monthly AI spend of R23.99 has reached the budget of R20.00`, UI "AI budget exhausted or skill not permitted", **NO Anthropic call**, **NO new execution row, spend UNCHANGED (R23.99, 26 exec, llm_calls=0)**. Budget RESTORED to **R5000** (`profile_version 14`) — headroom for V11/V12. (Spend reconciliation: cost-summary R23.99 = all 26 metered executions incl. 11 FAILED-with-cost; checkBudget compares full metered spend.) **V11.3 (prompt caching) + V11.4 (input validation) now PASS** (live cache write today + historical cache-HIT pair, repeat cheaper; pre-flight 400s on bad input). **V11.1 bad-key DEFERRED** (needs human key re-entry). **Next: V12 (audit trail).** ⚠️ Backend HikariPool thread-starvation re-appeared on a 2nd back-to-back live call — recommend a backend restart before any further live-call-heavy stages. Evidence: `qa_cycle/checkpoint-results/V9-final.md`, `V10-final.md`, `V11-final.md`.
- **Prior stage**: **V3–V7 all PASS (live); AIVERIFY-001 cluster MERGED to main (PR #1445, commit `fca27a476`).** V0–V7 complete. All 5 AI skills verified working against live Claude (rendered in browser, COMPLETED, gated, cost-metered) on the fix branch immediately before merge; the merged code is byte-identical, so V3–V7 are VERIFIED. Backend verify 5668 green + frontend 2344 green on the merged commit. **Next: V8** (execution-gate approval flow) — restart the stack from `main`/cycle branch (the Dev worktree was removed post-merge) and continue V8→V12.
- **V8 note**: fresh PENDING gates exist from the V3–V7 runs (SELECT_MATTER_TEMPLATE, CONFIRM_CONFLICT_SCREEN, MARK_KYC_COMPLETE, CREATE_REVIEW_REPORT, CREATE_DRAFT_DOCUMENT, Publish-Compliance-Report). AIVERIFY-008 (compliance publish-gate→report persistence) should be checked as part of V8.
- **Live testing peeled 3 layers** of the same bug class (all fixed on the branch): (1) parse-rollback → FAILED-with-cost; (2) API `output` was a raw fenced string → now a parsed JSON object; (3) skill `GateDto` lacked `executionId`/`createdAt` (crashed `ExecutionGateCard`) → now included. Plus 2 frontend mappers (contract-review, drafting) `JSON.parse`-d the now-object → simplified to passthrough.
- **Gate ids for V8** (from the V3 render run): PENDING `SELECT_MATTER_TEMPLATE` + `CONFIRM_CONFLICT_SCREEN` per invocation (execution `5bdc4399…`). Fresh gates exist.
- **Resolved org slug**: `verifain-attorneys` (tenant `tenant_c6107524c9b4`, org KC id `221d86fb-f1b6-440d-b2df-873239f6d784`, profile `legal-za`). V0 evidence + full detail: `qa_cycle/checkpoint-results/V0.md`. V1/V2/V3 detail: `qa_cycle/checkpoint-results/V1.md`, `V2.md`, `V3.md`.
- **Test client**: Sipho Dlamini (INDIVIDUAL), id `809563f8-1feb-4043-bbd7-c9aeaf356900`.

## Dev Stack

- **Status**: Running (RESUME, 2026-06-15). Local services restarted after the V11 pause: backend :8080 UP, gateway :8443 UP, frontend :3000 (200); KC realm `docteams` 200 (portal intentionally NOT started — not needed for AI verification). Docker infra (b2b-postgres, b2b-keycloak, b2b-mailpit, b2b-localstack) all healthy (up 17h). Backend started cleanly in 10.4s: **0 ERROR-level log entries**, no encryption-key / SecretStore / Anthropic / "Request cancelled" errors at startup. **Orphaned-row cleanup (resume checklist item 7):** queried `tenant_c6107524c9b4.ai_executions` for `status='IN_PROGRESS'` → **0 rows** (the orphaned IN_PROGRESS execution from the prior session was already resolved — most recent executions are all terminal FAILED/COMPLETED; the backend+gateway restart during the pause cleared it). No UPDATE needed. Env looks ready for live verification; the QA agent will confirm Anthropic key health/balance on its first real skill call.

## Flags

- **ENV-HEALTH (2026-06-15, V11):** a 2nd back-to-back live FICA ("Run Again") hung with coincident **HikariPool thread-starvation** warnings (housekeeper delta up to 1m21s, 12:35–12:37). Backend stayed UP (no "Request cancelled" — thread saturation, not socket death); no orphan left (0 IN_PROGRESS). Recommend a **backend restart before any further live-call-heavy stage** (e.g. V11.1 when run). Possible latent gap: connection/thread pool pressure under rapid concurrent skill calls — distinct from AIVERIFY-002 (which split the tx); worth a look if it recurs. (Orchestrator restarted backend post-V12.)

## Tracker

| Gap ID | Stage | Summary | Severity | Owner | Status |
|--------|-------|---------|----------|-------|--------|
| AIVERIFY-001 | V3 | **Matter-intake skill fails to parse live Claude output.** Claude returns JSON wrapped in a ```` ```json ```` markdown fence; backend calls `objectMapper.readValue` on the raw fenced text (`MatterIntakeSkill.java:220`) → `JacksonException` "Unexpected character ('`' code 96)". Because `createGates` throws inside the `@Transactional executeSkill` (l.82) AFTER the COMPLETED save (l.155) and OUTSIDE the LLM try/catch (l.137), the parse exception **rolls back the entire execution** — `ai_executions`/`ai_llm_calls`/`ai_specialist_invocations`/`ai_execution_gates` all 0 rows. Real Anthropic cost incurred but never metered; failure not even recorded as FAILED. Confirmed (file reads): same raw-`readValue` pattern in all 5 skills (intake:220, fica:177, contractreview:152, drafting:246, complianceaudit:200) and **no fence-strip helper exists anywhere** → genuine shared defect class, V4–V7 affected. PR #1443 ruled out as cause (only touched `AnthropicAiProvider.DEFAULT_MODEL`, not the skill parse path). Spec: `qa_cycle/fix-specs/AIVERIFY-001.md`. | **BLOCKER** | Product/Dev | **VERIFIED (merged #1445; all 5 skills live-PASS)** |
| AIVERIFY-002 | V3 | **HikariCP connection leak during skill execution.** `@Transactional executeMatterIntake` holds a DB connection open for the entire multi-second Anthropic HTTP round-trip → `ProxyLeakTask: Apparent connection leak detected` (origin `AiSkillExecutionService$$SpringCGLIB$$0.executeMatterIntake`). **Same root cause as 001 — LLM call + persistence share one long transaction. Product recommends ONE PR with 001** (fix spec §"One fix or two?"). | Medium | Dev | **VERIFIED (bundled in #1445; tx split, no leak)** |
| AIVERIFY-003 | V3 | **404 on customer-page assistant widget.** `GET /api/assistant/invocations?contextEntityType=customer&contextEntityId=...&status=PENDING_APPROVAL&size=10` → 404 from the customer-detail page. **Independent root cause** — endpoint exists (`AiSpecialistInvocationController.java:37–55`, accepts all those params; `PENDING_APPROVAL` is a valid enum); 404 is a `@RequiresCapability("AI_ASSISTANT_USE")` denial (security-by-obscurity) or FE/gateway path mismatch, NOT the parse defect. **Product recommends DEFER** to a separate investigation after V3 re-verified. | Low | Dev | OPEN (defer) |
| AIVERIFY-010 | V9.3 | **AI Monthly Budget save: no `PUT /api/ai/profile` reaches backend.** REOPENED — PR #1446 (`ceb51c856`) patched the wrong layer (budget field + budget zod preprocess). REAL root cause CONFIRMED + reproduced: `ai-profile-form.tsx:137-138` coerces empty notes via `houseStyleNotes: data.houseStyleNotes \|\| null` → `null`; the schema `ai-profile.ts:11,19` (`z.string().max(2000).optional().or(z.literal(""))`) does NOT accept `null`; the server action `settings/ai/actions.ts:19-22` re-`safeParse`s `submitData` and FAILS on the `null` notes → returns `{success:false,"Invalid input"}` and **never calls the PUT**. Repro: server-action-shaped payload with empty notes → `FAIL :: houseStyleNotes=Invalid input \| feeEstimationNotes=Invalid input`. NOT an RHF abort (handleSubmit fires onSubmit fine). Fix: schema accept `null` on the two notes fields + add `onInvalid` so rejections never swallow. Frontend-only, no migration. Spec: `qa_cycle/fix-specs/AIVERIFY-010.md`. **Fixed PR #1447 (`5290836b7`→main).** | **BLOCKER** | Dev | **VERIFIED** (2026-06-15 live: budget 7000→6000 persisted, DB `profile_version 8`, PUT fired w/ empty notes; reload + DB confirm. V9-final.md) |
| AIVERIFY-011 | V10.1 | **Members with AI_EXECUTE lack an AI sidebar entry + see false "not configured" intake.** Product DECIDED: members CAN execute AI (backend `V122:141-148` grant is correct; live-confirmed `org_role_capabilities` member has AI_EXECUTE). Root cause: (A) `nav-items.ts:303-316` AI group has only an `AI_REVIEW` item → members get no AI nav at all; (B) skill BUTTONS already correctly gated on `AI_EXECUTE` (customers:642 / projects-new:31 / documents-panel / compliance:33) — no change; (C) `projects/new/page.tsx:35-41` calls AI_MANAGE-gated `getAiProfile()` unconditionally → 403 for members → false "Connect an API key" disabled intake. Fix: add an `AI_EXECUTE`-gated AI nav item + guard `getAiProfile()` behind AI_MANAGE (member⇒isAiConfigured=true), mirroring customers/compliance. Approval (`AI_REVIEW`) + profile (`AI_MANAGE`) UI stays owner/admin. Frontend-only. Spec: `qa_cycle/fix-specs/AIVERIFY-011.md`. **Fixed PR #1448 (`f683a4170`→main).** | Medium | Dev | **VERIFIED** (2026-06-15 live as Pieter: AI Intake nav visible + intake enabled + matter-intake COMPLETED R1.56/4719 tok attributed to Pieter; still 403 on /ai/reviews + /settings/ai. V10-final.md) |
| AIVERIFY-013 | V12.1 | **Audit event-type naming mismatch vs scenario.** Scenario V12.1 expects `ai.specialist.invoked / .approved / .rejected`. The live skill+gate flow actually emits **`ai.skill.invoked`** (27, via `AiExecutionPersistenceService`) + **`ai.gate.approved`** (2) + **`ai.gate.rejected`** (2); only `ai.specialist.failed` (1) matches the literal scenario string. The `ai.specialist.*` invoked/approved/rejected types ARE registered (`AuditEventTypeRegistry.java:114-148`) but are emitted by the older automation/specialist-invocation path, not the live skill/gate flow. **Substantive property holds** — invoked/approved/rejected/failed are all distinctly recorded with correct actor attribution + notes + skill/entity, and are filterable; this is a naming/labelling discrepancy only, NOT a missing-audit defect. Disposition = product call: (a) accept `ai.skill.*`/`ai.gate.*` as canonical + amend scenario strings (authorized amendment), or (b) align emitted names to `ai.specialist.*`. NOT auto-disposed (§6). Found 2026-06-15 V12. Evidence: V12-final.md. **RESOLVED 2026-06-15 (user authorized option (a)): scenario V12.1 amended to `ai.skill.*`/`ai.gate.*` (commit on cycle branch); no code change. V12 PASS stands.** | Low | Product | **RESOLVED (scenario-amended, authorized)** |
| AIVERIFY-012 | V9.4 | **Monthly Budget input silently drops step-mismatched values.** The budget `<Input type="number">` has `step={100}` (`ai-profile-form.tsx:543`), so the smallest non-zero settable budget is R100 (10000c). Any step-mismatched value (R1/R15/R20/R33) **fails to save with NO error surfaced** — DB `profile_version`/`updated_at` frozen, no success/`onInvalid`/`FormMessage`, even though React `field.value` correctly holds the typed value. Step-valid values (4000/5000/6000) persist reliably. Mechanism: step-mismatch → submit resolves `monthlyBudgetCents`→null → server-action `safeParse` passes (budget optional) → PUT sends `monthlyBudgetCents:null` → entity `updateProfile` keeps old value (`x != null ? x : this`). **Blocks V9.4/9.5**: current spend R22.68 < R100 (min settable budget), so budget-below-spend cannot be set via the UI to drive the pre-LLM 403. Also a real UX bug (no feedback on rejected budget). Found 2026-06-15 during V9.4 verify. Evidence: V9-final.md. Confirmed mechanism (Product): browser empties `<input type=number>` value on step/min mismatch → onChange cleared-field branch sets `undefined` → zod optional passes (so AIVERIFY-010's `onInvalid` can't catch it) → PUT omits budget → entity keep-old. No backend constraint. Fix: `step={1}` + harden onChange to `setError` (FormMessage) on typed-but-invalid. Spec: `qa_cycle/fix-specs/AIVERIFY-012.md`. **Fixed PR #1449 (`25491cde7`→main).** | Medium | Product/Dev | **VERIFIED** (2026-06-15 live: `step="1"` in DOM; R20 saved → DB `monthly_budget_cents=2000` `profile_version 11`; invalid `-3` → visible FormMessage "Enter a whole Rand amount of zero or more." not silent drop. V9-final.md) |

## Stage Checklist

| Stage | Description | Result |
|-------|-------------|--------|
| V0 | Onboard firm (Nomsa owner, Pieter member) | ✅ PASS |
| V1 | BYOAK key configuration (human enters key) | ✅ PASS (live Claude proven) |
| V2 | Firm AI profile cold-start | ✅ PASS |
| V3 | Matter-intake skill (live Claude) | ✅ PASS (after AIVERIFY-001 fix; awaiting merge to main) |
| V4 | FICA verification skill (live Claude) | ✅ PASS (live; 544/3441 tok, R1.10, MARK_KYC_COMPLETE gate) |
| V5 | Contract review skill (live Claude) | ✅ PASS (live; 7071 tok/131s, R2.14, CREATE_REVIEW_REPORT gate; needed max_tokens+timeout) |
| V6 | Drafting skill (live Claude) | ✅ PASS (live; 7268 tok/130s, R2.26, CREATE_DRAFT_DOCUMENT gate) |
| V7 | Compliance audit skill (live Claude) | ✅ PASS (live; 5985 tok/107s, R1.80, Publish-Compliance-Report gate) |
| V8 | Execution-gate approval flow | ✅ PASS |
| V9 | Cost metering & budget enforcement | ✅ PASS — 9.1/9.2 PASS; 9.3 PASS (AIVERIFY-010 VERIFIED); **9.4/9.5 PASS** (AIVERIFY-012 VERIFIED, R20<spend → pre-LLM 403, no spend leak) |
| V10 | Capability / RBAC gating (Pieter) | ✅ PASS — 10.1 (member CAN execute, AIVERIFY-011 VERIFIED) + 10.2 + 10.3 all PASS |
| V11 | Error & graceful-degradation paths | ✅ PARTIAL/PASS — 11.3 (prompt caching) + 11.4 (input validation) PASS; **11.1 (bad-key) DEFERRED** (needs human key re-entry); 11.2 optional, not run |
| V12 | Audit trail | ✅ PASS — events exist (invoked/approved/rejected/failed), correct actor attribution (Nomsa vs Pieter incl. V10.1), reviewer notes, filterable by type/actor/entity (live, reconciled to DB). 1 Low finding AIVERIFY-013 (event-type naming) |

## Log

- **2026-06-15 (User decisions + AIVERIFY-013 resolved; deferred gaps reopened for fix)** — At the V0–V12 milestone (all blocking defects fixed + scenario substantively verified) the user decided: (1) **AIVERIFY-013 = accept + amend scenario** → scenario V12.1 + summary amended to `ai.skill.*`/`ai.gate.*` (authorized §6 amendment, no code change); AIVERIFY-013 → RESOLVED; V12 PASS stands. (2) **Fix the four previously-deferred gaps NOW in this cycle** — AIVERIFY-003 (assistant-widget 404), -004 (compliance concurrency), -009 (gate-card hydration/locale), CORS double-Origin on /ai/reviews approve. Product triage → Dev (one PR each) → QA verify. (3) **V11.1 bad-key = SKIP/DEFER** (would invalidate the human's funded key; error-path partly covered by V11.4 PASS). Next action: Product agent triages the four deferred gaps into specs.
- **2026-06-15 (Dev/Orchestrator, AIVERIFY-012 MERGED — socket-death salvage)** — PR **#1449** → main, squash `25491cde7`. **MERGED-AWAITING-VERIFY.** Two consecutive subagent socket deaths (Product agent, then Dev agent) mid-run; orchestrator salvaged per zombie-builder protocol. Dev agent's core edit was sound (`step={100}`→`step={1}` + 3-way `interpretBudgetInput` helper surfacing a FormMessage on typed-but-invalid instead of silently coercing to "no cap"). Orchestrator finished inline: extracted the helper from the Client Component into **`lib/ai-budget-input.ts`** (component import chain pulls `server-only` → blocked vitest import), added `lib/__tests__/ai-budget-input.test.ts` (7 cases incl. the "non-empty NaN must be invalid not no-cap" regression guard), ran full verify (lint 0 err / build ok / **377 files 2370 passed 2 skip**), marker `c1f5a5ab6` exit 0, merge-gate passed clean. `feature-dev:code-reviewer` → **APPROVE** (valid-save/clear-field/error-clear paths all correct; one low-sev UX nit — invalid keystroke over an *already-empty* field flashes an error then saves "no cap" on submit — NOT a regression, logged as minor follow-up, out of scope). Merged into cycle branch (frontend HMR'd). **QA must live-verify** V9.4/9.5: set R20 budget (< R22.68 spend) → saves + DB → skill → pre-LLM 403, no spend → bad input shows FormMessage → restore R5000.
- **2026-06-15 (Product, AIVERIFY-012 triage)** — Root cause confirmed in code (spec `qa_cycle/fix-specs/AIVERIFY-012.md`): `ai-profile-form.tsx:543` budget input `step={100}`; on step/min mismatch the browser blanks the DOM value → onChange cleared-field branch (`:556-559`) sets `monthlyBudgetCents=undefined` → zod preprocess (`ai-profile.ts:29-33`) accepts `undefined` as valid-optional, so the AIVERIFY-010 `onInvalid` never fires (resolver *succeeds*) → server-action passes → PUT omits budget → entity keep-old (`AiFirmProfile.java:109-110`, correct). No backend `@Min`/`multipleOf` constraint (`UpdateAiFirmProfileRequest.java` plain Long). Fix (frontend-only, one PR): `step={100}`→`step={1}` (orchestrator-authorized — needed to set budget < R22.68 spend for V9.4/9.5) + harden onChange so a non-empty-but-invalid value calls `form.setError` (renders `<FormMessage/>`) instead of silently coercing to "no cap". → **SPEC_READY**. (Product agent socket died before committing; orchestrator finished the bookkeeping.)
- **2026-06-15 (Dev, AIVERIFY-011 MERGED + main→cycle merge)** — PR **#1448** → main, squash `f683a4170`. **MERGED-AWAITING-VERIFY.** Fix: added `AI_EXECUTE`-gated "AI Intake" nav item (`nav-items.ts`; empty groups render null so members previously got no AI sidebar) + guarded `getAiProfile()` behind `AI_MANAGE` in `projects/new/page.tsx` (unconditional call 403'd for members → false "Connect a key" disabled intake). Skill buttons already `AI_EXECUTE`-gated (untouched). Owner/admin `AI_REVIEW`/`AI_MANAGE` UI unchanged (2 reviewer passes confirm no priv-esc). Reproduced w/ failing nav tests on origin/main. Frontend verify green (376 files, 2363 passed), CodeRabbit 0 comments. **Both #1447+#1448 now merged into cycle branch** (clean ort merge) → running frontend HMR'd. **QA must live-verify as member Pieter** (AI nav + skill 200 + new-matter intake works + still blocked from approval/profile) → re-verify V10.1.
- **2026-06-15 (Dev, AIVERIFY-010 — 2nd attempt MERGED)** — PR **#1447** → main, squash `5290836b7`. **MERGED-AWAITING-VERIFY.** Real root cause reproduced w/ vitest (not spec-trust): server action `actions.ts:19-22` re-`safeParse`s before the PUT; form coerces empty notes `|| null` → `null`; zod notes fields rejected `null` → `{success:false}`, PUT never sent. #1446 missed it (only touched budget field). Fix: notes fields `.nullable().optional()`, `onInvalid` callback surfaces future RHF rejections, Textarea `value ?? ""`. Frontend verify green (376 files, 2358 passed, 2 skip), marker `83a5feca3` exit 0, gate passed clean. code-reviewer pass clean. **QA must live-verify** budget save (200 PUT w/ monthlyBudgetCents+houseStyleNotes:null → reload persists → DB) → then V9.3/9.4/9.5.
- **2026-06-14** — Cycle initialized. Archived completed Legal-ZA cycle (ALL_DAYS_COMPLETE, 0 open gaps) to `qa_cycle/_archive_2026-06-13_legal-full-lifecycle-kc/`. Branch `bugfix_cycle_2026-06-14` created. Fresh tracker seeded. Human-in-the-loop key decision: human enters key at V1. Stack decision: clean-slate startup. Next action: Infra agent (Session 0 clean-slate startup).
- **2026-06-14 (V0–V7 complete)** — Infra clean-slate up; V0 onboarding PASS (Nomsa owner, Pieter member, legal-za). V1 BYOAK (human key) + V2 cold-start PASS. **All 5 AI skills (V3–V7) now verified working end-to-end against LIVE Claude** after the AIVERIFY-001 fix cluster on `fix/AIVERIFY-001` (PR #1445). Live testing peeled the onion through 6 distinct "never-run-live" defects, all fixed on the branch: **001** parse-rollback→FAILED-with-cost + transaction split (incl. review fixes: broaden catch to RuntimeException, parser prefer-`{`); **001** API `output`→parsed JsonNode object; **001** skill `GateDto` +executionId/+createdAt; **001** frontend contract-review/drafting mappers→passthrough; **005** max_tokens 4096→16384 (was truncating); **006** HTTP timeout 120s→240s (verbose skills exceeded it). Captured real Claude responses for all 5 skills → `qa_cycle/ai-live-captures/` (mock fixtures). Frontend verified green (lint 0 err, build ok, 2344 tests). **Deferred (separate PRs):** AIVERIFY-003 (PendingSuggestionsWidget + `/api/assistant/invocations` 404 — distinct async read-model path), AIVERIFY-004 (compliance-audit concurrency-guard window narrowed by the tx split — non-data-loss).
- **2026-06-14 (review pass 2 + V7 nuance)** — Independent code review of PR #1445 found: (BLOCKER) `compliance-audit.ts` still `JSON.parse`-d the now-object `output` → nulled it; fixed to passthrough. (HIGH) DTO used a rogue `new ObjectMapper()`; replaced with the Spring-managed `ObjectMapper`+`LlmJsonParser` injected into `AiSkillController` and threaded through `from(...)`. Frontend re-verified green; backend re-verify running on `66be148e5`. **V7 nuance:** compliance skill EXECUTION passes (COMPLETED, output object populated, gated, metered), but the *detailed findings UI* renders from the persisted report list (post "Publish Compliance Report" gate approval) — the post-invoke banner shows only grade+summary by design. **AIVERIFY-008 (defer, V8):** verify the compliance publish-gate→`compliance_audit_reports` persistence→detail-render flow (one ad-hoc approve attempt left the gate PENDING with no report row — needs proper V8 investigation). Next: merge PR #1445 on green verify; re-verify on main; V8–V12.
- **2026-06-14 (QA, V0)** — Onboarding walked end-to-end via Playwright MCP. **V0 PASS (all sub-steps 0.1–0.4).** Org slug resolved = `verifain-attorneys` (tenant `tenant_c6107524c9b4`). Vertical profile `legal-za` auto-assigned — confirmed via backend provisioning log (legal-za field/template/compliance/AI-specialist packs installed). Nomsa Verifain registered + logged in as **Owner** (Team page row). Pieter Botha invited as plain **member** (no 2-member plan gate hit), registered + logged in; KC shows only `default-roles-docteams` (no AI roles) and his sidebar lacks the "AI" nav item — clean RBAC negative-test subject for V10. 0 console errors throughout. Evidence: `qa_cycle/checkpoint-results/V0.md` + `v0-*.png`. No defects filed. **STOPPED before V1** for human key entry (BYOAK gate). Noted testing-only session-handoff caveat (KC SSO "already authenticated" when switching actors on invite links — resolved via admin-API session logout + browser-context reset; not a product defect).
- **2026-06-14 (QA, V1–V3)** — Walked V1→V3 via Playwright MCP as Nomsa (had to sign out a stale Pieter session + re-auth via Keycloak). **V1 PASS (live Claude proven):** AI card = anthropic / ••••nWbAAA / Sonnet 4.6 / Enabled / Active; connection test is a real Anthropic round-trip — proof = `testConnectionAction` latency 1427–1824 ms (vs ~25 ms for stubbed actions) + `integration.connection_tested {"success":true,"providerSlug":"anthropic"}` audit events + slug routing to `AnthropicLlmProvider.validateKey` (real `POST /v1/messages`). Stub ruled out. Negative bogus-key check deferred to V11. **V2 PASS:** firm AI profile cold-start persisted (`ai_firm_profiles`: cold_start_completed=t, ZA-WC, CONSERVATIVE, budget 500000c, claude-sonnet-4-6, Litigation+Conveyancing, FICA EDD+PEP, house-style + fee notes); page flipped "Set Up AI"→"AI Configuration"/"Save Configuration" after reload; usage panel renders R0/0. **V3 FAIL — BLOCKER (AIVERIFY-001):** matter-intake — live Claude responds but backend `objectMapper.readValue` chokes on Claude's markdown-fenced JSON (`MatterIntakeSkill.java:220`); the parse exception propagates out of the `@Transactional executeSkill` (outside the LLM try/catch) and **rolls back everything** → 0 rows in ai_executions/ai_llm_calls/ai_specialist_invocations/ai_execution_gates, real cost never metered, **no gate created** (V8 has nothing to approve). Same raw-readValue pattern in all 5 skills → V4–V7 almost certainly affected. Also filed AIVERIFY-002 (Hikari connection leak: JDBC connection held across the LLM HTTP call) and AIVERIFY-003 (404 on customer-page assistant-invocations widget). **QA HELD at V3** per stop-on-blocker rule — do not advance to V4 until AIVERIFY-001 fixed + V3 re-verified on main. Test client Sipho Dlamini = `809563f8-1feb-4043-bbd7-c9aeaf356900`. Evidence: `qa_cycle/checkpoint-results/V1.md`, `V2.md`, `V3.md` + `v1-*`/`v2-*`/`v3-*.png`.
- **2026-06-14 (Product, AIVERIFY-001 triage)** — Root cause **confirmed in actual code** (not hypothesised). (1) All 5 skills parse raw LLM text via `objectMapper.readValue` (intake:220, fica:177, contractreview:152, drafting:246, complianceaudit:200), each `catch(JacksonException)→InvalidStateException`; **no fence-strip helper exists in the codebase** — genuine shared defect class. (2) Transaction boundary: `AiSkillExecutionService.executeSkill` is `@Transactional` (:82); LLM call is the only thing in its own try/catch (:111–150); cost+COMPLETED save at :153–155; `createGates` at :159 is **outside** the try/catch **inside** the tx → its parse exception rolls back the metered execution (0 rows). (3) Cost metered at :153–155 inside the rolled-back tx → lost on parse failure; `markFailed` (AiExecution.java:102–106) carries no cost. (4) `ai.specialist.failed` is registered (AuditEventTypeRegistry:130) but **never emitted**. (5) Provider path confirmed = `AnthropicAiProvider` (domain=AI), distinct from chat's `AnthropicLlmProvider`; **PR #1443 ruled out** (only changed `DEFAULT_MODEL`, not parse code). **Elegant fix (3 parts): (a) one shared `LlmJsonParser` fence-stripper routing all 5 skills; (b) guard `createGates` so an unparseable response is recorded FAILED-with-cost-metered + emits `ai.specialist.failed`, never a full rollback; (c) split the long transaction into pre-flight / LLM-call-without-connection / short-persist (resolves AIVERIFY-002).** **Recommendation: ONE PR for 001+002** — shared transaction-boundary root cause; splitting would edit the same method twice and leave the leak open one cycle (CLAUDE.md §7 same-bug-class cluster). **AIVERIFY-003 = independent (capability/route, not parse) → DEFER.** Spec: `qa_cycle/fix-specs/AIVERIFY-001.md`. AIVERIFY-001→SPEC_READY, 002→SPEC_READY(fold), 003→defer. Effort M. No migration. New tests: `LlmJsonParserTest` + unparseable-output→FAILED-with-cost case in `AiSkillExecutionServiceTest` + fenced happy-path in `MatterIntakeSkillTest`; all stub-based, verifiable by `./mvnw verify` without a live key.
- **2026-06-14 (Infra, Session 0)** — Clean-slate stack brought up. Docker infra verified healthy (postgres/keycloak/mailpit/localstack). `keycloak-bootstrap.sh` run idempotently — padmin@docteams.local confirmed. Clean-slate teardown (0.A/0.B): **0 `tenant_verifain*` schemas** found, **0 `@verifain-test.local` KC users** found, **0 verifain `access_requests`** rows in public schema — environment was already clean, nothing to drop/delete. Encryption-key check (0.C): `integration.encryption-key` present in `application-local.yml:86` (valid 32-byte base64 dev key), backend launches with `SPRING_PROFILES_ACTIVE=local,keycloak` — **no encryption/SecretStore startup error**; V1 BYOAK unblocked. `svc.sh start all` → all 4 services healthy on first attempt. Sanity probes: backend/gateway `/actuator/health` UP, frontend 200, KC realm 200. QA Position remains V0.

- **2026-06-15 (V8 PASS, V9 partial, AIVERIFY-010 REOPENED)** — V8 gate-approval flow PASS (approval flips checklist→COMPLETE; rejection no-op; state guard clean 400; AIVERIFY-008 resolved: publish-gate persists report + detail renders). V9: 9.1/9.2 PASS (metering R-accurate, reconciles to DB). **9.3 FAIL — AIVERIFY-010 REOPENED**: PR #1446 merged (`ceb51c856`) but did NOT fix the save. Ground truth (network capture): editing the Monthly Budget field makes `form.handleSubmit(onSubmit)` **silently abort before any request is sent** — an RHF validation rejection with no surfaced FormMessage/toast; NO `PUT /api/ai/profile` reaches the backend. The merged fix corrected the field display/onChange + zod (unit-verified) but not the submit-block. Next dev step: instrument `form.formState.errors` on submit to find the blocking field, fix at root, **live-verify (network body shows the value + reload persists + DB confirms) BEFORE merge**. 9.4/9.5 blocked by 9.3 (backend 403 logic present + unit-verified: `AiCostService.checkBudget`→ForbiddenException pre-LLM, no spend). **Process miss owned:** #1446 merged on green markers + clean review WITHOUT a live pre-merge check — the exact thing the cycle warns against. Proceeding to V10–V12 (independent of budget-save) per user directive; AIVERIFY-010 to follow.

- **2026-06-15 (V10 partial, V11/V12 blocked by env)** — V10 RBAC: **10.2 (AI_REVIEW) + 10.3 (AI_MANAGE) PASS** (member Pieter gets clean 403 + UI gating on gate-approve and profile-edit). **10.1 (AI_EXECUTE) — PRODUCT DECISION NEEDED (AIVERIFY-011)**: UI hides AI skill buttons from members, but the backend deliberately grants `AI_EXECUTE` to the `member` role (`V122__ai_foundation.sql:141-148`, explicit comment), so a plain member CAN invoke all 5 skills via the API (200). Scenario assumed members lack AI_EXECUTE. Not auto-disposed (§6). Decision: (a) members SHOULD execute AI → fix the UI to show skill buttons for members (UI/backend inconsistency is the bug); or (b) members should NOT → remove AI_EXECUTE from the V122 member seed. **V11 — BLOCKED (environmental)**: live Claude calls began failing with `I/O error … "Request cancelled"` (backend→Anthropic hanging/cancelled) after ~25+ live calls + a long-running V11 agent (2.6h, socket died). Likely key rate-limit/throttle and/or backend thread saturation; one orphaned IN_PROGRESS execution left. 11.1 (bad-key) deferred regardless — it invalidates the stored key and needs human re-entry. Backend+gateway restarted to clear saturation. **V12 — not started.** Recommend resuming V11.3/11.4 + V12 on a healthy env (and confirm the Anthropic key still has balance/headroom).

- **2026-06-15 (DECISIONS + CYCLE PAUSED)** — User decisions: **AIVERIFY-011 = "members CAN execute AI"** → backend `AI_EXECUTE`-for-member seed is CORRECT; the **bug is the UI hiding AI skill buttons from members** — fix the frontend to SHOW skill buttons/nav for members (owners/admins keep exclusive AI_REVIEW + AI_MANAGE). Scenario V10.1 is authorized-amended to expect members CAN execute. **Cycle PAUSED at V11** (live-Claude env degraded: `"Request cancelled"` on backend→Anthropic; resume on a healthy env + confirm key balance). Local svc services stopped to free resources; Docker infra left up.
- **2026-06-15 (Infra, resume)** — Restarted local services on the cycle branch to resume from the V11 pause. Docker infra confirmed healthy (b2b-postgres/keycloak/mailpit/localstack, up 17h). `svc.sh start backend gateway frontend` → all three ready on first attempt (backend 15s, gateway 6s, frontend 3s; portal deliberately skipped to conserve resources). Health probes: backend :8080 `/actuator/health` UP, gateway :8443 UP, frontend :3000 → 200, KC realm `docteams` → 200. Backend log clean (0 ERROR entries; no encryption/SecretStore/Anthropic/"Request cancelled" at startup; started in 10.4s). **Orphaned IN_PROGRESS cleanup (checklist item 7):** SELECT on `tenant_c6107524c9b4.ai_executions WHERE status='IN_PROGRESS'` returned **0 rows** — already resolved (recent rows all terminal FAILED/COMPLETED). No UPDATE applied; reported as-is. Env ready for live verification; QA agent to confirm Anthropic key balance on first real skill call. Did NOT make a live Claude call (budget/auth).

- **2026-06-15 (Product, resume triage)** — Two fix specs written, both root-cause CONFIRMED in code + reproduced (not hypothesised), both Frontend-only, independent, one PR each (CLAUDE.md §7). **AIVERIFY-010 (BLOCKER) — REAL layer found:** the save break is NOT an RHF `handleSubmit` abort and NOT the budget field (correct post-#1446). It's the server action's defensive re-validation: `ai-profile-form.tsx:137-138` turns empty notes into `null` (`"" \|\| null`), the schema `ai-profile.ts:11,19` rejects `null` (accepts only string/undefined/`""`), and `settings/ai/actions.ts:19-22` `safeParse`s `submitData` BEFORE the PUT → fails → returns "Invalid input" and **never calls `updateAiProfile()`** → no `PUT /api/ai/profile`, exactly the network capture. Reproduced via vitest: server-action-shaped payload with empty notes → `FAIL :: houseStyleNotes=Invalid input \| feeEstimationNotes=Invalid input`. `git show ceb51c856` confirms #1446 only touched the budget field + budget preprocess, never the notes. Fix = schema accept `null` on the two notes fields + add `onInvalid` to surface rejections. **AIVERIFY-011 (DECIDED) — UI fix, narrower than first framed:** skill buttons are ALREADY gated on `AI_EXECUTE` everywhere (no change). Real gaps: (A) `nav-items.ts:303-316` AI group only has an `AI_REVIEW` item, so members get no AI sidebar entry — add an `AI_EXECUTE`-gated item; (C) `projects/new/page.tsx:35-41` calls `AI_MANAGE`-gated `getAiProfile()` unconditionally → 403 for members → false "not configured" intake — guard it behind `AI_MANAGE` (member⇒configured), mirroring customers/compliance pages. Member `AI_EXECUTE` grant live-confirmed in `tenant_c6107524c9b4` (V122 applied). Both → SPEC_READY. Specs: `qa_cycle/fix-specs/AIVERIFY-010.md`, `AIVERIFY-011.md`. Effort S each. No migration. Both MUST be live-verified BEFORE merge (010: network PUT body + reload + DB; 011: member skill 200 + still-denied approval/profile).

  - **RESUME CHECKLIST (next session):** (1) `svc.sh start backend gateway frontend` (cycle branch has all merged fixes); confirm a quick live skill call completes (env healthy + key has balance). (2) **AIVERIFY-010** — instrument `form.formState.errors` in `ai-profile-form.tsx` to find the field that silently aborts submit when budget is edited; fix at root; **live-verify (network body carries monthlyBudgetCents + reload persists + DB confirms) BEFORE merge.** Then finish V9.4/9.5. (3) **AIVERIFY-011** — UI fix: show AI skill buttons/nav for members (they have AI_EXECUTE); re-verify V10.1. (4) **V11.3** prompt caching, **V11.4** input validation; **V11.1** bad-key needs human to re-enter the real key afterward. (5) **V12** audit trail (`ai.specialist.invoked/approved/rejected/failed`, actor attribution, filters). (6) Deferred: AIVERIFY-003 (assistant-invocations 404 widget), AIVERIFY-004 (compliance concurrency), AIVERIFY-009 (gate-card hydration/locale), CORS double-Origin on /ai/reviews approve. (7) Clean orphaned IN_PROGRESS ai_executions row.

- **2026-06-15 (QA, resume — V9/V10 verify)** — Resumed from the V11 pause on a healthy env. **Env-health gate PASS**: live FICA on Sipho as Nomsa COMPLETED, `cost_cents=114 / 544 in / 3570 out`, no `"Request cancelled"` — Anthropic key healthy, degradation cleared. **AIVERIFY-010 → VERIFIED (V9.3 PASS):** reproduced the exact prior-failure condition (both notes emptied), changed Monthly Budget 7000→6000, Save → DB `monthly_budget_cents=600000`, `profile_version 8`, `updated_at` advanced; server-action POST 200; reload shows 6000. The PUT fires + persists with empty notes — the 3rd-attempt fix works. (Backend `AiFirmProfileService.updateProfile` has no budget-vs-spend rejection; entity keeps old budget only on null input.) **AIVERIFY-011 → VERIFIED (V10.1 PASS):** signed out Nomsa (gateway `/logout` + `context.clearCookies()` for the httpOnly BFF cookie), fresh KC login as **Pieter** (member). Sidebar now has an "AI Intake" nav item (absent at V0); `/projects/new` intake is ENABLED (no false "Connect a key"); Pieter ran **matter-intake → COMPLETED, R1.56, 1186 in / 4719 out, `invoked_by`=Pieter** (live, metered, attributed). Negative invariants still hold: `/ai/reviews` → "You do not have permission to review AI actions" (V10.2); `/settings/ai` → "You do not have permission to manage AI settings", config form not rendered (V10.3). No privilege over-exposure. **NEW DEFECT AIVERIFY-012 (Medium, OPEN):** Monthly Budget input `step={100}` → min settable budget R100 (>R22.68 spend), and step-mismatched values (R1/R15/R20/R33) **silently fail to save with no error** (DB unchanged, no FormMessage/onInvalid) even though React holds the value — confirmed step-valid (4000/5000/6000) persist, step-mismatched no-op. **Blocks V9.4/9.5** (can't set budget below spend via UI; SQL shortcut not allowed). Budget RESTORED to **R5000** (profile_version 10) for downstream stages. Spend now R22.68 (15 COMPLETED). Evidence: `qa_cycle/checkpoint-results/V9-final.md`, `V10-final.md`, `v9-health-gate-fica-complete.png`, `v9-3-budget-saved-6000-persists.png`, `v9-budget-restored-5000.png`, `v10-pieter-ai-intake-nav-visible.png`, `v10-1-pieter-matter-intake-completed-metered.png`, `v10-2-pieter-ai-reviews-denied.png`, `v10-3-pieter-ai-settings-denied.png`. Did NOT start V11/V12 (separate turn) and did NOT run V11.1 bad-key (preserves human's stored key).

- **2026-06-15 (QA, V11.3/11.4)** — Resumed on the cycle branch (Nomsa SSO live). **V11.3 prompt caching = ✅ PASS (observed live):** Anthropic prompt caching IS implemented — `AnthropicApiClient.java:30/:126-128/:142-144` set `cache_control: ephemeral` on the **system prompt**; `:230-236` parse `cache_read_input_tokens`/`cache_creation_input_tokens`; persisted on `ai_executions` (`ai_llm_calls` empty — this tenant meters on `ai_executions`). Ran FICA-on-Sipho today → COMPLETED R1.04, `cache_creation=1671/cache_read=0` (cold-cache WRITE). The DB already holds a live cache-HIT pair: compliance-audit 23:10:27 (`cache_creation=1848`) → 23:13:27 (`cache_read=1848`, `input_tokens=3`, **cost 125c** vs cold 167–200c) — a repeat within the 5-min TTL reads the cached system prompt and is cheaper. **V11.4 input validation = ✅ PASS:** matter-intake <20-char desc → live UI "Get AI Recommendations" button stays `disabled` (DOM-confirmed) + backend pre-flight `MatterIntakeSkill.java:99-103` throws `InvalidStateException("Description too short")` → **400** (`InvalidStateException` extends `ErrorResponseException` w/ `HttpStatus.BAD_REQUEST`+ProblemDetail), before any LLM call; FICA no-docs/no-pending → `FicaVerificationSkill.java:104-133` two pre-flight `InvalidStateException`→400 ("No documents" / "No active checklist"); unreadable-PDF input degrades to a structured NEEDS-REVIEW result (PDFBox WARN, no 500). **V11.1 bad-key DEFERRED** (invalidates human key). **⚠️ ENV note:** a 2nd back-to-back live FICA ("Run Again") hung — no exec row, no IN_PROGRESS, coincident **HikariPool thread-starvation** WARNs (delta 57s→1m21s, 12:35–12:37). Per ENV-HEALTH GUARD I did NOT retry-burn (cache-hit already proven). Backend stayed UP (no "Request cancelled"; thread saturation, not socket death). **Recommend a backend restart before further live-call-heavy work.** V12 (read-only audit) proceeds next. Evidence: `qa_cycle/checkpoint-results/V11-final.md`, `v11-4-matter-intake-short-desc-button-disabled.png`.

- **2026-06-15 (QA, V12 audit trail)** — Read-only verification (no LLM calls, env-safe). **V12 = ✅ PASS.** `tenant_c6107524c9b4.audit_events` AI events: **27 `ai.skill.invoked` + 2 `ai.gate.approved` + 2 `ai.gate.rejected` + 1 `ai.specialist.failed`**. (12.1) Every cycle action represented — 27 invoked = 27 `ai_executions` (1:1, all 5 skills, COMPLETED+FAILED); 2 approved = MARK_KYC_COMPLETE (V8.2) + PUBLISH_COMPLIANCE_REPORT (AIVERIFY-008); 2 rejected = SELECT_MATTER_TEMPLATE (V8.4, notes "Wrong template") + CONFIRM_CONFLICT_SCREEN; 1 failed = contract-review parse failure w/ full errorMessage + costCents=131. (12.2) **Actor attribution correct + distinguished:** Nomsa (`bbfdd8ac…`) 17 invoked + all gate reviews (w/ notes); **Pieter (`fffaa27f…`) 10 invoked incl. the V10.1 matter-intake @10:29 COMPLETED on CUSTOMER:Sipho attributed to Pieter** — the member's invocation recorded as the member's. `actor_type=USER, source=API` throughout. (12.3) **Filters work live** at `/settings/audit-log`: eventType=ai.gate.approved → 76→2; actorId=Pieter+eventType=ai.skill.invoked → 10 (all Pieter); entityType=ai_execution_gate → 4 (2 appr+2 rej) — all reconcile exactly to DB. Append-only table (prevent_delete/update triggers = tamper-evident). **NEW FINDING AIVERIFY-013 (Low, OPEN, product call):** emitted event-type names are `ai.skill.invoked`/`ai.gate.approved`/`ai.gate.rejected` not the scenario's literal `ai.specialist.*` (those `ai.specialist.*` types are registered but emitted by the older automation path). Substantive defensibility property holds — naming discrepancy only, NOT a missing-audit defect; NOT auto-disposed (§6). **Scenario substantively complete except V11.1 (human-key) + deferred AIVERIFY-003/004/009/CORS + AIVERIFY-013 triage.** Evidence: `qa_cycle/checkpoint-results/V12-final.md`, `v12-audit-filter-gate-approved.png`, `v12-audit-filter-actor-pieter.png`. ENV note carried from V11.3: HikariPool thread-starvation on a 2nd back-to-back live call — backend restart recommended before further live-call-heavy stages.

- **2026-06-15 (QA, V9.4/9.5 + AIVERIFY-012 verify)** — Resumed on a healthy env to live-verify PR #1449 (HMR'd into cycle branch) and finish V9.4/9.5. Session opened as Pieter → signed out (gateway form-POST `/logout` with CSRF + admin-API KC logout 204) → fresh KC login as Nomsa (owner). **AIVERIFY-012 → VERIFIED:** (a) budget input now `step="1"` (DOM-confirmed, was `step="100"`); set **R20 → DB `monthly_budget_cents=2000`, `profile_version 10→11`**, reload shows 20 — a sub-R100 value persists (PUT fired, not frozen); (b) invalid input `-3` → visible **`<FormMessage>` "Enter a whole Rand amount of zero or more."** (= `BUDGET_INVALID_MESSAGE`), DB not silently changed — the old silent-coerce-to-no-cap is fixed. **V9.4 → PASS (Observed pre-LLM 403):** spend reconciliation first — cost-summary **R23.99 (2399c)** counts all 26 metered executions (incl. 11 FAILED-with-cost 131c), not just COMPLETED 2268c; `checkBudget` compares the full metered spend, so R20 budget < R23.99 spend fires the gate. Ran FICA-on-Sipho as Nomsa → UI "AI budget exhausted or skill not permitted. Check AI settings."; backend `WARN GlobalExceptionHandler Forbidden: path=/api/ai/skills/fica-verification reason=Monthly AI spend of R23.99 has reached the budget of R20.00` (tenant `tenant_c6107524c9b4`) — clean **403** from `AiCostService.checkBudget`, **no Anthropic `/v1/messages` call** followed. **V9.5 → PASS (no spend leak):** after the rejected attempt `ai_executions` 26→26 (no new row), spend 2399c→2399c UNCHANGED, `ai_llm_calls` 0→0. **Step 3:** budget RESTORED to **R5000** via UI (DB `500000c`, `profile_version 13→14`), reload confirms 5000 / "R4976.01 remaining" — V11/V12 unblocked. **V9 = ✅ PASS (9.1–9.5).** No SQL data changes; all budget edits via the Settings UI under test; SELECT-only for verification. One minor pre-existing UX nit re-noted (setError doesn't hard-block submit if a valid value is still held — logged in #1449 review, out of scope). Evidence: `qa_cycle/checkpoint-results/V9-final.md` §"V9.4/9.5 + AIVERIFY-012 (resume verify)", screenshots `v9-aiverify012-budget-r20-saved.png`, `v9-aiverify012-invalid-input-formmessage.png`, `v9-4-budget-exhausted-403-ui.png`, `v9-budget-restored-5000-final.png`. Did NOT touch V11/V12 (separate turn).
