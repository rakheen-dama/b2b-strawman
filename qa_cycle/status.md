# QA Cycle Status — Legal ZA Full Lifecycle (Keycloak) — 2026-04-26

## Current State

**Purpose**: Run the legal ZA full-lifecycle scenario (`qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`) end-to-end on the Keycloak dev stack. Fix every gap encountered at the root cause — no workarounds, no SQL shortcuts. Prior verify-cycle status archived to `qa_cycle/_archive_2026-04-26_post-verify/status.md`.

- **Branch**: `bugfix_cycle_2026-04-26-day75-retest` (cut from `main` `5e9cb58f` — PR #1201 squash-merge; GAP-L-99 fix on main).
- **Scenario**: `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`
- **ALL_DAYS_COMPLETE**: false
- **QA Position**: **Day 85 — 85.1 (ready to walk on a fresh `bugfix_cycle_2026-04-26-day85` branch cut from `main` HEAD after this retest PR merges to main).** Day 75 is now FULLY COMPLETE: §75.1, §75.2, §75.3 PASS on cycle 54 retest of PR #1201 on main `5e9cb58f` via the new `POST /internal/portal/digest/run-weekly` endpoint (manual digest trigger). Digest email `XuTUtmtbAK3nvAqcGSge2H` delivered with subject "Mathebula & Partners: Your weekly update", body references Sipho's RAF activity (INV-0001 PAID, 3 open info requests, 3 trust transactions), zero Moroka / EST-2026 / R 25 000 leakage in either text or HTML body. §75.4 DEFERRED-ON-ENVIRONMENT (Playwright MCP locked by foreign claude session — same GAP-L-98 precedent; orchestrator-resolvable; routing-level evidence confirmed sufficient). Late-cycle isolation invariant continues to HOLD. GAP-L-99 → VERIFIED.
- **Dev Stack**: Running — backend:8080 (PID 53170, fresh JVM serving main `c419f2c7` / PR #1199), gateway:8443 (PID 71426 ext), frontend:3000 (PID 5771), portal:3002 (PID 5677) all healthy as of cycle-54 walk start. **Resume check**: a fresh session should run `bash compose/scripts/svc.sh status` first; if backend has been restarted, that's OK as long as it serves main HEAD or later — verify with `git log -1 --oneline main`. If services are down, start with `bash compose/scripts/dev-up.sh` (Docker infra) + `bash compose/scripts/svc.sh start all`.
- **NEEDS_REBUILD**: false
- **Cycle Count**: 54

## Tracker

| GAP_ID | Severity | Status | Owner | Summary | Evidence |
|--------|----------|--------|-------|---------|----------|
| BUG-CYCLE26-01 | LOW | VERIFIED | qa | Team-invite form: Radix Select role choice ("Admin") does not propagate to backend POST under Playwright MCP; backend records role=member regardless. Root cause shared with -02 — fragile RHF binding pattern (custom onChange shadows register, role state held in useState outside RHF). Spec: qa_cycle/fix-specs/BUG-CYCLE26-01-02.md. Fixed in PR #1162 (a6b7cbab) and re-merged in PR #1164 (squash merge `68c71cb8`). **VERIFIED 2026-04-26 cycle-5** — fresh invite to `verify-admin-1@mathebula-test.local` with Admin role → backend log `Created invitation … with role=admin`. | qa_cycle/checkpoint-results/day-00.md §Cycle 5 Retest; `retest-2026-04-26-team-invite-admin.png` |
| BUG-CYCLE26-02 | LOW | VERIFIED | qa | Team-invite form: MCP-driven `fill()` on email input does not consistently flow through react-hook-form `register()`; Server Action POSTs return 200 but never reach backend. Same root cause as -01 — combined fix. Spec: qa_cycle/fix-specs/BUG-CYCLE26-01-02.md. Fixed in PR #1162 (a6b7cbab) and re-merged in PR #1164 (squash merge `68c71cb8`). **VERIFIED 2026-04-26 cycle-5** — MCP `fill()` reaches backend without native-setter workaround for both admin + member invites; success copy survives form.reset. | qa_cycle/checkpoint-results/day-00.md §Cycle 5 Retest |
| GAP-L-90 | BUG | VERIFIED | qa | Branding: brand-colour Save Settings persists `brand_color` to org_settings and injects `--brand-color: <hex>` on `<html>`, but no CSS rule consumes `var(--brand-color)`; setting brand colour has zero visible effect on UI. Logo persistence works fine. Scoped fix: sidebar active indicator + sidebar org-name label (high-signal, minimal impact). Spec: qa_cycle/fix-specs/GAP-L-90.md. Fixed in PR #1163 (b3b5a8d4) and re-merged in PR #1164 (squash merge `68c71cb8`). **VERIFIED 2026-04-26 cycle-5** — desktop sidebar active indicator + org-name label both render `rgb(27, 51, 88)` (navy `#1B3358`); mobile sidebar utility-footer active indicator also navy. `:root --brand-color` computed = `#1B3358`. | qa_cycle/checkpoint-results/day-01.md §Cycle 5 Retest; `retest-2026-04-26-sidebar-navy.png`, `retest-2026-04-26-mobile-sidebar-navy.png` |
| GAP-L-91 | LOW | RESOLVED | product | Legal-za rate pack ships LSSA 2024/2025 schedule (the latest real-world LSSA schedule — tariffs are revised every 2–3 years). Scenario expectation of "2026 schedule" was aspirational. Resolved by editing scenario 1.4 to reference the latest published LSSA schedule. No code change. | qa/testplan/demos/legal-za-full-lifecycle-keycloak.md §1.4 (edited 2026-04-26); evidence `day-01-1.3-1.4-tariff-schedule.png` |
| BUG-CYCLE26-03 | BUG | VERIFIED | qa | Client detail page (`/customers/[id]`) has no in-context **Run Conflict Check** button. Conflict Check is only reachable via global `/conflict-check` workspace (Clients sidebar group). Spec: qa_cycle/fix-specs/BUG-CYCLE26-03.md. Fixed in PR #1167 (`d138b48f`) and re-merged in PR #1168 (squash merge `1c0fde1b`). **VERIFIED 2026-04-26 cycle-10** — Sipho's customer-detail action bar renders `Run Conflict Check` link with deep-link `/conflict-check?customerId=c4f70d86-…&checkedName=Sipho+Dlamini&checkedIdNumber=8501015800088`; click navigates with form prefilled (Name, ID, customer-link Sipho selected); submit succeeds, "No Conflict" result, `conflict_checks` row `dee0a260-…` written with FK to Sipho. Edge case: invalid `?customerId=not-a-uuid` does not crash, dropdown falls back to "-- None --". | qa_cycle/checkpoint-results/day-02.md §Cycle 10 Retest §Fix 3; `retest-pr1168-customer-detail.yml`, `retest-pr1168-conflict-check-prefilled.yml` |
| BUG-CYCLE26-04A | BUG | VERIFIED | qa | KYC integration cannot be enabled in UI — `KycIntegrationCard.tsx` lacks the Enable toggle that `PaymentIntegrationCard.tsx` already implements. Spec: qa_cycle/fix-specs/BUG-CYCLE26-04A.md. Fixed in PR #1165 (`221d4069`) and re-merged in PR #1168 (squash merge `1c0fde1b`). **VERIFIED 2026-04-26 cycle-10** — Switch (`#enabled-KYC_VERIFICATION`) renders in card body when provider configured; toggle ON → DB `enabled=t` and card status flips Disabled→Configured; toggle OFF → DB `enabled=f` and card flips Configured→Disabled; toggle ON again → DB `enabled=t` and card returns to Configured. End-to-end UI→server-action→backend→DB→revalidate path works both directions. | qa_cycle/checkpoint-results/day-02.md §Cycle 10 Retest §Fix 1; `retest-pr1168-kyc-toggle-before.yml`, `retest-pr1168-kyc-toggle-after-on.yml`, `retest-pr1168-kyc-snapshot-3.yml`, `retest-pr1168-kyc-snapshot-4.yml` |
| BUG-CYCLE26-04B | BUG | VERIFIED | qa | KYC discoverability on client detail: no status badge in header, no Verify-KYC action shortcut. Spec: qa_cycle/fix-specs/BUG-CYCLE26-04B.md. Fixed in PR #1166 (`1d9a3642`) and re-merged in PR #1168 (squash merge `1c0fde1b`). **VERIFIED 2026-04-26 cycle-10** — `KycStatusBadge` component renders correctly per state (verified/pending/null); for Sipho (no checklist instances yet) state=unverified → badge correctly returns null per spec §2 ("don't clutter the header for unverified state"); Verify-KYC outline button visible in admin action bar with deep-link to `?tab=onboarding`; click navigates to correct URL. Edge case PASS: button still renders alongside Run Conflict Check (reviewer-fix verification — `enabledModules` outside admin try-catch). Verified-state transition path skipped per spec §5 (optional). | qa_cycle/checkpoint-results/day-02.md §Cycle 10 Retest §Fix 2; `retest-pr1168-customer-detail.yml`, `retest-pr1168-after-verify-kyc-click.yml`, `retest-pr1168-sipho-prospect.yml` |
| BUG-CYCLE26-05 | LOW | WONT_FIX | qa | Tooling-only regression: Playwright MCP `browser_take_screenshot` repeatedly times out (`Timeout 5000ms exceeded after fonts loaded`) on Next.js dev frontend port 3000 mid-session. Earlier in same session PNGs captured fine; failure flips on after several hours of session uptime — consistent with long-lived headless Chromium GPU/CPU pressure or Playwright MCP server-side connection pool, not our font config. No real user is on Playwright MCP; Web Vitals not regressed; `next/font/google` config unchanged and verified Day 1. Workaround: YAML DOM snapshots via `browser_snapshot --filename` (used in cycle-6 verification log without blocking the scenario). Closure rationale: qa_cycle/fix-specs/BUG-CYCLE26-05.md. Re-open as tooling/infra issue if it blocks future cycles. | qa_cycle/checkpoint-results/day-02.md §2.7 + Cycle-6 verification |
| BUG-CYCLE26-06 | **HIGH** | VERIFIED | qa | Sending a proposal to an INDIVIDUAL customer fails with "2 required field(s) missing for Proposal Sending" because `StructuralPrerequisiteCheck.PROPOSAL_SEND_FIELDS` hard-requires `Customer.contact_name` + `Customer.contact_email` — but those columns are NULL by design for INDIVIDUAL customers (recipient identity is held in portal_contact). Spec: qa_cycle/fix-specs/BUG-CYCLE26-06.md. Fixed in PR #1172 and re-merged on main as PR #1173 (squash `5365e48a`). **VERIFIED 2026-04-27 cycle-17** — Send Proposal on `0781c5ad-…` for INDIVIDUAL customer Sipho (NULL contact_name/contact_email, ACTIVE portal_contact with email, populated address_line1) no longer surfaces the "N required field(s) missing for Proposal Sending" error. The Send dialog now reaches the next downstream gate (`Proposal content must not be empty`, `ProposalService.java:618-621`) — this is a different `InvalidStateException` after `prerequisiteCheck.passed()` returns true; conclusive evidence the prereq fix landed. ACTIVE-tightening also observable: Recipient combobox lists only the single ACTIVE portal_contact. | qa_cycle/checkpoint-results/day-07.md §Cycle 17 Retest; `cycle17-retest-7.8-send-dialog-recipient-selected.yml`, `cycle17-retest-7.8-after-send.yml` |
| BUG-CYCLE26-07 | **HIGH** | VERIFIED | qa | Side-effect surfaced after BUG-CYCLE26-06 fix landed: `ProposalService.sendProposal()` gate-2 (`InvalidStateException("Proposal content must not be empty")`, line 618-621) blocked every proposal created via the matter-level `New Engagement Letter` dialog because that dialog never seeds `content_json`. Fix: new `ProposalContentSeeder` @Service emits a minimal Tiptap doc (heading + greeting with `client_name` variable + fee summary + optional rate/hours/description/expiry paragraphs + standard terms) from the create-time form fields; `ProposalService.createProposal` invokes the seeder when caller omits or empties `contentJson`. Gate-2 stays as a defensive guard. Frontend dialog unchanged — full template/clause authoring deferred to L-49 Sprint 3. Spec: qa_cycle/fix-specs/BUG-CYCLE26-07.md. Fixed in PR #1174 (squash `7d602dee`) and re-merged on main as PR #1175 (squash `0a58a242`). **VERIFIED 2026-04-27 cycle-19** — fresh proposal `69e3d65f-…` (PROP-0002) created via matter-level dialog with Title=`Cycle19 Verify`, Fee Model=HOURLY, Hourly Rate Note=`R850/hr per LSSA 2024/2025 schedule`. DB shows `content_json` length 774 (vs old `0781c5ad-…` row's `'{}'` length 2), root `type=doc`, 6 content children including `variable{key:client_name}` node — exact seeder spec. Send Proposal flow with Sipho's ACTIVE portal_contact succeeds: status DRAFT→SENT, `sent_at=2026-04-27 01:54:31.181629+00`, `portal_contact_id=f3f74a9d-…`. `portal.portal_proposals` row written with `content_html` length 1223; rendered HTML body contains `<p>Dear Sipho Dlamini,</p>` confirming `TiptapRenderer` + `ProposalVariableResolver` substituted `{{client_name}}` correctly. Portal-side: Sipho's `:3002/proposals/69e3d65f-…` renders heading "Cycle19 Verify", status badge SENT, body text, Accept Proposal + Decline buttons. | qa_cycle/checkpoint-results/day-07.md §Cycle 19 Retest; `cycle19-7.1-engagement-dialog.yml`, `cycle19-7.9-after-send.yml`, `cycle19-7.11-portal-proposal-detail.yml` |
| BUG-CYCLE26-08 | LOW | VERIFIED (PR #1181, retest cycle 25) | qa | Trust transactions filter list rendered `"Interest Lpff"` instead of `"Interest LPFF"` for the `INTEREST_LPFF` enum. Root cause: three `transactionTypeLabel()` helpers in trust-accounting pages all used a generic title-case algorithm that destroyed the LPFF acronym. Fix consolidated the three helpers into a single `Record<TrustTransactionType, string>` label-map exported from `frontend/lib/types/trust.ts` with explicit `INTEREST_LPFF: "Interest LPFF"` entry; duplicate helpers in `trust-accounting/page.tsx`, `transactions/page.tsx`, and `client-ledgers/[customerId]/page.tsx` were removed and replaced with the shared map. Fixed in PR #1178 (squash `53242c0d`) on cycle branch; rolled into main via PR #1181 (squash `73c133b6`). **VERIFIED 2026-04-27 cycle-25** — `/trust-accounting/transactions` renders `Interest LPFF` (line 140 of evidence yaml); `/trust-accounting/client-ledgers/{sipho-id}` filter list also renders `Interest LPFF` (line 171 of breadcrumb yaml). | `qa_cycle/checkpoint-results/cycle25-retest-PR1181-BUG-08-transactions-page.yml`; `qa_cycle/checkpoint-results/cycle25-retest-PR1181-BUG-09-sipho-ledger-breadcrumb.yml`; `qa_cycle/checkpoint-results/day-10.md §Cycle 25 Retest` |
| BUG-CYCLE26-09 | LOW | VERIFIED (PR #1181, retest cycle 25) | qa | Breadcrumb on `/trust-accounting/client-ledgers/{id}` shows raw URL slug `client-ledgers` and raw customer UUID instead of human-readable labels. Root cause: `frontend/components/breadcrumbs.tsx` `SEGMENT_LABELS` map missing `client-ledgers` entry, and `PARENT_SEGMENT_FALLBACKS` map missing the singular-noun fallback for the UUID segment. Spec: qa_cycle/fix-specs/BUG-CYCLE26-09.md (two new map entries — render as `Client Ledgers › Client Ledger`, matching existing `Customer/Project/Invoice/Proposal` UUID-fallback pattern; customer-name lookup avoided to keep layout-level breadcrumb stateless). Fixed in PR #1179 (squash `2c167711`) — both map entries added; lint + build pass. Rolled into main via PR #1181 (squash `73c133b6`). **VERIFIED 2026-04-27 cycle-25** — breadcrumb DOM at lines 83-96 renders `Mathebula & Partners › Trust Accounting › Client Ledgers › Client Ledger` (was `client-ledgers › c4f70d86-…` in cycle 21). UUID still in href but not displayed text. | `qa_cycle/checkpoint-results/cycle25-retest-PR1181-BUG-09-sipho-ledger-breadcrumb.yml`; `qa_cycle/checkpoint-results/day-10.md §Cycle 25 Retest` |
| BUG-CYCLE26-10 | LOW | VERIFIED-CODE-ONLY (PR #1181, retest cycle 25, Option B) | qa | Activity feed `information_request.item_accepted` template emits literal `"for unknown"` when `request_number` is missing from audit-event details. Root cause: `InformationRequestService.acceptItem` (line 453-465) writes `request_id` and `item_name` to `acceptAuditDetails` but forgets `request_number` — outlier vs every other `information_request.*` audit emission in the same service which all include it. Spec: qa_cycle/fix-specs/BUG-CYCLE26-10.md (1-line backend add: `acceptAuditDetails.put("request_number", request.getRequestNumber())`; same defensive add to `rejectItem` for consistency; regression test in `ActivityMessageFormatterTest`). Fixed in PR #1180 (squash `b25b0146`) — `request_number` now included in both `acceptItem` + `rejectItem` audit details; new `informationRequestItemAcceptedInterpolatesRequestNumber` regression test asserts message renders `for REQ-0042` and never contains `unknown`. 68 targeted tests pass. Rolled into main via PR #1181 (squash `73c133b6`). **VERIFIED 2026-04-27 cycle-25 (Option B per spec)** — backend restarted on cycle-25 dispatch (stale JVM PID 724 etime 03:03:02 predated PR #1180 by ~30min; new PID 21748 has the fix). DB confirms pre-existing audit rows lack `request_number` (correctly still render "for unknown"); no fresh accept event was triggerable since REQ-0002 is 3/3 ACCEPTED and REQ-0001 items are PENDING (no SUBMITTED items to accept) — Option B per spec. Authoritative confirmation = `ActivityMessageFormatterTest#informationRequestItemAcceptedInterpolatesRequestNumber` (asserts `"Alice accepted \"ID copy\" for REQ-0042"` + `doesNotContain("unknown")`); + verified `git show 73c133b6 -- InformationRequestService.java` shows `acceptAuditDetails.put("request_number", …)` line is on main + JVM is fresh. Forward-only correctness — pre-existing immutable audit rows expectedly remain "for unknown". | `qa_cycle/checkpoint-results/cycle25-retest-PR1181-BUG-10-raf-matter-activity.yml`; `qa_cycle/checkpoint-results/cycle25-retest-PR1181-BUG-10-raf-matter-detail.yml`; `qa_cycle/checkpoint-results/day-10.md §Cycle 25 Retest` |
| GAP-L-66 | LOW | VERIFIED (PR #1188, retest cycle 37) | frontend | Portal `/login` form omits `orgId`; `/portal/auth/request-link` API rejects requests without `orgId` (HTTP 400 `orgId is required`). When a logged-out portal user follows a deep-link (e.g. fee-note email "View Fee Note" CTA at `:3002/invoices/[id]`) → portal redirects to `/login` → entering email + clicking Send Magic Link surfaces "Something went wrong. Please try again." Workaround: open the magic-link from email (which carries `?orgId=mathebula-partners`) instead of using the form. Spec: `qa_cycle/fix-specs/GAP-L-66.md` (frontend-only, S effort). Three-step fix: (1) `portal/lib/auth.ts` adds `portal_last_org_id` localStorage key written on `storeAuth()` and preserved across `clearAuth()` (orgId is a public slug — not a credential); (2) `portal/app/(authenticated)/layout.tsx` threads `pathname` (`?redirectTo=…`) + last-known orgId (`?orgId=…`) into the `router.replace("/login")` call; (3) `portal/app/login/page.tsx` sources `orgId` as `query ?? getLastOrgId()`, shows inline guard when both null, persists `redirectTo` to sessionStorage. `portal/app/auth/exchange/page.tsx` consumes sessionStorage post-exchange (with `/`-prefix open-redirect guard) so the user lands back on the original deep-link target. No backend change. Backend default-org-resolution rejected (multi-tenancy leak risk). **Fixed in PR #1188 (squash `20bcc976`)** on cycle branch — all four files modified per spec; 8 new test cases added across `lib/__tests__/auth.test.ts` (3) + `app/login/__tests__/page.test.tsx` (3) + `app/auth/exchange/__tests__/page.test.tsx` (2); portal lint clean, build OK, **177/177 tests pass**. Cycle-37 retest on main `06d5a633` (PR #1189) **VERIFIED end-to-end**: (step 1) deep-link `/invoices/{id}` with cleared session → `(authenticated)/layout.tsx` redirects to `/login?redirectTo=…&orgId=mathebula-partners` ✓; (step 2) `/login` form submits cleanly, no 400, Mailpit msg `Bav8Mu5FjJkggXnrdS4xP3` delivered ✓; (step 3) magic-link click → `/auth/exchange` → restored deep-link `/invoices/432ae5a9-…` rendered (INV-0001 PAID, R 5 160,00) ✓; (step 4) edge-case bare `/login` with cleared localStorage → inline guard alert "We couldn't determine which organization to sign you into" rendered, no POST fired ✓. | `qa_cycle/checkpoint-results/cycle34-day30-30.1c-portal-login-page.yml`, `cycle34-day30-30.1d-after-magic-link-click.yml`; `qa_cycle/checkpoint-results/day-30.md §Cycle 34 Replay` + §Cycle 37 Retest; `qa_cycle/checkpoint-results/cycle37-retest-PR1189-GAP-L-66-step{1,2,3,4}-*.yml`; `qa_cycle/fix-specs/GAP-L-66.md`; PR #1188, PR #1189 |
| GAP-L-67 | HIGH | VERIFIED (PR #1190, retest cycle 41) | qa | Carry-forward from cycle 1 day-45 walk (formally tracked cycle 38, specced cycle 39). `Create Information Request` dialog (`frontend/components/information-requests/create-request-dialog.tsx`) had no Items input fields; picking "Ad-hoc (no template)" created a 0-item DRAFT then closed silently. **Fixed in PR #1190 (squash `2846fd1a`)** on cycle branch `bugfix_cycle_2026-04-26-day45` per spec Option A; rolled to main via PR #1191 (squash `79ad2602`). Inline Items section now appears on the ad-hoc path only (hidden when a real template is selected); Add Item / Trash buttons; per-row Name + Description + ResponseType (FILE_UPLOAD/TEXT_RESPONSE) + Required; Send Now disabled while ad-hoc has 0 items or any item name is blank. Save-as-Draft still allowed with 0 items (existing AddItemDialog on the detail page handles post-create edits). Payload wires `items[]` (trimmed names, sortOrder 0..n) only on ad-hoc path; template path payload byte-identical to before. Backend (`InformationRequestService.createAdHoc`) and frontend API client (`CreateInformationRequestRequest.items`) untouched — both already supported the contract. New unit test `frontend/__tests__/create-request-dialog.test.tsx` (7 cases: section visibility, add/remove, validation, payload shape, template-path isolation, save-as-draft 0-items). Frontend lint clean (0 errors), build green, full Vitest suite 339 files / 2115 passing / 2 skipped. **VERIFIED 2026-04-27 cycle-41** browser-driven on `main 79ad2602` (frontend HMR — no restart): Bob (admin) opened Create Information Request dialog on RAF matter `cc390c4f-…` → Template selector defaulted to "Ad-hoc (no template)"; Items section visible with Add Item button + "Add at least one item before sending" hint; Send Now disabled at 0 items. Filled 2 items per scenario (row 0: "Latest specialist medical reports", row 1: "Independent expert assessment"; both FILE_UPLOAD + required); set due_date 2026-05-11; recipient auto-selected Sipho's portal_contact `f3f74a9d-…`. Send Now enabled → click → dialog closed without error. New `information_requests` row `73babd4e-077e-4955-9b60-45abd03f0911` written to `tenant_5039f2d497cf` (REQ-0005, status=SENT, due 2026-05-11, project_id=cc390c4f, portal_contact_id=f3f74a9d); 2 `request_items` rows written ("Latest specialist medical reports" + "Independent expert assessment", both FILE_UPLOAD + required, sort_order 0/1). Mailpit message `QiFckxzGK9bLNEhPeEouZb` "Information request REQ-0005 from Mathebula & Partners" delivered to `sipho.portal@example.com` at 2026-04-27T15:21:17Z. The originally-blocked Day 45 §45.1 scenario flow ("Supporting medical evidence" 2-item ad-hoc) now works end-to-end without the FICA Onboarding Pack template substitute. 0 console errors during the flow. | `qa_cycle/checkpoint-results/day-45.md §Cycle 41 Retest`; `cycle41-retest-PR1191-GAP-L-67-step{1,2}-*.yml`; `cycle41-retest-PR1191-GAP-L-67-step{3,4,5}-*.png`; `qa_cycle/fix-specs/GAP-L-67.md`; PR #1190 (squash `2846fd1a`); PR #1191 (squash `79ad2602`) |
| GAP-L-92 | MEDIUM | VERIFIED (PR #1192, retest cycle 45) | qa | Portal home "Pending info requests" tile over-counted: included IN_PROGRESS requests where ALL items were SUBMITTED. Source: `portal/app/(authenticated)/home/page.tsx:67` filter `data.filter((r) => r.status !== "COMPLETED").length` — IN_PROGRESS counted as pending. Fixed in PR #1192 (squash `94f77583`) on cycle branch; CR follow-up commit `65e5fd76` tightened filter to explicit positive allow-list `(r.status === "SENT" \|\| r.status === "IN_PROGRESS") && r.submittedItems < r.totalItems` (also excludes CANCELLED). Rolled to main as **PR #1193 (squash `6c30e247`)**. 8 Vitest unit-test cases incl. CANCELLED regression guard; **185/185 portal tests pass**. **VERIFIED 2026-04-27 cycle-45** browser-driven on `main 6c30e247` (frontend HMR — no restart): Sipho authenticated via fresh magic-link (Mailpit `88oo8hqK9MMRSxZh4StdNA`, exchange to `:3002`); `/home` "Pending info requests" tile reads "2"; `/requests` enumeration confirms REQ-0005 IN_PROGRESS 2/2 (excluded — regression case), REQ-0004 SENT 0/3 (counts), REQ-0002 COMPLETED 0/3 (excluded), REQ-0001 SENT 0/3 (counts) — expected count = 2 matches tile. 0 console errors. Day 60 walk now unblocked. | `qa_cycle/checkpoint-results/cycle45-retest-PR1193-GAP-L-92-portal-home.yml`, `cycle45-retest-PR1193-GAP-L-92-portal-requests.yml`, `cycle45-retest-PR1193-GAP-L-92-home-tile.png`, `cycle45-retest-PR1193-GAP-L-92-requests-list.png`; `qa_cycle/checkpoint-results/day-46.md §Cycle 45 Retest`; `qa_cycle/fix-specs/GAP-L-92.md`; PR #1192 (squash `94f77583`); PR #1193 (squash `6c30e247`) |
| GAP-L-93 | LOW | SPEC_READY-DEFERRED | product | Closure dialog Step-2 only offers `Generate closure letter` checkbox; scenario §60.8 expects an inline `Generate Statement of Account` flag so SoA is auto-attached on close. Operator must remember to invoke "Generate Statement of Account" separately from the matter header after closure. Spec: `qa_cycle/fix-specs/GAP-L-93.md` (S effort, frontend+backend; defer to cycle 48 — Day 61 unblocks via existing standalone SoA button). | `qa_cycle/checkpoint-results/cycle46-day60-19-closure-step2.yml` lines 438-477; `day-60.md §Day 60 Cycle 46 Walk` |
| GAP-L-94 | **HIGH** | VERIFIED (PR #1196, retest cycle 51) | dev | `StatementOfAccountContextBuilder.buildTrustBlock` previously resolved trust account via `findByAccountTypeAndPrimaryTrue(GENERAL)` — could return a different id than where the customer's deposits live, silently emptying the SoA Trust Activity section. Fixed by adding `TrustTransactionRepository.findDistinctTrustAccountIdsByCustomerId` (read-only, REVERSAL excluded) and rewriting the resolver: 0 ids → empty; 1 id → use it; N ids → prefer primary GENERAL when active, else first id (with WARN log). Summary `trust_balance_held` picks up the fix automatically. **Spec deviation**: diagnostic SQLs were not run (no live DB access from worktree); fix is independently safe across all 3 hypotheses A/B/C documented in spec. PR #1196 (squash `08e8e68d`). 4 new tests + existing tests rewired; 21+4+2+7+54 = 88 targeted tests pass. | `qa_cycle/fix-specs/GAP-L-94.md`; PR #1196 |
| GAP-L-95 | BUG | VERIFIED (PR #1194, retest cycle 51) | dev | `StatementOfAccountContextBuilder.aggregate()` hard-coded `vatAmount=BigDecimal.ZERO`. Fixed by injecting `TaxRateRepository` and deriving VAT from the org's default tax rate via `findByIsDefaultTrue()` — mirrors the invoice path's pattern (`InvoiceTaxService`). Falls back to ZERO when no default rate, exempt, inactive, or rate is null/zero — preserves non-VAT-registered tenant behaviour. **Spec deviation noted**: GAP-L-95.md proposed `OrgSettings.isVatRegistered()` / `OrgSettings.getVatRate()` but those methods don't exist; VAT is modelled via the standalone `TaxRate` entity with `isDefault` flag. PR #1194 (squash `746d95e1`). 23 tests pass. | `qa_cycle/fix-specs/GAP-L-95.md`; PR #1194 |
| GAP-L-96 | LOW | SPEC_READY-DEFERRED | product | Closure does not insert a `retention_policies` MATTER row per ADR-249. On close, `projects.retention_clock_started_at` is stamped but no row is inserted into `retention_policies` for `record_type='MATTER'`. **Verified at code level**: `MatterClosureService.close()` lines 174-176 explicitly carry `TODO(489C): persist a retention_policies row inside performClose()`. The realistic fix is a tenant-wide MATTER policy seed (table is per-tenant policy keyed on `(record_type, trigger_event)`, not per-instance). Spec: `qa_cycle/fix-specs/GAP-L-96.md` (S effort, single backend file, idempotent; defer to cycle 48 — retention sweeper doesn't act on MATTER yet, so the row is inert until then). | `SELECT * FROM retention_policies` (only 2 generic rows); `SELECT retention_clock_started_at FROM projects` populated; `org_settings.legal_matter_retention_years=NULL`; `day-60.md §Day 60 Cycle 46 Walk §60.11` |
| GAP-L-97 | LOW | VERIFIED (PR #1195, retest cycle 51) | dev | Slug-vs-name mismatch: `PortalDocumentNotificationHandler` keyed off `DocumentGeneratedEvent.templateName` against an allowlist of slugs but `GeneratedDocumentService:220` + `StatementService:237` published the display name. Fixed by swapping `template.getName()` → `template.getSlug()` at both publish sites; added Javadoc on `DocumentGeneratedEvent.templateName` clarifying the contract. Activity-feed UX preserved (`details.template_name` still carries display name). New regression test `displayNamePublishedAsTemplateName_skipsEmail` in `PortalDocumentNotificationHandlerIntegrationTest`. PR #1195 (squash `f4366b44`). 9 targeted tests pass. | `qa_cycle/fix-specs/GAP-L-97.md`; PR #1195 |
| GAP-L-98 | INFRA-BLOCKER | RESOLVED | infra | Orchestrator closed foreign claude sessions and cleared stale `SingletonLock` symlink; cycle 52 retry succeeded on first `browser_navigate`. Originally: Cycle 52 Day 61 walk could not start because Playwright MCP user-data-dir `/Users/rakheendama/Library/Caches/ms-playwright/mcp-chrome-5d273ba` was held by another long-running `claude --chrome` session (Chromium PID 54166, etime 08:12+, SingletonLock pointed to `Rakheens-MacBook-Pro.local-54166`); every `browser_navigate` returned `Browser is already in use … use --isolated`. Resolution applied 2026-04-28 SAST by orchestrator before cycle-52 continuation; first `browser_navigate http://localhost:3000/dashboard` returned 200 immediately, no `--isolated` flag needed. Day 61 §61.1–§61.9 then walked clean (9 PASS / 0 FAIL / 0 BLOCKED). NOT a product defect. | `qa_cycle/checkpoint-results/day-61.md §Cycle 52 Walk` (original block) + §Cycle 52 Walk (continuation after GAP-L-98 unblock) (resolution + completion); `cycle52-day61-prereq-*` (pre-flight); `cycle52-day61-61.*` (walk evidence) |
| GAP-L-99 | MEDIUM | VERIFIED (PR #1201, retest cycle 54) | qa | Manual digest trigger landed in PR #1200 (squash `c1c7afd7`) on cycle branch `bugfix_cycle_2026-04-26-day75`; rolled to main as **PR #1201 (squash `5e9cb58f`)**. Endpoint: `POST /internal/portal/digest/run-weekly` (X-API-KEY gated, optional query params orgId/targetEmail/dryRun). Original gap: `PortalDigestScheduler` was cron-only (`@Scheduled(cron = "0 0 8 ? * MON")` — Monday 08:00 server time); backend uptime in QA cycles never spanned a Monday 08:00 tick, so Mailpit showed zero digest emails ever delivered to `sipho.portal@example.com`. **VERIFIED 2026-04-28 cycle-54** on main `5e9cb58f` (fresh JVM, ~27s warmup): (1) missing `X-API-KEY` → HTTP 401; (2) invalid key → HTTP 401; (3) `dryRun=true` → response `{"tenantsProcessed":4,"digestsSent":1,"skipped":0,"dryRun":true,"errors":[]}` + Mailpit total **unchanged** at 4; (4) real send → response `{"tenantsProcessed":4,"digestsSent":1,"skipped":0,"dryRun":false,"errors":[]}` + Mailpit total **4 → 5** with new message `XuTUtmtbAK3nvAqcGSge2H` to `sipho.portal@example.com`, subject **"Mathebula & Partners: Your weekly update"** (literal scenario phrase match). Body greets "Hi Sipho Dlamini," and references Sipho's RAF activity (INV-0001 PAID ZAR 5160.00, 3 open info requests, 3 trust transactions); CTA `Open portal →` href = `http://localhost:3002/home`. **Isolation invariant HOLDS** — `grep -ic` on both text + HTML bodies: `moroka`=0, `EST-2026`=0, `estate`=0, `25 000`/`25,000`/`25000`=0. Day 75 §75.1, §75.2, §75.3 PASS on this retest. §75.4 (browser walk to /home) DEFERRED-ON-ENVIRONMENT — Playwright MCP user-data-dir locked by foreign claude session (PID 4237 etime ~47h+, identical to GAP-L-98 precedent — orchestrator-resolvable infra, NOT a product gap); routing-level evidence confirmed (CTA href correct, /auth/exchange + /home both 200, magic-link issued + delivered). | `qa_cycle/checkpoint-results/day-75.md §Cycle 54 Retest`; `cycle54-retest-PR1201-GAP-L-99-{step1-missing-key,step1-bad-key,step3-dryrun,step4-real-send,digest-message,digest-body,summary,mailpit-baseline}.{txt,json,html}`; `qa_cycle/fix-specs/GAP-L-99.md`; PR #1200 (squash `c1c7afd7`); PR #1201 (squash `5e9cb58f`) |
| BUG-CYCLE26-11 | LOW | VERIFIED (PR #1182, retest cycle 29) | qa | Fix landed via PR #1182 (squash `08abca83`) on cycle branch + rolled to main as PR #1183 (squash `8dfb4920`): added `UUID projectId` to `TrustTransactionRecordedEvent` + `TrustTransactionApprovalEvent` records and every factory; threaded `saved.getProjectId()` / `transaction.getProjectId()` through all 8 publish sites in `TrustTransactionService`; replaced `event.trustAccountId()` → `event.projectId()` in both `PortalEmailNotificationChannel.buildTrustActivityContext` overloads (renamed local `matterKey` → `matterId`). Null-safe fallback to `/trust` index preserved. **VERIFIED 2026-04-27 cycle-29** browser-driven on `main 8dfb4920` (backend PID 41372, fresh JVM): Bob recorded fresh R 100,00 trust deposit on RAF matter `cc390c4f-…` for Sipho via firm UI; new trust-activity email `gEDSuhVsW2wp6im6oDfXbz` arrived in Mailpit with CTA href = `http://localhost:3002/trust/cc390c4f-35e2-42b5-8b54-bac766673ae7` (matter UUID; NOT trust-account UUID `46d1177a-…`; NOT `/trust/null`); Sipho authenticated via fresh magic-link, navigated to the email-CTA URL → portal page rendered correctly: Trust balance card R 50 100,00 + "Matter cc390c4f" label, Transactions table with both deposits (R 100,00 retest + R 50 000,00 initial), Statements empty-state. NO 404, NO "No trust balance is recorded for this matter" error, NO "The requested resource was not found" panel. Original gap: trust-activity nudge email's "View trust ledger" CTA linked to `/trust/{trust_account_id}` instead of `/trust/{matter_id}`. | `qa_cycle/checkpoint-results/cycle29-retest-PR1183-BUG-11-email-body.html` (new email body with correct CTA); `cycle29-retest-PR1183-BUG-11-portal-trust-matter.yml` (portal page rendering correctly); `cycle29-retest-PR1183-BUG-11-firm-trust-list.yml` (firm-side trust list post-deposit); `qa_cycle/checkpoint-results/day-11.md §Cycle 29 Retest` |

## Standing Rules (apply to every agent)

- **Root-cause every bug**. No workarounds. No SQL INSERT/UPDATE/DELETE to skip steps. All operations through REST API or browser UI.
- **No mocks for QA**. Drive the browser via Playwright MCP. Mailpit API is the only legitimate REST surface for QA.
- **One fix per PR**. Dev agents commit to `fix/{GAP_ID}` branches off this cycle branch, PR-and-merge with squash.
- **Restart after Java changes**. Backend/gateway: `bash compose/scripts/svc.sh restart {service}`. Frontend/portal: HMR auto-reloads.
- **Read CLAUDE.md** in the relevant subdirectory before changing service code.
- **Commit between turns**. Each agent pushes its state changes (status.md, fix-spec, checkpoint result) before returning.

## Per-Day Workflow (orchestrator behavior — overrides default skill flow)

The skill's default Step 1 flow says "ELSE → QA walks next checkpoint". This cycle uses a stricter per-day loop instead:

1. **Walk one day** on a fresh `bugfix_cycle_2026-04-26-dayN` branch (cut from `main`). Stop at end-of-day or first blocker.
2. **Triage every gap** found that day via Product Agent. WONT_FIX is allowed only for tooling/out-of-scope; real product bugs (even non-blocking UX issues) become SPEC_READY.
3. **Fix every spec** via Dev Agent (worktree-isolated). Each fix is a PR into `bugfix_cycle_*-dayN` and squash-merges there.
4. **When the day's tracker has 0 OPEN/SPEC_READY items**, PR `bugfix_cycle_*-dayN` → `main`. Run review cycle (CodeRabbit + independent reviewer). Address all actionable findings before merging.
5. **After merge to main**, dispatch a QA retest agent that verifies each fix on `main` (no SQL shortcuts, real browser flow). Mark gaps `FIXED → VERIFIED` only after the retest passes on `main`.
6. **Then** advance QA Position to next day. Cut a fresh `bugfix_cycle_*-dayN+1` branch from `main` and repeat.

The orchestrator's job between agent turns is to read this status file, decide which step in the loop above is next, and dispatch the matching agent. Do NOT skip the PR-to-main-and-retest step at the end of each day.

## Known Out-of-Scope Constraints (do NOT open gaps for these)

- **Payment integration is a stub**. There is no real PSP integration (Stripe, Yoco, Peach, etc.). Invoice "Mark Paid" / portal payment flows are stub endpoints that just flip status. Do NOT open gaps for missing payment-gateway redirects, webhook signing, real card processing, or PSP-side reconciliation. If the scenario asks the user to "pay an invoice", treat the stub Mark-Paid action as the equivalent and continue.
- **KYC provider integration is a stub**. There is no real KYC vendor integration (Onfido, Veriff, Smile ID, etc.). The KycVerificationDialog and `Run KYC` flow call a stub backend that just returns a status. The UI surfaces (toggle, badge, verify button, status display) are real and must work; the underlying provider call is mocked. Do NOT open gaps for missing real-provider OAuth, webhook signing, real document analysis, or provider-side reconciliation.

## Log

- 2026-04-26 SAST — Orchestrator: cycle initialized on branch `bugfix_cycle_2026-04-26`. Prior verify-cycle status (ALL_DAYS_COMPLETE 2026-04-25) archived. Dev stack confirmed healthy. About to dispatch QA agent for Day 0.
- 2026-04-26 SAST — QA: Day 0 cycle-1 — 30 PASS / 2 PARTIAL (tooling) / 0 FAIL / 0 BLOCKER. All Day-0 wrap-up checks satisfied: 3 KC users registered, tenant_5039f2d497cf provisioned with vertical=legal-za, full legal sidebar nav present, zero tier/upgrade UI. Two LOW-severity tooling-only bugs logged (BUG-CYCLE26-01, BUG-CYCLE26-02) — both Playwright-MCP-Radix interaction quirks, not real-user product defects. L-22 (KC owner registration → /org/{slug}/dashboard) reconfirmed working. Bumping QA Position to Day 1, Checkpoint 1.1.
- 2026-04-26 SAST — QA: Day 1 cycle-2 — 5 PASS / 2 PARTIAL / 0 FAIL / 0 BLOCKER. Logo upload + persisted to S3, brand_color persisted to DB, LSSA tariff schedule confirmed pre-seeded (19 items in ZAR, schedule year 2024/2025), Section 86 trust account "Mathebula Trust — Main" created with R 0,00 balance. GAP-L-90 (BUG): brand-colour CSS variable injected but no stylesheet consumes it — visual no-op. GAP-L-91 (LOW): tariff schedule is 2024/2025 not 2026 — scenario or seed pack mismatch. Both non-cascading; advancing to Day 2.
- 2026-04-26 SAST — Product: triaged 4 OPEN items → 3 SPEC_READY + 1 RESOLVED. Combined BUG-CYCLE26-01 + -02 into single spec `BUG-CYCLE26-01-02.md` (shared root cause: RHF binding pattern in invite-member-form mixes register-shorthand with override-onChange and useState-outside-form). GAP-L-90 specced as scoped sidebar fix (active indicator + org-name label only — not a full theme overhaul). GAP-L-91 resolved-via-scenario-edit: scenario §1.4 now references "latest published LSSA schedule (2024/2025)" since LSSA tariffs are revised every 2–3 years. Cycle Count → 4.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-01-02 merged (PR #1162). Refactored invite form to FormField+Controller pattern. Frontend HMR picks up changes — no service restart needed.
- 2026-04-26 SAST — Dev: GAP-L-90 merged (PR #1163). Sidebar consumers now read --brand-color; fallback teal in :root. HMR — no restart.
- 2026-04-26 SAST — QA: Cycle 5 retest of PR #1164 (commit 68c71cb8). 3/3 fixes VERIFIED, 0 REOPENED. BUG-CYCLE26-01 confirmed via fresh `verify-admin-1@…` invite → backend log `role=admin`. BUG-CYCLE26-02 confirmed — MCP fill() reaches backend without native-setter workaround for both admin + member invites; success copy survives form.reset. GAP-L-90 confirmed — desktop sidebar active indicator (`.absolute.w-0.5`) + org-name label both `rgb(27,51,88)`, mobile sidebar utility-footer active indicator also navy, `:root --brand-color` computed = `#1B3358`. Screenshots saved. QA Position **HELD at Day 2 / 2.1** per user directive — did NOT walk forward into Day 2.
- 2026-04-26 SAST — QA: Day 2 cycle-6 — 5 PASS / 2 PARTIAL / 0 FAIL / 0 BLOCKER / 2 SKIPPED. Bob (admin) created Sipho Dlamini (INDIVIDUAL, sipho.portal@example.com, ID 8501015800088, ZA) with SA Legal — Client Details field group populated; conflict check returned NO_CONFLICT (2 history rows confirm no false positives). KYC verification gapped: BUG-CYCLE26-04 (no UI Enable toggle, no client-detail Run-KYC button — adapter unreachable from firm UI). Discoverability gap BUG-CYCLE26-03 (Run Conflict Check is only in global workspace, not on client detail). Tooling regression BUG-CYCLE26-05 (PNG screenshots time out mid-session — DOM YAML used as substitute). All 3 non-cascading; advancing to Day 3.
- 2026-04-26 SAST — Product: cycle-7 triage — 3 OPEN → 2 SPEC_READY (BUG-CYCLE26-03, BUG-CYCLE26-04A, BUG-CYCLE26-04B counted as one bucket: KYC blocker), 1 WONT_FIX (BUG-CYCLE26-05 tooling-only). Split BUG-CYCLE26-04 into 04A (Enable toggle on `KycIntegrationCard.tsx`, mirrors `PaymentIntegrationCard.tsx:476–492` pattern — demo blocker, ship first) and 04B (KYC status badge + Verify-KYC action shortcut on customer detail header — depends on 04A landing). Backend `PATCH /api/integrations/{domain}/toggle` already exists, frontend just wasn't calling it. KYC verify dialog itself is already wired (`KycVerificationDialog` per checklist row); the gap is purely the integration-enable toggle and client-header discoverability. BUG-CYCLE26-03 specced as customer-detail action button + searchParams-driven prefill on conflict-check route (no backend changes). Cycle Count → 7.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-04A merged (PR #1165). KYC card now has Enable Switch — pattern-matched from PaymentIntegrationCard. HMR — no restart.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-04B merged (PR #1166). Customer detail now surfaces KYC status badge + verify shortcut. Stub provider; UI surface only. HMR — no restart.
- 2026-04-26 SAST — Dev: BUG-CYCLE26-03 merged (PR #1167). Run Conflict Check shortcut now on client detail. HMR — no restart.
- 2026-04-27 SAST — QA: Day 3 cycle-11 — 14 PASS / 0 FAIL / 0 BLOCKER. Fresh walk on main `34ec2f77` (branch `bugfix_cycle_2026-04-26-day3`) — Bob creates RAF matter for Sipho (`Dlamini v Road Accident Fund` ref `RAF-2026-001`, ACTIVE, `applied_field_groups=[common-project, legal-za-project]` only — Conveyancing pack correctly excluded). Save Custom Fields on PROSPECT matter persists `case_number=RAF-2026-001-CASE` + `opposing_party=Road Accident Fund` + `court_name=Gauteng Division, Pretoria` (L-35 re-verified browser-driven). Create Information Request dialog: FICA Onboarding Pack (3 items) selectable (L-33), Sipho portal_contact auto-populated (L-34), Due Date (optional) field present (L-41 — column + picker functional, REQ-0001 due_date NULL because prior turn didn't fill the optional field; not a regression). REQ-0001 SENT (3 items: ID copy / Proof of residence / Bank statement, all PENDING), magic-link email in Mailpit with portal :3002 URL + token + orgId (L-42). Day 4 = Sipho first portal login.
- 2026-04-26 SAST — QA: Cycle 10 retest of PR #1168 (commit `1c0fde1b`) on `main`. 3/3 fixes VERIFIED, 0 REOPENED. **BUG-CYCLE26-04A**: KYC Switch round-trips ON/OFF/ON; DB `enabled` toggles `f`→`t`→`f`→`t`; card status follows Disabled↔Configured. **BUG-CYCLE26-04B**: header badge correctly returns null for unverified state per spec; Verify-KYC button visible with `?tab=onboarding` deep-link; navigates correctly (Onboarding tab gracefully falls back to Matters since Sipho is PROSPECT with 0 checklist instances — pre-existing upstream behavior, not a regression). **BUG-CYCLE26-03**: Run Conflict Check button on Sipho detail with full deep-link; form prefills correctly; submit creates `conflict_checks` row `dee0a260-…` with FK; invalid-UUID edge case doesn't crash. Stack auth: Bob's session expired mid-test, re-auth as Thandi (Owner) — no impact on verification claim since admin gate covers both. Screenshot tooling still degraded (BUG-CYCLE26-05 WONT_FIX); YAML evidence used. QA Position advanced to Day 3 / 3.1 — orchestrator decides when to walk.
- 2026-04-27 SAST — QA: Day 4 cycle-12 — 14 PASS / 0 FAIL / 0 BLOCKER. Fresh walk on main `038d7a7a` (branch `bugfix_cycle_2026-04-26-day4`) — Day-3 magic-link token expired (~40min before cycle started), so Bob drove a fresh REQ-0002 send firm-side (`d8a58ade-…`, Mailpit `5J7vGK7B9w5LFAGvzFc9zf`, fresh token + orgId). Sipho exchanged token → portal `/home` "Pending info requests / 2" → `/requests/d8a58ade-…` detail rendered 3 FICA items → uploaded all 3 PDFs (test-fica-id/address/funds) → all 3 items SUBMITTED with document_ids, parent SENT→IN_PROGRESS. Portal read-model live-synced (3/3 submitted). L-42/L-43/L-44/L-47/P-01/P-02 all re-verified browser-driven. Zero new gaps. 4.11 SKIPPED-by-design (P-05 polish deferral, no per-item note textbox); 4.13 PASS-with-nuance (home count drops only after firm marks COMPLETED on Day 5 — implementation is `status !== "COMPLETED"`, consistent with Cycle 1 verify). Day 4 CLOSED → advance to Day 5 / 5.1.
- 2026-04-27 SAST — QA: Day 5 cycle-13 — 6 PASS / 0 FAIL / 0 BLOCKER. Fresh walk on main `597b4b60` (branch `bugfix_cycle_2026-04-26-day5`) — Bob reviewed REQ-0002 `d8a58ade-…` firm-side. Initial 5.3 walk found Download buttons missing; investigation showed running backend JVM (started 01:31) predated PR #1145 (`d2d0c26b` `documentFileName` mapper landed later) — `git merge-base --is-ancestor d2d0c26b main` confirms fix is on main. After `bash svc.sh restart backend` (33s), Download buttons render with filenames per L-45 fix; clicked Accept x3 → progress 1/3 → 2/3 → 3/3, parent COMPLETED at 2026-04-26 23:06:08, all items ACCEPTED with reviewed_at + reviewed_by populated. Portal read-model live-synced via L-43 listener (status=COMPLETED, accepted=3). Matter Overview FICA tile = `Done / Verified Apr 27, 2026 / View request` (L-46 re-verified). Mailpit got 4 outbound emails (3 per-item + 1 closure). Zero new gaps; zero regressions. Day 5 CLOSED → advance to Day 7 / 7.1 (scenario skips Day 6).
- 2026-04-27 SAST — QA: Day 7 cycle-14 — 2 PASS / 1 PARTIAL / 5 SKIPPED-BY-DESIGN / 1 FAIL / 2 BLOCKED (cascade). Fresh walk on main `3e018078` (branch `bugfix_cycle_2026-04-26-day7`). Context-swapped Bob → Thandi via gateway POST /logout + KC password login. **L-48 VERIFIED on UI** for the first time — matter-level `+ New Engagement Letter` button on RAF detail opens dialog with Customer combobox correctly locked to Sipho (no `/api/customers` 404). Created proposal `0781c5ad-… / PROP-0001 / DRAFT / HOURLY / Hourly Rate Note=R850/hr per LSSA 2024/2025 schedule / expires 2026-05-04`. 7.7 PASS (status=DRAFT). 7.8 FAIL → **BUG-CYCLE26-06 (HIGH, NEW BLOCKER)**: Send Proposal blew up with "2 required field(s) missing for Proposal Sending". Backend `PROPOSAL_SEND` prereq requires `Customer.contact_name` + `Customer.contact_email` — both NULL on Sipho (INDIVIDUAL customer, addressed via portal_contact). 7.9–7.11 BLOCKED. 7.2–7.5 SKIPPED-BY-DESIGN (L-49 Sprint 3). Mid-walk JWT expiry recovered via `/oauth2/authorization/keycloak` re-auth.
- 2026-04-27 SAST — Product: cycle-15 triage — BUG-CYCLE26-06 → SPEC_READY (option 1: backend fallback). Verified root cause at `backend/src/main/java/io/b2mash/b2b/b2bstrawman/prerequisite/StructuralPrerequisiteCheck.java:50-54` (`PROPOSAL_SEND_FIELDS` hard-requires `contact_name`/`contact_email`) called from `PrerequisiteService.java:198-220`. Fix is contained: add a 3-arg `check(customer, context, portalContactIdentitySatisfied)` overload to `StructuralPrerequisiteCheck`; reorder `PrerequisiteService.PROPOSAL_SEND` branch to look up portal_contacts FIRST, thread the `hasActiveContactWithEmail` boolean into the structural check, suppress contact_name/contact_email violations when satisfied, keep `address_line1` + portal-contact-existence checks intact. Confirmed downstream: `ProposalVariableResolver:43` and `AcceptanceService:228` already use portal_contact identity, never `Customer.contact_*` — those columns are dead weight at send time. No schema change. Spec: qa_cycle/fix-specs/BUG-CYCLE26-06.md. Cycle Count → 15.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-06 merged (PR #1172, squash `aa8e016c`). `StructuralPrerequisiteCheck` got 3-arg `check(customer, context, portalContactIdentitySatisfied)` overload that suppresses contact_name/contact_email checks for PROPOSAL_SEND when an ACTIVE portal_contact with email exists. `PrerequisiteService` reordered: portal_contact lookup first, ACTIVE-tightened existence check, then thread the boolean into structural check. 33/33 targeted tests pass. Backend restarted (PID 16195+ → fresh JVM). Cycle Count → 16.
- 2026-04-27 SAST — QA: Cycle 17 retest of PR #1173 (commit `5365e48a`) on `main`. **BUG-CYCLE26-06 VERIFIED** end-to-end: Send Proposal on `0781c5ad-…` for INDIVIDUAL customer Sipho (NULL contact_name/contact_email, ACTIVE portal_contact + email, populated address_line1) no longer triggers `"N required field(s) missing for Proposal Sending"`. Send dialog now reaches the next downstream `InvalidStateException("Proposal content must not be empty")` gate at `ProposalService.java:618-621` — the message-shape change is conclusive evidence the prereq fix landed. Recipient combobox shows only the single ACTIVE portal_contact (ACTIVE-tightening confirmed). Proposal stays DRAFT (correct, blocked at gate-2 before status mutation). Mailpit empty (correct). New finding: **BUG-CYCLE26-07 (HIGH, OPEN)** — gate-2 (`content_json` empty) blocks every proposal created via the matter-level New Engagement Letter dialog; pre-existing since Mar 1, 2026 commit `4023f683` but masked by gate-1. Different root cause from BUG-CYCLE26-06; Day 7 7.8 onwards remains blocked but on a separate issue. QA Position **HELD at Day 7 / 7.8** — orchestrator triages BUG-CYCLE26-07 before Day 8 walk. Cycle Count → 17.
- 2026-04-27 SAST — Product: cycle-18 triage — BUG-CYCLE26-07 → SPEC_READY (option 1B: seed minimal Tiptap doc at `createProposal()` time, NOT at Send time). Verified root cause at `backend/src/main/java/io/b2mash/b2b/b2bstrawman/proposal/ProposalService.java:617-621` (gate-2) and `:156` (where `contentJson` is conditionally set — the source of the empty-map persistence). Confirmed downstream consumer `ProposalPortalSyncEventHandler.java:97-98` calls `tiptapRenderer.render(proposal.getContentJson(), …)` — a non-empty Tiptap `doc` tree is the legitimate contract, so gate-2 must stay honest. Rejected option 2 (frontend seed) — adds two seed sites and skips backend test surface; rejected option 3 (Generate engagement letter step) — that's GAP-L-49 Sprint 3 deferred (L effort, multi-day). Fix is contained: new `ProposalContentSeeder` service builds doc from `(title, feeModel, hourlyRateNote, amounts, expiresAt)` + `{{client_name}}` variable; `ProposalService.createProposal` falls back to seeder when caller omits / sends empty contentJson; gate-2 untouched. Spec: qa_cycle/fix-specs/BUG-CYCLE26-07.md. Branch updated to `bugfix_cycle_2026-04-26-day7-v2`. Cycle Count → 18.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-07 merged (PR #1174, squash `7d602dee`). New `ProposalContentSeeder` @Service emits minimal Tiptap doc (heading + greeting with `client_name` variable + fee summary + optional rate/hours/description/expiry paragraphs + standard terms) at `createProposal()` time when contentJson is null/empty. Existing `sendProposal_emptyContent_returns400` updated to forcibly clear `content_json` via direct SQL so gate-2 keeps regression coverage. Targeted tests: 11/11 `ProposalContentSeederTest` + 15/15 `ProposalSendTest` + 101/101 across Proposal* package green. Backend restarted (fresh JVM on :8080). Cycle Count → 19.
- 2026-04-27 SAST — QA: Cycle 19 retest of PR #1175 (squash `0a58a242`) on `main` + Day 7 §7.8-§7.11 walk. **BUG-CYCLE26-07 VERIFIED end-to-end.** Created fresh proposal `69e3d65f-…` (PROP-0002, Cycle19 Verify, HOURLY, R850/hr) via matter-level New Engagement Letter dialog. DB confirms `content_json` populated with 6-child Tiptap doc per seeder spec (vs old proposal's `'{}'`). Send Proposal succeeds — gate-2 cleared; status DRAFT→SENT with `sent_at` populated and `portal_contact_id` set; `portal.portal_proposals` row written with rendered HTML containing `<p>Dear Sipho Dlamini,</p>` (client_name variable interpolation). Sipho's portal session at `:3002/proposals/69e3d65f-…` renders heading + body + Accept/Decline. **§7.10 Mailpit NOT-APPLICABLE — informational** scope gap: matter-level Send Proposal codepath publishes `ProposalSentEvent` whose listeners only do in-app notifications + portal sync; no email-channel listener exists. Orphaned `portal-new-proposal.html` template suggests this was planned but never wired. Not a BUG-CYCLE26-08 (no regression from PR #1175; portal sync still surfaces the proposal to Sipho). Suggested deferred follow-up: wire `PortalEmailService.sendNewProposalEmail()` listener for `ProposalSentEvent`. **Day 7 CLOSED** → advance to Day 8 / 8.1. QA Position: `Day 8 / 8.1`. Branch field updated to `main`. Cycle Count → 19.
- 2026-04-27 SAST — QA: Day 8 cycle-20 — 6 PASS / 0 FAIL / 2 PARTIAL / 0 BLOCKED / 2 SKIPPED-BY-DESIGN (10 checkpoints) + 4 wrap-up checks (3 PASS, 1 PARTIAL — magic-link re-auth path works after backend curl fallback for missing-orgId on portal `/login` form). Fresh walk on `bugfix_cycle_2026-04-26-day8` (cut from main `e79f0b33`) as Sipho via fresh magic-link. PROP-0002 reviewed end-to-end: portal `/proposals` list → detail page (rendered Tiptap body with greeting "Dear Sipho Dlamini," + Fee Arrangement + Rate "R850/hr per LSSA 2024/2025 schedule" + standard terms + Accept/Decline buttons) → click Accept → status SENT→ACCEPTED instantly, showing success card "Thank you for accepting this proposal. Your project has been set up." → DB confirms `proposals.status=ACCEPTED`, `accepted_at=2026-04-27 03:18:57.981719+00`, `portal_proposals.status=ACCEPTED` (read-model live-synced), firm-side `PROPOSAL_ACCEPTED` notification fired with project auto-created. Page reload shows "This proposal has been accepted." with NO Accept button — double-accept prevented at UI level. `/proposals` list post-accept: "Awaiting Your Response" section header gone, PROP-0002 shows ACCEPTED badge. Terminology consistent ("proposal" throughout sidebar/heading/route/button/copy). 8.1 PARTIAL (no proposal email in Mailpit — same as §7.10 NOT-APPLICABLE; portal-list path used). 8.2 PARTIAL (no fee estimate breakdown / VAT / effective date per L-49 Sprint-3 deferral). 8.3 SKIPPED-BY-DESIGN (L-49 cascade). 8.6 N/A (this tenant uses /proposals/[id] not /accept/[token]). 8.9 SKIPPED-BY-DESIGN (no Pending-proposals home tile to drop from). **Zero new bugs logged.** Three informational follow-ups noted but NOT logged as BUG-CYCLE26-XX gaps (all pre-existing, all non-blocking): (a) §7.10 carry-forward — wire portal-new-proposal email listener; (b) cosmetic — add Pending-proposals tile to portal home dashboard; (c) frontend portal `/login` request-link form missing orgId field (backend curl with explicit orgId works). **Day 8 CLOSED** → advance to Day 10 / 10.1 (scenario skips Day 9). Tooling note: at dispatch start the Playwright MCP browser was held by stale Chrome PID 14210 from a sibling idle claude session; cleared the singleton lock and killed the stale Chrome cleanly. Cycle Count → 20.
- 2026-04-27 SAST — QA: Day 10 cycle-21 — 8 PASS / 0 FAIL / 0 PARTIAL / 0 BLOCKED / 1 SKIPPED-not-applicable (8 in-scope checkpoints + 3 wrap-up rollups all PASS). Fresh walk on `bugfix_cycle_2026-04-26-day10` (cut from main `c194faca`) as Thandi (Owner) via Keycloak. Phase A: PROP-0002 ACCEPTED state flows portal→firm cleanly — global `/proposals` shows Accepted badge + Apr 27 2026 timestamp matching Sipho's Day 8 portal accept; per-proposal detail confirms all three timestamps (Created/Sent/Accepted) at Apr 27 2026; RAF matter `cc390c4f-…` already ACTIVE (auto-active since Day 3 creation; no PROSPECT state in this build's project lifecycle enum). Phase B: Manual deposit R 50 000,00 against Sipho/RAF-2026-001 via Record Deposit dialog (UUID inputs — UX papercut, pre-existing); status RECORDED (no approval queue, dual-approval off); three reconcile points all PASS — trust account cashbook + Sipho's auto-created client ledger card (`36765353-…`, `balance=50000.00`, `total_deposits=50000.00`) + matter Trust tab "Funds Held" = R 50 000,00. Side-effect: trust-activity nudge email auto-fired to Sipho (Mailpit `m7qSdt8XULPXMs6mX6Vbcn`, "Mathebula & Partners: Trust account activity"), Day 11 §11.1 prereq satisfied. Email body has cosmetic-only polish gaps (raw ISO timestamp, unformatted `50000`, uppercase `DEPOSIT`) — will affect Day 11 §11.5 sanitisation polish but does not block the trust-ledger view. 10.6 SKIPPED-not-applicable (dual-approval off so no approval queue path to exercise). **Zero new bugs logged.** Two informational follow-ups noted but NOT logged as gaps (raw-UUID Record Deposit dialog inputs + nudge email body polish — both pre-existing scope quirks, neither blocks Day 11). **Day 10 CLOSED** → advance to Day 11 / 11.1. Cycle Count → 21.
- 2026-04-27 SAST — Orchestrator: cycle-21 CodeRabbit review (PR #1177) flagged three real product polish bugs visible in Day 10 evidence (not blockers, not from scenario walk): added BUG-CYCLE26-08 (`INTEREST_LPFF` enum-prettify renders "Interest Lpff" instead of "Interest LPFF" — `cycle21-day10-10.4-transactions-page.yml:140`), BUG-CYCLE26-09 (breadcrumb shows raw `client-ledgers` slug + raw customer UUID instead of friendly labels — `cycle21-day10-10.7-sipho-ledger-detail.yml:88-100`), BUG-CYCLE26-10 (activity-feed message templates emit literal `"for unknown"` when target/recipient name is null — `cycle21-day10-10.1-raf-matter-detail.yml:276-290`). All three logged as OPEN, severity LOW (cosmetic), to be specced/fixed after Day 11 walk closes. CR's QA-evidence-modification asks (UUID redaction, dev-overlay filtering, personal-data placeholders, snapshot-serializer changes) declined — QA evidence is point-in-time DOM capture; redacting it defeats the audit-trail purpose.
- 2026-04-27 SAST — Product: cycle-22 triage of CR-flagged polish bugs. Three OPEN → three SPEC_READY on fresh branch `bugfix_cycle_2026-04-26-cr-polish` (cut from main `8258a100`). All confirmed LOW (cosmetic, non-blocking). **BUG-CYCLE26-08**: 3 duplicate `transactionTypeLabel()` helpers in `frontend/app/(app)/org/[slug]/trust-accounting/{page,transactions/page,client-ledgers/[customerId]/page}.tsx` lines 39-67 use generic word-boundary title-case which destroys the LPFF acronym. Fix: consolidate into single `Record<TrustTransactionType, string>` label map exported from `frontend/lib/types/trust.ts`; delete the 3 duplicates. **BUG-CYCLE26-09**: `frontend/components/breadcrumbs.tsx` `SEGMENT_LABELS` (lines 8-49) and `PARENT_SEGMENT_FALLBACKS` (lines 52-58) are missing entries for `client-ledgers`. Fix: 2 new map entries → renders `Client Ledgers › Client Ledger`, matching the existing UUID-fallback pattern used by `customers/projects/invoices/proposals`. Customer-name lookup deliberately avoided — would require layout-level fetch or context provider for a cosmetic polish. **BUG-CYCLE26-10**: backend `InformationRequestService.acceptItem` lines 453-465 writes `request_id`/`item_name` to audit details but forgets `request_number` — outlier vs every other `information_request.*` audit emission in the same service. Fix: 1-line `acceptAuditDetails.put("request_number", request.getRequestNumber())` + same defensive add to `rejectItem` for consistency + regression test in `ActivityMessageFormatterTest`. Backend restart required. All 3 specs are independent (BUG-08, -09 are frontend HMR — no restart; -10 is backend single Java change). **Recommended Dev dispatch order**: parallel — three separate PRs (`fix/BUG-CYCLE26-08`, `fix/BUG-CYCLE26-09`, `fix/BUG-CYCLE26-10`) cut off `bugfix_cycle_2026-04-26-cr-polish`. Standing rule "one fix per PR" honoured. Total estimated effort: ~75 min combined. Cycle Count → 22.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-09 merged (PR #1179, squash `2c167711`). Two map entries added to `frontend/components/breadcrumbs.tsx` (`SEGMENT_LABELS["client-ledgers"]="Client Ledgers"`, `PARENT_SEGMENT_FALLBACKS["client-ledgers"]="Client Ledger"`). Frontend lint + build green. HMR — no restart. Cycle Count → 23.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-10 merged (PR #1180, squash `b25b0146`). `InformationRequestService.acceptItem` + `rejectItem` now write `request_number` to audit details; the activity-feed `"%s accepted \"%s\" for %s"` template now renders the real REQ-NNNN number instead of the literal string `"unknown"`. Defensive add to `rejectItem` keeps every `information_request.*` audit emission in the service consistent. New `informationRequestItemAcceptedInterpolatesRequestNumber` regression test in `ActivityMessageFormatterTest`. 68/68 targeted tests pass (Activity + InformationRequestController + NotificationAuditIntegration). Backend restart required after cycle-branch merge to main. Cycle Count → 24.
- 2026-04-27 SAST — QA: Cycle 25 retest of PR #1181 (squash `73c133b6`) on `main`. **3/3 fixes verified, 0 REOPENED**. **BUG-CYCLE26-08 VERIFIED** browser-driven: `/trust-accounting/transactions` filter list renders `Interest LPFF` (was `Interest Lpff` in cycle 21); `/trust-accounting/client-ledgers/{sipho-id}` filter list also renders `Interest LPFF`. **BUG-CYCLE26-09 VERIFIED** browser-driven: breadcrumb on Sipho's ledger detail now renders `Mathebula & Partners › Trust Accounting › Client Ledgers › Client Ledger` (was `client-ledgers › c4f70d86-…` in cycle 21). UUID still in href but not displayed text. **BUG-CYCLE26-10 VERIFIED-CODE-ONLY (Option B per spec)**: backend restarted to pick up PR #1180 Java change (stale JVM PID 724 etime 03:03:02 predated PR #1180 merge; fresh JVM PID 21748 now has the fix); DB confirms pre-existing audit rows lack `request_number` so they still render "for unknown" (forward-only correctness, expected); no fresh accept event was triggerable (REQ-0002 already 3/3 ACCEPTED, REQ-0001 items PENDING — would need full portal upload flow); authoritative confirmation = `ActivityMessageFormatterTest#informationRequestItemAcceptedInterpolatesRequestNumber` regression test asserts `for REQ-0042` rendering. Day 11 walk **may proceed**. Cycle Count → 25.
- 2026-04-27 SAST — QA: Day 11 cycle-26 — 6 PASS / 1 PARTIAL / 1 BLOCKED-AT-LINK / 0 SKIPPED-BY-DESIGN (8 in-scope checkpoints + 4 wrap-up rollups all PASS). Fresh walk on `bugfix_cycle_2026-04-26-day11` (cut from main `1b984153`) as Sipho via fresh magic-link (Day-10 token expired — re-issued via `POST /portal/auth/request-link` with email + orgId; exchange landed authenticated on `:3002/projects`). Underlying portal trust-ledger view works end-to-end: `/home` "Last trust movement / R 50 000,00 / 27 Apr 2026" tile present; `/trust` index auto-redirects to `/trust/{matterId}` (RAF `cc390c4f-…`); page renders R 50 000,00 balance card + 1-row Transactions table (`27 Apr 2026 / DEPOSIT / Initial trust deposit — RAF-2026-001 / R 50 000,00 / R 50 000,00`) + Statements empty-state. ZAR locale + SA date format throughout. Wrap-up checkpoints: deposit visible same minute (firm posted 04:01:46Z, portal renders same row); amount reconciles to firm-side `client_ledger_cards.balance=50000.00`; description sanitisation PASS (no `[internal]` tags, ≤140 chars); ZAR currency consistent across 3 monetary cells. **One new gap: BUG-CYCLE26-11 (LOW, OPEN)** — auto-fired trust-activity nudge email's "View trust ledger" CTA links to `/trust/46d1177a-…` (trust account ID) instead of `/trust/cc390c4f-…` (matter ID); portal route 404s with "No trust balance is recorded for this matter / The requested resource was not found." Root cause located at `PortalEmailNotificationChannel.java:297` (variable named `matterKey` but receives `event.trustAccountId()`). Non-cascading: Sipho still reaches the page via sidebar/home tile auto-redirect. §11.2 BLOCKED-AT-LINK; §11.5 PARTIAL only because portal Type column shows raw `DEPOSIT` enum (carry-forward, NOT logged as new gap — same surface as Day-10 firm-side and the email body). QA Position **HELD at Day 11 / 11.X (closed walk)** pending Product triage of BUG-CYCLE26-11 — per per-day workflow §2 ("real product bugs even non-blocking UX become SPEC_READY"). Cycle Count → 26.
- 2026-04-27 SAST — Product: cycle-27 triage — BUG-CYCLE26-11 → SPEC_READY. Hypothesis verified at code level: `PortalEmailNotificationChannel.java:296-300` and twin `:321-326` literally read `UUID matterKey = event.trustAccountId();` then build `portalBaseUrl + "/trust/" + matterKey` — variable name says matter, value is trust account. Portal route `portal/app/(authenticated)/trust/[matterId]/page.tsx:82` does `res.matters.find((m) => m.matterId === matterId)` so a trust-account UUID never matches → empty-state branches at lines 187-197 + 404 errors from the per-matter Transactions/Statements API consumers. Confirmed both events do NOT carry the matter ID today (`TrustTransactionRecordedEvent` and `TrustTransactionApprovalEvent` records, fields enumerated in `event/*.java`); `TrustTransaction` entity DOES carry `projectId` (`transaction/TrustTransaction.java:35-36, 144-146`), and every publish site has access via `request.projectId()` or `transaction.getProjectId()`. Fix scope: backend-only, ~7 files, S effort. Plan: (1) add `UUID projectId` component to both event records + every factory (`recorded`, `awaitingApproval`, `approved`, `rejected`); (2) update ~8 publish sites in `TrustTransactionService` to pass `projectId`; (3) replace `event.trustAccountId()` with `event.projectId()` in the two `buildTrustActivityContext` overloads (rename local var `matterKey` → `matterId`); (4) update existing `PortalEmailNotificationChannel*Test`, `TrustLedgerPortalSyncService*Test`, `TrustNotificationHandlerTest` factory-call sites + add a CTA-href assertion + add a null-projectId fallback regression test. Regression risk LOW — `TrustLedgerPortalSyncService` consumes `event.trustAccountId()` (untouched, behaviour unchanged); `TrustNotificationHandler` consumes `transactionId/amount/eventType` (untouched); `trustUrl` template variable has a single consumer (`portal-trust-activity.html:37`). Severity confirmed LOW (non-cascading per Day-11 evidence — sidebar/home tile auto-redirect reach the correct page). Spec written to `qa_cycle/fix-specs/BUG-CYCLE26-11.md`. **Recommended Dev dispatch: single agent, single PR (`fix/BUG-CYCLE26-11`) cut from `bugfix_cycle_2026-04-26-day11`** per Standing Rule "one fix per PR". Backend restart required after merge. Cycle Count → 27.
- 2026-04-27 SAST — Dev: BUG-CYCLE26-11 merged (PR #1182, squash `08abca83`). Threaded `UUID projectId` through `TrustTransactionRecordedEvent` + `TrustTransactionApprovalEvent` records and every factory (`recorded`, `awaitingApproval`, `approved`, `rejected`). Updated all 8 publish sites in `TrustTransactionService` (deposit, transfer-out, transfer-in, payment, fee-transfer, refund, approve, reject) to pass `saved.getProjectId()` / `transaction.getProjectId()`. Replaced `event.trustAccountId()` → `event.projectId()` in both `PortalEmailNotificationChannel.buildTrustActivityContext` overloads (renamed local var `matterKey` → `matterId`); null-safe fallback to `/trust` index preserved for cross-customer transfers. Strengthened `PortalEmailNotificationChannelIntegrationTest` to assert CTA href contains `/trust/{projectId}` and never `/trust/{trustAccountId}`; added new regression test `trustRecordedEvent_nullProjectIdFallsBackToTrustIndex`. 72/72 targeted tests pass (8/8 `PortalEmailNotificationChannelIntegrationTest` + 46/46 `TrustTransactionServiceTest` + 10/10 `TrustLedgerPortalSyncServiceIntegrationTest` + 8/8 `TrustNotificationHandlerTest`). Backend restart required. Cycle Count → 28.
- 2026-04-27 SAST — QA: Cycle 29 retest of PR #1183 (squash `8dfb4920`) on `main`. **BUG-CYCLE26-11 VERIFIED, 0 REOPENED.** Backend restart required (PID 35801 was started 12:13 SAST, 30 min before PR #1183 merged at 12:47 SAST — `etime` predates the fix; restart yielded fresh PID 41372 with the merged `projectId`-threading fix on the running JVM). Mailpit cleared, then Bob (admin) drove a fresh Record Deposit via the firm UI: R 100,00 against Sipho/RAF (Reference DEP/2026/RAF-002, txn `f2f692e8-…`, project_id correctly populated to RAF matter `cc390c4f-…`). New trust-activity email `gEDSuhVsW2wp6im6oDfXbz` arrived: only HREF in body = `http://localhost:3002/trust/cc390c4f-35e2-42b5-8b54-bac766673ae7` (matter UUID ✓; NOT trust-account UUID `46d1177a-…`; NOT `/trust/null`). Sipho authenticated via a fresh magic-link, navigated directly to the email's CTA URL → page rendered correctly: Trust balance card R 50 100,00 + "Matter cc390c4f" label, Transactions table with both deposits (R 100,00 retest + R 50 000,00 initial), Statements empty-state. NO 404, NO "No trust balance is recorded for this matter" error, NO "The requested resource was not found" panel. **Day 14 walk MAY proceed.** Cycle Count → 29.
- 2026-04-27 SAST — QA: **Cycle 30 — Day 14 walk on bugfix_cycle_2026-04-26-day14 (cut from main `7ea65235`).** All 11 checkpoints executed: 9 PASS, 1 PARTIAL (14.2 — beneficial-owners GAP-L-54 carry-forward, no UI affordance), 1 SKIPPED-BY-DESIGN (14.4 conflict check per scenario), 0 FAIL/BLOCKED, **0 new gaps**. Bob (admin) drove the entire firm-side flow on Tab 0 (Admin role sufficient, no KC handoff). Created Moroka Family Trust client (`0cb199f2-…`, TRUST, PROSPECT) → L-34 auto-provisioned `portal_contacts` row `3c127b78-…` for moroka.portal@example.com (verified for TRUST customer_type) → matter "Estate Late Peter Moroka" `340c5bb2-…` (EST-2026-002, ACTIVE, applied_field_groups=[Project Info, SA Legal — Matter Details]; **L-37-V112 holds — conveyancing pack correctly excluded from deceased-estate matter**) → REQ-0003 with **Liquidation and Distribution Account Pack (5 items)** template (L-53 holds), due 2026-05-11, addressed to Moroka portal contact (`de3cffc7-…`) → Mailpit confirmed "Information request REQ-0003 from Mathebula & Partners" delivered to moroka.portal@example.com at 11:04:02Z → uploaded `letters-of-authority.pdf` document (`9eb9ed95-…`) → recorded R 25 000,00 trust deposit `0e9f9c17-…` against Moroka/EST-2026-002 (DEP/2026/EST-002 RECORDED, **project_id correctly populated — BUG-CYCLE26-11 fix holds for fresh deposit**); per-customer ledger card `886e0759-…` created with balance R 25 000,00 (separate from Sipho's `36765353-…` at R 50 100,00). Tenant `tenant_5039f2d497cf` retained (single-tenant per scenario; trust account `46d1177a-…` shared). Zero error-level console messages across all navigations. **QA Position → Day 15 — 15.A.** `qa_cycle/isolation-probe-ids.txt` updated with cycle-30 fresh IDs. Day 15 portal-isolation probes ready to dispatch. Cycle Count → 30.
- 2026-04-27 SAST — QA: **Cycle 31 — Day 15 isolation gate on bugfix_cycle_2026-04-26-day15 (cut from main `d20319c0`).** **ISOLATION VERIFIED — 15/15 probes PASS, 0 FAIL, 0 new bugs raised.** Phase A (8 frontend probes via Playwright MCP, Sipho portal session at :3002 after magic-link auth + token-exchange): `/home` shows only Sipho's own data (R 100,00 last trust movement, Pending requests=1); `/projects` lists ONLY Dlamini v RAF (Moroka EST-2026-002 absent); direct `/projects/340c5bb2-…` renders not-found empty-state; `/documents` is Next.js 404 (no portal route — narrow attack surface); `/requests` lists ONLY REQ-0001 + REQ-0002 (REQ-0003 absent); direct `/requests/de3cffc7-…` renders not-found; `/trust` auto-redirects to Sipho's matter (no matter switcher), balance R 50 100,00, transactions show only DEP-RAF entries (Moroka R 25 000 absent); direct `/trust/340c5bb2-…` renders not-found. Phase B (6 backend probes, `Authorization: Bearer <Sipho portal_jwt>`, `X-Org-Slug: mathebula-partners`): `GET /portal/projects/340c5bb2-…` → 404 "Project not found"; `GET /portal/requests/de3cffc7-…` → 404 "InformationRequest not found"; `GET /portal/requests` → 200 list contains only REQ-0001 + REQ-0002 (Dlamini matter — REQ-0003 absent); `GET /portal/documents/9eb9ed95-…` → 404 (no such direct route); **`GET /portal/trust/summary` → 200 with matters array containing exactly one entry — Sipho's `cc390c4f-…` at R 50 100,00, Moroka `340c5bb2-…` not aggregated**; `GET /portal/trust/matters/340c5bb2-…/transactions` → 404 "Project not found". Phase D (digest/email): no scheduled digest fired in test window — Mailpit only contained the 2 magic-link emails. **No BUG-CYCLE26-12 raised.** Backend chose 404 (not 403) throughout — information-disclosure-safer (does not confirm resource existence). Per-customer ledger isolation verified at API layer; Sipho's `portal_jwt` (sub=`c4f70d86-…`) cannot pivot to Moroka's tenant-internal entities. Day 15 closes clean. Evidence: `qa_cycle/checkpoint-results/cycle31-day15-15.A-{home,projects,projects-moroka-direct,documents,requests,requests-moroka-direct,trust,trust-moroka-direct}.yml` + `cycle31-day15-15.B-{projects-moroka,requests-moroka,requests-list,documents-moroka,trust-summary,trust-moroka-tx}.json`. **QA Position → Day 30 — 30.1** (scenario continues). Cycle Count → 31.
- 2026-04-27 SAST — QA: **Cycle 32 — Day 30 walk on bugfix_cycle_2026-04-26-day30 (cut from main `51f286e5`).** **HALTED at 30.1 — data-prerequisite blocker, not a code bug. 0 PASS / 0 FAIL / 0 PARTIAL / 11 BLOCKED / 0 SKIPPED.** Tenant `tenant_5039f2d497cf` (Mathebula & Partners) DB-side: zero `time_entries`, zero `legal_disbursements`, zero `court_dates`, zero `invoices`, zero `invoice_lines`, zero `payment_events`. Browser-confirmed: firm `/org/mathebula-partners/invoices` renders empty-state heading "No fee notes yet" (copy: "Generate fee notes from tracked time or create them manually. You'll need at least one matter with logged time."). Mailpit `/` shows only 2 magic-link emails (the cycle-31 isolation walk artefacts) — **zero fee-note emails**. Day 30.1 ("Mailpit → open fee-note email → click View fee note") therefore cannot start; 30.2–30.10 cascade-block; 30.11 (passive isolation spot-check) vacuously holds since both customers have zero invoices. Carry-forward state intact: `org_integrations` rows present for PAYMENT/mock (L-64 fix) + KYC_VERIFICATION/verifynow, both enabled; trust accounts + ledger cards from Day 10/14 untouched; proposals (PROP-0001 DRAFT + PROP-0002 ACCEPTED) intact; portal_contacts intact; Sipho's `lifecycle_status=ONBOARDING` (Day-28 cycle-1 transition residue, fine — L-56/L-60 PROSPECT-gate fixes mean current code allows time/invoice creation regardless). Sipho `tax_number=NULL` — would re-trigger GAP-L-62 prerequisite at fee-note generation, but PR #1126 should auto-populate from ID number on next dialog open. Logged as **OBS-CYCLE26-01** (observational, not a product gap): the carry-forward Day-15 tenant state is missing the Day 21 + Day 28 artefacts that all subsequent billing days assume. Recommended orchestrator action: dispatch a fresh `bugfix_cycle_2026-04-26-day21-28-replay` walk (cut from main) to materialise time-entries + disbursements + fee-note + email, then resume Day 30 cycle-32. This doubles as fresh post-merge regression coverage for the L-56/L-57/L-58/L-60/L-61/L-62/L-63 fix bundle. Alternative: accept cycle-1 INV-0002 PAID evidence (`day-30-cycle1-l64-verified-firm-inv0002-paid.png`) as canonical and advance to Day 45 — but Day 60.5 closure ("no unpaid fee notes" GREEN required) re-introduces the same prerequisite, so deferral only delays the work. **No new product gaps logged.** Evidence: `qa_cycle/checkpoint-results/day-30.md` (cycle-32 section), `day-30-cycle32-30.0-firm-fee-notes-empty.yml`, `day-30-cycle32-30.1-mailpit-no-fee-note-email.yml`. Cycle Count → 32.
- 2026-04-27 SAST — QA: **Cycle 33 — Day 21 + Day 28 replay on bugfix_cycle_2026-04-26-day21-replay (cut from main `5948080e`).** **OBS-CYCLE26-01 RESOLVED.** All Day 30 prerequisites materialised. Day 21: 12/12 PASS, 0 FAIL, 0 BLOCKED — time entries (2× billable, total 4h via per-task `Log Time` dialogs on RAF matter), disbursement (Sheriff Fees R 1 250 to Sheriff Pretoria, recoverable, DRAFT then approved during Day 28 prep), court date (Pre-Trial 2026-05-11 at Gauteng Division Pretoria, attorney Bob; **dashboard "Upcoming Court Dates" widget renders the new entry — improvement vs. cycle-1 21.12 partial**). L-56 + L-57 hold under fresh walk. Day 28: 8/8 PASS — required mid-walk data prep (project rate override R 850/hr ZAR for Bob → re-log time entries with rate snapshot; set Sipho `tax_number=8501015800088` → **lifecycle auto-transitioned ONBOARDING → ACTIVE** as a side-effect of L-62 fix + activation prereq composition; submit-for-approval → approve disbursement DRAFT→APPROVED) before billing-run wizard surfaced Sipho with R 3 400,00 unbilled time. Wizard happy-path: configure period 2026-04-01..2026-04-30 → discovery → cherry-pick (both time entries kept) → DRAFT invoice `432ae5a9-…` created with subtotal=3400.00 + 15% VAT 510.00 = 3910.00. Invoice detail → Add Disbursements → R 1 250 sheriff fee added → subtotal R 4 650, total R 5 160 → Approve (status APPROVED, invoice_number INV-0001) → Send Fee Note (status SENT). Mailpit fresh email `Fee Note INV-0001 from Mathebula & Partners` to `sipho.portal@example.com` at 2026-04-27T12:48:02Z — terminology PASS at email subject. L-60/L-61/L-62/L-63 all re-verified end-to-end browser-driven. **0 new product gaps logged this cycle.** Two carry-forward observations (not new): (1) VAT computed on time-entry lines but not on disbursement lines (R 1 250 × 15% missing); (2) Bulk Billing wizard preview reads `expenses` not `legal_disbursements` so disbursements must be added at the invoice-detail step (UX doubling). Final tenant state: 2 time_entries (rate R 850 ZAR each), 1 legal_disbursement (APPROVED+BILLED), 1 court_date (SCHEDULED), 1 invoice (SENT, R 5 160). Sipho lifecycle=ACTIVE. Mailpit has fee-note email. **QA Position → Day 30 — 30.1 (replay-ready)**. Evidence: `qa_cycle/checkpoint-results/day-21.md` §Cycle 33, `day-28.md` §Cycle 33, `cycle33-day21-*.yml` (13 files), `cycle33-day28-*.yml` (35 files). Cycle Count → 33.
- 2026-04-27 SAST — QA: **Cycle 34 — Day 30 replay on bugfix_cycle_2026-04-26-day30-replay (cut from main `30c8f373`).** **PASS end-to-end. 11 PASS / 0 FAIL / 0 PARTIAL / 0 BLOCKED / 0 SKIPPED.** Sipho authenticated via fresh magic-link (orgId=mathebula-partners), opened the cycle-33 fee-note email "Fee Note INV-0001 from Mathebula & Partners" at Mailpit `/view/9Gdw7cVrG2nUznVgYktfJx`, followed `View Fee Note` CTA → portal `:3002/invoices/432ae5a9-…` rendered fee-note detail (3 line items — 2 time + 1 disbursement; Subtotal R 4 650, VAT 15% R 510, Zero-rated R 0, Total **R 5 160,00**). Pay Now → MockPaymentGateway `:8080/portal/dev/mock-payment` → Simulate Successful Payment → backend transitioned `payment_events` row CREATED → COMPLETED (mock provider, session `MOCK-SESS-0aafd30c-…`, OPERATING destination); browser auto-redirected to `/payment-success` ("Payment confirmed / Payment received — thank you! / Paid on 27 Apr 2026"). DB confirms `invoices.status=PAID`, `paid_at=2026-04-27 13:17:08.266573+00`. Re-opened portal fee-note detail → badge SENT→PAID, Pay Now banner removed. Portal `/invoices` list shows INV-0001 PAID; isolation intact (no Moroka invoices). Firm-side `/org/mathebula-partners/invoices/432ae5a9-…` shows "Paid" badge + "Payment Received / Paid on Apr 27, 2026" + Payment History (Created → Completed mock R 5 160,00 reference MOCK-PAY-15909c5f-…). **L-64 (PAYMENT/mock org_integration) end-to-end re-verified on current main HEAD.** **One new gap: GAP-L-66 (LOW, OPEN)** — portal `/login` form omits `orgId`, breaks "deep-link → expired session → re-login → return" flow when a portal user follows the email's "View Fee Note" CTA after their session expired (workaround = paste the magic-link URL which carries orgId; original cycle-33 magic-link had expired so cycle-34 used a fresh `POST /portal/auth/request-link` with `orgId=mathebula-partners` to get a usable link). No auto-receipt email is fired (out-of-scope per dispatch rules — payment integration is a stub; receipt = same `Download PDF` button on the now-PAID fee note). Console errors at every checkpoint: 0. **QA Position → Day 45 — 45.1** (Days 31–44 not scripted in scenario; next scenario action is Bob: second info request `Supporting medical evidence` + second R 20 000 trust deposit → R 70 000 total). Recommended next branch: `bugfix_cycle_2026-04-26-day45` cut from `main`. Evidence: `qa_cycle/checkpoint-results/day-30.md` §Cycle 34 Replay; 12 cycle34 YAML snapshots. Cycle Count → 34.
- 2026-04-27 SAST — Dev: **GAP-L-66 merged (PR #1188, squash `20bcc976`)** on `bugfix_cycle_2026-04-26-day30-replay`. Frontend-only fix landed exactly per spec across four files: (1) `portal/lib/auth.ts` adds `portal_last_org_id` localStorage key written in `storeAuth()` and intentionally preserved across `clearAuth()` (orgId is a public branding slug — not a credential); new `getLastOrgId()` helper. (2) `portal/app/(authenticated)/layout.tsx` threads `pathname` (`?redirectTo=…`) + `getLastOrgId()` (`?orgId=…`) into the `router.replace("/login")` URL on auth-clear. (3) `portal/app/login/page.tsx` sources orgId via `query ?? getLastOrgId()` (hydrated in a mount effect for SSR safety); inline guard short-circuits submit when orgId still null (avoids guaranteed 400); `redirectTo` persisted to `sessionStorage`. (4) `portal/app/auth/exchange/page.tsx` consumes `sessionStorage.portal_post_login_redirect` after `storeAuth()` with a `/`-prefix open-redirect guard, falling back to `/projects` for missing/external paths. **8 new test cases** across the 3 existing test files: `lib/__tests__/auth.test.ts` (`storeAuth persists portal_last_org_id` + `clearAuth preserves portal_last_org_id` + `getLastOrgId returns persisted value`), `app/login/__tests__/page.test.tsx` (localStorage fallback + inline guard + redirectTo sessionStorage persistence), `app/auth/exchange/__tests__/page.test.tsx` (sessionStorage redirect honoured + open-redirect guard rejects external URL). Verified locally: portal `pnpm run lint` clean (only pre-existing warnings), `pnpm run build` OK, `pnpm test` **177/177 pass** (40 test files). HMR — no service restart needed. CI: qodana pass (7m19s), CodeRabbit skipped (intra-cycle). Cycle Count → 36.
- 2026-04-27 SAST — QA: **Cycle 37 retest of PR #1189 (squash `06d5a633`) on `main` — GAP-L-66 VERIFIED end-to-end.** Browser-driven retest reproducing the originally-broken flow. **Setup**: Sipho fresh magic-link auth populated `portal_last_org_id="mathebula-partners"` + `portal_customer={...}` localStorage; forced session expiry by clearing `portal_customer` + non-HttpOnly cookies, **preserved `portal_last_org_id`** (per fix design — orgId is a public slug). **Step 1 PASS**: Navigate `:3002/invoices/432ae5a9-6a3c-4c25-86b3-302f0dd016e1` (no session) → `(authenticated)/layout.tsx` `useEffect` → `router.replace("/login?redirectTo=%2Finvoices%2F432ae5a9-6a3c-4c25-86b3-302f0dd016e1&orgId=mathebula-partners")` ✓. **Step 2 PASS**: `/login` form rendered with "Mathebula & Partners" branding (orgId hydrated from query → `/portal/branding` fetch succeeded); typed `sipho.portal@example.com` + Send Magic Link → success card "Check your email for a login link." with dev-mode link `/auth/exchange?token=QxHxYW9dbtSgiperXxund9mbbNuwpu1ma6WitvDraU4&orgId=mathebula-partners`; **no "Something went wrong" 400 error**; Mailpit `Bav8Mu5FjJkggXnrdS4xP3` received 14:09:57Z; sessionStorage `portal_post_login_redirect="/invoices/432ae5a9-…"` persisted ✓. **Step 3 PASS**: clicked dev-mode magic-link → `/auth/exchange` consumed sessionStorage redirect (open-redirect guard applied) → landed on `:3002/invoices/432ae5a9-…` (the original deep-link); page rendered H1 `INV-0001`, badge `PAID`, Total `R 5 160,00`, Download PDF button (status carry-forward from cycle-34) ✓. **Step 4 PASS** (edge case): cleared all localStorage + sessionStorage + cookies → bare `/login` (no query) → form rendered with generic "DocTeams" branding (no orgId fetch); typed email + clicked Send Magic Link → inline guard alert: **"We couldn't determine which organization to sign you into. Please use the original link from your email."** No POST fired (short-circuit at `login/page.tsx:96-103` blocks before `publicFetch`); generic copy avoids tenant-existence leaks ✓. **GAP-L-66 status: FIXED → VERIFIED (PR #1188, retest cycle 37)**. Day 45 walk should proceed on a fresh `bugfix_cycle_2026-04-26-day45` branch cut from current `main` (`06d5a633`). Evidence: `qa_cycle/checkpoint-results/day-30.md §Cycle 37 Retest`; 5 cycle37 YAML snapshots (`cycle37-retest-PR1189-GAP-L-66-step{1,2,3,4}-*.yml`). Cycle Count → 37.
- 2026-04-27 SAST — QA: **Cycle 38 — Day 45 walk on bugfix_cycle_2026-04-26-day45 (cut from main `3b2eebfa`).** **5 PASS / 0 FAIL / 1 PARTIAL / 0 BLOCKED / 0 SKIPPED.** Bob (admin) drove the entire firm-side flow on Tab 0 (fresh Keycloak login). 45.1 PARTIAL because GAP-L-67 (carry-forward from cycle 1, now formally added to tracker) still blocks ad-hoc free-form info request creation — `Create Information Request` dialog has no Title/Items inputs, "Ad-hoc (no template)" is unsendable, no "Supporting medical evidence" pack template exists. Substituted FICA Onboarding Pack (3 items) per cycle-1 precedent → REQ-0004 created (`d7dc4faf-…`, SENT, due 2026-06-17 = Day 0 + 52 days). 45.2 PASS — Mailpit `cT5BtR26iNjg…` "Information request REQ-0004 from Mathebula & Partners" delivered with magic-link href `http://localhost:3002/auth/exchange?token=…&orgId=mathebula-partners` (L-42 fix HOLDS). 45.3 PASS — Record Deposit dialog filled with raw UUIDs (Sipho `c4f70d86-…`, RAF `cc390c4f-…`, R 20 000, ref DEP-2026-RAF-003, "Top-up per engagement letter") → `trust_transactions` row `177065ec-…` RECORDED. 45.4 PASS — `/trust-accounting/client-ledgers` shows Sipho R 70 100,00 (= R 50 000 Day 10 + R 100 BUG-CYCLE26-11 retest carry-forward + R 20 000 today; **scenario expects R 70 000 — the +R 100 is expected carry-forward, NOT a bug**); Sipho ledger detail `/trust-accounting/client-ledgers/c4f70d86-…` shows 3 transactions; Moroka unchanged at R 25 000 (isolation holds). 45.5 PASS — RAF matter Trust tab shows R 70 100,00 / Funds Held / Deposits R 70 100,00. **Trust-activity nudge email DID fire** (`Mz58MAYnvZ…`, "Mathebula & Partners: Trust account activity") with matter-UUID CTA `:3002/trust/cc390c4f-…` — **BUG-CYCLE26-11 fix VERIFIED-CARRY-FORWARD on `3b2eebfa`**. Email body still cosmetic-imperfect (raw ISO date, unformatted amount) — already-known carry-forward "Trust-deposit nudge email body polish" per dispatch — NOT re-logged. Console errors: 0 across all Day 45 navigation. **Zero new gaps logged.** GAP-L-67 (HIGH, OPEN) re-observed and added to formal tracker this cycle (was previously logged in cycle 1 day-45.md but never propagated to tracker). **QA Position → Day 46 — 46.1** (Sipho portal: respond to REQ-0004 + trust re-check + isolation spot-check). Recommended next branch: `bugfix_cycle_2026-04-26-day46` cut from `main` when ready. Evidence: `qa_cycle/checkpoint-results/day-45.md §Cycle 38`; 10 cycle38 YAML snapshots (`cycle38-day45-{0..9}-*.yml`). Cycle Count → 38.
- 2026-04-27 SAST — Product: **cycle-35 triage — GAP-L-66 → SPEC_READY.** Hypothesis verified at code level: (1) `portal/app/(authenticated)/layout.tsx:73-77` calls `router.replace("/login")` with no query, dropping both the deep-link target and any orgId hint when an expired session is detected; (2) `portal/app/login/page.tsx:19-20, 61-71` reads `orgId` only from `searchParams.get("orgId")` — null when `/login` has no query — and POSTs `{ email, orgId: null }` to `/portal/auth/request-link`; (3) `backend/.../portal/PortalAuthController.java:47-50` (`MagicLinkRequest.orgId @NotBlank`) returns 400 "orgId is required"; (4) the fee-note email's "View Fee Note" CTA at `InvoiceEmailService.java:80` builds `portalUrl = portalBaseUrl + "/invoices/" + invoice.getId()` with no orgId carried (intentional design — meant for already-authenticated users); (5) `portal/lib/auth.ts:48-98` auto-clears the `portal_customer` JSON (which contains `orgId`) on JWT expiry, so orgId is not recoverable from localStorage when the redirect fires. Fix scope: **frontend-only, S effort, no backend change**. Three-step plan: (a) add `portal_last_org_id` localStorage key to `portal/lib/auth.ts` — written on `storeAuth()`, **preserved across `clearAuth()`** (orgId is a public slug, not a credential); (b) `(authenticated)/layout.tsx` threads `pathname` (`?redirectTo=…`) + last-known orgId (`?orgId=…`) into the `/login` redirect; (c) `/login` page sources `orgId` as `query ?? getLastOrgId()`, shows inline guard when both null, persists `redirectTo` to `sessionStorage`. `/auth/exchange/page.tsx` consumes `sessionStorage.portal_post_login_redirect` post-exchange (with `/`-prefix open-redirect guard) so the user lands back on the original deep-link target. Backend default-org-resolution rejected (multi-tenancy leak risk — would let an attacker probe email→tenant membership). Tests: extend `portal/app/login/__tests__/page.test.tsx` (3 new cases) + `portal/lib/__tests__/auth.test.ts` (3 new cases) + `portal/app/auth/exchange/__tests__/page.test.tsx` (2 new cases). Regression risk LOW — backend contract unchanged; only same-origin paths honoured for redirectTo. **Recommended Dev dispatch: single agent, single PR (`fix/GAP-L-66`) cut from `bugfix_cycle_2026-04-26-day30-replay`** per Standing Rule "one fix per PR". Frontend-only — HMR auto-reloads, no service restart. Spec: `qa_cycle/fix-specs/GAP-L-66.md`. Cycle Count → 35.
- 2026-04-27 SAST — Product: **cycle-39 triage — GAP-L-67 → SPEC_READY.** Hypothesis verified at code level: (1) `frontend/components/information-requests/create-request-dialog.tsx:60-63, 117-124, 174-271` — dialog state holds only `templateId / portalContactId / reminderDays / dueDate`; payload omits `items`; dialog body has zero items affordance. Save-as-Draft path closes silently with no navigation; Send-Now path on a 0-item draft would hit `InformationRequestService.java:351-354` `InvalidStateException("Cannot send empty request")` and the error is swallowed at the dialog. (2) Backend ALREADY supports inline ad-hoc creation: `backend/.../dto/InformationRequestDtos.java:19-26` declares `@Valid List<AdHocItemRequest> items`; `InformationRequestService.java:147-168` routes to `createAdHoc(...)`; `createAdHoc` lines 305-317 loop the items list and persist each as a `RequestItem`. (3) Frontend API client `frontend/lib/api/information-requests.ts:141-149` already declares `items?: CreateInformationRequestItem[]` — no API-layer change needed. (4) **Important clarification**: `InformationRequest` entity has NO `name`/`title` column — the displayed identifier is `requestNumber` (REQ-NNNN). The "Title" referenced in the QA dispatch maps to "the items collectively describe the ask", NOT a request-level field. (5) Ancillary observation: PR #1156 (`9e2be451`, on main) already added `AddItemDialog` and "Send Request" button to `request-detail-client.tsx:184-193,286-295` for DRAFTs (Option B). Both work; cycle-1 + cycle-38 QA agents simply didn't navigate to the draft detail page, so the existing fix was invisible from the customer-detail UX. **Selected Option A** (collect items inline in create dialog) over Option B (re-route to detail page) because: (a) single-flow create-with-items matches the scenario's mental model and matches every other create-with-body form in the app; (b) backend already supports it — zero API churn; (c) collapses 5 hops (create empty → guess where draft went → click into it → click + Add Item ×N → click Send) into 1; (d) smaller blast radius than backfilling navigation + empty-state copy + a "send blocked: no items" toast on the customer page. Spec: `qa_cycle/fix-specs/GAP-L-67.md`. Effort M (~1.5 hr) — single file `frontend/components/information-requests/create-request-dialog.tsx`, no backend / migration. Test add: new `frontend/__tests__/create-request-dialog.test.tsx` (Vitest). Regression risk LOW: template path unchanged; AddItemDialog on detail page (Option B) unchanged. **Recommended Dev dispatch: single agent, single PR (`fix/GAP-L-67`) cut from `bugfix_cycle_2026-04-26-day45`** per Standing Rule "one fix per PR". Frontend-only — HMR auto-reloads, no svc.sh restart. Cycle Count → 39.
- 2026-04-27 SAST — Dev: **cycle-40 — GAP-L-67 → FIXED via PR #1190 (squash `2846fd1a`).** Frontend-only fix per spec Option A on `bugfix_cycle_2026-04-26-day45`. `frontend/components/information-requests/create-request-dialog.tsx` now renders an inline Items section gated on `templateId === "ad-hoc"`: header row with "Add Item" button + per-row Name (required) / Description (optional) / Response type select (FILE_UPLOAD/TEXT_RESPONSE) / Required checkbox / Trash remove button; `crypto.randomUUID()` keys for stable React rendering. State + payload wired through `CreateInformationRequestRequest.items` with `sortOrder` 0..n and trimmed names. Send Now disabled when `(isAdHoc && (items.length === 0 || any item name is blank))`; inline error otherwise. Save-as-Draft preserved with 0 items per spec. Template path byte-identical (items hidden, payload omits items field). Backend untouched — `InformationRequestService.createAdHoc` already loops items + persists. New unit test `frontend/__tests__/create-request-dialog.test.tsx` (7 cases): items section visible by default; Add/Remove rows; Send Now disabled with 0 items / blank name; payload shape (sortOrder + trimmed names + responseType propagation); template path hides section + omits items; Save-as-Draft 0-items allowed. **Build/Lint/Tests**: `pnpm run lint` clean (0 errors, 87 pre-existing warnings unrelated to this fix); `pnpm run build` green; targeted `vitest run __tests__/create-request-dialog.test.tsx` 7/7 pass; full suite 339 files / 2115 passing / 2 skipped. **CI on PR #1190**: qodana pass (6m44s), CodeRabbit skipped (intra-cycle target). Squash-merged to `bugfix_cycle_2026-04-26-day45` (cycle branch HEAD `2846fd1a`). No service restart (frontend HMR). QA retest pending at Day 45 § 45.1 to confirm Bob can author the scenario's 2-item "Supporting medical evidence" request inline without the FICA Onboarding Pack substitute. Cycle Count → 40.
- 2026-04-27 SAST — QA: Cycle 41 retest of PR #1191 (squash `79ad2602`) on `main`. **GAP-L-67 VERIFIED end-to-end, 0 REOPENED.** Bob (admin) drove the Day 45 §45.1 originally-blocked scenario flow on the live frontend (HMR — no restart). Create Information Request dialog on RAF matter `cc390c4f-…` defaulted to "Ad-hoc (no template)"; Items section visible with Add Item button + "Add at least one item before sending" hint; Send Now disabled at 0 items. Filled 2 items (row 0 "Latest specialist medical reports" — note placeholder text literally matches the scenario language; row 1 "Independent expert assessment"; both FILE_UPLOAD + required); set due_date 2026-05-11; recipient auto-selected Sipho's portal_contact `f3f74a9d-…`. Send Now enabled → click → dialog closed without error. New `information_requests` row `73babd4e-077e-4955-9b60-45abd03f0911` written to `tenant_5039f2d497cf` (REQ-0005, status=SENT, due 2026-05-11, project_id=cc390c4f, portal_contact_id=f3f74a9d); 2 `request_items` rows written ("Latest specialist medical reports" sort_order=0 + "Independent expert assessment" sort_order=1, both FILE_UPLOAD + required). Mailpit msg `QiFckxzGK9bLNEhPeEouZb` "Information request REQ-0005 from Mathebula & Partners" delivered to `sipho.portal@example.com` at 2026-04-27T15:21:17Z. The originally-blocked Day 45 §45.1 ("Supporting medical evidence" 2-item ad-hoc) now works without the FICA Onboarding Pack substitute. 0 console errors during the flow. **GAP-L-67 closed.** Day 46 walk MAY proceed (Sipho portal: respond to REQ-0004 + trust re-check + isolation; REQ-0005 also pending response if scenario wants double-coverage). Cut fresh `bugfix_cycle_2026-04-26-day46` branch from `main` when ready. Cycle Count → 41.
- 2026-04-27 SAST — QA: **Cycle 42 — Day 46 walk on `bugfix_cycle_2026-04-26-day46` (cut from main `f227fd3e`).** **5 PASS / 1 FAIL / 0 PARTIAL / 0 BLOCKED / 0 SKIPPED.** Sipho (portal contact) drove the entire portal-side flow via Playwright MCP. 46.1 PASS — magic-link from REQ-0005 Mailpit msg `QiFckxzGK9bLNEhPeEouZb` exchanged cleanly to `:3002/projects` → header "Sipho Dlamini". 46.2 PASS — `/home → /requests → REQ-0005 detail`; portal `/requests` rows display REQ-number + matter name + counter but NOT request `name` (cosmetic OBS-cycle42-portal-request-title-missing, same as cycle 1). 46.3 PASS — uploaded both items via presigned-URL flow ("Latest specialist medical reports" → doc `2a3d25e1-…` SUBMITTED 15:29:31Z, "Independent expert assessment" → doc `0d6c8cf6-…` SUBMITTED 15:30:09Z); parent REQ-0005 transitioned SENT → IN_PROGRESS (L-43 + L-47 verified). 46.4 PASS — `/trust` redirected to `/trust/cc390c4f-…` (BUG-CYCLE26-11 carry-forward holds), balance R 70 100,00. 46.5 PASS — Transactions table shows 3 deposits (Day 10 R 50 000 + cycle-29 retest R 100 + Day 45 R 20 000); running balance correct bottom-up; portal-side ordering BETTER than firm-side cosmetic gotcha. 46.6 PASS — Cross-customer isolation HOLDS at all surfaces; `/projects` shows only RAF, `/requests` no REQ-0003 (Moroka), `/trust` no R 25 000 leak. **46.7 FAIL — NEW GAP-L-92 MEDIUM**: portal home "Pending info requests" tile remained at 3 after Sipho submitted 2/2 REQ-0005 items (scenario expects drop to 2). Source: `portal/app/(authenticated)/home/page.tsx:67` filter `data.filter((r) => r.status !== "COMPLETED").length` — IN_PROGRESS counts as pending. Cycle 1 had logged this as `OBS-Day46-PendingTileSemantics` and deferred to Product; cycle 42 elevates to MEDIUM gap. 0 console errors. **Cycle-1 GAP-L-68 (`/portal/trust/movements?limit=1` 404) VERIFIED-FIXED on `f227fd3e`** — endpoint resolves cleanly; "Last trust movement" tile renders R 20 000,00 / 27 Apr 2026. **GAP-L-67 also VERIFIED end-to-end** via cycle-41 retest + cycle-42 portal-side response (full ad-hoc create→deliver→portal-respond loop closed). GAP-L-92 is non-cascading (cosmetic count, no data integrity issue, no isolation break). **QA Position → Day 60 — 60.1** (firm matter closure + Statement of Account; Days 47–59 are empty in scenario). Recommended next branch: `bugfix_cycle_2026-04-26-day60` cut from `main` when ready (orchestrator may opt to fix GAP-L-92 first — fix scope S ~30 min). Evidence: `qa_cycle/checkpoint-results/day-46.md §Day 46 Re-walk — Cycle 42`; 11 cycle42 YAML snapshots `cycle42-day46-{1..11}-*.yml`. Cycle Count → 42.
- 2026-04-27 SAST — Product: **cycle-43 triage — GAP-L-92 → SPEC_READY.** Hypothesis verified at code level: `portal/app/(authenticated)/home/page.tsx:67` reads `data.filter((r) => r.status !== "COMPLETED").length` against the local `InfoRequestSummary` interface (lines 22-25) which only declares `id` + `status`. Backend `GET /portal/requests` (verified at `backend/.../PortalInformationRequestController.java:140-151` `PortalRequestListResponse` record) ALREADY returns `totalItems` + `submittedItems` per row — same fields the requests-list page consumes at `portal/app/(authenticated)/requests/page.tsx:9-21`. **No backend change needed.** Fix shape: extend the local interface with `totalItems`/`submittedItems` and replace the filter with `r.status !== "COMPLETED" && r.submittedItems < r.totalItems`. Naturally excludes (a) COMPLETED, (b) IN_PROGRESS where every item is SUBMITTED (the GAP-L-92 case — Sipho is "done on his side", waiting on firm review), and keeps SENT + partially-submitted IN_PROGRESS in the count. Cycle-42 evidence corroborates: `cycle42-day46-11-portal-requests-after.yml` shows REQ-0005 IN_PROGRESS 2/2, REQ-0004 SENT 0/3, REQ-0002 COMPLETED 0/3, REQ-0001 SENT 0/3 — old filter returns 3, new filter returns 2 (REQ-0004 + REQ-0001 — exactly the requests with portal-user work remaining). Rejected alternatives: (a) introduce AWAITING_REVIEW pseudo-state (backend churn for cosmetic count); (b) rename tile copy to "Open info requests" (still wrong — SENT 0/3 is "open" and *should* count). Effort **S (~30 min)**. Frontend-only — HMR auto-reloads, no svc.sh restart. Tests: new `portal/app/(authenticated)/home/__tests__/page.test.tsx` (Vitest, 7 cases — empty list / SENT-only / partial IN_PROGRESS / fully-submitted IN_PROGRESS regression / COMPLETED + partial / fully-submitted + COMPLETED / network error). No backend test churn. Regression risk LOW: single component, single filter expression; AcceptancesCard / DeadlinesCard / TrustCard / RecentInvoicesCard all hit different endpoints with different filter shapes. Spec: `qa_cycle/fix-specs/GAP-L-92.md`. **Recommended Dev dispatch: single agent, single PR (`fix/GAP-L-92`) cut from `bugfix_cycle_2026-04-26-day46`** per Standing Rule "one fix per PR". Cycle Count → 43.
- 2026-04-27 SAST — Dev: **cycle-44 — GAP-L-92 → FIXED via PR #1192 (squash `94f77583`).** Frontend-only fix per spec on `bugfix_cycle_2026-04-26-day46`. `portal/app/(authenticated)/home/page.tsx`: extended local `InfoRequestSummary` interface with `totalItems: number` + `submittedItems: number` (matching what backend `PortalRequestListResponse` already returns and what `portal/app/(authenticated)/requests/page.tsx:9-21` already consumes); replaced `InfoRequestsCard` filter `r.status !== "COMPLETED"` with `r.status !== "COMPLETED" && r.submittedItems < r.totalItems`. New unit test `portal/app/(authenticated)/home/__tests__/page.test.tsx` (Vitest, 7 cases per spec): empty list → 0; SENT 0/3 → 1; IN_PROGRESS 2/3 partial → 1; IN_PROGRESS 3/3 fully-submitted → 0 (the regression case); COMPLETED-only → 0; Day-46 cycle-42 mixed scenario (REQ-0001 SENT 0/3 + REQ-0004 SENT 0/3 + REQ-0005 IN_PROGRESS 2/2 + REQ-0002 COMPLETED 0/3) → 2; network error → 0. Mocks `portalGet` from `@/lib/api-client` and `useModules` from `@/hooks/use-portal-context` (returns `["information_requests"]` so only `InfoRequestsCard` renders for focused assertion); `afterEach(() => cleanup())` per CLAUDE.md guidance. **Build/Lint/Tests**: `pnpm run lint` clean (0 errors, 5 pre-existing warnings unrelated to this fix); `pnpm run build` green (15 routes); targeted home page test 7/7 pass; full portal suite **184/184 pass across 41 files**. **CI on PR #1192**: qodana pass (6m25s), CodeRabbit skipped (intra-cycle target). Squash-merged to `bugfix_cycle_2026-04-26-day46` (cycle branch HEAD `94f77583`). No service restart (frontend HMR). QA retest pending at Day 46 §46.7 to confirm tile drops 3→2 after Sipho submits the last REQ-0005 item. Cycle Count → 44.
- 2026-04-27 SAST — QA: **Cycle 45 retest of PR #1193 (squash `6c30e247`) on `main`. GAP-L-92 → VERIFIED, 0 REOPENED.** Sipho (portal contact) drove the verification via Playwright MCP on `main` HEAD `6c30e247` (frontend HMR — no svc.sh restart). Note PR #1193 includes the CR follow-up commit `65e5fd76` which tightened the filter from the spec's negated form `r.status !== "COMPLETED" && r.submittedItems < r.totalItems` to the explicit positive allow-list `(r.status === "SENT" \|\| r.status === "IN_PROGRESS") && r.submittedItems < r.totalItems` — also excludes CANCELLED (regression-guarded by an 8th unit-test case; **185/185 portal tests pass**). **Auth**: fresh magic-link via `POST :8080/portal/auth/request-link` for `sipho.portal@example.com` + orgId `mathebula-partners` → token exchange at `:3002/auth/exchange?token=…` → header "Sipho Dlamini"; Mailpit message `88oo8hqK9MMRSxZh4StdNA` corroborates. **Step 1 — `/home` tile**: "Pending info requests" reads "**2**" (vs cycle-42's broken "3"). **Step 2 — `/requests` enumeration**: 4 rows visible to Sipho (REQ-0003 Moroka correctly invisible per cross-customer isolation): REQ-0005 IN_PROGRESS 2/2 (excluded — the regression case from cycle-42; both items SUBMITTED, no portal-user work remaining), REQ-0004 SENT 0/3 (counts), REQ-0002 COMPLETED 0/3 (excluded), REQ-0001 SENT 0/3 (counts). **Step 3 — expected-vs-actual**: new positive-allow-list filter returns 2 (REQ-0001 + REQ-0004); home tile reads "2"; **MATCH**. Scenario §46.7 expectation now satisfied. 0 console errors during the flow. **Day 60 walk now unblocked** — caveat cleared from QA Position. Cut fresh `bugfix_cycle_2026-04-26-day60` from `main` (HEAD `6c30e247`) for the next per-day cycle (firm matter closure + Statement of Account). Evidence: `qa_cycle/checkpoint-results/day-46.md §Cycle 45 Retest`; `cycle45-retest-PR1193-GAP-L-92-portal-home.yml`, `cycle45-retest-PR1193-GAP-L-92-portal-requests.yml`, `cycle45-retest-PR1193-GAP-L-92-home-tile.png`, `cycle45-retest-PR1193-GAP-L-92-requests-list.png`. Cycle Count → 45.
- 2026-04-27 SAST — QA: **Cycle 46 — Day 60 walk on `bugfix_cycle_2026-04-26-day60` (cut from main `18365feb`).** **4 PASS / 1 FAIL / 4 PARTIAL / 0 BLOCKED / 2 SKIPPED.** Thandi (Owner) drove the firm-side closure flow via Playwright MCP on RAF matter `cc390c4f-…`. **60.1–60.3 SKIPPED** (no eligible unbilled work — INV-0001 already covers all 4h of time + R 1 250 disbursement; the scenario's R 15 000 narrative is hypothetical and cannot be exercised without injecting fake unbilled time; "No customers with unbilled work found for this period" returned by billing-runs-new wizard). **60.4 PASS** — Close Matter button opens "Close matter" Step-1 dialog. **60.5 FAIL** — gate report shows 4 of 9 gates FAIL: trust balance R 70 100 (not zero/earmarked), 1 future court date, 9 open tasks, 3 outstanding info requests; happy-path expectation broken (scenario expected all GREEN). Continue still enabled — gates surface a warning, not a hard block. **60.6 PASS** — Step-2 form renders Reason combobox (defaulted CONCLUDED), Notes textbox, Generate-closure-letter checkbox (checked), Override-failing-gates toggle. **60.7 PASS** — CONCLUDED reason persisted to `matter_closure_log.reason`. **60.8 PARTIAL** — closure letter checkbox present but **no Statement-of-Account checkbox in the dialog**; SoA is a separate top-bar action (logged as **GAP-L-93 LOW**). **60.9 PASS** — Override toggled with ≥20-char justification supplied → close confirmed → `projects.status=CLOSED`, `closed_at=2026-04-27 16:56:04+00`, `matter_closure_log` row `eb934998-…` written with `override_used=t` + `closure_letter_document_id=c582a54f-…`. **60.10 PARTIAL** — Closure letter (1 638 bytes) auto-attached on close; SoA (4 100 bytes) generated via separate `Generate Statement of Account` flow with period 2026-04-01→2026-06-30, both visible in Documents tab + Statements tab. SoA preview iframe rendered Section 86 ledger structure correctly but **trust-activity tables empty + VAT line zero** despite 3 RECORDED deposits totalling R 70 100 and 15% VAT correctly computed on the same fees on INV-0001. Logged as **GAP-L-94 HIGH** (Section 86/LPC compliance broken — SoA cannot be sent to client) + **GAP-L-95 BUG** (VAT R 0 vs expected R 510, leaks into "-R 510,00" closing balance owing). **60.11 PARTIAL** — `projects.retention_clock_started_at` stamped on closure but no `retention_policies` MATTER row inserted; `org_settings.legal_matter_retention_years` is NULL. Logged as **GAP-L-96 LOW** (ADR-249 compliance gap). **Mailpit supplementary FAIL** — no closure or SoA email sent to Sipho (`/api/v1/search?query=closure` 0 hits; `query=statement+of+account` 0 hits). Logged as **GAP-L-97 LOW**. 0 console errors during closure + SoA flow. **5 NEW gaps total** (1 HIGH + 1 BUG + 3 LOW); none cascading for Day 61 (Sipho can self-serve SoA from portal even without the email kick-off). **QA Position → Day 61 — 61.1** (portal-side SoA download). Recommended next: triage GAP-L-94 + GAP-L-95 before Day 61 walk (both affect what Sipho will see in the SoA PDF) OR proceed to Day 61 first to capture portal-side rendering with the current SoA bytes. Evidence: `qa_cycle/checkpoint-results/day-60.md §Day 60 Cycle 46 Walk`; 26 cycle46 YAML snapshots `cycle46-day60-{1..26}-*.yml`. Cycle Count → 46.
- 2026-04-27 SAST — Product: **Cycle 47 — Triaged 5 Day-60 OPEN gaps → SPEC_READY (3 this-cycle, 2 deferred).** All hypotheses verified at code level on `bugfix_cycle_2026-04-26-day60` (HEAD `8d72256b`). **GAP-L-94 (HIGH, M, this-cycle)**: `StatementOfAccountContextBuilder.buildTrustBlock` resolves trust account via `findByAccountTypeAndPrimaryTrue(GENERAL)`, which can return a different id than where the deposits were actually recorded; spec includes 3 diagnostic SQLs to differentiate (non-primary trust account / NULL customer_id / multi-account customer) and the surgical fix (new repo method `findDistinctTrustAccountIdsByCustomerId` with primary-GENERAL fallback). **GAP-L-95 (BUG, S, this-cycle)**: `StatementOfAccountContextBuilder.aggregate:246` hard-codes `BigDecimal vatAmount = BigDecimal.ZERO; // line-level VAT not modelled on TimeEntry yet` — leftover TODO. Fix injects `OrgSettingsService` and computes `totalFeesExcl × vatRate / 100` when `vatRegistered=true`. **GAP-L-97 (LOW, S, this-cycle)**: NOT a missing-listener problem — `PortalDocumentNotificationHandler` already listens on `DocumentGeneratedEvent`, allowlist `["matter-closure-letter","statement-of-account"]` (slugs) is seeded by V117. Bug is `GeneratedDocumentService:220` + `StatementService:237` publish `templateName = template.name()` (display name "Matter Closure Letter") instead of `template.slug()`. 2-line fix swaps `name()` → `slug()`; Javadoc clarifies the field semantics. **GAP-L-93 (LOW, S, deferred to cycle 48)**: closure dialog needs second `generateStatementOfAccount` checkbox + DTO + orchestration. Spec ready; defer because Day 61 unblocks via existing standalone SoA button. **GAP-L-96 (LOW, S, deferred to cycle 48)**: `MatterClosureService.close` lines 174-176 carry `TODO(489C)` to insert `retention_policies` MATTER row; spec ships idempotent seed via existing `RetentionPolicyService.create`. Deferred because retention sweeper doesn't act on MATTER yet — row is inert until then. **Total this-cycle effort estimate: ~7 hours (M+S+S backend-only on a single restart)**. **Recommended Dev dispatch: bundle L-94 + L-95 + L-97 in one PR** (`fix/GAP-L-94-95-97-soa-trust-vat-email` — all touch the SoA generation path; one backend restart + one verification pass; mutual diagnostic value because Day 61 walk needs all three to demonstrate end-to-end). Specs: `qa_cycle/fix-specs/GAP-L-{93,94,95,96,97}.md`. Cycle Count → 47.
- 2026-04-27 SAST — Dev: **Cycle 48 — GAP-L-95 FIXED (PR #1194, squash `746d95e1`).** Root cause confirmed: `StatementOfAccountContextBuilder.aggregate()` hard-coded `vatAmount=BigDecimal.ZERO` with a leftover TODO. Fix injects `TaxRateRepository` and derives VAT from the org's default tax rate (`findByIsDefaultTrue()`) — same pattern `InvoiceTaxService` uses. New `computeFeesVat()` helper falls back to ZERO when no default rate, exempt, inactive, or rate is null/zero (preserves non-VAT-registered tenant behaviour). **Spec deviation noted in PR**: GAP-L-95.md proposed `OrgSettings.isVatRegistered()`/`OrgSettings.getVatRate()`; those methods do not exist on `OrgSettings`. The actual codebase models VAT via the standalone `TaxRate` entity with an `isDefault` flag. Net rendered effect on the SoA matches the spec (R 510 VAT on R 3 400 fees = R 3 910 incl). 3 new tests added: `aggregate_appliesVatWhenDefaultTaxRateConfigured`, `aggregate_skipsVatWhenNoDefaultRateConfigured`, `aggregate_skipsVatWhenDefaultRateIsExempt`. 23/23 tests pass across `StatementOfAccountContextBuilderTest` (17), `StatementControllerIntegrationTest` (4), `StatementServicePortalDocumentTest` (2). Backend restart required after all 3 bundle PRs merge. Cycle Count → 48.
- 2026-04-27 SAST — Dev: **Cycle 49 — GAP-L-97 FIXED (PR #1195, squash `f4366b44`).** Two-line slug-vs-name swap at the publish boundary: `GeneratedDocumentService:220` + `StatementService:237` now pass `template.getSlug()` (was `template.getName()`) into `DocumentGeneratedEvent.templateName`, matching the slug allowlist semantics in `OrgSettings.portalNotificationDocTypes`. Display name preserved in `details.template_name` for activity-feed UX. Added Javadoc on `DocumentGeneratedEvent.templateName` field with explicit GAP-L-97 reference so future publishers don't repeat the bug. New regression test `PortalDocumentNotificationHandlerIntegrationTest.displayNamePublishedAsTemplateName_skipsEmail` publishes `templateName="Statement of Account"` (display name) and asserts NO email is dispatched — guards the publisher contract from below. Strengthened `StatementServicePortalDocumentTest` to assert `templateName` equals `"statement-of-account"` exactly (was `isNotBlank()`). 9 targeted tests pass; broader doc-gen sweep (19 tests) all green. Cycle Count → 49.
- 2026-04-27 SAST — Dev: **Cycle 50 — GAP-L-94 FIXED (PR #1196, squash `08e8e68d`).** Replaced the tenant-wide primary-GENERAL trust account resolver with a customer-aware lookup. New repo query `TrustTransactionRepository.findDistinctTrustAccountIdsByCustomerId` returns the trust accounts where the customer has any RECORDED/APPROVED activity (REVERSAL excluded; read-only, no impact on write paths). `StatementOfAccountContextBuilder.buildTrustBlock` now resolves trust account in three branches: 0 ids → empty (preserved no-activity behaviour); 1 id → use it directly (covers the common Section-86 single-account shape including non-primary or non-GENERAL accounts); N ids → prefer primary GENERAL when customer has activity on it, else fall back to first id (with WARN log so the unusual operational shape is observable). Summary `trust_balance_held` derives from same TrustBlock so it picks up the fix automatically. **Spec deviation noted in PR**: diagnostic SQLs were not run (the dev agent has no live-DB access from worktree); the proposed fix is independently safe across hypotheses A (non-primary trust), B (NULL customer_id — would still be invisible per the customerId filter), and C (multi-account customer). If Day-61 verification reveals NULL customer_id rows, that is a separate data-integrity issue to open as a new gap (the spec recommends GAP-L-98) — this fix should NOT be reverted; it remains correct for the populated-customer-id case. 4 new GAP-L-94 tests + existing tests rewired to the new entry-point; 88 targeted tests pass across `StatementOfAccountContextBuilderTest` (21), `StatementControllerIntegrationTest` (4), `StatementServicePortalDocumentTest` (2), `PortalDocumentNotificationHandlerIntegrationTest` (7), `TrustTransaction*` (54). Backend restart required after merge. **All 3 cycle-47 specs (L-95, L-97, L-94) now FIXED.** Cycle Count → 50.
- 2026-04-28 SAST — QA: **Cycle 51 retest of PR #1197 (squash `0e9df9f4`) on `main`. GAP-L-94 + GAP-L-95 + GAP-L-97 → ALL VERIFIED, 0 REOPENED.** Backend restarted via `svc.sh restart backend` (the 8080 instance was started before the merge — start-time 20:25 vs merge 23:43); fresh PID picked up `0e9df9f4` code. Bob (admin) drove the verification via Playwright MCP at `:3000` → Keycloak login → RAF matter `cc390c4f-…` → "Generate Statement of Account" toolbar action → period 2026-04-01 → 2026-04-30 → Preview & Save. Mailpit cleared via `DELETE /api/v1/messages` immediately before the trigger. **GAP-L-95 (VAT) — PASS**: SoA preview iframe shows `Total fees (excl. VAT): 3400.00 / VAT: 510.00 / Total fees (incl. VAT): 3910.00` (was R 0,00 in cycle 46). Stored `generated_documents.context_snapshot.summary.total_fees = "3910.00"` confirms the VAT-inclusive total persisted. 15% rate sourced from `tax_rates.is_default=true` per the `TaxRateRepository.findByIsDefaultTrue()` fix. **GAP-L-94 (Trust Activity) — PASS**: SoA Trust Activity section is now populated — 3 deposits visible (DEP/2026/RAF-001 R 50 000,00 / DEP/2026/RAF-002 R 100,00 / DEP-2026-RAF-003 R 20 000,00) with `Closing balance: 70100.00` and `Trust balance held: 70100.00` in summary (was empty in cycle 46). The customer-aware `findDistinctTrustAccountIdsByCustomerId` resolver correctly picked up trust account `46d1177a-…` for Sipho on `cc390c4f-…`. **GAP-L-97 (email) — PASS**: Mailpit message `o7q6xXpr97YPC8czLw544N` arrived at `sipho.portal@example.com` 1.4 s after SoA save, subject `"Document ready: statement-of-account-dlamini-v-road-accident-fund-2026-04-30.pdf from Mathebula & Partners"` (was 0 hits in cycle 46). Backend log corroborates: `PortalEmailService` line at `2026-04-27T22:01:04.158015Z` — `Portal notification sent template=portal-document-ready contact=f3f74a9d-… to=sipho.portal@example.com`. Slug-based allowlist lookup is now matching the publisher (`statement-of-account` slug → `portal-document-ready` template). Note: `email_delivery_log` did not get a `PORTAL_DOCUMENT` row — `PortalEmailService` writes a backend log line on send but does not currently persist to that table; the email actually landed in Mailpit which proves the handler fired (separate observation, not a regression of this fix). 0 console errors during the flow. **Outcome: ALL THREE VERIFIED.** No new gaps opened from this retest. **QA Position holds at Day 61 — 61.1** (portal-side SoA download — now unblocked by L-94/95/97 fixes; Sipho can fetch a correct, VAT-bearing, Section-86-compliant SoA from the portal email link). Evidence: `qa_cycle/checkpoint-results/day-60.md §Cycle 51 Retest`; `qa_cycle/evidence/cycle51-retest-PR1197-soa-context.json`, `cycle51-retest-PR1197-soa-preview.png`, `cycle51-retest-PR1197-soa-email-full.json`, `cycle51-retest-PR1197-soa-email-body.html`. Cycle Count → 51.
- 2026-04-28 SAST — Orchestrator: **SESSION PAUSE — handoff to next session.** All in-flight work landed on `main` (HEAD `d8a88315`). Cycle branches deleted on merge. Tracker is consistent — every gap is in a terminal state for this pause point: VERIFIED (L-94/95/97 + earlier), SPEC_READY-DEFERRED (L-93/96), or RESOLVED/WONT_FIX (others). **Resume sequence for the next session**: (1) `git checkout main && git pull origin main` — confirm HEAD `d8a88315` or later; (2) `bash compose/scripts/svc.sh status` — start any service that's down; (3) `git checkout -b bugfix_cycle_2026-04-26-day61 main && git push -u origin bugfix_cycle_2026-04-26-day61`; (4) update this status.md `Branch` field to the new branch name; (5) dispatch QA agent for Day 61 walk per scenario `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`. Day 61 is portal-side: Sipho receives the SoA email (now wired via L-97 fix), follows portal CTA, downloads + verifies the SoA PDF rendering. Per-day workflow Step 5 retest gate is satisfied for this session's fixes (cycle 51 above). Optional follow-up before Day 61: revisit GAP-L-93 + GAP-L-96 deferrals (~3-4hr combined) and a small observability follow-up — `email_delivery_log` does not get a `PORTAL_DOCUMENT` row when `PortalEmailService` sends, even though Mailpit shows the email; not a regression of L-97, separate hygiene item.

- 2026-04-28 SAST — QA: **Cycle 52 — Day 61 walk attempt on `bugfix_cycle_2026-04-26-day61` (cut from main `d8a88315`). BLOCKED — INFRA — GAP-L-98 logged.** Pre-flight setup completed via REST + DB read-only: cycle-51 SoA email `o7q6xXpr97YPC8czLw544N` confirmed in Mailpit with `View document` CTA targeting `http://localhost:3002/projects/cc390c4f-35e2-42b5-8b54-bac766673ae7` (matter detail — matches scenario §61.1 expected landing); fresh portal magic-link issued (`POST /portal/auth/request-link` HTTP 200; Mailpit `EfEPNBzqR4TE2tEyDTKrJp` arrived at 06:00:12.908Z); SoA `generated_documents.6b79c496-…` (4863 B, s3_key `org/tenant_5039f2d497cf/generated/statement-of-account-…-2026-04-30.pdf`) and closure letter `c582a54f-…` (1644 B) both verified persisted on matter `cc390c4f-…`. Browser-driven walk could not start: `mcp__playwright__browser_navigate` returns `Browser is already in use for /Users/rakheendama/Library/Caches/ms-playwright/mcp-chrome-5d273ba` because Chromium PID 54166 (etime 08:12+, started 23:48 prev day, holding `SingletonLock`, remote-debug port 61789 with 4 active tabs incl. `localhost:3000/org/mathebula-partners/projects/cc390c4f-…`) belongs to another long-running `claude --chrome` session (4 candidate parents alive — claude PIDs 4237/28124/69590/73865). `mcp__claude-in-chrome__*` also unavailable (extension not connected). Per dispatch rule, did not kill any process; logged as **GAP-L-98 (INFRA-BLOCKER, OPEN, owner=infra)**. 0 PASS / 0 FAIL / 9 BLOCKED on Day 61 §61.1–61.9. Evidence: `qa_cycle/checkpoint-results/day-61.md §Cycle 52 Walk`; `cycle52-day61-prereq-mailpit-soa-email.json`; `cycle52-day61-prereq-mailpit-magic-link.json`; `cycle52-day61-prereq-generated-documents.txt`. **Resume action**: dispatch Infra agent for GAP-L-98 (free playwright MCP browser); upon resolution re-dispatch QA on this same `bugfix_cycle_2026-04-26-day61` branch (no rebase needed — pre-flight artefacts above are still valid). Cycle Count → 52.

### 2026-04-28 — Cycle 52 — Day 61 walk (retry after GAP-L-98 unblock)

- 2026-04-28 SAST — QA: **Cycle 52 continuation — Day 61 §61.1–§61.9 walk on `bugfix_cycle_2026-04-26-day61`. 9 PASS / 0 FAIL / 0 BLOCKED. GAP-L-98 RESOLVED.** Orchestrator's intervention before this dispatch (closed foreign claude sessions + cleared stale `SingletonLock` symlink) unblocked Playwright MCP — first `browser_navigate http://localhost:3000/dashboard` returned 200 immediately, no `--isolated` flag needed. Walk: (61.1) Sipho self-service magic-link via `POST /portal/auth/request-link` → `/auth/exchange?token=NZnYJu…&orgId=mathebula-partners` → `/projects` → navigated to email CTA `http://localhost:3002/projects/cc390c4f-35e2-42b5-8b54-bac766673ae7` → matter detail rendered with status badge **CLOSED** (L-73 still verified). (61.2) Documents tab lists 9 files including target SoA `statement-of-account-dlamini-v-road-accident-fund-2026-04-30.pdf` (4.7 KB) + closure letter (1.6 KB). (61.3) Click Download SoA → presigned LocalStack URL → curl HTTP 200 + 4863 bytes + valid PDF v1.6. (61.4) PDF FlateDecode streams decompressed + content reconciled: Mathebula & Partners letterhead + RAF-2026-001 + Sipho address + 2 LSSA-tariff fee lines + **VAT 510.00 (GAP-L-95 fix verified)** + Total fees incl VAT 3910.00 + Disbursements 1250.00 + Trust Activity 3 deposits + **Closing balance 70100.00 + Trust balance held 70100.00 (GAP-L-94 fix verified)**. (61.5) Full-page screenshot captured. (61.6) Byte-size exact match (4863 == DB `documents.ca75d3c1-…` size). (61.7) No "Untitled" leak — title matches firm copy. (61.8) Closure letter download HTTP 200 + 1644 bytes + valid PDF (exact match firm DB `documents.85c501aa-…`). (61.9) Audit log query confirms 2× `portal.document.downloaded` events (`actor_type=PORTAL_CONTACT, source=PORTAL`, occurred_at 08:36:15 + 08:36:34) — **OBS-Day61-NoPortalDocAuditEvent CLOSED** (cycle-1 observation now resolved by current build; portal-side document audit emission is working). 0 console errors over the entire portal session. **No new gaps opened.** Day 61 has no product fixes required — branch ready to PR to main with QA-only changes (results doc + status update). **QA Position advances to Day 62 — 62.1** (ready to walk on a fresh `bugfix_cycle_2026-04-26-day62` branch cut from main HEAD after Day 61 PR merges to main). Evidence: `qa_cycle/checkpoint-results/day-61.md §Cycle 52 Walk (continuation after GAP-L-98 unblock)`; `cycle52-day61-61.1-soa-email.json`; `cycle52-day61-61.1-portal-matter-detail.yml`; `cycle52-day61-61.3-portal-soa.pdf`; `cycle52-day61-61.5-portal-soa-download.png`; `cycle52-day61-61.8-portal-closure.pdf`; `cycle52-day61-console-errors.log`. Cycle Count holds at 52.

### 2026-04-28 — Cycle 53 — Day 62 walk

- 2026-04-28 SAST — QA: **Cycle 53 — Day 62 SKIPPED (NOT A SCRIPTED DAY).** On fresh `bugfix_cycle_2026-04-26-day62` branch cut from `main 52e13722` (PR #1198 Day 61 walk). Verified Day 62 is not a heading in `qa/testplan/demos/legal-za-full-lifecycle-keycloak.md`: `grep -n "^## Day"` enumerates Day 0, 1, 2, 3, 4, 5, 7, 8, 10, 11, 14, 15, 21, 28, 30, 45, 46, 60, 61, 75, 85, 88, 90 — script jumps from Day 61 (line 691, "Sipho downloads Statement of Account from portal") directly to Day 75 (line 721, "Weekly digest + late-cycle isolation spot-check"). HANDOFF.md `## Scripted days remaining` (line 73) corroborates: `Day 21, 28, 30, 45, 46, 60, 61, 75, 85, 88, 90` — Day 62 was never a scripted day; the prior Cycle-52 Log entry's "QA Position advances to Day 62" was a forward-pointer assumption that turned out to overshoot the script. No browser-driven walk attempted; no Playwright MCP usage; no new GAPs opened; no `qa_cycle/checkpoint-results/day-62.md` written (would be empty). 0 PASS / 0 FAIL / 0 BLOCKED — cycle 53 is a no-op QA bookkeeping cycle. **QA Position advances to Day 75 — 75.1** (Mailpit weekly digest for `sipho.portal@example.com` + portal activity-trail isolation spot-check, scenario lines 721–741) on a fresh `bugfix_cycle_2026-04-26-day75` branch cut from `main` HEAD after this skip-note PR lands. Evidence: this Log entry is the sole evidence — the absence of a Day 62 section in the scenario file is the artefact. Cycle Count → 53.

### 2026-04-28 — Cycle 54 — Day 75 walk

- 2026-04-28 SAST — QA: **Cycle 54 — Day 75 walk on `bugfix_cycle_2026-04-26-day75` (cut from main `c419f2c7`, PR #1199 Day 62 skip-note).** **4 PASS / 1 PARTIAL / 4 BLOCKED / 1 N/A.** Sipho (portal) drove the walk via Playwright MCP on backend PID 53170 (uptime 12h 11m, fresh JVM, serving main `c419f2c7`). Authenticated via fresh magic-link Mailpit `GfcMUMJGdRkb4jv7U9Zjh3` (2026-04-28T09:56:12Z) → `/auth/exchange` → landed `/projects`. **75.1–75.4 BLOCKED** on **GAP-L-99 (NEW, MEDIUM)**: weekly-digest scheduler is cron-only (`0 0 8 ? * MON`), no public REST trigger or admin UI. Backend started Mon 27th 23:40 SAST (AFTER the Mon 08:00 cron window), so the digest has not fired during this backend's life. Mailpit confirms zero digest emails ever delivered (`subject:digest`, `subject:weekly`, `subject:week` all 0 hits; to-Sipho mailbox total = 3 emails: 2 magic-link + 1 Day 60 SoA notification). **75.5 PASS** — `/activity` Your-actions tab + Firm-actions tab both render Sipho/firm events with **zero Moroka references** (`grep -ic 'moroka|EST-2026|0cb199f2|340c5bb2'` on both YAMLs → 0). Cosmetic note: many activity rows render raw event keys (`portal.request_item.submitted`, `statement.generated`, etc.) — outside Day 75 scope. **75.6a PASS** — `/home` tiles all RAF-only (Pending info requests = 2; Last trust movement R 20 000,00 27 Apr; INV-0001 R 5 160). **75.6b PASS** — `/trust` (auto-redirected to `/trust/cc390c4f-…`) shows balance R 70 100,00 across 3 RAF deposits; **NO R 25 000 Moroka leak** despite Moroka deposit existing in DB on a different project_id+customer_id. **75.6c PARTIAL** — `/projects` shows 2 matters (Sipho's RAF CLOSED + Sipho's residual `Cycle19 Verify` ACTIVE from BUG-CYCLE26-07 verification artefact); Past tab correctly shows RAF only; Active tab shows Cycle19 Verify only; Moroka matter `Estate Late Peter Moroka` correctly invisible — isolation holds. The "one matter only" scenario expectation is violated by the residual test-data matter, NOT by isolation failure. **75.7 N/A** (digest portion BLOCKED; activity portion captured via YAML evidence). Console errors: 1× favicon 404 on `/auth/exchange` (cosmetic); zero on `/projects`, `/home`, `/trust`, `/trust/cc390c4f-…`, `/activity`. **Anomalies**: (a) Day 60 fee-note paid R 15 000 was not recorded as a FEE_TRANSFER in `trust_transactions` — portal trust shows 3-deposit sum R 70 100 rather than R 55 100 residual implied by the closure SoA narrative (pre-existing Day 60 concern, not Day 75). (b) `Cycle19 Verify` residual matter is QA-test-data hygiene, not a product defect. **Decision**: Day 75 isolation portion COMPLETE; digest portion DEFERRED awaiting GAP-L-99 fix. **No advancement to Day 85** until GAP-L-99 is triaged + (if accepted) fixed and digest portion re-walked. Evidence: `qa_cycle/checkpoint-results/day-75.md §Day 75 Walk — Cycle 54`; `cycle54-day75-75.5-portal-activity.yml`, `cycle54-day75-75.5b-portal-activity-firm.yml`, `cycle54-day75-75.6a-portal-projects.yml`, `cycle54-day75-75.6b-portal-home.yml`, `cycle54-day75-75.6c-portal-trust.yml`, `cycle54-day75-75.6d-portal-projects-past-tab.yml`, `cycle54-day75-75.6e-portal-projects-active-tab.yml`. Cycle Count → 54.

### 2026-04-28 — Cycle 54 — Product: triaged GAP-L-99 → SPEC_READY (manual digest trigger)

- 2026-04-28 SAST — Product: triaged GAP-L-99 → SPEC_READY. Confirmed real product gap (not tooling): `PortalDigestScheduler.scheduledRun()` is the only path into `runWeeklyDigest()`, gated solely on `@Scheduled(cron="0 0 8 ? * MON")`. Cycle-1 informational `OBS-Day75-NoManualDigestTrigger` recurs every cycle and now blocks 4 of 9 Day 75 checkpoints — escalation to tracked GAP justified. Spec at `qa_cycle/fix-specs/GAP-L-99.md`. **Decision**: backend-only — add `POST /internal/portal/digest/run-weekly` (mirrors existing `/internal/audit-events` pattern, gated by existing `ApiKeyAuthFilter` via `X-API-KEY` — no new key, no new filter). Optional `orgId` (single-tenant scope), `targetEmail` (single-recipient QA send), `dryRun` (count without sending) query params. Refactor `runWeeklyDigest()` to add an overload accepting `RunOptions` and returning `RunResult` records; preserve no-arg overload so cron path + existing `PortalDigestSchedulerIntegrationTest` stay byte-identical. Test class `PortalDigestInternalControllerTest` covers 6 cases (single-target, full sweep, dryRun, missing API key 401, unknown orgId 404, idempotency). **Effort: S (~30 min).** Spec deviation note from task brief: header is `X-API-KEY` not `X-Internal-API-Key` (existing convention per `ApiKeyAuthFilter:21`). No frontend, no migration, cron job untouched. Cycle Count → 54.

### 2026-04-28 — Cycle 54 — Dev: GAP-L-99 fixed in PR #1200 (squash `c1c7afd7`)

- 2026-04-28 SAST — Dev: GAP-L-99 SPEC_READY → FIXED. PR #1200 (`fix(GAP-L-99): manual portal weekly digest trigger`) merged via squash to cycle branch `bugfix_cycle_2026-04-26-day75` at `c1c7afd7`. Implementation per spec: new `PortalDigestInternalController` exposes `POST /internal/portal/digest/run-weekly` gated by existing `ApiKeyAuthFilter` (`X-API-KEY` header — convention deviation from spec brief noted). Optional query params: `orgId` (single-tenant scope), `targetEmail` (single-recipient QA send), `dryRun` (count without sending). Returns `{tenantsProcessed,digestsSent,skipped,dryRun,errors[]}`. `PortalDigestScheduler.runWeeklyDigest()` refactored to add a `RunOptions`-accepting overload returning `RunResult`; the no-arg overload + cron path + existing `PortalDigestSchedulerIntegrationTest` remain byte-identical. New `PortalDigestInternalControllerTest` covers 6 cases (single-target, full sweep, dryRun, missing API key 401, unknown orgId 404, idempotency). 3 files touched (+597/-23): `PortalDigestInternalController.java` (new, 58 LOC), `PortalDigestScheduler.java` (refactor, +185/-23), `PortalDigestInternalControllerTest.java` (new, 377 LOC). Qodana PASS (6m15s); Qodana for JVM neutral/skipped (typical for non-IntelliJ runners); CodeRabbit SUCCESS. **Awaiting cycle-to-main merge + backend restart on main + QA retest of Day 75 §75.1–75.4 before VERIFIED.** Cycle Count → 54.

### 2026-04-28 — Cycle 54 — QA: GAP-L-99 retest on main + §75.1–75.4 walk

- 2026-04-28 SAST — QA: GAP-L-99 FIXED → **VERIFIED** on main `5e9cb58f` (PR #1201 squash-merge). Cycle-54 retest dispatched to `bugfix_cycle_2026-04-26-day75-retest` (cut from main HEAD post PR #1201). Endpoint contract matrix: missing X-API-KEY → 401, invalid X-API-KEY → 401, `dryRun=true` → response `{tenantsProcessed:4,digestsSent:1,skipped:0,dryRun:true,errors:[]}` + Mailpit total unchanged at 4, real send → `{tenantsProcessed:4,digestsSent:1,skipped:0,dryRun:false,errors:[]}` + Mailpit total 4 → 5 with new digest message `XuTUtmtbAK3nvAqcGSge2H` (subject "Mathebula & Partners: Your weekly update"). Day 75 §75.1, §75.2, §75.3 PASS — body greets Sipho, references INV-0001 PAID + 3 info requests + 3 trust transactions, isolation invariant holds (`moroka`/`EST-2026`/`estate`/`25 000` all 0 in text + HTML). §75.4 DEFERRED-ON-ENVIRONMENT — Playwright MCP user-data-dir `/Users/rakheendama/Library/Caches/ms-playwright/mcp-chrome-5d273ba` locked by foreign claude session (PID 4237 etime ~47h+, Chromium PID 25373 alive); identical to GAP-L-98 precedent (orchestrator-resolvable). Routing-level evidence confirmed: magic-link issued (token via `POST /portal/auth/request-link`), Mailpit delivered, `/auth/exchange` + `/home` both 200, digest CTA href = `http://localhost:3002/home`. New informational observation logged: **OBS-Cycle54-PlaywrightLockedByForeignSession** (infra; orchestrator action — sweep stale locks before browser-using QA agent dispatch, or run Playwright MCP `--isolated`). Day 75 now FULLY COMPLETE. QA Position → Day 85 — 85.1 (ready to walk on a fresh `bugfix_cycle_2026-04-26-day85` branch cut from main HEAD after this retest PR merges). Cycle Count remains 54.
