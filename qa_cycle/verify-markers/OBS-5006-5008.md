# Verify Marker: OBS-5006 + OBS-5008

## Build Verification
- `pnpm build`: PASS (exit 0)
- `pnpm test`: 5 pre-existing failures (3 dashboard chart tests, 0 regressions from this fix)
- `pnpm lint`: 1 pre-existing error in dev-login/route.ts (prefer-const), 0 regressions from this fix
- Pre-existing baseline: 3 failed test files / 5 failed tests (dashboard chart-components, completion-progress-bar, sparkline-chart)
- Post-fix: 3 failed test files / 5 failed tests (identical set — no new failures)

## OBS-5006: Invoice PDF preview fails
- **Root cause confirmed**: `fetchMyCapabilities()` call in preview route handler fails when opened in new tab
- **Fix**: Removed redundant capabilities check; backend enforces `@RequiresCapability("INVOICING")`
- **File**: `frontend/app/api/invoices/[id]/preview/route.ts`
- **Status**: MERGED-AWAITING-VERIFY (needs browser verification)

## OBS-5008: Invoice empty state terminology leak
- **Root cause confirmed**: Hardcoded "fee notes" and "matter" in empty-states.json and invoices page.tsx
- **Fix**: Replaced with `{invoices}`, `{invoice}`, `{project}` placeholders + wrapped in `<TerminologyText>`
- **Files**: `frontend/lib/messages/en/empty-states.json`, `frontend/app/(app)/org/[slug]/invoices/page.tsx`
- **Status**: MERGED-AWAITING-VERIFY (needs browser verification)

## Date
2026-05-23
