# Slack tokens — from https://api.slack.com/apps → your app
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-level-token

# No ANTHROPIC_API_KEY needed — uses your local Claude Code auth (Max subscription)

# Path to the repo the agent should operate in (defaults to this mono-repo root)
CLAUDE_CWD=/Users/you/Projects/2026/b2b-strawman

# Permission mode: plan | acceptEdits | bypassPermissions | dontAsk | default
CLAUDE_PERMISSION_MODE=bypassPermissions

# Model alias: sonnet | opus | haiku (or full model ID)
CLAUDE_MODEL=sonnet

# Max agent turns per prompt (defaults to 50)
CLAUDE_MAX_TURNS=50
