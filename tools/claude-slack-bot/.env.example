# Slack tokens — from https://api.slack.com/apps → your app
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-level-token

# Anthropic API key — from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key

# Path to the repo the agent should operate in (defaults to this mono-repo root)
AGENT_CWD=/Users/you/Projects/2026/b2b-strawman

# Agent permission mode: plan | acceptEdits | full
# - plan: read-only (safe, can't write files or run commands)
# - acceptEdits: auto-approves file reads/writes (recommended for mobile)
# - full: auto-approves everything including bash (needed for /epic, /phase)
AGENT_PERMISSION_MODE=bypassPermissions

# Claude model to use (defaults to claude-sonnet-4-6 for speed/cost balance)
AGENT_MODEL=claude-sonnet-4-6

# Max agent turns per prompt (defaults to 50)
AGENT_MAX_TURNS=50
